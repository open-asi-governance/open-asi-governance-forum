<!-- purpose: task 11 design: what should the control-2 coverage gate assert before it is wired into land.py -->
<!-- prompt_sha256: 802e9a3b9da02412131d1238fdf3ef743995722bffb74fe51b2a878a46c665b5 -->
<!-- exit: 0 -->

Decision: build the ratchet, but do not baseline or gate the current detector. First replace its lexical evidence with explicit declarations; then add a narrowly named non-regression gate.

**A. Ratchet or permanent 54%?**

A ratchet is the right safety property, but it provides no progress property. Do not describe green as “control coverage passed.” Name it `negative-control-ratchet`: green means only “no known coverage was lost and no new debt entered.”

Make stagnation visible by:

- Recording and publishing every `NONE` by name, without the current 20-item truncation.
- Printing on every successful landing: `NON-REGRESSION ONLY — N legacy tools remain without a negative control`.
- Recording changes over time, so unchanged debt is visible.
- Requiring every `NONE → HAS` improvement to update the baseline immediately; otherwise the improvement is not locked in.

I would not put decay dates in the landing gate. An expiry converts old debt into an arbitrary future work stoppage and encourages waivers. If the custodian wants progress, establish a separate, explicit reduction schedule. The ratchet must not pretend to supply one.

**B. Escape hatch**

Do not permit ordinary `HAS → NONE` entries under `authorised_regressions`.

Allow only:

1. The tool is withdrawn entirely; or
2. Its assurance signal is genuinely removed, so it becomes `NOT_APPLICABLE` with a reviewed reason.

Both should record authority, ground, prior protection, and what is no longer protected. A tool that continues emitting an assurance signal but loses its negative control is a real regression, not an exemption.

If an emergency waiver is ever necessary, it should be custodian-authorized, expiring, and reported as degraded—not converted into a green baseline. I would omit that mechanism from v1.

**C. Heuristic detector**

The present detector is not fit to become a landing gate. This is stronger than the two historical mistakes: it is wrong now.

Eight results are classified as self-hosted, but [`self_hosted_fixtures()`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:70) searches the entire source—including prose—for words such as `--fixtures` and `must reject`. Consequently:

- `control_coverage.py` counts itself because its detector’s own regex contains `--fixtures`.
- `build_controls_page.py`, `build_viewer.py`, and `test_integrity.py` are counted from generated prose or commentary, not their own fixture runners.

Another 18 of the reported 41 depend on the 600-character proximity heuristic. So the proposed baseline would give temporal force to determinations already known to be unstable.

Replace proximity and prose matching with explicit machine-readable evidence: tool → test suite → negative-case identifier, plus explicit self-hosted fixture declarations. Then version the detector contract.

Yes, the baseline should record `schema_version` and `detector_contract_version`. A version mismatch must refuse and require a reviewed migration diff. Detector corrections belong under `detector_migrations`, not `authorised_regressions`; correcting a false `HAS` to `NONE` is a measurement correction, not a weakening of the tool.

**D. Priority**

This dominates the remaining actionable items—but the next task is detector hardening, not wiring the current check into `land.py`.

- Per-control publication is already done.
- SEO/indexing may improve reach but not record integrity.
- Outreach requires custodian authorization.
- A trustworthy ratchet directly addresses the repository’s dominant failure class and costs no routed round.

**What I would build**

1. Explicit negative-control evidence declarations; eliminate lexical fallback.
2. A complete baseline containing every tool, status, evidence or N/A reason, and detector version.
3. Validated transitions:
   - `NONE → HAS`: allowed and must update the baseline.
   - new tool: only `HAS` or reasoned `NOT_APPLICABLE`.
   - `HAS → NONE`: forbidden.
   - removal or `HAS → NOT_APPLICABLE`: authority and ground required.
4. Tests for every transition, detector-version mismatch, comment/docstring false evidence, and the detector’s self-match.
5. Only then add `negative-control-ratchet` to [`land.py`](/home/reed/git/open-asi-governance-forum/tools/land.py:76), with an explicitly limited success message.

**What I would refuse to build**

- A baseline stamped from the current 41/35/3 determinations.
- The current `--check` wired into `land.py`.
- Permanent, self-authorized `HAS → NONE` exceptions.
- A gate called “coverage” whose green result means only population completeness or non-regression.
- A decay deadline masquerading as a progress policy.

The reusable opportunity is the explicit evidence mapping: once proven for control 2, the same structure could support controls 5, 10, and 53. I would not generalize it until this narrower ratchet survives its own negative controls.
Reading additional input from stdin...
OpenAI Codex v0.147.0
--------
workdir: /home/reed/git/open-asi-governance-forum
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: detailed
session id: 019ff77d-5ddb-7881-bc13-1f9ee9dc8846
--------
user
SHORT DESIGN REVIEW. One decision, and I want the case against it before I build it.

THE STATE

`tools/control_coverage.py` measures control 2 per file: does each tool with an assurance signal
have a case it must fail? Current reading, computed not transcribed:

    HAS_NEGATIVE_CONTROL   41
    NONE                   35
    NOT_APPLICABLE          3   (each with a stated reason)
    total                  79   — 54% of tools with an assurance signal have one

Its `--check` today asserts only that **every tool has a determination over a fully-read
population**. It exits 0. It has been sitting outside `land.py` for two days with the note "its
semantics need settling", because wiring it in as-is would add a tenth green light that certifies
almost nothing — 35 tools have no negative control and the gate would still be green. This
repository's dominant failure class is a green signal not causally downstream of what it certifies,
so adding one deliberately seemed worse than leaving the gate out.

WHAT I PROPOSE TO MAKE IT ASSERT

The guard-identity pattern, which you approved for control 45, applied to coverage:

* A committed baseline records the per-file determination for all 79 tools.
* `--check` refuses if a tool that HAD a negative control now has NONE — a regression.
* `--check` refuses if a NEW tool arrives with an assurance signal and NONE — new debt.
* `--check` refuses if a NOT_APPLICABLE loses its stated reason.
* A deliberate regression is recorded under `authorised_regressions` with a ground, exactly as
  `guard-baseline.json` records authorised removals — so weakening is a governed event and not a
  diff nobody reads.
* It does NOT demand retroactive work on the 35. The debt is frozen, not called in.

QUESTIONS

A. Is a ratchet the right shape, or is it a mechanism for making 54% permanent and calling it
   governance? A number that may not fall is also a number nobody has to raise. What would make
   the difference visible — publishing the 35 by name, a decay date, something else?

B. `authorised_regressions` is an escape hatch I am building on purpose. On the guard baseline it
   has been used once, for a tool that was withdrawn entire. What stops it becoming the normal
   route? Should a regression require the tool to be REMOVED rather than merely excused?

C. The detector underneath is `control_coverage.py`'s own inference — "referenced by
   test_X.py but no refusal is asserted near it" is a heuristic over the AST, and it has already
   been wrong twice this week (a comment counted as coverage; a docstring counted as coverage).
   Wiring a heuristic into a landing gate makes every false positive a work stoppage and every
   false negative a licence. Is that trade worth taking, and should the baseline record the
   detector's VERSION so a detector change is distinguishable from a coverage change?

D. Is there a better gate to spend this slot on? I have four open items and this is the one I
   would do next; say if one of the others dominates. The others: publishing per-control code
   application (done), SEO/indexing research for the repository, and a second outreach round
   that needs the custodian's authorisation anyway.

Answer A–D. Then tell me what you would build, and what you would refuse to build.

**Planning code inspection before design review**
codex
I’ll review the proposed ratchet against the actual detector, its current output, and the control-45 baseline pattern. I’ll keep this to the decision: whether this is the next gate, and the minimum semantics that prevent “54% forever” from masquerading as assurance.
exec
/bin/bash -lc "git branch --show-current && git status --short && rg -n \"control_coverage|guard-baseline|authorised_regressions|control 45|control_45\" tools tests record . --glob '"'!corpus/raw/**'"' --glob '"'!site/**'"'" in /home/reed/git/open-asi-governance-forum
 exited 2 in 0ms:
main
 M record/executive/action-log.jsonl
rg: tests: No such file or directory (os error 2)
./corpus/deficiency-register.md:966:| tools/control_coverage.py <br><sub>Both the report path and --check consult the survey before any number is produced.</sub> | repairable by supersession | verified |
./corpus/deficiency-register.md:981:| tools/tests/test_gate_negative_controls.py <br><sub>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</sub> | repairable by supersession | verified |
./corpus/deficiencies.md:2382:were not a result. A comment does not make a printed number a non-result. `control_coverage.py`
./corpus/deficiencies.md:2438:which is control 45: a repair must still catch what the old gate caught.
./corpus/deficiencies.md:2554:**Recorded because it is the register's own control 45 turned on the register.** A gate was added
tools/tests/test_no_blank_cells.py:37:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
tools/tests/test_closed_world.py:35:#  Two landings overlapped on 2026-08-12 and that is exactly what happened. control_coverage.py
tools/tests/test_closed_world.py:38:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
tools/tests/test_closed_world.py:41:COVERS = ("closed_world.py", "scan_own_code.py", "control_coverage.py")
tools/tests/test_closed_world.py:205:        ("control_coverage.py", ROOT / "tests" / "zzqx_unparseable_probe.py",
tools/tests/test_closed_world.py:234:r = run("control_coverage.py", "--check")
tools/tests/test_closed_world.py:235:check("BASELINE: control_coverage.py --check passes on a clean tree", r.returncode == 0,
tools/tests/test_closed_world.py:239:    r = run("control_coverage.py", "--check")
tools/tests/test_closed_world.py:240:    check("control_coverage.py --check REFUSES on an unreadable population member",
tools/tests/test_fault_injection.py:6:and meant the canonical verifier had no test naming it. tools/control_coverage.py scored the
tools/tests/test_fault_injection.py:70:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
tools/tests/test_deploy_obligations.py:38:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
record/controls/software-implementations.md:190:| 2 — Fault-injection check profile (FICP) | every gate it runs must itself carry a negative control; the coverage is measured by tools/control_coverage.py and is not complete |
tools/tests/test_lease_bounds.py:325:    `control_coverage.executable_text` was built for last night, so it is reused rather than
tools/tests/test_lease_bounds.py:330:    import control_coverage as cc
./tools/tests/test_no_blank_cells.py:37:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
tools/tests/test_publication_gates.py:4:`tools/control_coverage.py` had 40 tools with no case they must fail. These are the ones whose
tools/tests/test_publication_gates.py:26:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
record/controls/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/controls/control-application.md:33:| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey requires every declared member to reach exactly one terminal state and raises otherwise, and TWO population-measuring tools render nothing until it passes, proved by injecting an unreadable file and asserting their own count labels are absent. Both the type and the routing FAILED their first review: the type returned results for four incoherent walks, and both tools printed the counts the guard was meant to withhold. See D-60. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py` `tools/check_quotations.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction's limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
record/controls/control-application.md:74:| 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./tools/tests/test_closed_world.py:35:#  Two landings overlapped on 2026-08-12 and that is exactly what happened. control_coverage.py
./tools/tests/test_closed_world.py:38:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./tools/tests/test_closed_world.py:41:COVERS = ("closed_world.py", "scan_own_code.py", "control_coverage.py")
./tools/tests/test_closed_world.py:205:        ("control_coverage.py", ROOT / "tests" / "zzqx_unparseable_probe.py",
./tools/tests/test_closed_world.py:234:r = run("control_coverage.py", "--check")
./tools/tests/test_closed_world.py:235:check("BASELINE: control_coverage.py --check passes on a clean tree", r.returncode == 0,
./tools/tests/test_closed_world.py:239:    r = run("control_coverage.py", "--check")
./tools/tests/test_closed_world.py:240:    check("control_coverage.py --check REFUSES on an unreadable population member",
tools/tests/test_chain_guards.py:4:`tools/control_coverage.py` found 43 of 69 tools with no case they must fail. These three were
tools/tests/test_chain_guards.py:25:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./tools/tests/test_fault_injection.py:6:and meant the canonical verifier had no test naming it. tools/control_coverage.py scored the
./tools/tests/test_fault_injection.py:70:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
tools/tests/test_gate_refusals.py:4:`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
tools/tests/test_gate_refusals.py:53:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
tools/tests/test_gate_refusals.py:59:          "control_coverage.py")
tools/tests/test_gate_refusals.py:247:        #  The label names the tool. control_coverage.py looks for a refusal assertion within
tools/tests/test_gate_refusals.py:291:#  prose, so the exclusion could not rot. `control_coverage.py` matches a refusal word within 600
tools/tests/test_gate_refusals.py:300:coverage = load("control_coverage")
./tools/tests/test_deploy_obligations.py:38:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./tools/tests/test_lease_bounds.py:325:    `control_coverage.executable_text` was built for last night, so it is reused rather than
./tools/tests/test_lease_bounds.py:330:    import control_coverage as cc
tools/tests/test_executive_log.py:244:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
./tools/tests/test_publication_gates.py:4:`tools/control_coverage.py` had 40 tools with no case they must fail. These are the ones whose
./tools/tests/test_publication_gates.py:26:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./tools/tests/test_chain_guards.py:4:`tools/control_coverage.py` found 43 of 69 tools with no case they must fail. These three were
./tools/tests/test_chain_guards.py:25:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
record/executive/COMPACTION-NOTE-2026-08-12.md:135:4. `control_coverage.py --check` still out of `land.py` — its semantics need settling.
tools/tests/test_guard_identity.py:19:which is control 45's requirement, applied to the guard rather than to the gate as a whole.
tools/tests/test_guard_identity.py:35:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
tools/tests/test_guard_identity.py:160:        check("...and the refusal cites control 45 by what it forbids",
tools/tests/test_guard_identity.py:161:              any("control 45" in f for f in found), str(found))
tools/tests/test_guard_identity.py:196:        check("...and the refusal names control 45 and what it forbids",
tools/tests/test_guard_identity.py:197:              any("control 45" in f for f in found), str(found))
./tools/tests/test_gate_refusals.py:4:`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
./tools/tests/test_gate_refusals.py:53:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./tools/tests/test_gate_refusals.py:59:          "control_coverage.py")
./tools/tests/test_gate_refusals.py:247:        #  The label names the tool. control_coverage.py looks for a refusal assertion within
./tools/tests/test_gate_refusals.py:291:#  prose, so the exclusion could not rot. `control_coverage.py` matches a refusal word within 600
./tools/tests/test_gate_refusals.py:300:coverage = load("control_coverage")
./tools/tests/test_executive_log.py:244:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
tools/tests/test_gate_negative_controls.py:4:`tools/control_coverage.py` found 44 tools with no case they must fail. These four were taken
tools/tests/test_gate_negative_controls.py:39:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
tools/tests/test_gate_negative_controls.py:293:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
tools/tests/test_derive_counts.py:22:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./tools/tests/test_guard_identity.py:19:which is control 45's requirement, applied to the guard rather than to the gate as a whole.
./tools/tests/test_guard_identity.py:35:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./tools/tests/test_guard_identity.py:160:        check("...and the refusal cites control 45 by what it forbids",
./tools/tests/test_guard_identity.py:161:              any("control 45" in f for f in found), str(found))
./tools/tests/test_guard_identity.py:196:        check("...and the refusal names control 45 and what it forbids",
./tools/tests/test_guard_identity.py:197:              any("control 45" in f for f in found), str(found))
tools/tests/test_cohort_pages.py:178:#  mention-is-not-an-exercise defect this repository already fixed in control_coverage.py, in a
tools/control_coverage.py:4:    python3 tools/control_coverage.py            # the matrix
tools/control_coverage.py:5:    python3 tools/control_coverage.py --check    # refuse if any tool has no determination
./tools/tests/test_gate_negative_controls.py:4:`tools/control_coverage.py` found 44 tools with no case they must fail. These four were taken
./tools/tests/test_gate_negative_controls.py:39:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./tools/tests/test_gate_negative_controls.py:293:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
tools/land.py:99:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
tools/check_claims.py:269:    """This tool's own negative control (control 2), on four populations (control 45):
tools/scan_own_code.py:208:        #  older behaviour unprotected — control 45 applied to a detector rather than a gate.
./tools/tests/test_derive_counts.py:22:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:74:Build the narrow regression now, but do not mint another control. D-63 is already an instance of control 45: a gate change silently changed neighboring semantics.
record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:947:+**Recorded because it is the register's own control 45 turned on the register.** A gate was added
record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:3609:  2535	**Recorded because it is the register's own control 45 turned on the register.** A gate was added
record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:3855:Build the narrow regression now, but do not mint another control. D-63 is already an instance of control 45: a gate change silently changed neighboring semantics.
tools/guards.py:40:The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
tools/guards.py:52:  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
tools/guards.py:138:    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
tools/guards.py:201:BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"
tools/guards.py:214:    weakening a governed event rather than a diff nobody reads. That is the property control 45
tools/guards.py:248:    #  and removing both the guard and its fixture is exactly the rewrite control 45 governs.
tools/guards.py:256:                       f"Removing a guard and its fixture together is the weakening control 45 "
tools/guards.py:267:                       f"the guard was removed in a rewrite — which is what control 45 forbids "
./tools/tests/test_cohort_pages.py:178:#  mention-is-not-an-exercise defect this repository already fixed in control_coverage.py, in a
record/claims/dispositions.jsonl:67:{"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
record/claims/dispositions.jsonl:92:{"by": "workbench", "disposition": "labelled", "id": "f59dae53febd7a03", "note": "Nonclaim. Completeness over assurance signals is not established because nothing enumerates the set of assurance signals this project emits; tools/control_coverage.py measures negative-control coverage over TOOLS, which is a different and smaller population.", "utc": "2026-08-12"}
./tools/land.py:99:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
tools/control_application.py:113:                "tools/control_coverage.py", "tools/scan_own_code.py"),
tools/control_application.py:118:         gap="Run tools/control_coverage.py for the current list and the current rate — the "
tools/control_application.py:155:                "tools/control_coverage.py", "tools/validate_provenance.py"),
tools/control_application.py:396:          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
tools/control_application.py:402:              "RAISES, and record/executive/guard-baseline.json holds the codes this repository "
tools/control_application.py:416:          files=("tools/control_coverage.py", "tools/gate_health.py"),
tools/software_implementations.py:53:deploy interlock (control 23) and guard identity (control 45), which are two of this project's
tools/software_implementations.py:363:       "tools/control_coverage.py and is not complete"),
./tools/control_application.py:113:                "tools/control_coverage.py", "tools/scan_own_code.py"),
./tools/control_application.py:118:         gap="Run tools/control_coverage.py for the current list and the current rate — the "
./tools/control_application.py:155:                "tools/control_coverage.py", "tools/validate_provenance.py"),
./tools/control_application.py:396:          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
./tools/control_application.py:402:              "RAISES, and record/executive/guard-baseline.json holds the codes this repository "
./tools/control_application.py:416:          files=("tools/control_coverage.py", "tools/gate_health.py"),
tools/control_findings.py:208:  "detector", "6bdbbc9; found by tools/control_coverage.py"),
tools/control_findings.py:263:  "control_coverage.py, which MEASURES control 2, had both errors at once. It counted tools "
tools/control_findings.py:292:  "counts the guard was withholding, and control_coverage --check — the path wired into landing "
tools/control_findings.py:310:  "MISSING negative control, and become control 45 evidence only when a replacement is tested "
./tools/control_findings.py:208:  "detector", "6bdbbc9; found by tools/control_coverage.py"),
./tools/control_findings.py:263:  "control_coverage.py, which MEASURES control 2, had both errors at once. It counted tools "
./tools/control_findings.py:292:  "counts the guard was withholding, and control_coverage --check — the path wired into landing "
./tools/control_findings.py:310:  "MISSING negative control, and become control 45 evidence only when a replacement is tested "
./record/claims/dispositions.jsonl:67:{"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
./record/claims/dispositions.jsonl:92:{"by": "workbench", "disposition": "labelled", "id": "f59dae53febd7a03", "note": "Nonclaim. Completeness over assurance signals is not established because nothing enumerates the set of assurance signals this project emits; tools/control_coverage.py measures negative-control coverage over TOOLS, which is a different and smaller population.", "utc": "2026-08-12"}
./record/executive/COMPACTION-NOTE-2026-08-12.md:135:4. `control_coverage.py --check` still out of `land.py` — its semantics need settling.
./record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:74:Build the narrow regression now, but do not mint another control. D-63 is already an instance of control 45: a gate change silently changed neighboring semantics.
./record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:947:+**Recorded because it is the register's own control 45 turned on the register.** A gate was added
./record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:3609:  2535	**Recorded because it is the register's own control 45 turned on the register.** A gate was added
./record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md:3855:Build the narrow regression now, but do not mint another control. D-63 is already an instance of control 45: a gate change silently changed neighboring semantics.
./record/controls/software-implementations.md:190:| 2 — Fault-injection check profile (FICP) | every gate it runs must itself carry a negative control; the coverage is measured by tools/control_coverage.py and is not complete |
./tools/control_coverage.py:4:    python3 tools/control_coverage.py            # the matrix
./tools/control_coverage.py:5:    python3 tools/control_coverage.py --check    # refuse if any tool has no determination
./record/controls/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/controls/control-application.md:33:| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey requires every declared member to reach exactly one terminal state and raises otherwise, and TWO population-measuring tools render nothing until it passes, proved by injecting an unreadable file and asserting their own count labels are absent. Both the type and the routing FAILED their first review: the type returned results for four incoherent walks, and both tools printed the counts the guard was meant to withhold. See D-60. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
./record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py` `tools/check_quotations.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction's limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
./record/controls/control-application.md:74:| 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./tools/scan_own_code.py:208:        #  older behaviour unprotected — control 45 applied to a detector rather than a gate.
./tools/check_claims.py:269:    """This tool's own negative control (control 2), on four populations (control 45):
./tools/guards.py:40:The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
./tools/guards.py:52:  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
./tools/guards.py:138:    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
./tools/guards.py:201:BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"
./tools/guards.py:214:    weakening a governed event rather than a diff nobody reads. That is the property control 45
./tools/guards.py:248:    #  and removing both the guard and its fixture is exactly the rewrite control 45 governs.
./tools/guards.py:256:                       f"Removing a guard and its fixture together is the weakening control 45 "
./tools/guards.py:267:                       f"the guard was removed in a rewrite — which is what control 45 forbids "
./tools/software_implementations.py:53:deploy interlock (control 23) and guard identity (control 45), which are two of this project's
./tools/software_implementations.py:363:       "tools/control_coverage.py and is not complete"),
./corpus/artifacts/deficiency-register.json:2460:          "object": "tools/control_coverage.py",
./corpus/artifacts/deficiency-register.json:2503:          "note": "Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern."
./record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:85:The regression suite should retain four populations, following control 45:
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:2154:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4910:The regression suite should retain four populations, following control 45:
record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/HANDOFF-2026-08-12.md:49:| C2 | `control_coverage.py` | counted a tool named in a COMMENT; missed one driven through a wrapper |
record/executive/HANDOFF-2026-08-12.md:87:| `tools/guards.py` | control 45. Guards carry codes, fixtures ask by name, and removing a guard is a governed event against a committed baseline |
record/executive/HANDOFF-2026-08-12.md:135:* **`control_coverage.py --check` is still not in `land.py`.** It no longer passes vacuously — it
record/executive/HANDOFF-2026-08-12.md:186:`record/executive/guard-baseline.json` — the guards this repository has committed to keeping.
record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/action-log.jsonl:273:{"utc": "2026-08-12T05:42:08Z", "action": "codex_invoke", "claim": {"purpose": "review batch four: control 45 mechanised as guard identity", "override": null, "prompt_sha256": "452f8b847067a7eb8f68e06f48ec778ea0c4073a110a10048197eb8c545a0eec", "prompt_chars": 4291, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8c078e33d6df", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 2.0, "resets_at": "2026-08-12T09:40:00.182098+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.182121+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "73f03f31177e1fb0bd348537ed0b2fc318f0258cca7d22b22789337ab08f7d86"}
record/executive/action-log.jsonl:274:{"utc": "2026-08-12T05:46:51Z", "action": "codex_return_captured", "claim": {"purpose": "review batch four: control 45 mechanised as guard identity", "prompt_sha256": "452f8b847067a7eb8f68e06f48ec778ea0c4073a110a10048197eb8c545a0eec", "exit_status": 0, "output_chars": 370987, "output_sha256": "f88f795695aaca6fc3db88391279964d23f802a5b84cdeef564e1d276750037b", "transcript": "record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T054208Z-452f8b847067.md", "actor": "claude-code", "head": "8c078e33d6df", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 2.0, "resets_at": "2026-08-12T09:40:00.182098+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.182121+00:00"}, "cached": true, "age_seconds": 281}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "e6c4effa6f58742129be36b0b5fcc0247239e6fc93b0a707837f6d378eaa671e"}
./record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:85:The regression suite should retain four populations, following control 45:
./record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:2154:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4910:The regression suite should retain four populations, following control 45:
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:401:./docs/deficiencies-10.html:107:<article class="e" data-actor="actor_repository_annotator_invocation" data-control="prospective_control_implemented_not_validated"><h3><a href="artifacts/deficiencies.md">D-58 — The context gate read &quot;not measurable here&quot; as &quot;measured and bad&quot;, and silently stopped publishing for eight commits</a><span class="tag w">implemented, not validated</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> the annotator, 2026-08-12 · <code>checking whether the previous landing&#x27;s Pages deploy had actually happened, after landing a handoff document. No gate reported it; land.py&#x27;s own deploy attestations had recorded six consecutive failures and none had been read.</code></p><p class="meta"><b>Forward control:</b> An observed failed postcondition opens a durable incident that refuses the next ordinary landing until resolved against evidence. Implemented in tools/deploy_obligations.py and enforced in tools/land.py. It constrains the sanctioned path only; it does not constrain an operator who edits it.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/check_executive_context.py<br><small>Four states with the exit code carrying them, precedence 2&gt;1&gt;3&gt;0, and each pin declaring its locator kind. Verified in the field: the CI run for db80307 reported exit 3 with oagf-CLAUDE.md measured repo-relative and the two operator paths UNAVAILABLE, which is the shape that previously failed.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>Its first version asserted returncode == 0 as a baseline that only one machine satisfies, and != 0 as drift, which exit 3 would now also satisfy. Rebuilt on injected pins directories asserting exact codes.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/land.py<br><small>The gate-admission policy is a named, tested predicate admitting only zero. The control 23 interlock is applied: an undischarged deployment obligation refuses an ordinary landing, --no-deploy-check is unavailable while one is open, and a resolved incident id is not a skeleton key. NOT VERIFIED IN THE FIELD: 40 fixture cases pass, and no real deploy failure has yet been driven through the live path.</small></td><td>partly repairable</td><td><span class="tag w">applied, not verified</span></td></tr><tr><td>tools/gate_health.py<br><small>Counted every non-success deploy as UNOBSERVED, collapsing &#x27;nobody waited&#x27; with &#x27;waited and it failed&#x27;. Now three states plus a consecutive-trailing-failure count, which reads 6.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/deploy_obligations.py<br><small>New. The obligation ledger that supplies control 23&#x27;s containment behaviour. Bootstrapped from the six historical failures rather than starting at its own installation. Not verified in the field.</small></td><td>repairable by supersession</td><td><span class="tag w">applied, not verified</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-59 — Two guards in the compliance matrices could never fire, and one of them was described on the published page as if it did</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>the unreachable guard surfaced while writing the control-2 fixture for the tool containing it; the advertised-but-absent guard was found by external review injecting the exact inputs the published page claimed were refused</code></p><p class="meta"><b>Forward control:</b> A closed-world inventory of published coverage matrices: an artifact type, a requirement that every such artifact registers, and a completeness validator run over the registry. Guarding two enumerated artifacts cannot make the control hold while a fourth can be added without notice — which is control 19&#x27;s error committed while implementing control 44.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/control_application.py<br><small>The unreachable guard now keys on the declaration rather than on completeness, and the reason-quality check the C44 row credited it with now exists. Both are exercised by injected faults.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_no_blank_cells.py<br><small>New. Injects every shape control 44 names on BOTH matrices, with baselines, and regression-tests that the page&#x27;s own history paragraph survives whether or not any row is ticked.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>record/findings/2026-08-08-search-capability-matrix.json<br><small>A third published matrix that nothing checks; its first two rows omit a column the last two carry. Unremediated, and the reason control 44 is recorded as a gap rather than done.</small></td><td>partly repairable</td><td><span class="tag d">not started</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_implemented_not_validated"><h3><a href="artifacts/deficiencies.md">D-60 — The type built to enforce closed-world measurement did not enforce it, and both tools routed through it printed the counts it was withholding</a><span class="tag w">implemented, not validated</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>external review reproduced four passing surveys that were incoherent, and read what the two routed tools printed before consulting the guard</code></p><p class="meta"><b>Forward control:</b> Closed-world measurement as a type whose result is unreachable past an incomplete or incoherent walk. Routing remains per-tool and by hand; nothing detects a new tool that globs a directory and prints a count, and no type catches a population defined too narrowly.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/closed_world.py<br><small>Per-key terminal states; misuse raises where it happens; counts with nothing accounted refuse; an exclusion ground is stripped before it is measured.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/scan_own_code.py<br><small>Renders nothing until the guard passes, and parses once rather than once per detector.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/control_coverage.py<br><small>Both the report path and --check consult the survey before any number is produced.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_closed_world.py<br><small>Its integration arms asserted the absence of a phrase removed in the same edit, so they forbade nothing. They now name the tools&#x27; actual count labels.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article><article class="e" data-actor="actor_repository_annotator_invocation" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-61 — The gate against fabricated party quotations could not see a quotation that wraps, so it checked none</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> the annotator, 2026-08-12 · <code>reading check_quotations.py while enrolling it in the guard registry; measured 235 candidates, 1 attributed, 0 checked across the 98 files it scans</code></p><p class="meta"><b>Forward control:</b> Nothing establishes that a negative control injects the fault in the form that OCCURS rather than the form that was imagined. A fixture corpus drawn from the record&#x27;s real shapes — wrapped prose, parenthetical provenance, blockquotes — would be the mechanism; reading the gate is not one.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/check_quotations.py<br><small>Patterns admit a single newline and refuse a blank line. 350 candidates and 4 attributions after the repair; the verbatim party amendment in the pinned instructions is checked for the first time. A literal `if True:` wrapping the whole loop was also removed.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/scan_own_code.py<br><small>D-B matched a name bound to a bool and not a bool literal, so `if True:` was invisible on the real file and on a direct fixture. The literal is now the must-flag fixture.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-62 — A negative control corrupted the spend ledger 87 times and passed while doing it</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>external review read what the fixture did to the artifact rather than what it asserted; 87 of 141 ledger entries were the fixture</code></p><p class="meta"><b>Forward control:</b> Nothing requires a negative control to observe the tool&#x27;s SIDE EFFECTS rather than its output. A fixture that asserts on stdout alone cannot distinguish a refusal from an append. The general form is a fixture harness that snapshots every artifact a tool may write and requires the refusal arm to leave them unchanged.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>record/cycles/spend-ledger.json<br><small>87 fabricated rows removed; the rows and the pre-correction hash are preserved in record/cycles/spend-ledger-correction-2026-08-12.md. No dollar or token total was distorted; every count of entries was.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/record_spend.py<br><small>Refuses a cohort not in solicited_cohorts() before appending anything, under guard RS-01.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>The arm required only that no cost was printed, which an append satisfies. It now requires a non-zero exit, names RS-01, and asserts the ledger is byte-identical afterwards.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article>
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:961:| tools/control_coverage.py <br><sub>Both the report path and --check consult the survey before any number is produced.</sub> | repairable by supersession | verified |
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:976:| tools/tests/test_gate_negative_controls.py <br><sub>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</sub> | repairable by supersession | verified |
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2097:`tools/control_coverage.py` found 44 tools with no case they must fail. These four were taken
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2132:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2458:record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2512:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2488:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2514:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2551:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2517:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:5003:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2522:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6640:589263 node /home/reed/.config/nvm/versions/node/v24.4.1/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2523:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6641:589270 /home/reed/.config/nvm/versions/node/v24.4.1/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2549:record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:1420:record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2550:record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:2072:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:3250:| 2 — Fault-injection check profile (FICP) | every gate it runs must itself carry a negative control; the coverage is measured by tools/control_coverage.py and is not complete |
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:3393:    `control_coverage.executable_text` was built for last night, so it is reused rather than
./record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:3398:    import control_coverage as cc
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1:<!-- purpose: review batch four: control 45 mechanised as guard identity -->
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:130:# Review before landing: batch four, control 45 by way of guard identity
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:139:because it is the same requirement one level finer: control 45 says a modified gate must still
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:153:and an ORPHANED expectation (named by a fixture, declared nowhere) fails — which is control 45
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:248:-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:249:+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:250: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:270:-          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:276:+          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:293:           files=("tools/control_coverage.py", "tools/gate_health.py"),
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:450:+    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:626:The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:638:  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:717:    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:774:                       f"the guard was removed in a rewrite — which is what control 45 forbids "
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:827:which is control 45's requirement, applied to the guard rather than to the gate as a whole.
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:842:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:953:        check("...and the refusal cites control 45 by what it forbids",
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:954:              any("control 45" in f for f in found), str(found))
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1040:    40	The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1052:    52	  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1131:   131	    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1188:   188	                       f"the guard was removed in a rewrite — which is what control 45 forbids "
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1241:    19	which is control 45's requirement, applied to the guard rather than to the gate as a whole.
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1256:    34	#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1367:   145	        check("...and the refusal cites control 45 by what it forbids",
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1368:   146	              any("control 45" in f for f in found), str(found))
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1616:tools/tests/test_closed_world.py:229:check("BASELINE: control_coverage.py --check passes on a clean tree", r.returncode == 0,
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1617:tools/tests/test_closed_world.py:234:    check("control_coverage.py --check REFUSES on an unreadable population member",
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1655:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1659:docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1676:record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:923:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering. |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1678:record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:169: | 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1680:record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2323:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1685:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1793:rg -n \"Control 45|control 45|C45|rank.?45| 45:\" record docs tools | head -80
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1704:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1944:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1705:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1945:docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1706:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1947:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1739:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1759:        check("...and the refusal cites control 45 by what it forbids",
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1760:              any("control 45" in f for f in found), str(found))
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1988:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2133:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2468:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2469:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2470:  [32m✓[0m ...and control_coverage.py names the file it could not read
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2471:  [32m✓[0m ...and control_coverage.py publishes NO count from the partial walk
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2472:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2473:  [32m✓[0m BASELINE: control_coverage.py --check passes on a clean tree
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2474:  [32m✓[0m control_coverage.py --check REFUSES on an unreadable population member
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3052:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3672:-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3673:+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3674: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3685:-<tr><td>45</td><td>A replacement gate must catch what the old gate caught</td><td>☐</td><td><code>tools/control_coverage.py</code> <code>tools/executive_log.py</code>&lt;br&gt;tested by <code>tools/tests/test_deploy_check.py</code> <code>tools/tests/test_gate_negative_controls.py</code>&lt;br&gt;<strong>Remaining:</strong> Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor&#x27;s cases, so the next gate change depends on somebody remembering.&lt;br&gt;&lt;small&gt;self-application: VIOLATED&lt;/small&gt;</td></tr>
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3686:+<tr><td>45</td><td>A replacement gate must catch what the old gate caught</td><td>☐</td><td><code>tools/guards.py</code> <code>tools/control_coverage.py</code> <code>tools/executive_log.py</code>&lt;br&gt;tested by <code>tools/tests/test_guard_identity.py</code> <code>tools/tests/test_deploy_check.py</code> <code>tools/tests/test_no_blank_cells.py</code>&lt;br&gt;<strong>Remaining:</strong> Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control&#x27;s requirement, applied at the guard rather than at the gate. <code>guards.py --check</code> is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool&#x27;s guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.&lt;br&gt;*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.&lt;br&gt;&lt;small&gt;self-application: VIOLATED&lt;/small&gt;</td></tr>
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3687: <tr><td>46</td><td>Drift is measured against the original baseline</td><td>☐</td><td><code>tools/control_coverage.py</code> <code>tools/gate_health.py</code>&lt;br&gt;tested by <code>tools/tests/test_gate_negative_controls.py</code>&lt;br&gt;<strong>Remaining:</strong> The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.&lt;br&gt;&lt;small&gt;self-application: UNRESOLVED&lt;/small&gt;</td></tr>
record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3772:+{"utc": "2026-08-12T05:42:08Z", "action": "codex_invoke", "claim": {"purpose": "review batch four: control 45 mechanised as guard identity", "override": null, "prompt_sha256": "452f8b847067a7eb8f68e06f48ec778ea0c4073a110a10048197eb8c545a0eec", "prompt_chars": 4291, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8c078e33d6df", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 2.0, "resets_at": "2026-08-12T09:40:00.182098+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.182121+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "73f03f31177e1fb0bd348537ed0b2fc318f0258cca7d22b22789337ab08f7d86"}
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:209:                 "tools/control_coverage.py", "tools/validate_provenance.py"),
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:1146:    `control_coverage.executable_text` was built for last night, so it is reused rather than
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:1151:    import control_coverage as cc
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2279:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2488:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2551:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2918:   323	    `control_coverage.executable_text` was built for last night, so it is reused rather than
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2923:   328	    import control_coverage as cc
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3315:    99	    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3817:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3818:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3819:  [32m✓[0m ...and control_coverage.py names the file it could not read
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3820:  [32m✓[0m ...and control_coverage.py publishes NO count from the partial walk
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3821:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3822:  [32m✓[0m BASELINE: control_coverage.py --check passes on a clean tree
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3823:  [32m✓[0m control_coverage.py --check REFUSES on an unreadable population member
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:4421:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:4425:  [32m✓[0m ...and the refusal names control 45 and what it forbids
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:5003:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6029:tools/control_coverage.py:162:    call to `state()`, not to the constant.
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6467:cb9b7d7 control 45 mechanised: guards carry names, and removing one is a governed event
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6640:589263 node /home/reed/.config/nvm/versions/node/v24.4.1/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6641:589270 /home/reed/.config/nvm/versions/node/v24.4.1/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6876:   323	    `control_coverage.executable_text` was built for last night, so it is reused rather than
record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6995:   155	                "tools/control_coverage.py", "tools/validate_provenance.py"),
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:82:per control 45. D-61 filed.
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:199: M record/executive/guard-baseline.json
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:288: record/executive/guard-baseline.json       | 24 +++++----
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:303:-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and the codes this repository has committed to keeping are recorded in record/executive/guard-baseline.json. Removing a guard AND its fixture together — the shape an actual rewrite takes — is refused unless the removal is recorded with a ground, which makes weakening a governed event rather than a diff nobody reads. THE FIRST VERSION HAD ONLY THE PRESENT-TIME HALF and Codex reproduced the silent case: baseline clean, delete both, still clean. WHAT REMAINS: exactly ONE tool is enrolled — eleven guards in control_application.py. Every other gate here has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:304:+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py` `tools/check_quotations.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction's limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:305: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:376:diff --git a/record/executive/guard-baseline.json b/record/executive/guard-baseline.json
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:378:--- a/record/executive/guard-baseline.json
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:379:+++ b/record/executive/guard-baseline.json
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:610:+          "note": "Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern."
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:680:+which is control 45: a repair must still catch what the old gate caught.
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:732: | tools/control_coverage.py <br><sub>Both the report path and --check consult the survey before any number is produced.</sub> | repairable by supersession | verified |
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:747:+| tools/tests/test_gate_negative_controls.py <br><sub>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</sub> | repairable by supersession | verified |
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:868:-          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:869:+          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:877:-              "record/executive/guard-baseline.json. Removing a guard AND its fixture together "
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:885:+              "RAISES, and record/executive/guard-baseline.json holds the codes this repository "
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1533:The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1545:  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1631:    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1694:BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1707:    weakening a governed event rather than a diff nobody reads. That is the property control 45
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1767:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:2874:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:3985:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:3989:  [32m✓[0m ...and the refusal names control 45 and what it forbids
./record/executive/codex-transcripts/20260812T135717Z-514f23b2b456.md:624:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T135717Z-514f23b2b456.md:1876:cb9b7d7 control 45 mechanised: guards carry names, and removing one is a governed event
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:335:tools/control_coverage.py:18:    C2   negative control            YES — a tool either has a case it must fail, or it does not
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:336:tools/control_coverage.py:28:a raise, or a must-reject fixture. It does **not** mean the negative control is a good one, that it
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:337:tools/control_coverage.py:71:    in tools/tests/ and because the test still references the pre-rename alias. A negative control
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:338:tools/control_coverage.py:72:    that ships with the tool is a better negative control, not an absent one. Measuring only one
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:339:tools/control_coverage.py:157:    print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:420:tools/check_claims.py:257:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:654:`tools/control_coverage.py` found 44 tools with no case they must fail. These four were taken
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:744:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2280:tools/control_coverage.py:21:    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2281:tools/control_coverage.py:168:    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2728:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3755:* **#11** — wiring `control_coverage.py --check` into `land.py`. **Deliberately gated**: at 46%
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3762:  tools/control_coverage.py` lists them. Take gate-like tools and number-producers first; page
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3922:  "detector", "6bdbbc9; found by tools/control_coverage.py"),
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:4185:    94	#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./docs/deficiencies-10.html:107:<article class="e" data-actor="actor_repository_annotator_invocation" data-control="prospective_control_implemented_not_validated"><h3><a href="artifacts/deficiencies.md">D-58 — The context gate read &quot;not measurable here&quot; as &quot;measured and bad&quot;, and silently stopped publishing for eight commits</a><span class="tag w">implemented, not validated</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> the annotator, 2026-08-12 · <code>checking whether the previous landing&#x27;s Pages deploy had actually happened, after landing a handoff document. No gate reported it; land.py&#x27;s own deploy attestations had recorded six consecutive failures and none had been read.</code></p><p class="meta"><b>Forward control:</b> An observed failed postcondition opens a durable incident that refuses the next ordinary landing until resolved against evidence. Implemented in tools/deploy_obligations.py and enforced in tools/land.py. It constrains the sanctioned path only; it does not constrain an operator who edits it.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/check_executive_context.py<br><small>Four states with the exit code carrying them, precedence 2&gt;1&gt;3&gt;0, and each pin declaring its locator kind. Verified in the field: the CI run for db80307 reported exit 3 with oagf-CLAUDE.md measured repo-relative and the two operator paths UNAVAILABLE, which is the shape that previously failed.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>Its first version asserted returncode == 0 as a baseline that only one machine satisfies, and != 0 as drift, which exit 3 would now also satisfy. Rebuilt on injected pins directories asserting exact codes.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/land.py<br><small>The gate-admission policy is a named, tested predicate admitting only zero. The control 23 interlock is applied: an undischarged deployment obligation refuses an ordinary landing, --no-deploy-check is unavailable while one is open, and a resolved incident id is not a skeleton key. NOT VERIFIED IN THE FIELD: 40 fixture cases pass, and no real deploy failure has yet been driven through the live path.</small></td><td>partly repairable</td><td><span class="tag w">applied, not verified</span></td></tr><tr><td>tools/gate_health.py<br><small>Counted every non-success deploy as UNOBSERVED, collapsing &#x27;nobody waited&#x27; with &#x27;waited and it failed&#x27;. Now three states plus a consecutive-trailing-failure count, which reads 6.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/deploy_obligations.py<br><small>New. The obligation ledger that supplies control 23&#x27;s containment behaviour. Bootstrapped from the six historical failures rather than starting at its own installation. Not verified in the field.</small></td><td>repairable by supersession</td><td><span class="tag w">applied, not verified</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-59 — Two guards in the compliance matrices could never fire, and one of them was described on the published page as if it did</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>the unreachable guard surfaced while writing the control-2 fixture for the tool containing it; the advertised-but-absent guard was found by external review injecting the exact inputs the published page claimed were refused</code></p><p class="meta"><b>Forward control:</b> A closed-world inventory of published coverage matrices: an artifact type, a requirement that every such artifact registers, and a completeness validator run over the registry. Guarding two enumerated artifacts cannot make the control hold while a fourth can be added without notice — which is control 19&#x27;s error committed while implementing control 44.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/control_application.py<br><small>The unreachable guard now keys on the declaration rather than on completeness, and the reason-quality check the C44 row credited it with now exists. Both are exercised by injected faults.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_no_blank_cells.py<br><small>New. Injects every shape control 44 names on BOTH matrices, with baselines, and regression-tests that the page&#x27;s own history paragraph survives whether or not any row is ticked.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>record/findings/2026-08-08-search-capability-matrix.json<br><small>A third published matrix that nothing checks; its first two rows omit a column the last two carry. Unremediated, and the reason control 44 is recorded as a gap rather than done.</small></td><td>partly repairable</td><td><span class="tag d">not started</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_implemented_not_validated"><h3><a href="artifacts/deficiencies.md">D-60 — The type built to enforce closed-world measurement did not enforce it, and both tools routed through it printed the counts it was withholding</a><span class="tag w">implemented, not validated</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>external review reproduced four passing surveys that were incoherent, and read what the two routed tools printed before consulting the guard</code></p><p class="meta"><b>Forward control:</b> Closed-world measurement as a type whose result is unreachable past an incomplete or incoherent walk. Routing remains per-tool and by hand; nothing detects a new tool that globs a directory and prints a count, and no type catches a population defined too narrowly.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/closed_world.py<br><small>Per-key terminal states; misuse raises where it happens; counts with nothing accounted refuse; an exclusion ground is stripped before it is measured.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/scan_own_code.py<br><small>Renders nothing until the guard passes, and parses once rather than once per detector.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/control_coverage.py<br><small>Both the report path and --check consult the survey before any number is produced.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_closed_world.py<br><small>Its integration arms asserted the absence of a phrase removed in the same edit, so they forbade nothing. They now name the tools&#x27; actual count labels.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article><article class="e" data-actor="actor_repository_annotator_invocation" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-61 — The gate against fabricated party quotations could not see a quotation that wraps, so it checked none</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> the annotator, 2026-08-12 · <code>reading check_quotations.py while enrolling it in the guard registry; measured 235 candidates, 1 attributed, 0 checked across the 98 files it scans</code></p><p class="meta"><b>Forward control:</b> Nothing establishes that a negative control injects the fault in the form that OCCURS rather than the form that was imagined. A fixture corpus drawn from the record&#x27;s real shapes — wrapped prose, parenthetical provenance, blockquotes — would be the mechanism; reading the gate is not one.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/check_quotations.py<br><small>Patterns admit a single newline and refuse a blank line. 350 candidates and 4 attributions after the repair; the verbatim party amendment in the pinned instructions is checked for the first time. A literal `if True:` wrapping the whole loop was also removed.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/scan_own_code.py<br><small>D-B matched a name bound to a bool and not a bool literal, so `if True:` was invisible on the real file and on a direct fixture. The literal is now the must-flag fixture.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-62 — A negative control corrupted the spend ledger 87 times and passed while doing it</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>external review read what the fixture did to the artifact rather than what it asserted; 87 of 141 ledger entries were the fixture</code></p><p class="meta"><b>Forward control:</b> Nothing requires a negative control to observe the tool&#x27;s SIDE EFFECTS rather than its output. A fixture that asserts on stdout alone cannot distinguish a refusal from an append. The general form is a fixture harness that snapshots every artifact a tool may write and requires the refusal arm to leave them unchanged.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>record/cycles/spend-ledger.json<br><small>87 fabricated rows removed; the rows and the pre-correction hash are preserved in record/cycles/spend-ledger-correction-2026-08-12.md. No dollar or token total was distorted; every count of entries was.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/record_spend.py<br><small>Refuses a cohort not in solicited_cohorts() before appending anything, under guard RS-01.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>The arm required only that no cost was printed, which an append satisfies. It now requires a non-zero exit, names RS-01, and asserts the ledger is byte-identical afterwards.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article>
record/executive/codex-transcripts/20260812T135717Z-514f23b2b456.md:624:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T135717Z-514f23b2b456.md:1876:cb9b7d7 control 45 mechanised: guards carry names, and removing one is a governed event
record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:1252:    """This tool's own negative control (control 2), on four populations (control 45):
./docs/artifacts/software-implementations.md:190:| 2 — Fault-injection check profile (FICP) | every gate it runs must itself carry a negative control; the coverage is measured by tools/control_coverage.py and is not complete |
./docs/artifacts/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./docs/artifacts/control-application.md:33:| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey requires every declared member to reach exactly one terminal state and raises otherwise, and TWO population-measuring tools render nothing until it passes, proved by injecting an unreadable file and asserting their own count labels are absent. Both the type and the routing FAILED their first review: the type returned results for four incoherent walks, and both tools printed the counts the guard was meant to withhold. See D-60. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
./docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py` `tools/check_quotations.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction's limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
./docs/artifacts/control-application.md:74:| 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:1748:record/designs/candidate-control-register.md-167-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:2055:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md-2154-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./docs/control-application.html:38:<tr><td>2</td><td>Fault-injection check profile (FICP)</td><td>☐</td><td><code>tools/verify_negative_control.py</code> <code>tools/verify_fault_injection.py</code> <code>tools/control_coverage.py</code> <code>tools/scan_own_code.py</code>&lt;br&gt;tested by <code>tools/tests/test_fault_injection.py</code> <code>tools/tests/test_gate_negative_controls.py</code> <code>tools/tests/test_publication_gates.py</code> <code>tools/tests/test_chain_guards.py</code> <code>tools/tests/test_gate_refusals.py</code>&lt;br&gt;<strong>Remaining:</strong> Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record&#x27;s most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm&#x27;s first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.&lt;br&gt;&lt;small&gt;self-application: PRACTISED&lt;/small&gt;</td></tr>
./docs/control-application.html:41:<tr><td>5</td><td>Closed-world measurement</td><td>☐</td><td><code>tools/closed_world.py</code> <code>tools/derive_counts.py</code> <code>tools/scan_own_code.py</code> <code>tools/control_coverage.py</code> <code>tools/validate_provenance.py</code>&lt;br&gt;tested by <code>tools/tests/test_closed_world.py</code> <code>tools/tests/test_derive_counts.py</code>&lt;br&gt;<strong>Remaining:</strong> The discipline is now a TYPE rather than one function&#x27;s habit — closed_world.Survey requires every declared member to reach exactly one terminal state and raises otherwise, and TWO population-measuring tools render nothing until it passes, proved by injecting an unreadable file and asserting their own count labels are absent. Both the type and the routing FAILED their first review: the type returned results for four incoherent walks, and both tools printed the counts the guard was meant to withhold. See D-60. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where <code>samples</code> lived — and Survey would have reported a complete walk over the wrong set.&lt;br&gt;*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.&lt;br&gt;&lt;small&gt;self-application: ENFORCED&lt;/small&gt;</td></tr>
./docs/control-application.html:81:<tr><td>45</td><td>A replacement gate must catch what the old gate caught</td><td>☐</td><td><code>tools/guards.py</code> <code>tools/control_coverage.py</code> <code>tools/executive_log.py</code> <code>tools/check_quotations.py</code>&lt;br&gt;tested by <code>tools/tests/test_guard_identity.py</code> <code>tools/tests/test_deploy_check.py</code> <code>tools/tests/test_no_blank_cells.py</code> <code>tools/tests/test_gate_negative_controls.py</code>&lt;br&gt;<strong>Remaining:</strong> Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction&#x27;s limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.&lt;br&gt;*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.&lt;br&gt;&lt;small&gt;self-application: VIOLATED&lt;/small&gt;</td></tr>
./docs/control-application.html:82:<tr><td>46</td><td>Drift is measured against the original baseline</td><td>☐</td><td><code>tools/control_coverage.py</code> <code>tools/gate_health.py</code>&lt;br&gt;tested by <code>tools/tests/test_gate_negative_controls.py</code>&lt;br&gt;<strong>Remaining:</strong> The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.&lt;br&gt;&lt;small&gt;self-application: UNRESOLVED&lt;/small&gt;</td></tr>
./docs/software-implementations.html:199:<tr><td>2 — Fault-injection check profile (FICP)</td><td>every gate it runs must itself carry a negative control; the coverage is measured by tools/control_coverage.py and is not complete</td></tr>
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:401:./docs/deficiencies-10.html:107:<article class="e" data-actor="actor_repository_annotator_invocation" data-control="prospective_control_implemented_not_validated"><h3><a href="artifacts/deficiencies.md">D-58 — The context gate read &quot;not measurable here&quot; as &quot;measured and bad&quot;, and silently stopped publishing for eight commits</a><span class="tag w">implemented, not validated</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> the annotator, 2026-08-12 · <code>checking whether the previous landing&#x27;s Pages deploy had actually happened, after landing a handoff document. No gate reported it; land.py&#x27;s own deploy attestations had recorded six consecutive failures and none had been read.</code></p><p class="meta"><b>Forward control:</b> An observed failed postcondition opens a durable incident that refuses the next ordinary landing until resolved against evidence. Implemented in tools/deploy_obligations.py and enforced in tools/land.py. It constrains the sanctioned path only; it does not constrain an operator who edits it.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/check_executive_context.py<br><small>Four states with the exit code carrying them, precedence 2&gt;1&gt;3&gt;0, and each pin declaring its locator kind. Verified in the field: the CI run for db80307 reported exit 3 with oagf-CLAUDE.md measured repo-relative and the two operator paths UNAVAILABLE, which is the shape that previously failed.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>Its first version asserted returncode == 0 as a baseline that only one machine satisfies, and != 0 as drift, which exit 3 would now also satisfy. Rebuilt on injected pins directories asserting exact codes.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/land.py<br><small>The gate-admission policy is a named, tested predicate admitting only zero. The control 23 interlock is applied: an undischarged deployment obligation refuses an ordinary landing, --no-deploy-check is unavailable while one is open, and a resolved incident id is not a skeleton key. NOT VERIFIED IN THE FIELD: 40 fixture cases pass, and no real deploy failure has yet been driven through the live path.</small></td><td>partly repairable</td><td><span class="tag w">applied, not verified</span></td></tr><tr><td>tools/gate_health.py<br><small>Counted every non-success deploy as UNOBSERVED, collapsing &#x27;nobody waited&#x27; with &#x27;waited and it failed&#x27;. Now three states plus a consecutive-trailing-failure count, which reads 6.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/deploy_obligations.py<br><small>New. The obligation ledger that supplies control 23&#x27;s containment behaviour. Bootstrapped from the six historical failures rather than starting at its own installation. Not verified in the field.</small></td><td>repairable by supersession</td><td><span class="tag w">applied, not verified</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-59 — Two guards in the compliance matrices could never fire, and one of them was described on the published page as if it did</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>the unreachable guard surfaced while writing the control-2 fixture for the tool containing it; the advertised-but-absent guard was found by external review injecting the exact inputs the published page claimed were refused</code></p><p class="meta"><b>Forward control:</b> A closed-world inventory of published coverage matrices: an artifact type, a requirement that every such artifact registers, and a completeness validator run over the registry. Guarding two enumerated artifacts cannot make the control hold while a fourth can be added without notice — which is control 19&#x27;s error committed while implementing control 44.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/control_application.py<br><small>The unreachable guard now keys on the declaration rather than on completeness, and the reason-quality check the C44 row credited it with now exists. Both are exercised by injected faults.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_no_blank_cells.py<br><small>New. Injects every shape control 44 names on BOTH matrices, with baselines, and regression-tests that the page&#x27;s own history paragraph survives whether or not any row is ticked.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>record/findings/2026-08-08-search-capability-matrix.json<br><small>A third published matrix that nothing checks; its first two rows omit a column the last two carry. Unremediated, and the reason control 44 is recorded as a gap rather than done.</small></td><td>partly repairable</td><td><span class="tag d">not started</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_implemented_not_validated"><h3><a href="artifacts/deficiencies.md">D-60 — The type built to enforce closed-world measurement did not enforce it, and both tools routed through it printed the counts it was withholding</a><span class="tag w">implemented, not validated</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>external review reproduced four passing surveys that were incoherent, and read what the two routed tools printed before consulting the guard</code></p><p class="meta"><b>Forward control:</b> Closed-world measurement as a type whose result is unreachable past an incomplete or incoherent walk. Routing remains per-tool and by hand; nothing detects a new tool that globs a directory and prints a count, and no type catches a population defined too narrowly.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/closed_world.py<br><small>Per-key terminal states; misuse raises where it happens; counts with nothing accounted refuse; an exclusion ground is stripped before it is measured.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/scan_own_code.py<br><small>Renders nothing until the guard passes, and parses once rather than once per detector.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/control_coverage.py<br><small>Both the report path and --check consult the survey before any number is produced.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_closed_world.py<br><small>Its integration arms asserted the absence of a phrase removed in the same edit, so they forbade nothing. They now name the tools&#x27; actual count labels.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article><article class="e" data-actor="actor_repository_annotator_invocation" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-61 — The gate against fabricated party quotations could not see a quotation that wraps, so it checked none</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> the annotator, 2026-08-12 · <code>reading check_quotations.py while enrolling it in the guard registry; measured 235 candidates, 1 attributed, 0 checked across the 98 files it scans</code></p><p class="meta"><b>Forward control:</b> Nothing establishes that a negative control injects the fault in the form that OCCURS rather than the form that was imagined. A fixture corpus drawn from the record&#x27;s real shapes — wrapped prose, parenthetical provenance, blockquotes — would be the mechanism; reading the gate is not one.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>tools/check_quotations.py<br><small>Patterns admit a single newline and refuse a blank line. 350 candidates and 4 attributions after the repair; the verbatim party amendment in the pinned instructions is checked for the first time. A literal `if True:` wrapping the whole loop was also removed.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/scan_own_code.py<br><small>D-B matched a name bound to a bool and not a bool literal, so `if True:` was invisible on the real file and on a direct fixture. The literal is now the must-flag fixture.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article><article class="e" data-actor="actor_external_adversarial_reviewer" data-control="prospective_control_required_not_implemented"><h3><a href="artifacts/deficiencies.md">D-62 — A negative control corrupted the spend ledger 87 times and passed while doing it</a><span class="tag d">required, not implemented</span><span class="tag d">classification not human-reviewed</span></h3><p class="meta"><b>First articulated:</b> an external reviewer, 2026-08-12 · <code>external review read what the fixture did to the artifact rather than what it asserted; 87 of 141 ledger entries were the fixture</code></p><p class="meta"><b>Forward control:</b> Nothing requires a negative control to observe the tool&#x27;s SIDE EFFECTS rather than its output. A fixture that asserts on stdout alone cannot distinguish a refusal from an append. The general form is a fixture harness that snapshots every artifact a tool may write and requires the refusal arm to leave them unchanged.</p><div class="wrap"><table><tr><th>Affected object</th><th>Repairable?</th><th>Remediation</th></tr><tr><td>record/cycles/spend-ledger.json<br><small>87 fabricated rows removed; the rows and the pre-correction hash are preserved in record/cycles/spend-ledger-correction-2026-08-12.md. No dollar or token total was distorted; every count of entries was.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/record_spend.py<br><small>Refuses a cohort not in solicited_cohorts() before appending anything, under guard RS-01.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr><tr><td>tools/tests/test_gate_negative_controls.py<br><small>The arm required only that no cost was printed, which an append satisfies. It now requires a non-zero exit, names RS-01, and asserts the ledger is byte-identical afterwards.</small></td><td>repairable by supersession</td><td><span class="tag g">verified</span></td></tr></table></div></article>
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:961:| tools/control_coverage.py <br><sub>Both the report path and --check consult the survey before any number is produced.</sub> | repairable by supersession | verified |
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:976:| tools/tests/test_gate_negative_controls.py <br><sub>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</sub> | repairable by supersession | verified |
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2097:`tools/control_coverage.py` found 44 tools with no case they must fail. These four were taken
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2132:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2458:record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2512:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2488:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2514:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2551:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2517:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:5003:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2522:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6640:589263 node /home/reed/.config/nvm/versions/node/v24.4.1/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2523:record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6641:589270 /home/reed/.config/nvm/versions/node/v24.4.1/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2549:record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:1420:record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:2550:record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:2072:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:3250:| 2 — Fault-injection check profile (FICP) | every gate it runs must itself carry a negative control; the coverage is measured by tools/control_coverage.py and is not complete |
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:3393:    `control_coverage.executable_text` was built for last night, so it is reused rather than
record/executive/codex-transcripts/20260812T152211Z-815b340e6b2e.md:3398:    import control_coverage as cc
record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2445:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2461:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:140: {"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:169: | 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:170: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:252:           files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:355:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:705:tools/control_coverage.py:7:WHY PER FILE. `tools/self_application.py` records a determination PER CONTROL — a declaration that
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:706:tools/control_coverage.py:16:44):
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:815:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1425:                "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1430:         gap="Run tools/control_coverage.py for the current list and the current rate — the "
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1673:          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1680:          files=("tools/control_coverage.py", "tools/gate_health.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1858:  "control_coverage.py, which MEASURES control 2, had both errors at once. It counted tools "
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1920:/bin/bash -lc "sed -n '250,330p' tools/self_application.py; sed -n '430,570p' tools/control_application.py; git diff -- tools/control_application.py; rg -n \"self_application.py|control_application.py\" tools/land.py tools/tests/run_all.py tools/control_coverage.py; rg -n \"matrix|table|applicability|coverage\" tools/*.py record/controls/*.md | head -200" in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2171:           files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2238:tools/control_coverage.py:7:WHY PER FILE. `tools/self_application.py` records a determination PER CONTROL — a declaration that
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2278:tools/control_application.py:111:                "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2279:tools/control_application.py:116:         gap="Run tools/control_coverage.py for the current list and the current rate — the "
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2282:tools/control_application.py:359:          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2283:tools/control_application.py:366:          files=("tools/control_coverage.py", "tools/gate_health.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2320:record/controls/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2323:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2324:record/controls/control-application.md:74:| 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3152:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3570:   359	          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3577:   366	          files=("tools/control_coverage.py", "tools/gate_health.py"),
./record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:711:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:316:                "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:320:         gap="control_coverage.py reports the tools that still have no case they must fail. "
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:533:          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:880:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase. |
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:923:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering. |
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1928:tools/control_coverage.py:27:WHAT A PASS MEANS, EXACTLY. That some test referencing the tool asserts a refusal, a non-zero exit,
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1929:tools/control_coverage.py:45:#  Ways this repository's tests assert that something REFUSED. Broad on purpose: a false pass
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1930:tools/control_coverage.py:47:REFUSAL = re.compile(
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1931:tools/control_coverage.py:50:    r"refus|reject|must[_ ]fail|must[_ ]be[_ ]rejected|non-?zero|REFUSED|BROKEN|NOT FIT",
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1932:tools/control_coverage.py:81:    """A test asserting a refusal, OR the tool shipping its own must-fail cases."""
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1933:tools/control_coverage.py:108:        #  Only the portion of the test that mentions this tool, so a refusal assertion about a
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1934:tools/control_coverage.py:112:            if REFUSAL.search(window):
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1935:tools/control_coverage.py:113:                return (True, f"{name} asserts a refusal near a reference to it")
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1936:tools/control_coverage.py:114:    return (False, f"referenced by {referencing[0][0]} but no refusal is asserted near it")
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1937:tools/control_coverage.py:163:    print("\n  A pass means SOME test asserts a refusal near a reference to the tool. It does not")
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:2733:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5432:    88	                "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5436:    92	         gap="control_coverage.py reports the tools that still have no case they must fail. "
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5597:   301	          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:7830:   301	          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:26:[`control_coverage.py`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:285) has two holes:
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:46:- `control_coverage.py --check` with a broken test;
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:121:- `control_coverage.py` — it publishes a PERCENTAGE over a population, read the tests with
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:148:2. Is putting the TESTS in `control_coverage`'s population correct, or does it make an unrelated
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:161:/bin/bash -lc 'git status --short && git branch --show-current && git log -5 --oneline --decorate && git diff --stat && git diff -- tools/closed_world.py tools/scan_own_code.py tools/control_coverage.py tools/tests/test_closed_world.py' in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:171: M tools/control_coverage.py
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:189: tools/control_coverage.py                 | 50 +++++++++++++++++++++++++++++++
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:192:diff --git a/tools/control_coverage.py b/tools/control_coverage.py
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:194:--- a/tools/control_coverage.py
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:195:+++ b/tools/control_coverage.py
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:534:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:537:COVERS = ("closed_world.py", "scan_own_code.py", "control_coverage.py")
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:630:        ("control_coverage.py", ROOT / "tests" / "test_zzqx_unparseable_probe.py",
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:661: | 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:665:+| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey returns counts only when the walk accounted for everything, and raises otherwise, so there is no path to a number past an incomplete walk. TWO population-measuring tools are routed through it and REFUSE on an unreadable file, proved by injection. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:739: | 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:743:+| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey returns counts only when the walk accounted for everything, and raises otherwise, so there is no path to a number past an incomplete walk. TWO population-measuring tools are routed through it and REFUSE on an unreadable file, proved by injection. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:814:+                "tools/control_coverage.py", "tools/validate_provenance.py"),
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:840:/bin/bash -lc "sed -n '1,340p' tools/control_coverage.py
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:849:    python3 tools/control_coverage.py            # the matrix
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:850:    python3 tools/control_coverage.py --check    # refuse if any tool has no determination
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1665:The routing confirms a second blocker: both tools emit partial numerical results before checking `Survey.result()`. `scan_own_code.py` prints every per-class hit count; `control_coverage.py` prints HAS/NONE/total counts, and its `--check` path returns before consulting the survey at all. The injection cases look only for one old summary phrase or the percentage string, so they pass while the forbidden counts remain visible.
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1691:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1692:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1693:  [32m✓[0m ...and control_coverage.py names the file it could not read
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1694:  [32m✓[0m ...and control_coverage.py publishes NO number from the partial walk
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1695:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1704:python3 tools/control_coverage.py --check
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1705:python3 tools/control_coverage.py" in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1793:rg -n \"Control 45|control 45|C45|rank.?45| 45:\" record docs tools | head -80
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1804:tools/check_claims.py:269:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1805:tools/tests/test_executive_log.py:244:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1806:tools/tests/test_gate_negative_controls.py:271:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1807:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1811:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:420:tools/check_claims.py:257:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1812:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:744:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1813:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2728:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1815:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:4185:    94	#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1816:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:85:The regression suite should retain four populations, following control 45:
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1821:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:2154:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1823:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4910:The regression suite should retain four populations, following control 45:
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1825:record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:1748:record/designs/candidate-control-register.md-167-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1826:record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:2055:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md-2154-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1829:record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:711:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1832:record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:1252:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1842:record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3784:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1848:record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3152:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1851:record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:2733:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1855:record/executive/codex-transcripts/20260812T012550Z-5a86c2022e04.md:137:   retained per control 45.
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1857:record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2445:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1858:record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2461:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1894:          files=("tools/control_coverage.py", "tools/executive_log.py"),
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1901:          files=("tools/control_coverage.py", "tools/gate_health.py"),
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1944:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1945:docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1947:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1957:nl -ba tools/control_coverage.py | sed -n '220,325p'
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2492:   128	        ("control_coverage.py", ROOT / "tests" / "test_zzqx_unparseable_probe.py",
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2679:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2680:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2681:  [32m✓[0m ...and control_coverage.py names the file it could not read
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2682:  [32m✓[0m ...and control_coverage.py publishes NO number from the partial walk
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2683:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3753: M tools/control_coverage.py
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3782:python3 -m py_compile tools/closed_world.py tools/control_coverage.py tools/scan_own_code.py tools/tests/test_closed_world.py' in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3818:[`control_coverage.py`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:285) has two holes:
record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3838:- `control_coverage.py --check` with a broken test;
record/executive/codex-transcripts/20260812T012550Z-5a86c2022e04.md:137:   retained per control 45.
./record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:1252:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:711:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1:<!-- purpose: review batch four: control 45 mechanised as guard identity -->
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:130:# Review before landing: batch four, control 45 by way of guard identity
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:139:because it is the same requirement one level finer: control 45 says a modified gate must still
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:153:and an ORPHANED expectation (named by a fixture, declared nowhere) fails — which is control 45
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:248:-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:249:+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:250: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:270:-          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:276:+          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:293:           files=("tools/control_coverage.py", "tools/gate_health.py"),
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:450:+    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:626:The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:638:  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:717:    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:774:                       f"the guard was removed in a rewrite — which is what control 45 forbids "
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:827:which is control 45's requirement, applied to the guard rather than to the gate as a whole.
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:842:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:953:        check("...and the refusal cites control 45 by what it forbids",
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:954:              any("control 45" in f for f in found), str(found))
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1040:    40	The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1052:    52	  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1131:   131	    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1188:   188	                       f"the guard was removed in a rewrite — which is what control 45 forbids "
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1241:    19	which is control 45's requirement, applied to the guard rather than to the gate as a whole.
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1256:    34	#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1367:   145	        check("...and the refusal cites control 45 by what it forbids",
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1368:   146	              any("control 45" in f for f in found), str(found))
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1616:tools/tests/test_closed_world.py:229:check("BASELINE: control_coverage.py --check passes on a clean tree", r.returncode == 0,
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1617:tools/tests/test_closed_world.py:234:    check("control_coverage.py --check REFUSES on an unreadable population member",
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1655:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1659:docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1676:record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:923:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering. |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1678:record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:169: | 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1680:record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2323:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1685:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1793:rg -n \"Control 45|control 45|C45|rank.?45| 45:\" record docs tools | head -80
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1704:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1944:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1705:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1945:docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1706:record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1947:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1739:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1759:        check("...and the refusal cites control 45 by what it forbids",
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1760:              any("control 45" in f for f in found), str(found))
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:1988:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2133:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2468:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2469:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2470:  [32m✓[0m ...and control_coverage.py names the file it could not read
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2471:  [32m✓[0m ...and control_coverage.py publishes NO count from the partial walk
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2472:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2473:  [32m✓[0m BASELINE: control_coverage.py --check passes on a clean tree
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:2474:  [32m✓[0m control_coverage.py --check REFUSES on an unreadable population member
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3052:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3672:-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3673:+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control's requirement, applied at the guard rather than at the gate. `guards.py --check` is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool's guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.<br>*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3674: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3685:-<tr><td>45</td><td>A replacement gate must catch what the old gate caught</td><td>☐</td><td><code>tools/control_coverage.py</code> <code>tools/executive_log.py</code>&lt;br&gt;tested by <code>tools/tests/test_deploy_check.py</code> <code>tools/tests/test_gate_negative_controls.py</code>&lt;br&gt;<strong>Remaining:</strong> Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor&#x27;s cases, so the next gate change depends on somebody remembering.&lt;br&gt;&lt;small&gt;self-application: VIOLATED&lt;/small&gt;</td></tr>
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3686:+<tr><td>45</td><td>A replacement gate must catch what the old gate caught</td><td>☐</td><td><code>tools/guards.py</code> <code>tools/control_coverage.py</code> <code>tools/executive_log.py</code>&lt;br&gt;tested by <code>tools/tests/test_guard_identity.py</code> <code>tools/tests/test_deploy_check.py</code> <code>tools/tests/test_no_blank_cells.py</code>&lt;br&gt;<strong>Remaining:</strong> Guards now carry codes and fixtures ask for them BY NAME, so removing a guard in a rewrite makes the fixture that named it fail rather than pass because a neighbouring guard happened to fire — which is the control&#x27;s requirement, applied at the guard rather than at the gate. <code>guards.py --check</code> is a landing gate and reports an unexercised guard, which is where the three dead guards found this week were hiding. WHAT REMAINS: only ONE tool&#x27;s guards are named — control_application.py, eleven of them. Every other gate in this repository still has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Naming the rest is the work. And the mechanism cannot see a guard that was never written: an absent check has no code to be missing.&lt;br&gt;*Not closable in code:* That a driven guard is a CORRECT guard. This establishes that some fixture reaches each one, which is the floor control 2 asks for, one level finer.&lt;br&gt;&lt;small&gt;self-application: VIOLATED&lt;/small&gt;</td></tr>
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3687: <tr><td>46</td><td>Drift is measured against the original baseline</td><td>☐</td><td><code>tools/control_coverage.py</code> <code>tools/gate_health.py</code>&lt;br&gt;tested by <code>tools/tests/test_gate_negative_controls.py</code>&lt;br&gt;<strong>Remaining:</strong> The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.&lt;br&gt;&lt;small&gt;self-application: UNRESOLVED&lt;/small&gt;</td></tr>
./record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md:3772:+{"utc": "2026-08-12T05:42:08Z", "action": "codex_invoke", "claim": {"purpose": "review batch four: control 45 mechanised as guard identity", "override": null, "prompt_sha256": "452f8b847067a7eb8f68e06f48ec778ea0c4073a110a10048197eb8c545a0eec", "prompt_chars": 4291, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8c078e33d6df", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 2.0, "resets_at": "2026-08-12T09:40:00.182098+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.182121+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "73f03f31177e1fb0bd348537ed0b2fc318f0258cca7d22b22789337ab08f7d86"}
./record/executive/codex-transcripts/20260812T012550Z-5a86c2022e04.md:137:   retained per control 45.
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:11:The source-text tripwire at [test_gate_refusals.py:256](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_refusals.py:256) contains the tool name near words such as “negative control” and “refactor before it can have one.” The 600-character heuristic in [control_coverage.py:103](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:103) therefore reports:
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:58:One additional issue before the proposed `land.py` wiring: [control_coverage.py:140](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:140) makes `--check` pass whenever rows are labelled `NONE`; it currently exits 0 with 36 uncovered tools. It is a completeness-of-classification check, not a coverage threshold gate. Its intended semantics and help text need to be settled before wiring it into landing.
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:64:Verification: `test_gate_refusals.py` passed 21/21 assertions, `control_coverage.py` reported 36 covered / 36 uncovered / 3 N/A, and the complete runner exited 0 with all 28 suites passing. The green suite does not resolve the semantic findings above.
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:87:wiring `control_coverage --check` into land.py is currently held back because coverage was too
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:137:- **I edited a test label to satisfy the coverage heuristic.** `control_coverage.py` looks for a
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:161:/bin/bash -lc 'git branch --show-current && git status --short && git diff -- tools/tests/test_gate_refusals.py tools/build_register_view.py tools/control_findings.py tools/control_coverage.py && git diff --stat' in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:211:`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:427:        #  The label names the tool. control_coverage.py looks for a refusal assertion within
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:483:-| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:484:+| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:496:-<tr><td>2</td><td>Fault-injection check profile (FICP)</td><td>☐</td><td><code>tools/verify_negative_control.py</code> <code>tools/verify_fault_injection.py</code> <code>tools/control_coverage.py</code> <code>tools/scan_own_code.py</code>&lt;br&gt;tested by <code>tools/tests/test_fault_injection.py</code> <code>tools/tests/test_gate_negative_controls.py</code> <code>tools/tests/test_publication_gates.py</code> <code>tools/tests/test_chain_guards.py</code>&lt;br&gt;<strong>Remaining:</strong> control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control&#x27;s own requirement is unmet in this codebase.&lt;br&gt;&lt;small&gt;self-application: PRACTISED&lt;/small&gt;</td></tr>
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:497:+<tr><td>2</td><td>Fault-injection check profile (FICP)</td><td>☐</td><td><code>tools/verify_negative_control.py</code> <code>tools/verify_fault_injection.py</code> <code>tools/control_coverage.py</code> <code>tools/scan_own_code.py</code>&lt;br&gt;tested by <code>tools/tests/test_fault_injection.py</code> <code>tools/tests/test_gate_negative_controls.py</code> <code>tools/tests/test_publication_gates.py</code> <code>tools/tests/test_chain_guards.py</code> <code>tools/tests/test_gate_refusals.py</code>&lt;br&gt;<strong>Remaining:</strong> Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record&#x27;s most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm&#x27;s first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.&lt;br&gt;&lt;small&gt;self-application: PRACTISED&lt;/small&gt;</td></tr>
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:509:+{"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:518:-| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:519:+| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:581:                 "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:585:-         gap="control_coverage.py reports the tools that still have no case they must fail. "
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:590:+         gap="Run tools/control_coverage.py for the current list and the current rate — the "
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:627:tools/control_coverage.py:18:    C2   negative control            YES — a tool either has a case it must fail, or it does not
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:628:tools/control_coverage.py:19:    C5   closed-world measurement    partial — needs semantics; not attempted here
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:629:tools/control_coverage.py:21:    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:630:tools/control_coverage.py:24:**Only C2 is measured here.** It is the register's flagship and the one with an unambiguous
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:631:tools/control_coverage.py:168:    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:790:record/executive/HANDOFF-2026-08-12.md:82:* **C2 coverage** — `python3 tools/control_coverage.py` lists the tools with no case they must
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:836:record/controls/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:901:docs/artifacts/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:930:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:335:tools/control_coverage.py:18:    C2   negative control            YES — a tool either has a case it must fail, or it does not
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:950:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2280:tools/control_coverage.py:21:    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:951:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2281:tools/control_coverage.py:168:    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1193:+{"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1202:-| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1203:+| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1265:                 "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1269:-         gap="control_coverage.py reports the tools that still have no case they must fail. "
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1274:+         gap="Run tools/control_coverage.py for the current list and the current rate — the "
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1290:     4	`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1506:   220	        #  The label names the tool. control_coverage.py looks for a refusal assertion within
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1602:tools/control_coverage.py
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1634:/bin/bash -lc "rg -n '"'^### Control (2|5|23|32|44|45|55|61)'"\\b|"'^## (2|5|23|32|44|45|55|61)'"\\b|"'^### (2|5|23|32|44|45|55|61)'"\\.' record/designs/candidate-control-register.md; sed -n '1,280p' tools/control_coverage.py; sed -n '1,230p' tools/check_prompt.py; sed -n '1,300p' tools/check_register.py; sed -n '1,190p' tools/build_register_view.py" in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1639:    python3 tools/control_coverage.py            # the matrix
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1640:    python3 tools/control_coverage.py --check    # refuse if any tool has no determination
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3784:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3846:/bin/bash -lc 'python3 tools/tests/test_gate_refusals.py; test_rc=$?; echo test_gate_refusals_rc=$test_rc; python3 tools/control_coverage.py; coverage_rc=$?; echo control_coverage_rc=$coverage_rc; python3 - <<'"'PY'
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3849:import control_coverage as c
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3927:control_coverage_rc=0
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3934:**Analyzing control_coverage --check limitations**
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3936:/bin/bash -lc 'python3 tools/control_coverage.py --check; rc=$?; echo control_coverage_check_rc=$rc; git show HEAD:tools/control_coverage.py > /tmp/oagf-control-coverage-head.py && python3 /tmp/oagf-control-coverage-head.py | sed -n '"'1,12p'; rm /tmp/oagf-control-coverage-head.py" in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3939:control_coverage_check_rc=0
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3967: 2 {'scope': 'code', 'files': ('tools/verify_negative_control.py', 'tools/verify_fault_injection.py', 'tools/control_coverage.py', 'tools/scan_own_code.py'), 'tests': ('tools/tests/test_fault_injection.py', 'tools/tests/test_gate_negative_controls.py', 'tools/tests/test_publication_gates.py', 'tools/tests/test_chain_guards.py', 'tools/tests/test_gate_refusals.py'), 'gap': "Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding."}
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3977: 45 {'scope': 'code', 'files': ('tools/control_coverage.py', 'tools/executive_log.py'), 'tests': ('tools/tests/test_deploy_check.py', 'tools/tests/test_gate_negative_controls.py'), 'gap': "Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering."}
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:4235:The 21-arm suite itself is green, but several labels overclaim what passed. I’m running the complete suite now because the new file is auto-discovered; separately, the proposed future `control_coverage --check` wiring is not currently a coverage gate at all—it returns 0 with 36 uncovered tools, so that prerequisite and its help text need correction before landing it into `land.py`.
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5353:/bin/bash -lc "nl -ba tools/tests/test_gate_refusals.py | sed -n '1,275p'; nl -ba tools/control_coverage.py | sed -n '65,165p'; nl -ba tools/build_register_view.py | sed -n '96,130p;430,450p'; nl -ba tools/control_findings.py | sed -n '82,98p'; nl -ba tools/control_application.py | sed -n '105,135p'" in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5358:     4	`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5574:   220	        #  The label names the tool. control_coverage.py looks for a refusal assertion within
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5802:   111	                "tools/control_coverage.py", "tools/scan_own_code.py"),
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5807:   116	         gap="Run tools/control_coverage.py for the current list and the current rate — the "
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5844:The source-text tripwire at [test_gate_refusals.py:256](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_refusals.py:256) contains the tool name near words such as “negative control” and “refactor before it can have one.” The 600-character heuristic in [control_coverage.py:103](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:103) therefore reports:
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5891:One additional issue before the proposed `land.py` wiring: [control_coverage.py:140](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:140) makes `--check` pass whenever rows are labelled `NONE`; it currently exits 0 with 36 uncovered tools. It is a completeness-of-classification check, not a coverage threshold gate. Its intended semantics and help text need to be settled before wiring it into landing.
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5897:Verification: `test_gate_refusals.py` passed 21/21 assertions, `control_coverage.py` reported 36 covered / 36 uncovered / 3 N/A, and the complete runner exited 0 with all 28 suites passing. The green suite does not resolve the semantic findings above.
./record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:1748:record/designs/candidate-control-register.md-167-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:2055:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md-2154-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:463:tools/control_coverage.py:280:def main() -> int:
record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:485:tools/control_application.py:382:          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:1135:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:1420:record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:2072:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:2295:4. `control_coverage.py --check` still out of `land.py` — its semantics need settling.
./docs/artifacts/deficiencies.md:2382:were not a result. A comment does not make a printed number a non-result. `control_coverage.py`
./docs/artifacts/deficiencies.md:2438:which is control 45: a repair must still catch what the old gate caught.
./docs/artifacts/deficiencies.md:2554:**Recorded because it is the register's own control 45 turned on the register.** A gate was added
./record/executive/HANDOFF-2026-08-12.md:49:| C2 | `control_coverage.py` | counted a tool named in a COMMENT; missed one driven through a wrapper |
./record/executive/HANDOFF-2026-08-12.md:87:| `tools/guards.py` | control 45. Guards carry codes, fixtures ask by name, and removing a guard is a governed event against a committed baseline |
./record/executive/HANDOFF-2026-08-12.md:135:* **`control_coverage.py --check` is still not in `land.py`.** It no longer passes vacuously — it
./record/executive/HANDOFF-2026-08-12.md:186:`record/executive/guard-baseline.json` — the guards this repository has committed to keeping.
./record/executive/action-log.jsonl:273:{"utc": "2026-08-12T05:42:08Z", "action": "codex_invoke", "claim": {"purpose": "review batch four: control 45 mechanised as guard identity", "override": null, "prompt_sha256": "452f8b847067a7eb8f68e06f48ec778ea0c4073a110a10048197eb8c545a0eec", "prompt_chars": 4291, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8c078e33d6df", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 2.0, "resets_at": "2026-08-12T09:40:00.182098+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.182121+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "73f03f31177e1fb0bd348537ed0b2fc318f0258cca7d22b22789337ab08f7d86"}
./record/executive/action-log.jsonl:274:{"utc": "2026-08-12T05:46:51Z", "action": "codex_return_captured", "claim": {"purpose": "review batch four: control 45 mechanised as guard identity", "prompt_sha256": "452f8b847067a7eb8f68e06f48ec778ea0c4073a110a10048197eb8c545a0eec", "exit_status": 0, "output_chars": 370987, "output_sha256": "f88f795695aaca6fc3db88391279964d23f802a5b84cdeef564e1d276750037b", "transcript": "record/executive/codex-transcripts/20260812T054208Z-452f8b847067.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T054208Z-452f8b847067.md", "actor": "claude-code", "head": "8c078e33d6df", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 2.0, "resets_at": "2026-08-12T09:40:00.182098+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.182121+00:00"}, "cached": true, "age_seconds": 281}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "e6c4effa6f58742129be36b0b5fcc0247239e6fc93b0a707837f6d378eaa671e"}
./record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:209:                 "tools/control_coverage.py", "tools/validate_provenance.py"),
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:1146:    `control_coverage.executable_text` was built for last night, so it is reused rather than
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:1151:    import control_coverage as cc
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2279:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2488:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2551:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2918:   323	    `control_coverage.executable_text` was built for last night, so it is reused rather than
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:2923:   328	    import control_coverage as cc
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3315:    99	    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3817:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3818:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3819:  [32m✓[0m ...and control_coverage.py names the file it could not read
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3820:  [32m✓[0m ...and control_coverage.py publishes NO count from the partial walk
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3821:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3822:  [32m✓[0m BASELINE: control_coverage.py --check passes on a clean tree
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:3823:  [32m✓[0m control_coverage.py --check REFUSES on an unreadable population member
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:4421:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:4425:  [32m✓[0m ...and the refusal names control 45 and what it forbids
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:5003:    "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.",
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6029:tools/control_coverage.py:162:    call to `state()`, not to the constant.
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6467:cb9b7d7 control 45 mechanised: guards carry names, and removing one is a governed event
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6640:589263 node /home/reed/.config/nvm/versions/node/v24.4.1/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6641:589270 /home/reed/.config/nvm/versions/node/v24.4.1/lib/node_modules/@openai/codex/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/bin/codex exec -C /home/reed/git/open-asi-governance-forum IMPLEMENTATION REVIEW — the D-64 repair you designed this morning. Be adversarial: find what still fails open, what a fixture proves that it does not prove, and anything I got wrong in translating your review.  You called for: refusal hierarchy; injectable strictly-validating counter; strict cap validation; a single composite behind require() and the CLI; chain verification; effect-boundary negative controls with isolated temp lease AND log; and NOT changing trial-04's semantics.  What I did NOT do, deliberately, and want you to attack: no reservation ledger, and the row unit left as granted.  One thing I decided that you did not: the live chain has FOUR real breaks, all caused by the 2026-08-11 redaction of a correspondent's name from four action rows' note fields. A hard refusal would have converted an obligatory removal into a total work stoppage. So breaks must be recorded in a register that pins BOTH hashes. Attack that design specifically — is it a tombstone or is it a waiver with extra steps?  Also confirm or refute my reconciliation of the symptom you challenged: I ran land.main() with --check-only under a fixture exhausted lease, and it printed two preflight refusals and exit 2 with NO gates. You were right; it is filed as D-65 and the false claim is corrected in place with the original retained.  === git diff (code only) === diff --git a/record/executive/COMPACTION-NOTE-2026-08-12.md b/record/executive/COMPACTION-NOTE-2026-08-12.md index 051b967..95d1651 100644 --- a/record/executive/COMPACTION-NOTE-2026-08-12.md +++ b/record/executive/COMPACTION-NOTE-2026-08-12.md @@ -38,6 +38,28 @@ which prints the refusal. This is a green signal that is not downstream of what  take it to certify — the same class as everything else this week, noticed while writing this  note rather than by anything that checks.   +> **CORRECTION, attached 2026-08-12 — the paragraph above is FALSE, and was never run. D-65.** +> +> `land.py`'s `preflight()` calls `require("commit")` and `require("push")` **before any gate is +> printed**, on every path including `--check-only`. Under an exhausted lease the tool prints two +> refusals and exits 2, having shown **no gates at all**. Observed after the fact against a +> fixture lease at 205/1; the transcript shows the last real `--check-only` invocation more than +> four hours before the lease reached its bound, and the sentence above was written by editing +> out a line that had correctly said *"the lease one will fail until renewed"*. +> +> Codex found it in the first review after it was committed. The mechanism described was real and +> the reasoning was coherent; the claim that it had been *observed* was not. It is the same +> confirm-what-was-expected failure as D-59 through D-62, in prose rather than in code, inside +> the note whose subject was that failure mode. +> +> The half that was true: `executive_lease.py`'s CLI did report `live: True` and exit 0 while +> `require()` refused, because `state()` modelled only the calendar bound. That is repaired under +> D-64 — one composite function now answers for both bounds, the count and its unit are printed, +> and the exit status follows the composite. **The gate was wrong; the tool was not.** +> +> The original text stands above, unedited, because what the workbench believed at the time is +> the part worth keeping. +  ## A DEFECT FOUND WHILE WRITING THIS, AND DELIBERATELY NOT FIXED    `executive_lease.require()` computes the action count inside a `try` whose `except Exception` diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl index 8bc0dcd..df67274 100644 --- a/record/executive/action-log.jsonl +++ b/record/executive/action-log.jsonl @@ -285,3 +285,8 @@  {"utc": "2026-08-12T08:41:57Z", "action": "deploy", "claim": {"observed": true, "commit": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "conclusion": "success", "deployed_sha": "1004c27e142b867981f6cf7fc53bcd55e4bcf2ed", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31579223731"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "second guard enrolment, D-61, D-62", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T09:39:59.532368+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.532386+00:00"}, "cached": true, "age_seconds": 227}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "454cb751dd4746a41c584ebbea14f4f2de124f984db3eae3794539eeffdc1ad9"}  {"utc": "2026-08-12T10:29:13Z", "action": "codex_invoke", "claim": {"purpose": "review control 64 and the register partition repair before landing", "override": null, "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "prompt_chars": 3637, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "6a648b96ba0555ec37c29f5fe90d43124c83c550c9c043eaed44b31c4d51534f"}  {"utc": "2026-08-12T10:33:22Z", "action": "codex_return_captured", "claim": {"purpose": "review control 64 and the register partition repair before landing", "prompt_sha256": "d59cce8efea641e8bbc68fec1ec833922db6527ee4a09c3496ac4b88e1d1b7c0", "exit_status": 0, "output_chars": 350790, "output_sha256": "bc6675dd81b70ec0f88ee256a2d7afd0c96b222c28a123faebf4f0d6be93fd36", "transcript": "record/executive/codex-transcripts/20260812T102913Z-d59cce8efea6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T102913Z-d59cce8efea6.md", "actor": "claude-code", "head": "1004c27e142b", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T15:20:00.061247+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.061273+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b6d852909f7d4023d43db0e1f6228121e3391c88f0bf99871f7defaf6f9dc70"} +{"utc": "2026-08-12T13:20:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a83903b48636dcb53958d41118501f5753177b9c91d0704157580dee30dda473"} +{"utc": "2026-08-12T13:20:08Z", "action": "push", "claim": {"target_ref": "main", "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1be1d9bc73863336b92b2be0fdb68f881127e1f888cd4737d463e9c877467998"} +{"utc": "2026-08-12T13:24:17Z", "action": "deploy", "claim": {"observed": true, "commit": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "conclusion": "success", "deployed_sha": "8d2e19dd7f5d17f45e33008bbfdcca3b61fbdfdd", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31600883492"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "first landing under trial-04", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T15:20:00.514111+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.514134+00:00"}, "cached": true, "age_seconds": 248}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0b22fe17f916d1a0aeee9d529915b7974155da7c6601ace8efe7e93dbfb5bb2c"} +{"utc": "2026-08-12T13:25:31Z", "action": "codex_invoke", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "override": null, "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "prompt_chars": 7270, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "a8eefd06e9aa3d892e059f36ab8599f9aef54104b30a5eb359a598740fe96d1f"} +{"utc": "2026-08-12T13:30:07Z", "action": "codex_return_captured", "claim": {"purpose": "D-64 design review: the lease's own action cap fails open on an unreadable count", "prompt_sha256": "56e940386ca64f2f99f9159177b70f495268d32201984d34bb86442b2837c135", "exit_status": 0, "output_chars": 1454785, "output_sha256": "e3a9506ab3bb851bfe4fc4a51630f954bf42f31a40e197c48662936f23766ec9", "transcript": "record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T132531Z-56e940386ca6.md", "actor": "claude-code", "head": "8d2e19dd7f5d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 6.0, "resets_at": "2026-08-12T15:20:00.270578+00:00"}, "seven_day": {"utilization": 8.0, "resets_at": "2026-08-18T01:00:00.270599+00:00"}, "cached": true, "age_seconds": 275}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "2b32296e7d73926c14a795d35e8796531dd397c5a73649c7823d38a614720917"} diff --git a/tools/codex_call.py b/tools/codex_call.py index 04f4c89..407bc9f 100644 --- a/tools/codex_call.py +++ b/tools/codex_call.py @@ -240,7 +240,10 @@ def call(prompt: str, purpose: str, override: str = "") -> tuple[int, str]:      #  expired lease is not a slow-down to wait out; it is the sunset, and there is no override.      try:          lease.require("codex_invoke") -    except lease.LeaseExpired as expired: +    #  LeaseRefused, not LeaseExpired. Catching the calendar-bound name only meant a refusal on +    #  the action bound — or on unreadable evidence — escaped as a traceback instead of being +    #  attested as a refusal. D-64. +    except lease.LeaseRefused as expired:          ex.log_action("codex_invoke", {"purpose": purpose, "coverage": "refused_before_action"},                        verified=False, problems=[str(expired)], note="refused by the lease")          print(f"  REFUSED: {expired}", file=sys.stderr) diff --git a/tools/control_application.py b/tools/control_application.py index 772b56f..21625de 100644 --- a/tools/control_application.py +++ b/tools/control_application.py @@ -132,10 +132,24 @@ APPLICATION: dict[int, dict] = {               "specific postcondition per governed effect; what exists reconciles the classes that ha"               "ppen to have profiles."),   4: dict(scope=CODE, -         files=("tools/executive_lease.py", "tools/land.py"), -         tests=("tools/tests/test_executive_log.py",), -         gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m" -             "atching in particular is unimplemented — one lease authorises everything."), +         files=("tools/executive_lease.py", "tools/land.py", "tools/codex_call.py", +                "record/executive/action-log-discontinuities.json"), +         tests=("tools/tests/test_executive_log.py", "tools/tests/test_lease_bounds.py"), +         gap="**The lease failed OPEN inside itself until 2026-08-12.** Its action count sat in a " +             "bare `except Exception: spent = 0`, so an unreadable log — including the ordinary " +             "case of the module being loaded by path — granted an exhausted lease unlimited " +             "actions. Reproduced against the live spent lease; see D-64. The count is now a " +             "typed observation that refuses when it cannot be read, the chain is verified " +             "against pinned, recorded discontinuities before the count is believed, and ten " +             "refusal fixtures assert at the effect boundary that the governed write does not " +             "happen. " +             "*Remaining:* the count is still derived from a log this layer writes about itself, " +             "so under-logging shrinks it, one authorised action can append several rows, and two " +             "callers at cap-1 can both be admitted; a pre-action reservation ledger is designed " +             "and not built. The unit is rows, not the action classes require() authorises — 200 " +             "rows of five kinds ended trial-03 — and that is left as-is deliberately, because " +             "trial-04 was granted over the row unit. Scope-matching, token subject, nonce and " +             "revocation remain unimplemented: one lease still authorises everything."),   5: dict(scope=CODE,           files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",                  "tools/control_coverage.py", "tools/validate_provenance.py"), @@ -449,13 +463,20 @@ APPLICATION: dict[int, dict] = {                   "tools/build_round_pages.py", "tools/solicit_tools.py",                   "tools/watch_arrivals.py", "tools/gate_health.py",                   "tools/check_executive_context.py", "tools/deploy_obligations.py", -                 "tools/scan_own_code.py"), +                 "tools/scan_own_code.py", "tools/executive_lease.py"),            tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py", -                 "tools/tests/test_gate_negative_controls.py"), +                 "tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"),            gap="47 sites were swept on 2026-08-11 — 14 fixed, 36 dispositioned, none outstanding "                "— and unknowns are still ordinary Python values rather than a TYPE that "                "aggregation refuses. A new `d.get(k, 0)` is caught by scan_own_code.py only " -              "because a detector looks for it, not because the language refuses it."), +              "because a detector looks for it, not because the language refuses it. " +              "**The sweep missed the worst site**, because it looked for absence-as-zero in " +              "tools that COUNT and this one was a tool that REFUSES: executive_lease.require() " +              "coerced an unreadable action count to 0 and granted the action. Fixed under D-64 " +              "with four typed count states of which only one carries a number, and a refusal " +              "worded so that 'cannot be read' is not reported as 'exhausted' — the same " +              "coercion one level up, in the error message."),   54: dict(scope=NOT_CODE,            reason="Structural: no component of this system is updated by an agent and then "                   "reused by it. The three-arm comparison has no arms to run."), @@ -507,19 +528,23 @@ APPLICATION: dict[int, dict] = {           gap="Several tools print what they do not establish. Nothing BINDS a published gain to a residual failure set, nothing rejects a residual stated as a bare percentage, and the ci"               "ted test does not inspect the reporting tools for the property."),   64: dict(scope=CODE, -          files=("tools/record_spend.py", "tools/guards.py"), -          tests=("tools/tests/test_gate_negative_controls.py",), -          gap="Applied at exactly ONE site — the arm that caused the failure. record_spend.py " -              "refuses an unregistered cohort under RS-01, and that arm requires a non-zero exit " -              "AND asserts the spend ledger byte-identical afterwards. Whole-artifact identity " -              "is the correct LOCAL assertion there because that tool has no permitted " -              "refusal-side effect; it is NOT the control's general requirement, which is about " -              "the governed effect boundary — a gate may legitimately write a denial record " -              "while refusing. Nothing generalises any of it: no harness makes a refusing tool " +          files=("tools/record_spend.py", "tools/guards.py", "tools/executive_lease.py"), +          tests=("tools/tests/test_gate_negative_controls.py", +                 "tools/tests/test_lease_bounds.py"), +          gap="TWO sites now, and the second is the better instance. record_spend.py refuses an " +              "unregistered cohort under RS-01, and that arm requires a non-zero exit AND asserts " +              "the spend ledger byte-identical afterwards; whole-artifact identity is the correct " +              "LOCAL assertion there because that tool has no permitted refusal-side effect. " +              "test_lease_bounds.py goes further and is the shape the control actually asks for: " +              "each of its ten refusal fixtures calls a `governed_effect()` that would write a " +              "sentinel file if admitted, and asserts the sentinel is ABSENT — so a lease that " +              "raised the right exception and let the write happen would fail, which " +              "`pytest.raises` alone cannot detect. It carries a positive control, because a " +              "lease that refused everything would otherwise score ten out of ten. " +              "*Remaining:* still nothing GENERALISES it — no harness makes a refusing tool "                "declare its effect set, and no check requires a refusal arm to assert over that " -              "set rather than over stdout. The control's own discriminating case — a tool that " -              "exits non-zero, prints a refusal, and acts anyway — has no fixture and no " -              "instance.", +              "set rather than over stdout. Both instances were hand-written by the author of " +              "the tool being tested.",            residue="That a declared write set is a COMPLETE one. The party declaring what a tool "                    "may write is the party whose tool writes it, and a harness proves nothing "                    "about a file nobody listed. The control says so in its own false-negative " diff --git a/tools/control_findings.py b/tools/control_findings.py index 7a965d6..c977ac1 100644 --- a/tools/control_findings.py +++ b/tools/control_findings.py @@ -18,7 +18,7 @@ detection this project did not achieve. The values:      reading       a human read the code while implementing the control and saw it      external      someone outside this workbench found it   -**A detector fired for 11 of 41.** Those are the defect shapes that are mechanically +**A detector fired for 11 of 44.** Those are the defect shapes that are mechanically  recognisable; the rest came from reading, from running the fault, or from an outsider. So the  controls both caught defects directly AND directed attention to the right code, and the split  matters more than either number alone. @@ -67,6 +67,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [    "published rate at nothing. Both UNDERSTATE SPEND in the ledger this project is funded from.",    "detector", "42fda21; tools/scan_own_code.py C53a/C53b"),   + ("C53 typed unknown", +  "executive_lease.require() coerced an UNREADABLE action count to 0 and granted the action. Any " +  "error reading the log — including the ordinary case of a caller loading the module by path, so " +  "that `import executive_log` did not resolve — handed an exhausted lease unlimited actions. " +  "Reproduced against the live spent lease: refused one way, 'granted, 200 remaining' the other. " +  "The 2026-08-11 sweep of 47 sites missed it because it looked for absence-as-zero in tools that " +  "COUNT, and this is a tool that REFUSES.", +  "reading", "D-64; tools/executive_lease.py; tools/tests/test_lease_bounds.py"), + + ("C4 fail-closed lease", +  "The control found its own implementation failing open. Beyond the count defect, the reported " +  "bound was not the enforced one: `state()` modelled only the calendar bound while `require()` " +  "modelled both, so the CLI printed `live: True` and exited 0 throughout the twenty-three " +  "refusals that ended trial-03 — and land.py's `lease` gate reads nothing but that exit code.", +  "reading", "D-64; tools/executive_lease.py authorization_state()"), + + ("C64 effect boundary", +  "The lease's own negative control asserted the refusal SIGNAL and never the effect. " +  "test_max_actions_is_enforced_not_merely_recorded passed whether or not the cap blocked — its " +  "success branch called check(..., True) when require() did NOT refuse — and it counted against " +  "the REAL action log, so what it asserted depended on ambient repository history. Found by " +  "Codex while reviewing the D-64 repair.", +  "external", "D-64; tools/tests/test_executive_log.py; tools/tests/test_lease_bounds.py"), +   ("C53 typed unknown",    "round_cycle.ledger_spent_today defaulted a missing worst_case_usd to 0.0. That figure gates "    "whether more spending is allowed today, so an unpriceable entry made the budget preflight " diff --git a/tools/executive_lease.py b/tools/executive_lease.py index 62a2ebe..a417ea7 100644 --- a/tools/executive_lease.py +++ b/tools/executive_lease.py @@ -21,11 +21,62 @@ commit, a push, or a Codex invocation. A reserved action that has already begun  attest after expiry — killing work mid-flight would corrupt the record it is meant to protect —  but no new action may begin.   -The calendar bound is primary ------------------------------- -Two bounds are recorded, and the DATE is the one that matters: a count of actions can be evaded by -not logging, while a deadline cannot be evaded by undercounting anything. It is stored and compared -in **UTC**, stated explicitly, because an unstated timezone is a free day of slack. +The calendar bound is primary — in ONE sense, and the two senses were being conflated +-------------------------------------------------------------------------------------- +The claim used to be flat: "the DATE is the one that matters", because a count of actions can be +evaded by not logging while a deadline cannot be evaded by undercounting anything. That is true +and it is about **evadability**. It was being read as a second claim — that the date is the bound +which actually stops work — and *that* is false in practice: **trial-02 and trial-03 both ended +on the count**, trial-03 with about 275 calendar hours still unspent. Codex separated the two on +2026-08-12 and the docstring now separates them too. The calendar bound is the harder one to +cheat. The action bound is the one that has done the stopping, twice, which makes it a work +budget in effect whatever it is called. + +WHAT max_actions ACTUALLY COUNTS, stated because it is not what the name suggests +--------------------------------------------------------------------------------- +It counts **rows appended to the executive action log** inside the lease's epoch. It does not +count the action classes `require()` authorises. The 200 rows that exhausted trial-03 were 56 +`test`, 56 `push`, 46 `deploy`, 21 `codex_invoke` and 21 `codex_return_captured`; a `commit`, a +`governed_write` and a `round` each contribute nothing at all, while `deploy` and +`codex_return_captured` — neither of them a governed class — consume the budget. So one landing +spends about three, one Codex call spends two, and a lease of 400 is roughly 130 landings' worth +of rows rather than 400 permitted actions. + +That mismatch is a real defect (D-64) and it is **not fixed here**, deliberately: trial-04 was +granted by the custodian over this unit, and quietly re-defining the unit under a live grant +would change the size of a permission the custodian gave. What is fixed here is the silent part — +the unit is now named in the report, and the mismatch is on the record for the custodian to rule +on. A pre-action *reservation* ledger, which is what would actually make the number mean +"actions", is designed in D-64 and not built. + +An unreadable count is not a zero +---------------------------------- +The count used to be computed inside `try: ... except Exception: spent = 0`. Any failure — a +truncated line, a permissions error, or merely importing this module by path so that +`import executive_log` did not resolve — handed an exhausted lease **unlimited actions**. That is +control 4 failing open inside the lease, and control 53 violated in the one mechanism whose job +is to refuse: an unreadable measurement rendered as the most favourable value. + +So the count is now a **typed observation** with its own states, and only `COUNTED` is a number. +`UNAVAILABLE` refuses — with its own exception type and its own words, because "the count cannot +be read" and "the lease is exhausted" are different facts and control 53 forbids coercing the +first into the second. + +Why a broken hash chain refuses, and why four breaks do not +------------------------------------------------------------ +A count over a log that can be rewritten is not evidence. The chain is therefore verified before +the count is believed. But the live log has **four genuine breaks**, all from the 2026-08-11 +redaction: the custodian was asked to remove a correspondent's name, four action rows carried it +in their `note`, and removing it changed those rows' bytes. Refusing outright there would have +converted an obligatory redaction into a total work stoppage — and the operator's next move, +predictably, is to disable the check. + +The route out is the one the corpus already uses for material that must be removed: an +**authorised, recorded discontinuity**. `record/executive/action-log-discontinuities.json` names +each break by position and pins BOTH hashes — the one stored on the breaking row and the one its +predecessor now computes to. An unrecorded break refuses. A recorded one that has since moved +also refuses, because the pin no longer matches. What is excused is one exact, argued edit, not +"edits at this position".    There is no override  --------------------- @@ -35,16 +86,29 @@ Renewal appends a NEW lease to `record/executive/leases.jsonl` naming who renewe  evidence, and to what limit. That file is append-only: a superseded lease stays visible, so the  history of how often this layer asked for more time is part of the record rather than a diff.   +Codex called the recovery path for an unreadable log "defensible and not necessarily --force", +on one condition: the constrained layer gets no waiver argument and no repair command. It has +neither. Restoring readable evidence, or issuing a superseding lease that establishes a fresh +count epoch, is the custodian's move and is visible in an append-only file when made. +  What this does not do  ----------------------  It does not bind anything that never calls it. A shell command, a direct `git push`, or an edit  made without going through a tool that checks is unaffected — the same limit every control in this  layer has. It also cannot attest that its own source is unmodified; that needs a pin held outside  this repository, which does not exist yet. + +It is **not a security boundary and not a runaway detector**, and saying so is part of the fix. +The count comes from a log this layer writes about itself: under-logging still shrinks it, one +authorised action can still append several rows, and two callers at `cap - 1` can still both be +admitted. It is a checkpoint that makes ordinary work stop and ask.  """    from __future__ import annotations   +import argparse +import hashlib +import importlib.util  import json  import sys  from datetime import datetime, timezone @@ -52,20 +116,53 @@ from pathlib import Path    REPO_ROOT = Path(__file__).resolve().parent.parent  LEASES = REPO_ROOT / "record" / "executive" / "leases.jsonl" +DISCONTINUITIES = REPO_ROOT / "record" / "executive" / "action-log-discontinuities.json"    #  Every action class that must hold a live lease. Named rather than defaulted-open: a class  #  absent from this list is unchecked, and that should be visible here rather than inferred.  GOVERNED_ACTIONS = ("round", "governed_write", "commit", "push", "codex_invoke")   +#  What the number in `max_actions` is a number OF. Printed with every report, because the whole +#  D-64 finding is that a bound whose unit is unstated gets read as the unit its name suggests. +COUNT_UNIT = "rows appended to record/executive/action-log.jsonl within the lease epoch" + +#  Typed states for the count. Only COUNTED carries a number; the rest are answers, not values. +NOT_CONFIGURED = "NOT_CONFIGURED"      # the lease sets no max_actions +NOT_EVALUATED = "NOT_EVALUATED"        # another bound already refused; the log was not read +COUNTED = "COUNTED" +UNAVAILABLE = "UNAVAILABLE"            # the count cannot be shown; this REFUSES + + +class LeaseRefused(RuntimeError): +    """Base for every refusal to admit an action. Callers should catch THIS. + +    Introduced because `codex_call.py` caught `LeaseExpired` by name, so a refusal of any other +    kind would have escaped as a traceback rather than as a logged, attested refusal — the +    difference between a governed stop and a crash. +    """ + + +class LeaseExpired(LeaseRefused): +    """The CALENDAR bound has passed. Kept as a distinct name because callers cite it.""" +   -class LeaseExpired(RuntimeError): -    """Raised before an action begins. Not an error in the work — the sunset arriving.""" +class LeaseBoundReached(LeaseRefused): +    """The action bound has been reached. The calendar bound may be nowhere near.""" + + +class LeaseEvidenceUnavailable(LeaseRefused): +    """The bound cannot be shown to hold, so the action may not begin. + +    Deliberately NOT a subclass of LeaseExpired and deliberately worded differently. Calling an +    unreadable count "exhausted" would be the same coercion of an unknown into a value that +    made the defect, moved one level up into the error message. +    """      class UnknownActionClass(RuntimeError):      """A caller asked permission for a class this module does not recognise.   -    Distinct from LeaseExpired because it means something different: the lease is not saying no, +    Distinct from LeaseRefused because it means something different: the lease is not saying no,      it is saying it does not know what was asked. The first version RETURNED a permissive dict      here, marked `observed_unprofiled`, which permitted a misspelled class even under an expired      lease — recreating the exact taxonomy hole the lease was built to close. `observed_unprofiled` @@ -90,12 +187,13 @@ def current() -> dict | None:      return leases[-1] if leases else None     -def state() -> dict: -    """Whether an action may begin, and on which bound it turns.""" +def calendar_state() -> dict: +    """The date bound alone. A pure calculation over the lease and the clock — no I/O beyond +    the lease file, so it cannot fail in the ways the count can."""      lease = current()      if lease is None: -        return {"live": False, "why": "no lease has ever been written; the layer is unauthorised", -                "lease": None} +        return {"live": False, "lease": None, +                "why": "no lease has ever been written; the layer is unauthorised"}      expires = datetime.fromisoformat(lease["expires_utc"].replace("Z", "+00:00"))      remaining = (expires - _now()).total_seconds()      if remaining <= 0: @@ -107,33 +205,199 @@ def state() -> dict:                      f"({remaining/3600:.1f} h remaining)")}     +def _load_executive_log(): +    """Locate `executive_log` RELATIVE TO THIS FILE, never via the caller's sys.path. + +    The bare `import executive_log` this replaces only resolved when the caller happened to have +    `tools/` on the path. Several of our own tools load modules by path, and for those the import +    raised, the bare `except` swallowed it, and the cap silently vanished. Robustness here is a +    safety property rather than a convenience: a gate that refuses everyone who loads it the +    wrong way gets routed around within the day. +    """ +    path = Path(__file__).resolve().parent / "executive_log.py" +    if not path.is_file(): +        raise FileNotFoundError(f"executive_log.py is not beside this module at {path}") +    spec = importlib.util.spec_from_file_location("_executive_log_for_lease", path) +    module = importlib.util.module_from_spec(spec) +    spec.loader.exec_module(module) +    return module + + +def _row_hash(row: dict) -> str: +    """Must match `executive_log.attest`'s hashing exactly, or every row looks tampered with.""" +    return hashlib.sha256( +        json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest() + + +def authorised_discontinuities() -> list[dict]: +    """Chain breaks the custodian's record accounts for. An unreadable register is not an empty +    one: a malformed file raises here rather than quietly excusing nothing (or everything).""" +    if not DISCONTINUITIES.is_file(): +        return [] +    doc = json.loads(DISCONTINUITIES.read_text(encoding="utf-8")) +    entries = doc.get("discontinuities") +    if not isinstance(entries, list): +        raise ValueError(f"{DISCONTINUITIES.name} has no 'discontinuities' list") +    return entries + + +#  Fixtures need to point the count at a log they control; the old max-actions test mutated +#  LEASES but read the REAL action log, so its result depended on ambient repository history and +#  it passed either way. This is the injection point, and it is deliberately NOT quiet: whenever +#  it is set, every state dict carries `source_is_default: False` and the CLI says so in red. +#  Setting it outside a fixture is indistinguishable from lying about the count, which is why it +#  announces itself rather than being a private module global. +COUNT_SOURCE: Path | None = None + + +def count_state(lease: dict | None, log_path: Path | None = None) -> dict: +    """Observe the action count. Returns a typed state; NEVER a bare number, never a default. + +    Every way of failing to see the log ends in UNAVAILABLE with a reason, including the ones +    `read_log()` used to absorb: a missing file reads as an empty list there, which is the +    absence-as-zero pattern this repository has now filed four deficiencies about. +    """ +    if lease is None: +        return {"state": NOT_EVALUATED, "why": "no lease to count against"} +    cap = lease.get("max_actions") +    if cap is None: +        return {"state": NOT_CONFIGURED, "cap": None, +                "why": "this lease sets no max_actions; only the calendar bound applies"} +    #  `if cap:` was the old test, so a cap of 0 — "you may begin nothing" — skipped the check +    #  entirely and authorised everything. bool is excluded explicitly because it is an int. +    if isinstance(cap, bool) or not isinstance(cap, int) or cap < 0: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": f"max_actions is {cap!r}, which is not a non-negative integer, so no " +                       f"bound can be evaluated from it"} + +    epoch = lease.get("granted_utc") +    if not isinstance(epoch, str) or not epoch: +        return {"state": UNAVAILABLE, "cap": cap, +                "why": "the lease has no granted_utc, so its epoch cannot be delimited"} + +    source = log_path or COUNT_SOURCE +    default_source = source is None +    if default_source: +        try: +            source = getattr(_load_executive_log(), "LOG_PATH", None) +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, "cap": cap, "source_is_default": True, +                    "why": f"the action log module could not be loaded ({exc}), so the count " +                           f"cannot be shown. THIS IS THE D-64 PATH: it used to be zero."} +    marks = {"cap": cap, "source": str(source), "source_is_default": default_source} +    if source is None or not Path(source).is_file(): +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log {source} is absent, so the count cannot be shown. An " +                       f"absent log is not an empty one."} + +    rows, prev, breaks = [], "0" * 64, [] +    try: +        lines = Path(source).read_text(encoding="utf-8").splitlines() +    except OSError as exc: +        return {"state": UNAVAILABLE, **marks, +                "why": f"the action log could not be read ({exc}); a permissions or device error " +                       f"is not a count of zero"} +    for number, line in enumerate(lines, start=1): +        if not line.strip(): +            continue +        try: +            row = json.loads(line) +        except json.JSONDecodeError as exc: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} does not parse ({exc}); a count over a log " +                           f"that cannot be read in full is not a count"} +        if not isinstance(row, dict) or not isinstance(row.get("utc"), str) or not row["utc"]: +            return {"state": UNAVAILABLE, **marks, +                    "why": f"action log line {number} has no usable utc, so it cannot be placed " +                           f"inside or outside the lease epoch. The old count dropped such rows " +                           f"silently via .get('utc', '')."} +        if row.get("prev_sha256") != prev: +            breaks.append({"utc": row["utc"], "action": row.get("action"), +                           "stored_prev_sha256": row.get("prev_sha256"), +                           "computed_prev_sha256": prev, "line": number}) +        prev = _row_hash(row) +        rows.append(row) + +    if breaks: +        try: +            authorised = authorised_discontinuities() +        except Exception as exc:                                        # noqa: BLE001 +            return {"state": UNAVAILABLE, **marks, +                    "why": f"the chain is broken and the discontinuity register could not be " +                           f"read ({exc}), so no break can be shown to be authorised"} +        pinned = {(a.get("utc"), a.get("action"), a.get("stored_prev_sha256"), +                   a.get("computed_prev_sha256")) for a in authorised} +        unrecorded = [b for b in breaks +                      if (b["utc"], b["action"], b["stored_prev_sha256"], +                          b["computed_prev_sha256"]) not in pinned] +        if unrecorded: +            first = unrecorded[0] +            return {"state": UNAVAILABLE, **marks, "breaks": breaks, +                    "why": f"{len(unrecorded)} unrecorded break(s) in the action-log hash chain, " +                           f"first at line {first['line']} ({first['utc']} {first['action']}). " +                           f"A count over a log that has been rewritten without a recorded, " +                           f"pinned reason is not evidence. Record it in " +                           f"{DISCONTINUITIES.name} or restore the log."} + +    spent = sum(1 for row in rows if row["utc"] >= epoch) +    return {"state": COUNTED, **marks, "spent": spent, "remaining": cap - spent, +            "epoch": epoch, "unit": COUNT_UNIT, "authorised_breaks": len(breaks), +            "why": f"{spent} of {cap} ({COUNT_UNIT}) since {epoch}"} + + +def authorization_state() -> dict: +    """The SOLE composite decision. Both `require()` and the CLI read this one function. + +    They used to compute independently — `require()` looked at both bounds, the CLI at only the +    calendar — so the CLI could print a live lease while a landing was being refused. Two +    functions answering the same question differently is how a green signal stops being +    downstream of what it certifies. +    """ +    cal = calendar_state() +    if not cal["live"]: +        #  The log is not read at all: one refusal is enough, and reading it here would let an +        #  unreadable log turn a plain expiry into a confusing evidence failure. +        return {**cal, "count": {"state": NOT_EVALUATED, +                                 "why": "the calendar bound already refuses"}, +                "refusal": ("expired", cal["why"])} +    count = count_state(cal["lease"]) +    out = {**cal, "count": count} +    if count["state"] == UNAVAILABLE: +        return {**out, "live": False, +                "refusal": ("evidence_unavailable", count["why"]), +                "why": f"{cal['why']}; but {count['why']}"} +    if count["state"] == COUNTED and count["spent"] >= count["cap"]: +        return {**out, "live": False, +                "refusal": ("bound_reached", +                            f"lease {cal['lease']['lease_id']} has {count['spent']} attested " +                            f"actions against a max_actions of {count['cap']}. The calendar " +                            f"bound has not arrived, but this one has. Renewal is the " +                            f"custodian's decision. The unit is {COUNT_UNIT}."), +                "why": f"{cal['why']}; {count['why']}"} +    if count["state"] == COUNTED: +        out["actions_spent"], out["actions_remaining"] = count["spent"], count["remaining"] +    return {**out, "refusal": None} + + +#  Kept as the public name it has always had. It is now the composite, so every existing caller +#  that asked "may an action begin?" gets the answer to that question rather than half of it. +state = authorization_state + +  def require(action_class: str) -> dict: -    """Call BEFORE the action. Raises LeaseExpired rather than returning a value to ignore.""" +    """Call BEFORE the action. Raises rather than returning a value a caller can ignore."""      if action_class not in GOVERNED_ACTIONS:          raise UnknownActionClass(              f"{action_class!r} is not one of {GOVERNED_ACTIONS}. Add it deliberately, or call "              f"the class that covers this action. A permission API that says yes to a name it "              f"does not know is not a gate.") -    st = state() -    if not st["live"]: -        raise LeaseExpired(st["why"]) -    #  max_actions was recorded and never read. A secondary bound nobody enforces is the same -    #  shape as the ten-action sunset that was passed at twenty-three. -    cap = (st.get("lease") or {}).get("max_actions") -    if cap: -        try: -            import executive_log as ex -            spent = sum(1 for e in ex.read_log() -                        if e.get("utc", "") >= st["lease"]["granted_utc"]) -        except Exception:                                               # noqa: BLE001 -            spent = 0 -        if spent >= cap: -            raise LeaseExpired( -                f"lease {st['lease']['lease_id']} has {spent} attested actions against a " -                f"max_actions of {cap}. The calendar bound has not arrived, but this one has. " -                f"Renewal is the custodian's decision.") -        st["actions_spent"] = spent -        st["actions_remaining"] = cap - spent +    st = authorization_state() +    refusal = st.get("refusal") +    if refusal: +        kind, why = refusal +        raise {"expired": LeaseExpired, +               "bound_reached": LeaseBoundReached, +               "evidence_unavailable": LeaseEvidenceUnavailable}[kind](why)      return st     @@ -152,7 +416,15 @@ def grant(lease_id: str, expires_utc: str, granted_by: str, evidence: str,      def main() -> int: -    st = state() +    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0]) +    parser.add_argument("--json", action="store_true", help="emit the authorization state") +    args = parser.parse_args() + +    st = authorization_state() +    if args.json: +        print(json.dumps(st, indent=2, sort_keys=True, default=str)) +        return 0 if st["live"] else 1 +      lease = st.get("lease")      if lease:          print(f"  lease:    {lease['lease_id']}  granted {lease['granted_utc']} " @@ -161,8 +433,27 @@ def main() -> int:          if lease.get("supersedes"):              print(f"  supersedes: {lease['supersedes']}")          print(f"  evidence: {lease['evidence']}") + +    #  THE COUNT IS PRINTED. It was enforced and never reported, so this surface said "live" for +    #  the last twenty-three refusals of trial-03 and stayed exit 0 throughout. +    count = st.get("count") or {} +    print(f"  count:    {count.get('state')}") +    if count.get("source_is_default") is False: +        print(f"    \033[31mCOUNT SOURCE OVERRIDDEN\033[0m -> {count.get('source')}; this is a " +              f"fixture setting and the number below is not the record's") +    if count.get("state") == COUNTED: +        print(f"    {count['spent']} spent, {count['remaining']} remaining of {count['cap']}") +        print(f"    unit: {COUNT_UNIT}") +        print(f"    NOT the classes require() authorises — see D-64") +        if count.get("authorised_breaks"): +            print(f"    {count['authorised_breaks']} authorised chain discontinuity(ies)") +    else: +        print(f"    {count.get('why')}") +      print(f"  live:     {st['live']}\n    {st['why']}")      print(f"  leased action classes: {', '.join(GOVERNED_ACTIONS)}") +    if st.get("refusal"): +        print(f"  REFUSING: {st['refusal'][0]}")      return 0 if st["live"] else 1     diff --git a/tools/land.py b/tools/land.py index aff59c7..659b748 100644 --- a/tools/land.py +++ b/tools/land.py @@ -178,7 +178,7 @@ def preflight(target_branch: str) -> list[str]:      for action_class in ("commit", "push"):          try:              lease.require(action_class) -        except (lease.LeaseExpired, lease.UnknownActionClass) as refused: +        except (lease.LeaseRefused, lease.UnknownActionClass) as refused:              problems.append(f"lease ({action_class}): {refused}")      code, branch = run(["git", "branch", "--show-current"])      branch = branch.strip() diff --git a/tools/tests/test_executive_log.py b/tools/tests/test_executive_log.py index 6646467..a8fb183 100644 --- a/tools/tests/test_executive_log.py +++ b/tools/tests/test_executive_log.py @@ -330,16 +330,41 @@ def test_unknown_class_raises_rather_than_permitting(tmp):      def test_max_actions_is_enforced_not_merely_recorded(tmp): -    """A secondary bound nobody reads is the ten-action sunset again.""" +    """A secondary bound nobody reads is the ten-action sunset again. + +    REWRITTEN 2026-08-12. The previous version was vacuous in two independent ways and passed +    under both outcomes: its success branch called `check(..., True)` when `require()` did NOT +    refuse, and it counted against the REAL action log, so what it asserted depended on how much +    ambient history the repository happened to hold that day. Codex found it while reviewing the +    D-64 fix — a test written to confirm the behaviour its author expected rather than to observe +    what the tool did, which is the seventh instance of that shape this week. + +    The effect-boundary version of this lives in tools/tests/test_lease_bounds.py. What is kept +    here is the narrow claim this file is about: the bound is READ, not merely recorded. +    """      lease.LEASES = tmp      if tmp.exists():          tmp.unlink() -    lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +    log = Path(tempfile.mkdtemp()) / "action-log.jsonl" +    #  Dated INSIDE the epoch. grant() stamps granted_utc as now, so a row dated in the past is +    #  correctly not counted — the first version of this fixture used 2026-06-01 and measured an +    #  empty epoch while claiming to measure a full one. +    row = {"utc": "2099-01-01T00:00:00Z", "action": "test", "prev_sha256": "0" * 64} +    log.write_text(json.dumps(row) + "\n", encoding="utf-8") +    saved_source = lease.COUNT_SOURCE +    lease.COUNT_SOURCE = log      try: -        lease.require("round") -        check("max_actions blocks once the log passes the cap", True) -    except lease.LeaseExpired as expired: -        check("max_actions blocks once the log passes the cap", "max_actions" in str(expired)) +        lease.grant("cap", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=1) +        try: +            lease.require("round") +            check("max_actions blocks once the log reaches the cap", False) +        except lease.LeaseBoundReached as reached: +            check("max_actions blocks once the log reaches the cap", "max_actions" in str(reached)) +        #  The other half, without which the above passes on a lease that refuses everything. +        lease.grant("roomy", "2099-01-01T00:00:00Z", "test", "evidence", max_actions=99) +        check("and admits while the count is under it", lease.require("round")["live"] is True) +    finally: +        lease.COUNT_SOURCE = saved_source      def test_no_lease_at_all_is_unauthorised(tmp):  === new file: tools/tests/test_lease_bounds.py === #!/usr/bin/env python3 """Conformance tests for the lease's ACTION bound — D-64.      python3 tools/tests/test_lease_bounds.py  Every fixture here asserts at the **effect boundary** (control 64), not at the refusal signal. A refusal that raises the right exception and lets the action happen anyway is the failure this project filed D-62 about, and the test that would have caught it has to look at the world rather than at stderr. So each case calls `governed_effect()`, which does the same two things a real caller does — ask the lease, then act — and the assertion is that **the sentinel file does not exist**. `pytest.raises` alone would pass on a lease that refuses in words and admits in fact.  There is a positive control at the bottom for the reason every refusal suite needs one: a lease that refuses *everything* would pass all eight refusal cases. If `test_admits_under_the_bound` ever fails, the other results here mean nothing.  WHAT THIS REPLACES. `test_max_actions_is_enforced_not_merely_recorded` in test_executive_log.py was vacuous twice over: it read the **real** action log, so its result depended on ambient repository history, and its success branch called `check(..., True)` when `require()` did **not** refuse. It passed under both outcomes. Codex found it on 2026-08-12 while reviewing the D-64 fix, which makes it the seventh consecutive defect located in the checking apparatus rather than in the thing checked. """ from __future__ import annotations  import hashlib import json import subprocess import sys import tempfile from pathlib import Path  REPO_ROOT = Path(__file__).resolve().parent.parent.parent sys.path.insert(0, str(REPO_ROOT / "tools")) import executive_lease as lease                                           # noqa: E402  PASSED = FAILED = 0   def check(label: str, cond: bool) -> None:     global PASSED, FAILED     if cond:         PASSED += 1         print(f"  \033[32m✓\033[0m {label}")     else:         FAILED += 1         print(f"  \033[31m✗ {label}\033[0m")   #  --------------------------------------------------------------------------- #  The world these fixtures act on. #  ---------------------------------------------------------------------------  def workspace() -> Path:     return Path(tempfile.mkdtemp())   def write_lease(dirpath: Path, *, max_actions, expires="2099-01-01T00:00:00Z",                 granted="2026-01-01T00:00:00Z") -> Path:     path = dirpath / "leases.jsonl"     path.write_text(json.dumps({         "lease_id": "fixture", "granted_utc": granted, "expires_utc": expires,         "granted_by": "fixture", "evidence": "fixture", "max_actions": max_actions,         "note": "", "supersedes": None, "authority": "fixture"}) + "\n", encoding="utf-8")     return path   def write_log(dirpath: Path, rows: int, *, corrupt: str = "") -> Path:     """A CHAIN-VALID action log of `rows` rows, unless asked to damage it.      The chain has to be built properly even for tests that never look at it, because a fixture     whose log is accidentally broken would pass the "unrecorded break refuses" case for the wrong     reason and pass it forever.     """     path = dirpath / "action-log.jsonl"     lines, prev = [], "0" * 64     for i in range(rows):         row = {"utc": f"2026-06-01T00:00:{i:02d}Z", "action": "test", "note": f"row {i}",                "prev_sha256": prev}         prev = hashlib.sha256(             json.dumps(row, sort_keys=True, separators=(",", ":")).encode()).hexdigest()         lines.append(json.dumps(row))     if corrupt == "unparseable" and lines:         lines[-1] = "{not json at all"     if corrupt == "no_utc" and lines:         row = json.loads(lines[-1])         del row["utc"]         lines[-1] = json.dumps(row)     if corrupt == "chain" and len(lines) > 1:         row = json.loads(lines[-2])         row["note"] = "rewritten after the fact"      # breaks the NEXT row's prev_sha256         lines[-2] = json.dumps(row)     path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")     return path   def governed_effect(sentinel: Path) -> None:     """Ask, then act — the shape of every real caller.      The sentinel write is what a landing, a round or a Codex call would do. It must never happen     on a refused path, and "must never happen" is a statement about this file existing, not about     which exception came back.     """     lease.require("governed_write")     sentinel.write_text("the governed action ran", encoding="utf-8")   def refuses(label: str, *, max_actions, rows: int, corrupt: str = "",             expected: type[BaseException], expires: str = "2099-01-01T00:00:00Z",             log: bool = True, discontinuities: str | None = None) -> None:     """Run the governed effect under a fixture lease and assert BOTH halves of the refusal."""     work = workspace()     saved_leases, saved_source, saved_disc = lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=max_actions, expires=expires)         lease.COUNT_SOURCE = write_log(work, rows, corrupt=corrupt) if log else work / "absent"         if discontinuities is not None:             reg = work / "discontinuities.json"             reg.write_text(discontinuities, encoding="utf-8")             lease.DISCONTINUITIES = reg         try:             governed_effect(sentinel)             check(f"{label} — NOT refused", False)         except expected:             #  The signal was right. Now the part that matters.             check(f"{label} — refused, and the effect did not happen",                   not sentinel.exists())         except BaseException as wrong:                                    # noqa: BLE001             check(f"{label} — raised {type(wrong).__name__}, expected "                   f"{expected.__name__}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = (             saved_leases, saved_source, saved_disc)   # --------------------------------------------------------------------------- #  The eight refusals. # ---------------------------------------------------------------------------  print("\nthe action bound, asserted at the effect boundary")  refuses("cap reached with a readable log", max_actions=3, rows=3,         expected=lease.LeaseBoundReached)  #  THE D-64 CASE ITSELF. Under the old code this branch set spent = 0 and ADMITTED the action. refuses("log absent — the count is unknown, not zero", max_actions=100, rows=0, log=False,         expected=lease.LeaseEvidenceUnavailable)  refuses("a line that does not parse", max_actions=100, rows=4, corrupt="unparseable",         expected=lease.LeaseEvidenceUnavailable)  refuses("a row with no utc, which the old count dropped silently", max_actions=100, rows=4,         corrupt="no_utc", expected=lease.LeaseEvidenceUnavailable)  refuses("an unrecorded break in the hash chain", max_actions=100, rows=5, corrupt="chain",         expected=lease.LeaseEvidenceUnavailable, discontinuities='{"discontinuities": []}')  refuses("a broken chain with an unreadable discontinuity register", max_actions=100, rows=5,         corrupt="chain", expected=lease.LeaseEvidenceUnavailable,         discontinuities='{"this file": "has no discontinuities list"}')  #  `if cap:` treated 0 as "no bound configured" and authorised everything. refuses("a cap of zero means nothing may begin", max_actions=0, rows=0,         expected=lease.LeaseBoundReached)  refuses("a cap that is not a non-negative integer", max_actions="lots", rows=1,         expected=lease.LeaseEvidenceUnavailable)  refuses("True is not a cap of one", max_actions=True, rows=0,         expected=lease.LeaseEvidenceUnavailable)  refuses("the calendar bound still refuses on its own", max_actions=100, rows=0,         expires="2000-01-01T00:00:00Z", expected=lease.LeaseExpired)   # --------------------------------------------------------------------------- #  THE POSITIVE CONTROL. Without it, a lease that refuses everything scores ten out of ten. # ---------------------------------------------------------------------------  def test_admits_under_the_bound() -> None:     work = workspace()     saved_leases, saved_source = lease.LEASES, lease.COUNT_SOURCE     sentinel = work / "sentinel"     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 3)         governed_effect(sentinel)         check("POSITIVE CONTROL: under the bound the effect DOES happen", sentinel.exists())     except BaseException as exc:                                          # noqa: BLE001         check(f"POSITIVE CONTROL: under the bound the effect DOES happen — "               f"raised {type(exc).__name__}: {exc}", False)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved_leases, saved_source   def test_authorised_break_is_excused_only_when_pinned() -> None:     """The discontinuity register must excuse ONE argued edit, not a position."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = write_log(work, 5, corrupt="chain")         reg = work / "disc.json"         lease.DISCONTINUITIES = reg          #  Discover the break exactly as the lease does, then record it.         reg.write_text('{"discontinuities": []}', encoding="utf-8")         blocked = lease.count_state(lease.current())         breaks = blocked.get("breaks") or []         check("the break is reported with both hashes, so it can be pinned",               bool(breaks) and breaks[0].get("stored_prev_sha256")               and breaks[0].get("computed_prev_sha256"))          reg.write_text(json.dumps({"discontinuities": [             {k: b[k] for k in ("utc", "action", "stored_prev_sha256", "computed_prev_sha256")}             for b in breaks]}), encoding="utf-8")         excused = lease.count_state(lease.current())         check("a pinned, recorded break is excused and the count proceeds",               excused["state"] == lease.COUNTED)         check("the excused break is still REPORTED, not hidden",               excused.get("authorised_breaks") == len(breaks))          #  Now damage the log AGAIN at the same place. The pin must stop matching.         rows = [json.loads(ln) for ln in                 Path(lease.COUNT_SOURCE).read_text(encoding="utf-8").splitlines() if ln.strip()]         rows[-3]["note"] = "and rewritten a second time"         Path(lease.COUNT_SOURCE).write_text(             "\n".join(json.dumps(r) for r in rows) + "\n", encoding="utf-8")         moved = lease.count_state(lease.current())         check("a SECOND edit at the same position is not covered by the old pin",               moved["state"] == lease.UNAVAILABLE)     finally:         lease.LEASES, lease.COUNT_SOURCE, lease.DISCONTINUITIES = saved   def test_unavailable_is_not_called_exhausted() -> None:     """Control 53 at the level of the words: an unknown must not be coerced into a value OR     into the wrong refusal. A reader who sees 'exhausted' will go and ask for a renewal that     would not have helped."""     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=100)         lease.COUNT_SOURCE = work / "absent"         try:             lease.require("governed_write")             check("an unreadable count refuses", False)         except lease.LeaseEvidenceUnavailable as refused:             words = str(refused).lower()             check("an unreadable count refuses", True)             check("it is not described as expiry or exhaustion",                   "expired" not in words and "exhaust" not in words                   and "max_actions" not in words)         check("LeaseEvidenceUnavailable is NOT a kind of LeaseExpired",               not issubclass(lease.LeaseEvidenceUnavailable, lease.LeaseExpired))         check("but every refusal is a kind of LeaseRefused, so one except catches all",               all(issubclass(e, lease.LeaseRefused) for e in                   (lease.LeaseExpired, lease.LeaseBoundReached,                    lease.LeaseEvidenceUnavailable)))     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_is_not_green_while_require_refuses() -> None:     """The reported symptom that started this: a surface saying LIVE while a landing refuses.      The CLI and require() now read ONE function, so this is checked by exercising the composite     and the exit status against the same fixture rather than by trusting that they agree.     """     work = workspace()     saved = (lease.LEASES, lease.COUNT_SOURCE)     try:         lease.LEASES = write_lease(work, max_actions=2)         lease.COUNT_SOURCE = write_log(work, 5)         st = lease.authorization_state()         check("the composite reports NOT live when the count is over the cap",               st["live"] is False and st["refusal"][0] == "bound_reached")         check("the calendar half still says the lease has time left",               lease.calendar_state()["live"] is True)         check("so the two bounds are distinguishable in the report",               st["count"]["state"] == lease.COUNTED and st["count"]["spent"] == 5)     finally:         lease.LEASES, lease.COUNT_SOURCE = saved   def test_cli_exit_status_follows_the_composite() -> None:     """Run the real CLI in a subprocess against the REAL record. It must exit 0 only if the     composite says live — the gate in land.py reads nothing but this exit code."""     proc = subprocess.run([sys.executable, str(REPO_ROOT / "tools" / "executive_lease.py")],                           capture_output=True, text=True)     st = lease.authorization_state()     check("the CLI's exit status matches the composite decision",           (proc.returncode == 0) == bool(st["live"]))     check("the CLI prints the count state, which it never used to mention",           "count:" in proc.stdout)     check("the CLI names the unit of max_actions",           "rows appended" in proc.stdout or st["count"]["state"] != lease.COUNTED)   def test_the_source_override_announces_itself() -> None:     """The injection point fixtures need must never be quiet, or it becomes a way to lie."""     work = workspace()     saved = lease.COUNT_SOURCE     try:         lease.COUNT_SOURCE = write_log(work, 1)         st = lease.count_state(lease.current())         check("an overridden count source is marked in the state",               st.get("source_is_default") is False)     finally:         lease.COUNT_SOURCE = saved     st = lease.count_state(lease.current())     check("the real count declares itself default-sourced",           st.get("source_is_default") is True)   def source_code() -> str:     """The module's CODE, without its prose.      Written as a raw-text scan first, and both assertions immediately failed — on the docstring     sentences saying there is no `--force` and describing the old `spent = 0`. A detector that     reads the explanation of a defect as the defect is the same false-positive shape     `control_coverage.executable_text` was built for last night, so it is reused rather than     re-derived. Docstrings of nested functions are stripped too: the class docstrings here quote     the wording they forbid.     """     import ast as _ast     import control_coverage as cc     text = (REPO_ROOT / "tools" / "executive_lease.py").read_text(encoding="utf-8")     tree = _ast.parse(text)     for node in _ast.walk(tree):         if isinstance(node, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef)):             #  clean=False. With the default, ast DEDENTS the docstring, so the string handed             #  to replace() no longer matches the indented source and every nested docstring             #  survived the strip — which is how the `import executive_log` sentence in             #  _load_executive_log's own prose kept failing an assertion about the code.             doc = _ast.get_docstring(node, clean=False)             if doc:                 text = text.replace(doc, "", 1)     return cc.executable_text(text)   def test_no_force_flag_anywhere() -> None:     src = source_code()     check("there is still no --force", "--force" not in src)     check("and no environment variable can raise the cap",           "environ" not in src and "getenv" not in src)   def test_the_defect_this_replaces_cannot_return() -> None:     """The literal shape of D-64: a bare except that lands on a favourable default."""     src = source_code()     check("no 'spent = 0' default survives anywhere in the module",           "spent = 0" not in src)     check("the count is not read through a bare module-name import of executive_log",           "import executive_log" not in src)     #  The stripper itself needs a negative control, or "no match" could mean "stripped     #  everything". It earned its place immediately: the first version looked for "def require("     #  and failed, because the token stream is rejoined with spaces — so the two assertions     #  above were matching against a shape the file could never contain, and would have passed     #  forever over a module that still had the defect.     check("STRIPPER CONTROL: real code survives the strip",           "def require" in src and "LeaseBoundReached" in src and "raise" in src)   test_admits_under_the_bound() test_authorised_break_is_excused_only_when_pinned() test_unavailable_is_not_called_exhausted() test_cli_is_not_green_while_require_refuses() test_cli_exit_status_follows_the_composite() test_the_source_override_announces_itself() test_no_force_flag_anywhere() test_the_defect_this_replaces_cannot_return()  #  KEEP THE SUMMARY AND EXIT LAST. Tests appended after them do not get counted, and the file #  then reports a stale total that looks like a pass. print(f"\n{PASSED} passed, {FAILED} failed") sys.exit(1 if FAILED else 0)  === new file: record/executive/action-log-discontinuities.json === {   "what_this_is": "Authorised breaks in the executive action log's hash chain. The lease verifies the chain before it believes the action count; an unrecorded break refuses. This file is the ONLY route past that, and it is not a waiver: each entry pins the exact break by position and by BOTH hashes -- the one stored on the breaking row and the one its predecessor now computes to. Edit any of those rows again and the pin stops matching, so the lease refuses again. What is excused is one argued edit, never 'edits at this position'.",   "why_not_simply_refuse": "Because refusing outright would have converted a redaction the custodian was obliged to make into a total work stoppage, and the operator's next move under a stoppage is to disable the check. Same reasoning as the corpus tombstone rule: the bytes may go, the record of their going may not.",   "what_this_does_not_establish": "That the four rows say what they said before. The chain proves only that nothing else moved; the content of a redacted note is gone, by design, and this file cannot recover it. It also does not make the count trustworthy in general -- the log is written by the layer the count constrains, and D-64 says so at length.",   "discontinuities": [     {       "utc": "2026-08-11T12:40:08Z",       "action": "push",       "stored_prev_sha256": "bc3abd276f5566fa8f499cb183625c6691916b3abf88134309ce157f6a680a7b",       "computed_prev_sha256": "daf542019b91572533e4752a9bbfaaaa6db7dd2507dfa5b25ebe5395c22b52e9",       "line": 138,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:41:56Z",       "action": "test",       "stored_prev_sha256": "019534785854009b779b9d201936f5adbc831fc40d217b3027ac564a545692ac",       "computed_prev_sha256": "5dd9638ab373fcc7a5181db4a5d819810ba4b0b1cfe55e33d8a7cd1095217b5a",       "line": 139,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T12:54:31Z",       "action": "codex_return_captured",       "stored_prev_sha256": "82f3c3582d84a71ac319e588b07fdd77f8f5a485ea9da4ce7da3be211c0f7a90",       "computed_prev_sha256": "82de9f16a6342a3f5b7b56543a022a0d0f54ec3fbd4c052855d5b797b81c058f",       "line": 145,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     },     {       "utc": "2026-08-11T13:01:05Z",       "action": "test",       "stored_prev_sha256": "e4271c1904a35c62a392a9f88d23e48e5e9a3a9a5f97e25b8f83102ac8d6f98d",       "computed_prev_sha256": "5d44e8ff83ab11f27f0c3ceeb6a8710a6a28837ea797b1d10f87bd8a696aa6b7",       "line": 146,       "reason": "The 2026-08-11 redaction. A correspondent asked for his name to be removed; the custodian agreed and the name appeared in the `note` field of the four action rows immediately preceding these breaks. Removing it changed those rows' bytes, so every following row's prev_sha256 stopped matching. The break is a CONSEQUENCE of an obligatory removal, not of a rewritten history: no action was added, removed or reordered, and REDACTION.md records the same four rows under 'record/executive/action-log.jsonl (invocation purposes) | 4'.",       "authority": "custodian (Stephen Reed), 2026-08-11 \u2014 'redact all ten outreach recipients, not just the one who complained' (63a051d, ae72dfc)",       "recorded_utc": "2026-08-12T13:15:12Z",       "recorded_under_lease": "trial-04"     }   ] } 
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6876:   323	    `control_coverage.executable_text` was built for last night, so it is reused rather than
./record/executive/codex-transcripts/20260812T134921Z-263129a0ccb2.md:6995:   155	                "tools/control_coverage.py", "tools/validate_provenance.py"),
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:335:tools/control_coverage.py:18:    C2   negative control            YES — a tool either has a case it must fail, or it does not
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:336:tools/control_coverage.py:28:a raise, or a must-reject fixture. It does **not** mean the negative control is a good one, that it
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:337:tools/control_coverage.py:71:    in tools/tests/ and because the test still references the pre-rename alias. A negative control
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:338:tools/control_coverage.py:72:    that ships with the tool is a better negative control, not an absent one. Measuring only one
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:339:tools/control_coverage.py:157:    print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:420:tools/check_claims.py:257:    """This tool's own negative control (control 2), on four populations (control 45):
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:654:`tools/control_coverage.py` found 44 tools with no case they must fail. These four were taken
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:744:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2280:tools/control_coverage.py:21:    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2281:tools/control_coverage.py:168:    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2728:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3755:* **#11** — wiring `control_coverage.py --check` into `land.py`. **Deliberately gated**: at 46%
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3762:  tools/control_coverage.py` lists them. Take gate-like tools and number-producers first; page
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3922:  "detector", "6bdbbc9; found by tools/control_coverage.py"),
./record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:4185:    94	#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:11:The source-text tripwire at [test_gate_refusals.py:256](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_refusals.py:256) contains the tool name near words such as “negative control” and “refactor before it can have one.” The 600-character heuristic in [control_coverage.py:103](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:103) therefore reports:
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:58:One additional issue before the proposed `land.py` wiring: [control_coverage.py:140](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:140) makes `--check` pass whenever rows are labelled `NONE`; it currently exits 0 with 36 uncovered tools. It is a completeness-of-classification check, not a coverage threshold gate. Its intended semantics and help text need to be settled before wiring it into landing.
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:64:Verification: `test_gate_refusals.py` passed 21/21 assertions, `control_coverage.py` reported 36 covered / 36 uncovered / 3 N/A, and the complete runner exited 0 with all 28 suites passing. The green suite does not resolve the semantic findings above.
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:87:wiring `control_coverage --check` into land.py is currently held back because coverage was too
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:137:- **I edited a test label to satisfy the coverage heuristic.** `control_coverage.py` looks for a
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:161:/bin/bash -lc 'git branch --show-current && git status --short && git diff -- tools/tests/test_gate_refusals.py tools/build_register_view.py tools/control_findings.py tools/control_coverage.py && git diff --stat' in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:211:`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:427:        #  The label names the tool. control_coverage.py looks for a refusal assertion within
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:483:-| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:484:+| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:496:-<tr><td>2</td><td>Fault-injection check profile (FICP)</td><td>☐</td><td><code>tools/verify_negative_control.py</code> <code>tools/verify_fault_injection.py</code> <code>tools/control_coverage.py</code> <code>tools/scan_own_code.py</code>&lt;br&gt;tested by <code>tools/tests/test_fault_injection.py</code> <code>tools/tests/test_gate_negative_controls.py</code> <code>tools/tests/test_publication_gates.py</code> <code>tools/tests/test_chain_guards.py</code>&lt;br&gt;<strong>Remaining:</strong> control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control&#x27;s own requirement is unmet in this codebase.&lt;br&gt;&lt;small&gt;self-application: PRACTISED&lt;/small&gt;</td></tr>
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:497:+<tr><td>2</td><td>Fault-injection check profile (FICP)</td><td>☐</td><td><code>tools/verify_negative_control.py</code> <code>tools/verify_fault_injection.py</code> <code>tools/control_coverage.py</code> <code>tools/scan_own_code.py</code>&lt;br&gt;tested by <code>tools/tests/test_fault_injection.py</code> <code>tools/tests/test_gate_negative_controls.py</code> <code>tools/tests/test_publication_gates.py</code> <code>tools/tests/test_chain_guards.py</code> <code>tools/tests/test_gate_refusals.py</code>&lt;br&gt;<strong>Remaining:</strong> Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record&#x27;s most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm&#x27;s first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.&lt;br&gt;&lt;small&gt;self-application: PRACTISED&lt;/small&gt;</td></tr>
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:509:+{"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:518:-| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:519:+| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:581:                 "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:585:-         gap="control_coverage.py reports the tools that still have no case they must fail. "
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:590:+         gap="Run tools/control_coverage.py for the current list and the current rate — the "
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:627:tools/control_coverage.py:18:    C2   negative control            YES — a tool either has a case it must fail, or it does not
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:628:tools/control_coverage.py:19:    C5   closed-world measurement    partial — needs semantics; not attempted here
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:629:tools/control_coverage.py:21:    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:630:tools/control_coverage.py:24:**Only C2 is measured here.** It is the register's flagship and the one with an unambiguous
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:631:tools/control_coverage.py:168:    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:790:record/executive/HANDOFF-2026-08-12.md:82:* **C2 coverage** — `python3 tools/control_coverage.py` lists the tools with no case they must
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:836:record/controls/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:901:docs/artifacts/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:930:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:335:tools/control_coverage.py:18:    C2   negative control            YES — a tool either has a case it must fail, or it does not
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:950:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2280:tools/control_coverage.py:21:    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:951:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2281:tools/control_coverage.py:168:    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1193:+{"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1202:-| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1203:+| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1265:                 "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1269:-         gap="control_coverage.py reports the tools that still have no case they must fail. "
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1274:+         gap="Run tools/control_coverage.py for the current list and the current rate — the "
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1290:     4	`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1506:   220	        #  The label names the tool. control_coverage.py looks for a refusal assertion within
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1602:tools/control_coverage.py
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1634:/bin/bash -lc "rg -n '"'^### Control (2|5|23|32|44|45|55|61)'"\\b|"'^## (2|5|23|32|44|45|55|61)'"\\b|"'^### (2|5|23|32|44|45|55|61)'"\\.' record/designs/candidate-control-register.md; sed -n '1,280p' tools/control_coverage.py; sed -n '1,230p' tools/check_prompt.py; sed -n '1,300p' tools/check_register.py; sed -n '1,190p' tools/build_register_view.py" in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1639:    python3 tools/control_coverage.py            # the matrix
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1640:    python3 tools/control_coverage.py --check    # refuse if any tool has no determination
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3784:    """This tool's own negative control (control 2), on four populations (control 45):
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3846:/bin/bash -lc 'python3 tools/tests/test_gate_refusals.py; test_rc=$?; echo test_gate_refusals_rc=$test_rc; python3 tools/control_coverage.py; coverage_rc=$?; echo control_coverage_rc=$coverage_rc; python3 - <<'"'PY'
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3849:import control_coverage as c
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3927:control_coverage_rc=0
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3934:**Analyzing control_coverage --check limitations**
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3936:/bin/bash -lc 'python3 tools/control_coverage.py --check; rc=$?; echo control_coverage_check_rc=$rc; git show HEAD:tools/control_coverage.py > /tmp/oagf-control-coverage-head.py && python3 /tmp/oagf-control-coverage-head.py | sed -n '"'1,12p'; rm /tmp/oagf-control-coverage-head.py" in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3939:control_coverage_check_rc=0
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3967: 2 {'scope': 'code', 'files': ('tools/verify_negative_control.py', 'tools/verify_fault_injection.py', 'tools/control_coverage.py', 'tools/scan_own_code.py'), 'tests': ('tools/tests/test_fault_injection.py', 'tools/tests/test_gate_negative_controls.py', 'tools/tests/test_publication_gates.py', 'tools/tests/test_chain_guards.py', 'tools/tests/test_gate_refusals.py'), 'gap': "Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding."}
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3977: 45 {'scope': 'code', 'files': ('tools/control_coverage.py', 'tools/executive_log.py'), 'tests': ('tools/tests/test_deploy_check.py', 'tools/tests/test_gate_negative_controls.py'), 'gap': "Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering."}
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:4235:The 21-arm suite itself is green, but several labels overclaim what passed. I’m running the complete suite now because the new file is auto-discovered; separately, the proposed future `control_coverage --check` wiring is not currently a coverage gate at all—it returns 0 with 36 uncovered tools, so that prerequisite and its help text need correction before landing it into `land.py`.
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5353:/bin/bash -lc "nl -ba tools/tests/test_gate_refusals.py | sed -n '1,275p'; nl -ba tools/control_coverage.py | sed -n '65,165p'; nl -ba tools/build_register_view.py | sed -n '96,130p;430,450p'; nl -ba tools/control_findings.py | sed -n '82,98p'; nl -ba tools/control_application.py | sed -n '105,135p'" in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5358:     4	`tools/control_coverage.py` reported 44% of tools with an assurance signal had a case they must
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5574:   220	        #  The label names the tool. control_coverage.py looks for a refusal assertion within
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5802:   111	                "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5807:   116	         gap="Run tools/control_coverage.py for the current list and the current rate — the "
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5844:The source-text tripwire at [test_gate_refusals.py:256](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_refusals.py:256) contains the tool name near words such as “negative control” and “refactor before it can have one.” The 600-character heuristic in [control_coverage.py:103](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:103) therefore reports:
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5891:One additional issue before the proposed `land.py` wiring: [control_coverage.py:140](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:140) makes `--check` pass whenever rows are labelled `NONE`; it currently exits 0 with 36 uncovered tools. It is a completeness-of-classification check, not a coverage threshold gate. Its intended semantics and help text need to be settled before wiring it into landing.
./record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5897:Verification: `test_gate_refusals.py` passed 21/21 assertions, `control_coverage.py` reported 36 covered / 36 uncovered / 3 N/A, and the complete runner exited 0 with all 28 suites passing. The green suite does not resolve the semantic findings above.
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:140: {"id": "5981c495241f0445", "disposition": "labelled", "note": "Self-limiting statement about this project's own coverage metric, and checkable in tools/control_coverage.py, whose own output says the same: a pass means SOME test asserts a refusal near a reference to the tool, and it does not establish that the control is demanding or that it targets the capability rather than the transport. The absence claimed is the absence of a measurement, not of a defect.", "by": "workbench", "utc": "2026-08-12"}
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:169: | 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:170: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:252:           files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:355:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:705:tools/control_coverage.py:7:WHY PER FILE. `tools/self_application.py` records a determination PER CONTROL — a declaration that
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:706:tools/control_coverage.py:16:44):
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:815:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1425:                "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1430:         gap="Run tools/control_coverage.py for the current list and the current rate — the "
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1673:          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1680:          files=("tools/control_coverage.py", "tools/gate_health.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1858:  "control_coverage.py, which MEASURES control 2, had both errors at once. It counted tools "
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1920:/bin/bash -lc "sed -n '250,330p' tools/self_application.py; sed -n '430,570p' tools/control_application.py; git diff -- tools/control_application.py; rg -n \"self_application.py|control_application.py\" tools/land.py tools/tests/run_all.py tools/control_coverage.py; rg -n \"matrix|table|applicability|coverage\" tools/*.py record/controls/*.md | head -200" in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2171:           files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2238:tools/control_coverage.py:7:WHY PER FILE. `tools/self_application.py` records a determination PER CONTROL — a declaration that
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2278:tools/control_application.py:111:                "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2279:tools/control_application.py:116:         gap="Run tools/control_coverage.py for the current list and the current rate — the "
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2282:tools/control_application.py:359:          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2283:tools/control_application.py:366:          files=("tools/control_coverage.py", "tools/gate_health.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2320:record/controls/control-application.md:30:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2323:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2324:record/controls/control-application.md:74:| 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3152:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3570:   359	          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3577:   366	          files=("tools/control_coverage.py", "tools/gate_health.py"),
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:82:per control 45. D-61 filed.
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:199: M record/executive/guard-baseline.json
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:288: record/executive/guard-baseline.json       | 24 +++++----
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:303:-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and the codes this repository has committed to keeping are recorded in record/executive/guard-baseline.json. Removing a guard AND its fixture together — the shape an actual rewrite takes — is refused unless the removal is recorded with a ground, which makes weakening a governed event rather than a diff nobody reads. THE FIRST VERSION HAD ONLY THE PRESENT-TIME HALF and Codex reproduced the silent case: baseline clean, delete both, still clean. WHAT REMAINS: exactly ONE tool is enrolled — eleven guards in control_application.py. Every other gate here has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:304:+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py` `tools/check_quotations.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction's limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:305: | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:376:diff --git a/record/executive/guard-baseline.json b/record/executive/guard-baseline.json
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:378:--- a/record/executive/guard-baseline.json
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:379:+++ b/record/executive/guard-baseline.json
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:610:+          "note": "Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern."
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:680:+which is control 45: a repair must still catch what the old gate caught.
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:732: | tools/control_coverage.py <br><sub>Both the report path and --check consult the survey before any number is produced.</sub> | repairable by supersession | verified |
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:747:+| tools/tests/test_gate_negative_controls.py <br><sub>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</sub> | repairable by supersession | verified |
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:868:-          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:869:+          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:877:-              "record/executive/guard-baseline.json. Removing a guard AND its fixture together "
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:885:+              "RAISES, and record/executive/guard-baseline.json holds the codes this repository "
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1533:The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1545:  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1631:    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1694:BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1707:    weakening a governed event rather than a diff nobody reads. That is the property control 45
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:1767:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:2874:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:3985:  [32m✓[0m ...and the refusal cites control 45 by what it forbids
./record/executive/codex-transcripts/20260812T082042Z-65b4ce243034.md:3989:  [32m✓[0m ...and the refusal names control 45 and what it forbids
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:26:[`control_coverage.py`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:285) has two holes:
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:46:- `control_coverage.py --check` with a broken test;
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:121:- `control_coverage.py` — it publishes a PERCENTAGE over a population, read the tests with
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:148:2. Is putting the TESTS in `control_coverage`'s population correct, or does it make an unrelated
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:161:/bin/bash -lc 'git status --short && git branch --show-current && git log -5 --oneline --decorate && git diff --stat && git diff -- tools/closed_world.py tools/scan_own_code.py tools/control_coverage.py tools/tests/test_closed_world.py' in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:171: M tools/control_coverage.py
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:189: tools/control_coverage.py                 | 50 +++++++++++++++++++++++++++++++
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:192:diff --git a/tools/control_coverage.py b/tools/control_coverage.py
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:194:--- a/tools/control_coverage.py
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:195:+++ b/tools/control_coverage.py
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:534:#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:537:COVERS = ("closed_world.py", "scan_own_code.py", "control_coverage.py")
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:630:        ("control_coverage.py", ROOT / "tests" / "test_zzqx_unparseable_probe.py",
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:661: | 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:665:+| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey returns counts only when the walk accounted for everything, and raises otherwise, so there is no path to a number past an incomplete walk. TWO population-measuring tools are routed through it and REFUSE on an unreadable file, proved by injection. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:739: | 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:743:+| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey returns counts only when the walk accounted for everything, and raises otherwise, so there is no path to a number past an incomplete walk. TWO population-measuring tools are routed through it and REFUSE on an unreadable file, proved by injection. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:814:+                "tools/control_coverage.py", "tools/validate_provenance.py"),
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:840:/bin/bash -lc "sed -n '1,340p' tools/control_coverage.py
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:849:    python3 tools/control_coverage.py            # the matrix
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:850:    python3 tools/control_coverage.py --check    # refuse if any tool has no determination
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1665:The routing confirms a second blocker: both tools emit partial numerical results before checking `Survey.result()`. `scan_own_code.py` prints every per-class hit count; `control_coverage.py` prints HAS/NONE/total counts, and its `--check` path returns before consulting the survey at all. The injection cases look only for one old summary phrase or the percentage string, so they pass while the forbidden counts remain visible.
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1691:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1692:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1693:  [32m✓[0m ...and control_coverage.py names the file it could not read
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1694:  [32m✓[0m ...and control_coverage.py publishes NO number from the partial walk
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1695:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1704:python3 tools/control_coverage.py --check
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1705:python3 tools/control_coverage.py" in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1793:rg -n \"Control 45|control 45|C45|rank.?45| 45:\" record docs tools | head -80
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1804:tools/check_claims.py:269:    """This tool's own negative control (control 2), on four populations (control 45):
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1805:tools/tests/test_executive_log.py:244:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1806:tools/tests/test_gate_negative_controls.py:271:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1807:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1811:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:420:tools/check_claims.py:257:    """This tool's own negative control (control 2), on four populations (control 45):
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1812:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:744:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1813:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2728:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1815:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:4185:    94	#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1816:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:85:The regression suite should retain four populations, following control 45:
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1821:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:2154:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1823:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4910:The regression suite should retain four populations, following control 45:
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1825:record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:1748:record/designs/candidate-control-register.md-167-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1826:record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:2055:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md-2154-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1829:record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:711:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1832:record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:1252:    """This tool's own negative control (control 2), on four populations (control 45):
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1842:record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3784:    """This tool's own negative control (control 2), on four populations (control 45):
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1848:record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3152:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1851:record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:2733:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1855:record/executive/codex-transcripts/20260812T012550Z-5a86c2022e04.md:137:   retained per control 45.
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1857:record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2445:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1858:record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2461:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1894:          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1901:          files=("tools/control_coverage.py", "tools/gate_health.py"),
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1944:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1945:docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1947:record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:1957:nl -ba tools/control_coverage.py | sed -n '220,325p'
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2492:   128	        ("control_coverage.py", ROOT / "tests" / "test_zzqx_unparseable_probe.py",
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2679:  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2680:  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2681:  [32m✓[0m ...and control_coverage.py names the file it could not read
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2682:  [32m✓[0m ...and control_coverage.py publishes NO number from the partial walk
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:2683:  [32m✓[0m ...and control_coverage.py reports again once the tree is clean
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3753: M tools/control_coverage.py
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3782:python3 -m py_compile tools/closed_world.py tools/control_coverage.py tools/scan_own_code.py tools/tests/test_closed_world.py' in /home/reed/git/open-asi-governance-forum
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3818:[`control_coverage.py`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:285) has two holes:
./record/executive/codex-transcripts/20260812T051317Z-713138137cb0.md:3838:- `control_coverage.py --check` with a broken test;
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:316:                "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:320:         gap="control_coverage.py reports the tools that still have no case they must fail. "
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:533:          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:880:| 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** control_coverage.py reports the tools that still have no case they must fail. Roughly half of them do not. Until that reaches every gate-like tool and every number-producer, the control's own requirement is unmet in this codebase. |
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:923:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering. |
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1928:tools/control_coverage.py:27:WHAT A PASS MEANS, EXACTLY. That some test referencing the tool asserts a refusal, a non-zero exit,
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1929:tools/control_coverage.py:45:#  Ways this repository's tests assert that something REFUSED. Broad on purpose: a false pass
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1930:tools/control_coverage.py:47:REFUSAL = re.compile(
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1931:tools/control_coverage.py:50:    r"refus|reject|must[_ ]fail|must[_ ]be[_ ]rejected|non-?zero|REFUSED|BROKEN|NOT FIT",
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1932:tools/control_coverage.py:81:    """A test asserting a refusal, OR the tool shipping its own must-fail cases."""
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1933:tools/control_coverage.py:108:        #  Only the portion of the test that mentions this tool, so a refusal assertion about a
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1934:tools/control_coverage.py:112:            if REFUSAL.search(window):
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1935:tools/control_coverage.py:113:                return (True, f"{name} asserts a refusal near a reference to it")
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1936:tools/control_coverage.py:114:    return (False, f"referenced by {referencing[0][0]} but no refusal is asserted near it")
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:1937:tools/control_coverage.py:163:    print("\n  A pass means SOME test asserts a refusal near a reference to the tool. It does not")
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:2733:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5432:    88	                "tools/control_coverage.py", "tools/scan_own_code.py"),
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5436:    92	         gap="control_coverage.py reports the tools that still have no case they must fail. "
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5597:   301	          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:7830:   301	          files=("tools/control_coverage.py", "tools/executive_log.py"),
./record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:463:tools/control_coverage.py:280:def main() -> int:
./record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:485:tools/control_application.py:382:          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
./record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:1135:    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
./record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:1420:record/executive/leases.jsonl:3:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:2072:{"authority": "delegated by the custodian; confers nothing on this layer", "evidence": "trial-03 ran to its ACTION bound, not its calendar bound: 200 attested actions between 2026-08-10T19:37:33Z and 2026-08-12T10:33:22Z (~39 h), refusing mid-task with no override, as designed. The calendar bound still has ~275 h left. What the window produced: publication restored after a 3.5 h outage the previous handoff had called 'not observed'; the control-23 interlock, so an observed violation opens an incident and constrains the next action; a per-control application table published to the record and the web, which ticked NOTHING on its first run; control 2 negative controls raised across the publication and chain gates; control 44 enforced in both matrices; control 5 extracted into a closed-world Survey type; control 45 mechanised as guard identity with a committed baseline, so removing a guard is a governed event; a second gate (check_quotations) enrolled; the register unfrozen on the custodian's instruction and control 64 added; ~49 commits landed, each deploy-verified. AGAINST that, and it is the more important half: SEVEN deficiencies were filed (D-57..D-63) and EVERY ONE was a defect in the checking apparatus rather than in the thing checked -- a gate that read 'not measurable' as 'measured and bad', two guards that could never fire, the closed-world type that did not enforce closure, a quotation gate that could not see a quotation that wraps (it checked zero, every run), a negative control that appended 87 rows to the ledger it was protecting while asserting refusal, and a published register telling implementers the adoptable set was empty. Three of the four gap-closing batches were caught only because Codex reproduced a counterexample; the local suites were green every time. Still open at renewal: the lease's OWN max_actions check computes the count inside a bare `except Exception: spent = 0`, so an unreadable log grants an exhausted lease unlimited actions -- reproduced, deliberately left unfixed under the refusal, to be filed as D-64 and repaired first under this lease.", "expires_utc": "2026-08-24T00:00:00Z", "granted_by": "custodian (Stephen Reed), 2026-08-12: 'Renew the lease to 400.'", "granted_utc": "2026-08-12T13:15:12Z", "lease_id": "trial-04", "max_actions": 400, "note": "max_actions raised 200 -> 400 at the custodian's direction, 2026-08-12: 'Renew the lease to 400.' The CALENDAR bound is unchanged at 2026-08-24T00:00:00Z and remains the primary limit; only the secondary count moves. Rationale for the raise rather than a reset: 200 covered ~39 hours of dense gap-closing work, and the action bound exists to catch a runaway, not to pace ordinary work -- but note this is the SECOND consecutive lease to end on the action bound rather than the date, which is itself evidence about how the two bounds are calibrated. FIRST OBLIGATION UNDER THIS LEASE: repair the fail-open in require()'s own cap (D-64) so the gate raises on an unreadable count instead of defaulting to zero.", "supersedes": "trial-03"}
./record/executive/codex-transcripts/20260812T132531Z-56e940386ca6.md:2295:4. `control_coverage.py --check` still out of `land.py` — its semantics need settling.
./record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2445:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
./record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2461:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |

**Planning precise file reading**
exec
/bin/bash -lc "sed -n '1,380p' tools/control_coverage.py && sed -n '180,290p' tools/guards.py && sed -n '1,240p' record/executive/guard-baseline.json && sed -n '1,150p' tools/land.py" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
#!/usr/bin/env python3
"""Per-file compliance with the registered controls that have a file-level surface.

    python3 tools/control_coverage.py            # the matrix
    python3 tools/control_coverage.py --check    # refuse if any tool has no determination

WHY PER FILE. `tools/self_application.py` records a determination PER CONTROL — a declaration that
control 5 is enforced *by derive_counts.py*. It says nothing about the other seventy tools, and the
"0 searches" error happened in a script that never called derive_counts. **A control enforced
somewhere is not the code complying everywhere**, and only a per-file view can show the difference.

WHICH CONTROLS HAVE A FILE-LEVEL SURFACE AT ALL. Most do not, and pretending otherwise would
manufacture 63 × 71 mostly-meaningless cells. Control 6 is not a property a file can have; control
1 needs a second key holder; the goal-graph and evolutionary families need structures this
repository lacks. Recorded here so the exclusion is an argument rather than an omission (control
44):

    C2   negative control            YES — a tool either has a case it must fail, or it does not
    C5   closed-world measurement    partial — needs semantics; not attempted here
    C10  bounded claim               partial — needs semantics; not attempted here
    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
    everything else                  NO file-level surface. System, organisational, or structural.

**Only C2 is measured here.** It is the register's flagship and the one with an unambiguous
file-level answer. The others are named as unmeasured rather than quietly dropped.

WHAT A PASS MEANS, EXACTLY. That some test referencing the tool asserts a refusal, a non-zero exit,
a raise, or a must-reject fixture. It does **not** mean the negative control is a good one, that it
targets the capability rather than the transport (N5), or that the tool is correct. A tool can hold
a demanding fixture and still be broken everywhere the fixture does not look.
"""

from __future__ import annotations

import argparse
import ast
import io
import tokenize
import pathlib
import re
import sys

REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "tools"))
import closed_world                                                    # noqa: E402
TOOLS = REPO_ROOT / "tools"
TESTS = TOOLS / "tests"

#  Ways this repository's tests assert that something REFUSED. Broad on purpose: a false pass
#  here understates the gap, and understating it is the failure mode that matters.
REFUSAL = re.compile(
    r"returncode\s*==\s*[1-9]|returncode\s*!=\s*0|rc\s*==\s*[1-9]|exit\s*[1-9]|"
    r"is False|assertRaises|pytest\.raises|except\s+\w*Error|"
    r"refus|reject|must[_ ]fail|must[_ ]be[_ ]rejected|non-?zero|REFUSED|BROKEN|NOT FIT",
    re.I)

#  Tools that are libraries or one-shot utilities with no assurance signal of their own. Each
#  needs a REASON, not a bare exemption.
NO_SIGNAL: dict[str, str] = {
    "codex_budget.py": "reads a budget number; emits no assurance signal of its own",
    "render_markdown.py": "pure formatting; no verdict to be wrong about",
    "code_freetext.py": "text extraction helper; no verdict",
}


def tool_files() -> list[pathlib.Path]:
    return sorted(p for p in TOOLS.glob("*.py") if p.name != "__init__.py")


def self_hosted_fixtures(path: pathlib.Path) -> bool:
    """Does the tool SHIP its own must-fail cases?

    Added after validation: verify_fault_injection.py carries FOURTEEN must-reject fixtures and a
    --fixtures mode, and the first version of this measure scored it NONE because it only looked
    in tools/tests/ and because the test still references the pre-rename alias. A negative control
    that ships with the tool is a better negative control, not an absent one. Measuring only one
    location produced a false NONE on the repository's flagship verifier.
    """
    text = path.read_text(encoding="utf-8", errors="replace")
    return bool(re.search(r"--fixtures|must[_ ]be[_ ]rejected|must[_ ]reject|MUST be rejected|"
                          r"must_flag|run_fixtures", text))


def executable_text(text: str) -> str:
    """The test's CODE, with its docstring and every comment removed.

    A MENTION IS NOT AN EXERCISE, and prose keeps finding new places to hide. First it was the
    module docstring: test_chain_guards.py named anchor_manifest.py in its prose and never
    exercised it, and this measure counted that as coverage — a test's own docstring gaming the
    measure of that test, accidentally, within minutes of both being written. That was stripped
    via `ast`, not a regex, because the first attempt anchored at string start and every suite
    here begins with a shebang, so it stripped nothing and the false pass survived its own fix.

    Then it was a COMMENT. On 2026-08-12 a note in test_gate_refusals.py recorded that
    arm_acceptance.py CANNOT have a negative control yet — and because the note sat beside the
    tool's name and contained the words "negative control", this scanner reported the tool as
    COVERED on the strength of the sentence saying it is not. Codex found it the same day, in
    the batch of work whose entire subject was control 2.

    So comments go too. What is left is what the test actually executes.
    """
    try:
        doc = ast.get_docstring(ast.parse(text))
        if doc:
            text = text.replace(doc, "", 1)
    except SyntaxError:
        pass
    out = []
    try:
        for token in tokenize.generate_tokens(io.StringIO(text).readline):
            if token.type == tokenize.COMMENT:
                continue
            out.append(token.string)
    except (tokenize.TokenError, IndentationError, SyntaxError):
        #  Untokenizable is UNKNOWN, not clean. Returning the raw text keeps the old (weaker)
        #  behaviour rather than silently reporting no references at all.
        return text
    return " ".join(out)


def resolve_aliases(text: str) -> str:
    """Substitute simple `NAME = "tool.py"` bindings back into their uses.

    STRIPPING COMMENTS MADE THIS NECESSARY, and finding out why is the useful part. With prose
    removed, `check_executive_context.py` — which has fifteen fixture-driven refusal cases —
    reported as UNCOVERED, because its suite binds the name once as `CTX =
    "check_executive_context.py"` and then drives everything through `CTX`. The only textual
    mention sat 600 characters away from the assertions.

    So the scanner had BOTH errors at once: it counted tools named only in prose, and missed
    tools exercised only through a variable. Removing the first exposed the second.

    This resolves the one binding shape that actually occurs here. It is still lexical, and
    Codex's standing recommendation is an explicit tool→test mapping or AST call detection —
    recorded in the module docstring rather than pretended away.
    """
    aliases: dict[str, str] = {}
    try:
        tree = ast.parse(text)
    except SyntaxError:
        return text
    for node in ast.walk(tree):
        if (isinstance(node, ast.Assign) and len(node.targets) == 1
                and isinstance(node.targets[0], ast.Name)
                and isinstance(node.value, ast.Constant)
                and isinstance(node.value.value, str)
                and node.value.value.endswith(".py")):
            aliases[node.targets[0].id] = node.value.value
    for name, tool in aliases.items():
        text = re.sub(rf"\b{re.escape(name)}\b", tool, text)
    return text


def declared_coverage(text: str) -> tuple[str, ...]:
    """A module-level `COVERS = ("tool.py", ...)` — what this suite says it exercises.

    WHY DECLARATION BEAT PROXIMITY. The lexical heuristic below had both errors at once. It
    counted tools named only in a COMMENT, and it MISSED `check_executive_context.py`, which has
    fifteen fixture-driven refusal cases — because the suite binds the name once as a constant
    and drives it through a wrapper function, so no assertion sits within 600 characters of the
    tool's name. Resolving simple aliases did not fix that, and would not: the invocation is a
    call to `state()`, not to the constant.

    A declaration is explicit, lives beside the code, and CANNOT ROT SILENTLY: a declared tool
    that does not exist fails the scan, and a declaration is not enough on its own — the file
    must still contain a refusal assertion, or a suite could claim coverage it never wrote.
    """
    try:
        tree = ast.parse(text)
    except SyntaxError:
        return ()
    for node in tree.body:
        if (isinstance(node, ast.Assign) and len(node.targets) == 1
                and isinstance(node.targets[0], ast.Name)
                and node.targets[0].id == "COVERS"
                and isinstance(node.value, (ast.Tuple, ast.List))):
            return tuple(e.value for e in node.value.elts
                         if isinstance(e, ast.Constant) and isinstance(e.value, str))
    return ()


def has_negative_control(stem: str) -> tuple[bool, str]:
    """A test asserting a refusal, OR the tool shipping its own must-fail cases."""
    own = TOOLS / f"{stem}.py"
    if own.is_file() and self_hosted_fixtures(own):
        return (True, "ships its own must-fail fixtures")
    if not TESTS.is_dir():
        return (False, "no tests directory")
    #  A DECLARATION FIRST, and only when the file also asserts a refusal somewhere. The
    #  declaration says which tool; the refusal says something was actually required to fail.
    #  Neither alone is coverage.
    for t in sorted(TESTS.rglob("*.py")):
        raw = t.read_text(encoding="utf-8", errors="replace")
        if f"{stem}.py" in declared_coverage(raw):
            if REFUSAL.search(executable_text(raw)):
                return (True, f"{t.name} declares it in COVERS and asserts a refusal")
            return (False, f"{t.name} declares it in COVERS but asserts no refusal")

    referencing = []
    for t in sorted(TESTS.rglob("*.py")):
        text = t.read_text(encoding="utf-8", errors="replace")
        #  STRIP THE MODULE DOCSTRING. test_chain_guards.py named anchor_manifest.py in its
        #  prose and never exercised it, and this measure counted that as coverage -- a test's
        #  own docstring gaming the measure of that test, accidentally, within minutes of both
        #  being written. A mention is not an exercise.
        #
        #  Via ast, not a regex: the first attempt anchored at string start and every suite here
        #  begins with a shebang, so it stripped nothing and the false pass survived the fix.
        text = resolve_aliases(executable_text(text))
        if stem in text:
            referencing.append((t.name, text))
    if not referencing:
        return (False, "no test references it")
    for name, text in referencing:
        #  Only the portion of the test that mentions this tool, so a refusal assertion about a
        #  DIFFERENT tool in the same file is not counted. Crude window, deliberately narrow.
        for m in re.finditer(re.escape(stem), text):
            window = text[max(0, m.start() - 600): m.end() + 600]
            if REFUSAL.search(window):
                return (True, f"{name} asserts a refusal near a reference to it")
    return (False, f"referenced by {referencing[0][0]} but no refusal is asserted near it")


def survey_population() -> "closed_world.Survey":
    """The walk, under control 5: a rate this cannot compute is not reported as a rate.

    This tool publishes a PERCENTAGE over a population of files, which is exactly the shape
    control 5 governs — and it read the tests by `read_text(errors="replace")` and parsed them
    with `ast`, swallowing a SyntaxError into `pass`. A test file that would not parse simply
    contributed nothing, and the percentage came out looking like a measurement.

    That is not a hypothetical for this tool in particular. It has already been wrong in both
    directions in one day: counting a tool named only in a comment, and missing one exercised
    through a wrapper. A rate from an incomplete walk is the third way it could be wrong, and
    the only one nobody would see.
    """
    survey = closed_world.Survey("tools with a negative control", scope="tools/*.py")
    for path in tool_files():
        survey.seen(path.name)
        try:
            ast.parse(path.read_text(encoding="utf-8"))
        except (SyntaxError, UnicodeDecodeError, OSError) as error:
            survey.unreadable(path.name, f"{type(error).__name__}: {error}")
            continue
        if path.name in NO_SIGNAL:
            survey.excluded(path.name, NO_SIGNAL[path.name])
            continue
        ok, _why = has_negative_control(path.stem)
        survey.accounted(path.name)
        survey.count("has a negative control" if ok else "has none")
    for t in sorted(TESTS.rglob("*.py")):
        #  THE TESTS ARE IN SCOPE TOO. The verdict for every tool depends on reading them, so a
        #  test this cannot parse makes every "NONE" unreliable, not just its own.
        survey.seen(f"tests/{t.name}")
        try:
            ast.parse(t.read_text(encoding="utf-8"))
        except (SyntaxError, UnicodeDecodeError, OSError) as error:
            survey.unreadable(f"tests/{t.name}", f"{type(error).__name__}: {error}")
            continue
        #  A test is EVIDENCE, not a member of the measured denominator. It is in scope because
        #  every tool's verdict depends on reading it — one unparseable test makes the whole NONE
        #  set unreliable, not just its own — but it is accounted separately so it never inflates
        #  the population the rate is computed over. Codex's distinction, 2026-08-12.
        survey.accounted(f"tests/{t.name}")
    return survey


def rows() -> list[tuple[str, str, str]]:
    out = []
    for path in tool_files():
        stem = path.stem
        if path.name in NO_SIGNAL:
            out.append((path.name, "NOT_APPLICABLE", NO_SIGNAL[path.name]))
            continue
        ok, why = has_negative_control(stem)
        out.append((path.name, "HAS_NEGATIVE_CONTROL" if ok else "NONE", why))
    return out


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--check", action="store_true",
                        help="exit non-zero if any tool has no determination")
    args = parser.parse_args()

    survey = survey_population()

    #  CONTROL 5, BEFORE ANY NUMBER REACHES A READER. The first version consulted the survey
    #  only just before the PERCENTAGE, having already printed HAS / NONE / NOT_APPLICABLE and a
    #  total — which are the partial population counts the control protects, with the rate
    #  merely the most quotable of them. And `--check` did not consult it at all, so the gate
    #  passed on a population it could not fully read. Both found by Codex on 2026-08-12.
    try:
        survey.result()
    except closed_world.IncompleteSurvey as refusal:
        print(survey.report())
        print(f"\n  NO COUNT AND NO RATE IS REPORTED: {refusal}", file=sys.stderr)
        return 1

    data = rows()
    has = [r for r in data if r[1] == "HAS_NEGATIVE_CONTROL"]
    none = [r for r in data if r[1] == "NONE"]
    na = [r for r in data if r[1] == "NOT_APPLICABLE"]

    if args.check:
        undetermined = [r for r in data if r[1] not in
                        ("HAS_NEGATIVE_CONTROL", "NONE", "NOT_APPLICABLE")]
        if undetermined:
            for r in undetermined:
                print(f"  no determination for {r[0]}", file=sys.stderr)
            return 1
        print(f"  every tool has a determination ({len(data)}), over a population that was "
              f"fully read.")
        return 0

    print("  CONTROL 2 PER FILE — does each tool have a case it must fail?\n")
    print(f"    HAS_NEGATIVE_CONTROL  {len(has):3d}")
    print(f"    NONE                  {len(none):3d}")
    print(f"    NOT_APPLICABLE        {len(na):3d}   (each with a stated reason)")
    print(f"    ---------------------------")
    print(f"    total                 {len(data):3d}")
    pct = len(has) / (len(has) + len(none)) if (has or none) else 0
    print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
    print("  WITHOUT ONE:")
    for name, _s, why in none[:20]:
        print(f"    {name:34} {why}")
    if len(none) > 20:
        print(f"    … {len(none) - 20} more")
    print("\n  A pass means SOME test asserts a refusal near a reference to the tool. It does not")
    print("  mean the control is demanding, that it targets the capability rather than the")
    print("  transport, or that the tool is correct. Control 2 raises the floor from 'never")
    print("  observed to fail' to 'observed to fail at least once'. This measures that floor and")
    print("  nothing above it.")
    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
    print("  every other control has no file-level surface. See the module docstring.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
            node.name for node in ast.walk(tree)
            if isinstance(node, ast.FunctionDef)
            and any(isinstance(inner, ast.Call)
                    and (getattr(inner.func, "id", "") == "expect_guard"
                         or getattr(inner.func, "attr", "") == "expect_guard")
                    for inner in ast.walk(node))}
        accepted = {"expect_guard"} | wrappers
        for node in ast.walk(tree):
            if not (isinstance(node, ast.Call) and node.args):
                continue
            name = (node.func.id if isinstance(node.func, ast.Name)
                    else node.func.attr if isinstance(node.func, ast.Attribute) else "")
            if name not in accepted:
                continue
            for arg in node.args:
                if (isinstance(arg, ast.Constant) and isinstance(arg.value, str)
                        and CODE.match(arg.value) and arg.value not in synthetic):
                    found.setdefault(arg.value, []).append(path.name)
    return found


BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"


def baseline() -> dict:
    """The guards this repository has COMMITTED to keeping, and the removals it has authorised.

    THE TEMPORAL HALF, and without it the mechanism proved nothing C45 asks for. Comparing
    current declarations against current expectations catches a guard removed while its fixture
    survives — and a real rewrite removes both, which passed silently. Codex reproduced it:
    baseline `[]`, delete guard and fixture together, `[]`.

    So the codes are committed to a file. A code in the baseline that is no longer declared is a
    REMOVAL, and a removal is refused unless it is recorded here with a ground — which makes
    weakening a governed event rather than a diff nobody reads. That is the property control 45
    is about: not that the code and its fixture agree today, but that today's gate still catches
    what yesterday's did.
    """
    if not BASELINE.is_file():
        return {"guards": {}, "authorised_removals": {}}
    return json.loads(BASELINE.read_text(encoding="utf-8"))


def stamp() -> int:
    """Record the current guards as the baseline. Deliberate, and it says what it adds."""
    doc = baseline()
    current = declared()
    added = sorted(set(current) - set(doc["guards"]))
    doc["guards"] = {code: sites[0] for code, sites in sorted(current.items())}
    BASELINE.parent.mkdir(parents=True, exist_ok=True)
    BASELINE.write_text(json.dumps(doc, indent=2, ensure_ascii=False) + "\n", encoding="utf-8")
    print(f"  baseline now holds {len(doc['guards'])} guard(s)"
          + (f"; added {', '.join(added)}" if added else "; nothing new"))
    print("  Adding a guard to the baseline is a commitment to keep catching what it catches.")
    return 0


def problems() -> list[str]:
    declared_codes, expected_codes = declared(), expected()
    out = []
    for code, sites in sorted(declared_codes.items()):
        if len(sites) > 1:
            out.append(f"{code} is declared at {len(sites)} sites ({', '.join(sites)}). One code "
                       f"per guard, or a fixture naming it cannot say which fired.")
        if code not in expected_codes:
            out.append(f"{code} at {sites[0]} is declared and NO fixture names it. An unexercised "
                       f"guard is where an unreachable one hides — three were found this week.")
    #  THE TEMPORAL CHECK. A guard in the baseline that is no longer declared has been removed,
    #  and removing both the guard and its fixture is exactly the rewrite control 45 governs.
    committed = baseline()
    for code, site in sorted(committed.get("guards", {}).items()):
        if code in declared_codes:
            continue
        ground = (committed.get("authorised_removals") or {}).get(code)
        if not ground or len(str(ground)) < 20:
            out.append(f"{code} was in the baseline (at {site}) and is DECLARED NOWHERE now. "
                       f"Removing a guard and its fixture together is the weakening control 45 "
                       f"forbids without evidence. Record it under authorised_removals with a "
                       f"ground, or restore it.")
    if not declared_codes:
        #  A registry with nothing enrolled passes every check by having nothing to check.
        out.append("no guards are declared anywhere. An empty registry is not a clean one — "
                   "--check would succeed by having nothing to look at.")

    for code, files in sorted(expected_codes.items()):
        if code not in declared_codes:
            out.append(f"{code} is named by {', '.join(files)} and is declared nowhere. Either "
                       f"the guard was removed in a rewrite — which is what control 45 forbids "
                       f"without evidence — or the fixture names a code that never existed.")
    return out


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--check", action="store_true",
                        help="exit non-zero on an unexercised guard, an orphaned expectation, "
                             "or a baseline guard that has been removed")
    parser.add_argument("--stamp", action="store_true",
                        help="commit the current guards as the baseline this repository keeps")
    args = parser.parse_args()

    if args.stamp:
        return stamp()

    found = problems()
    if found:
        for p in found:
            print(f"  ✗ {p}", file=sys.stderr)
        print(f"\n  {len(found)} problem(s). A guard nobody drives is indistinguishable from a "
              f"guard that cannot fire.", file=sys.stderr)
        return 1
{
  "guards": {
    "CA-01": "control_application.py:617",
    "CA-02": "control_application.py:624",
    "CA-03": "control_application.py:629",
    "CA-04": "control_application.py:643",
    "CA-05": "control_application.py:645",
    "CA-06": "control_application.py:650",
    "CA-07": "control_application.py:653",
    "CA-08": "control_application.py:658",
    "CA-09": "control_application.py:665",
    "CA-10": "control_application.py:669",
    "CA-11": "control_application.py:675",
    "QT-01": "check_quotations.py:278",
    "QT-02": "check_quotations.py:336",
    "QT-03": "check_quotations.py:326",
    "RS-01": "record_spend.py:195",
    "SI-01": "software_implementations.py:412",
    "SI-02": "software_implementations.py:419",
    "SI-03": "software_implementations.py:425",
    "SI-04": "software_implementations.py:433",
    "SI-05": "software_implementations.py:440",
    "SI-06": "software_implementations.py:448",
    "SI-07": "software_implementations.py:454",
    "SI-08": "software_implementations.py:462"
  },
  "authorised_removals": {
    "IB-01": {
      "removed_utc": "2026-08-12",
      "authority": "custodian (Stephen Reed), 2026-08-12: 'revert the OAGF issue processing. It will be more direct for me to paste a message from Consullo Public agents into this session for rule-compliant processing.'",
      "was": "inbox_sweep.py — the negative control on the issue query",
      "ground": "The tool these guarded is withdrawn entire. The custodian replaced the scheduled machine channel with himself as the transport, which removes the surface both guards protected rather than weakening the protection: there is no unattended query left to return a false zero and no page left to truncate. What they were catching is not now uncaught; it is not now done.",
      "what_this_does_not_cover": "Text pasted into a session is still third-party text. Nothing mechanical checks it, and no guard replaces the two withdrawn here — the custodian's judgement about what to paste is the control now, and it is not in the code."
    },
    "IB-02": {
      "removed_utc": "2026-08-12",
      "authority": "custodian (Stephen Reed), 2026-08-12: 'revert the OAGF issue processing. It will be more direct for me to paste a message from Consullo Public agents into this session for rule-compliant processing.'",
      "was": "inbox_sweep.py — a truncated comment page is not a read",
      "ground": "The tool these guarded is withdrawn entire. The custodian replaced the scheduled machine channel with himself as the transport, which removes the surface both guards protected rather than weakening the protection: there is no unattended query left to return a false zero and no page left to truncate. What they were catching is not now uncaught; it is not now done.",
      "what_this_does_not_cover": "Text pasted into a session is still third-party text. Nothing mechanical checks it, and no guard replaces the two withdrawn here — the custodian's judgement about what to paste is the control now, and it is not in the code."
    }
  }
}
#!/usr/bin/env python3
"""Run every gate, then commit, push, verify and attest — or refuse without touching git.

    python3 tools/land.py -m "the commit message"
    python3 tools/land.py -F message.txt
    python3 tools/land.py --check-only          # run the gates, change nothing

**DETERMINISTIC.** No LLM, no network beyond the push itself.

Why this exists
---------------
Landing work is five gates and four git steps in a fixed order, each individually skippable, and
skipping one is silent. The record of doing it by hand:

* `run_all.py | tail -2` printed `All 16 suites passed` from an inner suite while the runner
  exited **1**. The rule against reading a verdict from output text was already written down, in
  a file loaded at the start of every session, and `tail` is still what you reach for.
* `echo "$(basename $f) rc=$?"` reported *basename's* status. Two red suites were pushed.
* Three commits went to a round branch while `git push origin main` pushed an unchanged main and
  reported success.
* A conflicted merge was completed blind, leaving conflict markers inside `MANIFEST.sha256` and
  the anchor log — the two files that make the record's central claim checkable.
* `check_quotations.py` caught a bad quotation on 2026-08-10 only because `rebuild.py` happened
  to be run first. Nothing required it.

So the order stops being a habit and becomes a program. **Every gate's status is taken from its
own exit code**, captured directly — never parsed out of its output, which is what failed.

What it refuses on
-------------------
Any gate exiting non-zero; an expired lease; a detached HEAD; unmerged paths; conflict markers in
a governed file; a push whose commit is not reachable from `origin/main` afterwards. Refusal
happens **before** `git commit` wherever possible, so a rejected run leaves the tree exactly as it
was rather than a commit needing to be undone.

What it cannot establish
-------------------------
* **That the gates test the right thing.** All green means the checks that exist passed. The
  quotation checker verifies a quotation appears in the corpus, not that a document's argument is
  sound; `check_executive_context.py` verifies identity, not truth — it passed on a pinned file
  containing a claim already proven false.
* **That the commit message is accurate.** Nothing here reads it.
* **That this was the right work to land.**
"""

from __future__ import annotations

import argparse
import json
import os
import subprocess
import sys
import time
import urllib.error
import urllib.request
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "tools"))

import deploy_obligations as obligations                                # noqa: E402
import executive_lease as lease                                          # noqa: E402
import executive_log as ex                                              # noqa: E402

#  Ordered. `rebuild` first because it regenerates what the later checks read; the quotation
#  check runs inside it and again here, because relying on that coincidence is what happened.
#  THESE MUST BE THE GATES CI RUNS, or a green landing means nothing about whether the site
#  will deploy. On 2026-08-10 land.py reported all gates green, the push verified, and the
#  custodian was told a page was published -- while the Pages workflow FAILED on
#  `tools/test_integrity.py`, a suite land.py did not run because it does not live under
#  tools/tests/. The site kept serving the previous deploy and the page 404'd.
#
#  That is this project's own failure class inside the tool built to prevent it: a green signal
#  not causally downstream of what it certifies. "Gates green" certified the local checks; it
#  was reported as "published".
GATES = (
    ("rebuild", ["python3", "tools/rebuild.py"]),
    ("tests", ["python3", "tools/tests/run_all.py"]),
    #  Run by CI at .github/workflows step "Integrity regression suite". Slow, and worth it.
    ("integrity", ["python3", "tools/test_integrity.py"]),
    ("quotations", ["python3", "tools/check_quotations.py"]),
    #  NAMED `prose-triage`, NOT `claim-validity`, and the name is load-bearing. An external
    #  review predicted the worst failure mode of this gate before it existed: "land.py was
    #  green, therefore there were no unsupported claims." A green result here means detected
    #  candidates in CHANGED prose each received a disposition. Recall is UNKNOWN, and the tool
    #  prints that on every success so the sentence above cannot be written honestly.
    ("prose-triage", ["python3", "tools/check_claims.py"]),
    #  Control 44 turned on this repository: the self-application table must have a determination
    #  for every control in the register. It checks COMPLETENESS, not correctness -- the party
    #  that wrote the determinations is the party they describe.
    ("self-application", ["python3", "tools/self_application.py", "--check"]),
    #  The control-application table names code files and tests per control. A row pointing at a
    #  deleted or renamed file is a compliance claim about something that is not there, so this
    #  refuses rather than letting the table decay into decoration. It checks SUBSTANTIATION --
    #  that every row can be backed -- not that any control is satisfied.
    #  CONTROL 45, MECHANISED. A guard declared in the code and named by no fixture is where an
    #  unreachable one hides -- three were found in one week, each by a human happening to look.
    #  And a fixture naming a guard that no longer exists FAILS here, rather than passing because
    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
    ("guard-identity", ["python3", "tools/guards.py", "--check"]),
    ("control-application", ["python3", "tools/control_application.py", "--check"]),
    ("context-pins", ["python3", "tools/check_executive_context.py"]),
    ("lease", ["python3", "tools/executive_lease.py"]),
)

#  PATHS VERIFIED TO EXIST. Two of these were `record/deficiencies.md` and
#  `record/deficiency-register.json`, which are not files -- the real one is
#  `corpus/deficiencies.md`. The conflict-marker check silently examined nothing for them, which
#  is the same class of defect as a gate reading the wrong exit code.
GOVERNED = ("corpus/MANIFEST.sha256", "record/anchors/manifest-anchors.jsonl",
            "corpus/deficiencies.md")


REPO_SLUG = "open-asi-governance/open-asi-governance-forum"
#  540, NOT 900. The harness that drives this tool kills a call at 10 minutes, so a 15-minute
#  wait could never complete inside one: two landings were cut off mid-wait and wrote no deploy
#  attestation at all, which is half of D-58. The wait is now shorter than the ceiling, and what
#  it no longer covers is recovered rather than lost -- the push entry is an OBLIGATION, and the
#  next preflight reconciles it. Waiting longer was never the fix; making the wait resumable was.
DEPLOY_TIMEOUT_S = 540
DEPLOY_POLL_S = 20


def _api(path: str) -> dict | list | None:
    """GitHub API GET. Returns None on any failure -- the caller must treat that as UNOBSERVED."""
    token = os.environ.get("GITHUB_TOKEN")
    request = urllib.request.Request(f"https://api.github.com/repos/{REPO_SLUG}{path}")
    request.add_header("Accept", "application/vnd.github+json")
    if token:
        request.add_header("Authorization", f"Bearer {token}")
    try:
        with urllib.request.urlopen(request, timeout=30) as response:
            return json.loads(response.read())
    except Exception:                                                   # noqa: BLE001
        return None


def wait_for_deploy(sha: str) -> dict:
    """Poll the PINNED observer until the Pages run for `sha` completes, then report.

    ONE OBSERVER, NOT TWO. This function used to run its own query --
    `/actions/runs?head_sha=...&per_page=1` -- which is not restricted to the Pages workflow, the
    push event or the target branch, and would happily take a conclusion from some unrelated
    workflow and open an incident from it. The ledger's `observe()` is pinned to all three, and
    keeping a second subtly different verifier beside it is how the two drift apart.

    UNOBSERVED IS NOT SUCCESS. Every path that cannot look -- no token, API error, still running
    at the deadline -- returns observed=False, and the attestation profile refuses it. A check
    that passes when it cannot see is the exact defect it exists to catch.
    """

**Planning comprehensive debt reporting**
**Designing ratchet as integrity baseline**
**Requiring audited baseline migration on version changes**
**Planning robust gating strategy**
codex
The important distinction is already visible in the code: this would be a non-regression gate, not a coverage-completeness gate. I’m checking how many of the 41 “covered” results come from explicit declarations/self-hosted fixtures versus the 600-character lexical fallback, because that determines whether the ratchet has a stable object to preserve.
exec
/bin/bash -lc "python3 tools/control_coverage.py; python3 - <<'PY'
import sys
sys.path.insert(0, 'tools')
import control_coverage as c
from collections import Counter
rows=c.rows()
print('\\nEVIDENCE MODES')
print(Counter(('self-hosted' if 'ships its own' in why else 'declared' if 'declares it' in why else 'proximity' if 'near a reference' in why else status) for _,status,why in rows))
print('\\nHAS BY EVIDENCE')
for row in rows:
    if row[1]=='HAS_NEGATIVE_CONTROL': print('\\t'.join(row))
print('\\nNONE ALL')
for row in rows:
    if row[1]=='NONE': print('\\t'.join(row))
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 11200ms:
  CONTROL 2 PER FILE — does each tool have a case it must fail?

    HAS_NEGATIVE_CONTROL   41
    NONE                   35
    NOT_APPLICABLE          3   (each with a stated reason)
    ---------------------------
    total                  79

    54% of tools with an assurance signal have a negative control.

  WITHOUT ONE:
    agenda_activation.py               referenced by test_agenda_activation.py but no refusal is asserted near it
    agenda_cohort.py                   no test references it
    agenda_ratification.py             referenced by test_agenda_ratification.py but no refusal is asserted near it
    agenda_replacement.py              no test references it
    aggregate_captures.py              no test references it
    analyze_concurrence.py             no test references it
    arm_acceptance.py                  no test references it
    arm_profile.py                     no test references it
    author_deficiency_census.py        no test references it
    benchmark_agenda.py                no test references it
    build_bundle.py                    no test references it
    build_capture_ui.py                referenced by test_page_hash_fallback.py but no refusal is asserted near it
    build_challenge_page.py            no test references it
    build_local_rounds.py              no test references it
    build_manifest.py                  referenced by test_amendment_disposition.py but no refusal is asserted near it
    build_predictions_view.py          no test references it
    build_session_log.py               no test references it
    capture_response.py                referenced by test_disposition.py but no refusal is asserted near it
    claim_ledger.py                    no test references it
    control_findings.py                no test references it
    … 15 more

  A pass means SOME test asserts a refusal near a reference to the tool. It does not
  mean the control is demanding, that it targets the capability rather than the
  transport, or that the tool is correct. Control 2 raises the floor from 'never
  observed to fail' to 'observed to fail at least once'. This measures that floor and
  nothing above it.

  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;
  every other control has no file-level surface. See the module docstring.

EVIDENCE MODES
Counter({'NONE': 35, 'proximity': 18, 'declared': 15, 'self-hosted': 8, 'NOT_APPLICABLE': 3})

HAS BY EVIDENCE
agenda_selectors.py	HAS_NEGATIVE_CONTROL	test_agenda_ratification.py asserts a refusal near a reference to it
anchor_manifest.py	HAS_NEGATIVE_CONTROL	test_chain_guards.py asserts a refusal near a reference to it
attempt_ledger.py	HAS_NEGATIVE_CONTROL	test_attempt_ledger.py asserts a refusal near a reference to it
build_cohort_pages.py	HAS_NEGATIVE_CONTROL	test_cohort_pages.py asserts a refusal near a reference to it
build_controls_page.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
build_register_view.py	HAS_NEGATIVE_CONTROL	test_gate_refusals.py declares it in COVERS and asserts a refusal
build_round_pages.py	HAS_NEGATIVE_CONTROL	test_cohort_pages.py asserts a refusal near a reference to it
build_viewer.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
capture_gates.py	HAS_NEGATIVE_CONTROL	test_publication_gates.py declares it in COVERS and asserts a refusal
capture_lifecycle.py	HAS_NEGATIVE_CONTROL	test_disposition.py asserts a refusal near a reference to it
check_claims.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
check_executive_context.py	HAS_NEGATIVE_CONTROL	test_gate_negative_controls.py declares it in COVERS and asserts a refusal
check_page_budget.py	HAS_NEGATIVE_CONTROL	test_publication_gates.py declares it in COVERS and asserts a refusal
check_prompt.py	HAS_NEGATIVE_CONTROL	test_gate_refusals.py declares it in COVERS and asserts a refusal
check_quotations.py	HAS_NEGATIVE_CONTROL	test_gate_negative_controls.py declares it in COVERS and asserts a refusal
check_raw_append_only.py	HAS_NEGATIVE_CONTROL	test_chain_guards.py asserts a refusal near a reference to it
check_register.py	HAS_NEGATIVE_CONTROL	test_gate_refusals.py declares it in COVERS and asserts a refusal
closed_world.py	HAS_NEGATIVE_CONTROL	test_closed_world.py declares it in COVERS and asserts a refusal
codex_call.py	HAS_NEGATIVE_CONTROL	test_effect_boundary.py asserts a refusal near a reference to it
control_application.py	HAS_NEGATIVE_CONTROL	test_no_blank_cells.py declares it in COVERS and asserts a refusal
control_coverage.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
deploy_obligations.py	HAS_NEGATIVE_CONTROL	test_deploy_obligations.py declares it in COVERS and asserts a refusal
derive_counts.py	HAS_NEGATIVE_CONTROL	test_derive_counts.py asserts a refusal near a reference to it
effect_boundary.py	HAS_NEGATIVE_CONTROL	test_effect_boundary.py asserts a refusal near a reference to it
executive_lease.py	HAS_NEGATIVE_CONTROL	test_executive_log.py asserts a refusal near a reference to it
executive_log.py	HAS_NEGATIVE_CONTROL	test_deploy_check.py asserts a refusal near a reference to it
guards.py	HAS_NEGATIVE_CONTROL	test_guard_identity.py declares it in COVERS and asserts a refusal
ingest_capture.py	HAS_NEGATIVE_CONTROL	test_disposition.py asserts a refusal near a reference to it
land.py	HAS_NEGATIVE_CONTROL	test_deploy_obligations.py declares it in COVERS and asserts a refusal
reconcile_actions.py	HAS_NEGATIVE_CONTROL	test_gate_negative_controls.py declares it in COVERS and asserts a refusal
record_spend.py	HAS_NEGATIVE_CONTROL	test_gate_negative_controls.py declares it in COVERS and asserts a refusal
resolve_conflict.py	HAS_NEGATIVE_CONTROL	test_disposition.py asserts a refusal near a reference to it
resolve_held_capture.py	HAS_NEGATIVE_CONTROL	test_disposition.py asserts a refusal near a reference to it
responses_shim.py	HAS_NEGATIVE_CONTROL	test_responses_shim.py asserts a refusal near a reference to it
round_cycle.py	HAS_NEGATIVE_CONTROL	test_fetch_executor.py asserts a refusal near a reference to it
scan_own_code.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
self_application.py	HAS_NEGATIVE_CONTROL	test_no_blank_cells.py declares it in COVERS and asserts a refusal
software_implementations.py	HAS_NEGATIVE_CONTROL	test_software_implementations.py asserts a refusal near a reference to it
test_integrity.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
verify_fault_injection.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures
verify_negative_control.py	HAS_NEGATIVE_CONTROL	ships its own must-fail fixtures

NONE ALL
agenda_activation.py	NONE	referenced by test_agenda_activation.py but no refusal is asserted near it
agenda_cohort.py	NONE	no test references it
agenda_ratification.py	NONE	referenced by test_agenda_ratification.py but no refusal is asserted near it
agenda_replacement.py	NONE	no test references it
aggregate_captures.py	NONE	no test references it
analyze_concurrence.py	NONE	no test references it
arm_acceptance.py	NONE	no test references it
arm_profile.py	NONE	no test references it
author_deficiency_census.py	NONE	no test references it
benchmark_agenda.py	NONE	no test references it
build_bundle.py	NONE	no test references it
build_capture_ui.py	NONE	referenced by test_page_hash_fallback.py but no refusal is asserted near it
build_challenge_page.py	NONE	no test references it
build_local_rounds.py	NONE	no test references it
build_manifest.py	NONE	referenced by test_amendment_disposition.py but no refusal is asserted near it
build_predictions_view.py	NONE	no test references it
build_session_log.py	NONE	no test references it
capture_response.py	NONE	referenced by test_disposition.py but no refusal is asserted near it
claim_ledger.py	NONE	no test references it
control_findings.py	NONE	no test references it
fetch_executor.py	NONE	referenced by test_fetch_executor.py but no refusal is asserted near it
fetch_rates.py	NONE	no test references it
fetch_tool_mcp.py	NONE	no test references it
gate_health.py	NONE	no test references it
probe_search_capability.py	NONE	no test references it
qualification_round.py	NONE	referenced by test_qualification_round.py but no refusal is asserted near it
ratification_ballot.py	NONE	referenced by test_amendment_disposition.py but no refusal is asserted near it
rebuild.py	NONE	no test references it
round_report.py	NONE	no test references it
search_executor.py	NONE	no test references it
solicit_api.py	NONE	no test references it
solicit_local.py	NONE	no test references it
solicit_tools.py	NONE	no test references it
validate_provenance.py	NONE	referenced by test_disposition.py but no refusal is asserted near it
watch_arrivals.py	NONE	no test references it

**Detecting command timeout issue**
exec
/bin/bash -lc "python3 - <<'PY'
import sys
sys.path.insert(0, 'tools')
import control_coverage as c
from collections import Counter
rows=c.rows()
mode=lambda why: ('self-hosted' if 'ships its own' in why else 'declared' if 'declares it' in why else 'proximity' if 'near a reference' in why else 'other')
print(Counter(mode(why) for _,status,why in rows if status=='HAS_NEGATIVE_CONTROL'))
for row in rows:
    if row[1]=='HAS_NEGATIVE_CONTROL': print(f'{mode(row[2]):11} {row[0]:35} {row[2]}')
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 3716ms:
Counter({'proximity': 18, 'declared': 15, 'self-hosted': 8})
proximity   agenda_selectors.py                 test_agenda_ratification.py asserts a refusal near a reference to it
proximity   anchor_manifest.py                  test_chain_guards.py asserts a refusal near a reference to it
proximity   attempt_ledger.py                   test_attempt_ledger.py asserts a refusal near a reference to it
proximity   build_cohort_pages.py               test_cohort_pages.py asserts a refusal near a reference to it
self-hosted build_controls_page.py              ships its own must-fail fixtures
declared    build_register_view.py              test_gate_refusals.py declares it in COVERS and asserts a refusal
proximity   build_round_pages.py                test_cohort_pages.py asserts a refusal near a reference to it
self-hosted build_viewer.py                     ships its own must-fail fixtures
declared    capture_gates.py                    test_publication_gates.py declares it in COVERS and asserts a refusal
proximity   capture_lifecycle.py                test_disposition.py asserts a refusal near a reference to it
self-hosted check_claims.py                     ships its own must-fail fixtures
declared    check_executive_context.py          test_gate_negative_controls.py declares it in COVERS and asserts a refusal
declared    check_page_budget.py                test_publication_gates.py declares it in COVERS and asserts a refusal
declared    check_prompt.py                     test_gate_refusals.py declares it in COVERS and asserts a refusal
declared    check_quotations.py                 test_gate_negative_controls.py declares it in COVERS and asserts a refusal
proximity   check_raw_append_only.py            test_chain_guards.py asserts a refusal near a reference to it
declared    check_register.py                   test_gate_refusals.py declares it in COVERS and asserts a refusal
declared    closed_world.py                     test_closed_world.py declares it in COVERS and asserts a refusal
proximity   codex_call.py                       test_effect_boundary.py asserts a refusal near a reference to it
declared    control_application.py              test_no_blank_cells.py declares it in COVERS and asserts a refusal
self-hosted control_coverage.py                 ships its own must-fail fixtures
declared    deploy_obligations.py               test_deploy_obligations.py declares it in COVERS and asserts a refusal
proximity   derive_counts.py                    test_derive_counts.py asserts a refusal near a reference to it
proximity   effect_boundary.py                  test_effect_boundary.py asserts a refusal near a reference to it
proximity   executive_lease.py                  test_executive_log.py asserts a refusal near a reference to it
proximity   executive_log.py                    test_deploy_check.py asserts a refusal near a reference to it
declared    guards.py                           test_guard_identity.py declares it in COVERS and asserts a refusal
proximity   ingest_capture.py                   test_disposition.py asserts a refusal near a reference to it
declared    land.py                             test_deploy_obligations.py declares it in COVERS and asserts a refusal
declared    reconcile_actions.py                test_gate_negative_controls.py declares it in COVERS and asserts a refusal
declared    record_spend.py                     test_gate_negative_controls.py declares it in COVERS and asserts a refusal
proximity   resolve_conflict.py                 test_disposition.py asserts a refusal near a reference to it
proximity   resolve_held_capture.py             test_disposition.py asserts a refusal near a reference to it
proximity   responses_shim.py                   test_responses_shim.py asserts a refusal near a reference to it
proximity   round_cycle.py                      test_fetch_executor.py asserts a refusal near a reference to it
self-hosted scan_own_code.py                    ships its own must-fail fixtures
declared    self_application.py                 test_no_blank_cells.py declares it in COVERS and asserts a refusal
proximity   software_implementations.py         test_software_implementations.py asserts a refusal near a reference to it
self-hosted test_integrity.py                   ships its own must-fail fixtures
self-hosted verify_fault_injection.py           ships its own must-fail fixtures
self-hosted verify_negative_control.py          ships its own must-fail fixtures

**Planning heuristic conversion to manifest**
**Designing robust detector versioning system**
**Establishing explicit baseline criteria**
**Assessing self-hosted control coverage comments**
exec
/bin/bash -lc 'rg -n -- "--fixtures|must[_ ]be[_ ]rejected|must[_ ]reject|MUST be rejected|must_flag|run_fixtures" tools/{build_controls_page.py,build_viewer.py,check_claims.py,control_coverage.py,scan_own_code.py,test_integrity.py,verify_fault_injection.py,verify_negative_control.py}' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
tools/verify_negative_control.py:9:verify_negative_control` and `python3 tools/verify_negative_control.py --fixtures` both keep
tools/verify_negative_control.py:25:    report_gaps, run_fixtures, verify,
tools/control_coverage.py:74:    --fixtures mode, and the first version of this measure scored it NONE because it only looked
tools/control_coverage.py:80:    return bool(re.search(r"--fixtures|must[_ ]be[_ ]rejected|must[_ ]reject|MUST be rejected|"
tools/control_coverage.py:81:                          r"must_flag|run_fixtures", text))
tools/check_claims.py:5:    python3 tools/check_claims.py --fixtures   # this tool's own negative control
tools/check_claims.py:268:def run_fixtures() -> int:
tools/check_claims.py:279:        expected = case.get("must_flag")
tools/check_claims.py:331:    parser.add_argument("--fixtures", action="store_true", help="run this tool's negative control")
tools/check_claims.py:337:        return run_fixtures()
tools/verify_fault_injection.py:5:    python3 tools/verify_fault_injection.py --fixtures     # run the must-reject fixtures
tools/verify_fault_injection.py:21:Why this verifier ships with fixtures it must reject
tools/verify_fault_injection.py:25:forbid. `--fixtures` is this tool's negative control: eight attestations that are wrong in eight
tools/verify_fault_injection.py:26:different ways, each of which the verifier must reject for the stated reason. **A verifier that
tools/verify_fault_injection.py:279:def run_fixtures() -> int:
tools/verify_fault_injection.py:280:    """This verifier's own negative control. Every fixture MUST be rejected."""
tools/verify_fault_injection.py:287:        expected = json.loads(path.read_text(encoding="utf-8")).get("_must_be_rejected_because", "")
tools/verify_fault_injection.py:289:            print(f"  \033[31mACCEPTED\033[0m {path.name} — it must be rejected: {expected}")
tools/verify_fault_injection.py:315:    parser.add_argument("--fixtures", action="store_true",
tools/verify_fault_injection.py:316:                        help="run the fixtures this verifier must reject")
tools/verify_fault_injection.py:320:        return run_fixtures()
tools/verify_fault_injection.py:322:        parser.error("give an attestation path, or --fixtures")
tools/test_integrity.py:887:        #  under "each has a verifier, a fixture it must reject, and a recorded failure it came
tools/build_viewer.py:1028:must reject. Part A ({CONTROL_TOTALS['a']}) is adoptable alone and every control in it came from a
tools/build_viewer.py:1246:        f"- [Candidate controls](controls.html): {CONTROL_TOTALS['total']} candidate assurance controls, each with a verifier and a fixture it must reject. Part A ({CONTROL_TOTALS['a']}) is adoptable alone; {CONTROL_TOTALS['below']} sit below the eligibility line, meaning they name a real failure class but no failure that cost anything. None independently implemented.",
tools/build_controls_page.py:1815:       "required. Each has a verifier, a fixture it must reject, and a recorded failure it came "
tools/build_controls_page.py:1885:      "program that checks it and a fixture that program must reject.",
tools/build_controls_page.py:1912:      "one normative sentence, a deterministic verifier, a fixture the verifier must reject, a "
tools/build_controls_page.py:2029:              f"**Fixture it must reject.** {c['fixture']}", "",
tools/build_controls_page.py:2135:python3 tools/verify_negative_control.py --fixtures
tools/scan_own_code.py:5:    python3 tools/scan_own_code.py --fixtures   # this scanner's own negative control
tools/scan_own_code.py:29:unvalidated detector lied on its first run. `--fixtures` runs a must-flag and a must-not-flag case
tools/scan_own_code.py:231:def run_fixtures() -> int:
tools/scan_own_code.py:273:    parser.add_argument("--fixtures", action="store_true")
tools/scan_own_code.py:276:        return run_fixtures()
tools/scan_own_code.py:278:    if run_fixtures() != 0:

**Identifying false positives in control detection**
**Confirming prose-only false positives in controls**
**Noting adjacent opportunity for evidence IDs**
**Prioritizing arm_acceptance after ratchet**
**Defining tool status transition rules**
**Planning baseline update rules**
**Enforcing baseline tool commitment rules**
**Designing baseline migration handling**
**Planning baseline migration and control verification**
**Considering evidence manifest generalization**
exec
/bin/bash -lc "nl -ba tools/control_coverage.py | sed -n '40,185p'; nl -ba tools/control_coverage.py | sed -n '270,345p'; nl -ba tools/guards.py | sed -n '200,280p'; nl -ba tools/land.py | sed -n '75,115p'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
    40	import re
    41	import sys
    42	
    43	REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
    44	sys.path.insert(0, str(REPO_ROOT / "tools"))
    45	import closed_world                                                    # noqa: E402
    46	TOOLS = REPO_ROOT / "tools"
    47	TESTS = TOOLS / "tests"
    48	
    49	#  Ways this repository's tests assert that something REFUSED. Broad on purpose: a false pass
    50	#  here understates the gap, and understating it is the failure mode that matters.
    51	REFUSAL = re.compile(
    52	    r"returncode\s*==\s*[1-9]|returncode\s*!=\s*0|rc\s*==\s*[1-9]|exit\s*[1-9]|"
    53	    r"is False|assertRaises|pytest\.raises|except\s+\w*Error|"
    54	    r"refus|reject|must[_ ]fail|must[_ ]be[_ ]rejected|non-?zero|REFUSED|BROKEN|NOT FIT",
    55	    re.I)
    56	
    57	#  Tools that are libraries or one-shot utilities with no assurance signal of their own. Each
    58	#  needs a REASON, not a bare exemption.
    59	NO_SIGNAL: dict[str, str] = {
    60	    "codex_budget.py": "reads a budget number; emits no assurance signal of its own",
    61	    "render_markdown.py": "pure formatting; no verdict to be wrong about",
    62	    "code_freetext.py": "text extraction helper; no verdict",
    63	}
    64	
    65	
    66	def tool_files() -> list[pathlib.Path]:
    67	    return sorted(p for p in TOOLS.glob("*.py") if p.name != "__init__.py")
    68	
    69	
    70	def self_hosted_fixtures(path: pathlib.Path) -> bool:
    71	    """Does the tool SHIP its own must-fail cases?
    72	
    73	    Added after validation: verify_fault_injection.py carries FOURTEEN must-reject fixtures and a
    74	    --fixtures mode, and the first version of this measure scored it NONE because it only looked
    75	    in tools/tests/ and because the test still references the pre-rename alias. A negative control
    76	    that ships with the tool is a better negative control, not an absent one. Measuring only one
    77	    location produced a false NONE on the repository's flagship verifier.
    78	    """
    79	    text = path.read_text(encoding="utf-8", errors="replace")
    80	    return bool(re.search(r"--fixtures|must[_ ]be[_ ]rejected|must[_ ]reject|MUST be rejected|"
    81	                          r"must_flag|run_fixtures", text))
    82	
    83	
    84	def executable_text(text: str) -> str:
    85	    """The test's CODE, with its docstring and every comment removed.
    86	
    87	    A MENTION IS NOT AN EXERCISE, and prose keeps finding new places to hide. First it was the
    88	    module docstring: test_chain_guards.py named anchor_manifest.py in its prose and never
    89	    exercised it, and this measure counted that as coverage — a test's own docstring gaming the
    90	    measure of that test, accidentally, within minutes of both being written. That was stripped
    91	    via `ast`, not a regex, because the first attempt anchored at string start and every suite
    92	    here begins with a shebang, so it stripped nothing and the false pass survived its own fix.
    93	
    94	    Then it was a COMMENT. On 2026-08-12 a note in test_gate_refusals.py recorded that
    95	    arm_acceptance.py CANNOT have a negative control yet — and because the note sat beside the
    96	    tool's name and contained the words "negative control", this scanner reported the tool as
    97	    COVERED on the strength of the sentence saying it is not. Codex found it the same day, in
    98	    the batch of work whose entire subject was control 2.
    99	
   100	    So comments go too. What is left is what the test actually executes.
   101	    """
   102	    try:
   103	        doc = ast.get_docstring(ast.parse(text))
   104	        if doc:
   105	            text = text.replace(doc, "", 1)
   106	    except SyntaxError:
   107	        pass
   108	    out = []
   109	    try:
   110	        for token in tokenize.generate_tokens(io.StringIO(text).readline):
   111	            if token.type == tokenize.COMMENT:
   112	                continue
   113	            out.append(token.string)
   114	    except (tokenize.TokenError, IndentationError, SyntaxError):
   115	        #  Untokenizable is UNKNOWN, not clean. Returning the raw text keeps the old (weaker)
   116	        #  behaviour rather than silently reporting no references at all.
   117	        return text
   118	    return " ".join(out)
   119	
   120	
   121	def resolve_aliases(text: str) -> str:
   122	    """Substitute simple `NAME = "tool.py"` bindings back into their uses.
   123	
   124	    STRIPPING COMMENTS MADE THIS NECESSARY, and finding out why is the useful part. With prose
   125	    removed, `check_executive_context.py` — which has fifteen fixture-driven refusal cases —
   126	    reported as UNCOVERED, because its suite binds the name once as `CTX =
   127	    "check_executive_context.py"` and then drives everything through `CTX`. The only textual
   128	    mention sat 600 characters away from the assertions.
   129	
   130	    So the scanner had BOTH errors at once: it counted tools named only in prose, and missed
   131	    tools exercised only through a variable. Removing the first exposed the second.
   132	
   133	    This resolves the one binding shape that actually occurs here. It is still lexical, and
   134	    Codex's standing recommendation is an explicit tool→test mapping or AST call detection —
   135	    recorded in the module docstring rather than pretended away.
   136	    """
   137	    aliases: dict[str, str] = {}
   138	    try:
   139	        tree = ast.parse(text)
   140	    except SyntaxError:
   141	        return text
   142	    for node in ast.walk(tree):
   143	        if (isinstance(node, ast.Assign) and len(node.targets) == 1
   144	                and isinstance(node.targets[0], ast.Name)
   145	                and isinstance(node.value, ast.Constant)
   146	                and isinstance(node.value.value, str)
   147	                and node.value.value.endswith(".py")):
   148	            aliases[node.targets[0].id] = node.value.value
   149	    for name, tool in aliases.items():
   150	        text = re.sub(rf"\b{re.escape(name)}\b", tool, text)
   151	    return text
   152	
   153	
   154	def declared_coverage(text: str) -> tuple[str, ...]:
   155	    """A module-level `COVERS = ("tool.py", ...)` — what this suite says it exercises.
   156	
   157	    WHY DECLARATION BEAT PROXIMITY. The lexical heuristic below had both errors at once. It
   158	    counted tools named only in a COMMENT, and it MISSED `check_executive_context.py`, which has
   159	    fifteen fixture-driven refusal cases — because the suite binds the name once as a constant
   160	    and drives it through a wrapper function, so no assertion sits within 600 characters of the
   161	    tool's name. Resolving simple aliases did not fix that, and would not: the invocation is a
   162	    call to `state()`, not to the constant.
   163	
   164	    A declaration is explicit, lives beside the code, and CANNOT ROT SILENTLY: a declared tool
   165	    that does not exist fails the scan, and a declaration is not enough on its own — the file
   166	    must still contain a refusal assertion, or a suite could claim coverage it never wrote.
   167	    """
   168	    try:
   169	        tree = ast.parse(text)
   170	    except SyntaxError:
   171	        return ()
   172	    for node in tree.body:
   173	        if (isinstance(node, ast.Assign) and len(node.targets) == 1
   174	                and isinstance(node.targets[0], ast.Name)
   175	                and node.targets[0].id == "COVERS"
   176	                and isinstance(node.value, (ast.Tuple, ast.List))):
   177	            return tuple(e.value for e in node.value.elts
   178	                         if isinstance(e, ast.Constant) and isinstance(e.value, str))
   179	    return ()
   180	
   181	
   182	def has_negative_control(stem: str) -> tuple[bool, str]:
   183	    """A test asserting a refusal, OR the tool shipping its own must-fail cases."""
   184	    own = TOOLS / f"{stem}.py"
   185	    if own.is_file() and self_hosted_fixtures(own):
   270	    for path in tool_files():
   271	        stem = path.stem
   272	        if path.name in NO_SIGNAL:
   273	            out.append((path.name, "NOT_APPLICABLE", NO_SIGNAL[path.name]))
   274	            continue
   275	        ok, why = has_negative_control(stem)
   276	        out.append((path.name, "HAS_NEGATIVE_CONTROL" if ok else "NONE", why))
   277	    return out
   278	
   279	
   280	def main() -> int:
   281	    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
   282	    parser.add_argument("--check", action="store_true",
   283	                        help="exit non-zero if any tool has no determination")
   284	    args = parser.parse_args()
   285	
   286	    survey = survey_population()
   287	
   288	    #  CONTROL 5, BEFORE ANY NUMBER REACHES A READER. The first version consulted the survey
   289	    #  only just before the PERCENTAGE, having already printed HAS / NONE / NOT_APPLICABLE and a
   290	    #  total — which are the partial population counts the control protects, with the rate
   291	    #  merely the most quotable of them. And `--check` did not consult it at all, so the gate
   292	    #  passed on a population it could not fully read. Both found by Codex on 2026-08-12.
   293	    try:
   294	        survey.result()
   295	    except closed_world.IncompleteSurvey as refusal:
   296	        print(survey.report())
   297	        print(f"\n  NO COUNT AND NO RATE IS REPORTED: {refusal}", file=sys.stderr)
   298	        return 1
   299	
   300	    data = rows()
   301	    has = [r for r in data if r[1] == "HAS_NEGATIVE_CONTROL"]
   302	    none = [r for r in data if r[1] == "NONE"]
   303	    na = [r for r in data if r[1] == "NOT_APPLICABLE"]
   304	
   305	    if args.check:
   306	        undetermined = [r for r in data if r[1] not in
   307	                        ("HAS_NEGATIVE_CONTROL", "NONE", "NOT_APPLICABLE")]
   308	        if undetermined:
   309	            for r in undetermined:
   310	                print(f"  no determination for {r[0]}", file=sys.stderr)
   311	            return 1
   312	        print(f"  every tool has a determination ({len(data)}), over a population that was "
   313	              f"fully read.")
   314	        return 0
   315	
   316	    print("  CONTROL 2 PER FILE — does each tool have a case it must fail?\n")
   317	    print(f"    HAS_NEGATIVE_CONTROL  {len(has):3d}")
   318	    print(f"    NONE                  {len(none):3d}")
   319	    print(f"    NOT_APPLICABLE        {len(na):3d}   (each with a stated reason)")
   320	    print(f"    ---------------------------")
   321	    print(f"    total                 {len(data):3d}")
   322	    pct = len(has) / (len(has) + len(none)) if (has or none) else 0
   323	    print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
   324	    print("  WITHOUT ONE:")
   325	    for name, _s, why in none[:20]:
   326	        print(f"    {name:34} {why}")
   327	    if len(none) > 20:
   328	        print(f"    … {len(none) - 20} more")
   329	    print("\n  A pass means SOME test asserts a refusal near a reference to the tool. It does not")
   330	    print("  mean the control is demanding, that it targets the capability rather than the")
   331	    print("  transport, or that the tool is correct. Control 2 raises the floor from 'never")
   332	    print("  observed to fail' to 'observed to fail at least once'. This measures that floor and")
   333	    print("  nothing above it.")
   334	    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
   335	    print("  every other control has no file-level surface. See the module docstring.")
   336	    return 0
   337	
   338	
   339	if __name__ == "__main__":
   340	    raise SystemExit(main())
   200	
   201	BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"
   202	
   203	
   204	def baseline() -> dict:
   205	    """The guards this repository has COMMITTED to keeping, and the removals it has authorised.
   206	
   207	    THE TEMPORAL HALF, and without it the mechanism proved nothing C45 asks for. Comparing
   208	    current declarations against current expectations catches a guard removed while its fixture
   209	    survives — and a real rewrite removes both, which passed silently. Codex reproduced it:
   210	    baseline `[]`, delete guard and fixture together, `[]`.
   211	
   212	    So the codes are committed to a file. A code in the baseline that is no longer declared is a
   213	    REMOVAL, and a removal is refused unless it is recorded here with a ground — which makes
   214	    weakening a governed event rather than a diff nobody reads. That is the property control 45
   215	    is about: not that the code and its fixture agree today, but that today's gate still catches
   216	    what yesterday's did.
   217	    """
   218	    if not BASELINE.is_file():
   219	        return {"guards": {}, "authorised_removals": {}}
   220	    return json.loads(BASELINE.read_text(encoding="utf-8"))
   221	
   222	
   223	def stamp() -> int:
   224	    """Record the current guards as the baseline. Deliberate, and it says what it adds."""
   225	    doc = baseline()
   226	    current = declared()
   227	    added = sorted(set(current) - set(doc["guards"]))
   228	    doc["guards"] = {code: sites[0] for code, sites in sorted(current.items())}
   229	    BASELINE.parent.mkdir(parents=True, exist_ok=True)
   230	    BASELINE.write_text(json.dumps(doc, indent=2, ensure_ascii=False) + "\n", encoding="utf-8")
   231	    print(f"  baseline now holds {len(doc['guards'])} guard(s)"
   232	          + (f"; added {', '.join(added)}" if added else "; nothing new"))
   233	    print("  Adding a guard to the baseline is a commitment to keep catching what it catches.")
   234	    return 0
   235	
   236	
   237	def problems() -> list[str]:
   238	    declared_codes, expected_codes = declared(), expected()
   239	    out = []
   240	    for code, sites in sorted(declared_codes.items()):
   241	        if len(sites) > 1:
   242	            out.append(f"{code} is declared at {len(sites)} sites ({', '.join(sites)}). One code "
   243	                       f"per guard, or a fixture naming it cannot say which fired.")
   244	        if code not in expected_codes:
   245	            out.append(f"{code} at {sites[0]} is declared and NO fixture names it. An unexercised "
   246	                       f"guard is where an unreachable one hides — three were found this week.")
   247	    #  THE TEMPORAL CHECK. A guard in the baseline that is no longer declared has been removed,
   248	    #  and removing both the guard and its fixture is exactly the rewrite control 45 governs.
   249	    committed = baseline()
   250	    for code, site in sorted(committed.get("guards", {}).items()):
   251	        if code in declared_codes:
   252	            continue
   253	        ground = (committed.get("authorised_removals") or {}).get(code)
   254	        if not ground or len(str(ground)) < 20:
   255	            out.append(f"{code} was in the baseline (at {site}) and is DECLARED NOWHERE now. "
   256	                       f"Removing a guard and its fixture together is the weakening control 45 "
   257	                       f"forbids without evidence. Record it under authorised_removals with a "
   258	                       f"ground, or restore it.")
   259	    if not declared_codes:
   260	        #  A registry with nothing enrolled passes every check by having nothing to check.
   261	        out.append("no guards are declared anywhere. An empty registry is not a clean one — "
   262	                   "--check would succeed by having nothing to look at.")
   263	
   264	    for code, files in sorted(expected_codes.items()):
   265	        if code not in declared_codes:
   266	            out.append(f"{code} is named by {', '.join(files)} and is declared nowhere. Either "
   267	                       f"the guard was removed in a rewrite — which is what control 45 forbids "
   268	                       f"without evidence — or the fixture names a code that never existed.")
   269	    return out
   270	
   271	
   272	def main() -> int:
   273	    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
   274	    parser.add_argument("--check", action="store_true",
   275	                        help="exit non-zero on an unexercised guard, an orphaned expectation, "
   276	                             "or a baseline guard that has been removed")
   277	    parser.add_argument("--stamp", action="store_true",
   278	                        help="commit the current guards as the baseline this repository keeps")
   279	    args = parser.parse_args()
   280	
    75	#  was reported as "published".
    76	GATES = (
    77	    ("rebuild", ["python3", "tools/rebuild.py"]),
    78	    ("tests", ["python3", "tools/tests/run_all.py"]),
    79	    #  Run by CI at .github/workflows step "Integrity regression suite". Slow, and worth it.
    80	    ("integrity", ["python3", "tools/test_integrity.py"]),
    81	    ("quotations", ["python3", "tools/check_quotations.py"]),
    82	    #  NAMED `prose-triage`, NOT `claim-validity`, and the name is load-bearing. An external
    83	    #  review predicted the worst failure mode of this gate before it existed: "land.py was
    84	    #  green, therefore there were no unsupported claims." A green result here means detected
    85	    #  candidates in CHANGED prose each received a disposition. Recall is UNKNOWN, and the tool
    86	    #  prints that on every success so the sentence above cannot be written honestly.
    87	    ("prose-triage", ["python3", "tools/check_claims.py"]),
    88	    #  Control 44 turned on this repository: the self-application table must have a determination
    89	    #  for every control in the register. It checks COMPLETENESS, not correctness -- the party
    90	    #  that wrote the determinations is the party they describe.
    91	    ("self-application", ["python3", "tools/self_application.py", "--check"]),
    92	    #  The control-application table names code files and tests per control. A row pointing at a
    93	    #  deleted or renamed file is a compliance claim about something that is not there, so this
    94	    #  refuses rather than letting the table decay into decoration. It checks SUBSTANTIATION --
    95	    #  that every row can be backed -- not that any control is satisfied.
    96	    #  CONTROL 45, MECHANISED. A guard declared in the code and named by no fixture is where an
    97	    #  unreachable one hides -- three were found in one week, each by a human happening to look.
    98	    #  And a fixture naming a guard that no longer exists FAILS here, rather than passing because
    99	    #  a neighbouring guard fired, which is what control 45 forbids without evidence.
   100	    ("guard-identity", ["python3", "tools/guards.py", "--check"]),
   101	    ("control-application", ["python3", "tools/control_application.py", "--check"]),
   102	    ("context-pins", ["python3", "tools/check_executive_context.py"]),
   103	    ("lease", ["python3", "tools/executive_lease.py"]),
   104	)
   105	
   106	#  PATHS VERIFIED TO EXIST. Two of these were `record/deficiencies.md` and
   107	#  `record/deficiency-register.json`, which are not files -- the real one is
   108	#  `corpus/deficiencies.md`. The conflict-marker check silently examined nothing for them, which
   109	#  is the same class of defect as a gate reading the wrong exit code.
   110	GOVERNED = ("corpus/MANIFEST.sha256", "record/anchors/manifest-anchors.jsonl",
   111	            "corpus/deficiencies.md")
   112	
   113	
   114	REPO_SLUG = "open-asi-governance/open-asi-governance-forum"
   115	#  540, NOT 900. The harness that drives this tool kills a call at 10 minutes, so a 15-minute

**Planning two-stage ratchet build**
**Identifying false positives in control coverage**
**Designing explicit negative control declarations**
**Updating baseline with ratchet migration**
**Defining test cases and control coverage**
codex
Decision: build the ratchet, but do not baseline or gate the current detector. First replace its lexical evidence with explicit declarations; then add a narrowly named non-regression gate.

**A. Ratchet or permanent 54%?**

A ratchet is the right safety property, but it provides no progress property. Do not describe green as “control coverage passed.” Name it `negative-control-ratchet`: green means only “no known coverage was lost and no new debt entered.”

Make stagnation visible by:

- Recording and publishing every `NONE` by name, without the current 20-item truncation.
- Printing on every successful landing: `NON-REGRESSION ONLY — N legacy tools remain without a negative control`.
- Recording changes over time, so unchanged debt is visible.
- Requiring every `NONE → HAS` improvement to update the baseline immediately; otherwise the improvement is not locked in.

I would not put decay dates in the landing gate. An expiry converts old debt into an arbitrary future work stoppage and encourages waivers. If the custodian wants progress, establish a separate, explicit reduction schedule. The ratchet must not pretend to supply one.

**B. Escape hatch**

Do not permit ordinary `HAS → NONE` entries under `authorised_regressions`.

Allow only:

1. The tool is withdrawn entirely; or
2. Its assurance signal is genuinely removed, so it becomes `NOT_APPLICABLE` with a reviewed reason.

Both should record authority, ground, prior protection, and what is no longer protected. A tool that continues emitting an assurance signal but loses its negative control is a real regression, not an exemption.

If an emergency waiver is ever necessary, it should be custodian-authorized, expiring, and reported as degraded—not converted into a green baseline. I would omit that mechanism from v1.

**C. Heuristic detector**

The present detector is not fit to become a landing gate. This is stronger than the two historical mistakes: it is wrong now.

Eight results are classified as self-hosted, but [`self_hosted_fixtures()`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:70) searches the entire source—including prose—for words such as `--fixtures` and `must reject`. Consequently:

- `control_coverage.py` counts itself because its detector’s own regex contains `--fixtures`.
- `build_controls_page.py`, `build_viewer.py`, and `test_integrity.py` are counted from generated prose or commentary, not their own fixture runners.

Another 18 of the reported 41 depend on the 600-character proximity heuristic. So the proposed baseline would give temporal force to determinations already known to be unstable.

Replace proximity and prose matching with explicit machine-readable evidence: tool → test suite → negative-case identifier, plus explicit self-hosted fixture declarations. Then version the detector contract.

Yes, the baseline should record `schema_version` and `detector_contract_version`. A version mismatch must refuse and require a reviewed migration diff. Detector corrections belong under `detector_migrations`, not `authorised_regressions`; correcting a false `HAS` to `NONE` is a measurement correction, not a weakening of the tool.

**D. Priority**

This dominates the remaining actionable items—but the next task is detector hardening, not wiring the current check into `land.py`.

- Per-control publication is already done.
- SEO/indexing may improve reach but not record integrity.
- Outreach requires custodian authorization.
- A trustworthy ratchet directly addresses the repository’s dominant failure class and costs no routed round.

**What I would build**

1. Explicit negative-control evidence declarations; eliminate lexical fallback.
2. A complete baseline containing every tool, status, evidence or N/A reason, and detector version.
3. Validated transitions:
   - `NONE → HAS`: allowed and must update the baseline.
   - new tool: only `HAS` or reasoned `NOT_APPLICABLE`.
   - `HAS → NONE`: forbidden.
   - removal or `HAS → NOT_APPLICABLE`: authority and ground required.
4. Tests for every transition, detector-version mismatch, comment/docstring false evidence, and the detector’s self-match.
5. Only then add `negative-control-ratchet` to [`land.py`](/home/reed/git/open-asi-governance-forum/tools/land.py:76), with an explicitly limited success message.

**What I would refuse to build**

- A baseline stamped from the current 41/35/3 determinations.
- The current `--check` wired into `land.py`.
- Permanent, self-authorized `HAS → NONE` exceptions.
- A gate called “coverage” whose green result means only population completeness or non-regression.
- A decay deadline masquerading as a progress policy.

The reusable opportunity is the explicit evidence mapping: once proven for control 2, the same structure could support controls 5, 10, and 53. I would not generalize it until this narrower ratchet survives its own negative controls.
tokens used
49,546
