# Handoff, 2026-08-12 — read this before working in this repository

Written for a fresh session with no conversational memory. Everything load-bearing is in the repo;
this file says where.

## What changed since the 2026-08-11 handoff, and why it matters most

**Publication had been broken for three and a half hours and nothing had noticed.** The previous
handoff said the deploy for `2b57b57` "was not observed". It had in fact FAILED, along with seven
other commits — and `land.py` had attested six of those failures correctly while eight ordinary
landings proceeded anyway.

That is **D-58**, and it has two halves:

1. `check_executive_context.py` treated an absent live file as DRIFT. Two of its three pinned
   files are absolute paths under the operator's home directory, so in CI it reported a dimension
   it could not measure as one it had measured and found bad. It now has four states carried by
   the exit code — 0 verified, 1 contradicted, 2 the checker is broken, 3 coverage incomplete —
   and each pin declares a locator KIND. `land.py` admits only 0; the workflow admits 3
   explicitly, where a reader can see the policy.
2. **The instrument was not blind; nothing read it.** `tools/deploy_obligations.py` now models a
   verified push as an OBLIGATION that must be discharged, and an undischarged one refuses the
   next ordinary landing. Run `python3 tools/deploy_obligations.py --status` before assuming
   anything about publication.

The general lesson, which is worth more than either fix: **applying control 2 to a gate that had
never failed is what broke publication.** A check never observed to fail may be holding an
assumption only one machine satisfies.

## State

`HEAD == origin/main == d9bb9ff`, deploy observed and confirmed against the live site. Do not
take that from this file — it is exactly the sentence that was wrong last time. Verify:

    source /home/reed/environment-secrets-agi-agents.sh && export GITHUB_TOKEN=$GH_TOKEN_OAGF
    python3 tools/deploy_obligations.py --status

Six historical incidents are on record, all resolved by one recovery artifact. Zero open.

## Operational gotchas that cost real time

* **`GITHUB_TOKEN` must be set in the SAME bash call.** Shell state does not carry between tool
  invocations. `source /home/reed/environment-secrets-agi-agents.sh` then
  `export GITHUB_TOKEN=$GH_TOKEN_OAGF`, then the command. `deploy_obligations.observe` and its
  `_api` both read `os.environ.get("GITHUB_TOKEN")` — grep them. They briefly also accepted
  `GH_TOKEN_OAGF`, which meant a test that removed `GITHUB_TOKEN` to exercise the no-token path
  had not actually removed the credential, and the waiter polled for nine minutes.
* **No backticks in commit messages fed through a heredoc.** They trigger command substitution.
* **Bash calls are killed at 10 minutes.** `land.py` now waits 540s for the deploy, under that
  ceiling, and what it no longer covers is recovered by the obligation ledger rather than lost.
  Landing still takes several minutes of gates — run it with `run_in_background: true`.
* **Never pipe `land.py` into `tail`.** A pipeline's exit status is the LAST command's, so
  `land.py ... | tail -6` reports `tail`'s zero however land.py exited. That happened on
  2026-08-12: a background landing reported exit code 0 while `prose-triage` had failed and
  nothing was committed. It was caught only by checking `git log` afterwards. This is the
  repository's own catalogued defect — a status read from the wrong command — committed inside
  the session that spent the day on that defect class. Read `git rev-parse HEAD` and
  `origin/main`, always.
* **`land.py` is the only sanctioned way to land.** Eight gates.

## What is waiting on other people — do not chase

| | |
|---|---|
| `singnet/snet-daemon#694` | filed 2026-08-11. `record/outreach/POLL-snet-daemon-694.md` |
| the nine unasked outreach recipients | custodian's decision: **do not contact unsolicited**. If one replies, the disclosure goes out BEFORE their reply is recorded — trigger at the top of `record/outreach/POLL.md` |
| P-0031, P-0034 | the two mothball-checkpoint limbs, resolving 2026-10-05 |
| the AgentBuilder measurement | handed to another session |

Cron jobs are **session-only and expire after 7 days**. The durable mechanism is the
`record/outreach/POLL*.md` files plus `tools/watch_arrivals.py`.

## Open tasks

* **#10** — C5 and C10 remain unmeasured. C53 is done. My assessment: **C10 is probably not
  mechanizable** — its own false-negative clause rejects keyword matching.
* **#11** — wiring `control_coverage.py --check` into `land.py`. **Deliberately gated**: at 46%
  coverage a gate blocks work without improving anything, and one that fires constantly gets
  bypassed — which the 88% Codex override rate demonstrates.
* **#12, #13** — SEO/indexing research, and implementer research for a second outreach. Both
  permitted; **approaching anyone is not** without the custodian.
* **C2 coverage** — `python3 tools/control_coverage.py` lists the tools with no case they must
  fail. Take gate-like tools and number-producers first.

## The unlanded decision

`tools/control_findings.py` records 26 defects with how-found. The custodian raised **publishing
it as a page for implementers** and it is not done. It is a good idea and it is an assurance claim
under control 10 — its bounding needs writing before it goes out, not after. Note that **an
illustration is a claim no gate can read**: `prose-triage` scans text only.

## Three things a fresh session will be tempted to get wrong

1. **The Codex floor expires 2026-08-17.** After that there is **no Codex spend control at all**
   unless the custodian replaces one. Recorded in `tools/codex_call.py`,
   `record/executive/spend-policy.json` and the tests so it cannot quietly outlive its window.
2. **195 unique cloners against 2 unique human visitors.** Never report clone counts as interest.
3. **Do not skip the Codex design review.** On 2026-08-12 it rejected the design I had drafted,
   then found four blocking defects in the implementation, then two more in the re-review — every
   one of them reproduced, and every one past eight green gates and 63 passing tests. Green gates
   were real and insufficient. The rule is in `CLAUDE.md`; the evidence is D-58.

## Where the durable reasoning lives

`record/designs/` — mothball-checkpoint, witnessed-control-challenge, correspondent-disclosure,
implementer-disclosure, external-ingredient-routes, candidate-control-register.
`record/outreach/REDACTION.md` — the privacy failure and the custodian's decisions.
`record/findings/` — the pilot, the prior-art search, the NCP application.
`tools/self_application.py` — where this repository stands against its own 63 controls.
`tools/control_findings.py` — what each control actually found here, with how-found.
`corpus/deficiencies.md` D-58 — the fullest worked example this record has of a control finding a
defect in the control system itself.
