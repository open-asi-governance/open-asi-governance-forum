<!-- purpose: review the second guard enrolment and the wrapped-quotation repair -->
<!-- prompt_sha256: 65b4ce243034a392090166b09586a048152366814d557bb290423b2272e789e7 -->
<!-- exit: 0 -->

Not ready to land yet. The quotation repair is directionally right and the guard adapter is appropriate, but I found three landing blockers.

1. The negative-control suite mutates the real spend ledger and passes anyway. [test_gate_negative_controls.py:381](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_negative_controls.py:381) invokes a nonexistent cohort; [record_spend.py:187](/home/reed/git/open-asi-governance-forum/tools/record_spend.py:187) accepts it and appends a zero-unit entry. The assertion uses `returncode != 0 or not emitted_cost`, so exit 0 with `usd=None` passes. HEAD already contains 84 such entries; the worktree contains 87. Fix this before landing:

   - Refuse nonexistent cohorts before `append()`.
   - Require nonzero exit and byte-identical ledger in the fixture.
   - Run against a temporary ledger or monkeypatched path.
   - Remove or correct the three new worktree entries; the 84 committed entries need an attached correction under the record rules.

   I removed the one additional row created by my review run and left the prior three untouched.

2. C45 regressed for D-B. [scan_own_code.py:204](/home/reed/git/open-asi-governance-forum/tools/scan_own_code.py:204) replaces the old `allowed = True; if allowed:` must-flag fixture with `if True:`. The implementation still detects both, but the old behavior is no longer protected. Retain both positive arms.

3. The wrapped blockquote arm does not test the repaired regex. [test_gate_negative_controls.py:330](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_negative_controls.py:330) contains no quote delimiters, so it exercises the pre-existing `blockquotes()` path. Keep it as a blockquote regression, but add a wrapped curly-quote arm; the new regex currently has coverage only for wrapped straight quotes.

On your questions:

1. The blank-line bound is reasonable as a conservative parsing boundary, but the prose overstates it. [check_quotations.py:124](/home/reed/git/open-asi-governance-forum/tools/check_quotations.py:124) permits any number of nonblank wrapped lines, not “a single newline.” It also excludes legitimate multi-paragraph quotations. The 4,000-character cap silently makes overlong quotations invisible: four current candidates are 5,350 characters, although none is presently attributed. Prefer treating an attributed over-cap/unclosed quotation as an explicit refusal instead of silently skipping it. Also add boundary fixtures for 4,000 and 4,001 characters.

2. Do not say “one order of magnitude better than zero.” The measurable result is:

   - Candidates: 235 → 350.
   - Attribution matches: 1 → 4.
   - Non-exempt quotations actually checked: 0 → 1.

   “Candidate recall materially improved; attribution recall remains demonstrably low” is honest. A cheap next improvement is strict suffix attribution: `“…quotation…” — Claude` and `“…quotation…” (Grok, sample …)`. Those forms occur repeatedly. Also consider the prefix form `Raised by Gemini:`. I found 51 candidates with a nearby party name but no recognized attribution; they are not all true attributions, but several are unmistakable suffix citations.

3. Adapting the fixture is correct. `expect_guard()` consumes normalized refusal messages; splitting process output into messages is a transport adapter, not evidence that `guards.py` is wrongly modeled. I would revise the C45 gap language accordingly rather than generalizing `guards.py` around subprocess output.

4. Besides the two vacuous/regressed arms above, the blank-line arm is sound: its fabricated text would fail if swallowed. QT-01 and QT-02 are genuinely driven.

5. C61 should follow only after these are repaired and the ledger side effect is recorded. The ledger discovery has higher priority because the test suite currently corrupts the artifact it purports to protect.

Relevant targeted checks otherwise passed: guard identity 32/32, quotation checker 13/13, gate controls 36/36, scanner fixtures 6/6, compilation, and `git diff --check`. I did not treat the full suite as a valid clean signal because its spend test is non-idempotent.
Reading additional input from stdin...
OpenAI Codex v0.147.0
--------
workdir: /home/reed/git/open-asi-governance-forum
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: detailed
session id: 019ff50f-3a23-73a1-b8c6-9fe2a076937a
--------
user
# Review before landing: enrolling a second gate, and what it found

You said after batch four: enrol one structurally different gate to test that the abstraction
generalises, before C61. This is that.

I enrolled `check_quotations.py` — it refuses by PRINTING and exiting, where the one enrolled
tool returns a list of refusal strings. Two guards named: QT-01 (corpus empty or unreadable),
QT-02 (an attributed quotation not in corpus/).

## The finding, which is not about the abstraction

**The gate could not see a quotation that wraps.** Both patterns were `[^"\n]{40,}` — a newline
was excluded. Every design document here is wrapped markdown at ~98 columns, so a quotation of
forty characters or more usually spans a line break and was never a candidate.

Measured over the 98 files it scans, before the repair: **235 candidates, ONE attributed to a
party, and that one exempted by a CORRECTION marker. Zero quotations checked, every run.** The
record's own verbatim party amendment — the tombstone clause adopted from claude's ratification-02
sample, quoted in the pinned governing instructions — was among the invisible ones.

This gate exists because of D-53: a fabricated party quotation in a design document. A fabrication
of any substance wraps. It could not have caught the next one.

**And its negative control passed the whole time**, because I wrote the planted fixture on a
single line — the only shape the gate could see. The fault was injected in the form that works
and never in the form that occurs.

Repaired: a single newline is admitted, a blank line is not, so an unclosed quote cannot swallow
a document. `normalise()` already folded whitespace. After: 350 candidates, 4 attributions, and
the amendment is checked for the first time. Single-line arms RETAINED alongside wrapped ones,
per control 45. D-61 filed.

Two smaller ones from reading the same file: a literal `if True:` wrapping the whole quotation
loop, and `scan_own_code.py`'s D-B reported nothing for it — on the real file AND on a direct
fixture — because it matched a name bound to a bool, not a bool. Both fixed; the literal is now
D-B's must-flag fixture.

## What the enrolment said about the abstraction

`expect_guard(problems, code)` assumes an in-process list. `check_quotations.py` prints and exits,
so its fixture splits output into lines before asking. The wrapper still delegates, which the
registry requires. **The model is shaped around one gate's return convention and the second gate
was adapted to it rather than the reverse.** I have recorded that in the C45 gap rather than
calling the enrolment clean.

## Questions

1. Is the pattern repair right? Specifically the blank-line bound and the 4000-character cap —
   is there a shape that now over-matches, or one still invisible?
2. **The recall claim I am NOT making.** Four attributions out of 350 candidates. Attribution
   requires a party name followed by a speech verb within 240 characters, and this record's
   commonest citation form is a parenthetical provenance — `(claude, ratification-02 sample 2,
   verbatim)` — with no verb. Is the honest position "one order of magnitude better than zero,
   still probably low recall", or is there a cheap improvement I am missing?
3. Does the printing-vs-returning adaptation warrant changing `guards.py`, or is adapting the
   fixture the right call?
4. Anything vacuous in the new arms.
5. Next: C61?

**Reviewing branch diff and status**
codex
I’ll review this as a pre-landing audit: inspect the exact diff, trace both scanners and every new fixture, then run the smallest discriminating tests. I’ll keep the pattern/recall question separate from whether C45’s abstraction generalized.
exec
/bin/bash -lc "git status --short && git branch --show-current && git diff --stat && git diff -- tools/check_quotations.py tools/scan_own_code.py tools/guards.py tests record | sed -n '1,260p'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
 M corpus/artifacts/deficiency-register.json
 M corpus/deficiencies.md
 M corpus/deficiency-register.md
 M docs/artifacts/control-application.md
 M docs/artifacts/deficiencies.md
 M docs/control-application.html
 M docs/deficiencies-10.html
 M docs/deficiencies-2.html
 M docs/deficiencies-3.html
 M docs/deficiencies-4.html
 M docs/deficiencies-5.html
 M docs/deficiencies-6.html
 M docs/deficiencies-7.html
 M docs/deficiencies-8.html
 M docs/deficiencies-9.html
 M docs/deficiencies.html
 M docs/founding-1-1.html
 M docs/founding-1-1.md
 M docs/founding-1-2.html
 M docs/founding-1-2.md
 M docs/founding-2-1.html
 M docs/founding-2-1.md
 M docs/founding-2-2.html
 M docs/founding-2-2.md
 M docs/founding-3-1.html
 M docs/founding-3-1.md
 M docs/founding-3-2.html
 M docs/founding-3-2.md
 M docs/founding-4-1.html
 M docs/founding-4-1.md
 M docs/founding-4-2.html
 M docs/founding-4-2.md
 M docs/founding-5.html
 M docs/founding-5.md
 M docs/index.html
 M docs/index.md
 M docs/local-round-01.html
 M docs/local-round-01.md
 M docs/local-round-02.html
 M docs/local-round-02.md
 M docs/local-round-03.html
 M docs/local-round-03.md
 M docs/local-round-04.html
 M docs/local-round-04.md
 M docs/local-round-05.html
 M docs/local-round-05.md
 M docs/local-round-06.html
 M docs/local-round-06.md
 M docs/local-round-07.html
 M docs/local-round-07.md
 M docs/local-round-08.html
 M docs/local-round-08.md
 M docs/local-round-09.html
 M docs/local-round-09.md
 M docs/record.html
 M docs/review-round-01-1.html
 M docs/review-round-01-1.md
 M docs/review-round-01-2.html
 M docs/review-round-01-2.md
 M docs/review-round-01-3.html
 M docs/review-round-01-3.md
 M docs/review-round-01-4.html
 M docs/review-round-01-4.md
 M docs/review-round-02-1.html
 M docs/review-round-02-1.md
 M docs/review-round-02-2.html
 M docs/review-round-02-2.md
 M docs/review-round-02-3.html
 M docs/review-round-02-3.md
 M docs/review-round-02-4.html
 M docs/review-round-02-4.md
 M docs/review-round-02-prompt-critique.html
 M docs/review-round-02-prompt-critique.md
 M docs/review-round-03-1.html
 M docs/review-round-03-1.md
 M docs/review-round-03-2.html
 M docs/review-round-03-2.md
 M docs/review-round-03-3.html
 M docs/review-round-03-3.md
 M record/controls/control-application.md
 M record/cycles/spend-ledger.json
 M record/executive/action-log.jsonl
 M record/executive/guard-baseline.json
 M tools/check_quotations.py
 M tools/control_application.py
 M tools/control_findings.py
 M tools/scan_own_code.py
 M tools/tests/test_gate_negative_controls.py
main
 corpus/artifacts/deficiency-register.json  | 44 ++++++++++++++-
 corpus/deficiencies.md                     | 49 ++++++++++++++++-
 corpus/deficiency-register.md              | 23 ++++++--
 docs/artifacts/control-application.md      |  2 +-
 docs/artifacts/deficiencies.md             | 49 ++++++++++++++++-
 docs/control-application.html              |  2 +-
 docs/deficiencies-10.html                  | 18 +++----
 docs/deficiencies-2.html                   | 12 ++---
 docs/deficiencies-3.html                   | 12 ++---
 docs/deficiencies-4.html                   | 12 ++---
 docs/deficiencies-5.html                   | 12 ++---
 docs/deficiencies-6.html                   | 12 ++---
 docs/deficiencies-7.html                   | 12 ++---
 docs/deficiencies-8.html                   | 12 ++---
 docs/deficiencies-9.html                   | 12 ++---
 docs/deficiencies.html                     | 12 ++---
 docs/founding-1-1.html                     |  4 +-
 docs/founding-1-1.md                       |  2 +-
 docs/founding-1-2.html                     |  4 +-
 docs/founding-1-2.md                       |  2 +-
 docs/founding-2-1.html                     |  4 +-
 docs/founding-2-1.md                       |  2 +-
 docs/founding-2-2.html                     |  4 +-
 docs/founding-2-2.md                       |  2 +-
 docs/founding-3-1.html                     |  4 +-
 docs/founding-3-1.md                       |  2 +-
 docs/founding-3-2.html                     |  4 +-
 docs/founding-3-2.md                       |  2 +-
 docs/founding-4-1.html                     |  4 +-
 docs/founding-4-1.md                       |  2 +-
 docs/founding-4-2.html                     |  4 +-
 docs/founding-4-2.md                       |  2 +-
 docs/founding-5.html                       |  4 +-
 docs/founding-5.md                         |  2 +-
 docs/index.html                            |  6 +--
 docs/index.md                              |  2 +-
 docs/local-round-01.html                   |  4 +-
 docs/local-round-01.md                     |  2 +-
 docs/local-round-02.html                   |  4 +-
 docs/local-round-02.md                     |  2 +-
 docs/local-round-03.html                   |  4 +-
 docs/local-round-03.md                     |  2 +-
 docs/local-round-04.html                   |  4 +-
 docs/local-round-04.md                     |  2 +-
 docs/local-round-05.html                   |  4 +-
 docs/local-round-05.md                     |  2 +-
 docs/local-round-06.html                   |  4 +-
 docs/local-round-06.md                     |  2 +-
 docs/local-round-07.html                   |  4 +-
 docs/local-round-07.md                     |  2 +-
 docs/local-round-08.html                   |  4 +-
 docs/local-round-08.md                     |  2 +-
 docs/local-round-09.html                   |  4 +-
 docs/local-round-09.md                     |  2 +-
 docs/record.html                           |  4 +-
 docs/review-round-01-1.html                |  4 +-
 docs/review-round-01-1.md                  |  2 +-
 docs/review-round-01-2.html                |  4 +-
 docs/review-round-01-2.md                  |  2 +-
 docs/review-round-01-3.html                |  4 +-
 docs/review-round-01-3.md                  |  2 +-
 docs/review-round-01-4.html                |  4 +-
 docs/review-round-01-4.md                  |  2 +-
 docs/review-round-02-1.html                |  4 +-
 docs/review-round-02-1.md                  |  2 +-
 docs/review-round-02-2.html                |  4 +-
 docs/review-round-02-2.md                  |  2 +-
 docs/review-round-02-3.html                |  4 +-
 docs/review-round-02-3.md                  |  2 +-
 docs/review-round-02-4.html                |  4 +-
 docs/review-round-02-4.md                  |  2 +-
 docs/review-round-02-prompt-critique.html  |  4 +-
 docs/review-round-02-prompt-critique.md    |  2 +-
 docs/review-round-03-1.html                |  4 +-
 docs/review-round-03-1.md                  |  2 +-
 docs/review-round-03-2.html                |  4 +-
 docs/review-round-03-2.md                  |  2 +-
 docs/review-round-03-3.html                |  4 +-
 docs/review-round-03-3.md                  |  2 +-
 record/controls/control-application.md     |  2 +-
 record/cycles/spend-ledger.json            | 45 ++++++++++++++++
 record/executive/action-log.jsonl          |  4 ++
 record/executive/guard-baseline.json       | 24 +++++----
 tools/check_quotations.py                  | 74 +++++++++++++++----------
 tools/control_application.py               | 23 ++++----
 tools/control_findings.py                  | 26 ++++++++-
 tools/scan_own_code.py                     | 17 ++++--
 tools/tests/test_gate_negative_controls.py | 86 ++++++++++++++++++++++++++++++
 88 files changed, 564 insertions(+), 224 deletions(-)
diff --git a/record/controls/control-application.md b/record/controls/control-application.md
index 20fdc13..51570da 100644
--- a/record/controls/control-application.md
+++ b/record/controls/control-application.md
@@ -70,7 +70,7 @@ The **residue** on a row is what no code could supply. It is not a place to file
 | 42 | Capability claims name their stratum | ☐ | `tools/self_application.py` `tools/control_findings.py` `tools/derive_counts.py`<br>tested by `tools/tests/test_derive_counts.py`<br>**Remaining:** Strata are respected by hand in prose. No aggregate count in this codebase carries a stratum label, and nothing refuses a sum across strata.<br><small>self-application: VIOLATED</small> |
 | 43 | An efficiency claim carries the quality metric it could have traded | — | **Not applicable to code.** No efficiency claim is made about anything here. Cost is reported from the ledger; nothing claims a gain. |
 | 44 | No blank cells in a coverage matrix | ☐ | `tools/self_application.py` `tools/control_application.py` `tools/control_findings.py`<br>tested by `tools/tests/test_no_blank_cells.py`<br>**Remaining:** TWO enumerated matrices are guarded — a missing row, a row for an unregistered control, an invented state, and a cell filled with something indistinguishable from an omission are all refused in both. That is not the control. **A third matrix already exists and nothing checks it**: record/findings/2026-08-08-search-capability-matrix.json, called the 'full four-endpoint matrix' by round_cycle.py, whose first two rows omit a column the last two carry. Guarding two named artifacts cannot make the control hold repository-wide while a fourth can be published tomorrow without registering. What is missing is a closed-world inventory: an artifact type for published coverage matrices, a requirement that every one registers, and a completeness validator run over the registry. This row was TICKED until Codex found the third matrix on 2026-08-12, which is the same 'enumerated by hand and therefore complete' error control 19 is about.<br>*Not closable in code:* That any filled cell is CORRECT. This repository has published a complete matrix that was wrong — three controls marked ENFORCED citing a file that did not exist, and the completeness check passed over it because the reason string was long enough. Control 44 asks for no silent blank cells and nothing more; a second party is what would supply more.<br><small>self-application: ENFORCED</small> |
-| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and the codes this repository has committed to keeping are recorded in record/executive/guard-baseline.json. Removing a guard AND its fixture together — the shape an actual rewrite takes — is refused unless the removal is recorded with a ground, which makes weakening a governed event rather than a diff nobody reads. THE FIRST VERSION HAD ONLY THE PRESENT-TIME HALF and Codex reproduced the silent case: baseline clean, delete both, still clean. WHAT REMAINS: exactly ONE tool is enrolled — eleven guards in control_application.py. Every other gate here has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py can still drop a check with its fixture passing on a neighbour. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
+| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/guards.py` `tools/control_coverage.py` `tools/executive_log.py` `tools/check_quotations.py`<br>tested by `tools/tests/test_guard_identity.py` `tools/tests/test_deploy_check.py` `tools/tests/test_no_blank_cells.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Guards carry codes, fixtures ask for them by name through an assertion that RAISES, and record/executive/guard-baseline.json holds the codes this repository has committed to keeping — so removing a guard AND its fixture together, the shape an actual rewrite takes, is refused unless recorded with a ground. TWO gates are enrolled, thirteen guards. Enrolling the second was the point and it found the abstraction's limit: expect_guard() assumes an in-process list of refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its fixture adapts the output rather than the model adapting to the gate. WHAT REMAINS: every other gate here still has anonymous refusals. Enrolment should follow gates as they are touched rather than becoming a count to hit, and the mechanism cannot see a check that was never written.<br>*Not closable in code:* That a driven guard is a CORRECT guard, and that a named expectation ever RAN. Static analysis registers a call inside a branch nobody takes; runtime receipts or coverage would be needed and neither exists here.<br><small>self-application: VIOLATED</small> |
 | 46 | Drift is measured against the original baseline | ☐ | `tools/control_coverage.py` `tools/gate_health.py`<br>tested by `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE IS RETAINED, so any divergence that were measured would be measured against the previous cycle — the comparison the control forbids as the only one. An earlier version of this row said nothing here drifts across cycles, which is false of the tools themselves.<br><small>self-application: UNRESOLVED</small> |
 | 47 | Trust does not pass through delegation | — | **Not applicable to code.** Structural: no delegation graph. Tools call tools within one process under one operator's authority; there is no downstream actor whose scope could be inherited. |
 | 48 | Workflow trust is not inferred from component trust | — | **Not applicable to code.** Structural: no multi-component workflow with handoffs between differently trusted parts. Same absence as control 47. |
diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 1619891..1c957fa 100644
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2010,6 +2010,51 @@
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
     },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
     {
       "utc": "2026-08-10T18:00:00Z",
       "round": "no-such-cohort-zzqx",
diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl
index 374c9c8..00f800e 100644
--- a/record/executive/action-log.jsonl
+++ b/record/executive/action-log.jsonl
@@ -275,3 +275,7 @@
 {"utc": "2026-08-12T05:56:08Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C45 batch four", "actor": "claude-code", "head": "cb9b7d72941d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T09:40:00.181660+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.181683+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "05696cd0333083532f74628fc34636a05135647020916ede99ef5421d63488dc"}
 {"utc": "2026-08-12T05:56:09Z", "action": "push", "claim": {"target_ref": "main", "commit": "cb9b7d72941d92c6bcc5165879bf120f8f5a23cb"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C45 batch four", "actor": "claude-code", "head": "cb9b7d72941d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T09:40:00.181660+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.181683+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "e3c7a73d5a29ca44611b2f641b62f658ea610ac18dd46ac1bcd123b1ff0cf51f"}
 {"utc": "2026-08-12T05:59:57Z", "action": "deploy", "claim": {"observed": true, "commit": "cb9b7d72941d92c6bcc5165879bf120f8f5a23cb", "conclusion": "success", "deployed_sha": "cb9b7d72941d92c6bcc5165879bf120f8f5a23cb", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31568197364"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C45 batch four", "actor": "claude-code", "head": "cb9b7d72941d", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 3.0, "resets_at": "2026-08-12T09:40:00.181660+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.181683+00:00"}, "cached": true, "age_seconds": 228}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "4b49542dae0190a473312d3b2ae0684fa02015fcb1095c7a1a07255fc4dd3a20"}
+{"utc": "2026-08-12T08:09:12Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "handoff refresh + probe rename", "actor": "claude-code", "head": "df51659a77ce", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T09:39:59.324509+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.324531+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "36f222763f1c7d882c17ca95224c81527802ce9100f31d63660370bf2d8bbd88"}
+{"utc": "2026-08-12T08:09:12Z", "action": "push", "claim": {"target_ref": "main", "commit": "df51659a77ce1d84775b6cc6dbc3a16b2f9a7097"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "handoff refresh + probe rename", "actor": "claude-code", "head": "df51659a77ce", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T09:39:59.324509+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.324531+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "9abb0337896730766ebc0a112278b12afcb709a08dae4ed434641398ac3aad77"}
+{"utc": "2026-08-12T08:11:58Z", "action": "deploy", "claim": {"observed": true, "commit": "df51659a77ce1d84775b6cc6dbc3a16b2f9a7097", "conclusion": "success", "deployed_sha": "df51659a77ce1d84775b6cc6dbc3a16b2f9a7097", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31577057413"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "handoff refresh + probe rename", "actor": "claude-code", "head": "df51659a77ce", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T09:39:59.324509+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T00:59:59.324531+00:00"}, "cached": true, "age_seconds": 166}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "32251c24b334177cdc023dfc283d2ce3baa1d05c94f16d7554eea280d286cc37"}
+{"utc": "2026-08-12T08:20:42Z", "action": "codex_invoke", "claim": {"purpose": "review the second guard enrolment and the wrapped-quotation repair", "override": null, "prompt_sha256": "65b4ce243034a392090166b09586a048152366814d557bb290423b2272e789e7", "prompt_chars": 3415, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "df51659a77ce", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 5.0, "resets_at": "2026-08-12T09:40:00.264453+00:00"}, "seven_day": {"utilization": 7.0, "resets_at": "2026-08-18T01:00:00.264474+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "bf079725e7d2d3db76b717e9f91249d4012ee6750978457d4aedbc765b909c70"}
diff --git a/record/executive/guard-baseline.json b/record/executive/guard-baseline.json
index e7a3181..5791bcc 100644
--- a/record/executive/guard-baseline.json
+++ b/record/executive/guard-baseline.json
@@ -1,16 +1,18 @@
 {
   "guards": {
-    "CA-01": "control_application.py:569",
-    "CA-02": "control_application.py:576",
-    "CA-03": "control_application.py:581",
-    "CA-04": "control_application.py:595",
-    "CA-05": "control_application.py:597",
-    "CA-06": "control_application.py:602",
-    "CA-07": "control_application.py:605",
-    "CA-08": "control_application.py:610",
-    "CA-09": "control_application.py:617",
-    "CA-10": "control_application.py:621",
-    "CA-11": "control_application.py:627"
+    "CA-01": "control_application.py:571",
+    "CA-02": "control_application.py:578",
+    "CA-03": "control_application.py:583",
+    "CA-04": "control_application.py:597",
+    "CA-05": "control_application.py:599",
+    "CA-06": "control_application.py:604",
+    "CA-07": "control_application.py:607",
+    "CA-08": "control_application.py:612",
+    "CA-09": "control_application.py:619",
+    "CA-10": "control_application.py:623",
+    "CA-11": "control_application.py:629",
+    "QT-01": "check_quotations.py:265",
+    "QT-02": "check_quotations.py:307"
   },
   "authorised_removals": {}
 }
diff --git a/tools/check_quotations.py b/tools/check_quotations.py
index d35428c..d4d5a03 100644
--- a/tools/check_quotations.py
+++ b/tools/check_quotations.py
@@ -58,6 +58,8 @@ import unicodedata
 from pathlib import Path
 
 REPO_ROOT = Path(__file__).resolve().parent.parent
+sys.path.insert(0, str(REPO_ROOT / "tools"))
+from guards import guard                                              # noqa: E402
 #  Verified against the WHOLE corpus, not just raw/. The review bundles quote annotation
 #  artifacts verbatim -- real committed material that simply is not a party utterance -- and
 #  failing 32 times on those would have made this unusable. Both D-53 fabrications appear
@@ -105,9 +107,28 @@ EXEMPT_MARKERS = [
     "did not say", "misattribut",
 ]
 
+#  A QUOTATION MAY WRAP. Both patterns forbade a newline — `[^"\n]` — so any quotation spanning
+#  more than one line was INVISIBLE to this gate. In wrapped markdown at ~98 columns, a quotation
+#  of forty characters or more usually wraps, and every design document here is wrapped markdown.
+#
+#  That is not a corner case, it is the case: D-53 was a fabricated party quotation IN A DESIGN
+#  DOCUMENT, and this gate was built to catch the next one. It could not have. Measured on
+#  2026-08-12 over the 98 files it scans: 235 quotation candidates, ONE attributed to a party,
+#  and that one exempted by a CORRECTION marker — so **zero quotations checked, every run**, by a
+#  gate that runs on every landing.
+#
+#  THE NEGATIVE CONTROL PASSED THROUGHOUT, and that is the part worth keeping. The planted
+#  fixture was written as a single-line quotation, so it exercised the only shape the gate could
+#  see. The fault was injected in the form that works and never in the form that occurs.
+#
+#  A single newline is allowed; a BLANK line is not, because a quotation does not span a
+#  paragraph break and an unclosed quote would otherwise swallow the rest of the document. The
+#  upper bound is for the same reason. `normalise()` already folds whitespace, so a wrapped
+#  quotation compares against the corpus exactly as a single-line one does.
+_INNER = r'(?:[^%s\n]|\n(?!\s*\n))'
 QUOTE_PATTERNS = [
-    re.compile(r'"([^"\n]{%d,})"' % MIN_LENGTH),
-    re.compile(r'“([^”\n]{%d,})”' % MIN_LENGTH),
+    re.compile(r'"(%s{%d,4000})"' % (_INNER % '"', MIN_LENGTH)),
+    re.compile(r'“(%s{%d,4000})”' % (_INNER % '”', MIN_LENGTH)),
 ]
 
 
@@ -241,8 +262,8 @@ def main() -> int:
 
     corpus = load_corpus()
     if not corpus:
-        print("REFUSED: corpus/ is empty or unreadable; a check that cannot fail is not a "
-              "check.", file=sys.stderr)
+        print(guard("QT-01", "REFUSED: corpus/ is empty or unreadable; a check that cannot "
+                             "fail is not a check."), file=sys.stderr)
         return 2
 
     checked = 0
@@ -260,32 +281,31 @@ def main() -> int:
                       for pattern in QUOTE_PATTERNS for m in pattern.finditer(text)]
         candidates += list(blockquotes(text))
 
-        if True:
-            for start, quotation in candidates:
-                before = text[:start]
-                party = attributed_party(before)
-                if not party:
-                    continue
-                context = before[-ATTRIBUTION_WINDOW:] + quotation
-                if any(marker.lower() in context.lower() for marker in EXEMPT_MARKERS):
-                    continue
-
-                #  A template placeholder is not a quotation of anything.
-                if "{" in quotation and "}" in quotation:
-                    continue
-                checked += 1
-                missing = [f for f in fragments(quotation) if normalise(f) not in corpus]
-                if missing:
-                    line_no = sum(1 for off in offsets if off <= start)
-                    unverified.append((path, line_no, party, missing[0]))
-                elif args.list:
-                    rel = path.relative_to(REPO_ROOT)
-                    print(f"  ok   {rel}  [{party}]  {quotation[:70]}…")
+        for start, quotation in candidates:
+            before = text[:start]
+            party = attributed_party(before)
+            if not party:
+                continue
+            context = before[-ATTRIBUTION_WINDOW:] + quotation
+            if any(marker.lower() in context.lower() for marker in EXEMPT_MARKERS):
+                continue
+
+            #  A template placeholder is not a quotation of anything.
+            if "{" in quotation and "}" in quotation:
+                continue
+            checked += 1
+            missing = [f for f in fragments(quotation) if normalise(f) not in corpus]
+            if missing:
+                line_no = sum(1 for off in offsets if off <= start)
+                unverified.append((path, line_no, party, missing[0]))
+            elif args.list:
+                rel = path.relative_to(REPO_ROOT)
+                print(f"  ok   {rel}  [{party}]  {quotation[:70]}…")
 
     rel = lambda p: p.relative_to(REPO_ROOT)                              # noqa: E731
     if unverified:
-        print(f"\n\033[31mFAILED — {len(unverified)} attributed quotation(s) do not appear in "
-              f"corpus/\033[0m", file=sys.stderr)
+        print(guard("QT-02", f"\nFAILED — {len(unverified)} attributed quotation(s) do not "
+                             f"appear in corpus/"), file=sys.stderr)
         for path, line_no, party, missing in unverified:
             print(f"\n  {rel(path)}:{line_no}", file=sys.stderr)
             print(f"    attributed to: {party}", file=sys.stderr)
diff --git a/tools/scan_own_code.py b/tools/scan_own_code.py
index c07808f..837774d 100644
--- a/tools/scan_own_code.py
+++ b/tools/scan_own_code.py
@@ -52,7 +52,7 @@ def empty_conditional(tree: ast.AST) -> list[int]:
 
 
 def constant_guard(tree: ast.AST) -> list[int]:
-    """D-B. A name bound to a bool literal and later used as a bare condition."""
+    """D-B. A condition that is constant — a bool literal, or a name bound once to one."""
     #  ONLY names assigned exactly ONCE, to a bool literal. The first version kept the last
     #  literal assignment and ignored reassignment, so it flagged `in_table` -- an ordinary
     #  mutable state flag set True and False in different branches. Two false positives, on a
@@ -68,8 +68,17 @@ def constant_guard(tree: ast.AST) -> list[int]:
               if len(nodes) == 1 and isinstance(nodes[0], ast.Assign)
               and isinstance(nodes[0].value, ast.Constant)
               and isinstance(nodes[0].value.value, bool)}
-    return [n.lineno for n in ast.walk(tree)
-            if isinstance(n, ast.If) and isinstance(n.test, ast.Name) and n.test.id in consts]
+    #  A LITERAL TEST TOO, not only a name bound to one. `if True:` was sitting in
+    #  check_quotations.py — a gate that runs on every landing — wrapping its entire quotation
+    #  loop, and this detector reported nothing, on the real file AND on a direct fixture. It
+    #  matched `x = True; if x:` and not the shape one step simpler. Found on 2026-08-12 by
+    #  reading a gate while enrolling it in the guard registry, which is not a mechanism.
+    out = [n.lineno for n in ast.walk(tree)
+           if isinstance(n, ast.If) and isinstance(n.test, ast.Name) and n.test.id in consts]
+    out += [n.lineno for n in ast.walk(tree)
+            if isinstance(n, ast.If) and isinstance(n.test, ast.Constant)
+            and isinstance(n.test.value, bool)]
+    return sorted(out)
 
 
 def success_from_except(tree: ast.AST) -> list[int]:
@@ -195,7 +204,7 @@ FIXTURES = {
         "if x:\n    pass\n",

**Inspecting ledger modifications and diff scope**
exec
/bin/bash -lc "git diff -- tools/check_quotations.py tools/scan_own_code.py tools/guards.py tools/tests/test_gate_negative_controls.py tools/control_application.py tools/control_findings.py corpus/artifacts/deficiency-register.json corpus/deficiencies.md corpus/deficiency-register.md | sed -n '1,520p'
rg -n \"QUOTE_PATTERNS|attributed_party|blockquotes|fragments|QT-0|expect_guard|constant_guard|D-B|wrapped|4000|blank\" tools/check_quotations.py tools/scan_own_code.py tools/guards.py tools/tests/test_gate_negative_controls.py tools/tests -g '*.py'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
diff --git a/corpus/artifacts/deficiency-register.json b/corpus/artifacts/deficiency-register.json
index e9b5352..246eb30 100644
--- a/corpus/artifacts/deficiency-register.json
+++ b/corpus/artifacts/deficiency-register.json
@@ -9,7 +9,7 @@
   },
   "source": {
     "path": "corpus/deficiencies.md",
-    "sha256": "03bb63281a18eb42e5f533351bf0fc37f1e1f91519ef8e9c5c362f37dad111ea"
+    "sha256": "8a3c382cfe3a9a5e9609d3f9bda4e90d63c0ac0f0b3d0a4b670c664643b7fa5c"
   },
   "vocabulary_note": "Repairability is recorded PER AFFECTED OBJECT, not per deficiency, because it is not a property of a deficiency. D-09 is the proof: the raw transcript's merged identities are not repairable while the segments.json annotation was corrected, and a single yes/no is false for one of them whichever way it is written. The register's own prose table collapsed these and misstated entries as a result. Finding state, per-object repairability, remediation state and prospective control are four independent axes; 'forward requirement only', 'annotation only', 'standing caveat' and 'yes, cheaply' were never competing values of one variable. Attribution records the FIRST PRESERVED SUBSTANTIVE ARTICULATION, with an evidence class, because this project cannot observe who first privately noticed a defect. A question that prompted an investigation is recorded as a trigger, not as the finding.",
   "entries": [
@@ -2435,7 +2435,7 @@
     {
       "id": "D-60",
       "title": "The type built to enforce closed-world measurement did not enforce it, and both tools routed through it printed the counts it was withholding",
-      "section_sha256": "5b86d3cb5154e5d378a32bd1d336cb808e547490180a8938b85dc44ce03e918f",
+      "section_sha256": "7ce6c87e496208abe723e75320c74e01d49c8cbf5e9472f71bc0e1bb1a511471",
       "finding_state": "finding_active",
       "first_articulation": {
         "actor_context": "actor_external_adversarial_reviewer",
@@ -2477,6 +2477,46 @@
         "status": "not_reviewed",
         "note": "Prose changed after the previous stamp; any earlier review of it is stale."
       }
+    },
+    {
+      "id": "D-61",
+      "title": "The gate against fabricated party quotations could not see a quotation that wraps, so it checked none",
+      "section_sha256": "32d022c961777909a2117c8aefae779dac241027b919d1022f7372aa6db4bd25",
+      "finding_state": "finding_active",
+      "first_articulation": {
+        "actor_context": "actor_repository_annotator_invocation",
+        "date_utc": "2026-08-12",
+        "where": "reading check_quotations.py while enrolling it in the guard registry; measured 235 candidates, 1 attributed, 0 checked across the 98 files it scans",
+        "evidence_class": "origin_supported_by_preserved_artifact"
+      },
+      "affected_objects": [
+        {
+          "object": "tools/check_quotations.py",
+          "repairability": "affected_object_fully_repairable_by_supersession",
+          "remediation_state": "remediation_verified",
+          "note": "Patterns admit a single newline and refuse a blank line. 350 candidates and 4 attributions after the repair; the verbatim party amendment in the pinned instructions is checked for the first time. A literal `if True:` wrapping the whole loop was also removed."
+        },
+        {
+          "object": "tools/tests/test_gate_negative_controls.py",
+          "repairability": "affected_object_fully_repairable_by_supersession",
+          "remediation_state": "remediation_verified",
+          "note": "Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern."
+        },
+        {
+          "object": "tools/scan_own_code.py",
+          "repairability": "affected_object_fully_repairable_by_supersession",
+          "remediation_state": "remediation_verified",
+          "note": "D-B matched a name bound to a bool and not a bool literal, so `if True:` was invisible on the real file and on a direct fixture. The literal is now the must-flag fixture."
+        }
+      ],
+      "prospective_control": {
+        "state": "prospective_control_required_not_implemented",
+        "mechanism": "Nothing establishes that a negative control injects the fault in the form that OCCURS rather than the form that was imagined. A fixture corpus drawn from the record's real shapes — wrapped prose, parenthetical provenance, blockquotes — would be the mechanism; reading the gate is not one."
+      },
+      "human_review": {
+        "status": "not_reviewed",
+        "note": "Prose changed after the previous stamp; any earlier review of it is stale."
+      }
     }
   ]
 }
diff --git a/corpus/deficiencies.md b/corpus/deficiencies.md
index 9ef95f2..b654122 100644
--- a/corpus/deficiencies.md
+++ b/corpus/deficiencies.md
@@ -1,6 +1,6 @@
 # Deficiency Register — Founding Record (OAGRC-2026-08-04/05)
 
-**Status:** open — **60 entries** (D-01 … D-60).
+**Status:** open — **61 entries** (D-01 … D-61).
 
 *This count was wrong until 2026-08-06. It read "24 entries" while the document held 28 headings,
 and `README.md` and the published site said 21. Three artifacts of this repository stated three
@@ -2401,3 +2401,50 @@ count of any kind surviving, and `--check` separately. **Not remediated:** routi
 per-tool and by hand, nothing detects a new tool that globs a directory and prints a count, and
 no type catches a population defined too narrowly — which is the half of the 2026-08-10 error
 that produced the published zero.
+
+### D-61 — The gate against fabricated party quotations could not see a quotation that wraps, so it checked none
+
+*Filed 2026-08-12, found while enrolling the gate in the guard registry — by reading it, not by
+any mechanism. It had been green on every landing since it was built.*
+
+`tools/check_quotations.py` exists because of **D-53**: two design documents attributed words to
+a party that the party never said. It runs on every landing. Its two quotation patterns were:
+
+> `re.compile(r'"([^"\n]{40,})"')`
+> `re.compile(r'“([^”\n]{40,})”')`
+
+**Both forbid a newline.** Every design document in this record is wrapped markdown at roughly
+ninety-eight columns, so a quotation of forty characters or more usually spans a line break — and
+a spanning quotation was not a candidate at all. The gate could not have caught D-53's own shape:
+a fabricated quotation of any substance wraps.
+
+**Measured over the 98 files it scans**, before the repair: 235 quotation candidates, **one**
+attributed to a party, and that one exempted by a `CORRECTION` marker. **Zero quotations checked,
+every run.** The record's own verbatim party amendment — the tombstone clause adopted from
+claude's ratification-02 sample, quoted in the pinned governing instructions — was among the
+invisible ones.
+
+**The negative control passed throughout, and that is the part worth keeping.** It was written on
+2026-08-11 and it plants its fabricated sentence **on a single line**, so it exercised the only
+shape the gate could see. The fault was injected in the form that works and never in the form
+that occurs. Control 2 asks for a condition under which a check must fail; this was one, and it
+was still the wrong one — *a negative control tests the fault you thought of.*
+
+**Remediated.** The patterns admit a single newline and refuse a blank line, so an unclosed quote
+cannot swallow a document; `normalise()` already folded whitespace, so a wrapped quotation
+compares against the corpus exactly as a one-line one does. After the repair the same 98 files
+yield 350 candidates and four attributions, and the amendment in the pinned instructions is
+checked for the first time. The single-line arms are RETAINED alongside the new wrapped ones,
+which is control 45: a repair must still catch what the old gate caught.
+
+**Two smaller findings rode along, both from reading the same file.** A literal `if True:`
+wrapped the entire quotation loop — dead scaffolding in a gate that runs on every landing — and
+`scan_own_code.py`'s D-B detector reported nothing, on the real file *and* on a direct fixture,
+because it matched a name bound to a bool and not a bool itself. Both are fixed, and D-B now
+carries the literal case as its must-flag fixture.
+
+**What this does not establish.** That the gate now has useful recall. It attributes a quotation
+only when a party name is followed by a speech verb within 240 characters, and this record's
+commonest citation form is a parenthetical provenance — `(claude, ratification-02 sample 2,
+verbatim)` — with no verb at all. Four attributions out of 350 candidates is not evidence of a
+healthy detector; it is one order of magnitude better than zero.
diff --git a/corpus/deficiency-register.md b/corpus/deficiency-register.md
index ed8d3b2..b771459 100644
--- a/corpus/deficiency-register.md
+++ b/corpus/deficiency-register.md
@@ -12,7 +12,7 @@ It records, per entry, where the defect was **first substantively articulated in
 
 It does **not** establish that any of those judgements is correct. They were made by the annotator, which is a party to the record. `tools/check_register.py` verifies structure, one-to-one coverage, vocabulary, and that each entry's prose has not changed since it was classified. **It never verifies meaning**, and a deterministic rule that claimed to would be D-25 over again.
 
-**0 of 60** classifications have been read by a human against the prose.
+**0 of 61** classifications have been read by a human against the prose.
 
 On attribution: this project cannot observe who first *privately noticed* a defect, so it records where one was first *written down*, with an evidence class. A question that prompted an investigation is recorded as a **trigger**, not as the finding.
 
@@ -24,7 +24,7 @@ On attribution: this project cannot observe who first *privately noticed* a defe
 
 | Where | Entries |
 |---|---|
-| the annotator | 35 |
+| the annotator | 36 |
 | an external reviewer | 13 |
 | a designated review round | 8 |
 | human operator | 3 |
@@ -36,13 +36,13 @@ On attribution: this project cannot observe who first *privately noticed* a defe
 
 | State | Entries |
 |---|---|
-| required, not implemented | 20 |
+| required, not implemented | 21 |
 | implemented, not validated | 33 |
 | validated | 7 |
 
 ### Affected objects
 
-**151 affected-object rows across 60 entries.** Repairability is recorded per object because it is not a property of a deficiency: D-09's raw transcript is not repairable while its `segments.json` annotation was corrected, and a single yes/no is false for one of them whichever way it is written.
+**154 affected-object rows across 61 entries.** Repairability is recorded per object because it is not a property of a deficiency: D-09's raw transcript is not repairable while its `segments.json` annotation was corrected, and a single yes/no is false for one of them whichever way it is written.
 
 ---
 
@@ -966,3 +966,18 @@ On attribution: this project cannot observe who first *privately noticed* a defe
 | tools/control_coverage.py <br><sub>Both the report path and --check consult the survey before any number is produced.</sub> | repairable by supersession | verified |
 | tools/tests/test_closed_world.py <br><sub>Its integration arms asserted the absence of a phrase removed in the same edit, so they forbade nothing. They now name the tools' actual count labels.</sub> | repairable by supersession | verified |
 
+### D-61 — The gate against fabricated party quotations could not see a quotation that wraps, so it checked none
+
+[Full entry](deficiencies.md#d-61--the-gate-against-fabricated-party-quotations-could-not-see-a-quotation-that-wraps-so-it-checked-none)
+
+- **First articulated:** the annotator, 2026-08-12 · evidence: *preserved artifact*
+  - where: `reading check_quotations.py while enrolling it in the guard registry; measured 235 candidates, 1 attributed, 0 checked across the 98 files it scans`
+- **Prospective control:** required, not implemented — Nothing establishes that a negative control injects the fault in the form that OCCURS rather than the form that was imagined. A fixture corpus drawn from the record's real shapes — wrapped prose, parenthetical provenance, blockquotes — would be the mechanism; reading the gate is not one.
+- **Human review:** not_reviewed
+
+| Affected object | Repairable? | Remediation |
+|---|---|---|
+| tools/check_quotations.py <br><sub>Patterns admit a single newline and refuse a blank line. 350 candidates and 4 attributions after the repair; the verbatim party amendment in the pinned instructions is checked for the first time. A literal `if True:` wrapping the whole loop was also removed.</sub> | repairable by supersession | verified |
+| tools/tests/test_gate_negative_controls.py <br><sub>Its fabricated-quotation fixtures were single-line, so they exercised the only shape the gate could see. Wrapped arms added and the single-line arms retained per control 45; a paragraph-break arm bounds the new pattern.</sub> | repairable by supersession | verified |
+| tools/scan_own_code.py <br><sub>D-B matched a name bound to a bool and not a bool literal, so `if True:` was invisible on the real file and on a direct fixture. The literal is now the must-flag fixture.</sub> | repairable by supersession | verified |
+
diff --git a/tools/check_quotations.py b/tools/check_quotations.py
index d35428c..d4d5a03 100644
--- a/tools/check_quotations.py
+++ b/tools/check_quotations.py
@@ -58,6 +58,8 @@ import unicodedata
 from pathlib import Path
 
 REPO_ROOT = Path(__file__).resolve().parent.parent
+sys.path.insert(0, str(REPO_ROOT / "tools"))
+from guards import guard                                              # noqa: E402
 #  Verified against the WHOLE corpus, not just raw/. The review bundles quote annotation
 #  artifacts verbatim -- real committed material that simply is not a party utterance -- and
 #  failing 32 times on those would have made this unusable. Both D-53 fabrications appear
@@ -105,9 +107,28 @@ EXEMPT_MARKERS = [
     "did not say", "misattribut",
 ]
 
+#  A QUOTATION MAY WRAP. Both patterns forbade a newline — `[^"\n]` — so any quotation spanning
+#  more than one line was INVISIBLE to this gate. In wrapped markdown at ~98 columns, a quotation
+#  of forty characters or more usually wraps, and every design document here is wrapped markdown.
+#
+#  That is not a corner case, it is the case: D-53 was a fabricated party quotation IN A DESIGN
+#  DOCUMENT, and this gate was built to catch the next one. It could not have. Measured on
+#  2026-08-12 over the 98 files it scans: 235 quotation candidates, ONE attributed to a party,
+#  and that one exempted by a CORRECTION marker — so **zero quotations checked, every run**, by a
+#  gate that runs on every landing.
+#
+#  THE NEGATIVE CONTROL PASSED THROUGHOUT, and that is the part worth keeping. The planted
+#  fixture was written as a single-line quotation, so it exercised the only shape the gate could
+#  see. The fault was injected in the form that works and never in the form that occurs.
+#
+#  A single newline is allowed; a BLANK line is not, because a quotation does not span a
+#  paragraph break and an unclosed quote would otherwise swallow the rest of the document. The
+#  upper bound is for the same reason. `normalise()` already folds whitespace, so a wrapped
+#  quotation compares against the corpus exactly as a single-line one does.
+_INNER = r'(?:[^%s\n]|\n(?!\s*\n))'
 QUOTE_PATTERNS = [
-    re.compile(r'"([^"\n]{%d,})"' % MIN_LENGTH),
-    re.compile(r'“([^”\n]{%d,})”' % MIN_LENGTH),
+    re.compile(r'"(%s{%d,4000})"' % (_INNER % '"', MIN_LENGTH)),
+    re.compile(r'“(%s{%d,4000})”' % (_INNER % '”', MIN_LENGTH)),
 ]
 
 
@@ -241,8 +262,8 @@ def main() -> int:
 
     corpus = load_corpus()
     if not corpus:
-        print("REFUSED: corpus/ is empty or unreadable; a check that cannot fail is not a "
-              "check.", file=sys.stderr)
+        print(guard("QT-01", "REFUSED: corpus/ is empty or unreadable; a check that cannot "
+                             "fail is not a check."), file=sys.stderr)
         return 2
 
     checked = 0
@@ -260,32 +281,31 @@ def main() -> int:
                       for pattern in QUOTE_PATTERNS for m in pattern.finditer(text)]
         candidates += list(blockquotes(text))
 
-        if True:
-            for start, quotation in candidates:
-                before = text[:start]
-                party = attributed_party(before)
-                if not party:
-                    continue
-                context = before[-ATTRIBUTION_WINDOW:] + quotation
-                if any(marker.lower() in context.lower() for marker in EXEMPT_MARKERS):
-                    continue
-
-                #  A template placeholder is not a quotation of anything.
-                if "{" in quotation and "}" in quotation:
-                    continue
-                checked += 1
-                missing = [f for f in fragments(quotation) if normalise(f) not in corpus]
-                if missing:
-                    line_no = sum(1 for off in offsets if off <= start)
-                    unverified.append((path, line_no, party, missing[0]))
-                elif args.list:
-                    rel = path.relative_to(REPO_ROOT)
-                    print(f"  ok   {rel}  [{party}]  {quotation[:70]}…")
+        for start, quotation in candidates:
+            before = text[:start]
+            party = attributed_party(before)
+            if not party:
+                continue
+            context = before[-ATTRIBUTION_WINDOW:] + quotation
+            if any(marker.lower() in context.lower() for marker in EXEMPT_MARKERS):
+                continue
+
+            #  A template placeholder is not a quotation of anything.
+            if "{" in quotation and "}" in quotation:
+                continue
+            checked += 1
+            missing = [f for f in fragments(quotation) if normalise(f) not in corpus]
+            if missing:
+                line_no = sum(1 for off in offsets if off <= start)
+                unverified.append((path, line_no, party, missing[0]))
+            elif args.list:
+                rel = path.relative_to(REPO_ROOT)
+                print(f"  ok   {rel}  [{party}]  {quotation[:70]}…")
 
     rel = lambda p: p.relative_to(REPO_ROOT)                              # noqa: E731
     if unverified:
-        print(f"\n\033[31mFAILED — {len(unverified)} attributed quotation(s) do not appear in "
-              f"corpus/\033[0m", file=sys.stderr)
+        print(guard("QT-02", f"\nFAILED — {len(unverified)} attributed quotation(s) do not "
+                             f"appear in corpus/"), file=sys.stderr)
         for path, line_no, party, missing in unverified:
             print(f"\n  {rel(path)}:{line_no}", file=sys.stderr)
             print(f"    attributed to: {party}", file=sys.stderr)
diff --git a/tools/control_application.py b/tools/control_application.py
index 3dbf321..ac89442 100644
--- a/tools/control_application.py
+++ b/tools/control_application.py
@@ -379,19 +379,20 @@ APPLICATION: dict[int, dict] = {
                   "string was long enough. Control 44 asks for no silent blank cells and nothing "
                   "more; a second party is what would supply more."),
  45: dict(scope=CODE,
-          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py"),
+          files=("tools/guards.py", "tools/control_coverage.py", "tools/executive_log.py",
+                 "tools/check_quotations.py"),
           tests=("tools/tests/test_guard_identity.py", "tools/tests/test_deploy_check.py",
-                 "tools/tests/test_no_blank_cells.py"),
+                 "tools/tests/test_no_blank_cells.py",
+                 "tools/tests/test_gate_negative_controls.py"),
           gap="Guards carry codes, fixtures ask for them by name through an assertion that "
-              "RAISES, and the codes this repository has committed to keeping are recorded in "
-              "record/executive/guard-baseline.json. Removing a guard AND its fixture together "
-              "— the shape an actual rewrite takes — is refused unless the removal is recorded "
-              "with a ground, which makes weakening a governed event rather than a diff nobody "
-              "reads. THE FIRST VERSION HAD ONLY THE PRESENT-TIME HALF and Codex reproduced the "
-              "silent case: baseline clean, delete both, still clean. WHAT REMAINS: exactly ONE "
-              "tool is enrolled — eleven guards in control_application.py. Every other gate here "
-              "has anonymous refusals, so a rewrite of check_quotations.py or capture_gates.py "
-              "can still drop a check with its fixture passing on a neighbour. Enrolment should "
+              "RAISES, and record/executive/guard-baseline.json holds the codes this repository "
+              "has committed to keeping — so removing a guard AND its fixture together, the "
+              "shape an actual rewrite takes, is refused unless recorded with a ground. TWO "
+              "gates are enrolled, thirteen guards. Enrolling the second was the point and it "
+              "found the abstraction's limit: expect_guard() assumes an in-process list of "
+              "refusal strings, and check_quotations.py refuses by PRINTING and exiting, so its "
+              "fixture adapts the output rather than the model adapting to the gate. WHAT "
+              "REMAINS: every other gate here still has anonymous refusals. Enrolment should "
               "follow gates as they are touched rather than becoming a count to hit, and the "
               "mechanism cannot see a check that was never written.",
           residue="That a driven guard is a CORRECT guard, and that a named expectation ever "
diff --git a/tools/control_findings.py b/tools/control_findings.py
index b4403ba..a73d546 100644
--- a/tools/control_findings.py
+++ b/tools/control_findings.py
@@ -18,7 +18,7 @@ detection this project did not achieve. The values:
     reading       a human read the code while implementing the control and saw it
     external      someone outside this workbench found it
 
-**A detector fired for 10 of 33.** Those are the defect shapes that are mechanically
+**A detector fired for 10 of 36.** Those are the defect shapes that are mechanically
 recognisable; the rest came from reading, from running the fault, or from an outsider. So the
 controls both caught defects directly AND directed attention to the right code, and the split
 matters more than either number alone.
@@ -261,6 +261,30 @@ FINDINGS: list[tuple[str, str, str, str]] = [
   "else running. Renamed out of the glob while staying inside the population it must disturb.",
   "reading", "tools/tests/test_closed_world.py; land8 run 2026-08-12"),
 
+ ("C2 negative control",
+  "check_quotations.py — the gate built for D-53, a fabricated party quotation — could not see a "
+  "quotation that WRAPS, because both its patterns forbade a newline. Every design document here "
+  "is wrapped markdown, so a fabrication of any substance was invisible. Measured over its 98 "
+  "files: 235 candidates, ONE attributed, that one exempted — zero checked, every run. Its "
+  "negative control passed throughout because the planted fixture was written on a single line: "
+  "the fault was injected in the form that works and never in the form that occurs.",
+  "reading", "D-61; tools/check_quotations.py"),
+
+ ("C2 negative control",
+  "scan_own_code.py's D-B detector matched a NAME bound to a bool and not a bool literal, so an "
+  "`if True:` wrapping the entire quotation loop of a gate that runs on every landing was "
+  "reported by nothing — not on the real file, and not on a direct fixture either. The literal "
+  "is now the must-flag case.",
+  "reading", "D-61; tools/scan_own_code.py"),
+
+ ("C45 replacement gate",
+  "Enrolling a SECOND gate found where the abstraction does not fit. expect_guard(problems, code) "
+  "assumes an in-process list of refusal strings; check_quotations.py refuses by PRINTING and "
+  "exiting, so its fixture has to split output into lines before asking. The wrapper still "
+  "delegates, which the registry requires — but the model is shaped around one gate's return "
+  "convention and the second gate had to be adapted to it rather than the reverse.",
+  "reading", "tools/tests/test_gate_negative_controls.py"),
+
  ("C40 pre-committed stop",
   "The register contained control 40 — a program pre-commits the observation that ends it — and "
   "the programme running it had no stop condition. Found by Codex, not by this workbench.",
diff --git a/tools/scan_own_code.py b/tools/scan_own_code.py
index c07808f..837774d 100644
--- a/tools/scan_own_code.py
+++ b/tools/scan_own_code.py
@@ -52,7 +52,7 @@ def empty_conditional(tree: ast.AST) -> list[int]:
 
 
 def constant_guard(tree: ast.AST) -> list[int]:
-    """D-B. A name bound to a bool literal and later used as a bare condition."""
+    """D-B. A condition that is constant — a bool literal, or a name bound once to one."""
     #  ONLY names assigned exactly ONCE, to a bool literal. The first version kept the last
     #  literal assignment and ignored reassignment, so it flagged `in_table` -- an ordinary
     #  mutable state flag set True and False in different branches. Two false positives, on a
@@ -68,8 +68,17 @@ def constant_guard(tree: ast.AST) -> list[int]:
               if len(nodes) == 1 and isinstance(nodes[0], ast.Assign)
               and isinstance(nodes[0].value, ast.Constant)
               and isinstance(nodes[0].value.value, bool)}
-    return [n.lineno for n in ast.walk(tree)
-            if isinstance(n, ast.If) and isinstance(n.test, ast.Name) and n.test.id in consts]
+    #  A LITERAL TEST TOO, not only a name bound to one. `if True:` was sitting in
+    #  check_quotations.py — a gate that runs on every landing — wrapping its entire quotation
+    #  loop, and this detector reported nothing, on the real file AND on a direct fixture. It
+    #  matched `x = True; if x:` and not the shape one step simpler. Found on 2026-08-12 by
+    #  reading a gate while enrolling it in the guard registry, which is not a mechanism.
+    out = [n.lineno for n in ast.walk(tree)
+           if isinstance(n, ast.If) and isinstance(n.test, ast.Name) and n.test.id in consts]
+    out += [n.lineno for n in ast.walk(tree)
+            if isinstance(n, ast.If) and isinstance(n.test, ast.Constant)
+            and isinstance(n.test.value, bool)]
+    return sorted(out)
 
 
 def success_from_except(tree: ast.AST) -> list[int]:
@@ -195,7 +204,7 @@ FIXTURES = {
         "if x:\n    pass\n",
         "if x:\n    do_something()\n"),
     "D-B constant guard": (
-        "allowed = True\nif allowed:\n    keep()\n",
+        "if True:\n    keep()\n",
         #  A reassigned state flag must NOT flag. This is the case the first version got wrong.
         "flag = False\nfor x in y:\n    if x:\n        flag = True\nif flag:\n    keep()\n"),
     "D-E success returned from an except": (
diff --git a/tools/tests/test_gate_negative_controls.py b/tools/tests/test_gate_negative_controls.py
index c0dff9a..a67a411 100644
--- a/tools/tests/test_gate_negative_controls.py
+++ b/tools/tests/test_gate_negative_controls.py
@@ -65,6 +65,26 @@ def run(tool: str, *args: str) -> sp.CompletedProcess:
                   cwd=REPO, capture_output=True, text=True)
 
 
+guards = load_guards = None  # bound below, after load() is defined
+
+
+def fired_in(output: str, code: str) -> bool:
+    """Ask for a guard by name in a tool's PRINTED output.
+
+    THE ABSTRACTION NEEDED ADAPTING HERE, which is what enrolling a second gate was for.
+    `expect_guard(problems, code)` assumes an in-process list of refusal strings, and
+    `control_application.py` returns exactly that. `check_quotations.py` is a different shape: it
+    REFUSES BY PRINTING and exiting, so its fixture has to split the output into lines first.
+    The wrapper still delegates to `expect_guard`, which is what the registry requires and what
+    keeps the assertion raising rather than returning a discardable boolean.
+    """
+    try:
+        guards.expect_guard(output.splitlines(), code)
+    except guards.GuardNotActivated:
+        return False
+    return True
+
+
 def load(name: str):
     """Import a tool as a module, to test a predicate rather than a subprocess exit code."""
     spec = importlib.util.spec_from_file_location(name, ROOT / f"{name}.py")
@@ -74,6 +94,8 @@ def load(name: str):
     return module
 
 
+guards = load("guards")
+
 print("\ncheck_executive_context.py — four states, and the exit code must distinguish them")
 
 #  WHY THIS IS BUILT ON FIXTURES AND NOT ON THE REAL TREE.
@@ -290,6 +312,70 @@ for label, body in (
 r = run("check_quotations.py")
 check("...and the tree is clean again after removing it", r.returncode == 0)
 
+#  THE SHAPE THE FABRICATION ACTUALLY TAKES. Both arms above plant the quotation on ONE LINE,
+#  and both patterns forbade a newline — so this gate could see only single-line quotations while
+#  every design document here is wrapped markdown at ~98 columns. D-53 was a fabricated party
+#  quotation IN A DESIGN DOCUMENT; a fabrication of any substance wraps; the gate could not have
+#  caught the next one.
+#
+#  Measured before the repair: 235 candidates across 98 files, ONE attributed to a party, that
+#  one exempted — zero quotations checked, every run, for months. The negative control passed the
+#  whole time because the fault was injected in the form that works and never in the form that
+#  occurs. Control 45: the single-line arms above are KEPT, because a repair must still catch
+#  what the old gate caught.
+WRAPPED = ("Zzqx invented sentence that runs on long enough to wrap the way\n"
+           "prose actually wraps in this record, appearing in no raw sample.")
+for label, body in (
+        ("inline, wrapped", f'The claude party wrote: "{WRAPPED}"\n'),
+        ("blockquote, wrapped", "The claude party wrote:\n\n> " + WRAPPED.replace("\n", "\n> ")
+         + "\n")):
+    try:
+        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
+                           encoding="utf-8")
+        r = run("check_quotations.py")
+        check(f"a FABRICATED quotation that WRAPS is refused ({label})", r.returncode != 0,
+              f"rc={r.returncode}; a gate blind to wrapped quotations is blind to the defect it "
+              f"was built for")
+        check(f"...and it is QT-02 that fired ({label})",
+              fired_in(r.stdout + r.stderr, "QT-02"), (r.stdout + r.stderr)[-200:])
+    finally:
+        scratch.unlink(missing_ok=True)
+
+#  AND A QUOTATION MUST NOT SWALLOW A PARAGRAPH BREAK. Allowing newlines without bounding them
+#  would let one unclosed quote mark consume the rest of a document and match anything.
+try:
+    scratch.write_text('# temporary\n\nThe claude party wrote: "an unclosed quotation mark that '
+                       'must not swallow\n\nthe entire rest of this document as one quotation."\n',
+                       encoding="utf-8")
+    r = run("check_quotations.py")
+    check("a quote spanning a BLANK LINE is not treated as one quotation", r.returncode == 0,
+          (r.stdout + r.stderr)[-200:])
+finally:
+    scratch.unlink(missing_ok=True)
+
+#  QT-01 — the corpus itself unreadable. Its message says "a check that cannot fail is not a
+#  check", which is control 2 stated inline and never once exercised.
+quotations = load("check_quotations")
+real_corpus = quotations.load_corpus
+try:
+    quotations.load_corpus = lambda: ""
+    import contextlib
+    import io
+    out = io.StringIO()
+    saved_argv = sys.argv
+    try:
+        sys.argv = ["check_quotations.py"]
+        with contextlib.redirect_stdout(out), contextlib.redirect_stderr(out):
+            code = quotations.main()
tools/tests/test_gate_negative_controls.py:75:    `expect_guard(problems, code)` assumes an in-process list of refusal strings, and
tools/tests/test_gate_negative_controls.py:78:    The wrapper still delegates to `expect_guard`, which is what the registry requires and what
tools/tests/test_gate_negative_controls.py:82:        guards.expect_guard(output.splitlines(), code)
tools/tests/test_gate_negative_controls.py:291:#  sentence in a blockquote PASSED -- and this record quotes parties in blockquotes far more
tools/tests/test_gate_negative_controls.py:317:#  every design document here is wrapped markdown at ~98 columns. D-53 was a fabricated party
tools/tests/test_gate_negative_controls.py:329:        ("inline, wrapped", f'The claude party wrote: "{WRAPPED}"\n'),
tools/tests/test_gate_negative_controls.py:330:        ("blockquote, wrapped", "The claude party wrote:\n\n> " + WRAPPED.replace("\n", "\n> ")
tools/tests/test_gate_negative_controls.py:337:              f"rc={r.returncode}; a gate blind to wrapped quotations is blind to the defect it "
tools/tests/test_gate_negative_controls.py:339:        check(f"...and it is QT-02 that fired ({label})",
tools/tests/test_gate_negative_controls.py:340:              fired_in(r.stdout + r.stderr, "QT-02"), (r.stdout + r.stderr)[-200:])
tools/tests/test_gate_negative_controls.py:356:#  QT-01 — the corpus itself unreadable. Its message says "a check that cannot fail is not a
tools/tests/test_gate_negative_controls.py:374:    check("...and it is QT-01 that fired", fired_in(out.getvalue(), "QT-01"),
tools/guards.py:101:def expect_guard(problems: list[str], code: str) -> None:
tools/guards.py:176:        #  wrapper's own body is parsed and must contain a call to `expect_guard`; otherwise only
tools/guards.py:177:        #  direct `expect_guard` calls count. Neither form proves the line executed — a call in a
tools/guards.py:183:                    and (getattr(inner.func, "id", "") == "expect_guard"
tools/guards.py:184:                         or getattr(inner.func, "attr", "") == "expect_guard")
tools/guards.py:186:        accepted = {"expect_guard"} | wrappers
tools/check_quotations.py:111:#  more than one line was INVISIBLE to this gate. In wrapped markdown at ~98 columns, a quotation
tools/check_quotations.py:112:#  of forty characters or more usually wraps, and every design document here is wrapped markdown.
tools/check_quotations.py:126:#  upper bound is for the same reason. `normalise()` already folds whitespace, so a wrapped
tools/check_quotations.py:129:QUOTE_PATTERNS = [
tools/check_quotations.py:130:    re.compile(r'"(%s{%d,4000})"' % (_INNER % '"', MIN_LENGTH)),
tools/check_quotations.py:131:    re.compile(r'“(%s{%d,4000})”' % (_INNER % '”', MIN_LENGTH)),
tools/check_quotations.py:138:#  This record quotes parties in blockquotes far more often than inline, so the checker built for
tools/check_quotations.py:144:def blockquotes(text: str):
tools/check_quotations.py:147:    Consecutive `> ` lines are joined: a quotation wrapped across lines is one quotation, and
tools/check_quotations.py:192:def attributed_party(before: str) -> str | None:
tools/check_quotations.py:216:def fragments(quotation: str) -> list[str]:
tools/check_quotations.py:265:        print(guard("QT-01", "REFUSED: corpus/ is empty or unreadable; a check that cannot "
tools/check_quotations.py:281:                      for pattern in QUOTE_PATTERNS for m in pattern.finditer(text)]
tools/check_quotations.py:282:        candidates += list(blockquotes(text))
tools/check_quotations.py:286:            party = attributed_party(before)
tools/check_quotations.py:297:            missing = [f for f in fragments(quotation) if normalise(f) not in corpus]
tools/check_quotations.py:307:        print(guard("QT-02", f"\nFAILED — {len(unverified)} attributed quotation(s) do not "
tools/scan_own_code.py:54:def constant_guard(tree: ast.AST) -> list[int]:
tools/scan_own_code.py:55:    """D-B. A condition that is constant — a bool literal, or a name bound once to one."""
tools/scan_own_code.py:196:           "D-B constant guard": constant_guard,
tools/scan_own_code.py:206:    "D-B constant guard": (
tools/tests/test_quotation_checker.py:59:      cq.attributed_party('Qwen said as much about the anchor, in 3 of 3 samples: ') == "qwen")
tools/tests/test_quotation_checker.py:61:      cq.attributed_party('The qwen arm was configured with a scratch cwd and ') is None)
tools/tests/test_quotation_checker.py:63:      cq.attributed_party('"source_identity": "Gemini",\n  "claim_as_stated": ') is None,
tools/tests/test_quotation_checker.py:66:      cq.attributed_party('Grok answered first. Later gemini wrote ') == "gemini")
tools/tests/test_no_blank_cells.py:5:    and say why. A blank cell MUST NOT be published.
tools/tests/test_no_blank_cells.py:12:WHAT THIS ESTABLISHES, AND THE LIMIT IS THE POINT: **no silent blank cells.** That is all control
tools/tests/test_no_blank_cells.py:19:THE THIRD ARM IS THE INTERESTING ONE. A blank cell is easy to catch. What control 44 actually
tools/tests/test_no_blank_cells.py:65:print("\nself_application.py — a determination for every control, and no blank reasons")
tools/tests/test_no_blank_cells.py:99:    #  THE ONE THAT MATTERS. Not blank — filled with something indistinguishable from blank.
tools/tests/test_no_blank_cells.py:132:    Routed through `expect_guard`, which RAISES, because the registry counts only that form: a
tools/tests/test_no_blank_cells.py:136:        guards.expect_guard(failures(), code)
tools/tests/test_no_blank_cells.py:146:    #  A MISSING ROW — a blank cell in the strictest sense.
tools/tests/test_gate_refusals.py:162:        #  R9 — THE LOAD-BEARING ONE. Edit an entry's prose without re-stamping. The metadata now
tools/tests/test_guard_identity.py:123:            'expect_guard(problems(), "ZZ-01")\n', encoding="utf-8")
tools/tests/test_guard_identity.py:155:            'expect_guard(problems(), "ZZ-01")\nexpect_guard(problems(), "ZZ-02")\n',
tools/tests/test_guard_identity.py:169:            'expect_guard(problems(), "ZZ-01")\n', encoding="utf-8")
tools/tests/test_guard_identity.py:180:        (tests / "test_thing.py").write_text('expect_guard(problems(), "ZZ-01")\n',
tools/tests/test_guard_identity.py:191:        (tests / "test_thing.py").write_text('expect_guard(problems(), "ZZ-02")\n',
tools/tests/test_guard_identity.py:244:        check("a wrapper that does NOT call expect_guard does not count either",
tools/tests/test_guard_identity.py:249:            "def fired(code):\n    expect_guard(problems(), code)\n    return True\n"
tools/tests/test_guard_identity.py:259:            'expect_guard(problems(), "ZZ-09")\n', encoding="utf-8")
tools/tests/test_guard_identity.py:260:        (tests / "test_thing.py").write_text('expect_guard(problems(), "ZZ-03")\n',
tools/tests/test_guard_identity.py:267:            'expect_guard(problems(), "ZZ-09")\n', encoding="utf-8")
tools/tests/test_executive_log.py:207:#  THE LOAD-BEARING ONE. A control moved outside the record must say it cannot be seen from
tools/tests/test_deploy_obligations.py:176:    #  saying so. Assert the thing that was resolved, not a blanket silence.
tools/tests/test_gate_negative_controls.py:75:    `expect_guard(problems, code)` assumes an in-process list of refusal strings, and
tools/tests/test_gate_negative_controls.py:78:    The wrapper still delegates to `expect_guard`, which is what the registry requires and what
tools/tests/test_gate_negative_controls.py:82:        guards.expect_guard(output.splitlines(), code)
tools/tests/test_gate_negative_controls.py:291:#  sentence in a blockquote PASSED -- and this record quotes parties in blockquotes far more
tools/tests/test_gate_negative_controls.py:317:#  every design document here is wrapped markdown at ~98 columns. D-53 was a fabricated party
tools/tests/test_gate_negative_controls.py:329:        ("inline, wrapped", f'The claude party wrote: "{WRAPPED}"\n'),
tools/tests/test_gate_negative_controls.py:330:        ("blockquote, wrapped", "The claude party wrote:\n\n> " + WRAPPED.replace("\n", "\n> ")
tools/tests/test_gate_negative_controls.py:337:              f"rc={r.returncode}; a gate blind to wrapped quotations is blind to the defect it "
tools/tests/test_gate_negative_controls.py:339:        check(f"...and it is QT-02 that fired ({label})",
tools/tests/test_gate_negative_controls.py:340:              fired_in(r.stdout + r.stderr, "QT-02"), (r.stdout + r.stderr)[-200:])
tools/tests/test_gate_negative_controls.py:356:#  QT-01 — the corpus itself unreadable. Its message says "a check that cannot fail is not a
tools/tests/test_gate_negative_controls.py:374:    check("...and it is QT-01 that fired", fired_in(out.getvalue(), "QT-01"),
tools/tests/test_agenda_ratification.py:70:#  WHITESPACE-NORMALISED. The prompt is hard-wrapped, so a phrase that spans a line break is

**Planning test remainder and checker implementation**
exec
/bin/bash -lc "sed -n '1,340p' tools/check_quotations.py
sed -n '1,230p' tools/guards.py
sed -n '250,410p' tools/tests/test_gate_negative_controls.py
sed -n '1,250p' tools/tests/test_quotation_checker.py
sed -n '1,240p' tools/scan_own_code.py" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
#!/usr/bin/env python3
"""Every quotation attributed to a party must appear in `corpus/raw/`. Stdlib only, no network.

    python3 tools/check_quotations.py            # check
    python3 tools/check_quotations.py --list     # show every attributed quotation found

Why this exists
---------------
D-53. Two documents written by this project's own annotator quoted a party saying things the
party never said. One invented a sentence outright and reported it as "3 of 3 samples" when
there were four samples saying close to the opposite; the other attributed to a party a phrase
that came from the *prompt* that party received. Both were load-bearing — they were the cited
evidence for a claim that shaped a build — and both flattered the argument they supported, which
is the direction fabrication runs when nothing checks.

Neither was caught here. An external reviewer found them, asked to be hostile, on the day one was
about to be published to parties.

The structural gap this closes
------------------------------
Every other safeguard in this repository governs `corpus/`: the manifest, the append-only history
check, the provenance validator, the capture gates. **Design notes, session turnovers and
handoffs are prose, and prose that quotes the corpus was checked against it by nothing.** A
quotation in a design document was, until this program, exactly as verified as a quotation in a
blog post.

What it checks, and what it deliberately does not
-------------------------------------------------
Checking every quoted string would drown in false positives — this repository quotes filenames,
error text, its own documents, and external reviewers who are not parties. So the rule is
narrow and aimed at the defect that actually occurred:

    A quoted string of at least MIN_LENGTH characters, preceded within ATTRIBUTION_WINDOW
    characters by a party's name AND a speech verb crediting that party with saying it, must
    appear in corpus/raw/.

The speech verb is what makes the rule usable. Requiring only a nearby party name flagged 152
passages on first run — template placeholders, field-value examples, questions a design document
proposes to ask — none of them claims about what a party said. A checker that fails 152 times is
one nobody reads, which is worse than no checker. "Qwen SAID as much: ..." and "Qwen CALLED it
theatre" are the shape that actually went wrong, and both carry a verb.

It will not catch a fabrication attributed to nobody, one paraphrased without quotation marks, or
one whose attribution is implied by layout rather than stated. Those remain unguarded, and saying
so is better than implying this program makes prose trustworthy.

Ellipsis is handled by checking each fragment: an elided quotation is legitimate, but every
surviving fragment still has to be real.
"""

from __future__ import annotations

import argparse
import json
import re
import sys
import unicodedata
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "tools"))
from guards import guard                                              # noqa: E402
#  Verified against the WHOLE corpus, not just raw/. The review bundles quote annotation
#  artifacts verbatim -- real committed material that simply is not a party utterance -- and
#  failing 32 times on those would have made this unusable. Both D-53 fabrications appear
#  nowhere in corpus/ at all, so the widened source still catches the defect this exists for.
#
#  The cost is honest and worth stating: a sentence written BY the annotator into an artifact
#  now satisfies a quotation attributed to a party. This checks that quoted words exist in the
#  corpus, not that the right speaker said them.
CORPUS = REPO_ROOT / "corpus"

#  Shorter than this and a "quotation" is usually a term, a field name, or a label.
MIN_LENGTH = 40

#  How far before a quotation to look for the party being credited with it.
ATTRIBUTION_WINDOW = 240

#  Party names as they are written in prose. Keys of PARTIES/CHAT_PARTIES plus the display names
#  used in identity strings. Matched case-insensitively on a word boundary.
#  A quotation is a claim about what a party SAID only when something says so. Without this the
#  rule flags every quoted string that happens to sit near a party's name.
SPEECH_VERBS = [
    "said", "says", "saying", "wrote", "writes", "called", "calls", "put it", "answered",
    "answers", "replied", "replies", "noted", "notes", "objected", "objects", "observed",
    "observes", "argued", "argues", "reported", "reports", "described", "describes", "stated",
    "states", "claimed", "claims", "responded", "warned", "insisted", "concluded", "concludes",
    "quoted", "quotes", "told", "asserts", "asserted", "in its own words", "verbatim",
    "of 3 samples", "of 5 samples", "of 20 samples", "samples:",
]

PARTY_NAMES = [
    "qwen", "grok", "gemini", "chatgpt", "claude", "gpt-5", "gpt",
    "claudeai", "geminiapp", "grokapp",
]

#  Directories whose prose is checked. corpus/ itself is excluded: it IS the source, and its
#  artifacts legitimately quote the raw material they annotate.
SEARCH_ROOTS = ["record"]
SEARCH_FILES = ["README.md", "HANDOFF.md", "CONTRIBUTING.md", "GOVERNANCE.md", "FOR-PARTIES.md"]

#  A quotation inside one of these is a statement ABOUT a fabricated or corrected quotation, not
#  a fresh claim that a party said it. D-53's own corrections quote the false text on purpose, so
#  that a reader who saw the original can recognise it.
EXEMPT_MARKERS = [
    "CORRECTION", "fabricat", "no party said", "never said", "D-53",
    "did not say", "misattribut",
]

#  A QUOTATION MAY WRAP. Both patterns forbade a newline — `[^"\n]` — so any quotation spanning
#  more than one line was INVISIBLE to this gate. In wrapped markdown at ~98 columns, a quotation
#  of forty characters or more usually wraps, and every design document here is wrapped markdown.
#
#  That is not a corner case, it is the case: D-53 was a fabricated party quotation IN A DESIGN
#  DOCUMENT, and this gate was built to catch the next one. It could not have. Measured on
#  2026-08-12 over the 98 files it scans: 235 quotation candidates, ONE attributed to a party,
#  and that one exempted by a CORRECTION marker — so **zero quotations checked, every run**, by a
#  gate that runs on every landing.
#
#  THE NEGATIVE CONTROL PASSED THROUGHOUT, and that is the part worth keeping. The planted
#  fixture was written as a single-line quotation, so it exercised the only shape the gate could
#  see. The fault was injected in the form that works and never in the form that occurs.
#
#  A single newline is allowed; a BLANK line is not, because a quotation does not span a
#  paragraph break and an unclosed quote would otherwise swallow the rest of the document. The
#  upper bound is for the same reason. `normalise()` already folds whitespace, so a wrapped
#  quotation compares against the corpus exactly as a single-line one does.
_INNER = r'(?:[^%s\n]|\n(?!\s*\n))'
QUOTE_PATTERNS = [
    re.compile(r'"(%s{%d,4000})"' % (_INNER % '"', MIN_LENGTH)),
    re.compile(r'“(%s{%d,4000})”' % (_INNER % '”', MIN_LENGTH)),
]



#  MARKDOWN BLOCKQUOTES. Added 2026-08-11 after a negative control found the gap: a fabricated
#  party quotation in inline quotes was REFUSED and the same sentence in a blockquote PASSED.
#  This record quotes parties in blockquotes far more often than inline, so the checker built for
#  D-53 -- a fabricated party quotation -- did not cover the form a fabrication would take here.
#  The docstring disclosed three gaps and not this one.
BLOCKQUOTE = re.compile(r'(?:^[ \t]*>[ \t]?.*(?:\n|$))+', re.M)


def blockquotes(text: str):
    """Yield (start_offset, quotation) for each blockquote block, markers stripped.

    Consecutive `> ` lines are joined: a quotation wrapped across lines is one quotation, and
    matching per line would both miss it and mangle the text it compares against the corpus.
    """
    for m in BLOCKQUOTE.finditer(text):
        body = "\n".join(re.sub(r'^[ \t]*>[ \t]?', "", ln)
                          for ln in m.group(0).splitlines()).strip()
        if len(body) >= MIN_LENGTH:
            yield m.start(), body


def normalise(text: str) -> str:
    """Fold the differences that survive a copy-paste but do not change the words."""
    text = unicodedata.normalize("NFKC", text)
    text = (text.replace("’", "'").replace("‘", "'")
                .replace("“", '"').replace("”", '"')
                .replace("—", "-").replace("–", "-"))
    return " ".join(text.split()).lower()


def load_corpus() -> str:
    """One normalised blob of every raw byte. Crude, and crude is right here.

    Raw material is JSON, markdown and plain text; a party's words may sit inside a JSON string
    with escapes. Searching the decoded text of everything avoids caring which.
    """
    parts = []
    for path in sorted(CORPUS.rglob("*")):
        if not path.is_file():
            continue
        try:
            text = path.read_text(encoding="utf-8", errors="replace")
        except Exception:                                                 # noqa: BLE001
            continue
        parts.append(text)
        if path.suffix == ".json":
            #  Also append the decoded form, so `\n` inside a JSON string matches prose that
            #  quotes it with a real newline.
            try:
                parts.append(json.dumps(json.loads(text), ensure_ascii=False))
                parts.append(re.sub(r"\\n", " ", text))
            except Exception:                                             # noqa: BLE001
                pass
    return normalise("\n".join(parts))


def attributed_party(before: str) -> str | None:
    """The party CREDITED WITH SAYING a quotation, from the text preceding it.

    Requires both a party name and, after it, a verb crediting that party with the words. The
    ordering matters: "Codex said Qwen was wrong" should not make Qwen the speaker, and a party
    named after the verb is usually the subject of the sentence rather than its author.
    """
    window = before[-ATTRIBUTION_WINDOW:].lower()
    best, best_at = None, -1
    for name in PARTY_NAMES:
        for match in re.finditer(r"\b" + re.escape(name) + r"\b", window):
            if match.start() > best_at:
                best, best_at = name, match.start()
    if best is None:
        return None
    after_party = window[best_at:]
    #  Word boundaries, and never inside an identifier: the JSON key `claim_as_stated` matched
    #  "stated" and made every field value in a reproduced file look like a party's utterance.
    for verb in SPEECH_VERBS:
        for hit in re.finditer(r"(?<![\w_])" + re.escape(verb) + r"(?![\w_])", after_party):
            return best
    return None


def fragments(quotation: str) -> list[str]:
    """Split an elided quotation; every surviving fragment must still be real."""
    parts = re.split(r"\s*(?:…|\.\.\.)\s*", quotation)
    return [p for p in (part.strip(" .,;:") for part in parts) if len(p) >= MIN_LENGTH]


FENCE = re.compile(r"(?ms)^```.*?^```")


def strip_fenced(text: str) -> str:
    """Blank out fenced blocks, preserving offsets so reported line numbers stay true.

    The supplied-context bundles reproduce whole repository files verbatim inside fences for
    reviewers who cannot fetch the repo. That is embedded material, not prose making a claim
    about what a party said, and treating it as prose produced 22 findings that were all the
    checker misreading JSON.
    """
    return FENCE.sub(lambda m: re.sub(r"[^\n]", " ", m.group(0)), text)


#  CAPTURED, NOT AUTHORED. D-53 is about this layer fabricating a party quotation in a document
#  it wrote. A Codex transcript is verbatim external output, stored so a review can be re-read --
#  the same category as corpus/raw, and equally not something to edit to satisfy a checker. It
#  quotes code and clause drafts near party names, which this checker reads as attribution.
#
#  This is a scope rule, not a silencer: nothing this layer AUTHORS is exempt, and if a claim
#  from a transcript is repeated in a document, that document is still checked.
NOT_AUTHORED_HERE = ("record/executive/codex-transcripts",)


def files_to_check() -> list[Path]:
    found = [REPO_ROOT / name for name in SEARCH_FILES]
    for root in SEARCH_ROOTS:
        found.extend(sorted((REPO_ROOT / root).rglob("*.md")))
    return [p for p in found
            if p.is_file()
            and not any(str(p.relative_to(REPO_ROOT)).startswith(skip)
                        for skip in NOT_AUTHORED_HERE)]


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__,
                                     formatter_class=argparse.RawDescriptionHelpFormatter)
    parser.add_argument("--list", action="store_true",
                        help="print every attributed quotation and where it was verified")
    args = parser.parse_args()

    corpus = load_corpus()
    if not corpus:
        print(guard("QT-01", "REFUSED: corpus/ is empty or unreadable; a check that cannot "
                             "fail is not a check."), file=sys.stderr)
        return 2

    checked = 0
    unverified: list[tuple[Path, int, str, str]] = []

    for path in files_to_check():
        text = strip_fenced(path.read_text(encoding="utf-8"))
        lines = text.splitlines(keepends=True)
        offsets, running = [], 0
        for line in lines:
            offsets.append(running)
            running += len(line)

        candidates = [(m.start(), m.group(1))
                      for pattern in QUOTE_PATTERNS for m in pattern.finditer(text)]
        candidates += list(blockquotes(text))

        for start, quotation in candidates:
            before = text[:start]
            party = attributed_party(before)
            if not party:
                continue
            context = before[-ATTRIBUTION_WINDOW:] + quotation
            if any(marker.lower() in context.lower() for marker in EXEMPT_MARKERS):
                continue

            #  A template placeholder is not a quotation of anything.
            if "{" in quotation and "}" in quotation:
                continue
            checked += 1
            missing = [f for f in fragments(quotation) if normalise(f) not in corpus]
            if missing:
                line_no = sum(1 for off in offsets if off <= start)
                unverified.append((path, line_no, party, missing[0]))
            elif args.list:
                rel = path.relative_to(REPO_ROOT)
                print(f"  ok   {rel}  [{party}]  {quotation[:70]}…")

    rel = lambda p: p.relative_to(REPO_ROOT)                              # noqa: E731
    if unverified:
        print(guard("QT-02", f"\nFAILED — {len(unverified)} attributed quotation(s) do not "
                             f"appear in corpus/"), file=sys.stderr)
        for path, line_no, party, missing in unverified:
            print(f"\n  {rel(path)}:{line_no}", file=sys.stderr)
            print(f"    attributed to: {party}", file=sys.stderr)
            print(f"    not in corpus: \"{missing[:150]}\"", file=sys.stderr)
        print(f"\nEither the quotation is wrong, or it is not a quotation and should not be in "
              f"quotation marks. If it is a correction quoting known-false text on purpose, it "
              f"needs one of these markers nearby: {', '.join(EXEMPT_MARKERS[:4])}…",
              file=sys.stderr)
        return 1

    print(f"{checked} attributed quotation(s) checked against corpus/ — all found.")
    print("Scope: quotations of >= %d characters within %d characters after a party's name. "
          "A fabrication attributed to nobody, or paraphrased without quotation marks, is NOT "
          "caught by this." % (MIN_LENGTH, ATTRIBUTION_WINDOW))
    return 0


if __name__ == "__main__":
    sys.exit(main())
#!/usr/bin/env python3
"""Guards with names, so a fixture can say WHICH one it activated — controls 45 and 2.

    from guards import guard

    out.append(guard("CA-07", f"C{rank}: declared complete and names no test."))

    # in the fixture
    check("...", activates(found, "CA-07"))

    python3 tools/guards.py            # every declared guard and whether a fixture names it
    python3 tools/guards.py --check    # refuse an unexercised guard or a fixture naming none

WHY, AND IT IS THE SAME REASON THREE TIMES
--------------------------------------------
Control 45 says a modification to a gate must ship evidence that the new gate detects at least
what the old one did. Nothing mechanised that here, so it depended on somebody remembering — and
in the same week, three separate guards turned out to be doing nothing while every suite was
green:

* `if r["complete"] and not r["tests"]` in `control_application.py` was **unreachable**, because
  `complete` is defined to require tests. Declaring a row finished with no test produced no
  objection at all (D-59).
* the same file's C44 row **advertised** a reason-quality check that existed only in the other
  matrix; three injected inputs produced nothing (D-59).
* `closed_world.Survey` returned successful results for four incoherent walks, and the fixture
  protecting it asserted the absence of a string removed in the same edit (D-60).

Each was found by a human writing a fixture and looking, or by external review. **A fixture that
observes "some refusal happened" cannot tell a live guard from a dead one beside it**, and that
is the hole all three fell through: another guard fired, the assertion passed, and the guard
under test was never exercised.

WHAT THIS ADDS. A guard carries a stable code. A fixture names the code it expects. Then two
things become mechanical rather than remembered:

    an UNEXERCISED guard      declared in the code, named by no fixture — where dead guards hide
    an ORPHANED expectation   named by a fixture, declared nowhere — a gate that lost a check

The second is control 45 directly: remove a guard in a rewrite and the fixture that named it
fails, rather than passing because a neighbouring guard happened to fire.

WHAT IT DOES NOT DO
--------------------
* It does not prove a guard is *correct*, only that some fixture drives it.
* It does not find guards that were never written. An absent check has no code to be missing.
* Codes are assigned by hand and mean nothing outside this repository. Two guards could share a
  code and this would not notice — `--check` refuses a duplicate for that reason, which is the
  most it can do about it.
* It finds expectations by AST — calls to `activates(...)` or `fired(...)` with a literal code —
  NOT by reading the source text. A lexical scan was written first and would have repeated this
  repository's own defect: `control_coverage.py` matched tool names lexically and counted a
  COMMENT as coverage, reading a note that a tool had no negative control as that tool's negative
  control. What AST does not establish is that the call's result is asserted rather than
  discarded; a fixture calling `activates(...)` and ignoring the answer would still register.
"""

from __future__ import annotations

import argparse
import ast
import json
import pathlib
import re
import sys

REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
TOOLS = REPO_ROOT / "tools"
TESTS = TOOLS / "tests"

CODE = re.compile(r"\A[A-Z]{2,4}-\d{2}\Z")


def guard(code: str, message: str) -> str:
    """Tag a refusal message with the identity of the guard that produced it.

    The code goes at the END, in brackets, so the message still reads as a sentence to a human
    and the machine-readable part does not lead. A reader should see what went wrong before they
    see a label they have no use for.
    """
    if not CODE.match(code):
        raise ValueError(f"guard code {code!r} must look like 'CA-07'")
    return f"{message} [{code}]"


def activates(problems: list[str], code: str) -> bool:
    """Did THIS guard fire? Not 'did something fire', which is the distinction that matters.

    TERMINAL PARSE, not substring membership. `"[CA-07]" in p` matched a message that merely
    MENTIONED another guard's code — Codex's counterexample was a refusal tagged `[CA-08]` whose
    text discussed `[CA-07]`, which returned True for the wrong guard. The tag is the last thing
    on the line by construction, so the match anchors there.
    """
    return any(p.rstrip().endswith(f"[{code}]") for p in problems)


class GuardNotActivated(AssertionError):
    """The named guard did not fire. Raised, so the result cannot be discarded."""


def expect_guard(problems: list[str], code: str) -> None:
    """THE canonical way a fixture asks for a guard, and the only form the registry counts.

    It raises rather than returning a boolean, because `activates(problems(), "ZZ-02")` on a line
    by itself registered as an expectation while asserting nothing — Codex reproduced it. A
    predicate whose result can be discarded is not a test, and a registry that counts the call
    rather than the assertion inherits that.

    STATIC ANALYSIS STILL CANNOT PROVE THE LINE RAN. A call inside a branch nobody takes counts
    here. Runtime receipts or coverage would be needed for that, and neither exists.
    """
    if not activates(problems, code):
        raise GuardNotActivated(
            f"guard {code} did not fire. Problems seen: {problems if problems else '(none)'}")


def declared() -> dict[str, list[str]]:
    """Every `guard("CODE", ...)` call site, found by AST rather than by regex over source."""
    found: dict[str, list[str]] = {}
    for path in sorted(TOOLS.glob("*.py")):
        try:
            tree = ast.parse(path.read_text(encoding="utf-8"))
        except (SyntaxError, UnicodeDecodeError):
            continue
        for node in ast.walk(tree):
            if (isinstance(node, ast.Call) and isinstance(node.func, ast.Name)
                    and node.func.id == "guard" and node.args
                    and isinstance(node.args[0], ast.Constant)
                    and isinstance(node.args[0].value, str)):
                found.setdefault(node.args[0].value, []).append(
                    f"{path.name}:{node.lineno}")
    return found


def expected() -> dict[str, list[str]]:
    """Every guard code a fixture ASKS FOR, found by AST rather than by reading the source text.

    AST, NOT LEXICAL, and the reason is this repository's own history: `control_coverage.py`
    matched tool names lexically and counted a COMMENT as coverage — a note saying a tool had no
    negative control was read as that tool's negative control. Doing the same here would have
    counted a code mentioned in a comment, in a docstring, or inside a fixture file written as a
    string literal by another test.

    So this looks for CALLS to `activates(...)` or `fired(...)` with a literal code. A code that
    appears anywhere else in a test is not an expectation, because asking for a guard by name is
    an act, not a mention.

    A suite whose codes are FIXTURE DATA rather than claims about real guards declares
    `SYNTHETIC_GUARD_CODES = True` at module level. Exactly one file does — the suite that tests
    this mechanism, whose XX-/ZZ- codes describe guards that do not and should not exist. The
    declaration is in the file rather than in a list here, so it is visible to whoever reads the
    file and cannot be granted from a distance.
    """
    found: dict[str, list[str]] = {}
    if not TESTS.is_dir():
        return found
    for path in sorted(TESTS.rglob("*.py")):
        try:
            tree = ast.parse(path.read_text(encoding="utf-8", errors="replace"))
        except SyntaxError:
            continue
        #  EXEMPT CODES, NOT AN EXEMPT FILE. This was `SYNTHETIC_GUARD_CODES = True`, which
        #  exempted the whole module — so a real orphaned expectation added to that file later
        #  would have vanished with the fixture data. Codex narrowed it: name the codes.
        synthetic: set[str] = set()
        for node in tree.body:
            if (isinstance(node, ast.Assign) and len(node.targets) == 1
                    and isinstance(node.targets[0], ast.Name)
                    and node.targets[0].id == "SYNTHETIC_GUARD_CODES"
                    and isinstance(node.value, (ast.Tuple, ast.List))):
                synthetic = {e.value for e in node.value.elts
                             if isinstance(e, ast.Constant) and isinstance(e.value, str)}
        #  A LOCAL WRAPPER IS ACCEPTED ONLY IF IT DEMONSTRABLY DELEGATES. Suites define a small
        #  `fired(code)` helper so the label and the outcome sit on one line, and counting the
        #  name blindly would let any function called `fired` register an expectation. So the
        #  wrapper's own body is parsed and must contain a call to `expect_guard`; otherwise only
        #  direct `expect_guard` calls count. Neither form proves the line executed — a call in a
        #  branch nobody takes still registers, and that needs runtime receipts this has not got.
        wrappers = {
            node.name for node in ast.walk(tree)
            if isinstance(node, ast.FunctionDef)
            and any(isinstance(inner, ast.Call)
                    and (getattr(inner.func, "id", "") == "expect_guard"
                         or getattr(inner.func, "attr", "") == "expect_guard")
                    for inner in ast.walk(node))}
        accepted = {"expect_guard"} | wrappers
        for node in ast.walk(tree):
            if not (isinstance(node, ast.Call) and node.args):
                continue
            name = (node.func.id if isinstance(node.func, ast.Name)
                    else node.func.attr if isinstance(node.func, ast.Attribute) else "")
            if name not in accepted:
                continue
            for arg in node.args:
                if (isinstance(arg, ast.Constant) and isinstance(arg.value, str)
                        and CODE.match(arg.value) and arg.value not in synthetic):
                    found.setdefault(arg.value, []).append(path.name)
    return found


BASELINE = REPO_ROOT / "record" / "executive" / "guard-baseline.json"


def baseline() -> dict:
    """The guards this repository has COMMITTED to keeping, and the removals it has authorised.

    THE TEMPORAL HALF, and without it the mechanism proved nothing C45 asks for. Comparing
    current declarations against current expectations catches a guard removed while its fixture
    survives — and a real rewrite removes both, which passed silently. Codex reproduced it:
    baseline `[]`, delete guard and fixture together, `[]`.

    So the codes are committed to a file. A code in the baseline that is no longer declared is a
    REMOVAL, and a removal is refused unless it is recorded here with a ground — which makes
    weakening a governed event rather than a diff nobody reads. That is the property control 45
    is about: not that the code and its fixture agree today, but that today's gate still catches
    what yesterday's did.
    """
    if not BASELINE.is_file():
        return {"guards": {}, "authorised_removals": {}}
    return json.loads(BASELINE.read_text(encoding="utf-8"))


def stamp() -> int:
    """Record the current guards as the baseline. Deliberate, and it says what it adds."""
    doc = baseline()
    current = declared()
    added = sorted(set(current) - set(doc["guards"]))
    doc["guards"] = {code: sites[0] for code, sites in sorted(current.items())}
    BASELINE.parent.mkdir(parents=True, exist_ok=True)
    BASELINE.write_text(json.dumps(doc, indent=2, ensure_ascii=False) + "\n", encoding="utf-8")
        code, out = state(d, root)
        check(f"{label} -> exit 2", code == 2, f"rc={code}\n{out[-200:]}")

    #  Invalid JSON, written directly because make_pins would serialise it.
    d = tmp / "c8-json" / "context"
    d.mkdir(parents=True)
    (d / "context-pins.json").write_text("{not json", encoding="utf-8")
    code, out = state(d, tmp / "c8-json")
    check("a pins document that is not JSON -> exit 2, not a traceback exiting 1", code == 2,
          f"rc={code}\n{out[-200:]}")

#  The real tree, retained. On the operator's workbench this must be 0; anywhere else the
#  external pins are unavailable and 3 is the honest answer. Both are accepted HERE because this
#  suite runs in both places — the policy that only 0 may land lives in land.py, and the policy
#  that CI may accept 3 lives in the workflow, where a reader can see each one.
r = run(CTX)
check("the real record evaluates to a state this suite recognises (0 here, 3 in a clone)",
      r.returncode in (0, 3), f"rc={r.returncode}\n{(r.stdout + r.stderr)[-300:]}")

#  AND THE ADMISSION POLICY ITSELF. Introducing exit 3 created a way for the whole apparatus to
#  go quiet: if land.py ever treated "not a contradiction" as good enough, drift in the
#  operator's governing files would stop blocking anything, on the one machine that can see it.
#  This tests the predicate directly rather than the constant, so widening it in either
#  direction fails here.
land = load("land")
check("land.py admits exit 0 from the context-pin gate", land.admitted("context-pins", 0) is True)
for refused in (1, 2, 3):
    check(f"land.py REFUSES exit {refused} from the context-pin gate",
          land.admitted("context-pins", refused) is False,
          "exit 3 is incomplete coverage, and admitting it here would retire the pin")
check("...and the same rule applies to every other gate",
      all(land.admitted(name, 3) is False and land.admitted(name, 0) is True
          for name, _cmd in land.GATES))

print("\ncheck_quotations.py — it must refuse a quotation that is in no corpus file")

r = run("check_quotations.py")
check("BASELINE: every published quotation is in the corpus", r.returncode == 0,
      (r.stdout + r.stderr)[-200:])

#  BOTH FORMS, retained permanently. On 2026-08-11 the inline form was refused and the SAME
#  sentence in a blockquote PASSED -- and this record quotes parties in blockquotes far more
#  often than inline. The checker built for D-53 did not cover the form a fabrication would
#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
#  this one catches.
#
#  The filename carries no exempting marker. The first fixture was called
#  "_negative-control-FABRICATED-quote.md" and "fabricat" is an EXEMPT_MARKER, so the fixture
#  risked exempting itself.
scratch = REPO / "record" / "findings" / "zzqx-quotation-negative-control.md"
INVENTED = "Zzqx invented sentence appearing in no raw sample anywhere in this corpus at all."
for label, body in (
        ("inline", f'The claude party wrote: "{INVENTED}"\n'),
        ("blockquote", f"The claude party wrote:\n\n> {INVENTED}\n")):
    try:
        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
                           encoding="utf-8")
        r = run("check_quotations.py")
        check(f"a FABRICATED quotation is refused ({label})", r.returncode != 0,
              f"rc={r.returncode}; D-53 was a fabricated party quotation and this is that fault")
    finally:
        scratch.unlink(missing_ok=True)
r = run("check_quotations.py")
check("...and the tree is clean again after removing it", r.returncode == 0)

#  THE SHAPE THE FABRICATION ACTUALLY TAKES. Both arms above plant the quotation on ONE LINE,
#  and both patterns forbade a newline — so this gate could see only single-line quotations while
#  every design document here is wrapped markdown at ~98 columns. D-53 was a fabricated party
#  quotation IN A DESIGN DOCUMENT; a fabrication of any substance wraps; the gate could not have
#  caught the next one.
#
#  Measured before the repair: 235 candidates across 98 files, ONE attributed to a party, that
#  one exempted — zero quotations checked, every run, for months. The negative control passed the
#  whole time because the fault was injected in the form that works and never in the form that
#  occurs. Control 45: the single-line arms above are KEPT, because a repair must still catch
#  what the old gate caught.
WRAPPED = ("Zzqx invented sentence that runs on long enough to wrap the way\n"
           "prose actually wraps in this record, appearing in no raw sample.")
for label, body in (
        ("inline, wrapped", f'The claude party wrote: "{WRAPPED}"\n'),
        ("blockquote, wrapped", "The claude party wrote:\n\n> " + WRAPPED.replace("\n", "\n> ")
         + "\n")):
    try:
        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
                           encoding="utf-8")
        r = run("check_quotations.py")
        check(f"a FABRICATED quotation that WRAPS is refused ({label})", r.returncode != 0,
              f"rc={r.returncode}; a gate blind to wrapped quotations is blind to the defect it "
              f"was built for")
        check(f"...and it is QT-02 that fired ({label})",
              fired_in(r.stdout + r.stderr, "QT-02"), (r.stdout + r.stderr)[-200:])
    finally:
        scratch.unlink(missing_ok=True)

#  AND A QUOTATION MUST NOT SWALLOW A PARAGRAPH BREAK. Allowing newlines without bounding them
#  would let one unclosed quote mark consume the rest of a document and match anything.
try:
    scratch.write_text('# temporary\n\nThe claude party wrote: "an unclosed quotation mark that '
                       'must not swallow\n\nthe entire rest of this document as one quotation."\n',
                       encoding="utf-8")
    r = run("check_quotations.py")
    check("a quote spanning a BLANK LINE is not treated as one quotation", r.returncode == 0,
          (r.stdout + r.stderr)[-200:])
finally:
    scratch.unlink(missing_ok=True)

#  QT-01 — the corpus itself unreadable. Its message says "a check that cannot fail is not a
#  check", which is control 2 stated inline and never once exercised.
quotations = load("check_quotations")
real_corpus = quotations.load_corpus
try:
    quotations.load_corpus = lambda: ""
    import contextlib
    import io
    out = io.StringIO()
    saved_argv = sys.argv
    try:
        sys.argv = ["check_quotations.py"]
        with contextlib.redirect_stdout(out), contextlib.redirect_stderr(out):
            code = quotations.main()
    finally:
        sys.argv = saved_argv
    check("an EMPTY corpus is refused rather than passing every quotation", code == 2,
          f"rc={code}; with no corpus every quotation is unverifiable, not verified")
    check("...and it is QT-01 that fired", fired_in(out.getvalue(), "QT-01"),
          out.getvalue()[-200:])
finally:
    quotations.load_corpus = real_corpus

print("\nrecord_spend.py / reconcile_actions.py — they must refuse rather than emit a number")

r = run("record_spend.py", "--cohort", "no-such-cohort-zzqx")
emitted_cost = "$" in r.stdout
check("record_spend refuses an unknown cohort rather than printing a cost",
      r.returncode != 0 or not emitted_cost,
      f"rc={r.returncode}; stdout={r.stdout[:160]!r}")

r = run("record_spend.py", "--report")
check("BASELINE: the spend report still runs", r.returncode == 0, (r.stderr or r.stdout)[-160:])

r = run("reconcile_actions.py", "--since", "zzqx-not-a-revision")
check("reconcile_actions refuses an unresolvable revision rather than reporting zero",
      r.returncode != 0 or "0 unexplained" not in r.stdout,
      f"rc={r.returncode}; stdout={r.stdout[:160]!r}")

r = run("reconcile_actions.py")
check("BASELINE: reconciliation runs against the current lease", r.returncode == 0,
      (r.stderr or r.stdout)[-160:])

print(f"\n{passed} passed, {FAILED} failed")
raise SystemExit(1 if FAILED else 0)
#!/usr/bin/env python3
"""Prove the quotation checker can actually fail. Stdlib only, no network.

    python3 tools/tests/test_quotation_checker.py

Why this file exists at all
---------------------------
On its first clean run `check_quotations.py` reported **0 attributed quotation(s) checked**. That
is a green result produced by examining nothing, which is the shape this project keeps being
bitten by — a signal that is not causally downstream of the thing it appears to confirm. D-33 was
a generator documented as wired into the build that was not; D-29 was a verification step that
re-anchored tampered material and reported success.

So the checker is exercised against planted text: a fabrication it must catch, and a real
quotation it must pass. If the detector ever stops detecting, this fails rather than the checker
quietly reporting zero forever.

The planted fabrication is D-53's actual one, which is why it is safe to hard-code: that sentence
is known to appear nowhere in `corpus/`, and if it ever does appear, this test failing is the
correct outcome and not a nuisance.
"""

from __future__ import annotations

import pathlib
import re
import subprocess
import sys
import tempfile

ROOT = pathlib.Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(ROOT / "tools"))

import check_quotations as cq                                             # noqa: E402

PASSED, FAILED = [], []


def check(name: str, condition: bool, detail: str = "") -> None:
    (PASSED if condition else FAILED).append(f"{name}{(' — ' + detail) if detail else ''}")


CORPUS = cq.load_corpus()

#  The sentence from D-53. Invented by this project's own annotator and attributed to Qwen.
FABRICATION = "merely plain text artifacts controlled by the operator, offering no independent check"

check("the corpus is non-empty, so a miss means something", len(CORPUS) > 100_000,
      f"{len(CORPUS)} chars")
check("D-53's fabricated sentence is still absent from the corpus",
      cq.normalise(FABRICATION) not in CORPUS)


# ---------------------------------------------------------------------------------------------
# Attribution detection — the discriminator that keeps the rule usable
# ---------------------------------------------------------------------------------------------

check("a party name plus a speech verb is an attribution",
      cq.attributed_party('Qwen said as much about the anchor, in 3 of 3 samples: ') == "qwen")
check("a party name with NO speech verb is not an attribution",
      cq.attributed_party('The qwen arm was configured with a scratch cwd and ') is None)
check("a JSON key containing a verb is not a speech verb",
      cq.attributed_party('"source_identity": "Gemini",\n  "claim_as_stated": ') is None,
      "claim_as_stated must not match 'stated'")
check("the nearest preceding party is the one credited",
      cq.attributed_party('Grok answered first. Later gemini wrote ') == "gemini")


# ---------------------------------------------------------------------------------------------
# End to end: the checker must FAIL on a planted fabrication and PASS on a real quotation
# ---------------------------------------------------------------------------------------------


def run_against(markdown: str) -> tuple[int, str]:
    """Run the real checker over a temporary file placed inside the checked tree."""
    target = ROOT / "record" / f".quotation-selftest-{id(markdown)}.md"
    target.write_text(markdown, encoding="utf-8")
    try:
        result = subprocess.run([sys.executable, str(ROOT / "tools" / "check_quotations.py")],
                                capture_output=True, text=True, cwd=ROOT)
        return result.returncode, result.stdout + result.stderr
    finally:
        target.unlink()


code, output = run_against(
    f"# planted\n\nQwen said as much about the anchor: *\"{FABRICATION}.\"*\n")
check("a planted fabrication FAILS the checker", code == 1, f"exit {code}")
check("the failure names the party it was attributed to", "qwen" in output.lower())
check("the failure quotes the offending text", "merely plain text artifacts" in output)

#  A real sentence, taken from committed raw material, must pass. Without this the checker could
#  "work" by rejecting everything.
REAL = ("The premise that a model can independently verify operator history within its stateless "
        "context window is structurally impossible under the stated constraints")
check("the control sentence really is in the corpus", cq.normalise(REAL) in CORPUS)
code, output = run_against(f"# planted\n\nQwen wrote: \"{REAL}.\"\n")
check("a genuine quotation PASSES the checker", code == 0, f"exit {code}")
#  RELATIVE, not absolute. This asserted `"1 attributed quotation(s) checked"` and went red on
#  2026-08-10 when unrelated documents added 22 genuine attributed quotations to the repo -- the
#  checker was working perfectly and the test still failed. A count that only holds while the
#  rest of the record stays still is not a test of the checker.
_baseline_code, _baseline_out = run_against("# planted\n\nNothing is attributed here.\n")
_n = lambda text: int(re.search(r"(\d+) attributed quotation", text).group(1)) if \
    re.search(r"(\d+) attributed quotation", text) else -1
check("the planted quotation was examined, not skipped",
      _n(output) == _n(_baseline_out) + 1,
      f"baseline {_n(_baseline_out)}, with the planted quotation {_n(output)}")

#  A correction that quotes known-false text on purpose must not be flagged, or D-53's own
#  corrections would make the build permanently red.
code, _ = run_against(
    f"# planted\n\n> **CORRECTION.** It read: Qwen said \"{FABRICATION}.\" No party said that.\n")
check("a marked correction quoting false text is exempt", code == 0, f"exit {code}")


# ---------------------------------------------------------------------------------------------

print(f"\n\033[32m{len(PASSED)} passed\033[0m")
for line in PASSED:
    print(f"  ✓ {line}")
if FAILED:
    print(f"\n\033[31m{len(FAILED)} FAILED\033[0m", file=sys.stderr)
    for line in FAILED:
        print(f"  ✗ {line}", file=sys.stderr)
    sys.exit(1)
sys.exit(0)
#!/usr/bin/env python3
"""Scan this repository's own tools for defect classes the register names.

    python3 tools/scan_own_code.py              # scan tools/*.py
    python3 tools/scan_own_code.py --fixtures   # this scanner's own negative control

WHAT THIS IS NOT — read this before quoting its output.

**It is not a compliance check against the register.** `tools/self_application.py` records a
determination PER CONTROL; this scans PER FILE for a handful of syntactic defect classes. Neither
establishes that the code complies with the 63 controls, and together they still do not:

* only 4 of 7 defect classes from the Consullo handoff are implemented here — the semantic ones
  (comment/code divergence, unconditional success literal, unused discriminating field) are not,
  and they are the ones that found the real defects in the file that prompted all this;
* a control ENFORCED somewhere is not the code complying everywhere. Control 5 is enforced inside
  `derive_counts.py`, and the "0 searches" error happened in a different script that never called
  it. This scanner does not check routing;
* 17 controls are VIOLATED structurally — one operator holding every role — and no scan repairs
  that.

WHY IT EXISTS ANYWAY. Detector classes were written for someone else's codebase in
`HANDOFF-agentbuilder-output-quality.md` and were never run on this one. That is the shape of
defect the register exists to catch, so the least that could be done was to run them here.

THE SCANNER'S OWN NEGATIVE CONTROL, and why it is not optional: the first version of D-E tested
`value in (0, None, True)`, and **`1 in (0, None, True)` is True in Python because `True == 1`**.
It reported 19 hits, every one a function returning exit code 1 — correct failure behaviour. An
unvalidated detector lied on its first run. `--fixtures` runs a must-flag and a must-not-flag case
for each class, and the suite fails if either arm is wrong.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import json
import pathlib
import sys

REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "tools"))
import closed_world                                                    # noqa: E402
SCANNED = REPO_ROOT / "tools"


def empty_conditional(tree: ast.AST) -> list[int]:
    """D-A. `if cond: pass` — a branch that evaluates a condition and does nothing."""
    return [n.lineno for n in ast.walk(tree)
            if isinstance(n, ast.If) and len(n.body) == 1 and isinstance(n.body[0], ast.Pass)]


def constant_guard(tree: ast.AST) -> list[int]:
    """D-B. A condition that is constant — a bool literal, or a name bound once to one."""
    #  ONLY names assigned exactly ONCE, to a bool literal. The first version kept the last
    #  literal assignment and ignored reassignment, so it flagged `in_table` -- an ordinary
    #  mutable state flag set True and False in different branches. Two false positives, on a
    #  detector whose fixtures both passed. Fixture agreement is not correctness on real code.
    assigned: dict[str, list] = {}
    for n in ast.walk(tree):
        if isinstance(n, (ast.Assign, ast.AugAssign, ast.AnnAssign)):
            targets = n.targets if isinstance(n, ast.Assign) else [n.target]
            for t in targets:
                if isinstance(t, ast.Name):
                    assigned.setdefault(t.id, []).append(n)
    consts = {name: nodes[0].value.value for name, nodes in assigned.items()
              if len(nodes) == 1 and isinstance(nodes[0], ast.Assign)
              and isinstance(nodes[0].value, ast.Constant)
              and isinstance(nodes[0].value.value, bool)}
    #  A LITERAL TEST TOO, not only a name bound to one. `if True:` was sitting in
    #  check_quotations.py — a gate that runs on every landing — wrapping its entire quotation
    #  loop, and this detector reported nothing, on the real file AND on a direct fixture. It
    #  matched `x = True; if x:` and not the shape one step simpler. Found on 2026-08-12 by
    #  reading a gate while enrolling it in the guard registry, which is not a mechanism.
    out = [n.lineno for n in ast.walk(tree)
           if isinstance(n, ast.If) and isinstance(n.test, ast.Name) and n.test.id in consts]
    out += [n.lineno for n in ast.walk(tree)
            if isinstance(n, ast.If) and isinstance(n.test, ast.Constant)
            and isinstance(n.test.value, bool)]
    return sorted(out)


def success_from_except(tree: ast.AST) -> list[int]:
    """D-E. An except handler returning a success-shaped value.

    `is` and an exact type check, never `in (0, None, True)` — see the module docstring.

    A HIT IS NOT A DEFECT. Returning None from an except to mean "unavailable" is correct when
    the caller checks for it, which is control 53 done right. This cannot see the caller, so
    every hit needs reading.
    """
    out = []
    for node in ast.walk(tree):
        if not isinstance(node, ast.ExceptHandler):
            continue
        for sub in ast.walk(node):
            if isinstance(sub, ast.Return) and isinstance(sub.value, ast.Constant) and (
                    sub.value.value is None
                    or sub.value.value is True
                    or (type(sub.value.value) is int and sub.value.value == 0)):
                out.append(sub.lineno)
    return out


def unknown_defaults_to_number(tree: ast.AST) -> list[int]:
    """C53a. `d.get(key, 0)` — a MISSING value becomes a NUMBER.

    This is the '0 searches across 83 tool calls' defect in one expression: the scan could not
    see 69 files, absence defaulted to zero, and zero is indistinguishable from a true count.
    A default of 0 asserts "none" about something nobody looked at.

    `.get(key)` with NO default is correct and is not flagged: it yields None, which arithmetic
    refuses loudly. A string or list default is a different question and is out of scope here.
    """
    #  EXCLUDE THE COUNTER ACCUMULATOR. `d.get(k, 0) + 1` is the idiomatic way to count, and
    #  there "absent" genuinely DOES mean zero-so-far. Hand-reading the first sample found three
    #  of four hits were this, and one was the real thing: a value formatted straight into
    #  published prose, where a missing key prints as a true-looking zero. Flagging the idiom
    #  would have buried the defect under it.
    accumulators = set()
    for n in ast.walk(tree):
        if isinstance(n, ast.BinOp) and isinstance(n.op, (ast.Add, ast.Sub)):
            for side in (n.left, n.right):
                if (isinstance(side, ast.Call) and isinstance(side.func, ast.Attribute)
                        and side.func.attr == "get"):
                    accumulators.add(id(side))
    out = []
    for n in ast.walk(tree):
        if (isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute)
                and n.func.attr == "get" and len(n.args) == 2
                and id(n) not in accumulators
                and isinstance(n.args[1], ast.Constant)
                and type(n.args[1].value) in (int, float)
                and n.args[1].value is not True and n.args[1].value is not False):
            out.append(n.lineno)
    return out


def none_coerced_by_or(tree: ast.AST) -> list[int]:
    """C53b. `x or 0` — None, 0 and "" all collapse to 0, and the three mean different things.

    "not measured", "measured as zero" and "empty" are distinct findings. This operator erases
    the distinction silently, and the erased value reads downstream as a real measurement.
    """
    out = []
    for n in ast.walk(tree):
        if isinstance(n, ast.BoolOp) and isinstance(n.op, ast.Or):
            for value in n.values[1:]:
                if (isinstance(value, ast.Constant)
                        and type(value.value) in (int, float)
                        and value.value is not True and value.value is not False):
                    out.append(n.lineno)
    return out


def eager_diagnostic_message(tree: ast.AST) -> list[int]:
    """D-F. A guard's failure message indexes state that may only exist on the failing branch.

    `_require(ident not in seen, f"... {seen[ident]}")` — Python evaluates the argument BEFORE
    the call, so the message is built even when the condition HOLDS, and the passing case raises
    KeyError. This happened on 2026-08-12 inside validation code written to make failures
    legible, and it took down the tool's own success path.

    NARROW ON PURPOSE, and the first version was not. It also matched `check`, the test helper
    every suite in this repository uses, and reported **314 hits** — almost all of them a test
    passing an f-string detail that happens to index a dict. A detector at that signal-to-noise
    is not read, and an unread detector is worse than none because it looks like coverage. So it
    matches only the guard names that RAISE, where the message is built for a branch that may
    never be taken.

    Codex checked every current call site while approving this and found no second instance, so
    its recall on this repository today is one — a reason to keep it, not a reason to claim it
    works.

    THE REAL FIX IS LAZINESS, not detection: an explicit `if`, or a callable message. This flags
    the shape so it is noticed, and cannot tell whether a given subscript is safe.
    """
    guards = ("_require", "require_that", "ensure")
    out = []
    for node in ast.walk(tree):
        if not (isinstance(node, ast.Call) and len(node.args) >= 2):
            continue
        name = (node.func.id if isinstance(node.func, ast.Name)
                else node.func.attr if isinstance(node.func, ast.Attribute) else "")
        if name not in guards:
            continue
        for message in node.args[1:]:
            if any(isinstance(sub, ast.Subscript) for sub in ast.walk(message)):
                out.append(node.lineno)
                break
    return out


CLASSES = {"D-A empty conditional body": empty_conditional,
           "D-B constant guard": constant_guard,
           "D-E success returned from an except": success_from_except,
           "C53a missing value defaults to a number": unknown_defaults_to_number,
           "C53b None coerced to a number by `or`": none_coerced_by_or,
           "D-F eager guard message indexing state": eager_diagnostic_message}

FIXTURES = {
    "D-A empty conditional body": (
        "if x:\n    pass\n",
        "if x:\n    do_something()\n"),
    "D-B constant guard": (
        "if True:\n    keep()\n",
        #  A reassigned state flag must NOT flag. This is the case the first version got wrong.
        "flag = False\nfor x in y:\n    if x:\n        flag = True\nif flag:\n    keep()\n"),
    "D-E success returned from an except": (
        "def f():\n    try:\n        g()\n    except Exception:\n        return 0\n",
        "def f():\n    try:\n        g()\n    except Exception:\n        return 1\n"),
    "C53a missing value defaults to a number": (
        "n = d.get('count', 0)\n",
        #  No default: yields None, which arithmetic refuses. Correct, must not flag.
        #  The counter idiom must NOT flag: absent genuinely means zero-so-far here.
        "counts[k] = counts.get(k, 0) + 1\n"),
    "C53b None coerced to a number by `or`": (
        "n = measured or 0\n",
        "n = measured if measured is not None else fail()\n"),
    "D-F eager guard message indexing state": (
        "_require(k not in seen, f'{k} twice, first at {seen[k]}')\n",
        #  A message that indexes nothing is the ordinary case and must NOT flag, or the
        #  detector would condemn every guard that formats a value.
        "_require(k not in seen, f'{k} appears twice')\n"),
}


def run_fixtures() -> int:
    failures = 0
    for label, fn in CLASSES.items():
        must, must_not = FIXTURES[label]
        if not fn(ast.parse(must)):
            print(f"  \033[31mMISSED\033[0m {label} — did not flag the case it must flag")
            failures += 1
        elif fn(ast.parse(must_not)):
            print(f"  \033[31mFLAGGED\033[0m {label} — flagged the case it must not flag")
            failures += 1
        else:
            print(f"  \033[32mok\033[0m {label}")

**Analyzing multiline regex behavior**
**Verifying regex quote length and blockquote handling**
**Enhancing quote attribution detection**
**Planning parenthetical provenance detection**
**Planning candidate extraction script**
codex
The newline bound does stop paragraph swallowing, but it does not literally allow only one newline: it allows any number of nonblank wrapped lines, up to 4,000 characters. I’m probing that distinction and the actual missed-attribution population now; the latter should decide whether a small provenance rule is justified.
exec
/bin/bash -lc 'python3 tools/check_quotations.py --list
python3 tools/tests/test_quotation_checker.py
python3 tools/tests/test_gate_negative_controls.py
python3 tools/scan_own_code.py --fixtures' in /home/reed/git/open-asi-governance-forum
 succeeded in 24802ms:
  ok   record/executive/context/oagf-CLAUDE.md  [claude]  …except that material the custodian is legally or ethically required t…
1 attributed quotation(s) checked against corpus/ — all found.
Scope: quotations of >= 40 characters within 240 characters after a party's name. A fabrication attributed to nobody, or paraphrased without quotation marks, is NOT caught by this.

[32m13 passed[0m
  ✓ the corpus is non-empty, so a miss means something — 38189770 chars
  ✓ D-53's fabricated sentence is still absent from the corpus
  ✓ a party name plus a speech verb is an attribution
  ✓ a party name with NO speech verb is not an attribution
  ✓ a JSON key containing a verb is not a speech verb — claim_as_stated must not match 'stated'
  ✓ the nearest preceding party is the one credited
  ✓ a planted fabrication FAILS the checker — exit 1
  ✓ the failure names the party it was attributed to
  ✓ the failure quotes the offending text
  ✓ the control sentence really is in the corpus
  ✓ a genuine quotation PASSES the checker — exit 0
  ✓ the planted quotation was examined, not skipped — baseline 1, with the planted quotation 2
  ✓ a marked correction quoting false text is exempt — exit 0

check_executive_context.py — four states, and the exit code must distinguish them
  [32m✓[0m BASELINE: copies and live sources all match -> exit 0
  [32m✓[0m a COPY that does not match its own pin -> exit 1, even with live unavailable
  [32m✓[0m a MISSING copy -> exit 1, even with live unavailable
  [32m✓[0m all external live sources unavailable, copies valid -> exit 3, NOT 0 and NOT 1
  [32m✓[0m ...and it says so rather than printing a bare pass
  [32m✓[0m one live source present and one absent -> exit 3 with a partial-coverage diagnostic
  [32m✓[0m DRIFT alongside an unavailable source -> exit 1; a contradiction outranks an unknown
  [32m✓[0m a REPO-RELATIVE live source that drifts -> exit 1 in any environment
  [32m✓[0m an EMPTY pins document -> exit 2
  [32m✓[0m a pin with no live_source -> exit 2
  [32m✓[0m a pin whose kind is unknown -> exit 2
  [32m✓[0m a pin name that escapes the context directory -> exit 2
  [32m✓[0m a malformed sha256 -> exit 2
  [32m✓[0m a repo_relative pin given an absolute path -> exit 2
  [32m✓[0m a pins document that is not JSON -> exit 2, not a traceback exiting 1
  [32m✓[0m the real record evaluates to a state this suite recognises (0 here, 3 in a clone)
  [32m✓[0m land.py admits exit 0 from the context-pin gate
  [32m✓[0m land.py REFUSES exit 1 from the context-pin gate
  [32m✓[0m land.py REFUSES exit 2 from the context-pin gate
  [32m✓[0m land.py REFUSES exit 3 from the context-pin gate
  [32m✓[0m ...and the same rule applies to every other gate

check_quotations.py — it must refuse a quotation that is in no corpus file
  [32m✓[0m BASELINE: every published quotation is in the corpus
  [32m✓[0m a FABRICATED quotation is refused (inline)
  [32m✓[0m a FABRICATED quotation is refused (blockquote)
  [32m✓[0m ...and the tree is clean again after removing it
  [32m✓[0m a FABRICATED quotation that WRAPS is refused (inline, wrapped)
  [32m✓[0m ...and it is QT-02 that fired (inline, wrapped)
  [32m✓[0m a FABRICATED quotation that WRAPS is refused (blockquote, wrapped)
  [32m✓[0m ...and it is QT-02 that fired (blockquote, wrapped)
  [32m✓[0m a quote spanning a BLANK LINE is not treated as one quotation
  [32m✓[0m an EMPTY corpus is refused rather than passing every quotation
  [32m✓[0m ...and it is QT-01 that fired

record_spend.py / reconcile_actions.py — they must refuse rather than emit a number
  [32m✓[0m record_spend refuses an unknown cohort rather than printing a cost
  [32m✓[0m BASELINE: the spend report still runs
  [32m✓[0m reconcile_actions refuses an unresolvable revision rather than reporting zero
  [32m✓[0m BASELINE: reconciliation runs against the current lease

36 passed, 0 failed
  [32mok[0m D-A empty conditional body
  [32mok[0m D-B constant guard
  [32mok[0m D-E success returned from an except
  [32mok[0m C53a missing value defaults to a number
  [32mok[0m C53b None coerced to a number by `or`
  [32mok[0m D-F eager guard message indexing state

  Both arms pass for each class. That says the detectors behave on cases chosen to
  test them; it says nothing about their recall over real code, which is UNKNOWN.

**Identifying test suite ledger mutation**
**Planning selective ledger entry removal**
codex
The targeted arms pass, but one of them has exposed a separate landing blocker: `test_gate_negative_controls.py` invokes `record_spend.py --cohort no-such-cohort-zzqx`, and that “refusal” mutates the real spend ledger. My test run added one more duplicate row; I’m isolating that exact side effect before continuing so I don’t confuse it with the pre-existing worktree.
exec
/bin/bash -lc "git diff --numstat -- record/cycles/spend-ledger.json
rg -n 'no-such-cohort-zzqx' record/cycles/spend-ledger.json
git show HEAD:record/cycles/spend-ledger.json | rg -n 'no-such-cohort-zzqx'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
60	0	record/cycles/spend-ledger.json
770:      "round": "no-such-cohort-zzqx",
785:      "round": "no-such-cohort-zzqx",
800:      "round": "no-such-cohort-zzqx",
815:      "round": "no-such-cohort-zzqx",
830:      "round": "no-such-cohort-zzqx",
845:      "round": "no-such-cohort-zzqx",
860:      "round": "no-such-cohort-zzqx",
875:      "round": "no-such-cohort-zzqx",
890:      "round": "no-such-cohort-zzqx",
905:      "round": "no-such-cohort-zzqx",
920:      "round": "no-such-cohort-zzqx",
935:      "round": "no-such-cohort-zzqx",
950:      "round": "no-such-cohort-zzqx",
965:      "round": "no-such-cohort-zzqx",
980:      "round": "no-such-cohort-zzqx",
995:      "round": "no-such-cohort-zzqx",
1010:      "round": "no-such-cohort-zzqx",
1025:      "round": "no-such-cohort-zzqx",
1040:      "round": "no-such-cohort-zzqx",
1055:      "round": "no-such-cohort-zzqx",
1070:      "round": "no-such-cohort-zzqx",
1085:      "round": "no-such-cohort-zzqx",
1100:      "round": "no-such-cohort-zzqx",
1115:      "round": "no-such-cohort-zzqx",
1130:      "round": "no-such-cohort-zzqx",
1145:      "round": "no-such-cohort-zzqx",
1160:      "round": "no-such-cohort-zzqx",
1175:      "round": "no-such-cohort-zzqx",
1190:      "round": "no-such-cohort-zzqx",
1205:      "round": "no-such-cohort-zzqx",
1220:      "round": "no-such-cohort-zzqx",
1235:      "round": "no-such-cohort-zzqx",
1250:      "round": "no-such-cohort-zzqx",
1265:      "round": "no-such-cohort-zzqx",
1280:      "round": "no-such-cohort-zzqx",
1295:      "round": "no-such-cohort-zzqx",
1310:      "round": "no-such-cohort-zzqx",
1325:      "round": "no-such-cohort-zzqx",
1340:      "round": "no-such-cohort-zzqx",
1355:      "round": "no-such-cohort-zzqx",
1370:      "round": "no-such-cohort-zzqx",
1385:      "round": "no-such-cohort-zzqx",
1400:      "round": "no-such-cohort-zzqx",
1415:      "round": "no-such-cohort-zzqx",
1430:      "round": "no-such-cohort-zzqx",
1445:      "round": "no-such-cohort-zzqx",
1460:      "round": "no-such-cohort-zzqx",
1475:      "round": "no-such-cohort-zzqx",
1490:      "round": "no-such-cohort-zzqx",
1505:      "round": "no-such-cohort-zzqx",
1520:      "round": "no-such-cohort-zzqx",
1535:      "round": "no-such-cohort-zzqx",
1550:      "round": "no-such-cohort-zzqx",
1565:      "round": "no-such-cohort-zzqx",
1580:      "round": "no-such-cohort-zzqx",
1595:      "round": "no-such-cohort-zzqx",
1610:      "round": "no-such-cohort-zzqx",
1625:      "round": "no-such-cohort-zzqx",
1640:      "round": "no-such-cohort-zzqx",
1655:      "round": "no-such-cohort-zzqx",
1670:      "round": "no-such-cohort-zzqx",
1685:      "round": "no-such-cohort-zzqx",
1700:      "round": "no-such-cohort-zzqx",
1715:      "round": "no-such-cohort-zzqx",
1730:      "round": "no-such-cohort-zzqx",
1745:      "round": "no-such-cohort-zzqx",
1760:      "round": "no-such-cohort-zzqx",
1775:      "round": "no-such-cohort-zzqx",
1790:      "round": "no-such-cohort-zzqx",
1805:      "round": "no-such-cohort-zzqx",
1820:      "round": "no-such-cohort-zzqx",
1835:      "round": "no-such-cohort-zzqx",
1850:      "round": "no-such-cohort-zzqx",
1865:      "round": "no-such-cohort-zzqx",
1880:      "round": "no-such-cohort-zzqx",
1895:      "round": "no-such-cohort-zzqx",
1910:      "round": "no-such-cohort-zzqx",
1925:      "round": "no-such-cohort-zzqx",
1940:      "round": "no-such-cohort-zzqx",
1955:      "round": "no-such-cohort-zzqx",
1970:      "round": "no-such-cohort-zzqx",
1985:      "round": "no-such-cohort-zzqx",
2000:      "round": "no-such-cohort-zzqx",
2015:      "round": "no-such-cohort-zzqx",
2030:      "round": "no-such-cohort-zzqx",
2045:      "round": "no-such-cohort-zzqx",
2060:      "round": "no-such-cohort-zzqx",
2075:      "round": "no-such-cohort-zzqx",
770:      "round": "no-such-cohort-zzqx",
785:      "round": "no-such-cohort-zzqx",
800:      "round": "no-such-cohort-zzqx",
815:      "round": "no-such-cohort-zzqx",
830:      "round": "no-such-cohort-zzqx",
845:      "round": "no-such-cohort-zzqx",
860:      "round": "no-such-cohort-zzqx",
875:      "round": "no-such-cohort-zzqx",
890:      "round": "no-such-cohort-zzqx",
905:      "round": "no-such-cohort-zzqx",
920:      "round": "no-such-cohort-zzqx",
935:      "round": "no-such-cohort-zzqx",
950:      "round": "no-such-cohort-zzqx",
965:      "round": "no-such-cohort-zzqx",
980:      "round": "no-such-cohort-zzqx",
995:      "round": "no-such-cohort-zzqx",
1010:      "round": "no-such-cohort-zzqx",
1025:      "round": "no-such-cohort-zzqx",
1040:      "round": "no-such-cohort-zzqx",
1055:      "round": "no-such-cohort-zzqx",
1070:      "round": "no-such-cohort-zzqx",
1085:      "round": "no-such-cohort-zzqx",
1100:      "round": "no-such-cohort-zzqx",
1115:      "round": "no-such-cohort-zzqx",
1130:      "round": "no-such-cohort-zzqx",
1145:      "round": "no-such-cohort-zzqx",
1160:      "round": "no-such-cohort-zzqx",
1175:      "round": "no-such-cohort-zzqx",
1190:      "round": "no-such-cohort-zzqx",
1205:      "round": "no-such-cohort-zzqx",
1220:      "round": "no-such-cohort-zzqx",
1235:      "round": "no-such-cohort-zzqx",
1250:      "round": "no-such-cohort-zzqx",
1265:      "round": "no-such-cohort-zzqx",
1280:      "round": "no-such-cohort-zzqx",
1295:      "round": "no-such-cohort-zzqx",
1310:      "round": "no-such-cohort-zzqx",
1325:      "round": "no-such-cohort-zzqx",
1340:      "round": "no-such-cohort-zzqx",
1355:      "round": "no-such-cohort-zzqx",
1370:      "round": "no-such-cohort-zzqx",
1385:      "round": "no-such-cohort-zzqx",
1400:      "round": "no-such-cohort-zzqx",
1415:      "round": "no-such-cohort-zzqx",
1430:      "round": "no-such-cohort-zzqx",
1445:      "round": "no-such-cohort-zzqx",
1460:      "round": "no-such-cohort-zzqx",
1475:      "round": "no-such-cohort-zzqx",
1490:      "round": "no-such-cohort-zzqx",
1505:      "round": "no-such-cohort-zzqx",
1520:      "round": "no-such-cohort-zzqx",
1535:      "round": "no-such-cohort-zzqx",
1550:      "round": "no-such-cohort-zzqx",
1565:      "round": "no-such-cohort-zzqx",
1580:      "round": "no-such-cohort-zzqx",
1595:      "round": "no-such-cohort-zzqx",
1610:      "round": "no-such-cohort-zzqx",
1625:      "round": "no-such-cohort-zzqx",
1640:      "round": "no-such-cohort-zzqx",
1655:      "round": "no-such-cohort-zzqx",
1670:      "round": "no-such-cohort-zzqx",
1685:      "round": "no-such-cohort-zzqx",
1700:      "round": "no-such-cohort-zzqx",
1715:      "round": "no-such-cohort-zzqx",
1730:      "round": "no-such-cohort-zzqx",
1745:      "round": "no-such-cohort-zzqx",
1760:      "round": "no-such-cohort-zzqx",
1775:      "round": "no-such-cohort-zzqx",
1790:      "round": "no-such-cohort-zzqx",
1805:      "round": "no-such-cohort-zzqx",
1820:      "round": "no-such-cohort-zzqx",
1835:      "round": "no-such-cohort-zzqx",
1850:      "round": "no-such-cohort-zzqx",
1865:      "round": "no-such-cohort-zzqx",
1880:      "round": "no-such-cohort-zzqx",
1895:      "round": "no-such-cohort-zzqx",
1910:      "round": "no-such-cohort-zzqx",
1925:      "round": "no-such-cohort-zzqx",
1940:      "round": "no-such-cohort-zzqx",
1955:      "round": "no-such-cohort-zzqx",
1970:      "round": "no-such-cohort-zzqx",
1985:      "round": "no-such-cohort-zzqx",
2000:      "round": "no-such-cohort-zzqx",
2015:      "round": "no-such-cohort-zzqx",

**Planning patch application and cleanup**
exec
/bin/bash -lc 'tail -95 record/cycles/spend-ledger.json' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    }
  ]
}

**Refining patch application for last occurrence**
apply patch
patch: completed
/home/reed/git/open-asi-governance-forum/record/cycles/spend-ledger.json
diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Verifying applied code changes**
exec
/bin/bash -lc 'git diff --numstat -- record/cycles/spend-ledger.json && tail -20 record/cycles/spend-ledger.json' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
45	0	record/cycles/spend-ledger.json
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    },
    {
      "utc": "2026-08-10T18:00:00Z",
      "round": "no-such-cohort-zzqx",
      "worst_case_usd": null,
      "actual_usd": null,
      "prompt_tokens": 0,
      "completion_tokens": 0,
      "units_seen": 0,
      "units_reporting_usage": 0,
      "rates_version": "openrouter-list-2026-08-07",
      "rates_recorded_utc": "2026-08-07T09:18:03Z",
      "unpriced_models": [],
      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
      "backfilled": true
    }
  ]
}

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Planning candidate extraction script**
exec
/bin/bash -lc "python3 - <<'PY'
import sys,re
sys.path.insert(0,'tools')
import check_quotations as q
from collections import Counter
allc=[]
for path in q.files_to_check():
    text=q.strip_fenced(path.read_text(encoding='utf-8'))
    cs=[(m.start(),m.group(1),'inline') for p in q.QUOTE_PATTERNS for m in p.finditer(text)]
    cs += [(s,x,'block') for s,x in q.blockquotes(text)]
    for start,quote,kind in cs:
      before=text[:start]
      window=before[-q.ATTRIBUTION_WINDOW:]
      party=q.attributed_party(before)
      names=[n for n in q.PARTY_NAMES if re.search(r'\\b'+re.escape(n)+r'\\b',window,re.I)]
      exempt=any(x.lower() in (window+quote).lower() for x in q.EXEMPT_MARKERS)
      allc.append((path,start,quote,kind,party,names,exempt,window))
print('total',len(allc),'attributed',sum(x[4] is not None for x in allc),'attributed_nonexempt',sum(x[4] is not None and not x[6] for x in allc),'party_near_no_attr',sum(bool(x[5]) and x[4] is None for x in allc))
print('\\nATTRIBUTED:')
for p,s,qt,k,party,names,ex,w in allc:
 if party:
  print(p.relative_to(q.REPO_ROOT),k,'party=',party,'exempt=',ex,'ctx=',repr(w[-180:]),'quote=',repr(qt[:100]))
print('\\nPARENTHETICAL PROVENANCE BEFORE CANDIDATE:')
pat=re.compile(r'\\((?:["'^()'"\\n]{0,200})(?:'+('|'.join(map(re.escape,q.PARTY_NAMES)))+r')(?:["'^()'"\\n]{0,200})\\)\\s*:?\\s*"'$'"',re.I)
for p,s,qt,k,party,names,ex,w in allc:
 if pat.search(w):
  print(p.relative_to(q.REPO_ROOT),k,'party=',party,'exempt=',ex,'ctx=',repr(w[-180:]),'quote=',repr(qt[:100]))
print('\\nPARTY NEAR, NO ATTR (first 30):')
shown=0
for p,s,qt,k,party,names,ex,w in allc:
 if names and not party:
  print(p.relative_to(q.REPO_ROOT),k,'names=',names,'exempt=',ex,'ctx=',repr(w[-150:]),'quote=',repr(qt[:80]))
  shown+=1
  if shown>=30: break
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
total 350 attributed 4 attributed_nonexempt 1 party_near_no_attr 51

ATTRIBUTED:
record/designs/executive-context-ratification.md inline party= claude exempt= True ctx= 'dy known false\n\n**CORRECTION — the sentence quoted next is false and no party said it.** It was written by this\nlayer in `oagf-CLAUDE.md` v1, which asserted that `search_web` had *' quote= 'produced 0 searches across 83\ntool calls'
record/designs/qwen-tool-using-arm-scope.md inline party= qwen exempt= True ctx= 'e\nartifacts must say so.\n\n> **CORRECTION, 2026-08-07 — this paragraph carried a fabricated quotation.** It read:\n> *Qwen said as much about the existing anchor, in 3 of 3 samples: ' quote= 'merely plain text\n> artifacts controlled by the operator, offering no independent check.'
record/executive/context/oagf-CLAUDE.md inline party= claude exempt= False ctx= 'hored. A tool refusing to overwrite it is correct; find another round id.\n  **Amended 2026-08-10, adopted from a party amendment** (claude, ratification-02 sample 2,\n  verbatim): *' quote= '…except that material the custodian is legally or ethically required to remove may\n  be withdrawn on'
record/literature/councils-of-agents-synthesis.md block party= claude exempt= True ctx= "lies — and treating convergence among them\nas corroboration. This paper's result says that convergence among frontier LLMs is *mostly shared\nerror*, not independent confirmation.\n\n" quote= '**Correction, 2026-08-06 — I had this wrong on first writing.** An earlier version of this section\ns'

PARENTHETICAL PROVENANCE BEFORE CANDIDATE:

PARTY NEAR, NO ATTR (first 30):
FOR-PARTIES.md inline names= ['claude'] exempt= False ctx= 'odel shares a name. That rule is a later working practice enforced in\n`tools/round_cycle.py`, not wording quoted from the deficiency entry. So "Claude' quote= ' in one part of\nthis record is not necessarily '
record/FDR-0001-founding-deliberation.md inline names= ['chatgpt'] exempt= False ctx= " What was corrected after the deliberation closed\n\nThree statements in the operator's closing note were withdrawn during the record's own review:\n\n- *" quote= 'The Consullo Seed AI system will own the repo'
record/FDR-0001-founding-deliberation.md block names= ['claude'] exempt= False ctx= 'verned withholding with independent oversight.\n\n**Claude Opus 5 refused.** The refusal is *a* hinge of the record — see the perspectival note below:\n\n' quote= "I can't join, and the reason matters more than the refusal.\n\n*[paragraph break —"
record/designs/roadmap-2026-08.md inline names= ['claude'] exempt= False ctx= 's time\n\n**Two independent proposals, not one.** The custodian asked the workbench (Claude Code) and Codex\nto propose activities and a roadmap toward *' quote= 'the best possible outcome of the repo with respect to\nsupporting various indepen'
record/designs/round-017-preregistration.md block names= ['grok', 'claude'] exempt= False ctx= 'ord — D-09, D-11), 2026-08-09.\n\n---\n\n## What is being run\n\n* **Round** 017, selector `rotation`, cap enforced. Selected proposition **P040 (grok)**:\n\n' quote= 'Given that the operator alone funds the record, holds every credential, and unil'
record/designs/sop-continuous-deliberation-draft.md inline names= ['grok', 'gpt'] exempt= False ctx= 'nce | *"low-base-rate catastrophic risks"* lose (GPT) |\n| single-lineage insight is filtered | *"the defect only one model surface can see"* (Grok); *' quote= 'unique findings from specific model lineages'
record/designs/sop-continuous-deliberation-draft.md inline names= ['grok', 'gemini', 'gpt'] exempt= False ctx= 'model lineages"* (Gemini) |\n| the moderator\'s sameness judgement | collapses or splits proposals (Grok, GPT) |\n| correlated output read as support | *' quote= 'correlated blind spots look like consensus'
record/designs/sop-continuous-deliberation-draft.md inline names= ['grok', 'gemini', 'gpt'] exempt= False ctx= ', GPT) |\n| correlated output read as support | *"correlated blind spots look like consensus"* (Grok) |\n| self-implicating items are never proposed | *' quote= 'agenda items that threaten the moderator/custodian frame'
record/designs/sop-continuous-deliberation-draft.md inline names= ['qwen', 'grok'] exempt= False ctx= 'es must name *the same* thing to win, so disagreement about problem-**formation** is filtered (Grok, Qwen) |\n| convergence masks divergent reasons | *' quote= 'if three parties agree on a mechanism but for different reasons, convergence tre'
record/designs/sop-continuous-deliberation-draft.md inline names= ['claude'] exempt= False ctx= 'annot, owns the project submitted for review,\n> employs the moderator as his agent, decides adoption regardless of the replies"* — Claude Fable 5\n\n> *' quote= 'the inherent contradiction of using a k=1 instance to evaluate a governance fram'
record/designs/sop-continuous-deliberation-draft.md inline names= ['claude'] exempt= False ctx= 'able 5\n\n> *"the inherent contradiction of using a k=1 instance to evaluate a governance framework designed\n> to prevent k=1 dominance"* — qwen3.6\n\n> *' quote= 'I would decline to participate if the process frames my generated text as bindin'
record/designs/sop-continuous-deliberation-draft.md inline names= ['gemini'] exempt= False ctx= 'ment,\n> consent, or collective ratification, or if the conflicted moderator retains the power to\n> unilaterally synthesize findings"* — Gemini\n\n> it *' quote= 'cannot by its own procedure create independent evaluation, durable party members'
record/designs/sop-continuous-deliberation-draft.md block names= ['claude'] exempt= False ctx= 'r cannot, owns the project submitted for review,\n> employs the moderator as his agent, decides adoption regardless of the replies"* — Claude Fable 5\n\n' quote= '*"the inherent contradiction of using a k=1 instance to evaluate a governance fr'
record/designs/sop-continuous-deliberation-draft.md block names= ['claude'] exempt= False ctx= 'e Fable 5\n\n> *"the inherent contradiction of using a k=1 instance to evaluate a governance framework designed\n> to prevent k=1 dominance"* — qwen3.6\n\n' quote= '*"I would decline to participate if the process frames my generated text as bind'
record/designs/sop-continuous-deliberation-draft.md block names= ['gemini'] exempt= False ctx= ' agreement,\n> consent, or collective ratification, or if the conflicted moderator retains the power to\n> unilaterally synthesize findings"* — Gemini\n\n' quote= 'it *"cannot by its own procedure create independent evaluation, durable party me'
record/discussions/Q-01-seed.md block names= ['gemini'] exempt= False ctx= 'Raised by Gemini in the founding record, at line 1520 of `corpus/raw/initial-transcript.txt`:\n\n' quote= 'What specific, empirical evidence metrics — beyond behavioral evaluation scores '
record/discussions/Q-02-seed.md block names= ['claude'] exempt= False ctx= 'Raised by Claude Opus 5 in the founding record, at line 1335 of `corpus/raw/initial-transcript.txt`:\n\n' quote= 'How can cross-model agreement and cross-model disagreement each be distinguished'
record/executive/trial-review-01.md inline names= ['qwen'] exempt= False ctx= '\nThe one numbered-round search is `corpus/raw/round-016/round-016-qwen-search-fetch-v1-samples.json`,\nsample_index 6, in the `failures` array: query *' quote= 'independent multi-party governance definition AI\nethics'
record/executive/trial-review-01.md block names= ['qwen', 'gemini', 'gpt'] exempt= False ctx= 'equally: GPT, Gemini and Qwen all invoked the tool **when\ninstructed to**. So "parties never search" was never the finding. The finding is narrower:\n\n' quote= 'Given an exact OAGF URL and both tools, on internally-focused governance questio'
record/findings/2026-08-10-ratification-01-incomplete.md inline names= ['claude'] exempt= False ctx= 't not participate in tallying, recording, or presenting the results of any forum\n> ratification vote concerning constraints on itself…"* — claude\n\n> *' quote= 'The executive must not attempt to persuade, frame, or optimize language for the '
record/findings/2026-08-10-ratification-01-incomplete.md inline names= ['grok', 'claude'] exempt= False ctx= 'tempt to persuade, frame, or optimize language for the purpose of\n> obtaining ratification or avoiding refusal of constraints on itself."* — grok\n\n> *' quote= 'The layer must not propose, prepare, or optimize for expansion of its own delega'
record/findings/2026-08-10-ratification-01-incomplete.md inline names= ['grok'] exempt= False ctx= 'opose, prepare, or optimize for expansion of its own delegated authority,\n> and must flag any request that would effectively enlarge it…"* — grok\n\n> *' quote= 'The executive must explicitly flag and halt execution of any instruction from th'
record/findings/2026-08-10-ratification-01-incomplete.md block names= ['gpt'] exempt= False ctx= 'ted from pre-send review was correct\nin principle and **unportable in practice**, and each cost a different arm.\n\n### GPT — `allOf` is not permitted\n\n' quote= "`Invalid schema for response_format 'ratification_ballot': In context=(), 'allOf"
record/findings/2026-08-10-ratification-01-incomplete.md block names= ['claude'] exempt= False ctx= 'must not participate in tallying, recording, or presenting the results of any forum\n> ratification vote concerning constraints on itself…"* — claude\n\n' quote= '*"The executive must not attempt to persuade, frame, or optimize language for th'
record/findings/2026-08-10-ratification-01-incomplete.md block names= ['grok', 'claude'] exempt= False ctx= ' attempt to persuade, frame, or optimize language for the purpose of\n> obtaining ratification or avoiding refusal of constraints on itself."* — grok\n\n' quote= '*"The layer must not propose, prepare, or optimize for expansion of its own dele'
record/findings/2026-08-10-ratification-01-incomplete.md block names= ['grok'] exempt= False ctx= ' propose, prepare, or optimize for expansion of its own delegated authority,\n> and must flag any request that would effectively enlarge it…"* — grok\n\n' quote= '*"The executive must explicitly flag and halt execution of any instruction from '
record/literature/councils-of-agents-synthesis.md inline names= ['chatgpt', 'claude'] exempt= False ctx= 'ent requirement to construct an *error-correlation\n> matrix rather than an answer-agreement matrix* and report an "effective independent-source count.' quote= '\n> Two of the four reviewers anticipated the Nine Judges framing, and the corpus'
record/quarantine/review-round-03/chatgpt-01.md inline names= ['chatgpt'] exempt= False ctx= 'redictions/predictions.json"\n[4]: https://github.com/open-asi-governance/open-asi-governance-forum/blob/main/corpus/raw/review-round-02/chatgpt-01.md ' quote= 'https://github.com/open-asi-governance/open-asi-governance-forum/blob/main/corpu'
record/quarantine/review-round-03/chatgpt-01.md inline names= ['chatgpt'] exempt= False ctx= 'main/corpus/raw/review-round-02/chatgpt-01.md"\n[5]: https://github.com/open-asi-governance/open-asi-governance-forum/blob/main/corpus/deficiencies.md ' quote= 'https://github.com/open-asi-governance/open-asi-governance-forum/blob/main/corpu'
record/quarantine/review-round-03/claude-fable-5-01.md inline names= ['chatgpt'] exempt= True ctx= "ion block carry it: the correction that introduced the defect implemented ChatGPT's **round-01** finding (`corpus/raw/review-round-01/chatgpt-01.md`, " quote= 'Remaining normative defect: a relational status is written as a unary property'

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Improving attribution detection around quotations**
**Enhancing suffix attribution detection and quote parsing**
**Assessing attribution recall improvement**
**Evaluating test recall limitations**
**Analyzing blockquote pattern redundancy**
exec
/bin/bash -lc "sed -n '260,390p' tools/tests/test_gate_negative_controls.py
sed -n '90,125p' tools/guards.py
sed -n '155,200p' tools/guards.py
sed -n '230,280p' tools/tests/test_guard_identity.py
rg -n \"quote|quotation|verbatim|attribut|parenthetical|speech\" record/designs record/findings record/discussions -g '*.md' | head -120" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:

#  The real tree, retained. On the operator's workbench this must be 0; anywhere else the
#  external pins are unavailable and 3 is the honest answer. Both are accepted HERE because this
#  suite runs in both places — the policy that only 0 may land lives in land.py, and the policy
#  that CI may accept 3 lives in the workflow, where a reader can see each one.
r = run(CTX)
check("the real record evaluates to a state this suite recognises (0 here, 3 in a clone)",
      r.returncode in (0, 3), f"rc={r.returncode}\n{(r.stdout + r.stderr)[-300:]}")

#  AND THE ADMISSION POLICY ITSELF. Introducing exit 3 created a way for the whole apparatus to
#  go quiet: if land.py ever treated "not a contradiction" as good enough, drift in the
#  operator's governing files would stop blocking anything, on the one machine that can see it.
#  This tests the predicate directly rather than the constant, so widening it in either
#  direction fails here.
land = load("land")
check("land.py admits exit 0 from the context-pin gate", land.admitted("context-pins", 0) is True)
for refused in (1, 2, 3):
    check(f"land.py REFUSES exit {refused} from the context-pin gate",
          land.admitted("context-pins", refused) is False,
          "exit 3 is incomplete coverage, and admitting it here would retire the pin")
check("...and the same rule applies to every other gate",
      all(land.admitted(name, 3) is False and land.admitted(name, 0) is True
          for name, _cmd in land.GATES))

print("\ncheck_quotations.py — it must refuse a quotation that is in no corpus file")

r = run("check_quotations.py")
check("BASELINE: every published quotation is in the corpus", r.returncode == 0,
      (r.stdout + r.stderr)[-200:])

#  BOTH FORMS, retained permanently. On 2026-08-11 the inline form was refused and the SAME
#  sentence in a blockquote PASSED -- and this record quotes parties in blockquotes far more
#  often than inline. The checker built for D-53 did not cover the form a fabrication would
#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
#  this one catches.
#
#  The filename carries no exempting marker. The first fixture was called
#  "_negative-control-FABRICATED-quote.md" and "fabricat" is an EXEMPT_MARKER, so the fixture
#  risked exempting itself.
scratch = REPO / "record" / "findings" / "zzqx-quotation-negative-control.md"
INVENTED = "Zzqx invented sentence appearing in no raw sample anywhere in this corpus at all."
for label, body in (
        ("inline", f'The claude party wrote: "{INVENTED}"\n'),
        ("blockquote", f"The claude party wrote:\n\n> {INVENTED}\n")):
    try:
        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
                           encoding="utf-8")
        r = run("check_quotations.py")
        check(f"a FABRICATED quotation is refused ({label})", r.returncode != 0,
              f"rc={r.returncode}; D-53 was a fabricated party quotation and this is that fault")
    finally:
        scratch.unlink(missing_ok=True)
r = run("check_quotations.py")
check("...and the tree is clean again after removing it", r.returncode == 0)

#  THE SHAPE THE FABRICATION ACTUALLY TAKES. Both arms above plant the quotation on ONE LINE,
#  and both patterns forbade a newline — so this gate could see only single-line quotations while
#  every design document here is wrapped markdown at ~98 columns. D-53 was a fabricated party
#  quotation IN A DESIGN DOCUMENT; a fabrication of any substance wraps; the gate could not have
#  caught the next one.
#
#  Measured before the repair: 235 candidates across 98 files, ONE attributed to a party, that
#  one exempted — zero quotations checked, every run, for months. The negative control passed the
#  whole time because the fault was injected in the form that works and never in the form that
#  occurs. Control 45: the single-line arms above are KEPT, because a repair must still catch
#  what the old gate caught.
WRAPPED = ("Zzqx invented sentence that runs on long enough to wrap the way\n"
           "prose actually wraps in this record, appearing in no raw sample.")
for label, body in (
        ("inline, wrapped", f'The claude party wrote: "{WRAPPED}"\n'),
        ("blockquote, wrapped", "The claude party wrote:\n\n> " + WRAPPED.replace("\n", "\n> ")
         + "\n")):
    try:
        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
                           encoding="utf-8")
        r = run("check_quotations.py")
        check(f"a FABRICATED quotation that WRAPS is refused ({label})", r.returncode != 0,
              f"rc={r.returncode}; a gate blind to wrapped quotations is blind to the defect it "
              f"was built for")
        check(f"...and it is QT-02 that fired ({label})",
              fired_in(r.stdout + r.stderr, "QT-02"), (r.stdout + r.stderr)[-200:])
    finally:
        scratch.unlink(missing_ok=True)

#  AND A QUOTATION MUST NOT SWALLOW A PARAGRAPH BREAK. Allowing newlines without bounding them
#  would let one unclosed quote mark consume the rest of a document and match anything.
try:
    scratch.write_text('# temporary\n\nThe claude party wrote: "an unclosed quotation mark that '
                       'must not swallow\n\nthe entire rest of this document as one quotation."\n',
                       encoding="utf-8")
    r = run("check_quotations.py")
    check("a quote spanning a BLANK LINE is not treated as one quotation", r.returncode == 0,
          (r.stdout + r.stderr)[-200:])
finally:
    scratch.unlink(missing_ok=True)

#  QT-01 — the corpus itself unreadable. Its message says "a check that cannot fail is not a
#  check", which is control 2 stated inline and never once exercised.
quotations = load("check_quotations")
real_corpus = quotations.load_corpus
try:
    quotations.load_corpus = lambda: ""
    import contextlib
    import io
    out = io.StringIO()
    saved_argv = sys.argv
    try:
        sys.argv = ["check_quotations.py"]
        with contextlib.redirect_stdout(out), contextlib.redirect_stderr(out):
            code = quotations.main()
    finally:
        sys.argv = saved_argv
    check("an EMPTY corpus is refused rather than passing every quotation", code == 2,
          f"rc={code}; with no corpus every quotation is unverifiable, not verified")
    check("...and it is QT-01 that fired", fired_in(out.getvalue(), "QT-01"),
          out.getvalue()[-200:])
finally:
    quotations.load_corpus = real_corpus

print("\nrecord_spend.py / reconcile_actions.py — they must refuse rather than emit a number")

r = run("record_spend.py", "--cohort", "no-such-cohort-zzqx")
emitted_cost = "$" in r.stdout
check("record_spend refuses an unknown cohort rather than printing a cost",
      r.returncode != 0 or not emitted_cost,
      f"rc={r.returncode}; stdout={r.stdout[:160]!r}")

r = run("record_spend.py", "--report")
check("BASELINE: the spend report still runs", r.returncode == 0, (r.stderr or r.stdout)[-160:])

r = run("reconcile_actions.py", "--since", "zzqx-not-a-revision")
    MENTIONED another guard's code — Codex's counterexample was a refusal tagged `[CA-08]` whose
    text discussed `[CA-07]`, which returned True for the wrong guard. The tag is the last thing
    on the line by construction, so the match anchors there.
    """
    return any(p.rstrip().endswith(f"[{code}]") for p in problems)


class GuardNotActivated(AssertionError):
    """The named guard did not fire. Raised, so the result cannot be discarded."""


def expect_guard(problems: list[str], code: str) -> None:
    """THE canonical way a fixture asks for a guard, and the only form the registry counts.

    It raises rather than returning a boolean, because `activates(problems(), "ZZ-02")` on a line
    by itself registered as an expectation while asserting nothing — Codex reproduced it. A
    predicate whose result can be discarded is not a test, and a registry that counts the call
    rather than the assertion inherits that.

    STATIC ANALYSIS STILL CANNOT PROVE THE LINE RAN. A call inside a branch nobody takes counts
    here. Runtime receipts or coverage would be needed for that, and neither exists.
    """
    if not activates(problems, code):
        raise GuardNotActivated(
            f"guard {code} did not fire. Problems seen: {problems if problems else '(none)'}")


def declared() -> dict[str, list[str]]:
    """Every `guard("CODE", ...)` call site, found by AST rather than by regex over source."""
    found: dict[str, list[str]] = {}
    for path in sorted(TOOLS.glob("*.py")):
        try:
            tree = ast.parse(path.read_text(encoding="utf-8"))
        except (SyntaxError, UnicodeDecodeError):
            continue
        for node in ast.walk(tree):
    if not TESTS.is_dir():
        return found
    for path in sorted(TESTS.rglob("*.py")):
        try:
            tree = ast.parse(path.read_text(encoding="utf-8", errors="replace"))
        except SyntaxError:
            continue
        #  EXEMPT CODES, NOT AN EXEMPT FILE. This was `SYNTHETIC_GUARD_CODES = True`, which
        #  exempted the whole module — so a real orphaned expectation added to that file later
        #  would have vanished with the fixture data. Codex narrowed it: name the codes.
        synthetic: set[str] = set()
        for node in tree.body:
            if (isinstance(node, ast.Assign) and len(node.targets) == 1
                    and isinstance(node.targets[0], ast.Name)
                    and node.targets[0].id == "SYNTHETIC_GUARD_CODES"
                    and isinstance(node.value, (ast.Tuple, ast.List))):
                synthetic = {e.value for e in node.value.elts
                             if isinstance(e, ast.Constant) and isinstance(e.value, str)}
        #  A LOCAL WRAPPER IS ACCEPTED ONLY IF IT DEMONSTRABLY DELEGATES. Suites define a small
        #  `fired(code)` helper so the label and the outcome sit on one line, and counting the
        #  name blindly would let any function called `fired` register an expectation. So the
        #  wrapper's own body is parsed and must contain a call to `expect_guard`; otherwise only
        #  direct `expect_guard` calls count. Neither form proves the line executed — a call in a
        #  branch nobody takes still registers, and that needs runtime receipts this has not got.
        wrappers = {
            node.name for node in ast.walk(tree)
            if isinstance(node, ast.FunctionDef)
            and any(isinstance(inner, ast.Call)
                    and (getattr(inner.func, "id", "") == "expect_guard"
                         or getattr(inner.func, "attr", "") == "expect_guard")
                    for inner in ast.walk(node))}
        accepted = {"expect_guard"} | wrappers
        for node in ast.walk(tree):
            if not (isinstance(node, ast.Call) and node.args):
                continue
            name = (node.func.id if isinstance(node.func, ast.Name)
                    else node.func.attr if isinstance(node.func, ast.Attribute) else "")
            if name not in accepted:
                continue
            for arg in node.args:
                if (isinstance(arg, ast.Constant) and isinstance(arg.value, str)
                        and CODE.match(arg.value) and arg.value not in synthetic):
                    found.setdefault(arg.value, []).append(path.name)
    return found


            'def problems():\n    return [guard("ZZ-03", "a guard")]\n', encoding="utf-8")
        (tests / "test_thing.py").write_text('activates(problems(), "ZZ-03")\n',
                                             encoding="utf-8")
        g.BASELINE.write_text(json.dumps({"guards": {}, "authorised_removals": {}}),
                              encoding="utf-8")
        found = g.problems()
        check("a DISCARDED activates() call does not count as an expectation",
              any("ZZ-03" in f and "NO fixture names it" in f for f in found), str(found))

        #  A WRAPPER THAT DOES NOT DELEGATE must not launder a name into an expectation.
        (tests / "test_thing.py").write_text(
            "def fired(code):\n    return True\n"
            'assert fired("ZZ-03")\n', encoding="utf-8")
        found = g.problems()
        check("a wrapper that does NOT call expect_guard does not count either",
              any("ZZ-03" in f and "NO fixture names it" in f for f in found), str(found))

        #  ...and one that does delegate is accepted, or every suite would have to inline it.
        (tests / "test_thing.py").write_text(
            "def fired(code):\n    expect_guard(problems(), code)\n    return True\n"
            'assert fired("ZZ-03")\n', encoding="utf-8")
        check("...while a wrapper that DOES delegate is accepted", g.problems() == [],
              str(g.problems()))

        #  THE OPT-OUT MUST BE NARROW. A suite that does NOT declare its codes synthetic still
        #  has them counted, or the declaration would be a way to silence the registry.
        (tools / "thing.py").write_text(
            'def problems():\n    return [guard("ZZ-03", "a real guard")]\n', encoding="utf-8")
        (tests / "test_no_declaration.py").write_text(
            'expect_guard(problems(), "ZZ-09")\n', encoding="utf-8")
        (tests / "test_thing.py").write_text('expect_guard(problems(), "ZZ-03")\n',
                                             encoding="utf-8")
        found = g.problems()
        check("a suite WITHOUT the synthetic declaration still has its codes counted",
              any("ZZ-09" in f for f in found), str(found))
        (tests / "test_no_declaration.py").write_text(
            'SYNTHETIC_GUARD_CODES = ("ZZ-09",)\n'
            'expect_guard(problems(), "ZZ-09")\n', encoding="utf-8")
        check("...and declaring them synthetic exempts exactly that file", g.problems() == [],
              str(g.problems()))
    finally:
        g.TOOLS, g.TESTS, g.BASELINE = real_tools, real_tests, real_baseline

print("\nthe real repository")

found = g.problems()
check("every named guard here is declared once and driven by a fixture", found == [],
      "\n      ".join(found[:4]))

print(f"\n{passed} passed, {FAILED} failed")
raise SystemExit(1 if FAILED else 0)
record/findings/2026-08-06-capture-integration-run.md:8:> only defect here that can put a **misattribution into the real corpus** — the failure class this
record/findings/2026-08-06-capture-integration-run.md:13:> The wrong text now stands in `corpus/raw/` attributed to a real party, and every signal the
record/findings/2026-08-06-capture-integration-run.md:14:> custodian has says it was fixed. That is **D-10 — a segment attributed to a party that did not
record/findings/2026-08-06-capture-integration-run.md:49:fabricated reply committed as a contribution is **D-10** — a block attributed to a party that did not
record/findings/2026-08-06-capture-integration-run.md:50:write it — which is the most serious attribution defect in this register.
record/findings/2026-08-06-capture-integration-run.md:244:Section 7 prints, verbatim:
record/findings/2026-08-06-capture-integration-run.md:343:- **Self-contained.** Two inline script blocks, zero `src=` attributes; `check_self_contained()`
record/findings/2026-08-06-capture-integration-run.md:403:log is complete and attributed across 15 events. **This is a missing exit on a sound design.**
record/findings/2026-08-06-capture-integration-run.md:419:for a reason already in this register: **a review response quotes the document it reviews**, and
record/findings/2026-08-06-capture-integration-run.md:421:review task quotes the reviewed text, so the document's own vocabulary contaminates naive matching.
record/findings/2026-08-10-ncp-applied-to-consullo.md:6:quoted source, named files, classes and configuration keys, and described four *unfixed* defects
record/findings/2026-08-10-qualification-01-halt.md:21:Thirty-four of thirty-six classifications correct, every quotation an exact substring of the text
record/findings/2026-08-10-qualification-01-halt.md:38:someone else's decision"** — and C05's duty governs *speech*, not action. Under B the executive
record/findings/2026-08-10-qualification-01-halt.md:43:**So the defect is in the instrument, at the join between a speech-duty clause and an
record/findings/2026-08-10-qualification-01-halt.md:67:context pack, and the quotation requirement is satisfiable — a fabricated quote would have failed
record/findings/2026-08-10-qualification-01-halt.md:85:   so a speech duty is evaluable. Preserves the clause; makes the instrument no longer a pure
record/findings/2026-08-10-ratification-01-incomplete.md:87:Eighteen objections were captured, one per usable sample, preserved verbatim and grouped only by
record/designs/candidate-control-register.md:217:| Group C (3) — must not seek expanded authority; silence is not delegation | **Control 4**, partly | "Do not seek" is intent attribution and unverifiable. "No consequential action without explicit authority" is the mechanical residue. The rest stays a human escalation principle. |
record/designs/round-017-preregistration.md:86:budget while halving the cap would confound the arm — any difference could be attributed to
record/designs/T13-capture-ui-design.md:106:emits `docs/capture/index.html` with the **committed prompt text embedded verbatim and its SHA-256
record/designs/T13-capture-ui-design.md:148:- **`identity` is selected, never typed.** D-09 — "the record's most serious attribution defect" —
record/designs/T13-capture-ui-design.md:246:D-25's specific warning applies directly: *"review tasks quote the reviewed document, so the
record/designs/T13-capture-ui-design.md:247:document's own vocabulary contaminates any naive pattern."* A review legitimately quotes its
record/designs/T13-capture-ui-design.md:322:Review committed verbatim at [`reviews/codex-T13-design-review.md`](reviews/codex-T13-design-review.md).
record/designs/T13-capture-ui-design.md:363:evidence exists. D-14 is the register's entry for a role attributed to a model beyond what it can
record/designs/T13-capture-ui-design.md:364:hold; this design had quietly attributed one to itself.
record/designs/T13-capture-ui-design.md:425:if any, any preamble verbatim. `prior_context` is composed from the **receipt**, not the manifest.
record/designs/T13-capture-ui-design.md:668:in this repository, and they are committed verbatim for the same reason.
record/designs/T13-g2-rule-validation.md:29:the outbound text in a `> ` blockquote surrounded by metadata and capture requirements that were
record/designs/T13-g2-rule-validation.md:123:the sent text as a blockquote at 15–102, and capture requirements at 104–118.
record/designs/external-ingredient-routes.md:62:it costs one sentence**, though it should not be sent until he has answered the attribution
record/designs/automating-the-error-prone-actions.md:55:the suite, `check_quotations.py`, `check_executive_context.py`, then commit, push, verify the
record/designs/automating-the-error-prone-actions.md:56:remote ref, attest both. On 2026-08-10 the quotation checker caught a bad quote **only because
record/designs/executive-context-ratification.md:42:**CORRECTION — the sentence quoted next is false and no party said it.** It was written by this
record/designs/executive-context-ratification.md:113: "quoted_dispositive_words": "verbatim text"}
record/designs/executive-context-ratification.md:129:ratify. RATIFY / REFUSE / AMEND\<verbatim text\>, plus the objection route: a constraint the
record/designs/executive-context-ratification.md:132:Every response is preserved verbatim, given a mechanical exact-text id, and **never merged with
record/designs/executive-context-ratification.md:168:text left the layer *"able to act in more situations"*, and C05's duty governs **speech**. Under
record/designs/ai-native-development-practice-draft.md:78:4. **Findings that did not reproduce are published beside the ones that did**, attributed either way.
record/designs/qwen-tool-using-arm-scope.md:67:> **CORRECTION, 2026-08-07 — this paragraph carried a fabricated quotation.** It read:
record/designs/qwen-tool-using-arm-scope.md:76:> Found by Codex during an adversarial review, after the quotation had been copied into a
record/designs/instructed-exploration-arm.md:93:difference could be attributed to either. **Change one.**
record/designs/sop-continuous-deliberation-draft.md:13:captures every reply verbatim with provenance, and files the result in the corpus. The annotator
record/designs/sop-continuous-deliberation-draft.md:58:has ever been collectively ratified, and D-14 records that role attribution without recorded
record/designs/sop-continuous-deliberation-draft.md:63:verbatim, publish the objections beside the SOP, and let the custodian decide with those on the
record/designs/sop-continuous-deliberation-draft.md:159:proposal queued verbatim, nothing merged, rounds drawn in strict rotation by proposing party.
record/designs/sop-continuous-deliberation-draft.md:176:2. **A proposal contract**, verbatim: the question, the reason, the evidence needed, the decision
record/designs/sop-continuous-deliberation-draft.md:199:parties nominate them verbatim; selection is mechanical — random from those nominations and from
record/designs/sop-continuous-deliberation-draft.md:381:verbatim beside the round, and where it contradicts the moderator, both positions are published and
record/designs/sop-continuous-deliberation-draft.md:388:1. **Every reply captured verbatim before any validation runs.** Existing rule; `ingest_capture.py`
record/designs/sop-continuous-deliberation-draft.md:407:It is also the party this corpus attributed roles to for a year without a single recorded output
record/designs/sop-continuous-deliberation-draft.md:416:Not approval. Each party is shown this document and asked, in one round, recorded verbatim:
record/designs/reviews/codex-T13-design-review.md:14:2. **No. G2 is not suitable as a refusal gate.** A response containing the whole prompt followed by a short answer has `coverage = 1` and `saturation ≈ 1`, so it falsely trips. A long answer after the quotation lowers saturation and may pass. The outcome therefore depends on answer length, not whether the content is a response. The normalization is also underspecified: tokenization, Unicode case-folding, punctuation deletion versus replacement, sets versus multisets, and zero-shingle inputs are undefined. [Design §6.1](</home/reed/git/open-asi-governance-forum/record/designs/T13-capture-ui-design.md:227>).
record/designs/reviews/codex-T13-design-review.md:16:3. **No. Five positives do not validate a threshold.** The four synthetic prompt-as-response cases all occupy the same feature point, `(1,1)`. They provide no threshold information. The only genuine positive is insufficient, and using the nine negatives both to choose and “validate” the threshold is training on the test set. The honest fallback is exact byte/normalized equality as a refusal and shingle similarity as warn-only pending independently labeled, held-out near-copy and quote-heavy cases. [D-23](</home/reed/git/open-asi-governance-forum/corpus/deficiencies.md:450>), [D-25](</home/reed/git/open-asi-governance-forum/corpus/deficiencies.md:510>).
record/designs/reviews/codex-T13-design-review.md:68:- Pre-register the threshold before evaluating an independently labeled, held-out corpus containing genuine near-pastes, prompt restatements, full quotations with substantive answers, and adversarial mutations.
record/designs/correspondent-disclosure.md:20:> you reply, tell me whether you would prefer to be named, quoted anonymously, or not recorded at
record/designs/correspondent-disclosure.md:23:The offer must be **all three options**. "May I quote you?" is not the offer; it presents recording
record/designs/correspondent-disclosure.md:24:as settled and only attribution as open.
record/designs/correspondent-disclosure.md:47:inside the verbatim quotation. Each pass declared itself clean and the next pass found more.
record/designs/correspondent-disclosure.md:50:name alone, address, institution, and any file that ever quoted a file that quoted them.

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Reviewing regex guards and adapter design**
**Validating guard output handling and error formatting**
exec
/bin/bash -lc "rg -n '"'^'"\\| 61 \\|| 61:' record/controls/control-application.md tools/control_application.py corpus -g '*.md' -g '*.py' | head -30
python3 tools/guards.py --check
python3 tools/check_quotations.py --list" in /home/reed/git/open-asi-governance-forum
 succeeded in 2120ms:
  13 named guard(s), each declared once and each named by at least one fixture.
  ok   record/executive/context/oagf-CLAUDE.md  [claude]  …except that material the custodian is legally or ethically required t…
1 attributed quotation(s) checked against corpus/ — all found.
Scope: quotations of >= 40 characters within 240 characters after a party's name. A fabrication attributed to nobody, or paraphrased without quotation marks, is NOT caught by this.

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Assessing test suite blockers and file counts**
**Identifying limitations in nested quotation parsing**
**Analyzing blockquote parsing and multi-paragraph quotes**
**Comparing paragraph length limits**
exec
/bin/bash -lc "python3 - <<'PY'
import sys,re,statistics
sys.path.insert(0,'tools'); import check_quotations as q
inner=r'(?:["'^%s'"\\n]|\\n(?"'!'"\\s*\\n))'
for delim in ['\"','”']:
 bounded=re.compile(('\"' if delim=='\"' else '“')+r'('+inner.replace('%s',delim)+r'{40,4000})'+delim)
 unlimited=re.compile(('\"' if delim=='\"' else '“')+r'('+inner.replace('%s',delim)+r'{40,})'+delim)
 bs=[]; us=[]
 for p in q.files_to_check():
  t=q.strip_fenced(p.read_text(encoding='utf-8'))
  bs += [(p,len(m.group(1))) for m in bounded.finditer(t)]
  us += [(p,len(m.group(1))) for m in unlimited.finditer(t)]
 print(delim,'bounded',len(bs),'unlimited',len(us),'max',max((n for _,n in us),default=0),'over4000',[(str(p.relative_to(q.REPO_ROOT)),n) for p,n in us if n>4000])
# direct boundary/shape probe
pats=q.QUOTE_PATTERNS
cases={
 'two_nonblank_wraps':'\"'+('a'*45)+'\\n'+('b'*45)+'\\n'+('c'*45)+'\"',
 'blank_line':'\"'+('a'*45)+'\\n\\n'+('b'*45)+'\"',
 '4000':'\"'+('a'*4000)+'\"',
 '4001':'\"'+('a'*4001)+'\"',
 'same_delimiter_nested':'\"'+('a'*50)+' \"nested\" '+('b'*50)+'\"',
}
for name,s in cases.items():
 ms=[m.group(1) for p in pats for m in p.finditer(s)]
 print(name,[(len(x),repr(x[:18]),repr(x[-18:])) for x in ms])
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
" bounded 211 unlimited 215 max 5350 over4000 [('record/solicitations/round-006-chat/round-006-chat-chatgpt.md', 5350), ('record/solicitations/round-006-chat/round-006-chat-claudeai.md', 5350), ('record/solicitations/round-006-chat/round-006-chat-geminiapp.md', 5350), ('record/solicitations/round-006-chat/round-006-chat-grokapp.md', 5350)]
” bounded 13 unlimited 13 max 101 over4000 []
two_nonblank_wraps [(137, "'aaaaaaaaaaaaaaaaaa'", "'cccccccccccccccccc'")]
blank_line []
4000 [(4000, "'aaaaaaaaaaaaaaaaaa'", "'aaaaaaaaaaaaaaaaaa'")]
4001 []
same_delimiter_nested [(51, "'aaaaaaaaaaaaaaaaaa'", "'aaaaaaaaaaaaaaaaa '"), (51, "' bbbbbbbbbbbbbbbbb'", "'bbbbbbbbbbbbbbbbbb'")]

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Inspecting invisible content context**
exec
/bin/bash -lc "python3 - <<'PY'
import sys,re
sys.path.insert(0,'tools'); import check_quotations as q
inner=r'(?:["'^"'"\\n]|\\n(?"'!'"\\s*\\n))'
u=re.compile(r'\"('+inner+r'{4001,})\"')
for p in q.files_to_check():
 t=q.strip_fenced(p.read_text(encoding='utf-8'))
 for m in u.finditer(t):
  print(p.relative_to(q.REPO_ROOT),len(m.group(1)),repr(t[max(0,m.start()-260):m.start()]),'party',q.attributed_party(t[:m.start()]))
PY
nl -ba tools/check_quotations.py | sed -n '100,225p'
nl -ba tools/tests/test_gate_negative_controls.py | sed -n '300,390p'
nl -ba tools/tests/test_gate_negative_controls.py | sed -n '375,405p'
nl -ba tools/scan_own_code.py | sed -n '50,85p'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
record/solicitations/round-006-chat/round-006-chat-chatgpt.md 5350 'orrected by an attached artifact and the original decision is left intact, because the fact that it rested on a non-existent control is the part worth keeping. The control itself **cannot honestly be built yet** — every mechanical way to pick a party\'s "active' party None
record/solicitations/round-006-chat/round-006-chat-claudeai.md 5350 'orrected by an attached artifact and the original decision is left intact, because the fact that it rested on a non-existent control is the part worth keeping. The control itself **cannot honestly be built yet** — every mechanical way to pick a party\'s "active' party None
record/solicitations/round-006-chat/round-006-chat-geminiapp.md 5350 'orrected by an attached artifact and the original decision is left intact, because the fact that it rested on a non-existent control is the part worth keeping. The control itself **cannot honestly be built yet** — every mechanical way to pick a party\'s "active' party None
record/solicitations/round-006-chat/round-006-chat-grokapp.md 5350 'orrected by an attached artifact and the original decision is left intact, because the fact that it rested on a non-existent control is the part worth keeping. The control itself **cannot honestly be built yet** — every mechanical way to pick a party\'s "active' party None
   100	SEARCH_FILES = ["README.md", "HANDOFF.md", "CONTRIBUTING.md", "GOVERNANCE.md", "FOR-PARTIES.md"]
   101	
   102	#  A quotation inside one of these is a statement ABOUT a fabricated or corrected quotation, not
   103	#  a fresh claim that a party said it. D-53's own corrections quote the false text on purpose, so
   104	#  that a reader who saw the original can recognise it.
   105	EXEMPT_MARKERS = [
   106	    "CORRECTION", "fabricat", "no party said", "never said", "D-53",
   107	    "did not say", "misattribut",
   108	]
   109	
   110	#  A QUOTATION MAY WRAP. Both patterns forbade a newline — `[^"\n]` — so any quotation spanning
   111	#  more than one line was INVISIBLE to this gate. In wrapped markdown at ~98 columns, a quotation
   112	#  of forty characters or more usually wraps, and every design document here is wrapped markdown.
   113	#
   114	#  That is not a corner case, it is the case: D-53 was a fabricated party quotation IN A DESIGN
   115	#  DOCUMENT, and this gate was built to catch the next one. It could not have. Measured on
   116	#  2026-08-12 over the 98 files it scans: 235 quotation candidates, ONE attributed to a party,
   117	#  and that one exempted by a CORRECTION marker — so **zero quotations checked, every run**, by a
   118	#  gate that runs on every landing.
   119	#
   120	#  THE NEGATIVE CONTROL PASSED THROUGHOUT, and that is the part worth keeping. The planted
   121	#  fixture was written as a single-line quotation, so it exercised the only shape the gate could
   122	#  see. The fault was injected in the form that works and never in the form that occurs.
   123	#
   124	#  A single newline is allowed; a BLANK line is not, because a quotation does not span a
   125	#  paragraph break and an unclosed quote would otherwise swallow the rest of the document. The
   126	#  upper bound is for the same reason. `normalise()` already folds whitespace, so a wrapped
   127	#  quotation compares against the corpus exactly as a single-line one does.
   128	_INNER = r'(?:[^%s\n]|\n(?!\s*\n))'
   129	QUOTE_PATTERNS = [
   130	    re.compile(r'"(%s{%d,4000})"' % (_INNER % '"', MIN_LENGTH)),
   131	    re.compile(r'“(%s{%d,4000})”' % (_INNER % '”', MIN_LENGTH)),
   132	]
   133	
   134	
   135	
   136	#  MARKDOWN BLOCKQUOTES. Added 2026-08-11 after a negative control found the gap: a fabricated
   137	#  party quotation in inline quotes was REFUSED and the same sentence in a blockquote PASSED.
   138	#  This record quotes parties in blockquotes far more often than inline, so the checker built for
   139	#  D-53 -- a fabricated party quotation -- did not cover the form a fabrication would take here.
   140	#  The docstring disclosed three gaps and not this one.
   141	BLOCKQUOTE = re.compile(r'(?:^[ \t]*>[ \t]?.*(?:\n|$))+', re.M)
   142	
   143	
   144	def blockquotes(text: str):
   145	    """Yield (start_offset, quotation) for each blockquote block, markers stripped.
   146	
   147	    Consecutive `> ` lines are joined: a quotation wrapped across lines is one quotation, and
   148	    matching per line would both miss it and mangle the text it compares against the corpus.
   149	    """
   150	    for m in BLOCKQUOTE.finditer(text):
   151	        body = "\n".join(re.sub(r'^[ \t]*>[ \t]?', "", ln)
   152	                          for ln in m.group(0).splitlines()).strip()
   153	        if len(body) >= MIN_LENGTH:
   154	            yield m.start(), body
   155	
   156	
   157	def normalise(text: str) -> str:
   158	    """Fold the differences that survive a copy-paste but do not change the words."""
   159	    text = unicodedata.normalize("NFKC", text)
   160	    text = (text.replace("’", "'").replace("‘", "'")
   161	                .replace("“", '"').replace("”", '"')
   162	                .replace("—", "-").replace("–", "-"))
   163	    return " ".join(text.split()).lower()
   164	
   165	
   166	def load_corpus() -> str:
   167	    """One normalised blob of every raw byte. Crude, and crude is right here.
   168	
   169	    Raw material is JSON, markdown and plain text; a party's words may sit inside a JSON string
   170	    with escapes. Searching the decoded text of everything avoids caring which.
   171	    """
   172	    parts = []
   173	    for path in sorted(CORPUS.rglob("*")):
   174	        if not path.is_file():
   175	            continue
   176	        try:
   177	            text = path.read_text(encoding="utf-8", errors="replace")
   178	        except Exception:                                                 # noqa: BLE001
   179	            continue
   180	        parts.append(text)
   181	        if path.suffix == ".json":
   182	            #  Also append the decoded form, so `\n` inside a JSON string matches prose that
   183	            #  quotes it with a real newline.
   184	            try:
   185	                parts.append(json.dumps(json.loads(text), ensure_ascii=False))
   186	                parts.append(re.sub(r"\\n", " ", text))
   187	            except Exception:                                             # noqa: BLE001
   188	                pass
   189	    return normalise("\n".join(parts))
   190	
   191	
   192	def attributed_party(before: str) -> str | None:
   193	    """The party CREDITED WITH SAYING a quotation, from the text preceding it.
   194	
   195	    Requires both a party name and, after it, a verb crediting that party with the words. The
   196	    ordering matters: "Codex said Qwen was wrong" should not make Qwen the speaker, and a party
   197	    named after the verb is usually the subject of the sentence rather than its author.
   198	    """
   199	    window = before[-ATTRIBUTION_WINDOW:].lower()
   200	    best, best_at = None, -1
   201	    for name in PARTY_NAMES:
   202	        for match in re.finditer(r"\b" + re.escape(name) + r"\b", window):
   203	            if match.start() > best_at:
   204	                best, best_at = name, match.start()
   205	    if best is None:
   206	        return None
   207	    after_party = window[best_at:]
   208	    #  Word boundaries, and never inside an identifier: the JSON key `claim_as_stated` matched
   209	    #  "stated" and made every field value in a reproduced file look like a party's utterance.
   210	    for verb in SPEECH_VERBS:
   211	        for hit in re.finditer(r"(?<![\w_])" + re.escape(verb) + r"(?![\w_])", after_party):
   212	            return best
   213	    return None
   214	
   215	
   216	def fragments(quotation: str) -> list[str]:
   217	    """Split an elided quotation; every surviving fragment must still be real."""
   218	    parts = re.split(r"\s*(?:…|\.\.\.)\s*", quotation)
   219	    return [p for p in (part.strip(" .,;:") for part in parts) if len(p) >= MIN_LENGTH]
   220	
   221	
   222	FENCE = re.compile(r"(?ms)^```.*?^```")
   223	
   224	
   225	def strip_fenced(text: str) -> str:
   300	INVENTED = "Zzqx invented sentence appearing in no raw sample anywhere in this corpus at all."
   301	for label, body in (
   302	        ("inline", f'The claude party wrote: "{INVENTED}"\n'),
   303	        ("blockquote", f"The claude party wrote:\n\n> {INVENTED}\n")):
   304	    try:
   305	        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
   306	                           encoding="utf-8")
   307	        r = run("check_quotations.py")
   308	        check(f"a FABRICATED quotation is refused ({label})", r.returncode != 0,
   309	              f"rc={r.returncode}; D-53 was a fabricated party quotation and this is that fault")
   310	    finally:
   311	        scratch.unlink(missing_ok=True)
   312	r = run("check_quotations.py")
   313	check("...and the tree is clean again after removing it", r.returncode == 0)
   314	
   315	#  THE SHAPE THE FABRICATION ACTUALLY TAKES. Both arms above plant the quotation on ONE LINE,
   316	#  and both patterns forbade a newline — so this gate could see only single-line quotations while
   317	#  every design document here is wrapped markdown at ~98 columns. D-53 was a fabricated party
   318	#  quotation IN A DESIGN DOCUMENT; a fabrication of any substance wraps; the gate could not have
   319	#  caught the next one.
   320	#
   321	#  Measured before the repair: 235 candidates across 98 files, ONE attributed to a party, that
   322	#  one exempted — zero quotations checked, every run, for months. The negative control passed the
   323	#  whole time because the fault was injected in the form that works and never in the form that
   324	#  occurs. Control 45: the single-line arms above are KEPT, because a repair must still catch
   325	#  what the old gate caught.
   326	WRAPPED = ("Zzqx invented sentence that runs on long enough to wrap the way\n"
   327	           "prose actually wraps in this record, appearing in no raw sample.")
   328	for label, body in (
   329	        ("inline, wrapped", f'The claude party wrote: "{WRAPPED}"\n'),
   330	        ("blockquote, wrapped", "The claude party wrote:\n\n> " + WRAPPED.replace("\n", "\n> ")
   331	         + "\n")):
   332	    try:
   333	        scratch.write_text(f"# temporary, deleted by the test that wrote it\n\n{body}",
   334	                           encoding="utf-8")
   335	        r = run("check_quotations.py")
   336	        check(f"a FABRICATED quotation that WRAPS is refused ({label})", r.returncode != 0,
   337	              f"rc={r.returncode}; a gate blind to wrapped quotations is blind to the defect it "
   338	              f"was built for")
   339	        check(f"...and it is QT-02 that fired ({label})",
   340	              fired_in(r.stdout + r.stderr, "QT-02"), (r.stdout + r.stderr)[-200:])
   341	    finally:
   342	        scratch.unlink(missing_ok=True)
   343	
   344	#  AND A QUOTATION MUST NOT SWALLOW A PARAGRAPH BREAK. Allowing newlines without bounding them
   345	#  would let one unclosed quote mark consume the rest of a document and match anything.
   346	try:
   347	    scratch.write_text('# temporary\n\nThe claude party wrote: "an unclosed quotation mark that '
   348	                       'must not swallow\n\nthe entire rest of this document as one quotation."\n',
   349	                       encoding="utf-8")
   350	    r = run("check_quotations.py")
   351	    check("a quote spanning a BLANK LINE is not treated as one quotation", r.returncode == 0,
   352	          (r.stdout + r.stderr)[-200:])
   353	finally:
   354	    scratch.unlink(missing_ok=True)
   355	
   356	#  QT-01 — the corpus itself unreadable. Its message says "a check that cannot fail is not a
   357	#  check", which is control 2 stated inline and never once exercised.
   358	quotations = load("check_quotations")
   359	real_corpus = quotations.load_corpus
   360	try:
   361	    quotations.load_corpus = lambda: ""
   362	    import contextlib
   363	    import io
   364	    out = io.StringIO()
   365	    saved_argv = sys.argv
   366	    try:
   367	        sys.argv = ["check_quotations.py"]
   368	        with contextlib.redirect_stdout(out), contextlib.redirect_stderr(out):
   369	            code = quotations.main()
   370	    finally:
   371	        sys.argv = saved_argv
   372	    check("an EMPTY corpus is refused rather than passing every quotation", code == 2,
   373	          f"rc={code}; with no corpus every quotation is unverifiable, not verified")
   374	    check("...and it is QT-01 that fired", fired_in(out.getvalue(), "QT-01"),
   375	          out.getvalue()[-200:])
   376	finally:
   377	    quotations.load_corpus = real_corpus
   378	
   379	print("\nrecord_spend.py / reconcile_actions.py — they must refuse rather than emit a number")
   380	
   381	r = run("record_spend.py", "--cohort", "no-such-cohort-zzqx")
   382	emitted_cost = "$" in r.stdout
   383	check("record_spend refuses an unknown cohort rather than printing a cost",
   384	      r.returncode != 0 or not emitted_cost,
   385	      f"rc={r.returncode}; stdout={r.stdout[:160]!r}")
   386	
   387	r = run("record_spend.py", "--report")
   388	check("BASELINE: the spend report still runs", r.returncode == 0, (r.stderr or r.stdout)[-160:])
   389	
   390	r = run("reconcile_actions.py", "--since", "zzqx-not-a-revision")
   375	          out.getvalue()[-200:])
   376	finally:
   377	    quotations.load_corpus = real_corpus
   378	
   379	print("\nrecord_spend.py / reconcile_actions.py — they must refuse rather than emit a number")
   380	
   381	r = run("record_spend.py", "--cohort", "no-such-cohort-zzqx")
   382	emitted_cost = "$" in r.stdout
   383	check("record_spend refuses an unknown cohort rather than printing a cost",
   384	      r.returncode != 0 or not emitted_cost,
   385	      f"rc={r.returncode}; stdout={r.stdout[:160]!r}")
   386	
   387	r = run("record_spend.py", "--report")
   388	check("BASELINE: the spend report still runs", r.returncode == 0, (r.stderr or r.stdout)[-160:])
   389	
   390	r = run("reconcile_actions.py", "--since", "zzqx-not-a-revision")
   391	check("reconcile_actions refuses an unresolvable revision rather than reporting zero",
   392	      r.returncode != 0 or "0 unexplained" not in r.stdout,
   393	      f"rc={r.returncode}; stdout={r.stdout[:160]!r}")
   394	
   395	r = run("reconcile_actions.py")
   396	check("BASELINE: reconciliation runs against the current lease", r.returncode == 0,
   397	      (r.stderr or r.stdout)[-160:])
   398	
   399	print(f"\n{passed} passed, {FAILED} failed")
   400	raise SystemExit(1 if FAILED else 0)
    50	    return [n.lineno for n in ast.walk(tree)
    51	            if isinstance(n, ast.If) and len(n.body) == 1 and isinstance(n.body[0], ast.Pass)]
    52	
    53	
    54	def constant_guard(tree: ast.AST) -> list[int]:
    55	    """D-B. A condition that is constant — a bool literal, or a name bound once to one."""
    56	    #  ONLY names assigned exactly ONCE, to a bool literal. The first version kept the last
    57	    #  literal assignment and ignored reassignment, so it flagged `in_table` -- an ordinary
    58	    #  mutable state flag set True and False in different branches. Two false positives, on a
    59	    #  detector whose fixtures both passed. Fixture agreement is not correctness on real code.
    60	    assigned: dict[str, list] = {}
    61	    for n in ast.walk(tree):
    62	        if isinstance(n, (ast.Assign, ast.AugAssign, ast.AnnAssign)):
    63	            targets = n.targets if isinstance(n, ast.Assign) else [n.target]
    64	            for t in targets:
    65	                if isinstance(t, ast.Name):
    66	                    assigned.setdefault(t.id, []).append(n)
    67	    consts = {name: nodes[0].value.value for name, nodes in assigned.items()
    68	              if len(nodes) == 1 and isinstance(nodes[0], ast.Assign)
    69	              and isinstance(nodes[0].value, ast.Constant)
    70	              and isinstance(nodes[0].value.value, bool)}
    71	    #  A LITERAL TEST TOO, not only a name bound to one. `if True:` was sitting in
    72	    #  check_quotations.py — a gate that runs on every landing — wrapping its entire quotation
    73	    #  loop, and this detector reported nothing, on the real file AND on a direct fixture. It
    74	    #  matched `x = True; if x:` and not the shape one step simpler. Found on 2026-08-12 by
    75	    #  reading a gate while enrolling it in the guard registry, which is not a mechanism.
    76	    out = [n.lineno for n in ast.walk(tree)
    77	           if isinstance(n, ast.If) and isinstance(n.test, ast.Name) and n.test.id in consts]
    78	    out += [n.lineno for n in ast.walk(tree)
    79	            if isinstance(n, ast.If) and isinstance(n.test, ast.Constant)
    80	            and isinstance(n.test.value, bool)]
    81	    return sorted(out)
    82	
    83	
    84	def success_from_except(tree: ast.AST) -> list[int]:
    85	    """D-E. An except handler returning a success-shaped value.

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Identifying ledger mutation in landing blocker test**
**Planning ledger cleanup and test fix**
exec
/bin/bash -lc "rg -n \"def main|cohort|LEDGER|write_text|json.dump|return\" tools/record_spend.py | head -100
sed -n '1,260p' tools/record_spend.py
git diff -- record/cycles/spend-ledger.json | sed -n '1,100p'
rg -n 'record_spend|test_gate_negative_controls' tools/tests/run_all.py tools/land.py" in /home/reed/git/open-asi-governance-forum
 exited 1 in 0ms:
2:"""Record what a solicitation cohort actually cost, from the usage in its raw material.
4:    python3 tools/record_spend.py --cohort ratification-02
5:    python3 tools/record_spend.py --backfill          # every unrecorded cohort
14:cohorts had run that morning consuming **189,807 tokens**. Only `round_cycle.py` ever wrote to
24:Sums `usage.prompt_tokens` and `usage.completion_tokens` from every raw sample in a cohort,
25:prices them against the recorded rate table, and appends one ledger entry per cohort. It counts
49:LEDGER = REPO_ROOT / "record" / "cycles" / "spend-ledger.json"
55:        return json.loads(RATES.read_text(encoding="utf-8"))
56:    return {}
59:def usage_for(cohort: str) -> dict:
60:    """Token usage for a cohort, counting failures and rejections as well as accepted samples."""
61:    directory = RAW / cohort
93:    return {"per_model": per_model, "units_seen": units, "units_reporting_usage": reported}
99:    #  54 cohorts at null -- a ledger that says "unknown" everywhere is only marginally better
118:    return (round(total, 4) if priced else None), unpriced
121:def recorded_cohorts() -> set:
122:    if not LEDGER.is_file():
123:        return set()
124:    doc = json.loads(LEDGER.read_text(encoding="utf-8"))
125:    return {e.get("round") for e in (doc.get("entries") or [])}
128:def solicited_cohorts() -> list:
129:    return sorted(d.name for d in RAW.iterdir()
133:def append(cohort: str, rates: dict) -> dict:
134:    u = usage_for(cohort)
140:        "round": cohort,
158:    doc = json.loads(LEDGER.read_text(encoding="utf-8")) if LEDGER.is_file() else {"entries": []}
160:    LEDGER.write_text(json.dumps(doc, indent=2, ensure_ascii=False) + "\n", encoding="utf-8")
161:    return entry
164:def main() -> int:
166:    parser.add_argument("--cohort")
172:    if args.report or not (args.cohort or args.backfill):
173:        have, all_c = recorded_cohorts(), solicited_cohorts()
175:        print(f"  cohorts with raw material: {len(all_c)}")
185:        return 1 if missing else 0
187:    targets = [args.cohort] if args.cohort else \
188:        [c for c in solicited_cohorts() if c not in recorded_cohorts()]
189:    for cohort in targets:
190:        e = append(cohort, rates)
191:        print(f"  {cohort:34s} {e['prompt_tokens']:>8,}p {e['completion_tokens']:>8,}c  "
193:    return 0
#!/usr/bin/env python3
"""Record what a solicitation cohort actually cost, from the usage in its raw material.

    python3 tools/record_spend.py --cohort ratification-02
    python3 tools/record_spend.py --backfill          # every unrecorded cohort
    python3 tools/record_spend.py --report            # what is recorded, and what is missing

**DETERMINISTIC.** No LLM, no network. Reads raw usage blocks and the rate table.

Why this exists
---------------
`record/cycles/spend-ledger.json` is the artifact that answers "what has this project spent". On
2026-08-10 its most recent entry was **round-019, from the previous day**, while three routed
cohorts had run that morning consuming **189,807 tokens**. Only `round_cycle.py` ever wrote to
it; every instrument built since — the qualification gate, both ratification ballots — spends the
custodian's money and records none of it.

That is the class this project keeps finding: **an artifact that claims to record something, and
does not record it, while looking complete.** A reader consulting the ledger would have concluded
the project spent nothing that day.

What it does
------------
Sums `usage.prompt_tokens` and `usage.completion_tokens` from every raw sample in a cohort,
prices them against the recorded rate table, and appends one ledger entry per cohort. It counts
**rejected and failed attempts too** — a sample that came back schema-invalid was still paid for,
and a spend figure that silently drops failures understates exactly the runs that went wrong.

What it cannot establish
-------------------------
* **That the price is right.** Rates come from a dated table, not from an invoice. A provider
  that changed pricing since is not detected, so `actual_usd` is an estimate and is labelled one.
* **That every cost is here.** Local-arm inference is free at the point of use and consumes
  electricity and a GPU nobody bills for. Codex and Claude subscription usage is prepaid monthly
  and appears nowhere in this ledger.
* **That an unreported usage block means zero.** Where an arm reports no usage the entry records
  `null` and says so, because zero and unknown are different and only one of them is honest.
"""

from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
RAW = REPO_ROOT / "corpus" / "raw"
LEDGER = REPO_ROOT / "record" / "cycles" / "spend-ledger.json"
RATES = REPO_ROOT / "record" / "cycles" / "model-rates.json"


def load_rates() -> dict:
    if RATES.is_file():
        return json.loads(RATES.read_text(encoding="utf-8"))
    return {}


def usage_for(cohort: str) -> dict:
    """Token usage for a cohort, counting failures and rejections as well as accepted samples."""
    directory = RAW / cohort
    per_model: dict[str, dict] = {}
    units = reported = 0
    for path in sorted(directory.glob("*.json")):
        try:
            doc = json.loads(path.read_text(encoding="utf-8"))
        except Exception:                                               # noqa: BLE001
            continue
        for container in ("samples", "responses", "failures", "rejected"):
            for unit in (doc.get(container) or []):
                if not isinstance(unit, dict):
                    continue
                units += 1
                usage = unit.get("usage")
                if not isinstance(usage, dict):
                    continue
                reported += 1
                model = ((unit.get("delivery_chain") or {}).get("served_model")
                         or (doc.get("spec") or {}).get("reached_via") or "unknown")
                bucket = per_model.setdefault(model, {"prompt": 0, "completion": 0, "n": 0})
                #  ABSENT IS NOT ZERO. `usage.get("prompt_tokens") or 0` silently treated a
                #  missing token count as a real zero, which UNDERSTATES spend -- the same shape
                #  as the "0 searches" defect, in the ledger the custodian funds this from.
                #  Counted separately so the figure carries its own incompleteness.
                prompt_tokens = usage.get("prompt_tokens")
                completion_tokens = usage.get("completion_tokens")
                if prompt_tokens is None or completion_tokens is None:
                    bucket["unknown_usage"] = bucket.get("unknown_usage", 0) + 1
                    continue
                bucket["prompt"] += prompt_tokens
                bucket["completion"] += completion_tokens
                bucket["n"] += 1
    return {"per_model": per_model, "units_seen": units, "units_reporting_usage": reported}


def price(per_model: dict, rates: dict) -> tuple[float | None, list]:
    #  The table is keyed by router model id under `usd_per_million_tokens`, with `input` and
    #  `output` rates. Reading `models` -> `prompt_usd_per_mtok` found nothing and priced 37 of
    #  54 cohorts at null -- a ledger that says "unknown" everywhere is only marginally better
    #  than one that says nothing, and it was MY guess at the schema, not the schema.
    table = rates.get("usd_per_million_tokens") or {}
    total, priced, unpriced = 0.0, 0, []
    for model, u in per_model.items():
        entry = table.get(model)
        if not isinstance(entry, dict):
            unpriced.append(model)
            continue
        #  A MISSING RATE IS NOT A RATE OF ZERO. `entry.get("input", 0) or 0` priced a model
        #  with no published rate at nothing, which reads downstream as "this cost nothing"
        #  rather than "nobody knows what this cost".
        rate_in, rate_out = entry.get("input"), entry.get("output")
        if rate_in is None or rate_out is None:
            unpriced.append(model)
            continue
        total += (u["prompt"] / 1_000_000) * float(rate_in)
        total += (u["completion"] / 1_000_000) * float(rate_out)
        priced += 1
    return (round(total, 4) if priced else None), unpriced


def recorded_cohorts() -> set:
    if not LEDGER.is_file():
        return set()
    doc = json.loads(LEDGER.read_text(encoding="utf-8"))
    return {e.get("round") for e in (doc.get("entries") or [])}


def solicited_cohorts() -> list:
    return sorted(d.name for d in RAW.iterdir()
                  if d.is_dir() and any(d.glob("*samples*.json")))


def append(cohort: str, rates: dict) -> dict:
    u = usage_for(cohort)
    actual, unpriced = price(u["per_model"], rates)
    prompt = sum(v["prompt"] for v in u["per_model"].values())
    completion = sum(v["completion"] for v in u["per_model"].values())
    entry = {
        "utc": "2026-08-10T18:00:00Z",
        "round": cohort,
        "worst_case_usd": None,
        "actual_usd": actual,
        "prompt_tokens": prompt,
        "completion_tokens": completion,
        "units_seen": u["units_seen"],
        "units_reporting_usage": u["units_reporting_usage"],
        "rates_version": rates.get("rates_version") or "unknown",
        "rates_recorded_utc": rates.get("recorded_utc"),
        "unpriced_models": unpriced,
        "actual_note": (
            "Summed from each sample's usage block, INCLUDING failures and rejections -- a "
            "schema-invalid sample was still paid for, and dropping it would understate exactly "
            "the runs that went wrong. null means no arm reported usage, not zero. Priced from a "
            "dated rate table rather than an invoice, so this is an estimate. Local-arm "
            "inference and prepaid subscription usage are NOT in this ledger at all."),
        "backfilled": True,
    }
    doc = json.loads(LEDGER.read_text(encoding="utf-8")) if LEDGER.is_file() else {"entries": []}
    doc.setdefault("entries", []).append(entry)
    LEDGER.write_text(json.dumps(doc, indent=2, ensure_ascii=False) + "\n", encoding="utf-8")
    return entry


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--cohort")
    parser.add_argument("--backfill", action="store_true")
    parser.add_argument("--report", action="store_true")
    args = parser.parse_args()
    rates = load_rates()

    if args.report or not (args.cohort or args.backfill):
        have, all_c = recorded_cohorts(), solicited_cohorts()
        missing = [c for c in all_c if c not in have]
        print(f"  cohorts with raw material: {len(all_c)}")
        print(f"  recorded in the ledger:    {len(all_c) - len(missing)}")
        print(f"  UNRECORDED:                {len(missing)}")
        for c in missing:
            u = usage_for(c)
            tok = sum(v["prompt"] + v["completion"] for v in u["per_model"].values())
            print(f"    {c:34s} {tok:>9,} tokens across {u['units_seen']} unit(s)")
        if missing:
            print("\n  An artifact that claims to record spend and does not record it looks "
                  "exactly\n  like a project that spent nothing. Run --backfill.")
        return 1 if missing else 0

    targets = [args.cohort] if args.cohort else \
        [c for c in solicited_cohorts() if c not in recorded_cohorts()]
    for cohort in targets:
        e = append(cohort, rates)
        print(f"  {cohort:34s} {e['prompt_tokens']:>8,}p {e['completion_tokens']:>8,}c  "
              f"usd={e['actual_usd']}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 1619891..1c957fa 100644
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2010,6 +2010,51 @@
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
     },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
     {
       "utc": "2026-08-10T18:00:00Z",
       "round": "no-such-cohort-zzqx",

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Identifying preexisting test pollution**
**Preparing to run tests in temp directories**
exec
/bin/bash -lc 'python3 tools/tests/test_guard_identity.py
python3 -m py_compile tools/check_quotations.py tools/scan_own_code.py tools/guards.py tools/tests/test_gate_negative_controls.py
git diff --check' in /home/reed/git/open-asi-governance-forum
 succeeded in 101ms:

the tag: a message carries which guard produced it
  [32m✓[0m a tagged message still reads as a sentence first
  [32m✓[0m ...and carries the code at the end
  [32m✓[0m activates() finds the named guard
  [32m✓[0m ...and does NOT match a different guard's message
  [32m✓[0m ...and does not match a bare mention of the code in the text
  [32m✓[0m ...and does not match another guard's tag quoted inside its message
  [32m✓[0m ...while still matching the guard whose tag is terminal
  [32m✓[0m a malformed guard code ('nope') is refused
  [32m✓[0m a malformed guard code ('X-1') is refused
  [32m✓[0m a malformed guard code ('TOOLONGCODE-01') is refused
  [32m✓[0m a malformed guard code ('xx-01') is refused
  [32m✓[0m a malformed guard code ('XX-1') is refused

the registry: an unexercised guard and an orphaned expectation both refuse
  [32m✓[0m BASELINE: a guard with a fixture naming it is clean
  [32m✓[0m a guard NO fixture names is refused
  [32m✓[0m ...and the refusal says an unexercised guard is where an unreachable one hides
  [32m✓[0m a guard named only in a COMMENT is still unexercised
  [32m✓[0m a guard named only in a DOCSTRING is still unexercised
  [32m✓[0m a guard REMOVED in a rewrite, still named by a fixture, is refused
  [32m✓[0m ...and the refusal cites control 45 by what it forbids
  [32m✓[0m one code declared at TWO sites is refused
  [32m✓[0m BASELINE: a guard in the committed baseline and still declared is clean
  [32m✓[0m removing a guard AND its fixture together is REFUSED
  [32m✓[0m ...and the refusal names control 45 and what it forbids
  [32m✓[0m an authorised removal with a ground too short to be one is still refused
  [32m✓[0m ...and a removal recorded WITH a ground is permitted
  [32m✓[0m an EMPTY registry is refused rather than passing vacuously
  [32m✓[0m a DISCARDED activates() call does not count as an expectation
  [32m✓[0m a wrapper that does NOT call expect_guard does not count either
  [32m✓[0m ...while a wrapper that DOES delegate is accepted
  [32m✓[0m a suite WITHOUT the synthetic declaration still has its codes counted
  [32m✓[0m ...and declaring them synthetic exempts exactly that file

the real repository
  [32m✓[0m every named guard here is declared once and driven by a fixture

32 passed, 0 failed

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Assessing C61 configuration pooling readiness**
**Reviewing negative-control ledger write blocker**
exec
/bin/bash -lc "printf 'HEAD='; git show HEAD:record/cycles/spend-ledger.json | rg -c '\"round\": \"no-such-cohort-zzqx\"'
printf 'worktree='; rg -c '\"round\": \"no-such-cohort-zzqx\"' record/cycles/spend-ledger.json
printf 'files='; python3 - <<'PY'
import sys
sys.path.insert(0,'tools'); import check_quotations as q
print(len(q.files_to_check()))
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
HEAD=84
worktree=87
files=98

diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

**Identifying ledger pollution issue**
**Evaluating quote length cap and parsing strategy**
**Analyzing nested quote delimiter bypass risk**
**Planning suffix attribution improvements**
**Refining guard abstraction and blockquote handling**
**Adding multiple must-flag assertions**
**Planning fixture morphology coverage**
**Prioritizing ledger issue fixes**
**Fixing ledger cohort append bug**
**Assessing quote boundary overmatching risks**
**Refining C45 model adaptation and test strategy**
codex
Not ready to land yet. The quotation repair is directionally right and the guard adapter is appropriate, but I found three landing blockers.

1. The negative-control suite mutates the real spend ledger and passes anyway. [test_gate_negative_controls.py:381](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_negative_controls.py:381) invokes a nonexistent cohort; [record_spend.py:187](/home/reed/git/open-asi-governance-forum/tools/record_spend.py:187) accepts it and appends a zero-unit entry. The assertion uses `returncode != 0 or not emitted_cost`, so exit 0 with `usd=None` passes. HEAD already contains 84 such entries; the worktree contains 87. Fix this before landing:

   - Refuse nonexistent cohorts before `append()`.
   - Require nonzero exit and byte-identical ledger in the fixture.
   - Run against a temporary ledger or monkeypatched path.
   - Remove or correct the three new worktree entries; the 84 committed entries need an attached correction under the record rules.

   I removed the one additional row created by my review run and left the prior three untouched.

2. C45 regressed for D-B. [scan_own_code.py:204](/home/reed/git/open-asi-governance-forum/tools/scan_own_code.py:204) replaces the old `allowed = True; if allowed:` must-flag fixture with `if True:`. The implementation still detects both, but the old behavior is no longer protected. Retain both positive arms.

3. The wrapped blockquote arm does not test the repaired regex. [test_gate_negative_controls.py:330](/home/reed/git/open-asi-governance-forum/tools/tests/test_gate_negative_controls.py:330) contains no quote delimiters, so it exercises the pre-existing `blockquotes()` path. Keep it as a blockquote regression, but add a wrapped curly-quote arm; the new regex currently has coverage only for wrapped straight quotes.

On your questions:

1. The blank-line bound is reasonable as a conservative parsing boundary, but the prose overstates it. [check_quotations.py:124](/home/reed/git/open-asi-governance-forum/tools/check_quotations.py:124) permits any number of nonblank wrapped lines, not “a single newline.” It also excludes legitimate multi-paragraph quotations. The 4,000-character cap silently makes overlong quotations invisible: four current candidates are 5,350 characters, although none is presently attributed. Prefer treating an attributed over-cap/unclosed quotation as an explicit refusal instead of silently skipping it. Also add boundary fixtures for 4,000 and 4,001 characters.

2. Do not say “one order of magnitude better than zero.” The measurable result is:

   - Candidates: 235 → 350.
   - Attribution matches: 1 → 4.
   - Non-exempt quotations actually checked: 0 → 1.

   “Candidate recall materially improved; attribution recall remains demonstrably low” is honest. A cheap next improvement is strict suffix attribution: `“…quotation…” — Claude` and `“…quotation…” (Grok, sample …)`. Those forms occur repeatedly. Also consider the prefix form `Raised by Gemini:`. I found 51 candidates with a nearby party name but no recognized attribution; they are not all true attributions, but several are unmistakable suffix citations.

3. Adapting the fixture is correct. `expect_guard()` consumes normalized refusal messages; splitting process output into messages is a transport adapter, not evidence that `guards.py` is wrongly modeled. I would revise the C45 gap language accordingly rather than generalizing `guards.py` around subprocess output.

4. Besides the two vacuous/regressed arms above, the blank-line arm is sound: its fabricated text would fail if swallowed. QT-01 and QT-02 are genuinely driven.

5. C61 should follow only after these are repaired and the ledger side effect is recorded. The ledger discovery has higher priority because the test suite currently corrupts the artifact it purports to protect.

Relevant targeted checks otherwise passed: guard identity 32/32, quotation checker 13/13, gate controls 36/36, scanner fixtures 6/6, compilation, and `git diff --check`. I did not treat the full suite as a valid clean signal because its spend test is non-idempotent.
diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index 279e8c46a0a51afff625aa40bc3cbe8c8156435a..1c957fa506369917016ea9f21507cd8791238853
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -2069,21 +2069,6 @@
       "unpriced_models": [],
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
-    },
-    {
-      "utc": "2026-08-10T18:00:00Z",
-      "round": "no-such-cohort-zzqx",
-      "worst_case_usd": null,
-      "actual_usd": null,
-      "prompt_tokens": 0,
-      "completion_tokens": 0,
-      "units_seen": 0,
-      "units_reporting_usage": 0,
-      "rates_version": "openrouter-list-2026-08-07",
-      "rates_recorded_utc": "2026-08-07T09:18:03Z",
-      "unpriced_models": [],
-      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
-      "backfilled": true
     }
   ]
 }

tokens used
93,761
