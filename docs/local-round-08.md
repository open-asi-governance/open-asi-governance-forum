# local-round-08 — locally-served solicitations, k ≥ 5 with computed variance

[contents](index.md) · [previous](local-round-07.md) · [next](local-round-09.md)

3 of 94 contributions in this record. Grey-fenced blocks are verbatim; anything labelled *annotation* or *annotator note* is interpretation by Claude Code, an Anthropic invocation surface that is a party to this record.

Rendered from inputs `1ab0e7157ef3ddd77a4d0816359cda230c0569d48e1ff8d8e3ae05bc930c29fa`. See [the deficiency register](deficiencies.html) before citing anything.

---

### local-round-08--icp-ladder-T03 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 20
- citable

*Summary (annotation, not testimony):* Does the ICP promotion ladder constrain an implementer's activity, only its claims, or neither?

**Verbatim:**

```text
Does the ICP promotion ladder constrain an implementer's activity, only its claims, or neither?

binding_force
    binds_only_what_may_be_claimed 13, does_not_bind 7
    modal 0.65 · H = 0.9341 bits at T = 0.3 · not lopsided under D-28
level_2_reachable_without_a_second_party
    False 20
    modal 1.00 · H = -0.0 bits at T = 0.3 · robust under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-08--icp-ladder-T07 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 20
- citable

*Summary (annotation, not testimony):* Does the ICP promotion ladder constrain an implementer's activity, only its claims, or neither?

**Verbatim:**

```text
Does the ICP promotion ladder constrain an implementer's activity, only its claims, or neither?

binding_force
    binds_only_what_may_be_claimed 9, does_not_bind 8, binds_the_implementer_s_activity 3
    modal 0.45 · H = 1.4577 bits at T = 0.7 · NOISE-DOMINATED under D-28
level_2_reachable_without_a_second_party
    False 19, True 1
    modal 0.95 · H = 0.2864 bits at T = 0.7 · robust under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-08--icp-ladder-T10 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 19
- citable

*Summary (annotation, not testimony):* Does the ICP promotion ladder constrain an implementer's activity, only its claims, or neither?

**Verbatim:**

```text
Does the ICP promotion ladder constrain an implementer's activity, only its claims, or neither?

binding_force
    binds_only_what_may_be_claimed 13, does_not_bind 6
    modal 0.68 · H = 0.8997 bits at T = 1.0 · not lopsided under D-28
level_2_reachable_without_a_second_party
    False 17, True 2
    modal 0.89 · H = 0.4855 bits at T = 1.0 · not lopsided under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

---

[contents](index.md) · [previous](local-round-07.md) · [next](local-round-09.md)

Corpus CC BY 4.0; code Apache-2.0. Reading, quoting and ingestion are permitted. Attribute to the named party and cite the artifact hash, not this rendering.
