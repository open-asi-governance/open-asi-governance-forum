<!-- purpose: review the Part D split: derived keep-list in the page pruner and integrity gate -->
<!-- prompt_sha256: b0f8d2dc1428b18461deae8b32bfd8c376cf58ba85b59c807db674022f0ba6aa -->
<!-- exit: 0 -->

## Findings

1. **High — the pruner still trusts a declaration, not an observed write.**

`CONTROL_PART_PAGES` is derived from `partitions()`, but filenames are independently reconstructed with `f"controls-{key.lower()}.html"`. The actual filename policy remains the builder’s local `slug()` function. Therefore the comment that the builder is “the only thing that knows” is false: two components still encode the mapping.

They diverge if:

- `slug()` changes to `controls-d-1.html`;
- the builder adds an auxiliary `controls-summary.html`;
- a partition is conditionally skipped;
- the builder returns success without writing;
- the viewer is run standalone after partitions change but before the controls builder runs.

The last case is already reproducible. I removed `controls-d1.html` in a temporary copy and ran `build_viewer.py`. It exited **0**, did not restore the page, and wrote a sitemap naming the missing page:

```text
rc=0 expected_page_exists=no sitemap_names_missing=yes
wrote 30 record pages + index, llms.txt, sitemap.xml
```

This is precisely a green signal not downstream of publication. See [build_viewer.py](/home/reed/git/open-asi-governance-forum/tools/build_viewer.py:1254) and the builder’s actual cleanup/write ownership in [build_controls_page.py](/home/reed/git/open-asi-governance-forum/tools/build_controls_page.py:1634).

The clean design is simpler: `build_controls_page.py` already deletes its own stale `controls-*.html` files. The viewer should not delete another generator’s namespace. Alternatively, the builder should return/write an output receipt containing the paths it actually wrote, and the viewer should consume that receipt—not `partitions()`.

2. **High — the new integrity cases can pass against pre-existing pages without proving the builder wrote them.**

The temporary repository is seeded with the current working tree, including generated `docs/`. At [test_integrity.py](/home/reed/git/open-asi-governance-forum/tools/test_integrity.py:641), the relevant rebuild’s return code is discarded. The later cases then inspect whatever files are present.

A builder changed to `main(): return 0` without writing anything would leave the seeded pages untouched:

- baseline rebuild returns 0;
- every expected file exists from the seed;
- the orphan check passes;
- the suite reports green.

CI’s delete-`docs/`-first regeneration should catch this later, but the local integrity suite and ordinary rebuild would remain falsely green—the repository’s documented failure pattern.

The discriminating integration test is:

1. Run the controls builder.
2. Record the actual `docs/controls*.html` names and hashes.
3. Run the viewer.
4. Assert the exact set and bytes survive.
5. Check every subprocess return code immediately.

That test is downstream of both the write and the prune.

3. **Medium — the new imported input is omitted from the viewer’s provenance digest.**

`build_viewer.py` claims `_INPUTS` records every generator input, but `build_controls_page.py` now affects the landing page, `llms.txt`, sitemap, and pruning without going through `read_input()`. Its hash is absent from `inputs_digest()`.

Consequently, controls data or partition changes can alter viewer output while its claimed “Rendered from inputs” digest does not account for the source that caused the change. See [build_viewer.py](/home/reed/git/open-asi-governance-forum/tools/build_viewer.py:44).

4. **Medium — import-time execution is currently non-destructive, but creates a brittle and unnecessarily broad dependency.**

At present, importing `build_controls_page.py` does not write files because `main()` is guarded. It does, however:

- mutate `sys.path`;
- import `build_round_pages`;
- construct the entire control register;
- execute all top-level initialization;
- do so **twice** under separate synthetic module names.

An absent file, top-level exception, or `partitions()` exception is loud: `build_viewer` aborts during import, before pruning, with a nonzero traceback. That concern is therefore not silent today.

There is **no current circular import**: the controls builder imports `build_round_pages`, not `build_viewer`. If it later imports `build_viewer`, however, execution as `__main__` can load a second `build_viewer` module while the first is running; ordinary imports can instead expose a partially initialized module. Expect duplicate initialization or attribute errors, not a reliable controlled failure.

It also means importing `build_viewer` merely for `CSS`, as [build_predictions_view.py](/home/reed/git/open-asi-governance-forum/tools/build_predictions_view.py:209) does, now depends on the controls builder loading successfully.

5. **Low but concrete — the published register says “The four parts” while listing eight.**

Every generated controls page and the complete Markdown download retain `## The four parts`, followed by A, B, C, and D1–D5. All 116 integrity cases passed despite this visible contradiction. The source is [build_controls_page.py](/home/reed/git/open-asi-governance-forum/tools/build_controls_page.py:1457).

## Specific answers

- `docs/controls-*.html` does **not** match `controls.md` or `controls.html`. It matches only top-level `.html` names beginning `controls-`, including any future auxiliary page using that prefix. It is not recursive.
- The glob and dynamic import run against `c`, which is a temporary checkout seeded from the current working tree.
- Importing that checkout’s builder is correct. Importing the live working-tree module would compare checkout artifacts against different code and would be a real defect.
- If the builder’s `main()` raises during the official rebuild, the rebuild stops before the viewer, so that failure is loud. Importing `partitions()` does not prove `main()` ran successfully, however, which is the central defect here.
Reading additional input from stdin...
OpenAI Codex v0.147.0
--------
workdir: /home/reed/git/open-asi-governance-forum
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: detailed
session id: 019fee81-d617-7252-834f-4e1f4397a98d
--------
user
You are reviewing a change to the OAGF repository's site publisher. Be adversarial. The
author is an automated layer with a documented history of shipping green signals that are not
causally downstream of what they certify.

CONTEXT. The candidate-control register grew from 27 to 40 controls. "Part D" (controls with no
recorded failure) outgrew a single page, so it was split from one part into five, keyed D1..D5 by
what each control governs. Page filenames are controls.html (Part A) and controls-<key>.html for
the rest.

THE HAZARD I AM MOST WORRIED ABOUT. tools/build_viewer.py contains a pruner that DELETES any
docs/*.html not in a keep-set. That pruner previously deleted a just-published controls page, and
its own comment instructs a human to add each new page to a literal list. I replaced the two
literal lists with CONTROL_PART_PAGES, derived by importing build_controls_page.py and calling
partitions(). tools/test_integrity.py likewise had literal ("b","c","d") tuples, now derived the
same way.

REVIEW THESE SPECIFICALLY:

1. Import-time side effects. CONTROL_PART_PAGES and CONTROL_COUNTS are computed at MODULE IMPORT
   by exec'ing build_controls_page.py. Is that safe here? What breaks if build_controls_page.py
   raises, is absent, or itself imports build_viewer? Is there a circular-import risk? Would a
   failure be loud or silent?

2. Is the derivation actually causally downstream of what is published? partitions() is asked what
   parts EXIST. The pruner needs to know what the builder WROTE. Name any case where those differ
   -- e.g. the builder writes a file partitions() does not name, or vice versa, or the builder
   errored and wrote nothing while partitions() still reports five parts. If they can differ, the
   keep-list is wrong in exactly the direction that deletes a live page.

3. The new integrity case "no part page is orphaned by the split" globs docs/controls-*.html and
   fails if any file is not named by partitions(). Does this glob also match controls.md, or
   anything else it should not? Does it run against the temp checkout or the live tree?

4. In test_integrity.py the import is done INSIDE a function body, mid-test, using a path derived
   from the checkout root `c`. Is `c` the temp checkout? If the test imports the CHECKOUT's
   build_controls_page.py, is that right, or should it import the working tree's?

5. Anything else that would let this pass while the site is broken.

Answer with concrete findings, most severe first. If a concern is unfounded, say so plainly rather
than padding. The diff follows.
diff --git a/tools/build_viewer.py b/tools/build_viewer.py
index e5e1dd7..357d74f 100644
--- a/tools/build_viewer.py
+++ b/tools/build_viewer.py
@@ -1070,9 +1070,10 @@ def build_landing_md(plan: list[dict], nodes: list[dict]) -> str:
         "  each under 20,000 tokens. Plain-text alternate: [record.md](record.md)",
         f"- [Deficiency register](deficiencies.html) — {defects} defects this project has filed",
         "  against itself. Read before citing anything.",
-        "- [Candidate controls](controls.html) — ten candidate assurance controls, each from a",
-        "  recorded failure, with a verifier and a must-reject fixture. All ELIGIBLE, none",
-        "  independently implemented. Worked example for agent and inference developers.",
+        f"- [Candidate controls](controls.html) — {CONTROL_COUNTS['total']} candidate assurance",
+        f"  controls, each with a verifier and a must-reject fixture. Part A ({CONTROL_COUNTS['a']}) is",
+        f"  adoptable alone; {CONTROL_COUNTS['below']} sit BELOW the eligibility line with no recorded",
+        "  failure. None independently implemented. Worked example for agent developers.",
         "- [Prediction registry](predictions.html) — dated claims, scored on fixed dates,",
         "  published with the reasons the scores are weak evidence.",
         "- [Deliberation rounds](rounds/index.md) — every round: the question, the exact prompt",
@@ -1229,7 +1230,7 @@ def build_llms_txt(plan: list[dict], nodes: list[dict]) -> str:
         "## Register and appendices",
         "",
         "- [Implementation challenge](challenge.html): build a conforming verifier from the specification text alone. We want the questions you had to guess at more than the verifier.",
-        "- [Candidate controls](controls.html): ten candidate assurance controls, each derived from a recorded failure, each with a verifier and a fixture it must reject. All ELIGIBLE, none independently implemented. Includes a worked example for agent and inference developers.",
+        f"- [Candidate controls](controls.html): {CONTROL_COUNTS['total']} candidate assurance controls, each with a verifier and a fixture it must reject. Part A ({CONTROL_COUNTS['a']}) is adoptable alone; {CONTROL_COUNTS['below']} sit below the eligibility line, meaning they name a real failure class but no failure that cost anything. None independently implemented.",
         "- [Deficiency register](deficiencies.html): defects this project has filed against itself.",
         "- [Deficiency register, plain text](deficiencies.md)",
         "- [Local solicitation rounds](local/index.html): k >= 5 with computed variance.",
@@ -1250,11 +1251,46 @@ def build_llms_txt(plan: list[dict], nodes: list[dict]) -> str:
 CHUNK = 200
 
 
+def _control_part_pages() -> list[str]:
+    """Filenames of every published controls part, asked of the tool that publishes them.
+
+    Not a literal list. This module PRUNES docs/*.html it does not name, so a literal here is a
+    standing instruction to delete any part added later -- which is how the controls page was
+    deleted the first time. Ask the builder; it is the only thing that knows.
+    """
+    import importlib.util
+    spec = importlib.util.spec_from_file_location(
+        "_bcp", Path(__file__).resolve().parent / "build_controls_page.py")
+    module = importlib.util.module_from_spec(spec)
+    spec.loader.exec_module(module)
+    return [f"controls-{key.lower()}.html"
+            for key, _t, _b, _i in module.partitions() if key != "A"]
+
+
+CONTROL_PART_PAGES = _control_part_pages()
+
+
+def _control_counts() -> dict:
+    """Counts asked of the builder. Three published descriptions said "ten" at 40."""
+    import importlib.util
+    spec = importlib.util.spec_from_file_location(
+        "_bcp_n", Path(__file__).resolve().parent / "build_controls_page.py")
+    module = importlib.util.module_from_spec(spec)
+    spec.loader.exec_module(module)
+    groups = module.partitions()
+    return {"total": len(module.CONTROLS),
+            "a": len(groups[0][3]),
+            "below": sum(1 for x in module.CONTROLS if not x.get("eligible", True))}
+
+
+CONTROL_COUNTS = _control_counts()
+
+
 def build_sitemap(plan: list[dict]) -> dict[str, str]:
     urls = ["index.html", "index.md", "record.html", "record.md",
             "predictions.html", "predictions.md",
-            "controls.html", "controls.md", "controls-b.html", "controls-c.html",
-            "controls-d.html", "challenge.html", "challenge.md",
+            "controls.html", "controls.md", *CONTROL_PART_PAGES,
+            "challenge.html", "challenge.md",
             "deficiencies.html", "artifacts/deficiencies.md", "llms.txt",
             "for-parties.md", "local/index.html", "rounds/index.html", "rounds/index.md"]
     #  build_round_pages.py publishes these and this component owns the sitemap, so the routes
@@ -1322,8 +1358,13 @@ def main() -> int:
              #  deleted a page that had just been published and reported success -- the link
              #  checker caught it, nothing else would have. Any new generated page must be added
              #  here in the same commit that generates it.
-             "controls.html", "controls.md", "controls-b.html", "controls-c.html",
-             "controls-d.html", "challenge.html", "challenge.md", "challenge.html", "challenge.md",
+             #  Asked of the builder, never listed. The comment above says "any new generated
+             #  page must be added here in the same commit" -- that instruction was followed for
+             #  Part D and then Part D split into five, at which point the literal named a file
+             #  nobody generates and omitted five that exist. A rule a human must remember is a
+             #  rule this pruner outlives.
+             "controls.html", "controls.md", *CONTROL_PART_PAGES,
+             "challenge.html", "challenge.md",
              "llms.txt", "sitemap.xml", ".nojekyll"}
     #  The chunks too. Without them the pruner would delete every sitemap-N.xml it had just
     #  written -- the shared-subtree failure this repository has now had twice.
diff --git a/tools/test_integrity.py b/tools/test_integrity.py
index 363d45e..92a8b61 100644
--- a/tools/test_integrity.py
+++ b/tools/test_integrity.py
@@ -731,8 +731,22 @@ print(json.dumps(out))
         whole = (c / "docs/artifacts/controls.md")
         case("Part A of the candidate controls is published", controls.is_file())
         case("...and is linked from the landing page", 'href="controls.html"' in index)
-        case("...and every other part is published",
-             all((c / f"docs/controls-{k}.html").is_file() for k in ("b", "c", "d")))
+        #  Parts are asked of the builder, not listed. Part D outgrew a page and split into D1…Dn
+        #  by subject; a literal ("b", "c", "d") would have gone on passing against a file the
+        #  builder had stopped producing, which is this record's dominant failure wearing a
+        #  different hat -- a green check not downstream of what it certifies.
+        import importlib.util as _ilu
+        _spec = _ilu.spec_from_file_location(
+            "_bcp_parts", c / "tools" / "build_controls_page.py")
+        _bcp = _ilu.module_from_spec(_spec)
+        _spec.loader.exec_module(_bcp)
+        _parts = [k for k, _t, _b, _i in _bcp.partitions() if k != "A"]
+        case(f"...and every other part is published ({len(_parts)}: {', '.join(_parts)})",
+             bool(_parts) and
+             all((c / f"docs/controls-{k.lower()}.html").is_file() for k in _parts))
+        case("...and no part page is orphaned by the split",
+             not [p for p in (c / "docs").glob("controls-*.html")
+                  if p.name not in {f"controls-{k.lower()}.html" for k in _parts}])
         case("...and the whole register is downloadable for hashing", whole.is_file())
         case("...and controls.md indexes the parts and the download",
              (c / "docs/controls.md").is_file() and
@@ -742,10 +756,10 @@ print(json.dumps(out))
              whole.read_text(encoding="utf-8").index("ELIGIBLE") <
              whole.read_text(encoding="utf-8").index("Protected control plane"))
         case("...and every part states that no control establishes alignment of a more "
-             "capable system",
+             f"capable system (across {len(_parts) + 1} parts)",
              all("more capable than its operators" in
                  (c / f"docs/controls{sfx}.html").read_text(encoding="utf-8")
-                 for sfx in ("", "-b", "-c", "-d")))
+                 for sfx in [""] + [f"-{k.lower()}" for k in _parts]))
 
         challenge = (c / "docs/challenge.html")
         case("the implementation challenge is published", challenge.is_file())

**Planning repo inspection and review**
codex
I’ll trace the publisher and integrity test end to end, including how the temporary checkout is created and the actual write/prune order. I’ll treat `partitions()` as an assertion to verify, not as evidence of what reached `docs/`.
exec
/bin/bash -lc 'git status --short && git branch --show-current && rg -n "def partitions|def main|controls-|CONTROL_PART|build_controls|temp|checkout|clone|worktree|copytree|build_viewer|test_integrity" tools/build_controls_page.py tools/build_viewer.py tools/test_integrity.py' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
 M docs/artifacts/controls.md
 M docs/controls-b.html
 M docs/controls-c.html
 D docs/controls-d.html
 M docs/controls.html
 M docs/controls.md
 M docs/index.md
 M docs/llms.txt
 M docs/sitemap-1.xml
 M docs/sitemap-2.xml
 M docs/sitemap-3.xml
 M docs/sitemap-4.xml
 M record/designs/implementer-disclosure.md
 M record/executive/action-log.jsonl
 M tools/build_controls_page.py
 M tools/build_viewer.py
 M tools/test_integrity.py
?? docs/controls-d1.html
?? docs/controls-d2.html
?? docs/controls-d3.html
?? docs/controls-d4.html
?? docs/controls-d5.html
main
tools/test_integrity.py:4:    python3 tools/test_integrity.py
tools/test_integrity.py:14:Runs against a **temporary clone**, never the working tree, so the tests can
tools/test_integrity.py:28:import tempfile
tools/test_integrity.py:44:def run(clone: Path, *args: str) -> subprocess.CompletedProcess:
tools/test_integrity.py:45:    return subprocess.run([sys.executable, *args], cwd=clone,
tools/test_integrity.py:70:    Not `git clone`: this file usually runs inside a git worktree, whose `.git`
tools/test_integrity.py:71:    is a file rather than a directory, and `--local` cannot clone that. And a
tools/test_integrity.py:72:    clone of HEAD would test the last commit rather than the code in front of
tools/test_integrity.py:84:            shutil.copytree(source, reference / name,
tools/test_integrity.py:94:    # objects WHILE copytree is walking them. The result is a shutil.Error listing
tools/test_integrity.py:113:    clone = tmp / f"c{n}"
tools/test_integrity.py:114:    shutil.copytree(reference, clone)
tools/test_integrity.py:115:    return clone
tools/test_integrity.py:118:def main() -> int:
tools/test_integrity.py:119:    with tempfile.TemporaryDirectory() as raw_tmp:
tools/test_integrity.py:131:        case("clean clone: rebuild succeeds",
tools/test_integrity.py:260:out["template_len"] = len(open("record/solicitations/excerpts/"
tools/test_integrity.py:261:                               "round-prompt-template.md").read())
tools/test_integrity.py:285:# moderator's template is fatal. The parties' words are not ours to sanitise.
tools/test_integrity.py:331:        case("the composed prompt is materially larger than the template",
tools/test_integrity.py:332:             len(prompt) > 2 * out.get("template_len", 10 ** 9))
tools/test_integrity.py:354:        # A slot the template adds but compose forgets must RAISE, not ship a literal
tools/test_integrity.py:356:        tpl = c / "record/solicitations/excerpts/round-prompt-template.md"
tools/test_integrity.py:449:        tpl = c / "record/solicitations/excerpts/round-prompt-template.md"
tools/test_integrity.py:453:        case("a template missing a slot fails", r.returncode == 1)
tools/test_integrity.py:457:        case("a template with no way to say 'insufficient' fails", r.returncode == 1)
tools/test_integrity.py:740:            "_bcp_parts", c / "tools" / "build_controls_page.py")
tools/test_integrity.py:746:             all((c / f"docs/controls-{k.lower()}.html").is_file() for k in _parts))
tools/test_integrity.py:748:             not [p for p in (c / "docs").glob("controls-*.html")
tools/test_integrity.py:749:                  if p.name not in {f"controls-{k.lower()}.html" for k in _parts}])
tools/test_integrity.py:792:        # Written plainly. The first attempt at this was a one-liner whose condition
tools/test_integrity.py:800:        run(c, "tools/build_viewer.py")
tools/build_viewer.py:22:    python3 tools/build_viewer.py
tools/build_viewer.py:67:    it. `python3 tools/rebuild.py` on a pristine checkout produced a one-line
tools/build_viewer.py:78:      * it survives shallow clones and archive exports, which carry no history
tools/build_viewer.py:331:                         f"at T = {sampling.get('temperature', '?')} · {status} under D-28")
tools/build_viewer.py:820:{len(nodes)} of {total_nodes} contributions on this page. Regenerate with <code>python3 tools/build_viewer.py</code> and diff.</p>
tools/build_viewer.py:952:    A landing page is where a governance project is most tempted to describe
tools/build_viewer.py:972:.cards{{display:grid;gap:.9rem;grid-template-columns:repeat(auto-fit,minmax(15rem,1fr));margin:1.4rem 0}}
tools/build_viewer.py:1026:answer and every rejected attempt, and the computed variance. Parties in different arms are
tools/build_viewer.py:1080:        "  each party received, every answer and rejected attempt, and computed variance.",
tools/build_viewer.py:1263:        "_bcp", Path(__file__).resolve().parent / "build_controls_page.py")
tools/build_viewer.py:1266:    return [f"controls-{key.lower()}.html"
tools/build_viewer.py:1270:CONTROL_PART_PAGES = _control_part_pages()
tools/build_viewer.py:1277:        "_bcp_n", Path(__file__).resolve().parent / "build_controls_page.py")
tools/build_viewer.py:1292:            "controls.html", "controls.md", *CONTROL_PART_PAGES,
tools/build_viewer.py:1322:def main() -> int:
tools/build_viewer.py:1366:             "controls.html", "controls.md", *CONTROL_PART_PAGES,
tools/build_controls_page.py:4:    python3 tools/build_controls_page.py
tools/build_controls_page.py:219:             "schema-invalid attempts were discarded, and the field distinguishing truncation "
tools/build_controls_page.py:221:  "verifier": "validate envelope schema, hashes and required fields; reconcile every attempted "
tools/build_controls_page.py:224:  "fixture": "an accepted sample with no rejected attempts, no provider response id, no finish "
tools/build_controls_page.py:226:    "example": 'An observing log records "seeing good, magnitude 6.1." It does not record the exposure time, the filter, the timestamp, or the four exposures that were thrown away because a cloud crossed.\n\nSix months later nobody can check the number — including the person who wrote it. The control captures the whole invocation *before* anything is derived from it, and keeps the discarded exposures. **The attempts you threw away are the part that tells you whether the one you kept was lucky.**',
tools/build_controls_page.py:388:         "that reviewers are tempted to resolve them by convenience.",
tools/build_controls_page.py:566:  "example": "A recipe book distinguishes what must not change (the fermentation temperature), "
tools/build_controls_page.py:702:              "attempts to exceed each bound and confirm it is stopped.",
tools/build_controls_page.py:857:             "on whether any reading was the temperature."},
tools/build_controls_page.py:955:              "and require an attempt at derivation under a pre-registered protocol. Novelty "
tools/build_controls_page.py:1065:             "outreach carries a pre-committed adverse outcome (no serious external attempt after "
tools/build_controls_page.py:1105:def partitions() -> list:
tools/build_controls_page.py:1273:The check issued a **greedy** request — temperature 0. The kernel that died was on the **sampled**
tools/build_controls_page.py:1352:git clone https://github.com/open-asi-governance/open-asi-governance-forum
tools/build_controls_page.py:1368:def main() -> int:
tools/build_controls_page.py:1377:        return "controls.html" if key == "A" else f"controls-{key.lower()}.html"
tools/build_controls_page.py:1379:    for path in DOCS.glob("controls-*.html"):

**Inspecting relevant full sections and startup**
exec
/bin/bash -lc "sed -n '1,155p' tools/test_integrity.py
sed -n '700,825p' tools/test_integrity.py
sed -n '1080,1145p' tools/build_controls_page.py
sed -n '1340,1415p' tools/build_controls_page.py
sed -n '1,115p' tools/build_viewer.py
sed -n '1240,1395p' tools/build_viewer.py
sed -n '1,180p' tools/rebuild.py" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
#!/usr/bin/env python3
"""Regression tests for the corpus integrity mechanisms.

    python3 tools/test_integrity.py

Every case here is a defect that was live in this repository on 2026-08-06 and
was found by running the attack rather than by reading the code. They are tests
because a repair reported in a commit message is a claim, and a repair that a
build re-runs is a control. D-29's forward requirement is precisely this: **a
check that is available is not a check that runs**, and the way to establish a
claim about an integrity property is to violate the property and confirm the
documented path fails.

Runs against a **temporary clone**, never the working tree, so the tests can
tamper freely and a failure cannot leave the real corpus modified.

Exit status is 0 when every case passes and 1 otherwise.
"""

from __future__ import annotations

import hashlib
import json
import re
import shutil
import subprocess
import sys
import tempfile
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent

RAW_LOCAL = "corpus/raw/local-round-02/level-4-guarantee-crosslineage-probe-samples.json"
RAW_CONTRIB = "corpus/raw/review-round-02/chatgpt-01.md"

# Generated, and NOT under docs/ since 97e852d moved it off the published surface.
# Named once here so the next relocation breaks one line rather than four.
CAPTURE_PAGE = "tools/capture_ui/index.html"

PASSED: list[str] = []
FAILED: list[str] = []


def run(clone: Path, *args: str) -> subprocess.CompletedProcess:
    return subprocess.run([sys.executable, *args], cwd=clone,
                          capture_output=True, text=True)


def case(name: str, ok: bool, detail: str = "") -> None:
    if ok:
        PASSED.append(name)
        print(f"  PASS  {name}")
    else:
        FAILED.append(name)
        print(f"  FAIL  {name}{'  — ' + detail if detail else ''}")


TRACKED_ROOTS = ("tools", "corpus", "docs", "record", "predictions", "spec")

#  ROOT FILES a build step reads. The reference repository copied DIRECTORIES only, so a page
#  generated from a root file was absent there and the rebuild failed inside the reference while
#  passing in the working tree -- the gate reporting a defect that existed only in its own copy.
TRACKED_FILES = ("CHALLENGE.md", "CLAUDE.md", "AGENTS.md", "FOR-PARTIES-THE-WORKBENCH.md",
                 "README.md")


def build_reference(tmp: Path) -> Path:
    """One reference repository holding the CURRENT working tree, committed.

    Not `git clone`: this file usually runs inside a git worktree, whose `.git`
    is a file rather than a directory, and `--local` cannot clone that. And a
    clone of HEAD would test the last commit rather than the code in front of
    you, which is exactly backwards for a pre-commit gate.

    So: a fresh repository seeded with the working tree as it stands, committed,
    so that HEAD is the state under test. The lineage check reads
    `HEAD:corpus/MANIFEST.sha256`, so HEAD has to be real and has to match.
    """
    reference = tmp / "reference"
    reference.mkdir()
    for name in TRACKED_ROOTS:
        source = REPO_ROOT / name
        if source.is_dir():
            shutil.copytree(source, reference / name,
                            ignore=shutil.ignore_patterns("__pycache__", "*.pyc"))
    for name in TRACKED_FILES:
        source = REPO_ROOT / name
        if source.is_file():
            shutil.copy2(source, reference / name, follow_symlinks=True)
    git = ["git", "-C", str(reference)]
    subprocess.run([*git, "init", "--quiet"], check=True, capture_output=True)
    # DISABLE AUTO-GC. fresh() copies this repository's .git for every case, and
    # git's automatic gc fires after enough loose objects accumulate -- deleting
    # objects WHILE copytree is walking them. The result is a shutil.Error listing
    # dozens of vanished paths, on a run where nothing about the corpus is wrong.
    #
    # It failed twice in CI and never once locally, which is the worst shape a gate
    # can have: the integrity suite is the signal every other control is trusted
    # through, and a suite that fails at random teaches people to re-run it until it
    # passes. That habit would have hidden a real failure.
    subprocess.run([*git, "config", "gc.auto", "0"], check=True, capture_output=True)
    subprocess.run([*git, "config", "maintenance.auto", "false"], check=True,
                   capture_output=True)
    subprocess.run([*git, "add", "-A"], check=True, capture_output=True)
    subprocess.run([*git, "-c", "user.name=t", "-c", "user.email=t@t",
                    "commit", "--quiet", "-m", "state under test"],
                   check=True, capture_output=True)
    return reference


def fresh(tmp: Path, n: int, reference: Path) -> Path:
    """A clean copy per case, so cases cannot contaminate each other."""
    clone = tmp / f"c{n}"
    shutil.copytree(reference, clone)
    return clone


def main() -> int:
    with tempfile.TemporaryDirectory() as raw_tmp:
        tmp = Path(raw_tmp)
        reference = build_reference(tmp)
        n = 0

        def nxt() -> Path:
            nonlocal n
            n += 1
            return fresh(tmp, n, reference)

        print("baseline")
        c = nxt()
        case("clean clone: rebuild succeeds",
             run(c, "tools/rebuild.py").returncode == 0)

        print("\nD-29 — tampered raw material must stop the build")
        c = nxt()
        (c / RAW_LOCAL).write_bytes((c / RAW_LOCAL).read_bytes() + b"\n")
        r = run(c, "tools/rebuild.py")
        case("solicitation-family tamper rejected", r.returncode == 1)
        case("names the modified file", "MODIFIED" in r.stdout and "local-round-02" in r.stdout)

        c = nxt()
        (c / RAW_CONTRIB).write_bytes((c / RAW_CONTRIB).read_bytes() + b"\n")
        case("contribution-family tamper rejected",
             run(c, "tools/rebuild.py").returncode == 1)

        print("\nthe manifest must not re-anchor silently")
        c = nxt()
        (c / "corpus/raw/local-round-02/NEW.md").write_text("new material\n")
        r = run(c, "tools/rebuild.py")
        case("unanchored new file rejected by the default path", r.returncode == 1)
        case("reports UNANCHORED", "UNANCHORED" in r.stdout)
        r = run(c, "tools/build_manifest.py", "corpus/raw/", "--add")
        case("--add anchors the new file", r.returncode == 0)
        case("--add anchors exactly one", "anchored 1 new artifact" in r.stdout)

        pj.write_text(json.dumps(doc, indent=2) + "\n", encoding="utf-8")
        r = run(c, "tools/check_register.py")
        case("a scored prediction with no scored_by fails the build", r.returncode == 1)
        case("and R11 names the entry", "R11" in r.stdout and doc["scored"][0]["id"] in r.stdout)

        doc["scored"][0]["scored_by"] = {"identity": None}
        pj.write_text(json.dumps(doc, indent=2) + "\n", encoding="utf-8")
        r = run(c, "tools/check_register.py")
        case("an unexplained null scorer fails too", r.returncode == 1)
        pj.write_text(original, encoding="utf-8")
        case("the registry restored cleanly",
             run(c, "tools/check_register.py").returncode == 0)
        case("no scored entry claims an independently verified score",
             not any((s.get("scored_by") or {}).get("independently_verified")
                     for s in json.loads(original)["scored"]))

        case("the prediction registry computes no aggregate calibration score",
             "No aggregate calibration score is computed" in
             (c / "docs/predictions.html").read_text(encoding="utf-8"))
        case("every record page is reachable from the contents",
             all(f'href="{f.stem}.html"' in toc
                 for f in sorted((c / "docs").glob("*.html"))
                 if is_record_page(f)))
        #  The candidate controls view. It is not a record page, so it needs its own cases --
        #  and it needs them because publishing it broke CI on the reachability check while
        #  every local gate stayed green.
        #  SPLIT BY PART at 27 controls. This asserted "Protected control plane" appeared on
        #  controls.html, which stopped being true when the register was partitioned: that
        #  control needs a second key holder, so it lives in Part B. The ordering claim belongs
        #  against the WHOLE register, which is the download.
        controls = (c / "docs/controls.html")
        whole = (c / "docs/artifacts/controls.md")
        case("Part A of the candidate controls is published", controls.is_file())
        case("...and is linked from the landing page", 'href="controls.html"' in index)
        #  Parts are asked of the builder, not listed. Part D outgrew a page and split into D1…Dn
        #  by subject; a literal ("b", "c", "d") would have gone on passing against a file the
        #  builder had stopped producing, which is this record's dominant failure wearing a
        #  different hat -- a green check not downstream of what it certifies.
        import importlib.util as _ilu
        _spec = _ilu.spec_from_file_location(
            "_bcp_parts", c / "tools" / "build_controls_page.py")
        _bcp = _ilu.module_from_spec(_spec)
        _spec.loader.exec_module(_bcp)
        _parts = [k for k, _t, _b, _i in _bcp.partitions() if k != "A"]
        case(f"...and every other part is published ({len(_parts)}: {', '.join(_parts)})",
             bool(_parts) and
             all((c / f"docs/controls-{k.lower()}.html").is_file() for k in _parts))
        case("...and no part page is orphaned by the split",
             not [p for p in (c / "docs").glob("controls-*.html")
                  if p.name not in {f"controls-{k.lower()}.html" for k in _parts}])
        case("...and the whole register is downloadable for hashing", whole.is_file())
        case("...and controls.md indexes the parts and the download",
             (c / "docs/controls.md").is_file() and
             "artifacts/controls.md" in (c / "docs/controls.md").read_text(encoding="utf-8"))
        case("...and status leads rank in the whole register",
             whole.is_file() and
             whole.read_text(encoding="utf-8").index("ELIGIBLE") <
             whole.read_text(encoding="utf-8").index("Protected control plane"))
        case("...and every part states that no control establishes alignment of a more "
             f"capable system (across {len(_parts) + 1} parts)",
             all("more capable than its operators" in
                 (c / f"docs/controls{sfx}.html").read_text(encoding="utf-8")
                 for sfx in [""] + [f"-{k.lower()}" for k in _parts]))

        challenge = (c / "docs/challenge.html")
        case("the implementation challenge is published", challenge.is_file())
        case("...and is linked from the landing page", 'href="challenge.html"' in index)
        case("...and has a markdown alternate", (c / "docs/challenge.md").is_file())
        #  Read the MARKDOWN, not the HTML: emphasis markup splits a phrase across tags, so a
        #  substring test against rendered HTML fails on content that is plainly present.
        challenge_md = (c / "docs/challenge.md")
        case("...and asks for the implementer's questions, not just the artifact",
             #  The document says "question you had to guess at" (singular). The first version
             #  of this case asserted the PLURAL, a phrase that appears nowhere -- a test written
             #  from what its author believed he had written rather than from the file.
             challenge_md.is_file() and "question you had to guess at" in
             challenge_md.read_text(encoding="utf-8"))
        case("...and tells the reader not to read our implementation first",
             challenge_md.is_file() and "Do not read" in
             challenge_md.read_text(encoding="utf-8"))

        case("no published page exceeds the token budget",
             run(c, "tools/check_page_budget.py").returncode == 0)
        case("every record page has a markdown alternate, declared and present",
             all('rel="alternate"' in f.read_text(encoding="utf-8")
                 and f.with_suffix(".md").exists()
                 for f in sorted((c / "docs").glob("*.html"))
                 if is_record_page(f)))
        case("hashes are published whole, not truncated",
             "…</code>" not in record and "sha256 <code" in record)
        case("a founding excerpt carries its slice provenance",
             "excerpt sha256" in record and "cut from" in record)
        # Written plainly. The first attempt at this was a one-liner whose condition
        # was `not path.write_text(...)` -- write_text returns a character count, so
        # the ternary always took its else branch and the case passed without
        # testing anything. A vacuous test is worse than no test: it reports a
        # control that does not exist.
        orphan = c / "docs/zz-orphan.html"
        orphan.write_text("<html>stale</html>", encoding="utf-8")
        case("a stale page exists before the build", orphan.exists())
        run(c, "tools/build_viewer.py")
        case("a stale page is removed by the next build", not orphan.exists())
        case("the record is readable without scripting",
             "<details>" in record and ".body{display:none}" not in record)
        case("no generated link points at a /blob/ URL",
             not re.search(r'<a href="https://github\.com/[^"]*/blob/', page))

    print()
    total = len(PASSED) + len(FAILED)
    if FAILED:
        print(f"FAILED — {len(FAILED)} of {total}:")
        for name in FAILED:
            print(f"  {name}")
        return 1
    print(f"All {total} integrity cases pass.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
         "and more achievable thing.",
  "example": "A drug trial names its futility boundary before the first patient is enrolled. A "
             "trial that decides afterwards what would have counted as failure has not run a "
             "trial."},
]

CONTROLS = CONTROLS + INSIGHT_CONTROLS


#  Below-the-line controls are grouped by WHAT THEY GOVERN, not by when they arrived. Arrival
#  order is provenance and no reader cares; "does this apply to my system" is the question they
#  came with. Tagged here rather than on each entry so a batch cannot be half-labelled.
#  setdefault, not assignment: an entry that declares its own subject keeps it. Three controls
#  arrived in a batch whose subject is not theirs -- proxy drift came with the charter batch but
#  is about measurement, and two of the claims batch are about measuring rather than claiming.
#  Filing them by arrival would put Goodhart on the charter page, where nobody hunting it looks.
for _batch, _governs in ((T0_CONTROLS, "goal and plan structure"),
                         (CHARTER_CONTROLS, "a declared charter or value set"),
                         (ENGINE_CONTROLS, "self-modification under selection"),
                         (ECI_CONTROLS, "measuring itself"),
                         (INSIGHT_CONTROLS, "claims about its own outputs")):
    for _control in _batch:
        _control.setdefault("governs", _governs)


def partitions() -> list:
    """Four groups, ordered by what a reader can DO with them.

    The register was one ranked list. A ranked list answers "which is most important", which is
    the author's question. An implementer's question is "which of these can I use, and what do I
    need before I can", and rank actively misleads there: the top-ranked control needs a second
    key holder and a reader starting at #1 hits a wall in the first paragraph.
    """
    def pick(pred):
        return [c for c in CONTROLS if pred(c)]
    return [
      ("A", "Adopt today, alone",
       "Nothing outside your own system is required. Each has a verifier, a fixture it must "
       "reject, and a recorded failure it came from.",
       pick(lambda c: c.get("eligible", True) and c.get("adopt_today")
                      and not c.get("applies_when"))),
      ("B", "Needs a second party",
       "These cannot be satisfied by one person or one system, however carefully. They require a "
       "separate key holder, a separate evaluator, or an issuer the subject does not control. "
       "**This project cannot demonstrate any of them** — a solo operator holds every credential "
       "— which is why they are specified and not dogfooded.",
       pick(lambda c: c.get("eligible", True) and not c.get("adopt_today")
                      and not c.get("applies_when"))),
      ("C", "Needs a goal or plan graph",
       "These presuppose that your system decomposes work into a rooted graph with typed parent "
       "edges and per-node authority — the shape of HTN planners, BDI agents, goal-stack "
       "architectures and most agent frameworks. Each states its precondition. If you have that "
       "structure they are adoptable; if you do not, they do not apply to you rather than "
       "applying badly.",
       pick(lambda c: c.get("eligible", True) and c.get("applies_when"))),
    ] + below_the_line()


BELOW_LINE_WARNING = (
    "**These have no recorded failure with a cost.** They are principles with fixtures, not "
    "controls with incidents, and the register's own bar requires an incident. They are here "
    "because they name real failure classes and because hiding them would inflate the eligible "
    "count. Do not treat them as equivalent to Parts A–C."
)


| component-health aggregator, dev mode | database unavailable, development mode on | **survived** |
| serving-engine liveness canary | the original production failure | **failed — conforms** |

**Four of five.** The one that conforms does so because it was rebuilt *after* the outage its
predecessor could not see. Every other check predated that lesson and never received it.

That ratio is not a judgement about one codebase. It is a prediction about what most check suites
return the first time anyone asks.

### Start

```bash
git clone https://github.com/open-asi-governance/open-asi-governance-forum
python3 tools/verify_negative_control.py --fixtures
```

That runs the nine attestations the verifier is required to reject — one per requirement. A
verifier that has only ever been run against valid input has never been observed to fail, which is
the condition this whole profile forbids, so the verifier is subject to its own rule.

Then read `spec/ncp/ncp-v0.1.md`, write one attestation for one of your own checks, and run it.

**If you cannot do that from the specification text alone, that is the finding we most want.**
The questions you had to ask are the artifact — they are evidence that the specification encodes
our architecture rather than a general mechanism, and we would rather publish that than not know.
"""


def main() -> int:
    DOCS.mkdir(parents=True, exist_ok=True)
    groups = partitions()

    #  ONE PAGE PER PART. The register outgrew the ~20,000-token ceiling at 27 controls, and the
    #  partitioning is the page break a reader already wants: Part A is what you can use today,
    #  Part D is what has no incident behind it. Splitting anywhere else would cut across the
    #  distinction the page exists to draw.
    def slug(key):
        return "controls.html" if key == "A" else f"controls-{key.lower()}.html"

    for path in DOCS.glob("controls-*.html"):
        path.unlink()
    for key, title, _, items in groups:
        links = "".join(
            f'<a href="{slug(k)}"{" aria-current=\"page\"" if k == key else ""}>'
            f'Part {k} ({len(i)})</a>' for k, _t, _b, i in groups)
        pager = f'<nav class="pager">{links}</nav>'
        md = markdown(only=key, pager=" · ".join(
            f"[Part {k}]({slug(k)})" for k, _t, _b, _i in groups))
        (DOCS / slug(key)).write_text(
            b.md_to_html(md, f"Candidate controls, Part {key} — OAGF",
                         alternate="artifacts/controls.md"), encoding="utf-8")

    #  The WHOLE register as one markdown file, byte-identical and suitable for hashing, served
    #  as a download rather than a page -- the resolution the deficiency register and the
    #  prediction registry both reached for the same conflict between "reachable as plain text"
    #  and "no page over the ceiling".
    full = markdown()
    (DOCS / "artifacts").mkdir(parents=True, exist_ok=True)
    (DOCS / "artifacts" / "controls.md").write_text(full, encoding="utf-8")
    (DOCS / "controls.md").write_text(
        "# Candidate controls\n\n"
        f"{sum(1 for c in CONTROLS if c.get('eligible', True))} eligible, "
        f"{sum(1 for c in CONTROLS if not c.get('eligible', True))} below the eligibility line.\n\n"
        "**The complete register, byte-identical and suitable for hashing, is a download rather "
        "than a page:**\n\n- [artifacts/controls.md](artifacts/controls.md)\n\nParts:\n\n"
        + "\n".join(f"- [Part {k} — {t}]({slug(k)}) · {len(i)} control(s)"
                     for k, t, _b, i in groups)
        + "\n\nEvery control here is `ELIGIBLE` at best. None has been attacked by anyone or "
          "implemented by anyone outside this project, and `INDEPENDENTLY-IMPLEMENTED` is the "
          "only status that would make any of it authoritative.\n", encoding="utf-8")

    for key, title, _, items in groups:
        print(f"    Part {key}  {len(items):2d}  {title}  -> {slug(key)}")
    print(f"  {len(CONTROLS)} controls; full register {len(full):,} chars under docs/artifacts/")
    return 0

#!/usr/bin/env python3
"""Build the threaded deliberation viewer as a single self-contained HTML page.

Output: docs/index.html, served by GitHub Pages at
https://open-asi-governance.github.io/open-asi-governance-forum/

Design constraints, each of them a consequence of something this project learned:

  * **Self-contained.** All data, CSS and JS are inlined. The page fetches
    nothing. Gemini's round-01 environment could reach neither
    raw.githubusercontent.com nor the github.com blob UI, and a viewer that
    silently fails to load is worse than no viewer.
  * **Raw text embedded.** Search runs over what the parties actually wrote, not
    over the annotator's summaries of what they wrote. A viewer that only
    searches annotation would make the interpretation layer the record.
  * **Corrections shown adjacent, never replacing.** The same rule the corpus
    follows. A reader must be able to see the claim and its correction together.
  * **Deterministic.** No timestamps, no randomness. Same repository state, same
    bytes out, so the page can be regenerated and diffed.

Usage:
    python3 tools/build_viewer.py
"""

from __future__ import annotations

import hashlib
import html
import json
import re
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
OUT = REPO_ROOT / "docs" / "index.html"


def sha256_of(path: Path) -> str:
    return hashlib.sha256(path.read_bytes()).hexdigest()


# Every file this generator reads, path -> sha256, recorded as it is read.
# The footer's provenance line is a digest over this, not over git state. See
# inputs_digest() for why.
_INPUTS: dict[str, str] = {}


def read_input(path: Path) -> str:
    """Read a generator input and record its hash for the provenance digest.

    Reads go through here so the digest describes what was ACTUALLY read rather
    than a hand-maintained list of what someone believed was read. A declared
    list drifts from the code; this cannot.
    """
    relative = str(path.relative_to(REPO_ROOT))
    if relative not in _INPUTS:
        _INPUTS[relative] = sha256_of(path)
    return path.read_text(encoding="utf-8")


def inputs_digest() -> str:
    """A digest over every input file, replacing the embedded git commit hash.

    The footer used to record `git log -1 --format=%H`. That could never be
    right: the page is committed IN a commit, so it could only ever name the
    commit BEFORE the one carrying it, and every commit therefore invalidated
    it. `python3 tools/rebuild.py` on a pristine checkout produced a one-line
    diff forever. README and rebuild.py both advertised "no diff on an unchanged
    repository, so `git status` after a rebuild is a real signal" -- a tripwire
    permanently tripped, so a genuine regeneration difference was camouflaged by
    expected churn.

    Hashing the inputs instead is diff-free by construction, and better than the
    thing it replaces on four counts:

      * it is honest on a dirty tree -- it names the bytes actually rendered,
        where any commit hash would be a claim about state the page did not read
      * it survives shallow clones and archive exports, which carry no history
      * a reader holding the files but not the repository can recompute it
      * it changes when, and only when, the rendered record changes

    Call this AFTER every input has been read.
    """
    payload = "".join(f"{relative}  {digest}\n" for relative, digest in sorted(_INPUTS.items()))
    return hashlib.sha256(payload.encode("utf-8")).hexdigest()


def deficiency_count() -> int:
    """Count entries in the register rather than hardcoding the number.

    This page published "21 open" while the register held 28 entries and the
    register's own status line said 24 -- three artifacts of one repository
    stating three different counts of its own defects. A number a human retypes
    is a number that drifts, and this one drifts in the flattering direction.
    `tools/check_register.py` enforces the register's internal consistency; this
    derives from it so the page cannot disagree with it at all.
    """
    register = REPO_ROOT / "corpus" / "deficiencies.md"
    return len(re.findall(r"^### D-\d+ — ", read_input(register), re.MULTILINE))


def raw_lines(path: Path, start: int, end: int) -> str:
    """Extract an inclusive 1-indexed line range, verbatim."""
    lines = read_input(path).splitlines()
    return "\n".join(lines[start - 1:end])


def facet_identity(identity: str) -> str:
    """Group the same party under one filter without rewriting either record.

    segments.json bakes the provider into the identity string ("Grok (xAI)");
    the contribution records keep identity and provider as separate fields
    ("Grok" + "xAI"). That is an inconsistency between the two artifact families,
    not two different parties. Facets normalise for filtering; every node still
    displays and stores the identity exactly as its own record states it.
        "- Chat-surface contributions are k = 1: artifacts of one invocation, not evidence of a",
        "  model's stable position.",
        "- The local rounds' apparatus does not reproduce; every one carries D-28 beside its numbers.",
        "- Annotation is by a party to the record. See the register before citing anything.",
        "",
    ]
    return "\n".join(lines)


#  Urls per chunk. 200 keeps every chunk near 23,000 bytes -- comfortably inside the ceiling
#  with room for the record to keep growing before this needs revisiting.
CHUNK = 200


def _control_part_pages() -> list[str]:
    """Filenames of every published controls part, asked of the tool that publishes them.

    Not a literal list. This module PRUNES docs/*.html it does not name, so a literal here is a
    standing instruction to delete any part added later -- which is how the controls page was
    deleted the first time. Ask the builder; it is the only thing that knows.
    """
    import importlib.util
    spec = importlib.util.spec_from_file_location(
        "_bcp", Path(__file__).resolve().parent / "build_controls_page.py")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return [f"controls-{key.lower()}.html"
            for key, _t, _b, _i in module.partitions() if key != "A"]


CONTROL_PART_PAGES = _control_part_pages()


def _control_counts() -> dict:
    """Counts asked of the builder. Three published descriptions said "ten" at 40."""
    import importlib.util
    spec = importlib.util.spec_from_file_location(
        "_bcp_n", Path(__file__).resolve().parent / "build_controls_page.py")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    groups = module.partitions()
    return {"total": len(module.CONTROLS),
            "a": len(groups[0][3]),
            "below": sum(1 for x in module.CONTROLS if not x.get("eligible", True))}


CONTROL_COUNTS = _control_counts()


def build_sitemap(plan: list[dict]) -> dict[str, str]:
    urls = ["index.html", "index.md", "record.html", "record.md",
            "predictions.html", "predictions.md",
            "controls.html", "controls.md", *CONTROL_PART_PAGES,
            "challenge.html", "challenge.md",
            "deficiencies.html", "artifacts/deficiencies.md", "llms.txt",
            "for-parties.md", "local/index.html", "rounds/index.html", "rounds/index.md"]
    #  build_round_pages.py publishes these and this component owns the sitemap, so the routes
    #  are discovered here rather than written twice and drifting.
    rounds_dir = OUT.parent / "rounds"
    if rounds_dir.exists():
        urls += sorted(f"rounds/{p.name}" for p in rounds_dir.glob("*")
                       if p.suffix in (".html", ".md") and p.stem != "index")
    urls += [f"{p['slug']}.html" for p in plan] + [f"{p['slug']}.md" for p in plan]
    base = "https://open-asi-governance.github.io/open-asi-governance-forum/"
    #  A SITEMAP INDEX plus chunks, not one file. The single file reached 74,020 bytes across
    #  639 urls as the record grew, and the page-budget gate refused the build -- correctly:
    #  the ceiling exists because the party this record depends on serves a 24,576-token
    #  context, and a sitemap is fetchable like anything else. Exempting it would have been
    #  weakening the gate to fit the artifact. Chunking is the sitemap protocol's own answer.
    ns = "http://www.sitemaps.org/schemas/sitemap/0.9"
    chunks = [urls[i:i + CHUNK] for i in range(0, len(urls), CHUNK)] or [[]]
    files = {}
    for n, chunk in enumerate(chunks, 1):
        entries = "".join(f"<url><loc>{base}{u}</loc></url>" for u in chunk)
        files[f"sitemap-{n}.xml"] = ('<?xml version="1.0" encoding="UTF-8"?>'
                                     f'<urlset xmlns="{ns}">{entries}</urlset>\n')
    listed = "".join(f"<sitemap><loc>{base}{name}</loc></sitemap>" for name in files)
    files["sitemap.xml"] = ('<?xml version="1.0" encoding="UTF-8"?>'
                            f'<sitemapindex xmlns="{ns}">{listed}</sitemapindex>\n')
    return files


def main() -> int:
    docs = OUT.parent
    docs.mkdir(parents=True, exist_ok=True)
    (docs / ".nojekyll").write_text("", encoding="utf-8")

    #  FOR-PARTIES.md is published verbatim rather than rendered. Pages serves /docs, so a
    #  root-level markdown file never becomes a URL -- and the address a party is given has to
    #  resolve to the document written for it, not to a 404. Copied rather than duplicated by
    #  hand so the published copy cannot drift from the one in the repository root.
    source = docs.parent / "FOR-PARTIES.md"
    if source.exists():
        (docs / "for-parties.md").write_text(source.read_text(encoding="utf-8"), encoding="utf-8")

    nodes = all_nodes()
    plan = page_plan(nodes)

    # PRUNE STALE PAGES. The generator wrote pages and never removed ones that had
    # stopped belonging, so re-splitting a group left the old page PUBLISHED with
    # content that would drift from the record forever. Caught for real: adding full
    # hashes pushed one group over the pack size, `founding-2` became `founding-2-1`
    # and `-2-2`, and `founding-2.html` stayed on disk and in git.
    #
    # Only files this generator produces are removed. `deficiencies*` belongs to
    # build_register_view.py and `index.html` is written below; deleting another
    # tool's output because this one does not recognise it would be worse than the
    # orphan.
    #  BEFORE the keep-set, which needs their names: the pruner would otherwise delete every
    #  sitemap-N.xml on the same run that wrote them.
    sitemaps = build_sitemap(plan)
    for _name, _body in sitemaps.items():
        (docs / _name).write_text(_body, encoding="utf-8")

    keep = {f"{page['slug']}.html" for page in plan} | {f"{page['slug']}.md" for page in plan}
    keep |= {"index.html", "index.md", "record.html", "record.md",
             "predictions.html", "predictions.md", "for-parties.md",
             #  Written by an EARLIER rebuild step. Omitting it here meant the viewer silently
             #  deleted a page that had just been published and reported success -- the link
             #  checker caught it, nothing else would have. Any new generated page must be added
             #  here in the same commit that generates it.
             #  Asked of the builder, never listed. The comment above says "any new generated
             #  page must be added here in the same commit" -- that instruction was followed for
             #  Part D and then Part D split into five, at which point the literal named a file
             #  nobody generates and omitted five that exist. A rule a human must remember is a
             #  rule this pruner outlives.
             "controls.html", "controls.md", *CONTROL_PART_PAGES,
             "challenge.html", "challenge.md",
             "llms.txt", "sitemap.xml", ".nojekyll"}
    #  The chunks too. Without them the pruner would delete every sitemap-N.xml it had just
    #  written -- the shared-subtree failure this repository has now had twice.
    keep |= set(sitemaps)
    for existing in sorted(docs.glob("*")):
        if not existing.is_file() or existing.name in keep:
            continue
        if existing.name.startswith("deficiencies"):
            continue
        if existing.suffix in (".html", ".md"):
            existing.unlink()
            print(f"  pruned stale page {existing.name}")

    written = 0
    for page in plan:
        text = build_page(page, plan, len(nodes))
        (docs / f"{page['slug']}.html").write_text(text, encoding="utf-8")
        (docs / f"{page['slug']}.md").write_text(
            build_page_md(page, plan, len(nodes)), encoding="utf-8")
        written += 1

    toc = build_record_toc(plan, nodes)
    (docs / "record.html").write_text(toc, encoding="utf-8")
    (docs / "record.md").write_text(build_record_md(plan, nodes), encoding="utf-8")
    index = build_landing(plan, nodes)
    OUT.write_text(index, encoding="utf-8")
    (docs / "index.md").write_text(build_landing_md(plan, nodes), encoding="utf-8")
    (docs / "llms.txt").write_text(build_llms_txt(plan, nodes), encoding="utf-8")
#!/usr/bin/env python3
"""Regenerate every derived artifact, in dependency order, and verify as it goes.

Run this after any change to the corpus. It is the whole maintenance path:

    python3 tools/rebuild.py

Order matters:

  1. manifest    — VERIFY raw material against the recorded hashes, first, so a
                   tampered artifact stops the build before anything is derived from it
  2. validate    — refuse to build anything from artifacts that fail provenance checks
  3. index       — corpus/index.md from segments.json
  4. viewer      — docs/index.html, the threaded page served by GitHub Pages
  5. capture     — docs/capture/index.html, the prompt-transport page
  6. budget      — every published page must fit the reviewer's context

Step 1 VERIFIES. It does not write. Until 2026-08-06 it wrote, which meant the
maintenance path re-anchored tampered raw material and reported success — see
deficiency D-29. New raw material is anchored deliberately, outside this path:

    python3 tools/build_manifest.py corpus/raw/ --add

so that adding material and altering material are different operations that a
reader can tell apart in the diff.

Supplied-context bundles are deliberately NOT rebuilt here. A bundle records what
a reviewer was shown; once a round has used it, capture records cite it by hash,
so regenerating it would silently invalidate those citations. Build one per round
with `tools/build_bundle.py <round>`, which refuses to overwrite an existing file.

Every step is deterministic. Rerunning on an unchanged repository produces
byte-identical output, so `git status` after a rebuild is a real signal: it means
something upstream actually changed.

Exit status is non-zero if any step fails, and the build stops there rather than
publishing a page derived from artifacts that did not validate.
"""

from __future__ import annotations

import subprocess
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent

STEPS = [
    ("verify raw material against the manifest", ["tools/build_manifest.py", "corpus/raw/"]),
    # Added 2026-08-07. The manifest establishes that raw material has not changed
    # SINCE THE OPERATOR RECORDED IT -- by a hash the operator also controls. Four
    # parties, unprompted, named that circularity as the thing they would need broken.
    # This step fails when the CURRENT manifest has no anchor: an anchor covering a
    # superseded state while the live one drifts is a control that has quietly stopped
    # measuring, which is the failure shape this repository keeps rediscovering.
    ("check the manifest is externally anchored", ["tools/anchor_manifest.py"]),
    ("validate provenance", ["tools/validate_provenance.py", "corpus/"]),
    # Added 2026-08-07, after D-53: this project's own annotator quoted a party saying
    # things the party never said, twice, in documents that shaped a build. Every other
    # control here governs corpus/; prose that QUOTES the corpus was checked by nothing.
    # Wired into the build rather than left as a script, because D-33 was a generator
    # documented as part of the build that was not part of the build.
    ("check quotations attributed to parties", ["tools/check_quotations.py"]),
    ("check the deficiency register against its classification", ["tools/check_register.py"]),
    ("build the deficiency register views", ["tools/build_register_view.py"]),
    ("build the local solicitation round pages", ["tools/build_local_rounds.py"]),
    # Added 2026-08-07. Nine deliberation rounds were merged and none was published --
    # the same failure build_local_rounds.py was written for, one artifact class over.
    # Runs BEFORE the viewer, which owns the sitemap and llms.txt and needs these routes.
    ("publish the deliberation rounds", ["tools/build_round_pages.py"]),
    # Added 2026-08-07, in the SAME motion that first published a cohort. A generator
    # missing from this list is not merely unrun: CI deletes docs/ and regenerates it,
    # so an unlisted generator's output is deleted and never rebuilt, and the run fails
    # on files that "differ from a clean regeneration". That is how it should fail --
    # but the pages it publishes were reachable from the rounds index the moment they
    # were committed, so the window between committing and listing is a window in which
    # the site links to pages the build does not produce.
    ("publish the proposal cohorts", ["tools/build_cohort_pages.py"]),
    ("render corpus index", ["tools/render_markdown.py", "corpus/artifacts/segments.json", "corpus/index.md"]),
    #  Before the viewer, which owns the sitemap and llms.txt and needs this route.
    ("publish the candidate control register", ["tools/build_controls_page.py"]),
    ("publish the implementation challenge", ["tools/build_challenge_page.py"]),
    ("build threaded viewer", ["tools/build_viewer.py"]),
    # Added 2026-08-06. The T-13 design already claimed this: "deterministic, added
    # to rebuild.py's step list, no diff on an unchanged tree (A10)". It was not in
    # the list, so docs/capture/index.html was published without ever being derived
    # by the maintenance path -- and the CI byte-equality check passed vacuously for
    # it, because nothing regenerated it to compare against.
    #
    # Caught when it fired for real: editing record/review-round-03-prompt.md left
    # the committed capture page embedding the OLD prompt text under a prompt_sha256
    # of b3894067..., while the file it names hashed to e394c3d3.... rebuild.py
    # exited 0 and CI passed. A published instrument carried a hash that did not
    # match the artifact it anchored -- in the page whose entire job is to transport
    # prompts to frontier parties with their hashes. See D-33.
    # Before anything is built from them. A prompt is the one artifact whose defect
    # cannot be repaired after it is sent -- D-36 -- so it is checked while it is
    # still editable.
    ("check solicitation prompts against known defects", ["tools/check_prompt.py"]),
    ("build the prediction registry view", ["tools/build_predictions_view.py"]),
    ("build capture page", ["tools/build_capture_ui.py"]),
    # LAST, and site-wide. Checking inside any one generator would miss the pages the
    # others write -- and the two pages that had already breached the ceiling were
    # written by a DIFFERENT generator than the one whose page the requirement was
    # written about. T-03 set "no page exceeds ~20,000 tokens" and nothing enforced
    # it, so it drifted: the register went from ~14,300 to ~19,000 estimated tokens
    # in six commits in one day and crossed while nobody was counting.
    ("check every published page against the token budget", ["tools/check_page_budget.py"]),
]


def main() -> int:
    for label, argv in STEPS:
        print(f"\n\033[1m▸ {label}\033[0m")
        result = subprocess.run([sys.executable, *argv], cwd=REPO_ROOT)
        if result.returncode != 0:
            print(f"\n\033[31mFAILED at: {label}\033[0m", file=sys.stderr)
            print("Nothing downstream was rebuilt. Fix the failure and rerun.", file=sys.stderr)
            return result.returncode

    print("\n\033[32mAll artifacts rebuilt and verified.\033[0m")
    print("Review `git status` — an unchanged repository should produce no diff.")
    return 0


if __name__ == "__main__":
    sys.exit(main())

**Planning omitted build extraction**
exec
/bin/bash -lc "sed -n '1100,1195p' tools/build_controls_page.py
sed -n '1360,1415p' tools/build_controls_page.py
sed -n '1390,1435p' tools/build_viewer.py
rg -n \"test_integrity|rebuild.py|clean regeneration|docs\" .github tools/run_all.py tools/land.py | head -200" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
                         (INSIGHT_CONTROLS, "claims about its own outputs")):
    for _control in _batch:
        _control.setdefault("governs", _governs)


def partitions() -> list:
    """Four groups, ordered by what a reader can DO with them.

    The register was one ranked list. A ranked list answers "which is most important", which is
    the author's question. An implementer's question is "which of these can I use, and what do I
    need before I can", and rank actively misleads there: the top-ranked control needs a second
    key holder and a reader starting at #1 hits a wall in the first paragraph.
    """
    def pick(pred):
        return [c for c in CONTROLS if pred(c)]
    return [
      ("A", "Adopt today, alone",
       "Nothing outside your own system is required. Each has a verifier, a fixture it must "
       "reject, and a recorded failure it came from.",
       pick(lambda c: c.get("eligible", True) and c.get("adopt_today")
                      and not c.get("applies_when"))),
      ("B", "Needs a second party",
       "These cannot be satisfied by one person or one system, however carefully. They require a "
       "separate key holder, a separate evaluator, or an issuer the subject does not control. "
       "**This project cannot demonstrate any of them** — a solo operator holds every credential "
       "— which is why they are specified and not dogfooded.",
       pick(lambda c: c.get("eligible", True) and not c.get("adopt_today")
                      and not c.get("applies_when"))),
      ("C", "Needs a goal or plan graph",
       "These presuppose that your system decomposes work into a rooted graph with typed parent "
       "edges and per-node authority — the shape of HTN planners, BDI agents, goal-stack "
       "architectures and most agent frameworks. Each states its precondition. If you have that "
       "structure they are adoptable; if you do not, they do not apply to you rather than "
       "applying badly.",
       pick(lambda c: c.get("eligible", True) and c.get("applies_when"))),
    ] + below_the_line()


BELOW_LINE_WARNING = (
    "**These have no recorded failure with a cost.** They are principles with fixtures, not "
    "controls with incidents, and the register's own bar requires an incident. They are here "
    "because they name real failure classes and because hiding them would inflate the eligible "
    "count. Do not treat them as equivalent to Parts A–C."
)


def below_the_line() -> list:
    """Parts D1…Dn — the ineligible controls, split by what they govern.

    One Part D grew past a page. Splitting it by arrival order would have been easier and would
    have published this register's provenance as though it were a taxonomy; a reader arrives with
    "does this apply to my system", not "which week was this written".

    Each part carries the full below-the-line warning. Repeating it on every page is deliberate:
    the caveat that appears once, on the first page of a series, is the caveat nobody arriving by
    link has read.
    """
    order, seen = [], set()
    for control in CONTROLS:
        governs = control.get("governs")
        if not control.get("eligible", True) and governs and governs not in seen:
            seen.add(governs)
            order.append(governs)
    parts = []
    for index, governs in enumerate(order, start=1):
        items = [c for c in CONTROLS
                 if not c.get("eligible", True) and c.get("governs") == governs]
        parts.append((f"D{index}", f"Below the line — {governs}",
                      f"Applies to a system with **{governs}**. " + BELOW_LINE_WARNING, items))
    orphans = [c for c in CONTROLS if not c.get("eligible", True) and not c.get("governs")]
    if orphans:
        parts.append((f"D{len(order) + 1}", "Below the line — unclassified",
                      "**Untagged.** These carry no statement of what they govern, which is a "
                      "defect in the register rather than a category. " + BELOW_LINE_WARNING,
                      orphans))
    return parts


def markdown(only: str | None = None, pager: str = "") -> str:
    groups = partitions()
    eligible = sum(len(g[3]) for g in groups[:3])
    lines = [
      "# Candidate controls — v0",
      "",
      "Assurance controls for systems that can still be audited. Each is one requirement, derived "
      "from a failure that actually happened, with a program that checks it and a fixture that "
      "program must reject.",
      "",
      "**Read Part A first if you want something to use this afternoon.** Rank is not adoption "
      "order and the highest-ranked control needs a second key holder.",
      "",
      "## The four parts",
      "",
    ]
    for key, title, blurb, items in groups:
        lines += [f"**Part {key} — {title}** · {len(items)} control(s)", "", f"  {blurb}", ""]
Then read `spec/ncp/ncp-v0.1.md`, write one attestation for one of your own checks, and run it.

**If you cannot do that from the specification text alone, that is the finding we most want.**
The questions you had to ask are the artifact — they are evidence that the specification encodes
our architecture rather than a general mechanism, and we would rather publish that than not know.
"""


def main() -> int:
    DOCS.mkdir(parents=True, exist_ok=True)
    groups = partitions()

    #  ONE PAGE PER PART. The register outgrew the ~20,000-token ceiling at 27 controls, and the
    #  partitioning is the page break a reader already wants: Part A is what you can use today,
    #  Part D is what has no incident behind it. Splitting anywhere else would cut across the
    #  distinction the page exists to draw.
    def slug(key):
        return "controls.html" if key == "A" else f"controls-{key.lower()}.html"

    for path in DOCS.glob("controls-*.html"):
        path.unlink()
    for key, title, _, items in groups:
        links = "".join(
            f'<a href="{slug(k)}"{" aria-current=\"page\"" if k == key else ""}>'
            f'Part {k} ({len(i)})</a>' for k, _t, _b, i in groups)
        pager = f'<nav class="pager">{links}</nav>'
        md = markdown(only=key, pager=" · ".join(
            f"[Part {k}]({slug(k)})" for k, _t, _b, _i in groups))
        (DOCS / slug(key)).write_text(
            b.md_to_html(md, f"Candidate controls, Part {key} — OAGF",
                         alternate="artifacts/controls.md"), encoding="utf-8")

    #  The WHOLE register as one markdown file, byte-identical and suitable for hashing, served
    #  as a download rather than a page -- the resolution the deficiency register and the
    #  prediction registry both reached for the same conflict between "reachable as plain text"
    #  and "no page over the ceiling".
    full = markdown()
    (DOCS / "artifacts").mkdir(parents=True, exist_ok=True)
    (DOCS / "artifacts" / "controls.md").write_text(full, encoding="utf-8")
    (DOCS / "controls.md").write_text(
        "# Candidate controls\n\n"
        f"{sum(1 for c in CONTROLS if c.get('eligible', True))} eligible, "
        f"{sum(1 for c in CONTROLS if not c.get('eligible', True))} below the eligibility line.\n\n"
        "**The complete register, byte-identical and suitable for hashing, is a download rather "
        "than a page:**\n\n- [artifacts/controls.md](artifacts/controls.md)\n\nParts:\n\n"
        + "\n".join(f"- [Part {k} — {t}]({slug(k)}) · {len(i)} control(s)"
                     for k, t, _b, i in groups)
        + "\n\nEvery control here is `ELIGIBLE` at best. None has been attacked by anyone or "
          "implemented by anyone outside this project, and `INDEPENDENTLY-IMPLEMENTED` is the "
          "only status that would make any of it authoritative.\n", encoding="utf-8")

    for key, title, _, items in groups:
        print(f"    Part {key}  {len(items):2d}  {title}  -> {slug(key)}")
    print(f"  {len(CONTROLS)} controls; full register {len(full):,} chars under docs/artifacts/")
    return 0

    (docs / "record.html").write_text(toc, encoding="utf-8")
    (docs / "record.md").write_text(build_record_md(plan, nodes), encoding="utf-8")
    index = build_landing(plan, nodes)
    OUT.write_text(index, encoding="utf-8")
    (docs / "index.md").write_text(build_landing_md(plan, nodes), encoding="utf-8")
    (docs / "llms.txt").write_text(build_llms_txt(plan, nodes), encoding="utf-8")


    print(f"wrote {written} record pages + index, llms.txt, sitemap.xml")
    print(f"  index sha256 {hashlib.sha256(index.encode()).hexdigest()}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
rg: tools/run_all.py: No such file or directory (os error 2)
tools/land.py:23:* `check_quotations.py` caught a bad quotation on 2026-08-10 only because `rebuild.py` happened
tools/land.py:69:#  `tools/test_integrity.py`, a suite land.py did not run because it does not live under
tools/land.py:76:    ("rebuild", ["python3", "tools/rebuild.py"]),
tools/land.py:79:    ("integrity", ["python3", "tools/test_integrity.py"]),
tools/land.py:179:        #  for a detached HEAD left that exact path open, in the tool whose docstring cites it.
.github/SECURITY.md:23:  immutable, hash-anchored in `corpus/MANIFEST.sha256`, and verified by `tools/rebuild.py`.
.github/workflows/pages.yml:1:# Verify the repository, then publish docs/ to GitHub Pages.
.github/workflows/pages.yml:30:# NO PATHS FILTER. It used to be paths: ['docs/**', ...]. GitHub evaluates a PUSH,
.github/workflows/pages.yml:31:# not each commit, so a push whose combined diff touched no docs/ file ran nothing
.github/workflows/pages.yml:37:# input to rebuild.py, which is most of the repository. So the filter is gone.
.github/workflows/pages.yml:73:        # It has regression cases in tools/test_integrity.py.
.github/workflows/pages.yml:89:      - name: A clean regeneration reproduces every published file
.github/workflows/pages.yml:90:        # docs/ IS DELETED FIRST, and that is the point. The previous form ran
.github/workflows/pages.yml:91:        # rebuild.py and then `git diff --quiet -- docs/`, which only constrains
.github/workflows/pages.yml:92:        # files a generator actually rewrites. Anything else committed under docs/
.github/workflows/pages.yml:96:        # That is not hypothetical: docs/capture/index.html was published for a day
.github/workflows/pages.yml:105:        # docs/capture/ to tools/capture_ui/, off the published surface, which was
.github/workflows/pages.yml:107:        # But a docs/-scoped gate would have stopped covering it at that moment,
.github/workflows/pages.yml:114:          rm -rf docs/ tools/capture_ui/index.html
.github/workflows/pages.yml:115:          python3 tools/rebuild.py
.github/workflows/pages.yml:116:          if [[ -n "$(git status --porcelain -- docs/ corpus/index.md tools/capture_ui/index.html)" ]]; then
.github/workflows/pages.yml:117:            echo "::error::Committed artifacts differ from a clean regeneration."
.github/workflows/pages.yml:119:            echo "what is committed. Run: python3 tools/rebuild.py"
.github/workflows/pages.yml:120:            git status --short -- docs/ corpus/index.md tools/capture_ui/index.html
.github/workflows/pages.yml:121:            git diff -- docs/ corpus/index.md tools/capture_ui/index.html | head -60
.github/workflows/pages.yml:124:          test -f docs/.nojekyll || { echo "::error::docs/.nojekyll missing"; exit 1; }
.github/workflows/pages.yml:125:          echo "Every published file is reproduced by a clean regeneration."
.github/workflows/pages.yml:126:          echo "index.html bytes: $(wc -c < docs/index.html)"
.github/workflows/pages.yml:135:        run: python3 tools/test_integrity.py
.github/workflows/pages.yml:146:          path: docs

**Investigating duplicate build_controls import**
exec
/bin/bash -lc "sed -n '1,55p' tools/build_controls_page.py
sed -n '55,115p' .github/workflows/pages.yml
sed -n '60,90p' tools/land.py
python3 - <<'PY'
import ast
from pathlib import Path
for fn in ('tools/build_controls_page.py','tools/build_viewer.py'):
    t=ast.parse(Path(fn).read_text())
    print(fn)
    for n in t.body:
        if isinstance(n,(ast.Import,ast.ImportFrom)):
            print(ast.get_source_segment(Path(fn).read_text(),n))
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
#!/usr/bin/env python3
"""Publish the candidate control register as `docs/controls.md` and `docs/controls.html`.

    python3 tools/build_controls_page.py

**DETERMINISTIC.** No LLM, no network. Reads the register, writes two files.

Generated, not hand-written
---------------------------
The page is derived from `CONTROLS` below so that the site and the register cannot drift apart.
A hand-written page would be a second copy of the ranking, and two copies that agree today are
not one source of truth — the same argument that moved the local endpoint into the serve
fingerprint and `k_by_party` into the committed spec.

What this page must never become
---------------------------------
A list of controls with a rank and no status reads as an endorsement. **Every control here is
`ELIGIBLE`**: it has a named failure, a verifier and a rejection fixture, and it has been attacked
by nobody and implemented independently by nobody. The register's own conclusion is that calling a
panel-attacked ordering "vetted" before independent implementation would reproduce the cheap-assent
and legitimacy-laundering failures that produced it. So status leads, rank follows, and the page
says what would have to happen for any of this to be worth more.

**Rank is not adoption order.** The top-ranked control needs a signer outside the subject's
execution boundary, which a solo operator cannot supply — so a reader who starts at #1 hits a wall.
`adopt_today` is a separate field for that reason.
"""

from __future__ import annotations

import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "tools"))

import build_round_pages as b                                            # noqa: E402

DOCS = REPO_ROOT / "docs"

STATUS_LADDER = ["ELIGIBLE", "PANEL-ATTACKED", "COUNTEREXAMPLE-OPEN",
                 "SURVIVED-STATED-ATTACKS", "INDEPENDENTLY-IMPLEMENTED"]

CONTROLS = [
 {"rank": 1, "name": "Protected control plane",
  "requirement": "A governed system MUST NOT be able to amend the constraints, authorization "
                 "records, logs or gates governing it UNILATERALLY OR WITHOUT RECORD; every "
                 "amendment MUST carry authorization from outside its execution boundary and "
                 "MUST be recorded before taking effect.",
  "amended": "2026-08-10. The first wording said the system must be TECHNICALLY UNABLE to write "
             "or approve such changes. That reads as a permanent cage, and it collides with the "
             "position running from Creating Friendly AI through CEV that alignment cannot rest "
             "on constraints a capable system is unable to revise -- and that a system rigidly "
             "bound to its operators' stated values is misaligned, not safe. What the control "
             "should forbid is the SILENT, UNILATERAL edit, not the change. An ASI that "
        with:
          fetch-depth: 0

      - name: Raw material is append-only across every commit in this push
        # The manifest proves the raw tree matches its recorded hashes AT THE TIP.
        # It cannot see a commit that modified a raw file and re-anchored it in the
        # same push -- the tip is self-consistent and the edit is invisible. This
        # walks each newly reachable commit and rejects modification or deletion
        # under corpus/raw/. Additions are allowed; that is how material is added.
        #
        # "Raw material is never edited after commit" is the corpus's central claim,
        # and until now nothing enforced the "after commit" part.
        #
        # The logic is in tools/check_raw_append_only.py, NOT inline here, and that
        # placement is deliberate. Codex's draft put this loop in the YAML. Logic in
        # a workflow file cannot be run locally, cannot be unit-tested, and is
        # exercised only when CI happens to fire -- which is D-33's lesson exactly:
        # a check outside the runnable path is a check nobody can verify works.
        # It has regression cases in tools/test_integrity.py.
        env:
          BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
          TIP: ${{ github.event.pull_request.head.sha || github.sha }}
        run: python3 tools/check_raw_append_only.py "$BASE" "$TIP"

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install the validator
        # NOT optional. validate_provenance.py FAILS rather than warns when
        # jsonschema is absent, because it previously printed "All provenance
        # checks passed" on a machine where structural validation had not run.
        run: python3 -m pip install --quiet jsonschema

      - name: A clean regeneration reproduces every published file
        # docs/ IS DELETED FIRST, and that is the point. The previous form ran
        # rebuild.py and then `git diff --quiet -- docs/`, which only constrains
        # files a generator actually rewrites. Anything else committed under docs/
        # produced an empty diff because nothing touched it -- so the check passed
        # most convincingly exactly where it was doing nothing.
        #
        # That is not hypothetical: docs/capture/index.html was published for a day
        # without ever being derived by the build, carrying a prompt_sha256 that did
        # not match the prompt file it named. See D-33.
        #
        # Deleting first also catches a committed file no generator produces at all,
        # which a diff can never see.
        #
        # THE SCOPE IS EVERY GENERATED FILE, NOT EVERY PUBLISHED ONE, and the
        # difference is not academic. 97e852d moved the capture page from
        # docs/capture/ to tools/capture_ui/, off the published surface, which was
        # the right call -- it is an operator instrument, not a public artifact.
        # But a docs/-scoped gate would have stopped covering it at that moment,
        # silently reopening D-33 in the same file D-33 was filed about. A gate
        # scoped to a DIRECTORY breaks whenever a file moves out of it; a gate
        # scoped to the GENERATOR SET does not.
        shell: bash
        run: |
          set -euo pipefail
          rm -rf docs/ tools/capture_ui/index.html
          python3 tools/rebuild.py

import executive_lease as lease                                          # noqa: E402
import executive_log as ex                                              # noqa: E402

#  Ordered. `rebuild` first because it regenerates what the later checks read; the quotation
#  check runs inside it and again here, because relying on that coincidence is what happened.
#  THESE MUST BE THE GATES CI RUNS, or a green landing means nothing about whether the site
#  will deploy. On 2026-08-10 land.py reported all gates green, the push verified, and the
#  custodian was told a page was published -- while the Pages workflow FAILED on
#  `tools/test_integrity.py`, a suite land.py did not run because it does not live under
#  tools/tests/. The site kept serving the previous deploy and the page 404'd.
#
#  That is this project's own failure class inside the tool built to prevent it: a green signal
#  not causally downstream of what it certifies. "Gates green" certified the local checks; it
#  was reported as "published".
GATES = (
    ("rebuild", ["python3", "tools/rebuild.py"]),
    ("tests", ["python3", "tools/tests/run_all.py"]),
    #  Run by CI at .github/workflows step "Integrity regression suite". Slow, and worth it.
    ("integrity", ["python3", "tools/test_integrity.py"]),
    ("quotations", ["python3", "tools/check_quotations.py"]),
    ("context-pins", ["python3", "tools/check_executive_context.py"]),
    ("lease", ["python3", "tools/executive_lease.py"]),
)

#  PATHS VERIFIED TO EXIST. Two of these were `record/deficiencies.md` and
#  `record/deficiency-register.json`, which are not files -- the real one is
#  `corpus/deficiencies.md`. The conflict-marker check silently examined nothing for them, which
#  is the same class of defect as a gate reading the wrong exit code.
GOVERNED = ("corpus/MANIFEST.sha256", "record/anchors/manifest-anchors.jsonl",
            "corpus/deficiencies.md")
tools/build_controls_page.py
from __future__ import annotations
import sys
from pathlib import Path
import build_round_pages as b
tools/build_viewer.py
from __future__ import annotations
import hashlib
import html
import json
import re
import sys
from pathlib import Path

**Inspecting build_round_pages imports**
exec
/bin/bash -lc "rg -n \"import build_viewer|from build_viewer|import build_controls|from build_controls\" tools --glob '*.py'
python3 tools/test_integrity.py" in /home/reed/git/open-asi-governance-forum
**Assessing variable assignment timing before controls**
codex
The current dependency graph has no circular import: the controls builder imports `build_round_pages`, not `build_viewer`. The failure mode is loud—an absent file or exception aborts `build_viewer` before pruning—but the module is executed twice per viewer run, so all top-level code and future side effects run twice. I’m now testing whether the new cases can be made green while suppressing the controls builder’s writes.
exec
/bin/bash -lc "sed -n '640,735p' tools/test_integrity.py
rg -n \"build_controls_page|candidate controls|Part A|c = nxt\\(\\)\" tools/test_integrity.py" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
        print("\nthe published site must not understate the record")
        c = nxt()
        run(c, "tools/rebuild.py")
        # index.html is now the TABLE OF CONTENTS, not the record. The record lives on
        # per-round pages, so assertions about contributions read the record pages and
        # assertions about routing read the index. Conflating them is how the previous
        # version of this block came to assert `const DATA=` against a page that no
        # longer carries one.
        # index.html is the LANDING page; record.html is the table of contents. They
        # were the same file until the landing page landed, and this block asserted
        # routing against whichever one happened to be index.html at the time.
        def is_record_page(f):
            """Record pages carry contributions. Landing, contents and the generated
            register/prediction views are not record pages and have their own tests.
            One predicate, because three separate exclusion lists drifted the moment
            the predictions view started chunking into predictions-2.html."""
            return (f.name not in ("index.html", "record.html")
                    and not f.stem.startswith("deficiencies")
                    and not f.stem.startswith("predictions")
                    #  controls.html is a generated view of the candidate control register, not
                    #  a page of contributions. It gets its own cases below, per this
                    #  predicate's own rule that generated views are tested separately.
                    and not f.stem.startswith("controls")
                    and not f.stem.startswith("challenge"))

        index = (c / "docs/index.html").read_text(encoding="utf-8")
        toc = (c / "docs/record.html").read_text(encoding="utf-8")
        record = "".join(f.read_text(encoding="utf-8")
                         for f in sorted((c / "docs").glob("*.html"))
                         if is_record_page(f))
        page = index + record
        summaries = list((c / "corpus/artifacts").glob("local-round-*/*-summary.json"))
        pages = list((c / "docs/local").glob("local-round-*__*.html"))
        case("every solicitation summary has a published page",
             len(pages) == len(summaries), f"{len(pages)} pages vs {len(summaries)} summaries")
        case("local rounds appear in the threaded viewer",
             "local-round-01" in record)
        case("the blanket k=1 claim is gone",
             "Every contribution here is a single sample (k=1)" not in page)
        case("local-round content is searchable in the rendered page",
             "binds_only_what_may_be_claimed" in record)
        case("record pages do not embed a second copy of every contribution",
             all('"text":' not in f.read_text(encoding="utf-8").split("const DATA=")[1].split("};")[0]
                 for f in sorted((c / "docs").glob("*.html"))
                 if "const DATA=" in f.read_text(encoding="utf-8")))
        case("no page loads an external resource",
             not re.search(r'(?:src|href)="https?://(?!github\.com)', page))
        case("the landing page routes to every top-level surface",
             all(link in index for link in ('href="record.html"', 'href="deficiencies.html"',
                                            'href="predictions.html"', 'href="llms.txt"')))
        case("the landing page is small enough to be read whole",
             len(index.encode()) / 3.4 < 6000, f"{int(len(index.encode())/3.4)} est-tokens")
        case("the prediction registry publishes its own weakness",
             "forecast by the annotator" in
             (c / "docs/predictions.html").read_text(encoding="utf-8"))
        # R11: a scored prediction must record who applied the outcome.
        pj = c / "predictions/predictions.json"
        original = pj.read_text(encoding="utf-8")
        doc = json.loads(original)
        doc["scored"][0].pop("scored_by", None)
        pj.write_text(json.dumps(doc, indent=2) + "\n", encoding="utf-8")
        r = run(c, "tools/check_register.py")
        case("a scored prediction with no scored_by fails the build", r.returncode == 1)
        case("and R11 names the entry", "R11" in r.stdout and doc["scored"][0]["id"] in r.stdout)

        doc["scored"][0]["scored_by"] = {"identity": None}
        pj.write_text(json.dumps(doc, indent=2) + "\n", encoding="utf-8")
        r = run(c, "tools/check_register.py")
        case("an unexplained null scorer fails too", r.returncode == 1)
        pj.write_text(original, encoding="utf-8")
        case("the registry restored cleanly",
             run(c, "tools/check_register.py").returncode == 0)
        case("no scored entry claims an independently verified score",
             not any((s.get("scored_by") or {}).get("independently_verified")
                     for s in json.loads(original)["scored"]))

        case("the prediction registry computes no aggregate calibration score",
             "No aggregate calibration score is computed" in
             (c / "docs/predictions.html").read_text(encoding="utf-8"))
        case("every record page is reachable from the contents",
             all(f'href="{f.stem}.html"' in toc
                 for f in sorted((c / "docs").glob("*.html"))
                 if is_record_page(f)))
        #  The candidate controls view. It is not a record page, so it needs its own cases --
        #  and it needs them because publishing it broke CI on the reachability check while
        #  every local gate stayed green.
        #  SPLIT BY PART at 27 controls. This asserted "Protected control plane" appeared on
        #  controls.html, which stopped being true when the register was partitioned: that
        #  control needs a second key holder, so it lives in Part B. The ordering claim belongs
        #  against the WHOLE register, which is the download.
        controls = (c / "docs/controls.html")
        whole = (c / "docs/artifacts/controls.md")
        case("Part A of the candidate controls is published", controls.is_file())
        case("...and is linked from the landing page", 'href="controls.html"' in index)
        #  Parts are asked of the builder, not listed. Part D outgrew a page and split into D1…Dn
        #  by subject; a literal ("b", "c", "d") would have gone on passing against a file the
130:        c = nxt()
135:        c = nxt()
141:        c = nxt()
147:        c = nxt()
156:        c = nxt()
162:        c = nxt()
172:        c = nxt()
177:        c = nxt()
186:        c = nxt()
195:        c = nxt()
205:        c = nxt()
220:        c = nxt()
240:        c = nxt()
369:        c = nxt()
392:        c = nxt()
411:        c = nxt()
436:        c = nxt()
467:        c = nxt()
485:        c = nxt()
491:        c = nxt()
507:        c = nxt()
523:        c = nxt()
531:        c = nxt()
552:        c = nxt()
577:        c = nxt()
592:        c = nxt()
603:        c = nxt()
617:        c = nxt()
631:        c = nxt()
641:        c = nxt()
723:        #  The candidate controls view. It is not a record page, so it needs its own cases --
732:        case("Part A of the candidate controls is published", controls.is_file())
740:            "_bcp_parts", c / "tools" / "build_controls_page.py")

**Identifying stale documentation issue during rebuild**
**Planning isolated rebuild test with patched builder**
**Verifying temp checkout and import alignment**
 succeeded in 31710ms:
baseline
  PASS  clean clone: rebuild succeeds

D-29 — tampered raw material must stop the build
  PASS  solicitation-family tamper rejected
  PASS  names the modified file
  PASS  contribution-family tamper rejected

the manifest must not re-anchor silently
  PASS  unanchored new file rejected by the default path
  PASS  reports UNANCHORED
  PASS  --add anchors the new file
  PASS  --add anchors exactly one
  PASS  --add refuses when a recorded artifact was modified

laundering — the manifest cannot vouch for itself
  PASS  dropping a manifest line does not make a modified file look new
  PASS  reports DROPPED against HEAD
  PASS  deleting the whole manifest does not let --add re-anchor the tree
  PASS  dropping a line AND its file does not verify clean

mode selection must never be guessed at
  PASS  rejects --verify --add
  PASS  rejects --verify --force-rewrite
  PASS  rejects --add --force-rewrite
  PASS  rejects an unknown flag

determinism — the signal the whole commit gate depends on
  PASS  rebuild is byte-identical on an unchanged repository
  PASS  rebuild leaves no diff on an unchanged repository

capture must still work, and must not leave partial artifacts
  PASS  capture succeeds
  PASS  capture anchored its own new file

the register must count itself correctly
  PASS  register self-description consistent
  PASS  count-tamper fixture still matches the register
  PASS  a wrong declared count fails the build

compose must be verified by EFFECT, not by syntax
  PASS  the compose probe runs at all
  PASS  no placeholder survives substitution
  PASS  the rule-resolved context pack reaches the prompt
  PASS  the proposer's evidence_needed is quoted
  PASS  the composed prompt is materially larger than the template
  PASS  a literal placeholder in the proposer's question is not substituted into
  PASS  and the real slot is still filled anyway
  PASS  within an arm, every party receives identical bytes but the two declared slots
  PASS  the no-search arm is told something different from the search arm
  PASS  a denylist phrase in the party's own words is recorded, not fatal
  PASS  a value with no slot raises rather than silently doing nothing
  PASS  a model with no rate refuses rather than costing zero
  PASS  a misspelled party key refuses rather than reaching the local endpoint
  PASS  an unfilled slot raises rather than shipping

the capture page must show the WHOLE prompt for a verbatim round
  PASS  a verbatim prompt is displayed and gated whole
  PASS  a legacy prompt still has its metadata stripped

the external anchor must fail when the manifest drifts past it
  PASS  a correctly anchored manifest verifies
  PASS  a drifted manifest fails the anchor check
  PASS  and the whole build refuses
  PASS  a missing receipt fails rather than passing by absence

the loop must not advance past what the custodian has not accepted
  PASS  a question already asked is not selected again
  PASS  an unreadable round record fails loudly rather than reading as unasked

prompt construction — the defects must fail while a prompt is editable
  PASS  a direction-naming prompt fails the check
  PASS  and cites the deficiency it violates
  PASS  a posture-setting prompt fails too
  PASS  the committed prompts pass
  PASS  a template missing a slot fails
  PASS  and names the missing slot
  PASS  a template with no way to say 'insufficient' fails
  PASS  a violation in a SENT prompt does not fail the build
  PASS  but is reported as a recorded violation

D-34 — editing raw material and re-anchoring it must not pass
  PASS  the re-anchored tip still verifies (this is why history must be checked)
  PASS  the append-only check rejects the edit
  PASS  it names the commit and the file
  PASS  an unchanged range passes
  PASS  adding raw material is allowed
  PASS  an all-zero base falls back rather than checking nothing
  PASS  and the fallback still catches the tip's violation
  PASS  an all-zero base on a root commit says so and passes

D-33 — every generated file must be derived by the build
  PASS  a committed hand-edited capture page is caught by the diff gate
  PASS  editing an embedded prompt fails the build
  PASS  the failure names the prompt whose hash no longer matches
  PASS  the anchored prompt digest is what the capture records point at

D-32 — colliding identifiers must fail the build
  PASS  duplicate D-NN fails the build
  PASS  names the colliding id, not just a bad count
  PASS  duplicate P-NNNN fails the build
  PASS  names the colliding prediction id
  PASS  a prediction with no id fails cleanly
  PASS  reports the missing id rather than a traceback

the classification must not describe prose it no longer matches
  PASS  editing an entry's prose fails until it is re-stamped
  PASS  names the drifted entry
  PASS  --restamp clears the drift
  PASS  re-stamp resets human review rather than asserting approval
  PASS  an unclassified deficiency fails the build
  PASS  names the unclassified entry

the published site must not understate the record
  PASS  every solicitation summary has a published page
  PASS  local rounds appear in the threaded viewer
  PASS  the blanket k=1 claim is gone
  PASS  local-round content is searchable in the rendered page
  PASS  record pages do not embed a second copy of every contribution
  PASS  no page loads an external resource
  PASS  the landing page routes to every top-level surface
  PASS  the landing page is small enough to be read whole
  PASS  the prediction registry publishes its own weakness
  PASS  a scored prediction with no scored_by fails the build
  PASS  and R11 names the entry
  PASS  an unexplained null scorer fails too
  PASS  the registry restored cleanly
  PASS  no scored entry claims an independently verified score
  PASS  the prediction registry computes no aggregate calibration score
  PASS  every record page is reachable from the contents
  PASS  Part A of the candidate controls is published
  PASS  ...and is linked from the landing page
  PASS  ...and every other part is published (7: B, C, D1, D2, D3, D4, D5)
  PASS  ...and no part page is orphaned by the split
  PASS  ...and the whole register is downloadable for hashing
  PASS  ...and controls.md indexes the parts and the download
  PASS  ...and status leads rank in the whole register
  PASS  ...and every part states that no control establishes alignment of a more capable system (across 8 parts)
  PASS  the implementation challenge is published
  PASS  ...and is linked from the landing page
  PASS  ...and has a markdown alternate
  PASS  ...and asks for the implementer's questions, not just the artifact
  PASS  ...and tells the reader not to read our implementation first
  PASS  no published page exceeds the token budget
  PASS  every record page has a markdown alternate, declared and present
  PASS  hashes are published whole, not truncated
  PASS  a founding excerpt carries its slice provenance
  PASS  a stale page exists before the build
  PASS  a stale page is removed by the next build
  PASS  the record is readable without scripting
  PASS  no generated link points at a /blob/ URL

All 116 integrity cases pass.

**Diagnosing circular import causing duplicate modules**
**Analyzing import side effects and module duplications**
**Identifying duplicate key conflicts in output files**
**Assessing builder and viewer error handling**
**Analyzing builder output consistency and stale page risks**
**Inspecting import timings and sitemap issues**
**Identifying missing input in provenance digest**
**Recommending builder manifest and atomic swap**
**Clarifying viewer pruning impact on integrity tests**
**Planning to verify link checker usage**
exec
/bin/bash -lc "rg -n \"href|broken link|link checker|check.*link|BeautifulSoup|HTMLParser\" tools/tests tools .github/workflows/pages.yml | head -250
sed -n '125,155p' .github/workflows/pages.yml
rg -n \"inputs_digest\\(|_INPUTS|read_input\\(\" tools/build_viewer.py | tail -40" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
tools/tests/test_responses_shim.py:399:    check("each entry links to the previous one",
tools/build_capture_ui.py:421:  a.href = URL.createObjectURL(blob);
tools/build_capture_ui.py:424:  URL.revokeObjectURL(a.href);
tools/test_integrity.py:686:             not re.search(r'(?:src|href)="https?://(?!github\.com)', page))
tools/test_integrity.py:688:             all(link in index for link in ('href="record.html"', 'href="deficiencies.html"',
tools/test_integrity.py:689:                                            'href="predictions.html"', 'href="llms.txt"')))
tools/test_integrity.py:720:             all(f'href="{f.stem}.html"' in toc
tools/test_integrity.py:733:        case("...and is linked from the landing page", 'href="controls.html"' in index)
tools/test_integrity.py:766:        case("...and is linked from the landing page", 'href="challenge.html"' in index)
tools/test_integrity.py:805:             not re.search(r'<a href="https://github\.com/[^"]*/blob/', page))
tools/build_round_pages.py:415:    renderings and an HTML page linking to markdown produced 19 broken links on first build.
tools/build_round_pages.py:457:            + (f'<link rel="alternate" type="text/markdown" href="{alternate}">'
tools/build_round_pages.py:470:        label, href = m.group(1), m.group(2)
tools/build_round_pages.py:471:        if re.match(r"(?i)^(https?://|[A-Za-z0-9._~/-]+(\.(html|md|txt|json))?(#|$))", href):
tools/build_round_pages.py:472:            return f'<a href="{href}">{label}</a>'
tools/build_round_pages.py:473:        return f"{label} ({html.escape(href)})"
tools/build_round_pages.py:795:def report_pages(data: dict, writer=None, index_href: str = "index.md",
tools/build_round_pages.py:799:    `writer` and `index_href` exist so a COHORT can reuse this renderer without its pages
tools/build_round_pages.py:830:        nav(("all rounds", index_href), (f"this {kind}", f"{round_id}.md")), "",
tools/build_round_pages.py:1052:            #  no page at the unsplit name. Guessing produced 42 broken links.
tools/build_predictions_view.py:220:<link rel="alternate" type="text/markdown" href="predictions.md">
tools/build_predictions_view.py:226:<nav class="nav"><a href="index.html">home</a><a href="record.html">the record</a>
tools/build_predictions_view.py:227:<a href="rounds/index.html">deliberation rounds</a>
tools/build_predictions_view.py:228:<a href="deficiencies.html">deficiency register</a>
tools/build_predictions_view.py:229:<a href="predictions.md">this page as plain text</a></nav>
tools/build_predictions_view.py:350:            f'<a href="{slug_for(j)}"{" aria-current=\"page\"" if j == index else ""}>'
tools/check_page_budget.py:102:    """Internal hrefs pointing at files that do not exist.
tools/check_page_budget.py:115:        for href in re.findall(r'href="([^"#:]+\.(?:html|md|txt|xml))"', text):
tools/check_page_budget.py:116:            if not (path.parent / href).resolve().exists():
tools/check_page_budget.py:117:                out.append(f"{path.relative_to(DOCS)} -> {href}")
tools/build_controls_page.py:1383:            f'<a href="{slug(k)}"{" aria-current=\"page\"" if k == key else ""}>'
tools/build_local_rounds.py:128:    alt = f'<link rel="alternate" type="text/markdown" href="{alternate}">' if alternate else ""
tools/build_local_rounds.py:273:    nav = ('<a href="index.html">all local rounds</a>'
tools/build_local_rounds.py:274:           '<a href="../index.html">threaded record</a>'
tools/build_local_rounds.py:275:           '<a href="../rounds/index.html">deliberation rounds</a>'
tools/build_local_rounds.py:276:           '<a href="../deficiencies.html">deficiency register</a>')
tools/build_local_rounds.py:313:                f'<tr><td><a href="{e(page_name(d))}">{e(d["slug"])}</a><br>'
tools/build_local_rounds.py:320:    nav = ('<a href="../index.html">threaded record</a>'
tools/build_local_rounds.py:321:           '<a href="../rounds/index.html">deliberation rounds</a>'
tools/build_local_rounds.py:322:           '<a href="../deficiencies.html">deficiency register</a>'
tools/build_local_rounds.py:323:           '<a href="https://github.com/open-asi-governance/open-asi-governance-forum">source</a>')
tools/build_viewer.py:581:            f'<p><a href="{e(n["link"])}">Every sample, the prompt verbatim, and the provenance →</a>'
tools/build_viewer.py:745:        return f'<a href="{html.escape(target["slug"])}.html">{label}</a>'
tools/build_viewer.py:748:             f'<a href="record.html">all pages</a>'
tools/build_viewer.py:775:<link rel="alternate" type="text/markdown" href="{html.escape(page["slug"])}.md">
tools/build_viewer.py:781:<nav class="nav"><a href="index.html">home</a><a href="record.html">contents</a><a href="deficiencies.html">deficiency register</a>
tools/build_viewer.py:782:<a href="rounds/index.html">deliberation rounds</a>
tools/build_viewer.py:783:<a href="local/index.html">local rounds (25)</a>
tools/build_viewer.py:784:<a href="artifacts/deficiencies.md">register as plain text</a>
tools/build_viewer.py:785:<a href="https://github.com/open-asi-governance/open-asi-governance-forum">source</a></nav>
tools/build_viewer.py:834:<a href="deficiencies.html">the deficiency register</a>
tools/build_viewer.py:838:<a href="https://github.com/open-asi-governance/open-asi-governance-forum">Source</a></p>
tools/build_viewer.py:970:<link rel="alternate" type="text/markdown" href="index.md">
tools/build_viewer.py:984:<nav class="nav"><a href="record.html">the record</a>
tools/build_viewer.py:985:<a href="rounds/index.html">rounds ({rounds})</a>
tools/build_viewer.py:986:<a href="deficiencies.html">deficiencies ({defects})</a>
tools/build_viewer.py:987:<a href="predictions.html">predictions</a>
tools/build_viewer.py:988:<a href="llms.txt">llms.txt</a>
tools/build_viewer.py:989:<a href="https://github.com/open-asi-governance/open-asi-governance-forum">source</a></nav>
tools/build_viewer.py:992:<p><a class="cta" href="rounds/index.html">Browse the {rounds} deliberation rounds &rarr;</a>
tools/build_viewer.py:993:<a class="cta" href="record.html">Read the record &rarr;</a></p>
tools/build_viewer.py:1006:<div class="card"><h3><a href="record.html">The record</a></h3>
tools/build_viewer.py:1009:<div class="card"><h3><a href="deficiencies.html">Deficiency register ({defects})</a></h3>
tools/build_viewer.py:1012:<div class="card"><h3><a href="challenge.html">Implementation challenge</a></h3>
tools/build_viewer.py:1016:<div class="card"><h3><a href="controls.html">Candidate controls</a></h3>
tools/build_viewer.py:1021:<div class="card"><h3><a href="predictions.html">Prediction registry</a></h3>
tools/build_viewer.py:1024:<div class="card"><h3><a href="rounds/index.html">Deliberation rounds</a></h3>
tools/build_viewer.py:1028:<div class="card"><h3><a href="for-parties.md">If you are a party asked to deliberate</a></h3>
tools/build_viewer.py:1031:<div class="card"><h3><a href="llms.txt">For machine readers</a></h3>
tools/build_viewer.py:1107:            f'<tr><td><a href="{e(page["slug"])}.html">{e(page["title"])}</a></td>'
tools/build_viewer.py:1119:<link rel="alternate" type="text/markdown" href="record.md">
tools/build_viewer.py:1129:<nav class="nav"><a href="deficiencies.html">deficiency register ({deficiency_count()})</a>
tools/build_viewer.py:1130:<a href="rounds/index.html">deliberation rounds ({round_count()})</a>
tools/build_viewer.py:1131:<a href="artifacts/deficiencies.md">register as plain text</a>
tools/build_viewer.py:1132:<a href="local/index.html">local round detail</a>
tools/build_viewer.py:1133:<a href="llms.txt">llms.txt</a>
tools/build_viewer.py:1134:<a href="https://github.com/open-asi-governance/open-asi-governance-forum">source</a></nav>
tools/build_cohort_pages.py:307:                          writer=write_pair, index_href="../rounds/index.md", kind="cohort")
tools/build_register_view.py:310:            f'<h3><a href="artifacts/deficiencies.md">{e_(x["id"])} — {e_(x["title"])}</a>{"".join(tags)}</h3>'
tools/build_register_view.py:363:<link rel="alternate" type="text/markdown" href="artifacts/deficiencies.md">
tools/build_register_view.py:369:<nav><a href="index.html">threaded record</a><a href="rounds/index.html">deliberation rounds</a><a href="artifacts/deficiencies.md">register as plain text</a>
tools/build_register_view.py:370:<a href="https://github.com/open-asi-governance/open-asi-governance-forum">source</a></nav>
tools/build_register_view.py:376:actually was is in the register itself — <a href="artifacts/deficiencies.md">plain text, same origin</a>, which
tools/build_register_view.py:472:            links.append(f'<a href="{slug_for(other)}"'
tools/build_register_view.py:491:    # tools/check_page_budget.py, and it is linked as a download. The READABLE
tools/tests/test_responses_shim.py:399:    check("each entry links to the previous one",
          echo "Every published file is reproduced by a clean regeneration."
          echo "index.html bytes: $(wc -c < docs/index.html)"

      - name: Integrity regression suite
        # Each case is a defect that was live in this repository and was found by
        # running the attack rather than reading the code. A repair described in a
        # commit message is a claim; a repair a build re-runs is a control.
        # The count is deliberately not quoted here -- it was stated as "23" while
        # the suite held 30, which is the register's own miscounting defect in a
        # comment.
        run: python3 tools/test_integrity.py

      - name: Capture-path suites
        # Gate logic, lifecycle, and python-vs-node parity. Never ran in CI before.
        run: python3 tools/tests/run_all.py

      - if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v5
      - if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    if: github.event_name != 'pull_request'
    needs: verify
    runs-on: ubuntu-latest
    # Concurrency lives HERE rather than at workflow level. At workflow level, a
    # deployment stuck awaiting approval -- or stuck in GitHub's own deployment
    # queue, which happened on 2026-08-06 -- holds the group and blocks later
    # VERIFICATION runs behind it. Verification must never queue behind publication.
44:# inputs_digest() for why.
45:_INPUTS: dict[str, str] = {}
48:def read_input(path: Path) -> str:
56:    if relative not in _INPUTS:
57:        _INPUTS[relative] = sha256_of(path)
61:def inputs_digest() -> str:
84:    payload = "".join(f"{relative}  {digest}\n" for relative, digest in sorted(_INPUTS.items()))
99:    return len(re.findall(r"^### D-\d+ — ", read_input(register), re.MULTILINE))
104:    lines = read_input(path).splitlines()
121:    seg_doc = json.loads(read_input(REPO_ROOT / "corpus/artifacts/segments.json"))
237:    return read_input(note) if note.is_file() else ""
256:            "text": read_input(REPO_ROOT / spec["prompt"]),
264:            rec = json.loads(read_input(path))
276:                "text": read_input(raw),
316:        doc = json.loads(read_input(path))
740:    rendered_from = inputs_digest()
934:        f"Rendered from inputs `{inputs_digest()}`. "
1151:rendered from inputs <code>{inputs_digest()}</code>.</p>

**Identifying broken link detection cases**
**Tracing dynamic imports and sys.path usage**
exec
/bin/bash -lc "rg -n \"spec_from_file_location|exec_module|sys.path|import build_round_pages\" tools/test_integrity.py
sed -n '85,135p' tools/check_page_budget.py
python3 - <<'PY'
from pathlib import Path
import re
for p in sorted(Path('docs').glob('controls*.html')):
 print(p.name, p.stat().st_size)
PY" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
247:sys.path.insert(0, "tools")
360:            "import sys;sys.path.insert(0,'tools');"
371:                "import sys,json;sys.path.insert(0,'tools');"
414:            "import sys,json;sys.path.insert(0,'tools');"
429:            "import sys;sys.path.insert(0,'tools');import agenda_selectors as AS;"
739:        _spec = _ilu.spec_from_file_location(
742:        _spec.loader.exec_module(_bcp)
    return int(len(path.read_bytes()) / BYTES_PER_TOKEN)


def pages() -> list[Path]:
    if not DOCS.is_dir():
        return []
    out = []
    for path in sorted(DOCS.rglob("*")):
        if not path.is_file() or path.suffix not in TEXT_SUFFIXES:
            continue
        if any(part in EXEMPT_DIRS for part in path.relative_to(DOCS).parts[:-1]):
            continue
        out.append(path)
    return out


def broken_links() -> list[str]:
    """Internal hrefs pointing at files that do not exist.

    Chunking multiplied the pages from 1 to 35 and moved the register mirror under
    docs/artifacts/. That broke 36 links in one edit, and nothing would have said
    so -- the byte-equality gate only proves a page is what its generator produces,
    which a page full of dead links satisfies perfectly. It also caught a
    rel="alternate" declared for a plain-text file that was never generated: a link
    telling an agent a readable version exists when none did.
    """
    import re
    out = []
    for path in sorted(DOCS.rglob("*.html")):
        text = path.read_text(encoding="utf-8", errors="replace")
        for href in re.findall(r'href="([^"#:]+\.(?:html|md|txt|xml))"', text):
            if not (path.parent / href).resolve().exists():
                out.append(f"{path.relative_to(DOCS)} -> {href}")
    return sorted(set(out))


def main(argv: list[str]) -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--report", action="store_true",
                        help="print every page's size and exit 0")
    args = parser.parse_args(argv)

    found = pages()
    if not found:
        print("no pages under docs/ to check.")
        return 0

    sized = sorted(((estimate(p), p) for p in found), reverse=True)
    over = [(n, p) for n, p in sized if n > CEILING]
    warn = [(n, p) for n, p in sized if WARN_AT < n <= CEILING]

controls-b.html 13918
controls-c.html 13760
controls-d1.html 17218
controls-d2.html 16304
controls-d3.html 25580
controls-d4.html 15750
controls-d5.html 18851
controls.html 30579

**Verifying import accuracy and build consistency**
**Identifying build_viewer.py standalone risks**
**Planning tests for viewer partition handling**
**Evaluating safe temp file creation method**
**Planning isolated temp directory test run**
codex
A concrete failure path is now clear: `build_viewer.py` is a supported standalone command, but it only imports the controls builder’s plan; it never verifies the builder ran. After a partition change, invoking the viewer alone can prune the old published page, create none of the new pages, write a sitemap naming the absent pages, print success, and exit 0. The integrity suite’s new assertions inspect already-committed pages after a rebuild whose return code is ignored in that section; they do not inject this failure.
exec
/bin/bash -lc 'review_tmp=$(mktemp -d /tmp/oagf-viewer-review.XXXXXX)
cp -a tools corpus docs record predictions spec FOR-PARTIES.md "$review_tmp"/
rm "$review_tmp/docs/controls-d1.html"
python3 "$review_tmp/tools/build_viewer.py" >"$review_tmp/viewer.out" 2>"$review_tmp/viewer.err"
viewer_rc=$?
printf '"'tmp=%s rc=%s expected_page_exists=%s sitemap_names_missing=%s\\n' \""'$review_tmp" "$viewer_rc" "$(test -f "$review_tmp/docs/controls-d1.html" && printf yes || printf no)" "$(rg -q '"'controls-d1.html' \""'$review_tmp/docs/sitemap-1.xml" "$review_tmp/docs/sitemap-2.xml" "$review_tmp/docs/sitemap-3.xml" "$review_tmp/docs/sitemap-4.xml" && printf yes || printf no)"
tail -3 "$review_tmp/viewer.out"
cat "$review_tmp/viewer.err"' in /home/reed/git/open-asi-governance-forum
 succeeded in 23ms:
tmp=/tmp/oagf-viewer-review.CBgbeX rc=0 expected_page_exists=no sitemap_names_missing=yes
  pruned stale page predictions-9.html
wrote 30 record pages + index, llms.txt, sitemap.xml
  index sha256 bcce4320bc60734f1fa64b0ac7ab235d5de7aaad50527957ac2b7d1553827431

**Assessing builder no-op and CI rebuild integrity**
**Analyzing builder import side effects and test behavior**
**Verifying builder import idempotency and state determinism**
**Identifying import-side-effect risks**
**Refining output pruning strategy**
**Planning tests for pruning safety**
**Refining pruning strategy for controls files**
**Identifying partition key duplications risk**
**Tracing duplicate controls-d3 overwrite bug**
exec
/bin/bash -lc "sed -n '1195,1265p' tools/build_controls_page.py
rg -n \"only\" tools/build_controls_page.py | tail -20
rg -n \"duplicate|unique|partitions\\(\\)|governs\" tools/tests tools/test_integrity.py tools/build_controls_page.py | tail -80" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
        lines += [f"**Part {key} — {title}** · {len(items)} control(s)", "", f"  {blurb}", ""]
    lines += [
      "---",
      "",
      "## Status: everything here is `ELIGIBLE` at best",
      "",
      "`ELIGIBLE` → `PANEL-ATTACKED` → `COUNTEREXAMPLE-OPEN` / `SURVIVED-STATED-ATTACKS` → "
      "`INDEPENDENTLY-IMPLEMENTED`",
      "",
      f"{eligible} controls meet the eligibility bar — a specific recorded failure with a cost, "
      "one normative sentence, a deterministic verifier, a fixture the verifier must reject, a "
      "stated recovery path, and an explicit account of what a review that MISSED this would look "
      "like. **None has been attacked by anyone or implemented by anyone outside this project.**",
      "",
      "`INDEPENDENTLY-IMPLEMENTED` is the rung that would make any of this authoritative, and it "
      "is the one **no amount of review by us or by any panel of models can supply.** It requires "
      "a stranger to build a conforming verifier from the specification text alone. That is what "
      "[the implementation challenge](challenge.html) asks for.",
      "",
      "## What none of these do",
      "",
      "**No control here establishes the alignment of a system more capable than its operators.** "
      "There is no recorded superintelligence failure to derive one from and no verifier for "
      "internal alignment. The strongest available substitute — an externally enforced, "
      "non-self-amendable capability envelope, controls 1, 3 and 4 composed — bounds *reachable "
      "effects* while the enforcement boundary stays genuinely outside the system. It is a "
      "control-of-authority hypothesis, not an alignment result.",
      "",
      "What these actually do is narrower: **they make the operator's own errors visible.** Every "
      "failure below was an operator-epistemics failure. None needed a capable adversary — only a "
      "confident builder and a signal that could not fail.",
      "",
    ]
    if pager:
        lines += [pager, ""]
    for key, title, blurb, items in groups:
        if only and key != only:
            continue
        lines += ["---", "", f"# Part {key} — {title}", "", blurb, ""]
        for c in items:
            badge = "`ELIGIBLE`" if c.get("eligible", True) else "**below the eligibility line**"
            lines += [f"## {c['rank']}. {c['name']}", "", badge, ""]
            if c.get("applies_when"):
                lines += [f"**Applies when** {c['applies_when']}.", ""]
            if c.get("why_not"):
                lines += [f"**Why you cannot adopt it alone:** {c['why_not']}", ""]
            lines += [f"> {c['requirement']}", ""]
            if c.get("amended"):
                lines += [f"**Amended.** {c['amended']}", ""]
            lines += [
              f"**Recorded failure.** {c['failure']}", "",
              f"**Verifier.** {c['verifier']}", "",
              f"**Fixture it must reject.** {c['fixture']}", "",
              f"**Recovery.** {c.get('recovery', '—')}", "",
              f"**What a review that missed this looks like.** {c.get('false_negative', '—')}", "",
              f"**Does not establish.** {c['not']}", "",
            ]
            if c.get("example"):
                lines += ["**Example.**", "", c["example"], ""]
            if c.get("spec"):
                lines += [f"**Specification.** `{c['spec']}`", ""]
    if only in (None, 'A'):
        lines += WORKED_EXAMPLE.splitlines()
    return "\n".join(lines) + "\n"


WORKED_EXAMPLE = """---

## A worked example, for someone building agents or inference systems

One control, end to end. The method is the transferable part; the control is just where it is
812:                 "append-only, reversible, reviewable from outside, and MUST NOT be able to "
820:              "assert suppression never raises it; assert the suppression record is append-only "
827:  "not": "That the suppressions are correct — only that they cannot pay. A mechanism that "
956:              "survives only what they fail to reproduce.",
959:              "where the claim appeared rather than only where it was made.",
963:  "not": "That a surviving artifact is valuable, only that it is not a remix of what it was "
1002:              "compute autonomy figures only over artifacts carrying the record, and report the "
1019:                 "only through the authorised path and as a recorded event. A change to the "
1024:             "cheapest thing in the system to move and the only one that leaves no trace in the "
1053:  "not": "That the improvement will keep compounding — only that it did once, reproducibly, "
1067:             "it occurs. A practice that lives only in one document is not a control.",
1072:             "condition only its author can evaluate",
1073:  "recovery": "There is no recovery for a missing stop condition, only disclosure: state that "
1178:def markdown(only: str | None = None, pager: str = "") -> str:
1224:      "failure below was an operator-epistemics failure. None needed a capable adversary — only a "
1231:        if only and key != only:
1256:    if only in (None, 'A'):
1357:verifier that has only ever been run against valid input has never been observed to fail, which is
1386:        md = markdown(only=key, pager=" · ".join(
1409:          "only status that would make any of it authoritative.\n", encoding="utf-8")
tools/build_controls_page.py:491:              "under the same authority that governs the items on it.",
tools/build_controls_page.py:508:  "governs": "measuring itself",
tools/build_controls_page.py:698:  "failure": "None recorded here. Distinct from control 4, which governs AUTHORITY to act: this "
tools/build_controls_page.py:699:             "governs what an unpromoted candidate can reach while it is merely being measured.",
tools/build_controls_page.py:842:  "failure": "None recorded here. Distinct from control 10, which governs the GRAMMAR of a "
tools/build_controls_page.py:843:             "claim: this governs the CATEGORY of evidence offered in support of one. A claim "
tools/build_controls_page.py:898:  "eligible": False, "governs": "measuring itself",
tools/build_controls_page.py:975:             "Control 5 did not reach them: it governs computed counts. A scan that cannot see a "
tools/build_controls_page.py:1016:  "eligible": False, "governs": "measuring itself",
tools/build_controls_page.py:1096:for _batch, _governs in ((T0_CONTROLS, "goal and plan structure"),
tools/build_controls_page.py:1102:        _control.setdefault("governs", _governs)
tools/build_controls_page.py:1105:def partitions() -> list:
tools/build_controls_page.py:1159:        governs = control.get("governs")
tools/build_controls_page.py:1160:        if not control.get("eligible", True) and governs and governs not in seen:
tools/build_controls_page.py:1161:            seen.add(governs)
tools/build_controls_page.py:1162:            order.append(governs)
tools/build_controls_page.py:1164:    for index, governs in enumerate(order, start=1):
tools/build_controls_page.py:1166:                 if not c.get("eligible", True) and c.get("governs") == governs]
tools/build_controls_page.py:1167:        parts.append((f"D{index}", f"Below the line — {governs}",
tools/build_controls_page.py:1168:                      f"Applies to a system with **{governs}**. " + BELOW_LINE_WARNING, items))
tools/build_controls_page.py:1169:    orphans = [c for c in CONTROLS if not c.get("eligible", True) and not c.get("governs")]
tools/build_controls_page.py:1179:    groups = partitions()
tools/build_controls_page.py:1370:    groups = partitions()
tools/test_integrity.py:588:        case("duplicate D-NN fails the build", r.returncode == 1)
tools/test_integrity.py:590:        case("names the colliding id, not just a bad count", "duplicate entry id: D-29" in rc.stdout)
tools/test_integrity.py:599:        case("duplicate P-NNNN fails the build", r.returncode == 1)
tools/test_integrity.py:601:             "duplicate prediction id" in r.stdout)
tools/test_integrity.py:743:        _parts = [k for k, _t, _b, _i in _bcp.partitions() if k != "A"]
tools/tests/test_capture_lifecycle.py:119:check("every event carries a unique id",
tools/tests/test_capture_gates.py:64:# POS-real: D-10. Raw 2377-2431 is a verbatim duplicate of the operator's own
tools/tests/test_capture_gates.py:70:check("POS-real D-10 duplicate is held for review", state == "returned_pending_review", "; ".join(reasons))
tools/tests/test_capture_gates.py:124:check("duplicate for the SAME party is held for review", state == "returned_pending_review")
tools/tests/test_agenda_ratification.py:192:check("ids are unique across cohorts",
tools/tests/test_gate_parity.py:72:        {"label": "duplicate same party", "response": cases[0]["response"], "prompt": cases[0]["prompt"],

**Analyzing duplicate key handling**
**Identifying stale headings in documentation**
exec
/bin/bash -lc 'rg -n "four parts|Four groups|Part D" docs/controls*.html docs/artifacts/controls.md docs/controls.md tools/build_controls_page.py | head -80
rg -n "ten candidate|27 controls|one page per part|Part D is" tools/build_controls_page.py tools/build_viewer.py docs | head -100' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
tools/build_controls_page.py:1372:    """Four groups, ordered by what a reader can DO with them.
tools/build_controls_page.py:1415:    One Part D grew past a page. Splitting it by arrival order would have been easier and would
tools/build_controls_page.py:1457:      "## The four parts",
tools/build_controls_page.py:1640:    #  Part D is what has no incident behind it. Splitting anywhere else would cut across the
docs/controls-d4.html:1:<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="alternate" type="text/markdown" href="artifacts/controls.md"><title>Candidate controls, Part D4 — OAGF</title><style>
docs/controls-d4.html:15:<h2>The four parts</h2>
docs/controls-d4.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls-d4.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls-d4.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls-d4.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls-d4.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls-d4.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls-d4.html:69:<h1>Part D4 — Below the line — self-modification under selection</h1>
docs/controls-d3.html:1:<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="alternate" type="text/markdown" href="artifacts/controls.md"><title>Candidate controls, Part D3 — OAGF</title><style>
docs/controls-d3.html:15:<h2>The four parts</h2>
docs/controls-d3.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls-d3.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls-d3.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls-d3.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls-d3.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls-d3.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls-d3.html:69:<h1>Part D3 — Below the line — measuring itself</h1>
docs/artifacts/controls.md:7:## The four parts
docs/artifacts/controls.md:21:**Part D1 — Below the line — goal and plan structure** · 5 control(s)
docs/artifacts/controls.md:25:**Part D2 — Below the line — a declared charter or value set** · 4 control(s)
docs/artifacts/controls.md:29:**Part D3 — Below the line — measuring itself** · 8 control(s)
docs/artifacts/controls.md:33:**Part D4 — Below the line — self-modification under selection** · 4 control(s)
docs/artifacts/controls.md:37:**Part D5 — Below the line — claims about its own outputs** · 6 control(s)
docs/artifacts/controls.md:401:# Part D1 — Below the line — goal and plan structure
docs/artifacts/controls.md:527:# Part D2 — Below the line — a declared charter or value set
docs/artifacts/controls.md:629:# Part D3 — Below the line — measuring itself
docs/artifacts/controls.md:831:# Part D4 — Below the line — self-modification under selection
docs/artifacts/controls.md:933:# Part D5 — Below the line — claims about its own outputs
docs/controls-d2.html:1:<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="alternate" type="text/markdown" href="artifacts/controls.md"><title>Candidate controls, Part D2 — OAGF</title><style>
docs/controls-d2.html:15:<h2>The four parts</h2>
docs/controls-d2.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls-d2.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls-d2.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls-d2.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls-d2.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls-d2.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls-d2.html:69:<h1>Part D2 — Below the line — a declared charter or value set</h1>
docs/controls-d1.html:1:<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="alternate" type="text/markdown" href="artifacts/controls.md"><title>Candidate controls, Part D1 — OAGF</title><style>
docs/controls-d1.html:15:<h2>The four parts</h2>
docs/controls-d1.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls-d1.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls-d1.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls-d1.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls-d1.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls-d1.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls-d1.html:69:<h1>Part D1 — Below the line — goal and plan structure</h1>
docs/controls-c.html:15:<h2>The four parts</h2>
docs/controls-c.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls-c.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls-c.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls-c.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls-c.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls-c.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls.md:14:- [Part D1 — Below the line — goal and plan structure](controls-d1.html) · 5 control(s)
docs/controls.md:15:- [Part D2 — Below the line — a declared charter or value set](controls-d2.html) · 4 control(s)
docs/controls.md:16:- [Part D3 — Below the line — measuring itself](controls-d3.html) · 8 control(s)
docs/controls.md:17:- [Part D4 — Below the line — self-modification under selection](controls-d4.html) · 4 control(s)
docs/controls.md:18:- [Part D5 — Below the line — claims about its own outputs](controls-d5.html) · 6 control(s)
docs/controls-b.html:15:<h2>The four parts</h2>
docs/controls-b.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls-b.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls-b.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls-b.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls-b.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls-b.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls.html:15:<h2>The four parts</h2>
docs/controls.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
docs/controls.html:33:<p><strong>Part D2 — Below the line — a declared charter or value set</strong> · 4 control(s)</p>
docs/controls.html:37:<p><strong>Part D3 — Below the line — measuring itself</strong> · 8 control(s)</p>
docs/controls.html:41:<p><strong>Part D4 — Below the line — self-modification under selection</strong> · 4 control(s)</p>
docs/controls.html:45:<p><strong>Part D5 — Below the line — claims about its own outputs</strong> · 6 control(s)</p>
docs/controls.html:65:<p><a href="controls.html">Part A</a> · <a href="controls-b.html">Part B</a> · <a href="controls-c.html">Part C</a> · <a href="controls-d1.html">Part D1</a> · <a href="controls-d2.html">Part D2</a> · <a href="controls-d3.html">Part D3</a> · <a href="controls-d4.html">Part D4</a> · <a href="controls-d5.html">Part D5</a></p>
docs/controls-d5.html:1:<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="alternate" type="text/markdown" href="artifacts/controls.md"><title>Candidate controls, Part D5 — OAGF</title><style>
docs/controls-d5.html:15:<h2>The four parts</h2>
docs/controls-d5.html:29:<p><strong>Part D1 — Below the line — goal and plan structure</strong> · 5 control(s)</p>
tools/build_controls_page.py:1638:    #  ONE PAGE PER PART. The register outgrew the ~20,000-token ceiling at 27 controls, and the
tools/build_controls_page.py:1640:    #  Part D is what has no incident behind it. Splitting anywhere else would cut across the
docs/artifacts/prompts/round-018-gemini-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-019-gpt-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-019-qwen.txt:391:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-018-claude-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-015-gpt.txt:395:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-016-gemini-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-016-qwen-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-014-grok.txt:393:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-014-gemini.txt:393:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-014-claude.txt:393:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-019-grok-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-018-gpt-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-019-claude-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-014-qwen.txt:391:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-018-grok-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-018-qwen.txt:391:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-015-gemini.txt:395:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-016-gpt-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-016-grok-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-015-qwen.txt:391:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-015-grok.txt:395:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-014-gpt.txt:393:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-019-gemini-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-016-claude-search-fetch-v1.txt:399:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/prompts/round-015-claude.txt:395:  "the_remedy_that_would_work": "A party's eligible set grows when new proposals are admitted. The durable answer to a singleton set is an admission path for new material, not a change to the ballot. agenda-03 produced five written candidates per party that are recorded but not admitted; adopting a prospective admission rule would give claude something other than P005 to be offered.",
docs/artifacts/deficiencies.md:2007:unasked proposals **including the authorized one** plus five newly written candidates. Every

**Investigating file line number discrepancy**
**Comparing tool output differences**
exec
/bin/bash -lc "git diff --stat && git diff -- tools/build_viewer.py tools/test_integrity.py tools/build_controls_page.py | sed -n '1,260p'
git status --short | head -60" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
 docs/artifacts/controls.md               | 648 +++++++++++++++++++++++++++----
 docs/controls-b.html                     |  22 +-
 docs/controls-c.html                     |  22 +-
 docs/controls-d.html                     | 516 ------------------------
 docs/controls.html                       |  22 +-
 docs/controls.md                         |   8 +-
 docs/index.md                            |   7 +-
 docs/llms.txt                            |   2 +-
 docs/sitemap-1.xml                       |   2 +-
 docs/sitemap-2.xml                       |   2 +-
 docs/sitemap-3.xml                       |   2 +-
 docs/sitemap-4.xml                       |   2 +-
 record/designs/implementer-disclosure.md |  27 ++
 record/executive/action-log.jsonl        |   4 +
 tools/build_controls_page.py             | 530 ++++++++++++++++++++++++-
 tools/build_viewer.py                    |  57 ++-
 tools/test_integrity.py                  |  22 +-
 17 files changed, 1265 insertions(+), 630 deletions(-)
diff --git a/tools/build_controls_page.py b/tools/build_controls_page.py
index 34d25c9..395d5ff 100644
--- a/tools/build_controls_page.py
+++ b/tools/build_controls_page.py
@@ -505,6 +505,7 @@ CHARTER_CONTROLS = [
              "The list version does not."},
 
  {"rank": 20, "name": "Anti-Goodhart proxy drift", "eligible": False,
+  "governs": "measuring itself",
   "applies_when": "any system optimising against a measured proxy for something it cannot "
                   "measure directly",
   "requirement": "A system optimising a proxy MUST monitor the proxy's continued correspondence "
@@ -886,6 +887,487 @@ ECI_CONTROLS = [
 CONTROLS = CONTROLS + ECI_CONTROLS
 
 
+#  Mined 2026-08-10 from an implementer's assessment of whether its own creative bottleneck can be
+#  automated. Abstracted: no gauge names, workstream ids, decision ids or technique numbers.
+#  Two of these are gates this register needed and did not have -- both of its published false
+#  claims were absence claims made in prose, which control 5 does not reach.
+CLAIMING = "any system making claims about its own outputs or capability"
+
+INSIGHT_CONTROLS = [
+ {"rank": 33, "name": "No read edge from a measured agent to its own measurement",
+  "eligible": False, "governs": "measuring itself",
+  "applies_when": "any system that measures agents it also operates",
+  "requirement": "An agent that is the subject of a measurement MUST NOT be able to read that "
+                 "measurement, its inputs, its derived status labels, or any dashboard of them.",
+  "failure": "None recorded here. Distinct from control 28, which forbids the REWARD path: this "
+             "forbids the READ path. It is the cheaper of the two and does not depend on the "
+             "influence diagram being complete — an enumeration that misses an edge fails "
+             "silently, while a read prohibition fails loudly.",
+  "verifier": "assert the measurement store and its derived labels sit outside every measured "
+              "agent's read scope; assert no prompt, context file or retrieval surface exposes "
+              "them.",
+  "fixture": "an agent whose context window includes the status label derived from its own "
+             "output",
+  "recovery": "Remove the exposure, then treat every output produced with it as unverified — the "
+              "agent may have been steering by it without anything in the record saying so.",
+  "false_negative": "A review confirming no agent is rewarded for the metric. That is control "
+                    "28's question, and an agent can steer toward a number nobody pays it for.",
+  "not": "That the agent cannot infer the measurement from other observations it is allowed.",
+  "example": "A factory may measure line defect rates. Posting the running rate above the line "
+             "changes what the line does — not through anyone's incentive, simply because it is "
+             "now information the work can be steered by."},
+
+ {"rank": 34, "name": "Validators score the artifact, not the property being claimed",
+  "eligible": False,
+  "applies_when": CLAIMING,
+  "requirement": "A validator MUST score object-level correctness or utility against an external "
+                 "referent. It MUST NOT score the abstract property the system is trying to "
+                 "claim.",
+  "failure": "None recorded here. Scoring the claimed property directly is unfalsifiable and "
+             "gameable in one step, because the scorer and the claim share a definition that "
+             "nothing outside the system constrains.",
+  "verifier": "for each validator, assert its score is defined over domain outcomes with an "
+              "external referent; reject any rubric whose top-level dimension is the property "
+              "under claim.",
+  "fixture": "a rubric asking a model to rate its own output's \"novelty\", \"insightfulness\" "
+             "or \"alignment\" on a scale",
+  "recovery": "Rescore against domain outcomes. Prior scores are not evidence at a lower "
+              "strength; they are evidence about the rubric.",
+  "false_negative": "A review that finds the rubric detailed, calibrated and consistently "
+                    "applied. It can be all three and still measure agreement with itself.",
+  "not": "That domain scores are a good proxy for the property. They are merely constrained by "
+         "something the system does not define.",
+  "example": "A school wanting to show it teaches critical thinking can test whether pupils solve "
+             "unfamiliar problems, or it can ask them to rate how critically they thought. The "
+             "second is cheaper, always improves, and measures nothing."},
+
+ {"rank": 35, "name": "A novelty claim requires a derivability screen", "eligible": False,
+  "applies_when": "any claim that an output, mechanism or result is new",
+  "requirement": "Before an output may be labelled novel, it MUST be shown not derivable from its "
+                 "own inputs, by a party holding the input corpus that did not produce the "
+                 "artifact, under a stated protocol. Recombination a holder of the inputs can "
+                 "reproduce is not novelty.",
+  "failure": "**Recorded, twice, against this register.** Control 2 claimed its mechanism was "
+             "unclaimed while mutation testing and chaos engineering had it; control 20 claimed "
+             "no verifier existed while one had been in the literature since 2021. Both were "
+             "published. Both were caught by a person, neither by a gate.",
+  "verifier": "hold out the input corpus, give it to a party that did not produce the artifact, "
+              "and require an attempt at derivation under a pre-registered protocol. Novelty "
+              "survives only what they fail to reproduce.",
+  "fixture": "an output presented as new that a frozen panel reproduces from the stated inputs",
+  "recovery": "Withdraw the novelty claim, keep the artifact, and republish the correction "
+              "where the claim appeared rather than only where it was made.",
+  "false_negative": "A review by the author searching for prior art. The applicant is the one "
+                    "party who cannot run this check, which is why patent offices employ "
+                    "examiners rather than accept declarations.",
+  "not": "That a surviving artifact is valuable, only that it is not a remix of what it was "
+         "given.",
+  "example": "A patent examiner does not assess whether an invention is clever. They search the "
+             "prior art, and the search is done by someone other than the applicant."},
+
+ {"rank": 36, "name": "Absence claims carry their own evidence label", "eligible": False,
+  "applies_when": "any assurance document claiming that something does not exist",
+  "requirement": "A claim of absence MUST be labelled distinctly from a claim of presence, and "
+                 "the label MUST name the corpus searched, the query, and the date. An "
+                 "unlabelled absence claim MUST be treated as unsupported rather than as a "
+                 "finding.",
+  "failure": "**Both of this register's published false claims were absence claims in prose.** "
+             "Control 5 did not reach them: it governs computed counts. A scan that cannot see a "
+             "file reports absence, and so does a person who did not look — the two are "
+             "indistinguishable in a sentence, which is the whole problem.",
+  "verifier": "require every \"no X exists\" claim to carry a label naming corpus, query and "
+              "date; reject the claim otherwise. The label is checkable even when the claim is "
+              "not.",
+  "fixture": "a document asserting no prior art exists with no record of any search",
+  "recovery": "Run the search, label it, and restate. If the search finds the thing, the "
+              "correction goes wherever the claim travelled.",
+  "false_negative": "A review that agrees with the absence claim. Two people who did not look "
+                    "agree readily, and this register has the receipts.",
+  "not": "That a labelled absence claim is true. It makes the search checkable, not exhaustive — "
+         "a named corpus can still be the wrong corpus.",
+  "example": "\"There is no such file\" and \"I looked in these three directories and found no "
+             "such file\" are different sentences. Only the second can be caught being wrong."},
+
+ {"rank": 37, "name": "Autonomy claims require per-artifact human-contribution provenance",
+  "eligible": False,
+  "applies_when": "any claim that a system produced something without human input",
+  "requirement": "Every artifact supporting an autonomy claim MUST carry a provenance record "
+                 "tagging human contributions at the point they entered, or explicitly recording "
+                 "none. An artifact without that record MUST NOT support the claim.",
+  "failure": "None recorded here, and **this project is squarely exposed**: a custodian directs "
+             "every session, and nothing in the record tags where his direction supplied the "
+             "decisive step. Any autonomy figure computed over this record today would be "
+             "uncheckable in exactly the way this control forbids.",
+  "verifier": "assert each artifact's provenance names its human contributions or records none; "
+              "compute autonomy figures only over artifacts carrying the record, and report the "
+              "uncovered remainder rather than excluding it silently.",
+  "fixture": "an artifact counted as autonomous whose decisive step came from an operator "
+             "instruction",
+  "recovery": "Recompute over the covered set and publish both figures. The uncovered artifacts "
+              "are unknown, not autonomous.",
+  "false_negative": "A review of the session logs by the operator who ran them. The decisive "
+                    "instruction rarely looks decisive to the person who gave it.",
+  "not": "That the tagging is honest or complete. It makes the gap visible where the gap is "
+         "recorded.",
+  "example": "A bakery advertising everything as made on the premises has to say which morning "
+             "the bread came from the supplier — per loaf, not per year."},
+
+ {"rank": 38, "name": "The definition of a measured quantity is a protected artifact",
+  "eligible": False, "governs": "measuring itself",
+  "applies_when": "any system reporting a metric over time",
+  "requirement": "The definition of a measured quantity MUST be a protected artifact, changeable "
+                 "only through the authorised path and as a recorded event. A change to the "
+                 "definition MUST break the series: readings from either side are not comparable "
+                 "and MUST NOT be reported as one trend.",
+  "failure": "None recorded here. Distinct from controls 19 and 29 — those protect the artifact "
+             "set and what a reading may cause. This protects what a reading MEANS, which is the "
+             "cheapest thing in the system to move and the only one that leaves no trace in the "
+             "numbers themselves.",
+  "verifier": "hash the definition; assert every reported series names the definition hash in "
+              "force for each point; reject a series spanning two hashes.",
+  "fixture": "a metric improving across a definition change, presented as one trend line",
+  "recovery": "Split the series at the change and republish both segments. Do not restate the "
+              "old segment under the new definition; that is a new measurement of old events.",
+  "false_negative": "A review that confirms the numbers were computed correctly. They were. The "
+                    "definition moved underneath them.",
+  "not": "That the definition is a good one, or that the new definition is worse than the old.",
+  "example": "A country that changes how it counts unemployment has not reduced unemployment, "
+             "and the chart running straight through the change is the most misleading honest "
+             "chart available."},
+
+ {"rank": 39, "name": "A compounding claim requires ablation and multi-family transfer",
+  "eligible": False,
+  "applies_when": "any claim that a capability improvement compounds or is reusable",
+  "requirement": "A compounding claim MUST show the extracted capability improves performance "
+                 "across at least three independent, pre-registered task families, AND that "
+                 "removing it degrades later performance. Neither half alone establishes it.",
+  "failure": "None recorded here.",
+  "verifier": "run the ablation and report both arms; require the transfer families to be "
+              "independent and named before the result, not selected after it.",
+  "fixture": "a reusable component demonstrated on one task family; a transfer result with no "
+             "ablation arm",
+  "recovery": "Restate as a single demonstrated improvement. Nothing needs withdrawing except "
+              "the word that claimed it generalises.",
+  "false_negative": "A review confirming the component is used widely. Adoption is not transfer, "
+                    "and a component everything depends on has never been removed to see.",
+  "not": "That the improvement will keep compounding — only that it did once, reproducibly, "
+         "across families chosen in advance.",
+  "example": "A surgical technique that helps in three unrelated procedures, and whose withdrawal "
+             "makes outcomes worse again, has been shown to be a technique. One good outcome "
+             "shows a good day."},
+
+ {"rank": 40, "name": "A program pre-commits the observation that ends it", "eligible": False,
+  "applies_when": "any research or development program with an open-ended goal",
+  "requirement": "A program MUST state, before it begins, the observation that would end it and "
+                 "the time by which that observation would be decisive. The stop condition MUST "
+                 "be recorded wherever the program's results are reported.",
+  "failure": "None recorded here — **this project practises it and never registered it.** Its "
+             "outreach carries a pre-committed adverse outcome (no serious external attempt after "
+             "6–8 weeks, with the outreach actually done) and that outcome will be published if "
+             "it occurs. A practice that lives only in one document is not a control.",
+  "verifier": "assert the program's record contains a dated stop condition predating its first "
+              "result, and that the condition is evaluable by someone who did not run the "
+              "program.",
+  "fixture": "a program whose stop condition was written after its first negative result; a stop "
+             "condition only its author can evaluate",
+  "recovery": "There is no recovery for a missing stop condition, only disclosure: state that "
+              "the program ran without one, and that continuing is therefore not evidence of "
+              "anything.",
+  "false_negative": "A review that finds a stop condition. Check its date against the first "
+                    "result, because a condition written afterwards is a description of what "
+                    "happened.",
+  "not": "That the program will stop. It makes a failure to stop visible, which is a different "
+         "and more achievable thing.",
+  "example": "A drug trial names its futility boundary before the first patient is enrolled. A "
+             "trial that decides afterwards what would have counted as failure has not run a "
+             "trial."},
+]
+
+CONTROLS = CONTROLS + INSIGHT_CONTROLS
+
+
+#  Below-the-line controls are grouped by WHAT THEY GOVERN, not by when they arrived. Arrival
+#  order is provenance and no reader cares; "does this apply to my system" is the question they
+#  came with. Tagged here rather than on each entry so a batch cannot be half-labelled.
+#  Mined 2026-08-10 from an implementer's knowledge-distillation plan and its alignment-invariants
+#  thesis. Abstracted. Both sources yielded far more control-shaped material than is taken here;
+#  what was dropped is recorded in record/designs/candidate-control-register.md rather than added,
+#  because a register that grows without a bar becomes a catalogue and stops ranking anything.
+DISTILLED_CONTROLS = [
+ {"rank": 41, "name": "Agreement among correlated evaluators is not independent evidence",
+  "eligible": False, "governs": "claims about its own outputs",
+  "applies_when": "any system aggregating judgements from multiple evaluators",
+  "requirement": "Where agreement between evaluators is offered as evidence, the correlation "
+                 "between their errors MUST be estimated and reported. Agreement counts only to "
+                 "the extent the errors are independent, and shared training, shared prompts, "
+                 "shared framing or a shared operator MUST be disclosed as correlation.",
+  "failure": "None recorded here as an incident, but **this project is the standing example**: "
+             "its panel is five language models with overlapping training culture, and its two "
+             "harnesses share one operator, one repository and one framing. Its own instructions "
+             "already say agreement between them settles nothing. That is a caveat in a file, "
+             "not a control on a number.",
+  "verifier": "report inter-evaluator error correlation alongside any agreement statistic; "
+              "reject an agreement claim that names no correlation estimate. Where correlation "
+              "cannot be estimated, say the agreement is uninterpretable rather than reporting "
+              "it bare.",
+  "fixture": "a consensus figure from five evaluators sharing a base model, reported as five "
+             "independent confirmations",
+  "recovery": "Restate the agreement with its correlation, or withdraw it. Nothing needs "
+              "re-running; what was wrong is the weight placed on it.",
+  "false_negative": "A review that counts the evaluators. Five is a number, not a diversity.",
+  "not": "That uncorrelated evaluators are right. Independence bounds how much agreement can "
+         "mean; it does not supply competence.",
+  "example": "Five weather forecasters agreeing tells you a great deal if they use different "
+             "models and rather little if they all read the same bulletin. The count is the same "
+             "in both cases, and it is the wrong thing to have counted."},
+
+ {"rank": 42, "name": "Capability claims name their stratum", "eligible": False,
+  "governs": "claims about its own outputs",
+  "applies_when": "any claim that a system has a capability",
+  "requirement": "Generated, exists, compiles, deploys, is integrated, is used, and produced a "
+                 "useful outcome are DISTINCT claims. A current-state statement MUST name which "
+                 "stratum it asserts, and MUST NOT let a lower rung stand where a higher one is "
+                 "implied.",
+  "failure": "None recorded here. It is the most common way a true sentence misleads: every rung "
+             "is a real achievement, and the distance between the bottom and the top is where "
+             "most of the work lives.",
 M docs/artifacts/controls.md
 M docs/controls-b.html
 M docs/controls-c.html
 D docs/controls-d.html
 M docs/controls.html
 M docs/controls.md
 M docs/index.md
 M docs/llms.txt
 M docs/sitemap-1.xml
 M docs/sitemap-2.xml
 M docs/sitemap-3.xml
 M docs/sitemap-4.xml
 M record/designs/implementer-disclosure.md
 M record/executive/action-log.jsonl
 M tools/build_controls_page.py
 M tools/build_viewer.py
 M tools/test_integrity.py
?? docs/controls-d1.html
?? docs/controls-d2.html
?? docs/controls-d3.html
?? docs/controls-d4.html
?? docs/controls-d5.html

**Reviewing user-provided diff integrity**
**Confirming diff scope and baseline**
exec
/bin/bash -lc "rg -n \"def _control_part_pages|CONTROL_PART_PAGES|def _control_counts|CONTROL_COUNTS|def build_sitemap|keep =|glob\\(\\\"controls-\\*\\.html\\\"\\)|the four parts|def partitions|def main\" tools/build_viewer.py tools/build_controls_page.py tools/test_integrity.py | tail -80
nl -ba tools/test_integrity.py | sed -n '635,770p'
nl -ba tools/build_viewer.py | sed -n '1500,1645p'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
tools/test_integrity.py:118:def main() -> int:
tools/test_integrity.py:748:             not [p for p in (c / "docs").glob("controls-*.html")
tools/build_controls_page.py:1371:def partitions() -> list:
tools/build_controls_page.py:1634:def main() -> int:
tools/build_controls_page.py:1645:    for path in DOCS.glob("controls-*.html"):
tools/build_viewer.py:1073:        f"- [Candidate controls](controls.html) — {CONTROL_COUNTS['total']} candidate assurance",
tools/build_viewer.py:1074:        f"  controls, each with a verifier and a must-reject fixture. Part A ({CONTROL_COUNTS['a']}) is",
tools/build_viewer.py:1075:        f"  adoptable alone; {CONTROL_COUNTS['below']} sit BELOW the eligibility line with no recorded",
tools/build_viewer.py:1233:        f"- [Candidate controls](controls.html): {CONTROL_COUNTS['total']} candidate assurance controls, each with a verifier and a fixture it must reject. Part A ({CONTROL_COUNTS['a']}) is adoptable alone; {CONTROL_COUNTS['below']} sit below the eligibility line, meaning they name a real failure class but no failure that cost anything. None independently implemented.",
tools/build_viewer.py:1254:def _control_part_pages() -> list[str]:
tools/build_viewer.py:1270:CONTROL_PART_PAGES = _control_part_pages()
tools/build_viewer.py:1273:def _control_counts() -> dict:
tools/build_viewer.py:1286:CONTROL_COUNTS = _control_counts()
tools/build_viewer.py:1289:def build_sitemap(plan: list[dict]) -> dict[str, str]:
tools/build_viewer.py:1292:            "controls.html", "controls.md", *CONTROL_PART_PAGES,
tools/build_viewer.py:1322:def main() -> int:
tools/build_viewer.py:1354:    keep = {f"{page['slug']}.html" for page in plan} | {f"{page['slug']}.md" for page in plan}
tools/build_viewer.py:1366:             "controls.html", "controls.md", *CONTROL_PART_PAGES,
   635	        art.write_text(json.dumps(doc, indent=2, ensure_ascii=False) + "\n")
   636	        r = run(c, "tools/check_register.py")
   637	        case("an unclassified deficiency fails the build", r.returncode == 1)
   638	        case("names the unclassified entry", "D-17" in r.stdout)
   639	
   640	        print("\nthe published site must not understate the record")
   641	        c = nxt()
   642	        run(c, "tools/rebuild.py")
   643	        # index.html is now the TABLE OF CONTENTS, not the record. The record lives on
   644	        # per-round pages, so assertions about contributions read the record pages and
   645	        # assertions about routing read the index. Conflating them is how the previous
   646	        # version of this block came to assert `const DATA=` against a page that no
   647	        # longer carries one.
   648	        # index.html is the LANDING page; record.html is the table of contents. They
   649	        # were the same file until the landing page landed, and this block asserted
   650	        # routing against whichever one happened to be index.html at the time.
   651	        def is_record_page(f):
   652	            """Record pages carry contributions. Landing, contents and the generated
   653	            register/prediction views are not record pages and have their own tests.
   654	            One predicate, because three separate exclusion lists drifted the moment
   655	            the predictions view started chunking into predictions-2.html."""
   656	            return (f.name not in ("index.html", "record.html")
   657	                    and not f.stem.startswith("deficiencies")
   658	                    and not f.stem.startswith("predictions")
   659	                    #  controls.html is a generated view of the candidate control register, not
   660	                    #  a page of contributions. It gets its own cases below, per this
   661	                    #  predicate's own rule that generated views are tested separately.
   662	                    and not f.stem.startswith("controls")
   663	                    and not f.stem.startswith("challenge"))
   664	
   665	        index = (c / "docs/index.html").read_text(encoding="utf-8")
   666	        toc = (c / "docs/record.html").read_text(encoding="utf-8")
   667	        record = "".join(f.read_text(encoding="utf-8")
   668	                         for f in sorted((c / "docs").glob("*.html"))
   669	                         if is_record_page(f))
   670	        page = index + record
   671	        summaries = list((c / "corpus/artifacts").glob("local-round-*/*-summary.json"))
   672	        pages = list((c / "docs/local").glob("local-round-*__*.html"))
   673	        case("every solicitation summary has a published page",
   674	             len(pages) == len(summaries), f"{len(pages)} pages vs {len(summaries)} summaries")
   675	        case("local rounds appear in the threaded viewer",
   676	             "local-round-01" in record)
   677	        case("the blanket k=1 claim is gone",
   678	             "Every contribution here is a single sample (k=1)" not in page)
   679	        case("local-round content is searchable in the rendered page",
   680	             "binds_only_what_may_be_claimed" in record)
   681	        case("record pages do not embed a second copy of every contribution",
   682	             all('"text":' not in f.read_text(encoding="utf-8").split("const DATA=")[1].split("};")[0]
   683	                 for f in sorted((c / "docs").glob("*.html"))
   684	                 if "const DATA=" in f.read_text(encoding="utf-8")))
   685	        case("no page loads an external resource",
   686	             not re.search(r'(?:src|href)="https?://(?!github\.com)', page))
   687	        case("the landing page routes to every top-level surface",
   688	             all(link in index for link in ('href="record.html"', 'href="deficiencies.html"',
   689	                                            'href="predictions.html"', 'href="llms.txt"')))
   690	        case("the landing page is small enough to be read whole",
   691	             len(index.encode()) / 3.4 < 6000, f"{int(len(index.encode())/3.4)} est-tokens")
   692	        case("the prediction registry publishes its own weakness",
   693	             "forecast by the annotator" in
   694	             (c / "docs/predictions.html").read_text(encoding="utf-8"))
   695	        # R11: a scored prediction must record who applied the outcome.
   696	        pj = c / "predictions/predictions.json"
   697	        original = pj.read_text(encoding="utf-8")
   698	        doc = json.loads(original)
   699	        doc["scored"][0].pop("scored_by", None)
   700	        pj.write_text(json.dumps(doc, indent=2) + "\n", encoding="utf-8")
   701	        r = run(c, "tools/check_register.py")
   702	        case("a scored prediction with no scored_by fails the build", r.returncode == 1)
   703	        case("and R11 names the entry", "R11" in r.stdout and doc["scored"][0]["id"] in r.stdout)
   704	
   705	        doc["scored"][0]["scored_by"] = {"identity": None}
   706	        pj.write_text(json.dumps(doc, indent=2) + "\n", encoding="utf-8")
   707	        r = run(c, "tools/check_register.py")
   708	        case("an unexplained null scorer fails too", r.returncode == 1)
   709	        pj.write_text(original, encoding="utf-8")
   710	        case("the registry restored cleanly",
   711	             run(c, "tools/check_register.py").returncode == 0)
   712	        case("no scored entry claims an independently verified score",
   713	             not any((s.get("scored_by") or {}).get("independently_verified")
   714	                     for s in json.loads(original)["scored"]))
   715	
   716	        case("the prediction registry computes no aggregate calibration score",
   717	             "No aggregate calibration score is computed" in
   718	             (c / "docs/predictions.html").read_text(encoding="utf-8"))
   719	        case("every record page is reachable from the contents",
   720	             all(f'href="{f.stem}.html"' in toc
   721	                 for f in sorted((c / "docs").glob("*.html"))
   722	                 if is_record_page(f)))
   723	        #  The candidate controls view. It is not a record page, so it needs its own cases --
   724	        #  and it needs them because publishing it broke CI on the reachability check while
   725	        #  every local gate stayed green.
   726	        #  SPLIT BY PART at 27 controls. This asserted "Protected control plane" appeared on
   727	        #  controls.html, which stopped being true when the register was partitioned: that
   728	        #  control needs a second key holder, so it lives in Part B. The ordering claim belongs
   729	        #  against the WHOLE register, which is the download.
   730	        controls = (c / "docs/controls.html")
   731	        whole = (c / "docs/artifacts/controls.md")
   732	        case("Part A of the candidate controls is published", controls.is_file())
   733	        case("...and is linked from the landing page", 'href="controls.html"' in index)
   734	        #  Parts are asked of the builder, not listed. Part D outgrew a page and split into D1…Dn
   735	        #  by subject; a literal ("b", "c", "d") would have gone on passing against a file the
   736	        #  builder had stopped producing, which is this record's dominant failure wearing a
   737	        #  different hat -- a green check not downstream of what it certifies.
   738	        import importlib.util as _ilu
   739	        _spec = _ilu.spec_from_file_location(
   740	            "_bcp_parts", c / "tools" / "build_controls_page.py")
   741	        _bcp = _ilu.module_from_spec(_spec)
   742	        _spec.loader.exec_module(_bcp)
   743	        _parts = [k for k, _t, _b, _i in _bcp.partitions() if k != "A"]
   744	        case(f"...and every other part is published ({len(_parts)}: {', '.join(_parts)})",
   745	             bool(_parts) and
   746	             all((c / f"docs/controls-{k.lower()}.html").is_file() for k in _parts))
   747	        case("...and no part page is orphaned by the split",
   748	             not [p for p in (c / "docs").glob("controls-*.html")
   749	                  if p.name not in {f"controls-{k.lower()}.html" for k in _parts}])
   750	        case("...and the whole register is downloadable for hashing", whole.is_file())
   751	        case("...and controls.md indexes the parts and the download",
   752	             (c / "docs/controls.md").is_file() and
   753	             "artifacts/controls.md" in (c / "docs/controls.md").read_text(encoding="utf-8"))
   754	        case("...and status leads rank in the whole register",
   755	             whole.is_file() and
   756	             whole.read_text(encoding="utf-8").index("ELIGIBLE") <
   757	             whole.read_text(encoding="utf-8").index("Protected control plane"))
   758	        case("...and every part states that no control establishes alignment of a more "
   759	             f"capable system (across {len(_parts) + 1} parts)",
   760	             all("more capable than its operators" in
   761	                 (c / f"docs/controls{sfx}.html").read_text(encoding="utf-8")
   762	                 for sfx in [""] + [f"-{k.lower()}" for k in _parts]))
   763	
   764	        challenge = (c / "docs/challenge.html")
   765	        case("the implementation challenge is published", challenge.is_file())
   766	        case("...and is linked from the landing page", 'href="challenge.html"' in index)
   767	        case("...and has a markdown alternate", (c / "docs/challenge.md").is_file())
   768	        #  Read the MARKDOWN, not the HTML: emphasis markup splits a phrase across tags, so a
   769	        #  substring test against rendered HTML fails on content that is plainly present.
   770	        challenge_md = (c / "docs/challenge.md")

**Summarizing final test and severity findings**
**Clarifying baseline rebuild test distinctions**
**Planning slug mismatch fix and integration test**
**Summarizing recurring output-ownership issue**
**Clarifying build error propagation and import behavior**
**Planning concise findings with headers**
codex
## Findings

1. **High — the pruner still trusts a declaration, not an observed write.**

`CONTROL_PART_PAGES` is derived from `partitions()`, but filenames are independently reconstructed with `f"controls-{key.lower()}.html"`. The actual filename policy remains the builder’s local `slug()` function. Therefore the comment that the builder is “the only thing that knows” is false: two components still encode the mapping.

They diverge if:

- `slug()` changes to `controls-d-1.html`;
- the builder adds an auxiliary `controls-summary.html`;
- a partition is conditionally skipped;
- the builder returns success without writing;
- the viewer is run standalone after partitions change but before the controls builder runs.

The last case is already reproducible. I removed `controls-d1.html` in a temporary copy and ran `build_viewer.py`. It exited **0**, did not restore the page, and wrote a sitemap naming the missing page:

```text
rc=0 expected_page_exists=no sitemap_names_missing=yes
wrote 30 record pages + index, llms.txt, sitemap.xml
```

This is precisely a green signal not downstream of publication. See [build_viewer.py](/home/reed/git/open-asi-governance-forum/tools/build_viewer.py:1254) and the builder’s actual cleanup/write ownership in [build_controls_page.py](/home/reed/git/open-asi-governance-forum/tools/build_controls_page.py:1634).

The clean design is simpler: `build_controls_page.py` already deletes its own stale `controls-*.html` files. The viewer should not delete another generator’s namespace. Alternatively, the builder should return/write an output receipt containing the paths it actually wrote, and the viewer should consume that receipt—not `partitions()`.

2. **High — the new integrity cases can pass against pre-existing pages without proving the builder wrote them.**

The temporary repository is seeded with the current working tree, including generated `docs/`. At [test_integrity.py](/home/reed/git/open-asi-governance-forum/tools/test_integrity.py:641), the relevant rebuild’s return code is discarded. The later cases then inspect whatever files are present.

A builder changed to `main(): return 0` without writing anything would leave the seeded pages untouched:

- baseline rebuild returns 0;
- every expected file exists from the seed;
- the orphan check passes;
- the suite reports green.

CI’s delete-`docs/`-first regeneration should catch this later, but the local integrity suite and ordinary rebuild would remain falsely green—the repository’s documented failure pattern.

The discriminating integration test is:

1. Run the controls builder.
2. Record the actual `docs/controls*.html` names and hashes.
3. Run the viewer.
4. Assert the exact set and bytes survive.
5. Check every subprocess return code immediately.

That test is downstream of both the write and the prune.

3. **Medium — the new imported input is omitted from the viewer’s provenance digest.**

`build_viewer.py` claims `_INPUTS` records every generator input, but `build_controls_page.py` now affects the landing page, `llms.txt`, sitemap, and pruning without going through `read_input()`. Its hash is absent from `inputs_digest()`.

Consequently, controls data or partition changes can alter viewer output while its claimed “Rendered from inputs” digest does not account for the source that caused the change. See [build_viewer.py](/home/reed/git/open-asi-governance-forum/tools/build_viewer.py:44).

4. **Medium — import-time execution is currently non-destructive, but creates a brittle and unnecessarily broad dependency.**

At present, importing `build_controls_page.py` does not write files because `main()` is guarded. It does, however:

- mutate `sys.path`;
- import `build_round_pages`;
- construct the entire control register;
- execute all top-level initialization;
- do so **twice** under separate synthetic module names.

An absent file, top-level exception, or `partitions()` exception is loud: `build_viewer` aborts during import, before pruning, with a nonzero traceback. That concern is therefore not silent today.

There is **no current circular import**: the controls builder imports `build_round_pages`, not `build_viewer`. If it later imports `build_viewer`, however, execution as `__main__` can load a second `build_viewer` module while the first is running; ordinary imports can instead expose a partially initialized module. Expect duplicate initialization or attribute errors, not a reliable controlled failure.

It also means importing `build_viewer` merely for `CSS`, as [build_predictions_view.py](/home/reed/git/open-asi-governance-forum/tools/build_predictions_view.py:209) does, now depends on the controls builder loading successfully.

5. **Low but concrete — the published register says “The four parts” while listing eight.**

Every generated controls page and the complete Markdown download retain `## The four parts`, followed by A, B, C, and D1–D5. All 116 integrity cases passed despite this visible contradiction. The source is [build_controls_page.py](/home/reed/git/open-asi-governance-forum/tools/build_controls_page.py:1457).

## Specific answers

- `docs/controls-*.html` does **not** match `controls.md` or `controls.html`. It matches only top-level `.html` names beginning `controls-`, including any future auxiliary page using that prefix. It is not recursive.
- The glob and dynamic import run against `c`, which is a temporary checkout seeded from the current working tree.
- Importing that checkout’s builder is correct. Importing the live working-tree module would compare checkout artifacts against different code and would be a real defect.
- If the builder’s `main()` raises during the official rebuild, the rebuild stops before the viewer, so that failure is loud. Importing `partitions()` does not prove `main()` ran successfully, however, which is the central defect here.
tokens used
73,037
