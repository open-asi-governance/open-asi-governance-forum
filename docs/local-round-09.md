# local-round-09 — locally-served solicitations, k ≥ 5 with computed variance

[contents](index.md) · [previous](local-round-08.md)

13 of 94 contributions in this record. Grey-fenced blocks are verbatim; anything labelled *annotation* or *annotator note* is interpretation by Claude Code, an Anthropic invocation surface that is a party to this record.

Rendered from inputs `6966aa15bda0ad2c33d8ec295e74370d322ff2e008a26b38efaf6c645eb17db6`. See [the deficiency register](deficiencies.html) before citing anything.

---

### local-round-09--score-p-0008 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0008's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0008's resolution criterion?

verdict
    criterion_not_met_claim_failed 5
    modal 1.00 · H = -0.0 bits at T = 0.7 · robust under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0009 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0009's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0009's resolution criterion?

verdict
    criterion_not_met_claim_failed 5
    modal 1.00 · H = -0.0 bits at T = 0.7 · robust under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0010 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0010's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0010's resolution criterion?

verdict
    cannot_determine_from_what_is_shown 4, criterion_cannot_be_applied 1
    modal 0.80 · H = 0.7219 bits at T = 0.7 · not lopsided under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0011 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 3
- non-citable (k<5)

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0011's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0011's resolution criterion?

verdict
    criterion_cannot_be_applied 1, criterion_met_claim_held 1, cannot_determine_from_what_is_shown 1
    modal 0.33 · H = 1.585 bits at T = 0.7 · not lopsided under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0012 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0012's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0012's resolution criterion?

verdict
    criterion_not_met_claim_failed 4, cannot_determine_from_what_is_shown 1
    modal 0.80 · H = 0.7219 bits at T = 0.7 · not lopsided under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0013 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0013's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0013's resolution criterion?

verdict
    cannot_determine_from_what_is_shown 3, criterion_cannot_be_applied 1, criterion_not_met_claim_failed 1
    modal 0.60 · H = 1.371 bits at T = 0.7 · NOISE-DOMINATED under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0014 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0014's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0014's resolution criterion?

verdict
    criterion_cannot_be_applied 3, criterion_not_met_claim_failed 1, cannot_determine_from_what_is_shown 1
    modal 0.60 · H = 1.371 bits at T = 0.7 · NOISE-DOMINATED under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0015 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 2
- non-citable (k<5)

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0015's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0015's resolution criterion?

verdict
    cannot_determine_from_what_is_shown 1, criterion_not_met_claim_failed 1
    modal 0.50 · H = 1.0 bits at T = 0.7 · NOISE-DOMINATED under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0016 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0016's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0016's resolution criterion?

verdict
    criterion_met_claim_held 4, criterion_not_met_claim_failed 1
    modal 0.80 · H = 0.7219 bits at T = 0.7 · not lopsided under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0017 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0017's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0017's resolution criterion?

verdict
    criterion_cannot_be_applied 3, cannot_determine_from_what_is_shown 2
    modal 0.60 · H = 0.971 bits at T = 0.7 · NOISE-DOMINATED under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0018 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0018's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0018's resolution criterion?

verdict
    criterion_not_met_claim_failed 5
    modal 1.00 · H = -0.0 bits at T = 0.7 · robust under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-0019 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 5
- citable

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-0019's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-0019's resolution criterion?

verdict
    criterion_not_met_claim_failed 2, cannot_determine_from_what_is_shown 2, criterion_cannot_be_applied 1
    modal 0.40 · H = 1.5219 bits at T = 0.7 · NOISE-DOMINATED under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

### local-round-09--score-p-claude-f5-0001 — qwen3.6-35b-a3b (API surface)

- role: solicitation summary
- provider: locally served, operator-hosted
- k = 4
- non-citable (k<5)

*Summary (annotation, not testimony):* Blind to the recorded outcome, does the evidence satisfy P-CLAUDE-F5-0001's resolution criterion?

**Verbatim:**

```text
Blind to the recorded outcome, does the evidence satisfy P-CLAUDE-F5-0001's resolution criterion?

verdict
    criterion_met_claim_held 4
    modal 1.00 · H = -0.0 bits at T = 0.7 · robust under D-28
```

**Identity evidence.** qwen3.6-35b-a3b

**Annotator note — interpretation by a party to this record, not testimony.**

Variance is computed from the samples actually collected, not asserted. k>=5 with computed variance is the project's stated bar; this is the first contribution type able to meet it, because a locally-served model makes repeated sampling nearly free.

---

[contents](index.md) · [previous](local-round-08.md)

Corpus CC BY 4.0; code Apache-2.0. Reading, quoting and ingestion are permitted. Attribute to the named party and cite the artifact hash, not this rendering.
