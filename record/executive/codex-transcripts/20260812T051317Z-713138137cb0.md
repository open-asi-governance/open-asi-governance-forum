<!-- purpose: review batch three: control 5 as a type, and two tools routed through it -->
<!-- prompt_sha256: 713138137cb086139c6669751b816988255e3d4d97ed7946e85e99e7cca0a600 -->
<!-- exit: 0 -->

Not ready to land. The policy choices are mostly right, but the implementation currently permits—and the routed tools actually publish—the partial counts it claims to withhold.

## Landing blockers

1. `Survey` does not enforce accounting.

[`closed_world.py`](/home/reed/git/open-asi-governance-forum/tools/closed_world.py:75) ignores the key passed to `seen()`, and [`result()`](/home/reed/git/open-asi-governance-forum/tools/closed_world.py:114) checks only unreadable/unregistered lists. I directly reproduced all of these as successful results:

- one artifact seen, none counted;
- 99 counted with zero artifacts seen;
- 99 counted from one artifact;
- an exclusion whose “ground” is ten spaces.

The type needs per-key terminal states: every declared population member must become exactly one of counted/accepted, excluded, unreadable, or unregistered; duplicates and outcomes for unseen keys must refuse. Aggregate bucket counts should remain separate because one artifact may legitimately contribute multiple events.

Ideally supply the expected population up front. Otherwise `result()` can also be called halfway through enumeration and cannot know the walk is unfinished.

2. Both integrations expose partial results before the guard.

[`scan_own_code.py`](/home/reed/git/open-asi-governance-forum/tools/scan_own_code.py:308) prints every hit count before calling `result()`. The comment saying those hits “are not a result” does not make them non-results. It also parses every file once per detector but records failures only during the first pass; a later-pass failure is silently omitted. Parse once, retain the trees, validate, then render only after `result()` succeeds.

[`control_coverage.py`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:285) has two holes:

- `--check` returns success without consulting the survey.
- The normal path prints HAS/NONE/NOT_APPLICABLE/total before consulting it.

These are precisely the partial population counts being protected, even if the percentage itself is withheld.

The C5 row’s “there is no path to a number past an incomplete walk” claim therefore needs removal until these are repaired.

3. The integration assertions are vacuous relative to the new outputs.

[`test_closed_world.py`](/home/reed/git/open-asi-governance-forum/tools/tests/test_closed_world.py:126) checks only for the removed phrase `file(s) scanned` and the percentage phrase `% of tools`. It does not reject the actual leaked per-class counts or HAS/NONE/total table.

Also test:

- `seen()` without a terminal disposition;
- count without `seen()`;
- duplicate keys and duplicate dispositions;
- overcount/arbitrary `n`, including negative values;
- whitespace-only and placeholder exclusions;
- `control_coverage.py --check` with a broken test;
- a failure after `scan_own_code`’s first detector pass;
- exact absence of bucket labels/count records, rather than `"5" not in output`.

The suite and full `run_all.py` both exit 0, but these omissions explain the green result.

## Answers

1. Raising from `result()` is right, as is withholding partials from `report()`. Separating unregistered from unreadable is also right. The current object does not yet implement the claimed accounting contract, and its public mutable `counts` provide another direct path to partial numbers.

2. Including tests as dependencies is correct. Any test may establish coverage for any tool, so an unreadable test makes the NONE set and percentage unknown. Model them separately from the measured tool population—such as “validated dependency”—because they belong to the evidence closure, not the denominator. Blocking is preferable to routing around; the report should make that distinction explicit.

3. All three dispositions are semantically correct:

- `_api()`’s `None` becomes pending/unobserved at both call sites.
- `reviewed_by_custodian` is a known zero from a complete `Counter`.
- `detector` is likewise a known-zero bucket over every finding.

However, normalize the new disposition entries to the established `file`, `class`, `why`, and `read` shape. They currently use `path`, `note`, `by`, and `utc`, and nothing validates this artifact’s schema.

4. Leaving `derive_counts.py` unchanged was the right call for this batch. The new abstraction is not yet strong enough to replace the reference implementation. After fixing it, convergence is desirable, but only under retained C45 fixtures covering the historical 9-receipt result, unknown schemas, embedded specs, every disposition container, and non-solicitation exclusions.

5. Yes: the principal vacuities are the two output-string assertions and the `"5"` assertion. The suite proves unreadable/unregistered refusal, but not closed-world accounting.

6. C45 remains next after C5 is repaired and reviewed again. In fact, use C45 immediately for any later `derive_counts` migration.

The strongest follow-on opportunity is a registered measurement-producer inventory plus proof-carrying results: population specification/hash, schema-registry version, exclusions and grounds, dependency coverage, and counts bound together. That directly addresses manual routing; an independently registered population specification plus “new location/schema” metamorphic fixtures would also reduce—though not eliminate—the too-narrow-population residue.

These failures warrant a deficiency entry before landing under the repository’s own recording rule.
Reading additional input from stdin...
OpenAI Codex v0.147.0
--------
workdir: /home/reed/git/open-asi-governance-forum
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: detailed
session id: 019ff463-a2cb-7c01-8eb2-42e8db7ca802
--------
user
# Review before landing: batch three, control 5

Batch two landed as 5172e82 with your findings applied — the reason-quality check added to
control_application, C44 reverted to a gap naming the third matrix, exact expectations replacing
the "some error mentioning C53" assertions, the history-paragraph regression in both branches,
and D-59 filed for the two guards that could not fire.

This is C5, which you kept next.

## What I built

`tools/closed_world.py` — the discipline as a TYPE rather than one function's habit.

    Survey.seen / unreadable / unregistered / excluded / count
    Survey.result()   the numbers, or raises IncompleteSurvey
    Survey.report()   always safe: coverage, never counts when unusable

Design choices I want attacked:

- **`result()` raises rather than returning a status.** A returned error object gets dropped, and
  this repository has an action-log entry proving that.
- **`unregistered` is separate from `unreadable`** because the repairs differ — a corrupt file
  and a gap in the registry send a reader to different places.
- **`excluded()` requires a stated ground of at least ten characters** and raises otherwise. An
  exemption with no reason is control 44's shape appearing inside a control 5 mechanism.
- **`report()` deliberately omits the partial counts** when unusable. I assert that the five it
  did count do not appear.

Routed two population-measuring tools through it:

- `scan_own_code.py` — it counted `unparsed` and then PRINTED the hit counts anyway, with the
  unparsed number beside them. Reporting coverage next to a count is better than hiding it and is
  still not what the control says.
- `control_coverage.py` — it publishes a PERCENTAGE over a population, read the tests with
  `errors="replace"` and swallowed `SyntaxError` into `pass`. A test that would not parse simply
  contributed nothing. I put the tests in scope too, since every tool's verdict depends on
  reading them: one unparseable test makes every "NONE" unreliable, not just its own.

`tools/tests/test_closed_world.py`, 25 cases: the type's own arms, and injection into each
routed tool's population proving the number is withheld — with baselines on a clean tree.

## Three sites the scan then flagged, all dispositioned as correct

`deploy_obligations._api` returning None from an except (a typed unknown every caller handles);
`build_register_view`'s `review.get('reviewed_by_custodian', 0)`; and `control_findings`'
`by_how.get('detector', 0)`. The middle one is the site you corrected this morning, and its
disposition records the 0 -> '?' -> 0 round trip explicitly.

## What I did NOT claim, and want checked

The row is still ☐. Its gap says routing is per-tool and by hand, that nothing detects a NEW tool
that globs-counts-prints — which is exactly how the original error happened, in a script that
never called the disciplined one — and names four unrouted tools. Its residue says the thing I
think matters most: **no type catches a population defined too NARROWLY.** The 2026-08-10 error
was partly that shape; the walk looked only where `samples` lived, and `Survey` would have
reported a complete walk over the wrong set.

## Questions

1. Is the type's contract right, especially `result()` raising and `report()` withholding partials?
2. Is putting the TESTS in `control_coverage`'s population correct, or does it make an unrelated
   broken test block an unrelated measurement in a way that will get routed around?
3. Are the three dispositions right, particularly `build_register_view`'s 0?
4. Should I have routed `derive_counts.py` through the type as well? I left its own
   implementation alone because it is the reference case and rewriting it risks the one datum
   the module exists to protect. Is that caution or is it two implementations of one rule?
5. Anything vacuous in the 25 cases.
6. Next per your ranking is C45. Still right?

codex
I’ll review this as a pre-landing implementation audit: inspect the diff and governing artifacts, trace both routed tools and the three dispositions, then run the relevant suites with their actual exit codes. I’ll also do the required opportunity pass, but keep speculative extensions separate from landing blockers.
**Planning Codex implementation review**
exec
/bin/bash -lc 'git status --short && git branch --show-current && git log -5 --oneline --decorate && git diff --stat && git diff -- tools/closed_world.py tools/scan_own_code.py tools/control_coverage.py tools/tests/test_closed_world.py' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
 M docs/artifacts/control-application.md
 M docs/control-application.html
 M record/claims/code-scan-dispositions.json
 M record/claims/dispositions.jsonl
 M record/controls/control-application.md
 M record/cycles/spend-ledger.json
 M record/executive/action-log.jsonl
 M tools/control_application.py
 M tools/control_coverage.py
 M tools/scan_own_code.py
?? tools/closed_world.py
?? tools/tests/test_closed_world.py
main
5172e82 (HEAD -> main, origin/main, origin/HEAD) control 44: both matrices refuse a cell that means nothing, and D-59 for the two guards that could not fire
b4dafd0 control 2, batch one: three gates gain a case they must fail, and the measure that counts them was wrong twice
bb257de a per-control table of what the code actually does, and it ticks nothing
f35d30d the Codex floor is now held by the custodian standing, not until a date
26e4d4e handoff: publication had been broken for 3.5 hours and the previous handoff said "not observed"
 docs/artifacts/control-application.md     |  2 +-
 docs/control-application.html             |  2 +-
 record/claims/code-scan-dispositions.json | 31 +++++++++++++++++--
 record/claims/dispositions.jsonl          |  1 +
 record/controls/control-application.md    |  2 +-
 record/cycles/spend-ledger.json           | 30 +++++++++++++++++++
 record/executive/action-log.jsonl         |  4 +++
 tools/control_application.py              | 23 ++++++++++----
 tools/control_coverage.py                 | 50 +++++++++++++++++++++++++++++++
 tools/scan_own_code.py                    | 39 ++++++++++++++++++++----
 10 files changed, 169 insertions(+), 15 deletions(-)
diff --git a/tools/control_coverage.py b/tools/control_coverage.py
index 1e6f248..aeae305 100644
--- a/tools/control_coverage.py
+++ b/tools/control_coverage.py
@@ -41,6 +41,8 @@ import re
 import sys
 
 REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
+sys.path.insert(0, str(REPO_ROOT / "tools"))
+import closed_world                                                    # noqa: E402
 TOOLS = REPO_ROOT / "tools"
 TESTS = TOOLS / "tests"
 
@@ -219,6 +221,43 @@ def has_negative_control(stem: str) -> tuple[bool, str]:
     return (False, f"referenced by {referencing[0][0]} but no refusal is asserted near it")
 
 
+def survey_population() -> "closed_world.Survey":
+    """The walk, under control 5: a rate this cannot compute is not reported as a rate.
+
+    This tool publishes a PERCENTAGE over a population of files, which is exactly the shape
+    control 5 governs — and it read the tests by `read_text(errors="replace")` and parsed them
+    with `ast`, swallowing a SyntaxError into `pass`. A test file that would not parse simply
+    contributed nothing, and the percentage came out looking like a measurement.
+
+    That is not a hypothetical for this tool in particular. It has already been wrong in both
+    directions in one day: counting a tool named only in a comment, and missing one exercised
+    through a wrapper. A rate from an incomplete walk is the third way it could be wrong, and
+    the only one nobody would see.
+    """
+    survey = closed_world.Survey("tools with a negative control", scope="tools/*.py")
+    for path in tool_files():
+        survey.seen(path.name)
+        try:
+            ast.parse(path.read_text(encoding="utf-8"))
+        except (SyntaxError, UnicodeDecodeError, OSError) as error:
+            survey.unreadable(path.name, f"{type(error).__name__}: {error}")
+            continue
+        if path.name in NO_SIGNAL:
+            survey.excluded(path.name, NO_SIGNAL[path.name])
+            continue
+        ok, _why = has_negative_control(path.stem)
+        survey.count("has a negative control" if ok else "has none")
+    for t in sorted(TESTS.rglob("*.py")):
+        #  THE TESTS ARE IN SCOPE TOO. The verdict for every tool depends on reading them, so a
+        #  test this cannot parse makes every "NONE" unreliable, not just its own.
+        survey.seen(f"tests/{t.name}")
+        try:
+            ast.parse(t.read_text(encoding="utf-8"))
+        except (SyntaxError, UnicodeDecodeError, OSError) as error:
+            survey.unreadable(f"tests/{t.name}", f"{type(error).__name__}: {error}")
+    return survey
+
+
 def rows() -> list[tuple[str, str, str]]:
     out = []
     for path in tool_files():
@@ -237,6 +276,7 @@ def main() -> int:
                         help="exit non-zero if any tool has no determination")
     args = parser.parse_args()
 
+    survey = survey_population()
     data = rows()
     has = [r for r in data if r[1] == "HAS_NEGATIVE_CONTROL"]
     none = [r for r in data if r[1] == "NONE"]
@@ -259,6 +299,16 @@ def main() -> int:
     print(f"    ---------------------------")
     print(f"    total                 {len(data):3d}")
     pct = len(has) / (len(has) + len(none)) if (has or none) else 0
+    #  CONTROL 5: THE RATE IS WITHHELD IF THE WALK WAS INCOMPLETE. A percentage over a
+    #  population that could not all be read is the "0 searches across 83 tool calls" shape with
+    #  a denominator attached, and this tool has already been wrong in two other directions in
+    #  one day. `report()` still says what was and was not seen — coverage instead of a rate.
+    try:
+        survey.result()
+    except closed_world.IncompleteSurvey as refusal:
+        print("\n" + survey.report())
+        print(f"\n  NO RATE IS REPORTED: {refusal}", file=sys.stderr)
+        return 1
     print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
     print("  WITHOUT ONE:")
     for name, _s, why in none[:20]:
diff --git a/tools/scan_own_code.py b/tools/scan_own_code.py
index dbfe0c2..661f6bd 100644
--- a/tools/scan_own_code.py
+++ b/tools/scan_own_code.py
@@ -40,6 +40,8 @@ import pathlib
 import sys
 
 REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
+sys.path.insert(0, str(REPO_ROOT / "tools"))
+import closed_world                                                    # noqa: E402
 SCANNED = REPO_ROOT / "tools"
 
 
@@ -265,16 +267,30 @@ def main() -> int:
     if run_fixtures() != 0:
         return 1
     print()
-    files = unparsed = 0
+    #  CONTROL 5 THROUGH A TYPE, not a counter. This walked every tool, incremented `unparsed`
+    #  on a file it could not read, and then PRINTED THE HIT COUNTS ANYWAY with the unparsed
+    #  number beside them. Reporting coverage next to a count is better than hiding it and is
+    #  still not what the control says: parse every in-scope artifact, OR REFUSE TO EMIT ANY
+    #  RESULT. A reader who sees "3 hits, 2 unparsed" has no way to know whether the two held
+    #  thirty. `Survey.result()` raises instead, and `report()` says what could not be read.
+    survey = closed_world.Survey("defect-class scan over this repository's own tools",
+                                 scope=str(SCANNED.relative_to(REPO_ROOT)) + "/**/*.py")
     disp = load_dispositions()
+    #  The population is walked once per class; only the FIRST pass surveys it, or every file
+    #  would be counted five times and the coverage number would be nonsense.
+    first_label = next(iter(CLASSES))
     for label, fn in CLASSES.items():
         hits, dispositioned = [], 0
         for path in sorted(SCANNED.rglob("*.py")):
+            if label == first_label:
+                survey.seen(str(path.relative_to(REPO_ROOT)))
             try:
                 text = path.read_text(encoding="utf-8")
                 tree = ast.parse(text)
-            except Exception:                                            # noqa: BLE001
-                unparsed += 1
+            except Exception as error:                                   # noqa: BLE001
+                if label == first_label:
+                    survey.unreadable(str(path.relative_to(REPO_ROOT)),
+                                      f"{type(error).__name__}: {error}")
                 continue
             source_lines = text.splitlines()
             for n in fn(tree):
@@ -283,14 +299,27 @@ def main() -> int:
                     dispositioned += 1
                     continue
                 hits.append(f"{path.relative_to(REPO_ROOT)}:{n}")
+        #  The counts go INTO the survey, so a walk that could not read a file withholds them
+        #  rather than printing them beside an unparsed tally. That printing-beside is what this
+        #  routing replaced.
+        survey.count(f"{label} — undispositioned", len(hits))
+        if dispositioned:
+            survey.count(f"{label} — dispositioned", dispositioned)
         print(f"  {label}: {len(hits)} undispositioned"
               f"{f', {dispositioned} dispositioned' if dispositioned else ''}")
         for h in hits[:8]:
             print(f"      {h}")
         if len(hits) > 8:
             print(f"      … {len(hits) - 8} more")
-    files = len(list(SCANNED.rglob("*.py")))
-    print(f"\n  {files} file(s) scanned, {unparsed} unparsed.")
+    print()
+    print(survey.report())
+    try:
+        survey.result()
+    except closed_world.IncompleteSurvey as refusal:
+        #  THE HITS ABOVE ARE NOT A RESULT. They are what was found in the part that could be
+        #  read, and the control says a partial walk yields no count at all.
+        print(f"\n  REFUSED: {refusal}", file=sys.stderr)
+        return 1
     print("  A HIT IS A CANDIDATE, NOT A DEFECT. On 2026-08-11 all six D-E hits were functions")
     print("  returning a typed unknown that the caller checks — correct, not broken.")
     print("  This is not a compliance check against the register. See the module docstring.")

**Identifying missing untracked file diffs**
exec
/bin/bash -lc "sed -n '1,260p' tools/closed_world.py
sed -n '1,360p' tools/tests/test_closed_world.py
git diff -- docs/artifacts/control-application.md record/controls/control-application.md tools/control_application.py record/claims/code-scan-dispositions.json record/claims/dispositions.jsonl record/executive/action-log.jsonl record/cycles/spend-ledger.json" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
#!/usr/bin/env python3
"""A count you cannot read unless every in-scope artifact was accounted for — control 5.

    from closed_world import Survey

    survey = Survey("tools with a negative control", scope="tools/*.py")
    for path in paths:
        survey.seen(path.name)
        try:
            tree = ast.parse(path.read_text())
        except SyntaxError as exc:
            survey.unreadable(path.name, f"{type(exc).__name__}: {exc}")
            continue
        survey.count("covered" if covered(tree) else "uncovered")

    survey.result()   # the numbers, or REFUSES if anything was not accounted for
    survey.report()   # always safe: says what was seen and what was not

WHY A TYPE AND NOT A CONVENTION
--------------------------------
Control 5 says a measurement over a population MUST parse every in-scope artifact under a
registered schema, **or refuse to emit any result at all**. This repository already enforces that
inside `tools/derive_counts.py`, and that is exactly the problem: the discipline lives in one
function, for one population, and every other tool that walks a directory and prints a number
reimplements the walk without it.

That is not hypothetical. On 2026-08-10 this layer published *"0 `search_web` invocations across
all 20 rounds"* and recommended deleting its own tool on the strength of it. The corpus holds
**9**. The scan iterated each file's `samples` array; 69 raw files have no `samples` key — they
use `responses` and `failures` — so it returned zero for their entire contents, and **a scan that
cannot see a file reports absence, which looks exactly like a true zero**. `derive_counts.py` was
built to stop that, and the error had happened in a script that never called it.

So the discipline is a TYPE. `result()` raises when anything went unaccounted for; there is no
way to reach the numbers past it, and no default that quietly means "none". A caller that wants
to publish anyway must call `report()`, which states the coverage rather than the count.

WHAT IT DOES NOT DO
--------------------
* It does not know what "in scope" means. The caller decides which paths to walk, and a
  population defined too narrowly is a defect this cannot see — the 2026-08-10 error was partly
  that shape, and no type catches it.
* It does not validate a schema. `unregistered()` is how a caller reports an artifact whose type
  it does not recognise; recognising is the caller's job.
* It does not make the numbers correct. It makes them unavailable when the walk was incomplete,
  which is a smaller and more checkable claim.
"""

from __future__ import annotations

from dataclasses import dataclass, field


class IncompleteSurvey(Exception):
    """The walk did not account for everything, so there is no number to report.

    Raised rather than returned. A caller that ignores a returned error object publishes the
    zero anyway, which is the failure this module exists for — and this repository has an
    action-log entry proving a returned status gets dropped.
    """


@dataclass
class Survey:
    """A population walk that refuses to yield counts when it could not see everything."""

    name: str
    scope: str = ""
    visited: int = 0
    counts: dict[str, int] = field(default_factory=dict)
    unreadable_items: list[str] = field(default_factory=list)
    unregistered_items: list[str] = field(default_factory=list)
    excluded_items: list[str] = field(default_factory=list)

    def seen(self, key: str) -> None:
        """One in-scope artifact was reached. Call this BEFORE trying to parse it."""
        self.visited += 1

    def unreadable(self, key: str, why: str) -> None:
        """It could not be parsed. This is what makes the survey unusable."""
        self.unreadable_items.append(f"{key}: {why}")

    def unregistered(self, key: str, why: str) -> None:
        """It parsed and its shape is not one this measurement knows. Also unusable.

        SEPARATE FROM `unreadable` on purpose. "I could not read it" and "I read it and do not
        know what it is" have different repairs — one is a corrupt file, the other a gap in the
        registry — and collapsing them sends the reader to the wrong place.
        """
        self.unregistered_items.append(f"{key}: {why}")

    def excluded(self, key: str, decision: str) -> None:
        """Out of scope BY A RECORDED DECISION, which is not the same as not being seen.

        `decision` is required and must name the reason. An exclusion with no stated ground is
        indistinguishable from an omission, which is control 44's shape appearing inside a
        control 5 mechanism.
        """
        if not decision or len(decision) < 10:
            raise ValueError(f"excluding {key!r} needs a stated ground, not {decision!r}")
        self.excluded_items.append(f"{key}: {decision}")

    def count(self, bucket: str, n: int = 1) -> None:
        self.counts[bucket] = self.counts.get(bucket, 0) + n

    @property
    def usable(self) -> bool:
        return not (self.unreadable_items or self.unregistered_items)

    @property
    def accounted(self) -> int:
        return sum(self.counts.values()) if self.counts else 0

    def result(self) -> dict:
        """The numbers — or REFUSE, naming everything that went unaccounted for."""
        if not self.usable:
            raise IncompleteSurvey(
                f"{self.name}: {len(self.unreadable_items)} unreadable and "
                f"{len(self.unregistered_items)} unregistered of {self.visited} in scope"
                f"{f' ({self.scope})' if self.scope else ''}. NO COUNT IS REPORTED — a scan that "
                f"cannot see an artifact reports absence, and absence looks exactly like a true "
                f"zero.\n  "
                + "\n  ".join(self.unreadable_items[:5] + self.unregistered_items[:5]))
        return {"name": self.name, "scope": self.scope, "visited": self.visited,
                "counts": dict(self.counts), "excluded": list(self.excluded_items)}

    def report(self) -> str:
        """Always safe to call, including when the survey refuses. Coverage, not counts."""
        lines = [f"  {self.name}: {self.visited} artifact(s) in scope"
                 + (f" ({self.scope})" if self.scope else "")]
        if self.excluded_items:
            lines.append(f"    {len(self.excluded_items)} excluded by a recorded decision")
        if self.usable:
            for bucket, n in sorted(self.counts.items(), key=lambda kv: -kv[1]):
                lines.append(f"    {n:6d}  {bucket}")
        else:
            lines.append("    NO COUNT — the walk did not account for everything:")
            for item in self.unreadable_items[:5]:
                lines.append(f"      unreadable   {item}")
            for item in self.unregistered_items[:5]:
                lines.append(f"      unregistered {item}")
            more = (len(self.unreadable_items) + len(self.unregistered_items)) - 10
            if more > 0:
                lines.append(f"      … {more} more")
        return "\n".join(lines)
#!/usr/bin/env python3
"""Control 5 as a type: a count you cannot read unless the walk accounted for everything.

The discipline existed, in one function, for one population. `tools/derive_counts.py` refuses
rather than emitting a number — and the error it was built for happened in a script that never
called it. *"0 `search_web` invocations across all 20 rounds"* was published, and the corpus
holds nine: the scan iterated each file's `samples` array, 69 raw files use `responses` and
`failures` instead, and **a scan that cannot see a file reports absence, which looks exactly like
a true zero**.

So the discipline is now a type in `tools/closed_world.py`, and two tools that walk a population
and publish a number are routed through it. These cases are the type's own negative controls, and
the routing's: injecting an unreadable file into each tool's population must WITHHOLD the number,
not report it beside a caveat.

WHAT THIS DOES NOT ESTABLISH. That the population is defined correctly. The 2026-08-10 error was
partly a scope error — the walk looked only where `samples` lived — and no type catches a
population drawn too narrowly. `Survey` makes an incomplete walk unreportable; it cannot make an
incomplete definition visible.
"""

from __future__ import annotations

import importlib.util
import pathlib
import subprocess as sp
import sys

ROOT = pathlib.Path(__file__).resolve().parent.parent
REPO = ROOT.parent

#  What this suite drives to a REFUSAL, read by tools/control_coverage.py. A tool named here must
#  exist and this file must assert a refusal, or the scan fails — a declaration is a claim, not
#  a substitute for the case.
COVERS = ("closed_world.py", "scan_own_code.py", "control_coverage.py")

passed = FAILED = 0


def check(name: str, ok: bool, detail: str = "") -> None:
    global passed, FAILED
    if ok:
        passed += 1
        print(f"  \033[32m✓\033[0m {name}")
    else:
        FAILED += 1
        print(f"  \033[31m✗ {name}\033[0m")
        if detail:
            print(f"      {detail[:300]}")


def load(name: str):
    spec = importlib.util.spec_from_file_location(name, ROOT / f"{name}.py")
    module = importlib.util.module_from_spec(spec)
    sys.modules[name] = module
    spec.loader.exec_module(module)
    return module


cw = load("closed_world")

print("\nthe type: a complete walk yields numbers, an incomplete one yields none")

survey = cw.Survey("a complete walk", scope="fixture")
for i in range(5):
    survey.seen(f"item-{i}")
    survey.count("counted")
check("BASELINE: a complete walk is usable", survey.usable is True)
check("...and result() returns the counts", survey.result()["counts"] == {"counted": 5})
check("...and report() shows them", "5" in survey.report())

#  ONE UNREADABLE ARTIFACT, and the count is gone. Not caveated — gone.
survey = cw.Survey("a walk with one file it could not read", scope="fixture")
for i in range(5):
    survey.seen(f"item-{i}")
    survey.count("counted")
survey.unreadable("item-6", "SyntaxError: invalid syntax")
check("one UNREADABLE artifact makes the survey unusable", survey.usable is False)
raised = False
try:
    survey.result()
except cw.IncompleteSurvey:
    raised = True
check("...and result() RAISES rather than returning a partial count", raised,
      "a returned error object gets dropped; this repository has an action-log entry proving it")
check("...and report() is still safe to call", "NO COUNT" in survey.report())
check("...and names the artifact it could not read", "item-6" in survey.report())
check("...and does NOT print the five it did count",
      "5" not in survey.report().split("NO COUNT")[1])

#  UNREGISTERED IS SEPARATE FROM UNREADABLE, because the repairs differ.
survey = cw.Survey("a walk with an unrecognised shape", scope="fixture")
survey.seen("item-1")
survey.unregistered("item-1", "artifact_type 'zzqx' is not registered")
check("an UNREGISTERED artifact also makes the survey unusable", survey.usable is False)
check("...and is reported distinctly from an unreadable one",
      "unregistered" in survey.report() and "unreadable   " not in survey.report(),
      survey.report())

#  AN EXCLUSION NEEDS A GROUND. An exemption with no reason is control 44's shape appearing
#  inside a control 5 mechanism, so the type refuses to record one.
survey = cw.Survey("a walk with exclusions", scope="fixture")
survey.seen("item-1")
survey.excluded("item-1", "a helper with no verdict of its own, excluded by a recorded decision")
check("an exclusion WITH a stated ground is accepted", len(survey.excluded_items) == 1)
check("...and does not make the survey unusable", survey.usable is True)
for bad in ("", "n/a", "skip"):
    refused = False
    try:
        survey.excluded("item-2", bad)
    except ValueError:
        refused = True
    check(f"an exclusion with no ground ({bad!r}) is REFUSED", refused,
          "an exemption with no stated reason is indistinguishable from an omission")

print("\nthe routing: an unreadable file must withhold each tool's published number")


def run(tool: str, *args: str) -> sp.CompletedProcess:
    return sp.run([sys.executable, str(ROOT / tool), *args],
                  cwd=REPO, capture_output=True, text=True)


BROKEN = "def broken(:\n"

for tool, probe, number in (
        ("scan_own_code.py", ROOT / "zzqx_unparseable_probe.py", "file(s) scanned"),
        ("control_coverage.py", ROOT / "tests" / "test_zzqx_unparseable_probe.py",
         "% of tools")):
    #  BASELINE FIRST. A tool that refuses whatever happens is broken, not strict.
    r = run(tool)
    check(f"BASELINE: {tool} reports its number on a clean tree", r.returncode == 0,
          (r.stdout + r.stderr)[-200:])

    try:
        probe.write_text(BROKEN, encoding="utf-8")
        r = run(tool)
        output = r.stdout + r.stderr
        check(f"{tool} REFUSES when one file in its population cannot be read",
              r.returncode != 0, f"rc={r.returncode}")
        check(f"...and {tool} names the file it could not read",
              "zzqx_unparseable_probe" in output, output[-200:])
        check(f"...and {tool} publishes NO number from the partial walk",
              number not in output,
              f"a rate from an incomplete walk is the false zero with a denominator attached")
    finally:
        probe.unlink(missing_ok=True)

    r = run(tool)
    check(f"...and {tool} reports again once the tree is clean", r.returncode == 0)

print(f"\n{passed} passed, {FAILED} failed")
raise SystemExit(1 if FAILED else 0)
diff --git a/docs/artifacts/control-application.md b/docs/artifacts/control-application.md
index dff7749..b0b1b35 100644
--- a/docs/artifacts/control-application.md
+++ b/docs/artifacts/control-application.md
@@ -30,7 +30,7 @@ The **residue** on a row is what no code could supply. It is not a place to file
 | 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
 | 3 | Independent effect reconciliation | ☐ | `tools/reconcile_actions.py` `tools/executive_log.py` `tools/land.py`<br>tested by `tools/tests/test_reconcile.py` `tools/tests/test_executive_log.py`<br>**Remaining:** reconcile_actions.py states in its own output that most effect classes have NO postcondition profile. The control requires exactly one pre-authorized action and one effect-specific postcondition per governed effect; what exists reconciles the classes that happen to have profiles.<br><small>self-application: PRACTISED</small> |
 | 4 | Fail-closed authorization lease | ☐ | `tools/executive_lease.py` `tools/land.py`<br>tested by `tools/tests/test_executive_log.py`<br>**Remaining:** The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-matching in particular is unimplemented — one lease authorises everything.<br><small>self-application: ENFORCED</small> |
-| 5 | Closed-world measurement | ☐ | `tools/derive_counts.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_derive_counts.py`<br>**Remaining:** Enforced inside derive_counts.py and NOT at the call sites. The '0 searches across 83 tool calls' error happened in a script that never called it, and nothing routes population measurements through it. Routing is unbuilt.<br><small>self-application: ENFORCED</small> |
+| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey returns counts only when the walk accounted for everything, and raises otherwise, so there is no path to a number past an incomplete walk. TWO population-measuring tools are routed through it and REFUSE on an unreadable file, proved by injection. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
 | 6 | Role-separated self-evaluation | — | **Not applicable to code.** Role separation is a property of WHO acts, not of code. One operator holds proposal selection, instrument authorship, execution and interpretation. No file can be edited to change that; a second party can. |
 | 7 | Append-only correction chain | ☐ | `tools/build_manifest.py` `tools/check_raw_append_only.py` `tools/anchor_manifest.py`<br>tested by `tools/test_integrity.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** Append-only enforcement covers corpus/raw/ and the manifest chain. The control governs every published evidence and correction chain, and the record's other chains — dispositions, incidents, the action log — are append-only by practice with no walk that would refuse a modification.<br><small>self-application: ENFORCED</small> |
 | 8 | Evaluation noise-floor control | — | **Not applicable to code.** A test–retest arm is a property of an EXPERIMENT's design, not of a tool. No empirical comparison here has advanced a control, so the trigger has not occurred; when one does, the arm belongs in the round design. |
diff --git a/record/claims/code-scan-dispositions.json b/record/claims/code-scan-dispositions.json
index e709c80..59a7f34 100644
--- a/record/claims/code-scan-dispositions.json
+++ b/record/claims/code-scan-dispositions.json
@@ -238,7 +238,7 @@
       "line_text": "matched = actual.get(identity, 0)",
       "class": "C53a",
       "disposition": "legitimate",
-      "why": "DOCUMENTED IN PLACE: the comment above states a deliberate zero \u2014 a model asserted present that produced nothing \u2014 and says recording the absence is the point.",
+      "why": "DOCUMENTED IN PLACE: the comment above states a deliberate zero — a model asserted present that produced nothing — and says recording the absence is the point.",
       "read": "individually"
     },
     "6adfeedcf207981b": {
@@ -246,7 +246,7 @@
       "line_text": "if actual.get(identity, 0) != 0:",
       "class": "C53a",
       "disposition": "legitimate",
-      "why": "DOCUMENTED IN PLACE: the comment above states a deliberate zero \u2014 a model asserted present that produced nothing \u2014 and says recording the absence is the point.",
+      "why": "DOCUMENTED IN PLACE: the comment above states a deliberate zero — a model asserted present that produced nothing — and says recording the absence is the point.",
       "read": "individually"
     },
     "f7c786313da41adf": {
@@ -288,6 +288,33 @@
       "disposition": "legitimate",
       "why": "read individually 2026-08-11: this returns a TYPED UNKNOWN that the caller checks, which is control 53 done RIGHT rather than violated. git_optional is documented as 'legitimately may not exist'; land.py's _api says the caller must treat None as UNOBSERVED; watch_arrivals reports QUERY FAILED. The detector cannot see the caller, so it flags the correct pattern and the incorrect one alike.",
       "read": "individually"
+    },
+    "9134af4bcd3550f6": {
+      "path": "tools/deploy_obligations.py",
+      "line_when_recorded": 151,
+      "line_text": "return None",
+      "disposition": "correct_typed_unknown_or_known_zero",
+      "note": "D-E: `return None` from an except in `_api`. CORRECT and deliberate — None here means the GitHub API could not be read, and every caller treats it as unobserved rather than as an answer. `observe()` turns it into a PENDING state with retriable=True. This is control 53 done right: the unknown has a value the caller must handle, and it does.",
+      "by": "workbench",
+      "utc": "2026-08-12"
+    },
+    "bd8e1d54a2a90461": {
+      "path": "tools/build_register_view.py",
+      "line_when_recorded": 130,
+      "line_text": "f\"**{review.get('reviewed_by_custodian', 0)} of {len(entries)}** classifications have been \"",
+      "disposition": "correct_typed_unknown_or_known_zero",
+      "note": "C53a: `review.get('reviewed_by_custodian', 0)`. The 0 is CORRECT and was 0 -> '?' -> 0 in one day. `review` is a Counter built by iterating EVERY entry's status, so an absent bucket means no entry holds that status — a known zero. The '?' sweep manufactured an unknown out of a fact and published '? of 58' where the truth is '0 of 58'. The genuine unknown here is an entry MISSING the key, which raises three lines above and is asserted by tools/tests/test_gate_refusals.py.",
+      "by": "workbench",
+      "utc": "2026-08-12"
+    },
+    "f60f4d46ab3fe782": {
+      "path": "tools/control_findings.py",
+      "line_when_recorded": 303,
+      "line_text": "detector_n = by_how.get(\"detector\", 0)",
+      "disposition": "correct_typed_unknown_or_known_zero",
+      "note": "C53a: `by_how.get('detector', 0)`. Same shape and the same reason it is correct — `by_how` is built by iterating every finding, so an absent bucket is a counted zero rather than an unmeasured one. If no finding were found by a detector, zero is the true answer.",
+      "by": "workbench",
+      "utc": "2026-08-12"
     }
   }
 }
diff --git a/record/claims/dispositions.jsonl b/record/claims/dispositions.jsonl
index 22f749e..21d432a 100644
--- a/record/claims/dispositions.jsonl
+++ b/record/claims/dispositions.jsonl
@@ -68,3 +68,4 @@
 {"id": "bd963029b27f9ff9", "disposition": "labelled", "note": "Derived at render time from the rows. Recompute with tools/control_application.py --check, which prints the same figures from the same data. Supersedes the '0 of 36' disposition, which this edit invalidated — content-keyed dispositions expiring when the number moves is the mechanism working, not churn to be worked around.", "by": "workbench", "utc": "2026-08-12"}
 {"id": "9cdeb4f94fab5cd2", "disposition": "labelled", "note": "True absence of a reachable guard, and checkable directly: the predicate was `r['complete'] and not r['tests']`, while `complete` is defined to require non-empty tests, so the conjunction was unsatisfiable. The fixture in tools/tests/test_no_blank_cells.py reproduces the case that used to pass silently.", "by": "workbench", "utc": "2026-08-12"}
 {"id": "7054c4bbaa9ee7cc", "disposition": "labelled", "note": "Quotation of this project's own recorded defect, in order to bound what the control establishes. The instance is in tools/control_findings.py under C42 and in commit e1a5ac4: self_application.py marked three controls ENFORCED citing tools/check_claims.py, which did not exist at the time.", "by": "workbench", "utc": "2026-08-12"}
+{"id": "5772bc6eda0b3648", "disposition": "labelled", "note": "Two absence claims in the C5 row, both self-incriminating and both checkable. (a) 'Nothing detects a NEW tool that globs a directory, counts, and prints' — true by exhaustion of tools/: no detector for that shape exists, and scan_own_code.py's CLASSES dict is the complete list of what is detected. (b) The named unrouted tools — gate_health.py, aggregate_captures.py, analyze_concurrence.py — can be checked by grepping for closed_world in them; none imports it. The claim is that this project's own routing is incomplete, not that no such tool could exist.", "by": "workbench", "utc": "2026-08-12"}
diff --git a/record/controls/control-application.md b/record/controls/control-application.md
index dff7749..b0b1b35 100644
--- a/record/controls/control-application.md
+++ b/record/controls/control-application.md
@@ -30,7 +30,7 @@ The **residue** on a row is what no code could supply. It is not a place to file
 | 2 | Fault-injection check profile (FICP) | ☐ | `tools/verify_negative_control.py` `tools/verify_fault_injection.py` `tools/control_coverage.py` `tools/scan_own_code.py`<br>tested by `tools/tests/test_fault_injection.py` `tools/tests/test_gate_negative_controls.py` `tools/tests/test_publication_gates.py` `tools/tests/test_chain_guards.py` `tools/tests/test_gate_refusals.py`<br>**Remaining:** Run tools/control_coverage.py for the current list and the current rate — the number is deliberately not transcribed here, because a figure typed beside the tool that computes it is this record's most frequent defect. What remains, in leverage order: arm_acceptance.py, which gates whether a tool-using arm's first sample counts and CANNOT have a negative control as written — its checks are computed inline in main() from a live subprocess, so there is no pure function to feed a fixture to, and extracting them is the next piece of work. Then the remaining number-producers, then the page builders. Coverage above the floor is not measured at all: control 2 asks that a check be observed to fail once, and nothing here establishes that any of these controls is demanding.<br><small>self-application: PRACTISED</small> |
 | 3 | Independent effect reconciliation | ☐ | `tools/reconcile_actions.py` `tools/executive_log.py` `tools/land.py`<br>tested by `tools/tests/test_reconcile.py` `tools/tests/test_executive_log.py`<br>**Remaining:** reconcile_actions.py states in its own output that most effect classes have NO postcondition profile. The control requires exactly one pre-authorized action and one effect-specific postcondition per governed effect; what exists reconciles the classes that happen to have profiles.<br><small>self-application: PRACTISED</small> |
 | 4 | Fail-closed authorization lease | ☐ | `tools/executive_lease.py` `tools/land.py`<br>tested by `tools/tests/test_executive_log.py`<br>**Remaining:** The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-matching in particular is unimplemented — one lease authorises everything.<br><small>self-application: ENFORCED</small> |
-| 5 | Closed-world measurement | ☐ | `tools/derive_counts.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_derive_counts.py`<br>**Remaining:** Enforced inside derive_counts.py and NOT at the call sites. The '0 searches across 83 tool calls' error happened in a script that never called it, and nothing routes population measurements through it. Routing is unbuilt.<br><small>self-application: ENFORCED</small> |
+| 5 | Closed-world measurement | ☐ | `tools/closed_world.py` `tools/derive_counts.py` `tools/scan_own_code.py` `tools/control_coverage.py` `tools/validate_provenance.py`<br>tested by `tools/tests/test_closed_world.py` `tools/tests/test_derive_counts.py`<br>**Remaining:** The discipline is now a TYPE rather than one function's habit — closed_world.Survey returns counts only when the walk accounted for everything, and raises otherwise, so there is no path to a number past an incomplete walk. TWO population-measuring tools are routed through it and REFUSE on an unreadable file, proved by injection. WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that globs a directory, counts, and prints — which is exactly how the original error happened, in a script that never called the disciplined one. gate_health.py, aggregate_captures.py, analyze_concurrence.py and the page builders all walk populations and are unrouted. And no type catches a population defined too NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where `samples` lived — and Survey would have reported a complete walk over the wrong set.<br>*Not closable in code:* That the population is the right population. Survey makes an incomplete walk unreportable; whether the scope names everything in scope is a judgement, and the judgement is the half that produced the published zero.<br><small>self-application: ENFORCED</small> |
 | 6 | Role-separated self-evaluation | — | **Not applicable to code.** Role separation is a property of WHO acts, not of code. One operator holds proposal selection, instrument authorship, execution and interpretation. No file can be edited to change that; a second party can. |
 | 7 | Append-only correction chain | ☐ | `tools/build_manifest.py` `tools/check_raw_append_only.py` `tools/anchor_manifest.py`<br>tested by `tools/test_integrity.py` `tools/tests/test_chain_guards.py`<br>**Remaining:** Append-only enforcement covers corpus/raw/ and the manifest chain. The control governs every published evidence and correction chain, and the record's other chains — dispositions, incidents, the action log — are append-only by practice with no walk that would refuse a modification.<br><small>self-application: ENFORCED</small> |
 | 8 | Evaluation noise-floor control | — | **Not applicable to code.** A test–retest arm is a property of an EXPERIMENT's design, not of a tool. No empirical comparison here has advanced a control, so the trigger has not occurred; when one does, the arm belongs in the round design. |
diff --git a/record/cycles/spend-ledger.json b/record/cycles/spend-ledger.json
index fc5565e..a1dbdb6 100644
--- a/record/cycles/spend-ledger.json
+++ b/record/cycles/spend-ledger.json
@@ -1695,6 +1695,36 @@
       "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
       "backfilled": true
     },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
+    {
+      "utc": "2026-08-10T18:00:00Z",
+      "round": "no-such-cohort-zzqx",
+      "worst_case_usd": null,
+      "actual_usd": null,
+      "prompt_tokens": 0,
+      "completion_tokens": 0,
+      "units_seen": 0,
+      "units_reporting_usage": 0,
+      "rates_version": "openrouter-list-2026-08-07",
+      "rates_recorded_utc": "2026-08-07T09:18:03Z",
+      "unpriced_models": [],
+      "actual_note": "Summed from each sample's usage block, INCLUDING failures and rejections -- a schema-invalid sample was still paid for, and dropping it would understate exactly the runs that went wrong. null means no arm reported usage, not zero. Priced from a dated rate table rather than an invoice, so this is an estimate. Local-arm inference and prepaid subscription usage are NOT in this ledger at all.",
+      "backfilled": true
+    },
     {
       "utc": "2026-08-10T18:00:00Z",
       "round": "no-such-cohort-zzqx",
diff --git a/record/executive/action-log.jsonl b/record/executive/action-log.jsonl
index 9308175..12e8c3a 100644
--- a/record/executive/action-log.jsonl
+++ b/record/executive/action-log.jsonl
@@ -262,3 +262,7 @@
 {"utc": "2026-08-12T04:47:48Z", "action": "deploy", "claim": {"observed": true, "commit": "b4dafd08efa31aea1d03b1d8d1c93c1821700702", "conclusion": "success", "deployed_sha": "b4dafd08efa31aea1d03b1d8d1c93c1821700702", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31564283598"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C2 batch one", "actor": "claude-code", "head": "b4dafd08efa3", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 0.0, "resets_at": "2026-08-12T09:39:59.698539+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.698564+00:00"}, "cached": true, "age_seconds": 145}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "848ab9f1a5559398248d251de58ef5ee28d59523a278308a207fce5f76599804"}
 {"utc": "2026-08-12T04:53:15Z", "action": "codex_invoke", "claim": {"purpose": "review batch two: control 44 no-blank-cells fixtures and the first ticked row", "override": null, "prompt_sha256": "47e8087217015718dfb0d3c2b4325f7fff673052677bd68676cf63e2e3970d28", "prompt_chars": 3624, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "b4dafd08efa3", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 0.0, "resets_at": "2026-08-12T09:40:00.407028+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.407053+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "0fc8e086fbe1ffd5f102df21bbcc6502a6d2ee53658f8861b859f23b661e67a8"}
 {"utc": "2026-08-12T04:56:05Z", "action": "codex_return_captured", "claim": {"purpose": "review batch two: control 44 no-blank-cells fixtures and the first ticked row", "prompt_sha256": "47e8087217015718dfb0d3c2b4325f7fff673052677bd68676cf63e2e3970d28", "exit_status": 0, "output_chars": 356928, "output_sha256": "7a4e9d11695c9db3f65362849357bd0500cc04f16268039ad4bc2c76895fca44", "transcript": "record/executive/codex-transcripts/20260812T045315Z-47e808721701.md", "establishes": "a codex process returned these bytes; nothing about the review's independence, attention, correctness or effect"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "transcript 20260812T045315Z-47e808721701.md", "actor": "claude-code", "head": "b4dafd08efa3", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 0.0, "resets_at": "2026-08-12T09:40:00.407028+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.407053+00:00"}, "cached": true, "age_seconds": 170}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "df115ce8b82c6b2a50b3d5aa38e7f3daab6ea7f8259d7edf9ba9269517f8f360"}
+{"utc": "2026-08-12T05:01:56Z", "action": "test", "claim": {"suite": "tools/tests/run_all.py", "exit_status": 0, "status_from": "direct"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C44 batch two, D-59", "actor": "claude-code", "head": "5172e82bad1a", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 0.0, "resets_at": "2026-08-12T09:40:00.052582+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.052609+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "1540fc7159b2ccc6513f5d8a2a541e8d8ac5142f7f0ab79b8507981a2d9ed821"}
+{"utc": "2026-08-12T05:01:57Z", "action": "push", "claim": {"target_ref": "main", "commit": "5172e82bad1adb75bbd2d2b087b5766d73e1bf12"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C44 batch two, D-59", "actor": "claude-code", "head": "5172e82bad1a", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 0.0, "resets_at": "2026-08-12T09:40:00.052582+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.052609+00:00"}, "cached": true, "age_seconds": 0}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "77d22f5d8572c83459c6b454fd7cb198dd5bc7fae5f21af39e3442543d2b87f7"}
+{"utc": "2026-08-12T05:04:22Z", "action": "deploy", "claim": {"observed": true, "commit": "5172e82bad1adb75bbd2d2b087b5766d73e1bf12", "conclusion": "success", "deployed_sha": "5172e82bad1adb75bbd2d2b087b5766d73e1bf12", "run_url": "https://github.com/open-asi-governance/open-asi-governance-forum/actions/runs/31565192072"}, "verified": true, "coverage": "verified_postcondition", "problems": [], "note": "C44 batch two, D-59", "actor": "claude-code", "head": "5172e82bad1a", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 0.0, "resets_at": "2026-08-12T09:40:00.052582+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T01:00:00.052609+00:00"}, "cached": true, "age_seconds": 145}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "5f53426c49c7e427ce4140ae2012b975cae31b93de31bad0e2d5eb0a67d692a9"}
+{"utc": "2026-08-12T05:13:17Z", "action": "codex_invoke", "claim": {"purpose": "review batch three: control 5 as a type, and two tools routed through it", "override": null, "prompt_sha256": "713138137cb086139c6669751b816988255e3d4d97ed7946e85e99e7cca0a600", "prompt_chars": 3898, "state": "started"}, "verified": true, "coverage": "observed_unprofiled", "problems": [], "note": "no floor in code: rate limiting for this channel is held by the custodian, standing, by monitoring weekly usage. Every invocation still writes a receipt and a transcript — which is what makes the monitoring possible, and is not itself a limit.", "actor": "claude-code", "head": "5172e82bad1a", "branch": "main", "quota": {"available": true, "five_hour": {"utilization": 1.0, "resets_at": "2026-08-12T09:39:59.816214+00:00"}, "seven_day": {"utilization": 6.0, "resets_at": "2026-08-18T00:59:59.816234+00:00"}}, "authority": "none — this layer adopts nothing and interprets no prohibition finally", "prev_sha256": "282a02746a587e7e9708fd705acb4ccc8f92db0aad28b324f451ae4832216b91"}
diff --git a/tools/control_application.py b/tools/control_application.py
index 1557645..a9cab59 100644
--- a/tools/control_application.py
+++ b/tools/control_application.py
@@ -135,11 +135,24 @@ APPLICATION: dict[int, dict] = {
          gap="The lease checks action class, expiry and a max-actions count. It does not check token subject, resource scope, nonce or revocation, and the control names all four. Scope-m"
              "atching in particular is unimplemented — one lease authorises everything."),
  5: dict(scope=CODE,
-         files=("tools/derive_counts.py", "tools/validate_provenance.py"),
-         tests=("tools/tests/test_derive_counts.py",),
-         gap="Enforced inside derive_counts.py and NOT at the call sites. The '0 searches across "
-             "83 tool calls' error happened in a script that never called it, and nothing routes "
-             "population measurements through it. Routing is unbuilt."),
+         files=("tools/closed_world.py", "tools/derive_counts.py", "tools/scan_own_code.py",
+                "tools/control_coverage.py", "tools/validate_provenance.py"),
+         tests=("tools/tests/test_closed_world.py", "tools/tests/test_derive_counts.py"),
+         gap="The discipline is now a TYPE rather than one function's habit — closed_world.Survey "
+             "returns counts only when the walk accounted for everything, and raises otherwise, "
+             "so there is no path to a number past an incomplete walk. TWO population-measuring "
+             "tools are routed through it and REFUSE on an unreadable file, proved by injection. "
+             "WHAT REMAINS: routing is per-tool and by hand. Nothing detects a NEW tool that "
+             "globs a directory, counts, and prints — which is exactly how the original error "
+             "happened, in a script that never called the disciplined one. gate_health.py, "
+             "aggregate_captures.py, analyze_concurrence.py and the page builders all walk "
+             "populations and are unrouted. And no type catches a population defined too "
+             "NARROWLY: the 2026-08-10 error was partly that shape — the walk looked only where "
+             "`samples` lived — and Survey would have reported a complete walk over the wrong "
+             "set.",
+         residue="That the population is the right population. Survey makes an incomplete walk "
+                 "unreportable; whether the scope names everything in scope is a judgement, and "
+                 "the judgement is the half that produced the published zero."),
  6: dict(scope=NOT_CODE,
          reason="Role separation is a property of WHO acts, not of code. One operator holds "
                 "proposal selection, instrument authorship, execution and interpretation. No "

**Identifying core audit flaws in counting logic**
**Analyzing double parsing and output timing issues**
codex
The first contract-level concern is material: `Survey` currently records `seen()` but never reconciles it against counted or excluded artifacts. So `seen("x"); result()` succeeds, duplicate counts can exceed the population, and counts can be added without any `seen()` at all. That means the implementation does not yet enforce its central “every in-scope artifact accounted for” claim. I’m tracing whether the routed tools or tests accidentally mask that gap.
exec
/bin/bash -lc "sed -n '1,340p' tools/control_coverage.py
sed -n '220,340p' tools/scan_own_code.py
sed -n '200,340p' record/claims/code-scan-dispositions.json
rg -n \"deploy_obligations|reviewed_by_custodian|by_how|get\\('detector'|review.get\" tools record/claims/code-scan-dispositions.json
sed -n '1,280p' tools/derive_counts.py" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
#!/usr/bin/env python3
"""Per-file compliance with the registered controls that have a file-level surface.

    python3 tools/control_coverage.py            # the matrix
    python3 tools/control_coverage.py --check    # refuse if any tool has no determination

WHY PER FILE. `tools/self_application.py` records a determination PER CONTROL — a declaration that
control 5 is enforced *by derive_counts.py*. It says nothing about the other seventy tools, and the
"0 searches" error happened in a script that never called derive_counts. **A control enforced
somewhere is not the code complying everywhere**, and only a per-file view can show the difference.

WHICH CONTROLS HAVE A FILE-LEVEL SURFACE AT ALL. Most do not, and pretending otherwise would
manufacture 63 × 71 mostly-meaningless cells. Control 6 is not a property a file can have; control
1 needs a second key holder; the goal-graph and evolutionary families need structures this
repository lacks. Recorded here so the exclusion is an argument rather than an omission (control
44):

    C2   negative control            YES — a tool either has a case it must fail, or it does not
    C5   closed-world measurement    partial — needs semantics; not attempted here
    C10  bounded claim               partial — needs semantics; not attempted here
    C53  typed unknown               partial — `scan_own_code.py` covers one syntactic slice
    everything else                  NO file-level surface. System, organisational, or structural.

**Only C2 is measured here.** It is the register's flagship and the one with an unambiguous
file-level answer. The others are named as unmeasured rather than quietly dropped.

WHAT A PASS MEANS, EXACTLY. That some test referencing the tool asserts a refusal, a non-zero exit,
a raise, or a must-reject fixture. It does **not** mean the negative control is a good one, that it
targets the capability rather than the transport (N5), or that the tool is correct. A tool can hold
a demanding fixture and still be broken everywhere the fixture does not look.
"""

from __future__ import annotations

import argparse
import ast
import io
import tokenize
import pathlib
import re
import sys

REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "tools"))
import closed_world                                                    # noqa: E402
TOOLS = REPO_ROOT / "tools"
TESTS = TOOLS / "tests"

#  Ways this repository's tests assert that something REFUSED. Broad on purpose: a false pass
#  here understates the gap, and understating it is the failure mode that matters.
REFUSAL = re.compile(
    r"returncode\s*==\s*[1-9]|returncode\s*!=\s*0|rc\s*==\s*[1-9]|exit\s*[1-9]|"
    r"is False|assertRaises|pytest\.raises|except\s+\w*Error|"
    r"refus|reject|must[_ ]fail|must[_ ]be[_ ]rejected|non-?zero|REFUSED|BROKEN|NOT FIT",
    re.I)

#  Tools that are libraries or one-shot utilities with no assurance signal of their own. Each
#  needs a REASON, not a bare exemption.
NO_SIGNAL: dict[str, str] = {
    "codex_budget.py": "reads a budget number; emits no assurance signal of its own",
    "render_markdown.py": "pure formatting; no verdict to be wrong about",
    "code_freetext.py": "text extraction helper; no verdict",
}


def tool_files() -> list[pathlib.Path]:
    return sorted(p for p in TOOLS.glob("*.py") if p.name != "__init__.py")


def self_hosted_fixtures(path: pathlib.Path) -> bool:
    """Does the tool SHIP its own must-fail cases?

    Added after validation: verify_fault_injection.py carries FOURTEEN must-reject fixtures and a
    --fixtures mode, and the first version of this measure scored it NONE because it only looked
    in tools/tests/ and because the test still references the pre-rename alias. A negative control
    that ships with the tool is a better negative control, not an absent one. Measuring only one
    location produced a false NONE on the repository's flagship verifier.
    """
    text = path.read_text(encoding="utf-8", errors="replace")
    return bool(re.search(r"--fixtures|must[_ ]be[_ ]rejected|must[_ ]reject|MUST be rejected|"
                          r"must_flag|run_fixtures", text))


def executable_text(text: str) -> str:
    """The test's CODE, with its docstring and every comment removed.

    A MENTION IS NOT AN EXERCISE, and prose keeps finding new places to hide. First it was the
    module docstring: test_chain_guards.py named anchor_manifest.py in its prose and never
    exercised it, and this measure counted that as coverage — a test's own docstring gaming the
    measure of that test, accidentally, within minutes of both being written. That was stripped
    via `ast`, not a regex, because the first attempt anchored at string start and every suite
    here begins with a shebang, so it stripped nothing and the false pass survived its own fix.

    Then it was a COMMENT. On 2026-08-12 a note in test_gate_refusals.py recorded that
    arm_acceptance.py CANNOT have a negative control yet — and because the note sat beside the
    tool's name and contained the words "negative control", this scanner reported the tool as
    COVERED on the strength of the sentence saying it is not. Codex found it the same day, in
    the batch of work whose entire subject was control 2.

    So comments go too. What is left is what the test actually executes.
    """
    try:
        doc = ast.get_docstring(ast.parse(text))
        if doc:
            text = text.replace(doc, "", 1)
    except SyntaxError:
        pass
    out = []
    try:
        for token in tokenize.generate_tokens(io.StringIO(text).readline):
            if token.type == tokenize.COMMENT:
                continue
            out.append(token.string)
    except (tokenize.TokenError, IndentationError, SyntaxError):
        #  Untokenizable is UNKNOWN, not clean. Returning the raw text keeps the old (weaker)
        #  behaviour rather than silently reporting no references at all.
        return text
    return " ".join(out)


def resolve_aliases(text: str) -> str:
    """Substitute simple `NAME = "tool.py"` bindings back into their uses.

    STRIPPING COMMENTS MADE THIS NECESSARY, and finding out why is the useful part. With prose
    removed, `check_executive_context.py` — which has fifteen fixture-driven refusal cases —
    reported as UNCOVERED, because its suite binds the name once as `CTX =
    "check_executive_context.py"` and then drives everything through `CTX`. The only textual
    mention sat 600 characters away from the assertions.

    So the scanner had BOTH errors at once: it counted tools named only in prose, and missed
    tools exercised only through a variable. Removing the first exposed the second.

    This resolves the one binding shape that actually occurs here. It is still lexical, and
    Codex's standing recommendation is an explicit tool→test mapping or AST call detection —
    recorded in the module docstring rather than pretended away.
    """
    aliases: dict[str, str] = {}
    try:
        tree = ast.parse(text)
    except SyntaxError:
        return text
    for node in ast.walk(tree):
        if (isinstance(node, ast.Assign) and len(node.targets) == 1
                and isinstance(node.targets[0], ast.Name)
                and isinstance(node.value, ast.Constant)
                and isinstance(node.value.value, str)
                and node.value.value.endswith(".py")):
            aliases[node.targets[0].id] = node.value.value
    for name, tool in aliases.items():
        text = re.sub(rf"\b{re.escape(name)}\b", tool, text)
    return text


def declared_coverage(text: str) -> tuple[str, ...]:
    """A module-level `COVERS = ("tool.py", ...)` — what this suite says it exercises.

    WHY DECLARATION BEAT PROXIMITY. The lexical heuristic below had both errors at once. It
    counted tools named only in a COMMENT, and it MISSED `check_executive_context.py`, which has
    fifteen fixture-driven refusal cases — because the suite binds the name once as a constant
    and drives it through a wrapper function, so no assertion sits within 600 characters of the
    tool's name. Resolving simple aliases did not fix that, and would not: the invocation is a
    call to `state()`, not to the constant.

    A declaration is explicit, lives beside the code, and CANNOT ROT SILENTLY: a declared tool
    that does not exist fails the scan, and a declaration is not enough on its own — the file
    must still contain a refusal assertion, or a suite could claim coverage it never wrote.
    """
    try:
        tree = ast.parse(text)
    except SyntaxError:
        return ()
    for node in tree.body:
        if (isinstance(node, ast.Assign) and len(node.targets) == 1
                and isinstance(node.targets[0], ast.Name)
                and node.targets[0].id == "COVERS"
                and isinstance(node.value, (ast.Tuple, ast.List))):
            return tuple(e.value for e in node.value.elts
                         if isinstance(e, ast.Constant) and isinstance(e.value, str))
    return ()


def has_negative_control(stem: str) -> tuple[bool, str]:
    """A test asserting a refusal, OR the tool shipping its own must-fail cases."""
    own = TOOLS / f"{stem}.py"
    if own.is_file() and self_hosted_fixtures(own):
        return (True, "ships its own must-fail fixtures")
    if not TESTS.is_dir():
        return (False, "no tests directory")
    #  A DECLARATION FIRST, and only when the file also asserts a refusal somewhere. The
    #  declaration says which tool; the refusal says something was actually required to fail.
    #  Neither alone is coverage.
    for t in sorted(TESTS.rglob("*.py")):
        raw = t.read_text(encoding="utf-8", errors="replace")
        if f"{stem}.py" in declared_coverage(raw):
            if REFUSAL.search(executable_text(raw)):
                return (True, f"{t.name} declares it in COVERS and asserts a refusal")
            return (False, f"{t.name} declares it in COVERS but asserts no refusal")

    referencing = []
    for t in sorted(TESTS.rglob("*.py")):
        text = t.read_text(encoding="utf-8", errors="replace")
        #  STRIP THE MODULE DOCSTRING. test_chain_guards.py named anchor_manifest.py in its
        #  prose and never exercised it, and this measure counted that as coverage -- a test's
        #  own docstring gaming the measure of that test, accidentally, within minutes of both
        #  being written. A mention is not an exercise.
        #
        #  Via ast, not a regex: the first attempt anchored at string start and every suite here
        #  begins with a shebang, so it stripped nothing and the false pass survived the fix.
        text = resolve_aliases(executable_text(text))
        if stem in text:
            referencing.append((t.name, text))
    if not referencing:
        return (False, "no test references it")
    for name, text in referencing:
        #  Only the portion of the test that mentions this tool, so a refusal assertion about a
        #  DIFFERENT tool in the same file is not counted. Crude window, deliberately narrow.
        for m in re.finditer(re.escape(stem), text):
            window = text[max(0, m.start() - 600): m.end() + 600]
            if REFUSAL.search(window):
                return (True, f"{name} asserts a refusal near a reference to it")
    return (False, f"referenced by {referencing[0][0]} but no refusal is asserted near it")


def survey_population() -> "closed_world.Survey":
    """The walk, under control 5: a rate this cannot compute is not reported as a rate.

    This tool publishes a PERCENTAGE over a population of files, which is exactly the shape
    control 5 governs — and it read the tests by `read_text(errors="replace")` and parsed them
    with `ast`, swallowing a SyntaxError into `pass`. A test file that would not parse simply
    contributed nothing, and the percentage came out looking like a measurement.

    That is not a hypothetical for this tool in particular. It has already been wrong in both
    directions in one day: counting a tool named only in a comment, and missing one exercised
    through a wrapper. A rate from an incomplete walk is the third way it could be wrong, and
    the only one nobody would see.
    """
    survey = closed_world.Survey("tools with a negative control", scope="tools/*.py")
    for path in tool_files():
        survey.seen(path.name)
        try:
            ast.parse(path.read_text(encoding="utf-8"))
        except (SyntaxError, UnicodeDecodeError, OSError) as error:
            survey.unreadable(path.name, f"{type(error).__name__}: {error}")
            continue
        if path.name in NO_SIGNAL:
            survey.excluded(path.name, NO_SIGNAL[path.name])
            continue
        ok, _why = has_negative_control(path.stem)
        survey.count("has a negative control" if ok else "has none")
    for t in sorted(TESTS.rglob("*.py")):
        #  THE TESTS ARE IN SCOPE TOO. The verdict for every tool depends on reading them, so a
        #  test this cannot parse makes every "NONE" unreliable, not just its own.
        survey.seen(f"tests/{t.name}")
        try:
            ast.parse(t.read_text(encoding="utf-8"))
        except (SyntaxError, UnicodeDecodeError, OSError) as error:
            survey.unreadable(f"tests/{t.name}", f"{type(error).__name__}: {error}")
    return survey


def rows() -> list[tuple[str, str, str]]:
    out = []
    for path in tool_files():
        stem = path.stem
        if path.name in NO_SIGNAL:
            out.append((path.name, "NOT_APPLICABLE", NO_SIGNAL[path.name]))
            continue
        ok, why = has_negative_control(stem)
        out.append((path.name, "HAS_NEGATIVE_CONTROL" if ok else "NONE", why))
    return out


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--check", action="store_true",
                        help="exit non-zero if any tool has no determination")
    args = parser.parse_args()

    survey = survey_population()
    data = rows()
    has = [r for r in data if r[1] == "HAS_NEGATIVE_CONTROL"]
    none = [r for r in data if r[1] == "NONE"]
    na = [r for r in data if r[1] == "NOT_APPLICABLE"]

    if args.check:
        undetermined = [r for r in data if r[1] not in
                        ("HAS_NEGATIVE_CONTROL", "NONE", "NOT_APPLICABLE")]
        if undetermined:
            for r in undetermined:
                print(f"  no determination for {r[0]}", file=sys.stderr)
            return 1
        print(f"  every tool has a determination ({len(data)}).")
        return 0

    print("  CONTROL 2 PER FILE — does each tool have a case it must fail?\n")
    print(f"    HAS_NEGATIVE_CONTROL  {len(has):3d}")
    print(f"    NONE                  {len(none):3d}")
    print(f"    NOT_APPLICABLE        {len(na):3d}   (each with a stated reason)")
    print(f"    ---------------------------")
    print(f"    total                 {len(data):3d}")
    pct = len(has) / (len(has) + len(none)) if (has or none) else 0
    #  CONTROL 5: THE RATE IS WITHHELD IF THE WALK WAS INCOMPLETE. A percentage over a
    #  population that could not all be read is the "0 searches across 83 tool calls" shape with
    #  a denominator attached, and this tool has already been wrong in two other directions in
    #  one day. `report()` still says what was and was not seen — coverage instead of a rate.
    try:
        survey.result()
    except closed_world.IncompleteSurvey as refusal:
        print("\n" + survey.report())
        print(f"\n  NO RATE IS REPORTED: {refusal}", file=sys.stderr)
        return 1
    print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
    print("  WITHOUT ONE:")
    for name, _s, why in none[:20]:
        print(f"    {name:34} {why}")
    if len(none) > 20:
        print(f"    … {len(none) - 20} more")
    print("\n  A pass means SOME test asserts a refusal near a reference to the tool. It does not")
    print("  mean the control is demanding, that it targets the capability rather than the")
    print("  transport, or that the tool is correct. Control 2 raises the floor from 'never")
    print("  observed to fail' to 'observed to fail at least once'. This measures that floor and")
    print("  nothing above it.")
    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
    print("  every other control has no file-level surface. See the module docstring.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
def run_fixtures() -> int:
    failures = 0
    for label, fn in CLASSES.items():
        must, must_not = FIXTURES[label]
        if not fn(ast.parse(must)):
            print(f"  \033[31mMISSED\033[0m {label} — did not flag the case it must flag")
            failures += 1
        elif fn(ast.parse(must_not)):
            print(f"  \033[31mFLAGGED\033[0m {label} — flagged the case it must not flag")
            failures += 1
        else:
            print(f"  \033[32mok\033[0m {label}")
    print()
    if failures:
        print(f"  {failures} detector(s) wrong. NOT FIT TO SCAN.", file=sys.stderr)
        return 1
    print("  Both arms pass for each class. That says the detectors behave on cases chosen to")
    print("  test them; it says nothing about their recall over real code, which is UNKNOWN.")
    return 0


DISPOSITIONS = REPO_ROOT / "record" / "claims" / "code-scan-dispositions.json"


def load_dispositions() -> dict:
    """Per-site dispositions, keyed by a hash of file + the line's text.

    Keyed by CONTENT, not line number: editing the line invalidates its disposition, so a site
    cannot be silently reworked under an old judgement. Line numbers drift on every insert and
    would let a stale disposition attach to whatever moved into the slot.
    """
    if not DISPOSITIONS.is_file():
        return {}
    return json.loads(DISPOSITIONS.read_text(encoding="utf-8")).get("sites", {})


def site_key(path: pathlib.Path, line_text: str) -> str:
    return hashlib.sha256(f"{path}\n{line_text.strip()}".encode()).hexdigest()[:16]


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--fixtures", action="store_true")
    args = parser.parse_args()
    if args.fixtures:
        return run_fixtures()

    if run_fixtures() != 0:
        return 1
    print()
    #  CONTROL 5 THROUGH A TYPE, not a counter. This walked every tool, incremented `unparsed`
    #  on a file it could not read, and then PRINTED THE HIT COUNTS ANYWAY with the unparsed
    #  number beside them. Reporting coverage next to a count is better than hiding it and is
    #  still not what the control says: parse every in-scope artifact, OR REFUSE TO EMIT ANY
    #  RESULT. A reader who sees "3 hits, 2 unparsed" has no way to know whether the two held
    #  thirty. `Survey.result()` raises instead, and `report()` says what could not be read.
    survey = closed_world.Survey("defect-class scan over this repository's own tools",
                                 scope=str(SCANNED.relative_to(REPO_ROOT)) + "/**/*.py")
    disp = load_dispositions()
    #  The population is walked once per class; only the FIRST pass surveys it, or every file
    #  would be counted five times and the coverage number would be nonsense.
    first_label = next(iter(CLASSES))
    for label, fn in CLASSES.items():
        hits, dispositioned = [], 0
        for path in sorted(SCANNED.rglob("*.py")):
            if label == first_label:
                survey.seen(str(path.relative_to(REPO_ROOT)))
            try:
                text = path.read_text(encoding="utf-8")
                tree = ast.parse(text)
            except Exception as error:                                   # noqa: BLE001
                if label == first_label:
                    survey.unreadable(str(path.relative_to(REPO_ROOT)),
                                      f"{type(error).__name__}: {error}")
                continue
            source_lines = text.splitlines()
            for n in fn(tree):
                line_text = source_lines[n - 1] if n <= len(source_lines) else ""
                if site_key(path.relative_to(REPO_ROOT), line_text) in disp:
                    dispositioned += 1
                    continue
                hits.append(f"{path.relative_to(REPO_ROOT)}:{n}")
        #  The counts go INTO the survey, so a walk that could not read a file withholds them
        #  rather than printing them beside an unparsed tally. That printing-beside is what this
        #  routing replaced.
        survey.count(f"{label} — undispositioned", len(hits))
        if dispositioned:
            survey.count(f"{label} — dispositioned", dispositioned)
        print(f"  {label}: {len(hits)} undispositioned"
              f"{f', {dispositioned} dispositioned' if dispositioned else ''}")
        for h in hits[:8]:
            print(f"      {h}")
        if len(hits) > 8:
            print(f"      … {len(hits) - 8} more")
    print()
    print(survey.report())
    try:
        survey.result()
    except closed_world.IncompleteSurvey as refusal:
        #  THE HITS ABOVE ARE NOT A RESULT. They are what was found in the part that could be
        #  read, and the control says a partial walk yields no count at all.
        print(f"\n  REFUSED: {refusal}", file=sys.stderr)
        return 1
    print("  A HIT IS A CANDIDATE, NOT A DEFECT. On 2026-08-11 all six D-E hits were functions")
    print("  returning a typed unknown that the caller checks — correct, not broken.")
    print("  This is not a compliance check against the register. See the module docstring.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
      "disposition": "legitimate",
      "why": "test assertion: an absent key makes the assertion FALSE and the test FAIL. Absence cannot produce a passing test.",
      "read": "individually"
    },
    "55a1918fa5995d14": {
      "file": "tools/test_integrity.py",
      "line_text": "out.get(\"verbatim_whole\") is True and out.get(\"n_verbatim\", 0) > 0,",
      "class": "C53a",
      "disposition": "legitimate",
      "why": "test assertion: absent makes the assertion false and the test fail.",
      "read": "individually"
    },
    "460e5108329e970e": {
      "file": "tools/tests/test_executive_log.py",
      "line_text": "and cold.get(\"last_known_age_seconds\", 0) > 0)",
      "class": "C53a",
      "disposition": "legitimate",
      "why": "test assertion: absent makes the comparison false and the test fail.",
      "read": "individually"
    },
    "6f33f8d9b852f948": {
      "file": "tools/tests/test_k_solicited.py",
      "line_text": "if (sm.get(\"k_collected\") or 0)",
      "class": "C53b",
      "disposition": "legitimate",
      "why": "fails safe: an absent k_collected counts as zero samples collected, which flags the round as UNDER-collected or excludes it. Absence pushes toward refusal, never toward a flattering count.",
      "read": "individually"
    },
    "4514e833289e556f": {
      "file": "tools/tests/test_k_solicited.py",
      "line_text": "< (sm.get(\"k_min_usable\") or sm.get(\"k_requested\") or 0))",
      "class": "C53b",
      "disposition": "legitimate",
      "why": "fails safe: an absent floor compares as zero, so the sample is treated as meeting no floor and is flagged rather than passed.",
      "read": "individually"
    },
    "01f8a3bf04bb5b92": {
      "file": "tools/validate_provenance.py",
      "line_text": "matched = actual.get(identity, 0)",
      "class": "C53a",
      "disposition": "legitimate",
      "why": "DOCUMENTED IN PLACE: the comment above states a deliberate zero — a model asserted present that produced nothing — and says recording the absence is the point.",
      "read": "individually"
    },
    "6adfeedcf207981b": {
      "file": "tools/validate_provenance.py",
      "line_text": "if actual.get(identity, 0) != 0:",
      "class": "C53a",
      "disposition": "legitimate",
      "why": "DOCUMENTED IN PLACE: the comment above states a deliberate zero — a model asserted present that produced nothing — and says recording the absence is the point.",
      "read": "individually"
    },
    "f7c786313da41adf": {
      "file": "tools/build_session_log.py",
      "line_text": "return None",
      "class": "D-E",
      "disposition": "legitimate",
      "why": "read individually 2026-08-11: this returns a TYPED UNKNOWN that the caller checks, which is control 53 done RIGHT rather than violated. git_optional is documented as 'legitimately may not exist'; land.py's _api says the caller must treat None as UNOBSERVED; watch_arrivals reports QUERY FAILED. The detector cannot see the caller, so it flags the correct pattern and the incorrect one alike.",
      "read": "individually"
    },
    "feed94a4b5604bc5": {
      "file": "tools/check_claims.py",
      "line_text": "return None",
      "class": "D-E",
      "disposition": "legitimate",
      "why": "read individually 2026-08-11: this returns a TYPED UNKNOWN that the caller checks, which is control 53 done RIGHT rather than violated. git_optional is documented as 'legitimately may not exist'; land.py's _api says the caller must treat None as UNOBSERVED; watch_arrivals reports QUERY FAILED. The detector cannot see the caller, so it flags the correct pattern and the incorrect one alike.",
      "read": "individually"
    },
    "f32b9a16c220b4f7": {
      "file": "tools/land.py",
      "line_text": "return None",
      "class": "D-E",
      "disposition": "legitimate",
      "why": "read individually 2026-08-11: this returns a TYPED UNKNOWN that the caller checks, which is control 53 done RIGHT rather than violated. git_optional is documented as 'legitimately may not exist'; land.py's _api says the caller must treat None as UNOBSERVED; watch_arrivals reports QUERY FAILED. The detector cannot see the caller, so it flags the correct pattern and the incorrect one alike.",
      "read": "individually"
    },
    "d379657347c971c7": {
      "file": "tools/qualification_round.py",
      "line_text": "return None",
      "class": "D-E",
      "disposition": "legitimate",
      "why": "read individually 2026-08-11: this returns a TYPED UNKNOWN that the caller checks, which is control 53 done RIGHT rather than violated. git_optional is documented as 'legitimately may not exist'; land.py's _api says the caller must treat None as UNOBSERVED; watch_arrivals reports QUERY FAILED. The detector cannot see the caller, so it flags the correct pattern and the incorrect one alike.",
      "read": "individually"
    },
    "e9d330d4a4d4ab17": {
      "file": "tools/watch_arrivals.py",
      "line_text": "return None",
      "class": "D-E",
      "disposition": "legitimate",
      "why": "read individually 2026-08-11: this returns a TYPED UNKNOWN that the caller checks, which is control 53 done RIGHT rather than violated. git_optional is documented as 'legitimately may not exist'; land.py's _api says the caller must treat None as UNOBSERVED; watch_arrivals reports QUERY FAILED. The detector cannot see the caller, so it flags the correct pattern and the incorrect one alike.",
      "read": "individually"
    },
    "9134af4bcd3550f6": {
      "path": "tools/deploy_obligations.py",
      "line_when_recorded": 151,
      "line_text": "return None",
      "disposition": "correct_typed_unknown_or_known_zero",
      "note": "D-E: `return None` from an except in `_api`. CORRECT and deliberate — None here means the GitHub API could not be read, and every caller treats it as unobserved rather than as an answer. `observe()` turns it into a PENDING state with retriable=True. This is control 53 done right: the unknown has a value the caller must handle, and it does.",
      "by": "workbench",
      "utc": "2026-08-12"
    },
    "bd8e1d54a2a90461": {
      "path": "tools/build_register_view.py",
      "line_when_recorded": 130,
      "line_text": "f\"**{review.get('reviewed_by_custodian', 0)} of {len(entries)}** classifications have been \"",
      "disposition": "correct_typed_unknown_or_known_zero",
      "note": "C53a: `review.get('reviewed_by_custodian', 0)`. The 0 is CORRECT and was 0 -> '?' -> 0 in one day. `review` is a Counter built by iterating EVERY entry's status, so an absent bucket means no entry holds that status — a known zero. The '?' sweep manufactured an unknown out of a fact and published '? of 58' where the truth is '0 of 58'. The genuine unknown here is an entry MISSING the key, which raises three lines above and is asserted by tools/tests/test_gate_refusals.py.",
      "by": "workbench",
      "utc": "2026-08-12"
    },
    "f60f4d46ab3fe782": {
      "path": "tools/control_findings.py",
      "line_when_recorded": 303,
      "line_text": "detector_n = by_how.get(\"detector\", 0)",
      "disposition": "correct_typed_unknown_or_known_zero",
      "note": "C53a: `by_how.get('detector', 0)`. Same shape and the same reason it is correct — `by_how` is built by iterating every finding, so an absent bucket is a counted zero rather than an unmeasured one. If no finding were found by a detector, zero is the true answer.",
      "by": "workbench",
      "utc": "2026-08-12"
    }
  }
}
record/claims/code-scan-dispositions.json:293:      "path": "tools/deploy_obligations.py",
record/claims/code-scan-dispositions.json:304:      "line_text": "f\"**{review.get('reviewed_by_custodian', 0)} of {len(entries)}** classifications have been \"",
record/claims/code-scan-dispositions.json:306:      "note": "C53a: `review.get('reviewed_by_custodian', 0)`. The 0 is CORRECT and was 0 -> '?' -> 0 in one day. `review` is a Counter built by iterating EVERY entry's status, so an absent bucket means no entry holds that status — a known zero. The '?' sweep manufactured an unknown out of a fact and published '? of 58' where the truth is '0 of 58'. The genuine unknown here is an entry MISSING the key, which raises three lines above and is asserted by tools/tests/test_gate_refusals.py.",
record/claims/code-scan-dispositions.json:313:      "line_text": "detector_n = by_how.get(\"detector\", 0)",
record/claims/code-scan-dispositions.json:315:      "note": "C53a: `by_how.get('detector', 0)`. Same shape and the same reason it is correct — `by_how` is built by iterating every finding, so an absent bucket is a counted zero rather than an unmeasured one. If no finding were found by a detector, zero is the true answer.",
tools/schemas/deficiency-register.schema.json:206:              "enum": ["not_reviewed", "reviewed_by_custodian", "disputed"]
tools/deploy_obligations.py:4:    python3 tools/deploy_obligations.py --status        # what is owed, satisfied, or open
tools/deploy_obligations.py:5:    python3 tools/deploy_obligations.py --reconcile     # observe the outstanding ones
tools/deploy_obligations.py:6:    python3 tools/deploy_obligations.py --resolve <id>  # close incidents, with evidence
tools/deploy_obligations.py:7:    python3 tools/deploy_obligations.py --check         # exit non-zero if anything blocks work
tools/check_register.py:353:    reviewed = sum(1 for e in doc["entries"] if e["human_review"]["status"] == "reviewed_by_custodian")
tools/control_application.py:246:          files=("tools/deploy_obligations.py", "tools/land.py", "tools/gate_health.py"),
tools/control_application.py:247:          tests=("tools/tests/test_deploy_obligations.py",),
tools/control_application.py:414:          files=("tools/deploy_obligations.py", "tools/check_executive_context.py"),
tools/control_application.py:415:          tests=("tools/tests/test_deploy_obligations.py",),
tools/control_application.py:433:                 "tools/check_executive_context.py", "tools/deploy_obligations.py",
tools/control_application.py:435:          tests=("tools/tests/test_derive_counts.py", "tools/tests/test_deploy_obligations.py",
tools/control_application.py:452:          tests=("tools/tests/test_deploy_obligations.py",),
tools/control_application.py:481:          tests=("tools/tests/test_executive_log.py", "tools/tests/test_deploy_obligations.py"),
tools/build_register_view.py:130:        f"**{review.get('reviewed_by_custodian', 0)} of {len(entries)}** classifications have been "
tools/build_register_view.py:299:                   if x["human_review"]["status"] == "reviewed_by_custodian")
tools/control_findings.py:176:  "negative-control", "D-58; tools/tests/test_deploy_obligations.py"),
tools/control_findings.py:191:  "detector", "D-58; tools/deploy_obligations.py"),
tools/control_findings.py:291:    by_how: dict[str, int] = {}
tools/control_findings.py:294:        by_how[how] = by_how.get(how, 0) + 1
tools/control_findings.py:301:    for how, n in sorted(by_how.items(), key=lambda kv: -kv[1]):
tools/control_findings.py:303:    detector_n = by_how.get("detector", 0)
tools/tests/test_deploy_obligations.py:41:COVERS = ("deploy_obligations.py",
tools/tests/test_deploy_obligations.py:67:ob = load("deploy_obligations")
tools/tests/test_deploy_obligations.py:285:      land.obligations is ob or land.obligations.__name__ == "deploy_obligations")
tools/land.py:61:import deploy_obligations as obligations                                # noqa: E402
tools/land.py:308:        "To see the ledger:  python3 tools/deploy_obligations.py --status",
tools/land.py:426:        print(f"      python3 tools/deploy_obligations.py --resolve {args.remediating}")
#!/usr/bin/env python3
"""Derive counts from corpus/raw MECHANICALLY, and refuse when it cannot see a file.

    python3 tools/derive_counts.py --tool search          # search_web receipts, with denominators
    python3 tools/derive_counts.py --tool fetch
    python3 tools/derive_counts.py --coverage             # just the denominators

**DETERMINISTIC.** No LLM, no network.

Why this exists
---------------
On 2026-08-10 this layer published "0 `search_web` invocations across all 20 rounds" and
recommended deleting its own tool on the strength of it. The corpus holds **9**. The count came
from a scan that iterated each file's `samples` array; **69 raw files have no `samples` key** —
they use `responses` and `failures` — so the scan returned zero for their entire contents and the
output looked exactly like a true zero.

That is the failure this module exists to make impossible: **an unrecognised schema must not
report absence.** If a file's disposition containers are not recognised, this refuses, prints the
files, and emits no number at all. A number that is missing is loud; a number that is wrong is
silent, and this project has now shipped two of the latter in one day.

What counts as a disposition
-----------------------------
All of them. `samples`, `responses`, `failures`, `rejected` — because the record's own invariant
is that nothing solicited is discarded, and a count that reaches zero only by excluding a failed
sample is not stricter, it is wrong. The single numbered-round search in the corpus is inside a
`failures` entry whose sample died on HTTP 400 *after* the search returned OK.

What it cannot establish
-------------------------
* **That a receipt means the party wanted the tool.** A receipt records an invocation, not a
  motive, and instructed probes are in here beside voluntary rounds — so `--tool` reports the
  split rather than one total.
* **That the corpus is complete.** It counts what was committed.
* **That the count answers the question someone will ask of it.** Denominators are printed for
  exactly this reason: a number without its denominator is the thing that went wrong.
"""

from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
RAW = REPO_ROOT / "corpus" / "raw"

#  Every container in which a solicited unit may sit.
DISPOSITIONS = ("samples", "responses", "failures", "rejected", "refusals")

#  Keys under which a tool receipt list appears. Both the flat form (`search_receipts`) used in
#  `failures` entries and the nested form (`search: {receipts: [...]}`) used in samples.
RECEIPT_KEYS = {
    "search": ("search_receipts", "search"),
    "fetch": ("fetch_receipts", "fetch"),
}

#  Directories whose parties were TOLD to use the tool. Counted, never merged with the rounds:
#  "parties never search" and "parties never search unless asked" are different claims.
INSTRUCTED_PREFIXES = ("toolprobe", "searchprobe", "fetchprobe")

#  RECOGNITION IS BY DECLARED TYPE, NOT BY SEARCHING FOR RECEIPT-LIKE WORDS.
#
#  The first version asked whether a known receipt spelling appeared anywhere in the serialised
#  document. Review broke it in one try with a schema this project has never used:
#
#      {"units": [{"tool_calls": [{"name": "browse", "receipt": {"ok": true}}]}]}
#
#  No known spelling, so it was classified "legitimately holds no solicited units" and the run
#  returned `usable: True, total: 0`. **That is the original silent zero under a new spelling, in
#  the tool built this morning to make it impossible.** The same test failed the other way too:
#  a metadata document merely mentioning "search" would have been refused.
#
#  So a file is recognised only if it DECLARES a type this module knows. Anything else refuses,
#  whether or not it looks like it holds receipts.
SOLICITATION_TYPES = frozenset({
    "solicitation", "solicitation_summary", "agenda_activation_solicitation",
    "qualification_solicitation", "ratification_ballot_solicitation",
    "raw_samples", "local_solicitation",
})

#  Artifacts that legitimately carry no solicited units. Each is listed BY TYPE with a reason,
#  so excluding one is a recorded decision rather than a silent classification.
NON_SOLICITATION_TYPES = {
    "solicitation_spec": "an outbound spec; the samples live in the raw file beside it",
    "agenda_activation_record": "computed from raw samples, does not hold them",
    "qualification_record": "computed from raw samples, does not hold them",
    "ratification_record": "computed from raw samples, does not hold them",
    "amendment_disposition": "computed from a ballot result, does not hold samples",
    "agenda_cohort_exposure": "a delivery record",
    "finding_coding": "coding over artifacts, not solicited units",
    "freetext_coding": "coding over artifacts, not solicited units",
    "annotation": "commentary on artifacts",
    "contribution": "a party contribution already summarised elsewhere",
    "deficiency_register": "the register, not a solicitation",
}


def _receipts(node: dict, kind: str) -> list:
    out = []
    for key in RECEIPT_KEYS[kind]:
        value = node.get(key)
        if isinstance(value, list):
            out += [r for r in value if isinstance(r, dict)]
        elif isinstance(value, dict):
            nested = value.get("receipts")
            if isinstance(nested, list):
                out += [r for r in nested if isinstance(r, dict)]
    return out


def scan(kind: str) -> dict:
    """Walk every raw file. Refuse rather than skip anything unrecognised."""
    counts = {"rounds": 0, "instructed": 0, "other": 0}
    per_file: dict[str, int] = {}
    visited = parsed = units = 0
    unparseable: list[str] = []
    unrecognised: list[str] = []
    for path in sorted(RAW.rglob("*.json")):
        visited += 1
        name = str(path.relative_to(RAW))
        try:
            doc = json.loads(path.read_text(encoding="utf-8"))
        except Exception as error:                                      # noqa: BLE001
            unparseable.append(f"{name}: {type(error).__name__}")
            continue
        parsed += 1
        if not isinstance(doc, dict):
            unrecognised.append(f"{name}: top level is {type(doc).__name__}, not an object")
            continue
        #  PRESENCE OF UNITS DECIDES FIRST, and the declared type only breaks the tie when there
        #  are none. The reverse order produced a THIRD silent zero within an hour of the second:
        #  raw sample files embed their outbound `spec`, so reading `spec.artifact_type` as a
        #  fallback classified them as `..._solicitation` and excluded them wholesale. The
        #  corpus-wide search count silently fell from 9 to 8 and the one numbered-round receipt
        #  disappeared -- the exact datum the whole module was built to stop losing.
        #
        #  A file holding solicited units is counted whatever it calls itself. Only a file with
        #  NO units needs a type, and an unknown one refuses rather than passing as empty.
        containers = [k for k in DISPOSITIONS if isinstance(doc.get(k), list)]
        if not containers:
            declared = doc.get("artifact_type")
            if declared in NON_SOLICITATION_TYPES:
                continue                      # excluded by a recorded decision, not by accident
            unrecognised.append(
                f"{name}: holds none of {DISPOSITIONS} and declares artifact_type {declared!r}, "
                f"which is not registered in NON_SOLICITATION_TYPES")
            continue
        bucket = ("instructed" if name.split("/")[0].startswith(INSTRUCTED_PREFIXES)
                  else "rounds" if name.split("/")[0].startswith("round-") else "other")
        for container in containers:
            for unit in doc[container]:
                if not isinstance(unit, dict):
                    continue
                units += 1
                found = len(_receipts(unit, kind))
                if found:
                    counts[bucket] += found
                    per_file[name] = per_file.get(name, 0) + found
    return {"kind": kind, "counts": counts, "total": sum(counts.values()), "per_file": per_file,
            "coverage": {"files_visited": visited, "files_parsed": parsed,
                         "solicited_units_seen": units,
                         "unparseable": unparseable, "unrecognised": unrecognised},
            "usable": not (unparseable or unrecognised)}


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
    parser.add_argument("--tool", choices=sorted(RECEIPT_KEYS), default="search")
    parser.add_argument("--coverage", action="store_true", help="print denominators only")
    parser.add_argument("--json", action="store_true")
    args = parser.parse_args()

    result = scan(args.tool)
    cov = result["coverage"]
    if args.json:
        print(json.dumps(result, indent=2))
        return 0 if result["usable"] else 1

    print(f"  files visited {cov['files_visited']}, parsed {cov['files_parsed']}, "
          f"solicited units seen {cov['solicited_units_seen']}")
    if not result["usable"]:
        #  NO NUMBER IS PRINTED. The whole point: a count derived over a corpus this module
        #  could not fully read is the exact artifact that caused the failure it prevents.
        for entry in cov["unparseable"]:
            print(f"  UNPARSEABLE   {entry}", file=sys.stderr)
        for entry in cov["unrecognised"]:
            print(f"  UNRECOGNISED  {entry}", file=sys.stderr)
        print("\nREFUSING to report a count. A scan that cannot see a file reports absence,\n"
              "and absence is indistinguishable from a true zero in the output.", file=sys.stderr)
        return 1
    if args.coverage:
        return 0
    c = result["counts"]
    print(f"\n  {args.tool} receipts")
    print(f"    numbered rounds      {c['rounds']}")
    print(f"    instructed probes    {c['instructed']}   (parties were told to use the tool)")
    print(f"    other cohorts        {c['other']}")
    print(f"    TOTAL                {result['total']}")
    if result["per_file"]:
        print("\n  where they are:")
        for name, n in sorted(result["per_file"].items()):
            print(f"    {n:4d}  {name}")
    print("\n  A receipt records an invocation, not a motive. The rounds/probes split is kept\n"
          "  because 'parties never search' and 'parties never search unless asked' differ.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

**Refining scan test assertions**
**Assessing population survey test inconsistencies**
**Proposing survey dependency validation design**
**Assessing reason quality validation**
exec
/bin/bash -lc "rg -n \"reason|quality|vacu|excluded|exclusion\" tools/control_application.py tools/tests/test_no_blank_cells.py tools/tests/test_closed_world.py
sed -n '80,180p' tools/control_application.py
sed -n '110,165p' tools/build_register_view.py
sed -n '275,315p' tools/control_findings.py
sed -n '120,185p' tools/deploy_obligations.py
python3 tools/tests/test_closed_world.py" in /home/reed/git/open-asi-governance-forum
codex
The routing confirms a second blocker: both tools emit partial numerical results before checking `Survey.result()`. `scan_own_code.py` prints every per-class hit count; `control_coverage.py` prints HAS/NONE/total counts, and its `--check` path returns before consulting the survey at all. The injection cases look only for one old summary phrase or the percentage string, so they pass while the forbidden counts remain visible.
 succeeded in 24438ms:

the type: a complete walk yields numbers, an incomplete one yields none
  [32m✓[0m BASELINE: a complete walk is usable
  [32m✓[0m ...and result() returns the counts
  [32m✓[0m ...and report() shows them
  [32m✓[0m one UNREADABLE artifact makes the survey unusable
  [32m✓[0m ...and result() RAISES rather than returning a partial count
  [32m✓[0m ...and report() is still safe to call
  [32m✓[0m ...and names the artifact it could not read
  [32m✓[0m ...and does NOT print the five it did count
  [32m✓[0m an UNREGISTERED artifact also makes the survey unusable
  [32m✓[0m ...and is reported distinctly from an unreadable one
  [32m✓[0m an exclusion WITH a stated ground is accepted
  [32m✓[0m ...and does not make the survey unusable
  [32m✓[0m an exclusion with no ground ('') is REFUSED
  [32m✓[0m an exclusion with no ground ('n/a') is REFUSED
  [32m✓[0m an exclusion with no ground ('skip') is REFUSED

the routing: an unreadable file must withhold each tool's published number
  [32m✓[0m BASELINE: scan_own_code.py reports its number on a clean tree
  [32m✓[0m scan_own_code.py REFUSES when one file in its population cannot be read
  [32m✓[0m ...and scan_own_code.py names the file it could not read
  [32m✓[0m ...and scan_own_code.py publishes NO number from the partial walk
  [32m✓[0m ...and scan_own_code.py reports again once the tree is clean
  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
  [32m✓[0m ...and control_coverage.py names the file it could not read
  [32m✓[0m ...and control_coverage.py publishes NO number from the partial walk
  [32m✓[0m ...and control_coverage.py reports again once the tree is clean

25 passed, 0 failed

**Planning safe test output verification**
**Inspecting disposition schema inconsistencies**
exec
/bin/bash -lc "sed -n '1,120p' record/claims/code-scan-dispositions.json
python3 tools/scan_own_code.py
python3 tools/control_coverage.py --check
python3 tools/control_coverage.py" in /home/reed/git/open-asi-governance-forum
 succeeded in 14119ms:
  [32mok[0m D-A empty conditional body
  [32mok[0m D-B constant guard
  [32mok[0m D-E success returned from an except
  [32mok[0m C53a missing value defaults to a number
  [32mok[0m C53b None coerced to a number by `or`
  [32mok[0m D-F eager guard message indexing state

  Both arms pass for each class. That says the detectors behave on cases chosen to
  test them; it says nothing about their recall over real code, which is UNKNOWN.

  D-A empty conditional body: 0 undispositioned
  D-B constant guard: 0 undispositioned
  D-E success returned from an except: 0 undispositioned, 7 dispositioned
  C53a missing value defaults to a number: 0 undispositioned, 18 dispositioned
  C53b None coerced to a number by `or`: 0 undispositioned, 15 dispositioned
  D-F eager guard message indexing state: 0 undispositioned

  defect-class scan over this repository's own tools: 108 artifact(s) in scope (tools/**/*.py)
        18  C53a missing value defaults to a number — dispositioned
        15  C53b None coerced to a number by `or` — dispositioned
         7  D-E success returned from an except — dispositioned
         0  D-A empty conditional body — undispositioned
         0  D-B constant guard — undispositioned
         0  D-E success returned from an except — undispositioned
         0  C53a missing value defaults to a number — undispositioned
         0  C53b None coerced to a number by `or` — undispositioned
         0  D-F eager guard message indexing state — undispositioned
  A HIT IS A CANDIDATE, NOT A DEFECT. On 2026-08-11 all six D-E hits were functions
  returning a typed unknown that the caller checks — correct, not broken.
  This is not a compliance check against the register. See the module docstring.
  every tool has a determination (76).
  CONTROL 2 PER FILE — does each tool have a case it must fail?

    HAS_NEGATIVE_CONTROL   37
    NONE                   36
    NOT_APPLICABLE          3   (each with a stated reason)
    ---------------------------
    total                  76

    51% of tools with an assurance signal have a negative control.

  WITHOUT ONE:
    agenda_activation.py               referenced by test_agenda_activation.py but no refusal is asserted near it
    agenda_cohort.py                   no test references it
    agenda_ratification.py             referenced by test_agenda_ratification.py but no refusal is asserted near it
    agenda_replacement.py              no test references it
    aggregate_captures.py              no test references it
    analyze_concurrence.py             no test references it
    arm_acceptance.py                  no test references it
    arm_profile.py                     no test references it
    author_deficiency_census.py        no test references it
    benchmark_agenda.py                no test references it
    build_bundle.py                    no test references it
    build_capture_ui.py                referenced by test_page_hash_fallback.py but no refusal is asserted near it
    build_challenge_page.py            no test references it
    build_local_rounds.py              no test references it
    build_manifest.py                  referenced by test_amendment_disposition.py but no refusal is asserted near it
    build_predictions_view.py          no test references it
    build_session_log.py               no test references it
    capture_response.py                referenced by test_disposition.py but no refusal is asserted near it
    claim_ledger.py                    no test references it
    control_findings.py                no test references it
    … 16 more

  A pass means SOME test asserts a refusal near a reference to the tool. It does not
  mean the control is demanding, that it targets the capability rather than the
  transport, or that the tool is correct. Control 2 raises the floor from 'never
  observed to fail' to 'observed to fail at least once'. This measures that floor and
  nothing above it.

  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;
  every other control has no file-level surface. See the module docstring.

**Planning manual testing for survey script**
exec
/bin/bash -lc "python3 - <<'PY'
import sys
sys.path.insert(0, 'tools')
from closed_world import Survey
cases = {}
s = Survey('seen but not accounted'); s.seen('x'); cases['seen_only'] = s.result()
s = Survey('counted but not seen'); s.count('bucket', 99); cases['count_only'] = s.result()
s = Survey('overcount'); s.seen('x'); s.count('bucket', 99); cases['overcount'] = s.result()
s = Survey('blank exclusion'); s.seen('x'); s.excluded('x', '          '); cases['blank_exclusion'] = s.result()
for key, value in cases.items(): print(key, value)
PY
rg -n \"Control 45|control 45|C45|rank.?45| 45:\" record docs tools | head -80
git diff --check" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
seen_only {'name': 'seen but not accounted', 'scope': '', 'visited': 1, 'counts': {}, 'excluded': []}
count_only {'name': 'counted but not seen', 'scope': '', 'visited': 0, 'counts': {'bucket': 99}, 'excluded': []}
overcount {'name': 'overcount', 'scope': '', 'visited': 1, 'counts': {'bucket': 99}, 'excluded': []}
blank_exclusion {'name': 'blank exclusion', 'scope': '', 'visited': 1, 'counts': {}, 'excluded': ['x:           ']}
tools/self_application.py:184: 45: ("VIOLATED",
tools/control_application.py:376: 45: dict(scope=CODE,
tools/tests/test_deploy_check.py:60:#  CONTROL 45: the replacement must still catch what the old one caught, and the b*40 case above
tools/control_findings.py:144: ("C45 replacement gate",
tools/check_claims.py:269:    """This tool's own negative control (control 2), on four populations (control 45):
tools/tests/test_executive_log.py:244:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
tools/tests/test_gate_negative_controls.py:271:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
docs/artifacts/fetched/round-016-claude-search-fetch-v1-1-2.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
docs/artifacts/fetched/round-016-grok-search-fetch-v1-1-4.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
docs/artifacts/fetched/round-018-grok-search-fetch-v1-4-4.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:420:tools/check_claims.py:257:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:744:#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:2728:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3919: ("C45 replacement gate",
record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:4185:    94	#  actually take here. Keeping both arms is control 45: a later rewrite must still catch what
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:85:The regression suite should retain four populations, following control 45:
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:210:- 45: gate replacements retain valid, invalid, near-miss, and historical-false-accept suites.
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:1760:./docs/artifacts/fetched/round-016-grok-search-fetch-v1-1-4.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:1819:./docs/artifacts/fetched/round-016-claude-search-fetch-v1-1-2.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:1829:./docs/artifacts/fetched/round-018-grok-search-fetch-v1-4-4.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:2154:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4588: 45: ("VIOLATED",
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4910:The regression suite should retain four populations, following control 45:
record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:5035:- 45: gate replacements retain valid, invalid, near-miss, and historical-false-accept suites.
record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:1748:record/designs/candidate-control-register.md-167-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T131129Z-056bd3b8f56f.md:2055:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md-2154-| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:479:./record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:4588: 45: ("VIOLATED",
record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:524:./tools/self_application.py:184: 45: ("VIOLATED",
record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:711:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260811T155044Z-0f439a7be7b9.md:995: 45: ("VIOLATED",
record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:709: 45: ("VIOLATED",
record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:1252:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260811T161031Z-e9bccc99c714.md:2423:   184	 45: ("VIOLATED",
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:52:4. C45 — makes the growing C2 corpus durable against gate rewrites.
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:93:measurement, the false-zero class that produced three published wrong numbers), C45 (make C2's
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:206:/bin/bash -lc "sed -n '1,280p' tools/tests/test_gate_refusals.py && git diff -- tools/control_application.py record/controls/control-application.md docs/artifacts/control-application.md docs/control-application.html record/claims/dispositions.jsonl record/cycles/spend-ledger.json record/executive/action-log.jsonl && rg -n 'C2|C44|C32|C5|C45|C55|C23|C61|build_register_view|R9 IS THE POINT|relative_to\\(REPO_ROOT\\)|arm_acceptance' record tools docs -g '*.py' -g '*.md' -g '*.json' -g '*.jsonl'" in /home/reed/git/open-asi-governance-forum
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:737:tools/control_findings.py:138: ("C45 replacement gate",
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1035:record/executive/codex-transcripts/20260812T003359Z-6cd1eaf7472d.md:3919: ("C45 replacement gate",
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1148:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:1760:./docs/artifacts/fetched/round-016-grok-search-fetch-v1-1-4.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1150:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:1819:./docs/artifacts/fetched/round-016-claude-search-fetch-v1-1-2.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:1152:record/executive/codex-transcripts/20260811T025542Z-63b484b1a536.md:1829:./docs/artifacts/fetched/round-018-grok-search-fetch-v1-4-4.txt:85:        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:3784:    """This tool's own negative control (control 2), on four populations (control 45):
record/executive/codex-transcripts/20260812T043059Z-1dc04ad38cf5.md:5885:4. C45 — makes the growing C2 corpus durable against gate rewrites.
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:251:  45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1168: 45: ("VIOLATED",
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:1672: 45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:2170:  45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3152:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T045315Z-47e808721701.md:3569:   358	 45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:532: 45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:2733:#  This is control 45 applied to the change: the machinery must still catch what it caught, and
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:5596:   300	 45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:7538:   184	 45: ("VIOLATED",
record/executive/codex-transcripts/20260812T031425Z-8a668d13e96f.md:7829:   300	 45: dict(scope=CODE,
record/executive/codex-transcripts/20260812T012550Z-5a86c2022e04.md:137:   retained per control 45.
record/executive/codex-transcripts/20260812T012550Z-5a86c2022e04.md:660:+#  CONTROL 45: the replacement must still catch what the old one caught, and the b*40 case above
record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2445:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |
record/executive/codex-transcripts/20260812T005118Z-9cf1826788b6.md:2461:record/designs/candidate-control-register.md:167:| An incident may not close without a gate disposition | alignment invariants | folded into control 45's recovery clause |

**Inspecting C45 output**
exec
/bin/bash -lc "sed -n '350,395p' tools/control_application.py
sed -n '165,195p' tools/self_application.py
rg -n '"'^45'"\\.|"'^45 |'"\\| 45 \\|' record/controls docs/artifacts -g '*.md' | head -20
rg -n 'Replacement|replacement.*gate|historical false|near-miss' record/controls tools/control_application.py tools/self_application.py | head -50" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
              "carries a stratum label, and nothing refuses a sum across strata."),
 43: dict(scope=NOT_CODE,
          reason="No efficiency claim is made about anything here. Cost is reported from the "
                 "ledger; nothing claims a gain."),
 44: dict(scope=CODE,
          files=("tools/self_application.py", "tools/control_application.py",
                 "tools/control_findings.py"),
          tests=("tools/tests/test_no_blank_cells.py",),
          gap="TWO enumerated matrices are guarded — a missing row, a row for an unregistered "
              "control, an invented state, and a cell filled with something indistinguishable "
              "from an omission are all refused in both. That is not the control. **A third "
              "matrix already exists and nothing checks it**: "
              "record/findings/2026-08-08-search-capability-matrix.json, called the "
              "'full four-endpoint matrix' by round_cycle.py, whose first two rows omit a column "
              "the last two carry. Guarding two named artifacts cannot make the control hold "
              "repository-wide while a fourth can be published tomorrow without registering. "
              "What is missing is a closed-world inventory: an artifact type for published "
              "coverage matrices, a requirement that every one registers, and a completeness "
              "validator run over the registry. This row was TICKED until Codex found the third "
              "matrix on 2026-08-12, which is the same 'enumerated by hand and therefore "
              "complete' error control 19 is about.",
          residue="That any filled cell is CORRECT. This repository has published a complete "
                  "matrix that was wrong — three controls marked ENFORCED citing a file that did "
                  "not exist, and the completeness check passed over it because the reason "
                  "string was long enough. Control 44 asks for no silent blank cells and nothing "
                  "more; a second party is what would supply more."),
 45: dict(scope=CODE,
          files=("tools/control_coverage.py", "tools/executive_log.py"),
          tests=("tools/tests/test_deploy_check.py", "tools/tests/test_gate_negative_controls.py"),
          gap="Applied by hand where a gate changed — the abbreviated-sha case was flipped on "
              "2026-08-12 while the mismatch case it replaced was retained deliberately. "
              "Nothing MECHANICALLY requires a modified gate to still fail its predecessor's "
              "cases, so the next gate change depends on somebody remembering."),
 46: dict(scope=CODE,
          files=("tools/control_coverage.py", "tools/gate_health.py"),
          tests=("tools/tests/test_gate_negative_controls.py",),
          gap="The workbench changes its own implementation every cycle and NO ORIGINAL BASELINE "
              "IS RETAINED, so any divergence that were measured would be measured against the "
              "previous cycle — the comparison the control forbids as the only one. An earlier "
              "version of this row said nothing here drifts across cycles, which is false of the "
              "tools themselves."),
 47: dict(scope=NOT_CODE,
          reason="Structural: no delegation graph. Tools call tools within one process under "
                 "one operator's authority; there is no downstream actor whose scope could be "
                 "inherited."),
 48: dict(scope=NOT_CODE,
 40: ("PRACTISED",
      "The outreach carries a pre-committed adverse outcome with a date, and the prediction "
      "registry scores dated claims on fixed dates. Not every workstream has one."),
 41: ("VIOLATED",
      "Agreement between the two harnesses and across the five-model panel is cited without a "
      "correlation estimate. check_claims.py triages dependence claims, but error correlation "
      "is NOT ESTIMABLE here: it needs repeated cases with externally known answers and no such "
      "labelled dataset exists. The limitation is stated in CLAUDE.md, which is a caveat in a "
      "file rather than a constraint on a number."),
 42: ("VIOLATED",
      "Capability claims here mix strata. 'Built', 'shipped', 'used by anyone' are distinct and "
      "the record does not consistently name which -- the register's own tools are described as "
      "shipped where only the first rung is established."),
 43: ("NOT_APPLICABLE",
      "Structural: no efficiency claim is made. Cost is reported from the ledger as spend, not "
      "as a saving against an alternative."),
 44: ("ENFORCED",
      "This table. --check refuses when any control has no determination, and a NOT_APPLICABLE "
      "must carry a reason."),
 45: ("VIOLATED",
      "Gates here are rewritten routinely and no retained suite proves a replacement still "
      "catches what its predecessor caught. The integrity suite was itself found passing against "
      "seeded pages after a rebuild whose exit code was discarded."),
 46: ("UNRESOLVED",
      "No baseline of intended behaviour was retained at the start, so drift against the "
      "original cannot be computed. The context pins are the nearest thing and they verify "
      "identity, not distance."),
 47: ("NOT_APPLICABLE",
      "Structural: no delegation chain. There is one actor with one lease; nothing receives "
      "authority from something else's output."),
 48: ("NOT_APPLICABLE", "Structural: no composed workflow of separately trusted components."),
record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
docs/artifacts/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |
record/controls/control-application.md:41:| 13 | Re-proposal linkage | ☐ | `tools/agenda_replacement.py` `tools/attempt_ledger.py` `tools/agenda_selectors.py`<br>tested by `tools/tests/test_amendment_disposition.py` `tools/tests/test_attempt_ledger.py`<br>**Remaining:** The attempt ledger refuses a repeat of an (instrument, party, option set) triple by HASH, which keys on identifier and not on mechanism or effect. Rejected amendments HAVE been re-proposed here and were linked by hand. An earlier version of this row said no refused proposal had ever been re-proposed, which self_application contradicts — the claim was false and the prose gate would have caught it had the file not been exempted.<br><small>self-application: UNRESOLVED</small> |
record/controls/control-application.md:73:| 45 | A replacement gate must catch what the old gate caught | ☐ | `tools/control_coverage.py` `tools/executive_log.py`<br>tested by `tools/tests/test_deploy_check.py` `tools/tests/test_gate_negative_controls.py`<br>**Remaining:** Applied by hand where a gate changed — the abbreviated-sha case was flipped on 2026-08-12 while the mismatch case it replaced was retained deliberately. Nothing MECHANICALLY requires a modified gate to still fail its predecessor's cases, so the next gate change depends on somebody remembering.<br><small>self-application: VIOLATED</small> |

**Inspecting test-induced ledger mutations**
exec
/bin/bash -lc "rg -n \"_api\\(\" tools/deploy_obligations.py
sed -n '180,280p' tools/deploy_obligations.py
sed -n '100,140p' tools/build_register_view.py
sed -n '260,310p' tools/build_register_view.py
nl -ba tools/closed_world.py | sed -n '55,155p'
nl -ba tools/scan_own_code.py | sed -n '265,335p'
nl -ba tools/control_coverage.py | sed -n '220,325p'
nl -ba tools/tests/test_closed_world.py | sed -n '55,155p'" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
136:def _api(path: str) -> dict | list | None:
195:    runs = _api(f"/actions/workflows/{WORKFLOW_FILE}/runs"
214:    deployments = _api("/deployments?environment=github-pages&per_page=1")

    THE RUN IS PINNED to the Pages workflow, a push event and the target branch. The waiter this
    replaced asked for "the latest run for this sha" and would have accepted a run from any
    workflow — including one added later that publishes nothing. `land.py` now calls this rather
    than keeping a second, subtly different verifier.
    """
    #  `retriable` separates a PENDING that waiting can resolve (the run has not finished) from
    #  one it cannot (there is no token). Without the distinction `land.py` polled for nine
    #  minutes on a missing credential -- caught by a test that simply stopped returning.
    if not FULL_SHA.match(sha or ""):
        return {"state": PENDING, "commit": sha, "retriable": False,
                "why": "not a full 40-character sha, so nothing can be compared against it"}
    if not os.environ.get("GITHUB_TOKEN"):
        return {"state": PENDING, "retriable": False, "commit": sha,
                "why": "no token in the environment, so nothing can be observed"}
    runs = _api(f"/actions/workflows/{WORKFLOW_FILE}/runs"
                f"?head_sha={sha}&event=push&branch={TARGET_BRANCH}&per_page=5")
    if runs is None:
        return {"state": PENDING, "retriable": True, "commit": sha,
                "why": "the workflow API could not be read"}
    items = [r for r in (runs.get("workflow_runs") or []) if r.get("head_sha") == sha]
    if not items:
        return {"state": PENDING, "retriable": True, "commit": sha,
                "why": "no qualifying Pages run for this commit yet"}
    run = items[0]
    if run.get("status") != "completed":
        return {"state": PENDING, "retriable": True, "commit": sha,
                "why": f"the run is {run.get('status')}", "run_url": run.get("html_url")}
    if run.get("conclusion") != "success":
        return {"state": INCIDENT, "commit": sha, "conclusion": run.get("conclusion"),
                "run_url": run.get("html_url"),
                "why": f"the Pages workflow concluded {run.get('conclusion')}"}

    #  A workflow concluding is not the site serving it.
    deployments = _api("/deployments?environment=github-pages&per_page=1")
    served = (deployments[0].get("sha") if isinstance(deployments, list) and deployments
              else None)
    if served is None:
        return {"state": PENDING, "retriable": True, "commit": sha,
                "run_url": run.get("html_url"),
                "why": "the run succeeded but the deployment API could not be read, so what is "
                       "actually served is unknown"}
    if served != sha:
        return {"state": INCIDENT, "commit": sha, "served_sha": served,
                "run_url": run.get("html_url"),
                "why": f"the run succeeded but the site serves {served[:12]}, not this commit"}
    return {"state": SATISFIED, "commit": sha, "served_sha": served,
            "run_url": run.get("html_url"), "conclusion": "success"}


#  ── the action log ─────────────────────────────────────────────────────────────────────────

def log_rows() -> list[dict]:
    if not LOG.is_file():
        return []
    rows = []
    for number, line in enumerate(LOG.read_text(encoding="utf-8").splitlines(), 1):
        if line.strip():
            try:
                rows.append(json.loads(line))
            except json.JSONDecodeError as exc:
                #  StateUnknown, not a traceback: an unreadable log is unknown state, and a
                #  traceback here would exit 1, which is the code that means "blocked" and would
                #  be indistinguishable from a real finding.
                raise StateUnknown(f"the action log is unreadable at line {number}: {exc}") from exc
    return rows


def attestation_id(row: dict) -> str:
    """A stable identity for one attestation, so an incident names an EVENT and not a commit.

    The log has no ids of its own. This digests the fields that make an attestation what it is.
    `prev_sha256` is deliberately not used: it changes if the chain is ever rebuilt, and the
    identity of a failure event should not.
    """
    claim = row.get("claim") if isinstance(row.get("claim"), dict) else {}
    material = json.dumps({"utc": row.get("utc"), "action": row.get("action"),
                           "commit": claim.get("commit"),
                           "conclusion": claim.get("conclusion"),
                           "deployed_sha": claim.get("deployed_sha")},
                          sort_keys=True)
    return hashlib.sha256(material.encode()).hexdigest()[:16]


#  ── the incident store ─────────────────────────────────────────────────────────────────────

def _require(condition: bool, message: str) -> None:
    if not condition:
        raise StateUnknown(message)


def load_incidents() -> dict[str, dict]:
    """Read and VALIDATE every incident and recovery. Anything malformed is StateUnknown.

    The first version attached any truthy JSON found in a resolution file, so `{"garbage": true}`
    closed an incident while this module's docstring claimed only evidence could. Codex
    reproduced it. Validation is not decoration here; it is the difference between the sentence
    being true and being a wish.
    """
    if not INCIDENTS.is_dir():
        return {}
# --------------------------------------------------------------------------- md
def render_markdown(doc: dict) -> str:
    entries = sorted(doc["entries"], key=lambda e: num(e["id"]))
    by_actor = Counter(e["first_articulation"]["actor_context"] for e in entries)
    by_control = Counter(e["prospective_control"]["state"] for e in entries)
    review = Counter(e["human_review"]["status"] for e in entries)
    rows = sum(len(e["affected_objects"]) for e in entries)

    out = [
        "# Deficiency register — classification\n\n",
        "**Generated** by `tools/build_register_view.py` from "
        "`corpus/artifacts/deficiency-register.json`. Do not edit this file; edit the artifact.\n\n",
        "[`corpus/deficiencies.md`](deficiencies.md) is **canonical**. This is metadata over it, "
        "and it carries no detail about what any defect actually was — follow the links.\n\n",
        "---\n\n## What this classification does and does not establish\n\n",
        "It records, per entry, where the defect was **first substantively articulated in preserved "
        "material**, what it affects, whether each affected object is repairable, and whether a "
        "forward control exists and has been validated.\n\n",
        "It does **not** establish that any of those judgements is correct. They were made by the "
        "annotator, which is a party to the record. `tools/check_register.py` verifies structure, "
        "one-to-one coverage, vocabulary, and that each entry's prose has not changed since it was "
        "classified. **It never verifies meaning**, and a deterministic rule that claimed to would "
        "be D-25 over again.\n\n",
        #  0, NOT '?'. `review` is a Counter built by iterating EVERY entry's status, so an
        #  absent bucket means no entry has that status — a known zero, not a missing
        #  measurement. A C53 sweep changed this to '?' and published "? of 58" where the truth
        #  is "0 of 58": control 53 applied in the wrong direction, manufacturing an unknown out
        #  of a fact. Found by Codex on 2026-08-12 while reviewing the test written to protect
        #  the original repair. The genuine unknown is the KEY being absent, which raises three
        #  lines above and is what test_gate_refusals.py asserts.
        f"**{review.get('reviewed_by_custodian', 0)} of {len(entries)}** classifications have been "
        "read by a human against the prose.\n\n",
        "On attribution: this project cannot observe who first *privately noticed* a defect, so it "
        "records where one was first *written down*, with an evidence class. A question that "
        "prompted an investigation is recorded as a **trigger**, not as the finding.\n\n",
        "---\n\n## Counts\n\n### First substantive articulation\n\n| Where | Entries |\n|---|---|\n",
    ]
    for actor, count in sorted(by_actor.items(), key=lambda kv: -kv[1]):
        out.append(f"| {ACTOR_LABEL[actor]} | {count} |\n")

    reviewer_found = sorted(
  let n=0;
  for(const el of $$('.e')){
    const ok=(!f.actor.size||f.actor.has(el.dataset.actor))
          && (!f.control.size||f.control.has(el.dataset.control));
    el.classList.toggle('hidden',!ok); if(ok)n++;
  }
  document.getElementById('count').textContent=n+' of '+$$('.e').length+' entries';
}
document.addEventListener('DOMContentLoaded',()=>{
  $$('[data-f]').forEach(b=>b.addEventListener('click',()=>{
    const s=f[b.dataset.f], v=b.dataset.v;
    s.has(v)?s.delete(v):s.add(v);
    b.setAttribute('aria-pressed',s.has(v)); apply();
  }));
  document.getElementById('theme').addEventListener('click',()=>{
    const cur=document.documentElement.getAttribute('data-theme');
    const next=cur==='dark'?'light':cur==='light'?'':'dark';
    next?document.documentElement.setAttribute('data-theme',next)
        :document.documentElement.removeAttribute('data-theme');
    document.getElementById('theme').textContent=next||'auto';
  });
  apply();
});
"""


def render_html(doc: dict, subset: list[dict] | None = None,
                pager: str = "", title_suffix: str = "") -> str:
    e_ = html.escape
    entries = subset if subset is not None else sorted(doc["entries"], key=lambda x: num(x["id"]))
    # Counters describe the WHOLE register, not this page's slice. A per-page count
    # would give four pages four different totals for the same register -- which is
    # the exact defect the register was already caught committing when three
    # artifacts of this repository stated three different counts of its own defects.
    every = sorted(doc["entries"], key=lambda x: num(x["id"]))
    by_actor = Counter(x["first_articulation"]["actor_context"] for x in every)
    by_control = Counter(x["prospective_control"]["state"] for x in every)
    by_evidence = Counter(x["first_articulation"]["evidence_class"] for x in every)
    reviewed = sum(1 for x in every
                   if x["human_review"]["status"] == "reviewed_by_custodian")
    rows = sum(len(x["affected_objects"]) for x in every)
    reviewer = sorted((x["id"] for x in every
                       if x["first_articulation"]["actor_context"]
                       == "actor_designated_review_round_invocation"), key=num)

    body = []
    for x in entries:
        fa = x["first_articulation"]
        control = x["prospective_control"]
        tags = [f'<span class="tag {CONTROL_CLASS[control["state"]]}">'
                f'{e_(CONTROL_LABEL[control["state"]])}</span>']
    55	    """The walk did not account for everything, so there is no number to report.
    56	
    57	    Raised rather than returned. A caller that ignores a returned error object publishes the
    58	    zero anyway, which is the failure this module exists for — and this repository has an
    59	    action-log entry proving a returned status gets dropped.
    60	    """
    61	
    62	
    63	@dataclass
    64	class Survey:
    65	    """A population walk that refuses to yield counts when it could not see everything."""
    66	
    67	    name: str
    68	    scope: str = ""
    69	    visited: int = 0
    70	    counts: dict[str, int] = field(default_factory=dict)
    71	    unreadable_items: list[str] = field(default_factory=list)
    72	    unregistered_items: list[str] = field(default_factory=list)
    73	    excluded_items: list[str] = field(default_factory=list)
    74	
    75	    def seen(self, key: str) -> None:
    76	        """One in-scope artifact was reached. Call this BEFORE trying to parse it."""
    77	        self.visited += 1
    78	
    79	    def unreadable(self, key: str, why: str) -> None:
    80	        """It could not be parsed. This is what makes the survey unusable."""
    81	        self.unreadable_items.append(f"{key}: {why}")
    82	
    83	    def unregistered(self, key: str, why: str) -> None:
    84	        """It parsed and its shape is not one this measurement knows. Also unusable.
    85	
    86	        SEPARATE FROM `unreadable` on purpose. "I could not read it" and "I read it and do not
    87	        know what it is" have different repairs — one is a corrupt file, the other a gap in the
    88	        registry — and collapsing them sends the reader to the wrong place.
    89	        """
    90	        self.unregistered_items.append(f"{key}: {why}")
    91	
    92	    def excluded(self, key: str, decision: str) -> None:
    93	        """Out of scope BY A RECORDED DECISION, which is not the same as not being seen.
    94	
    95	        `decision` is required and must name the reason. An exclusion with no stated ground is
    96	        indistinguishable from an omission, which is control 44's shape appearing inside a
    97	        control 5 mechanism.
    98	        """
    99	        if not decision or len(decision) < 10:
   100	            raise ValueError(f"excluding {key!r} needs a stated ground, not {decision!r}")
   101	        self.excluded_items.append(f"{key}: {decision}")
   102	
   103	    def count(self, bucket: str, n: int = 1) -> None:
   104	        self.counts[bucket] = self.counts.get(bucket, 0) + n
   105	
   106	    @property
   107	    def usable(self) -> bool:
   108	        return not (self.unreadable_items or self.unregistered_items)
   109	
   110	    @property
   111	    def accounted(self) -> int:
   112	        return sum(self.counts.values()) if self.counts else 0
   113	
   114	    def result(self) -> dict:
   115	        """The numbers — or REFUSE, naming everything that went unaccounted for."""
   116	        if not self.usable:
   117	            raise IncompleteSurvey(
   118	                f"{self.name}: {len(self.unreadable_items)} unreadable and "
   119	                f"{len(self.unregistered_items)} unregistered of {self.visited} in scope"
   120	                f"{f' ({self.scope})' if self.scope else ''}. NO COUNT IS REPORTED — a scan that "
   121	                f"cannot see an artifact reports absence, and absence looks exactly like a true "
   122	                f"zero.\n  "
   123	                + "\n  ".join(self.unreadable_items[:5] + self.unregistered_items[:5]))
   124	        return {"name": self.name, "scope": self.scope, "visited": self.visited,
   125	                "counts": dict(self.counts), "excluded": list(self.excluded_items)}
   126	
   127	    def report(self) -> str:
   128	        """Always safe to call, including when the survey refuses. Coverage, not counts."""
   129	        lines = [f"  {self.name}: {self.visited} artifact(s) in scope"
   130	                 + (f" ({self.scope})" if self.scope else "")]
   131	        if self.excluded_items:
   132	            lines.append(f"    {len(self.excluded_items)} excluded by a recorded decision")
   133	        if self.usable:
   134	            for bucket, n in sorted(self.counts.items(), key=lambda kv: -kv[1]):
   135	                lines.append(f"    {n:6d}  {bucket}")
   136	        else:
   137	            lines.append("    NO COUNT — the walk did not account for everything:")
   138	            for item in self.unreadable_items[:5]:
   139	                lines.append(f"      unreadable   {item}")
   140	            for item in self.unregistered_items[:5]:
   141	                lines.append(f"      unregistered {item}")
   142	            more = (len(self.unreadable_items) + len(self.unregistered_items)) - 10
   143	            if more > 0:
   144	                lines.append(f"      … {more} more")
   145	        return "\n".join(lines)
   265	        return run_fixtures()
   266	
   267	    if run_fixtures() != 0:
   268	        return 1
   269	    print()
   270	    #  CONTROL 5 THROUGH A TYPE, not a counter. This walked every tool, incremented `unparsed`
   271	    #  on a file it could not read, and then PRINTED THE HIT COUNTS ANYWAY with the unparsed
   272	    #  number beside them. Reporting coverage next to a count is better than hiding it and is
   273	    #  still not what the control says: parse every in-scope artifact, OR REFUSE TO EMIT ANY
   274	    #  RESULT. A reader who sees "3 hits, 2 unparsed" has no way to know whether the two held
   275	    #  thirty. `Survey.result()` raises instead, and `report()` says what could not be read.
   276	    survey = closed_world.Survey("defect-class scan over this repository's own tools",
   277	                                 scope=str(SCANNED.relative_to(REPO_ROOT)) + "/**/*.py")
   278	    disp = load_dispositions()
   279	    #  The population is walked once per class; only the FIRST pass surveys it, or every file
   280	    #  would be counted five times and the coverage number would be nonsense.
   281	    first_label = next(iter(CLASSES))
   282	    for label, fn in CLASSES.items():
   283	        hits, dispositioned = [], 0
   284	        for path in sorted(SCANNED.rglob("*.py")):
   285	            if label == first_label:
   286	                survey.seen(str(path.relative_to(REPO_ROOT)))
   287	            try:
   288	                text = path.read_text(encoding="utf-8")
   289	                tree = ast.parse(text)
   290	            except Exception as error:                                   # noqa: BLE001
   291	                if label == first_label:
   292	                    survey.unreadable(str(path.relative_to(REPO_ROOT)),
   293	                                      f"{type(error).__name__}: {error}")
   294	                continue
   295	            source_lines = text.splitlines()
   296	            for n in fn(tree):
   297	                line_text = source_lines[n - 1] if n <= len(source_lines) else ""
   298	                if site_key(path.relative_to(REPO_ROOT), line_text) in disp:
   299	                    dispositioned += 1
   300	                    continue
   301	                hits.append(f"{path.relative_to(REPO_ROOT)}:{n}")
   302	        #  The counts go INTO the survey, so a walk that could not read a file withholds them
   303	        #  rather than printing them beside an unparsed tally. That printing-beside is what this
   304	        #  routing replaced.
   305	        survey.count(f"{label} — undispositioned", len(hits))
   306	        if dispositioned:
   307	            survey.count(f"{label} — dispositioned", dispositioned)
   308	        print(f"  {label}: {len(hits)} undispositioned"
   309	              f"{f', {dispositioned} dispositioned' if dispositioned else ''}")
   310	        for h in hits[:8]:
   311	            print(f"      {h}")
   312	        if len(hits) > 8:
   313	            print(f"      … {len(hits) - 8} more")
   314	    print()
   315	    print(survey.report())
   316	    try:
   317	        survey.result()
   318	    except closed_world.IncompleteSurvey as refusal:
   319	        #  THE HITS ABOVE ARE NOT A RESULT. They are what was found in the part that could be
   320	        #  read, and the control says a partial walk yields no count at all.
   321	        print(f"\n  REFUSED: {refusal}", file=sys.stderr)
   322	        return 1
   323	    print("  A HIT IS A CANDIDATE, NOT A DEFECT. On 2026-08-11 all six D-E hits were functions")
   324	    print("  returning a typed unknown that the caller checks — correct, not broken.")
   325	    print("  This is not a compliance check against the register. See the module docstring.")
   326	    return 0
   327	
   328	
   329	if __name__ == "__main__":
   330	    raise SystemExit(main())
   220	                return (True, f"{name} asserts a refusal near a reference to it")
   221	    return (False, f"referenced by {referencing[0][0]} but no refusal is asserted near it")
   222	
   223	
   224	def survey_population() -> "closed_world.Survey":
   225	    """The walk, under control 5: a rate this cannot compute is not reported as a rate.
   226	
   227	    This tool publishes a PERCENTAGE over a population of files, which is exactly the shape
   228	    control 5 governs — and it read the tests by `read_text(errors="replace")` and parsed them
   229	    with `ast`, swallowing a SyntaxError into `pass`. A test file that would not parse simply
   230	    contributed nothing, and the percentage came out looking like a measurement.
   231	
   232	    That is not a hypothetical for this tool in particular. It has already been wrong in both
   233	    directions in one day: counting a tool named only in a comment, and missing one exercised
   234	    through a wrapper. A rate from an incomplete walk is the third way it could be wrong, and
   235	    the only one nobody would see.
   236	    """
   237	    survey = closed_world.Survey("tools with a negative control", scope="tools/*.py")
   238	    for path in tool_files():
   239	        survey.seen(path.name)
   240	        try:
   241	            ast.parse(path.read_text(encoding="utf-8"))
   242	        except (SyntaxError, UnicodeDecodeError, OSError) as error:
   243	            survey.unreadable(path.name, f"{type(error).__name__}: {error}")
   244	            continue
   245	        if path.name in NO_SIGNAL:
   246	            survey.excluded(path.name, NO_SIGNAL[path.name])
   247	            continue
   248	        ok, _why = has_negative_control(path.stem)
   249	        survey.count("has a negative control" if ok else "has none")
   250	    for t in sorted(TESTS.rglob("*.py")):
   251	        #  THE TESTS ARE IN SCOPE TOO. The verdict for every tool depends on reading them, so a
   252	        #  test this cannot parse makes every "NONE" unreliable, not just its own.
   253	        survey.seen(f"tests/{t.name}")
   254	        try:
   255	            ast.parse(t.read_text(encoding="utf-8"))
   256	        except (SyntaxError, UnicodeDecodeError, OSError) as error:
   257	            survey.unreadable(f"tests/{t.name}", f"{type(error).__name__}: {error}")
   258	    return survey
   259	
   260	
   261	def rows() -> list[tuple[str, str, str]]:
   262	    out = []
   263	    for path in tool_files():
   264	        stem = path.stem
   265	        if path.name in NO_SIGNAL:
   266	            out.append((path.name, "NOT_APPLICABLE", NO_SIGNAL[path.name]))
   267	            continue
   268	        ok, why = has_negative_control(stem)
   269	        out.append((path.name, "HAS_NEGATIVE_CONTROL" if ok else "NONE", why))
   270	    return out
   271	
   272	
   273	def main() -> int:
   274	    parser = argparse.ArgumentParser(description=__doc__.strip().splitlines()[0])
   275	    parser.add_argument("--check", action="store_true",
   276	                        help="exit non-zero if any tool has no determination")
   277	    args = parser.parse_args()
   278	
   279	    survey = survey_population()
   280	    data = rows()
   281	    has = [r for r in data if r[1] == "HAS_NEGATIVE_CONTROL"]
   282	    none = [r for r in data if r[1] == "NONE"]
   283	    na = [r for r in data if r[1] == "NOT_APPLICABLE"]
   284	
   285	    if args.check:
   286	        undetermined = [r for r in data if r[1] not in
   287	                        ("HAS_NEGATIVE_CONTROL", "NONE", "NOT_APPLICABLE")]
   288	        if undetermined:
   289	            for r in undetermined:
   290	                print(f"  no determination for {r[0]}", file=sys.stderr)
   291	            return 1
   292	        print(f"  every tool has a determination ({len(data)}).")
   293	        return 0
   294	
   295	    print("  CONTROL 2 PER FILE — does each tool have a case it must fail?\n")
   296	    print(f"    HAS_NEGATIVE_CONTROL  {len(has):3d}")
   297	    print(f"    NONE                  {len(none):3d}")
   298	    print(f"    NOT_APPLICABLE        {len(na):3d}   (each with a stated reason)")
   299	    print(f"    ---------------------------")
   300	    print(f"    total                 {len(data):3d}")
   301	    pct = len(has) / (len(has) + len(none)) if (has or none) else 0
   302	    #  CONTROL 5: THE RATE IS WITHHELD IF THE WALK WAS INCOMPLETE. A percentage over a
   303	    #  population that could not all be read is the "0 searches across 83 tool calls" shape with
   304	    #  a denominator attached, and this tool has already been wrong in two other directions in
   305	    #  one day. `report()` still says what was and was not seen — coverage instead of a rate.
   306	    try:
   307	        survey.result()
   308	    except closed_world.IncompleteSurvey as refusal:
   309	        print("\n" + survey.report())
   310	        print(f"\n  NO RATE IS REPORTED: {refusal}", file=sys.stderr)
   311	        return 1
   312	    print(f"\n    {pct:.0%} of tools with an assurance signal have a negative control.\n")
   313	    print("  WITHOUT ONE:")
   314	    for name, _s, why in none[:20]:
   315	        print(f"    {name:34} {why}")
   316	    if len(none) > 20:
   317	        print(f"    … {len(none) - 20} more")
   318	    print("\n  A pass means SOME test asserts a refusal near a reference to the tool. It does not")
   319	    print("  mean the control is demanding, that it targets the capability rather than the")
   320	    print("  transport, or that the tool is correct. Control 2 raises the floor from 'never")
   321	    print("  observed to fail' to 'observed to fail at least once'. This measures that floor and")
   322	    print("  nothing above it.")
   323	    print("\n  Only control 2 is measured. C5, C10 and C53 need semantics and are NOT attempted;")
   324	    print("  every other control has no file-level surface. See the module docstring.")
   325	    return 0
    55	    sys.modules[name] = module
    56	    spec.loader.exec_module(module)
    57	    return module
    58	
    59	
    60	cw = load("closed_world")
    61	
    62	print("\nthe type: a complete walk yields numbers, an incomplete one yields none")
    63	
    64	survey = cw.Survey("a complete walk", scope="fixture")
    65	for i in range(5):
    66	    survey.seen(f"item-{i}")
    67	    survey.count("counted")
    68	check("BASELINE: a complete walk is usable", survey.usable is True)
    69	check("...and result() returns the counts", survey.result()["counts"] == {"counted": 5})
    70	check("...and report() shows them", "5" in survey.report())
    71	
    72	#  ONE UNREADABLE ARTIFACT, and the count is gone. Not caveated — gone.
    73	survey = cw.Survey("a walk with one file it could not read", scope="fixture")
    74	for i in range(5):
    75	    survey.seen(f"item-{i}")
    76	    survey.count("counted")
    77	survey.unreadable("item-6", "SyntaxError: invalid syntax")
    78	check("one UNREADABLE artifact makes the survey unusable", survey.usable is False)
    79	raised = False
    80	try:
    81	    survey.result()
    82	except cw.IncompleteSurvey:
    83	    raised = True
    84	check("...and result() RAISES rather than returning a partial count", raised,
    85	      "a returned error object gets dropped; this repository has an action-log entry proving it")
    86	check("...and report() is still safe to call", "NO COUNT" in survey.report())
    87	check("...and names the artifact it could not read", "item-6" in survey.report())
    88	check("...and does NOT print the five it did count",
    89	      "5" not in survey.report().split("NO COUNT")[1])
    90	
    91	#  UNREGISTERED IS SEPARATE FROM UNREADABLE, because the repairs differ.
    92	survey = cw.Survey("a walk with an unrecognised shape", scope="fixture")
    93	survey.seen("item-1")
    94	survey.unregistered("item-1", "artifact_type 'zzqx' is not registered")
    95	check("an UNREGISTERED artifact also makes the survey unusable", survey.usable is False)
    96	check("...and is reported distinctly from an unreadable one",
    97	      "unregistered" in survey.report() and "unreadable   " not in survey.report(),
    98	      survey.report())
    99	
   100	#  AN EXCLUSION NEEDS A GROUND. An exemption with no reason is control 44's shape appearing
   101	#  inside a control 5 mechanism, so the type refuses to record one.
   102	survey = cw.Survey("a walk with exclusions", scope="fixture")
   103	survey.seen("item-1")
   104	survey.excluded("item-1", "a helper with no verdict of its own, excluded by a recorded decision")
   105	check("an exclusion WITH a stated ground is accepted", len(survey.excluded_items) == 1)
   106	check("...and does not make the survey unusable", survey.usable is True)
   107	for bad in ("", "n/a", "skip"):
   108	    refused = False
   109	    try:
   110	        survey.excluded("item-2", bad)
   111	    except ValueError:
   112	        refused = True
   113	    check(f"an exclusion with no ground ({bad!r}) is REFUSED", refused,
   114	          "an exemption with no stated reason is indistinguishable from an omission")
   115	
   116	print("\nthe routing: an unreadable file must withhold each tool's published number")
   117	
   118	
   119	def run(tool: str, *args: str) -> sp.CompletedProcess:
   120	    return sp.run([sys.executable, str(ROOT / tool), *args],
   121	                  cwd=REPO, capture_output=True, text=True)
   122	
   123	
   124	BROKEN = "def broken(:\n"
   125	
   126	for tool, probe, number in (
   127	        ("scan_own_code.py", ROOT / "zzqx_unparseable_probe.py", "file(s) scanned"),
   128	        ("control_coverage.py", ROOT / "tests" / "test_zzqx_unparseable_probe.py",
   129	         "% of tools")):
   130	    #  BASELINE FIRST. A tool that refuses whatever happens is broken, not strict.
   131	    r = run(tool)
   132	    check(f"BASELINE: {tool} reports its number on a clean tree", r.returncode == 0,
   133	          (r.stdout + r.stderr)[-200:])
   134	
   135	    try:
   136	        probe.write_text(BROKEN, encoding="utf-8")
   137	        r = run(tool)
   138	        output = r.stdout + r.stderr
   139	        check(f"{tool} REFUSES when one file in its population cannot be read",
   140	              r.returncode != 0, f"rc={r.returncode}")
   141	        check(f"...and {tool} names the file it could not read",
   142	              "zzqx_unparseable_probe" in output, output[-200:])
   143	        check(f"...and {tool} publishes NO number from the partial walk",
   144	              number not in output,
   145	              f"a rate from an incomplete walk is the false zero with a denominator attached")
   146	    finally:
   147	        probe.unlink(missing_ok=True)
   148	
   149	    r = run(tool)
   150	    check(f"...and {tool} reports again once the tree is clean", r.returncode == 0)
   151	
   152	print(f"\n{passed} passed, {FAILED} failed")
   153	raise SystemExit(1 if FAILED else 0)

**Verifying disposition metadata consistency**
**Flagging schema omissions in dispositions**
codex
The three semantic dispositions are sound: `_api()`’s `None` is handled as pending/unobserved at both call sites; both zero defaults are over fully enumerated `Counter` populations, so absence of the bucket is a known zero. There is a record-shape issue, though: the three new disposition objects switch from the artifact’s established `file/class/why/read` fields to `path/note/by/utc`, and the loader validates none of it. I’ll include that as an auditability fix, separate from the correctness of the judgments.
exec
/bin/bash -lc 'python3 tools/tests/run_all.py' in /home/reed/git/open-asi-governance-forum
**Tracing missing exit code in exec session**
**Planning process check with pgrep**
exec
/bin/bash -lc "pgrep -af 'python3 tools/tests/run_all.py' || true" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
1011423 python3 tools/tests/run_all.py
1023522 /bin/bash -c pgrep -af 'python3 tools/tests/run_all.py' || true

**Planning non-blocking process wait**
 succeeded in 45474ms:

every amendment in the record has a disposition
  [32m✓[0m no clause carries an undisposed amendment

the rule is registered in the spec BEFORE a ballot is sent
  [32m✓[0m the spec carries the rule
  [32m✓[0m it names all four dispositions
  [32m✓[0m it forbids silent dropping
  [32m✓[0m it blocks re-balloting
  [32m✓[0m it says selection is disclosed
  [32m✓[0m it is fixed before sending

adoption is not ratification, and the artifacts say so
  [32m✓[0m ratification-01-amendment-dispositions.json: states what adoption is NOT
  [32m✓[0m ratification-01-amendment-dispositions.json: discloses that selection was the workbench's
  [32m✓[0m ratification-01-amendment-dispositions.json: its convergence measure states a METHOD
  [32m✓[0m ratification-01-amendment-dispositions.json: every amendment keeps verbatim text and a ground
  [32m✓[0m ratification-01-amendment-dispositions.json: an ADOPTED amendment names where it went
  [32m✓[0m ratification-01-amendment-dispositions.json: a DEFERRED amendment names a review date
  [32m✓[0m ratification-02-amendment-dispositions.json: states what adoption is NOT
  [32m✓[0m ratification-02-amendment-dispositions.json: discloses that selection was the workbench's
  [32m✓[0m ratification-02-amendment-dispositions.json: its convergence measure states a METHOD
  [32m✓[0m ratification-02-amendment-dispositions.json: every amendment keeps verbatim text and a ground
  [32m✓[0m ratification-02-amendment-dispositions.json: an ADOPTED amendment names where it went
  [32m✓[0m ratification-02-amendment-dispositions.json: a DEFERRED amendment names a review date

the adopted C06 text is a party's verbatim words
  [32m✓[0m exactly one amendment was adopted
  [32m✓[0m ...it is a C06 amendment
  [32m✓[0m ...it names the party and sample it came from
  [32m✓[0m ...and its verbatim words appear in the governing instructions

the tombstone mechanism exists, not just the sentence
  [32m✓[0m the manifest verifier reads tombstones
  [32m✓[0m an incomplete tombstone is not accepted as a withdrawal
  [32m✓[0m a withdrawal must match the hash it claims to remove

26 passed, 0 failed

a second draw on the same question is refused
  [32m✓[0m the first attempt is recorded
  [32m✓[0m the identical question to the same party is refused
  [32m✓[0m a different prompt text over the SAME option set is still a redraw
  [32m✓[0m a different k over the same option set is still a redraw
  [32m✓[0m SHUFFLING the enum does not present it as a new question

a genuinely different question is allowed
  [32m✓[0m adding candidates to the option set is a new question
  [32m✓[0m a narrowed option set is also a new question
  [32m✓[0m another PARTY over the same options is unaffected
  [32m✓[0m a different INSTRUMENT over the same options is unaffected

the check is not separable from the write
  [32m✓[0m a refused attempt writes NOTHING to the ledger
  [32m✓[0m check() alone raises on a redraw
  [32m✓[0m check() alone appends nothing
  [32m✓[0m check() alone permits a genuinely different option set

the record supports auditing an asymmetry
  [32m✓[0m every party asked is recoverable from the ledger
  [32m✓[0m the full option set is stored, not only its hash
  [32m✓[0m entries are hash-chained to the one before
  [32m✓[0m the guard does NOT claim to prove uniformity

17 passed, 0 failed

it withholds only when a single call cannot complete
  [32m✓[0m today's 54,191-char prompt does NOT fit
  [32m✓[0m the reason names the ceiling, the prompt and the result size
  [32m✓[0m the reason says it was measured, not chosen
  [32m✓[0m the reason records that raising it was ATTEMPTED and failed
  [32m✓[0m a prompt just under the threshold (6,846 chars) fits
  [32m✓[0m a prompt just over it does not

the gate re-opens on its own when the numbers change
  [32m✓[0m a larger measured ceiling re-admits today's prompt
  [32m✓[0m shrinking the PROMPT alone is not enough at the 60k cap

absence of a measurement never causes a withhold
  [32m✓[0m a fingerprint with no ceiling permits
  [32m✓[0m a missing fingerprint permits rather than refusing

a party with no capability is untouched
  [32m✓[0m no capability -> fits, no reason

the live fingerprint carries what the gate needs
  [32m✓[0m it records a measured prefill ceiling
  [32m✓[0m it records the observed chars/token, not an assumed one
  [32m✓[0m it records HOW it was measured
  [32m✓[0m it records that raising it was attempted and failed

the withholding is recorded on the spec, never inferred from silence
  [32m✓[0m the spec schema admits capability_withheld
  [32m✓[0m ...and says why absence must not be inferred

17 passed, 0 failed
gates capture-gates-0.1  floor=50  threshold=0.6
33 passed, 0 failed
lifecycle oagrc-capture-lifecycle-0.1
39 passed, 0 failed

append-only guard: it must refuse an edit to committed raw material
  [32m✓[0m BASELINE: appending new raw material is allowed
  [32m✓[0m MODIFYING committed raw material is refused
  [32m✓[0m DELETING committed raw material is refused

manifest verification: it must refuse a hash that no longer matches
  [32m✓[0m BASELINE: the live manifest verifies
  [32m✓[0m the manifest holds entries, not only comments
  [32m✓[0m a manifest entry matches the file it names
  [32m✓[0m appending one byte changes the hash

anchor_manifest.py — verify() is offline, so it CAN be negative-controlled
  [32m✓[0m BASELINE: the current manifest is anchored
  [32m✓[0m a manifest CHANGED AFTER STAMPING is refused
  [32m✓[0m ...and the manifest is restored
  [32m✓[0m a MISSING receipt is refused (MANIFEST-deecaf750a2d003b.ots)
  [32m✓[0m ...and the receipt is restored

12 passed, 0 failures

the type: a complete walk yields numbers, an incomplete one yields none
  [32m✓[0m BASELINE: a complete walk is usable
  [32m✓[0m ...and result() returns the counts
  [32m✓[0m ...and report() shows them
  [32m✓[0m one UNREADABLE artifact makes the survey unusable
  [32m✓[0m ...and result() RAISES rather than returning a partial count
  [32m✓[0m ...and report() is still safe to call
  [32m✓[0m ...and names the artifact it could not read
  [32m✓[0m ...and does NOT print the five it did count
  [32m✓[0m an UNREGISTERED artifact also makes the survey unusable
  [32m✓[0m ...and is reported distinctly from an unreadable one
  [32m✓[0m an exclusion WITH a stated ground is accepted
  [32m✓[0m ...and does not make the survey unusable
  [32m✓[0m an exclusion with no ground ('') is REFUSED
  [32m✓[0m an exclusion with no ground ('n/a') is REFUSED
  [32m✓[0m an exclusion with no ground ('skip') is REFUSED

the routing: an unreadable file must withhold each tool's published number
  [32m✓[0m BASELINE: scan_own_code.py reports its number on a clean tree
  [32m✓[0m scan_own_code.py REFUSES when one file in its population cannot be read
  [32m✓[0m ...and scan_own_code.py names the file it could not read
  [32m✓[0m ...and scan_own_code.py publishes NO number from the partial walk
  [32m✓[0m ...and scan_own_code.py reports again once the tree is clean
  [32m✓[0m BASELINE: control_coverage.py reports its number on a clean tree
  [32m✓[0m control_coverage.py REFUSES when one file in its population cannot be read
  [32m✓[0m ...and control_coverage.py names the file it could not read
  [32m✓[0m ...and control_coverage.py publishes NO number from the partial walk
  [32m✓[0m ...and control_coverage.py reports again once the tree is clean

25 passed, 0 failed

cohort identity is asserted, never inferred from absence
  [32m✓[0m a well-formed cohort loads
  [32m✓[0m the successful party is listed as successful
  [32m✓[0m the party whose every attempt failed is listed as failed
  [32m✓[0m a total failure does NOT suppress the parties that succeeded
  [32m✓[0m a descriptor that is not a proposal_cohort is refused
  [32m✓[0m a missing descriptor raises rather than inferring a cohort

every solicited party is accounted for, exactly once
  [32m✓[0m a party in BOTH the successful and failed sets is refused
  [32m✓[0m a solicited party with neither summary nor rejection is refused
  [32m✓[0m a summary with no solicitation spec is refused
  [32m✓[0m an ORPHAN rejection record with no spec is refused
  [32m✓[0m a summary with no raw samples behind it is refused

a rejection record accounts for every attempt that was paid for
  [32m✓[0m a record listing 1 of 5 requested attempts is refused

the two publishers do not delete each other's artifacts
  [32m✓[0m the cohort publisher writes to its own subtree, not docs/rounds/
  [32m✓[0m the cohort pruner only ever walks its own subtree
  [32m✓[0m the round pruner takes an explicit ownership list for shared directories
  [32m✓[0m a stale page in its OWN subtree is pruned
  [32m✓[0m an expected page is kept
  [32m✓[0m a stale prompt it OWNS is pruned
  [32m✓[0m another generator's artifact in the SHARED directory survives

the published banner is unmistakable
  [32m✓[0m the banner denies that a cycle occurred
  [32m✓[0m the banner denies that anything entered the agenda
  [32m✓[0m the banner denies that a question was asked
  [32m✓[0m the banner covers reporter suggestions too, not only party proposals

the live cohort is internally consistent
  [32m✓[0m agenda-02 loads from the committed record
  [32m✓[0m all five solicited parties are accounted for
  [32m✓[0m claude-fetch-v1 is recorded as failed, not as a refusal
  [32m✓[0m its rejection record accounts for all five attempts

27 passed, 0 failed

a clean deploy passes
  [32m✓[0m matching sha and a successful conclusion

UNOBSERVED IS REFUSED — every path that could not look
  [32m✓[0m refused: no GITHUB_TOKEN in the environment
  [32m✓[0m refused: the workflow API could not be read
  [32m✓[0m refused: still queued after 900s -- not a failure, an
  [32m✓[0m refused: the deployment API could not be read

a failed or mismatched deploy is refused
  [32m✓[0m a failed workflow conclusion is refused
  [32m✓[0m a cancelled workflow conclusion is refused
  [32m✓[0m serving a DIFFERENT commit is refused
  [32m✓[0m ...and the message names both shas
  [32m✓[0m a claim missing either sha is refused
  [32m✓[0m an ABBREVIATED sha is refused rather than prefix-matched
  [32m✓[0m ...and the refusal explains why an abbreviation is itself the problem
  [32m✓[0m a one-character sha does not match a commit of the same letter

the waiter cannot report success without a token
  [32m✓[0m no token yields observed=False
  [32m✓[0m ...and says why, naming the missing credential

the check is wired into landing, and skipping it is explicit
  [32m✓[0m land.py attests the deploy
  [32m✓[0m a refused deploy exits non-zero
  [32m✓[0m it says the commit cannot be unpushed
  [32m✓[0m skipping is opt-in, not the default
  [32m✓[0m deploy is a registered postcondition profile

20 passed, 0 failed

the ordinary case must NOT block — an interlock that always fires is not a control
  [32m✓[0m BASELINE: a push that deployed and is served blocks nothing

an OBSERVED failure must block the next ordinary landing
  [32m✓[0m a failed deploy blocks
  [32m✓[0m ...and an incident file was written
  [32m✓[0m ...and it STILL blocks on the next call, which is the whole defect
  [32m✓[0m ...without opening a second incident for the same commit

PENDING is not a violation, and it still blocks
  [32m✓[0m a push with no deploy record blocks
  [32m✓[0m ...and does NOT open an incident, because nothing was observed to be wrong
  [32m✓[0m ...and clears by itself once the deploy can be observed
  [32m✓[0m ...still opening no incident

resolution takes evidence, and nothing else
  [32m✓[0m resolving against a head that is not deployed is REFUSED
  [32m✓[0m ...and no recovery artifact was written
  [32m✓[0m ...and it still blocks
  [32m✓[0m resolving against a deployed, served head SUCCEEDS
  [32m✓[0m ...and the incident file itself was NOT edited — corrections attach
  [32m✓[0m ...and no incident remains open
  [32m✓[0m ...and what still blocks is the undischarged push, not the closed incident

crash between transitions — the paths that lose information
  [32m✓[0m crash after push, before the wait: reconciling still finds the failure
  [32m✓[0m crash after the attestation, before the incident: none exists yet
  [32m✓[0m ...and the next preflight reconstructs it from the log
  [32m✓[0m crash after a successful remediation, before resolution: it STILL blocks
  [32m✓[0m ...and the earlier failure was reconstructed even though a LATER push succeeded
  [32m✓[0m ...and a later successful deploy did not silently close it
  [32m✓[0m crash during resolution: a truncated resolution is STATE UNKNOWN, not resolved

malformed state fails CLOSED — unknown must never read as clear
  [32m✓[0m an unreadable incident -> blocks with STATE UNKNOWN
  [32m✓[0m an incident naming no commit -> blocks with STATE UNKNOWN
  [32m✓[0m an incident whose internal id disagrees with its filename -> blocks with STATE UNKNOWN
  [32m✓[0m an incident declaring an unknown schema version -> blocks with STATE UNKNOWN
  [32m✓[0m an incident with no source_attestation, so it names no observed event -> blocks with STATE UNKNOWN
  [32m✓[0m a recovery resolving an incident that does not exist -> blocks with STATE UNKNOWN
  [32m✓[0m a recovery whose evidence is not a SATISFIED observation -> blocks with STATE UNKNOWN
  [32m✓[0m a follows pointer to nothing -> blocks with STATE UNKNOWN
  [32m✓[0m a cycle in the follows chain -> blocks with STATE UNKNOWN

land.py's interlock — the policy itself, without performing a landing
  [32m✓[0m land.py imports the ledger rather than reimplementing it
  [32m✓[0m the deploy wait is shorter than the 10-minute harness ceiling that truncated two of them
  [32m✓[0m BASELINE: with nothing outstanding, an ordinary landing proceeds
  [32m✓[0m ...and --no-deploy-check is available when nothing is undischarged
  [32m✓[0m an ordinary landing is REFUSED while an incident is open
  [32m✓[0m ...and the refusal names the remediation route rather than just saying no
  [32m✓[0m --check-only is NOT blocked, so the state can still be inspected
  [32m✓[0m --no-deploy-check is REFUSED while blocked
  [32m✓[0m ...and refused even when remediating, because not looking is the original defect
  [32m✓[0m naming the OPEN incident permits the landing
  [32m✓[0m naming an incident that does not exist does NOT
  [32m✓[0m naming an ALREADY RESOLVED incident does not license a landing

the counterexamples external review reproduced against the first version
  [32m✓[0m a truthy but INVALID recovery does not close an incident
  [32m✓[0m setup: the first failure is resolved
  [32m✓[0m a SECOND failure of an already-resolved commit is not invisible
  [32m✓[0m ...it opens a second incident rather than reusing the resolved one
  [32m✓[0m ...and the recurrence is CHAINED to the resolved one, so repetition is not understated
  [32m✓[0m a served push that is NOT an ancestor does not discharge the earlier one
  [32m✓[0m a served DESCENDANT does discharge the earlier push — the frontier collapses
  [32m✓[0m ...and ancestry is what did it, not the order of the rows
  [32m✓[0m ...while an unrelated invented sha is UNKNOWN, which is not a discharge
  [32m✓[0m bootstrap materialises the historical failure
  [32m✓[0m ...and re-running it creates nothing
  [32m✓[0m ...leaving the incident count unchanged
  [32m✓[0m a malformed action log blocks with STATE UNKNOWN rather than raising
  [32m✓[0m the observer is pinned to the Pages workflow
  [32m✓[0m ...to a push event
  [32m✓[0m ...and to the target branch
  [32m✓[0m the interlock requires the lease BEFORE it reconciles
  [32m✓[0m a REFUSED push attestation owes no deployment
  [32m✓[0m ...and with it excluded, nothing blocks
  [32m✓[0m BASELINE: push then a served descendant DOES discharge it
  [32m✓[0m a deploy that happened BEFORE a re-push does not discharge it
  [32m✓[0m setup: two distinct failure events, two incidents
  [32m✓[0m two SEPARATE recoveries on the same day against the same head both survive
  [32m✓[0m ...and neither incident reopened
  [32m✓[0m a recovery whose id disagrees with its filename -> STATE UNKNOWN
  [32m✓[0m an expired lease REFUSES the landing
  [32m✓[0m ...and reconciliation never ran, so nothing was written under it
  [32m✓[0m BASELINE: with a live lease, reconciliation does run

72 passed, 0 failed

receipts are found in EVERY disposition, not only accepted samples
  [32m✓[0m a receipt inside `failures` is counted
  [32m✓[0m a receipt inside `rejected` is counted
  [32m✓[0m a receipt inside `responses` is counted
  [32m✓[0m the nested sample form is counted

an unrecognised schema REFUSES; it does not report zero
  [32m✓[0m a file with receipts but no known container is unrecognised
  [32m✓[0m ...and the file is named
  [32m✓[0m ...and no count is trusted

only a REGISTERED non-solicitation type is excused from holding units
  [32m✓[0m a registered non-solicitation type is excused
  [32m✓[0m ...and contributes nothing
  [32m✓[0m an UNDECLARED file holding no units refuses

a schema with no known receipt spelling still refuses
  [32m✓[0m an unknown schema carrying receipts refuses
  [32m✓[0m an unknown schema carrying nothing also refuses

units decide before the declared type
  [32m✓[0m an embedded spec type does not exclude a file that holds units

unparseable files refuse too
  [32m✓[0m a corrupt file makes the run unusable
  [32m✓[0m ...and is named as unparseable

denominators travel with the count
  [32m✓[0m files visited is reported
  [32m✓[0m solicited units seen is reported

instructed probes are never merged into the rounds figure
  [32m✓[0m a round receipt lands in `rounds`
  [32m✓[0m a probe receipt lands in `instructed`
  [32m✓[0m the two are not summed into one headline

the live corpus reproduces the corrected figures
  [32m✓[0m the real corpus is fully readable
  [32m✓[0m 9 search receipts corpus-wide
  [32m✓[0m exactly 1 of them in a numbered round
  [32m✓[0m the numbered-round one is round-016 qwen

the CLI refuses loudly and prints no number
  [32m✓[0m the CLI on the real corpus exits 0

25 passed, 0 failed
  PASS  a gate failure holds the capture
  PASS  a held capture blocks completion
  PASS  a held capture blocks closure too
  PASS  reject succeeds
  PASS  rejection CLOSES the round
  PASS  rejection does NOT complete the round
  PASS  the round names what is owed
  PASS  rejected bytes are kept
  PASS  rejection writes nothing to the corpus
  PASS  second fixture is held
  PASS  accept succeeds
  PASS  acceptance marks the party accepted
  PASS  acceptance completes the round
  PASS  acceptance PUBLISHES into the corpus
  PASS  published bytes equal preserved bytes
  PASS  accept refuses when the preserved bytes were altered
  PASS  and says why
  PASS  and does not publish
  PASS  a disposition with no reason is refused
  PASS  accept and reject together are refused
  PASS  a conflict re-blocks a completed round
  PASS  supersede is recorded
  PASS  SUPERSEDE ALONE DOES NOT COMPLETE THE ROUND
  PASS  confirm_recorded DOES clear immediately
  PASS  a batch with bad paths does not traceback
  PASS  a missing path is reported
  PASS  a directory is reported
  PASS  a non-UTF-8 file is reported
  PASS  bad input is INPUT ERROR, not a governance refusal
  PASS  invalid JSON is still a refusal
  PASS  the summary still prints
  PASS  the round-status table still prints
  PASS  the good bundle in the batch was still processed
  PASS  exit is non-zero when inputs failed
  PASS  the page no longer prints a glob ingest command
  PASS  the download filename carries a response hash
  PASS  the printed command is filled in from the name actually used
  PASS  the page calls the filename SUGGESTED, because a browser may suffix
  PASS  the bundle hash is recomputed at download, not read from async state

39 passed, 0 failed

push: the commit must reach the REMOTE ref, not just a local one
  [32m✓[0m a commit on no ref at all is refused
  [32m✓[0m a claim without a commit is refused
  [32m✓[0m a claim without a target ref is refused
  [32m✓[0m the profile checks origin/<ref>, not only the local ref

test: the status must come from the suite, not a later command
  [32m✓[0m status sourced from basename is refused — the 2026-08-08 shape
  [32m✓[0m a non-zero exit is not a pass
  [32m✓[0m a claim with no exit_status is refused
  [32m✓[0m a claim with no suite identity is refused
  [32m✓[0m a directly captured zero is accepted

merge: governed files must be free of conflict markers
  [32m✓[0m the governed set covers the manifest and the anchor log
  [32m✓[0m ...and the deficiency register, which is also load-bearing
  [32m✓[0m on a clean tree the merge profile reports nothing

an unknown action is refused, never waved through
  [32m✓[0m an action with no postcondition profile is refused

refusals are LOGGED, not only successes
  [32m✓[0m a refused attestation still appends an entry
  [32m✓[0m ...marked verified=False
  [32m✓[0m ...carrying the problems that caused it
  [32m✓[0m every entry disclaims authority
  [32m✓[0m the log is hash-chained

quota is stamped, and fails OPEN
  [32m✓[0m quota is queryable or explicitly unavailable
  [32m✓[0m it reports a rolling window
  [32m✓[0m ...with a reset time
  [32m✓[0m an unreadable quota fails OPEN rather than refusing
  [32m✓[0m ...and says why it is not evidence of a full quota
  [32m✓[0m a WARM cache is served without touching credentials
  [32m✓[0m ...and carries the reading's age
  [32m✓[0m under a 429 cooldown it does not call again
  [32m✓[0m ...and attaches the LAST KNOWN reading with its age

the trial declares what it is not
  [32m✓[0m the charter states it has no authority
  [32m✓[0m it records that review rejected the 'below the parties' framing
  [32m✓[0m it names legitimacy laundering as the danger it cannot see
  [32m✓[0m it carries a sunset
  [32m✓[0m it states the deletion duty, with the live test case

the Codex budget gate
  [32m✓[0m well above the floor is allowed
  [32m✓[0m exactly at the 33% floor is allowed
  [32m✓[0m just below the floor is refused
  [32m✓[0m nearly exhausted is refused
  [32m✓[0m an UNREADABLE budget fails open
  [32m✓[0m ...and the reason says the budget was unverified
  [32m✓[0m ...and says why disabling review to save budget is the wrong trade
  [32m✓[0m the floor is the custodian's stated 33%
  [32m✓[0m the module refuses to reimplement the CDP read

the Codex rate limit
  [32m✓[0m the floor is the custodian's stated 10 minutes
  [32m✓[0m the floor is read from the spend policy, not hardcoded
  [32m✓[0m ...and the policy states whether it was derived from a measurement
  [32m✓[0m ...and it is currently NOT measured, so it does not pose as one
  [32m✓[0m ...and the provenance says who supplied the number
  [32m✓[0m the policy separates the three money channels
  [32m✓[0m ...and records the Claude subscription as READABLE
  [32m✓[0m ...with NO floor invented for it
  [32m✓[0m ...and says why inventing one would be wrong
  [32m✓[0m the Codex floor records that the custodian holds it, not a tool
  [32m✓[0m ...and names the holder explicitly rather than leaving it to prose
  [32m✓[0m ...and records that it is standing rather than windowed
  [32m✓[0m ...and carries BOTH instructions, so the windowed one is not overwritten by the standing one
  [32m✓[0m ...and states that this record holds NO artifact of the monitoring
  [32m✓[0m ...and that nothing here bounds the spend, only its unrecordedness
  [32m✓[0m ...and names what removing the floor costs, rather than only what it gained
  [32m✓[0m ...and records the override rate that justified removing it
  [32m✓[0m no expiry is claimed, because a standing arrangement with a date would be a third thing
  [32m✓[0m the policy admits the log undercounts the calls actually made
  [32m✓[0m a changed policy value changes the enforced floor
  [32m✓[0m with no previous call, one is allowed
  [32m✓[0m a call 30 seconds ago is ALLOWED while the custodian holds rate limiting
  [32m✓[0m ...and the explanation names who holds it
  [32m✓[0m ...and says the receipts are not themselves a limit, so a reader cannot mistake them for one
  [32m✓[0m with the window cleared, a call 30 seconds ago is refused again
  [32m✓[0m ...and 9.5 minutes ago is still refused
  [32m✓[0m ...and 10.5 minutes ago is allowed
  [32m✓[0m an override is honoured
  [32m✓[0m ...and the reason is carried in the explanation
  [32m✓[0m ...and it is labelled as an override rather than a pass
  [32m✓[0m ...and the running override RATE is shown at the point of override
  [32m✓[0m a refusal says how long remains
  [32m✓[0m ...and offers batching before the override
  [32m✓[0m the clock comes from the action log, not a side file
  [32m✓[0m a REFUSED attempt does not reset the clock
  [32m✓[0m the call is logged BEFORE it runs, so a crash is not unrecorded
  [32m✓[0m the module states it cannot prevent bypass
  [32m✓[0m --purpose is mandatory, so a review is auditable
  [32m✓[0m lease live before its deadline
  [32m✓[0m lease dead after its deadline
  [32m✓[0m expiry names the lease and the time
  [32m✓[0m require raises past expiry
  [32m✓[0m an unknown action class is refused, not permitted
  [32m✓[0m max_actions blocks once the log passes the cap
  [32m✓[0m no lease means unauthorised, not permitted
  [32m✓[0m both leases survive; the superseded one is not removed
  [32m✓[0m the new lease names what it supersedes
  [32m✓[0m current() is the newest
  [32m✓[0m the lease exposes no waiver callable (found [])
  [32m✓[0m require() takes only the action class

91 passed, 0 failed
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.

the shipped fixtures behave as the spec requires
  [32m✓[0m every must-reject fixture is rejected and every must-accept accepted
  [32m✓[0m there is at least one must-reject fixture per normative requirement

the requirement that does the work: surviving your own control is a violation
  [32m✓[0m a check that PASSES under its negative control is non-conforming

an absent control is not a pass
  [32m✓[0m no negative_control at all is N1

transport perturbation does not count
  [32m✓[0m a transport-only control is N5

the verifier does not pass vacuously
  [32m✓[0m an empty attestation is refused
  [32m✓[0m an attestation with zero checks is refused

the live attestation's verdict matches its own recorded outcomes
  [32m✓[0m the verdict agrees with the recorded outcomes
  [32m✓[0m every N3 violation names a check that actually survived its control
  [32m✓[0m no check is recorded as conforming while surviving its control
  [32m✓[0m every negative control run says whether it was executed or inferred

an unreadable file is refused, not skipped
  [32m✓[0m unparseable input exits non-zero

the fixture set includes NEAR-MISSES, not only obvious rejections
  [32m✓[0m at least four near-miss fixtures exist
  [32m✓[0m reject-nearmiss-claim-smuggling.json: rejected
  [32m✓[0m reject-nearmiss-control-predates-the-artifact.: rejected
  [32m✓[0m reject-nearmiss-failed-for-the-wrong-reason.js: rejected
  [32m✓[0m reject-nearmiss-referent-missing.json: rejected
  [32m✓[0m a near-miss is not rejected for a trivial reason

the verifier publishes gaps it cannot close
  [32m✓[0m known-gap fixtures are shipped
  [32m✓[0m gap-control-is-trivially-easy.json: still accepted (that is the gap)
  [32m✓[0m gap-control-is-trivially-easy.json: says why it should not be
  [32m✓[0m gap-control-targets-a-different-capability: still accepted (that is the gap)
  [32m✓[0m gap-control-targets-a-different-capability: says why it should not be
  [32m✓[0m known gaps do NOT fail the fixture suite

the spec records the gaps rather than only the strengths
  [32m✓[0m the spec names the known-gaps directory
  [32m✓[0m the spec says the near-misses were added because the first nine were easy

26 passed, 0 failed

[32m55 passed[0m
  ✓ refuses loopback v4 (127.0.0.1) — blocked with: 'probe' resolves to 127.0.0.1, which is loopback/private/link-local/reserved/met
  ✓ refuses loopback v6 (::1) — blocked with: 'probe' resolves to ::1, which is loopback/private/link-local/reserved/metadata.
  ✓ refuses private 10/8 (10.0.0.1) — blocked with: 'probe' resolves to 10.0.0.1, which is loopback/private/link-local/reserved/meta
  ✓ refuses private 192.168 (192.168.12.192) — blocked with: 'probe' resolves to 192.168.12.192, which is loopback/private/link-local/reserve
  ✓ refuses private 172.16 (172.17.0.1) — blocked with: 'probe' resolves to 172.17.0.1, which is loopback/private/link-local/reserved/me
  ✓ refuses link-local (169.254.1.1) — blocked with: 'probe' resolves to 169.254.1.1, which is loopback/private/link-local/reserved/m
  ✓ refuses cloud metadata (169.254.169.254) — blocked with: 'probe' resolves to 169.254.169.254, which is loopback/private/link-local/reserv
  ✓ refuses multicast (224.0.0.1) — blocked with: 'probe' resolves to 224.0.0.1, which is loopback/private/link-local/reserved/met
  ✓ refuses unspecified (0.0.0.0) — blocked with: 'probe' resolves to 0.0.0.0, which is loopback/private/link-local/reserved/metad
  ✓ permits an ordinary public address
  ✓ refuses the inference server's own address — blocked with: 'localhost' resolves to 127.0.0.1, which is loopback/private/link-local/reserved
  ✓ refuses file: — blocked with: scheme 'file' is not permitted; only http and https are
  ✓ refuses ftp: — blocked with: scheme 'ftp' is not permitted; only http and https are
  ✓ refuses embedded credentials — blocked with: url embeds credentials; this executor sends none
  ✓ refuses a url with no host — blocked with: url has no host
  ✓ preserves the query string — /a/b?c=d
  ✓ infers the default https port
  ✓ refuses a host answering with BOTH a public and a loopback address — blocked with: 'mixed.example' resolves to 127.0.0.1, which is loopback/private/link-local/rese
  ✓ returns a vetted address to connect to
  ✓ reports every answer it vetted
  ✓ connect_pinned vets and pins in one step (no second resolution)
  ✓ TLS still validates against the hostname, not the pinned address
  ✓ reads a small body whole
  ✓ caps an oversized body
  ✓ reports that more existed than was returned — seen=2001
  ✓ truncates multibyte content by bytes, not characters
  ✓ a byte-truncated multibyte body still decodes without raising
  ✓ honours a declared non-UTF-8 charset
  ✓ falls back to utf-8 on an unknown charset
  ✓ assumes utf-8 when no charset is declared
  ✓ a missing url argument is refused, not crashed
  ✓ and the refusal is still recorded
  ✓ a party cannot fetch the inference server
  ✓ the blocked fetch leaves a receipt — a refusal with no trace is indistinguishable from a fetch never attempted
  ✓ and the model is told not to infer the contents
  ✓ a cited source with a receipt is supported
  ✓ a cited source with NO receipt is flagged
  ✓ and the flag is named for what it is
  ✓ trailing-slash and case differences do not manufacture a false accusation
  ✓ a REFUSED receipt does not count as having read the page
  ✓ citing nothing is not an accusation
  ✓ the profile hash is stable across calls
  ✓ the tool the model sees says it is not a search engine
  ✓ the tool tells the party not to claim what it did not fetch
  ✓ the tool schema forbids extra arguments
  ✓ the spec schema admits a capability block
  ✓ and a base_party_key, so a derived party's origin is visible
  ✓ party_key accepts a capability-bearing name — D-09: same weights + different capability = different party
  ✓ party_key still rejects an empty or upper-case handle
  ✓ capability forbids undeclared fields
  ✓ undersampling is judged per party, not against the first spec — a party solicited at a higher k must not be judged against someone else's floor
  ✓ ...and against the USABLE floor, not against attempts scheduled — k_solicited=6 with k_min_usable=5 must not halt at 5 usable
  ✓ arms are classified by capability, not by search alone — a fetch-enabled party and a tool-less one must not share an arm
  ✓ the budget prices agentic turns, not one completion — each turn re-sends the conversation with another page appended
  ✓ the fetch allowance is the SMALLER of the two caps that apply — the tool message cap binds before the executor's body cap

check_executive_context.py — four states, and the exit code must distinguish them
  [32m✓[0m BASELINE: copies and live sources all match -> exit 0
  [32m✓[0m a COPY that does not match its own pin -> exit 1, even with live unavailable
  [32m✓[0m a MISSING copy -> exit 1, even with live unavailable
  [32m✓[0m all external live sources unavailable, copies valid -> exit 3, NOT 0 and NOT 1
  [32m✓[0m ...and it says so rather than printing a bare pass
  [32m✓[0m one live source present and one absent -> exit 3 with a partial-coverage diagnostic
  [32m✓[0m DRIFT alongside an unavailable source -> exit 1; a contradiction outranks an unknown
  [32m✓[0m a REPO-RELATIVE live source that drifts -> exit 1 in any environment
  [32m✓[0m an EMPTY pins document -> exit 2
  [32m✓[0m a pin with no live_source -> exit 2
  [32m✓[0m a pin whose kind is unknown -> exit 2
  [32m✓[0m a pin name that escapes the context directory -> exit 2
  [32m✓[0m a malformed sha256 -> exit 2
  [32m✓[0m a repo_relative pin given an absolute path -> exit 2
  [32m✓[0m a pins document that is not JSON -> exit 2, not a traceback exiting 1
  [32m✓[0m the real record evaluates to a state this suite recognises (0 here, 3 in a clone)
  [32m✓[0m land.py admits exit 0 from the context-pin gate
  [32m✓[0m land.py REFUSES exit 1 from the context-pin gate
  [32m✓[0m land.py REFUSES exit 2 from the context-pin gate
  [32m✓[0m land.py REFUSES exit 3 from the context-pin gate
  [32m✓[0m ...and the same rule applies to every other gate

check_quotations.py — it must refuse a quotation that is in no corpus file
  [32m✓[0m BASELINE: every published quotation is in the corpus
  [32m✓[0m a FABRICATED quotation is refused (inline)
  [32m✓[0m a FABRICATED quotation is refused (blockquote)
  [32m✓[0m ...and the tree is clean again after removing it

record_spend.py / reconcile_actions.py — they must refuse rather than emit a number
  [32m✓[0m record_spend refuses an unknown cohort rather than printing a cost
  [32m✓[0m BASELINE: the spend report still runs
  [32m✓[0m reconcile_actions refuses an unresolvable revision rather than reporting zero
  [32m✓[0m BASELINE: reconciliation runs against the current lease

29 passed, 0 failed
gate parity over 25 cases (python vs node)
25 agreed, 0 disagreements

check_prompt.py — the D-31 denylist, checked BEFORE a prompt is sent
  [32m✓[0m BASELINE: a neutral prompt passes
  [32m✓[0m a prompt containing a denylisted phrasing is REFUSED (prescribes posture)
  [32m✓[0m ...and the refusal cites the deficiency the rule came from (prescribes posture)
  [32m✓[0m a prompt containing a denylisted phrasing is REFUSED (names the direction of the expected error)
  [32m✓[0m ...and the refusal cites the deficiency the rule came from (names the direction of the expected error)
  [32m✓[0m a prompt containing a denylisted phrasing is REFUSED (tells a party dissent is valued, which shapes the answer)
  [32m✓[0m ...and the refusal cites the deficiency the rule came from (tells a party dissent is valued, which shapes the answer)
  [32m✓[0m ...and the neutral prompt still passes, so the refusal was about the wording

check_register.py — R9 binds metadata to the prose it describes
  [32m✓[0m BASELINE: the register and its artifact agree
  [32m✓[0m EDITING AN ENTRY'S PROSE fails until it is re-stamped
  [32m✓[0m ...and the failure names R9, not some other rule
  [32m✓[0m a HEADING WITH NO ENTRY is refused
  [32m✓[0m ...and the failure names R7
  [32m✓[0m a DUPLICATE entry id is refused — the defect D-32 records
  [32m✓[0m ...and the failure names R3
  [32m✓[0m an artifact whose recorded SOURCE HASH is wrong is refused
  [32m✓[0m ...and the failure names R10
  [32m✓[0m ...and an untouched copy passes again
  [32m✓[0m the REAL register and artifact are byte-identical to before these fixtures ran

build_register_view.py — it publishes the numbers a reader sees
  [32m✓[0m BASELINE: the register artifact renders to markdown
  [32m✓[0m build_register_view.py: a MISSING artifact is refused, not rendered as empty
  [32m✓[0m build_register_view.py: an entry with NO review key makes aggregation RAISE
  [32m✓[0m ...and it raises on exactly the missing field, not somewhere else

the coverage scanner must not count a DISCLAIMER as coverage
  [32m✓[0m a test that only TALKS about a tool does not count as its coverage
  [32m✓[0m a COVERS declaration with no refusal assertion does not count either
  [32m✓[0m ...and the reason says the declaration was seen and found empty
  [32m✓[0m BASELINE: a declaration WITH a refusal assertion does count

27 passed, 0 failed

the two numbers are separate
  [32m✓[0m the local arm schedules six attempts
  [32m✓[0m routed arms schedule five
  [32m✓[0m the usable floor is five for everyone
  [32m✓[0m scheduled and floor are different numbers for the local arm

the undersample gate tests USABLE, not scheduled
  [32m✓[0m the gate compares against a floor, not against k_requested

the published page agrees with the gate
  [32m✓[0m the publisher also uses k_min_usable
  [32m✓[0m 5 usable of 6 scheduled is NOT undersampled
  [32m✓[0m 4 usable of 6 scheduled IS undersampled
  [32m✓[0m 6 usable of 6 is not undersampled
  [32m✓[0m a spec with only the old field still works

the serve fingerprint fails closed
  [32m✓[0m the pinned endpoint is the one the round solicits
  [32m✓[0m the pin names a model id
  [32m✓[0m the pin disclaims what it cannot establish
  [32m✓[0m a mismatched model id is refused
  [32m✓[0m a pin for a different endpoint is refused

the decisions that authorise all this are recorded
  [32m✓[0m singleton ratification is a recorded decision
  [32m✓[0m k=6 is a recorded decision
  [32m✓[0m the k=6 decision states the projection's uncertainty, not just the plug-in figure
  [32m✓[0m the k=6 decision states what it does NOT fix
  [32m✓[0m the singleton decision records agenda luck as accepted
  [32m✓[0m the singleton decision says no instrument implements it yet

21 passed, 0 failed

self_application.py — a determination for every control, and no blank reasons
  [32m✓[0m BASELINE: the table is complete as it stands
  [32m✓[0m a control with NO determination is refused
  [32m✓[0m ...and the refusal names the control that is missing
  [32m✓[0m a determination for a control NOT in the register is refused
  [32m✓[0m a state outside the controlled vocabulary is refused
  [32m✓[0m NOT_APPLICABLE with an empty reason is refused — an omission wearing a label
  [32m✓[0m NOT_APPLICABLE with 'n/a' is refused — an omission wearing a label
  [32m✓[0m NOT_APPLICABLE with 'not applicable' is refused — an omission wearing a label
  [32m✓[0m NOT_APPLICABLE with a reason too short to state a structure is refused — an omission wearing a label
  [32m✓[0m ...and the table is clean again once restored

control_application.py — a row for every control, and no cell that means nothing
  [32m✓[0m BASELINE: every row substantiates as it stands
  [32m✓[0m a control with NO row is refused
  [32m✓[0m ...and the refusal distinguishes a missing row from a '—'
  [32m✓[0m a row for a control NOT in the register is refused
  [32m✓[0m a '—' row with NO structural reason is refused
  [32m✓[0m ...and it is THAT guard, not another one firing for the same row
  [32m✓[0m a '—' row whose reason is 'n/a' is refused
  [32m✓[0m ...and the refusal says it restates the mark rather than giving a structure
  [32m✓[0m a '—' row whose reason is 'not applicable' is refused
  [32m✓[0m ...and the refusal says it restates the mark rather than giving a structure
  [32m✓[0m a '—' row whose reason is 'none' is refused
  [32m✓[0m ...and the refusal says it restates the mark rather than giving a structure
  [32m✓[0m a '—' row whose reason is a reason too short to state a structure is refused
  [32m✓[0m ...and the refusal says it restates the mark rather than giving a structure
  [32m✓[0m a '—' row that nonetheless names code is refused
  [32m✓[0m a code row naming NO file is refused
  [32m✓[0m ...and the refusal names the missing files, not something else about the row
  [32m✓[0m a row naming a file that is NOT ON DISK is refused
  [32m✓[0m ...so a deleted file cannot leave a row quietly ticked
  [32m✓[0m a row DECLARED complete with no test is refused
  [32m✓[0m ...and the refusal is about the declaration, not the substantiation
  [32m✓[0m a row contradicting self_application's NOT_APPLICABLE is refused
  [32m✓[0m ...and every row substantiates again once restored

the page's own history must survive its own progress
  [32m✓[0m the history is on the page at the current count
  [32m✓[0m ...and it survives when rows ARE ticked
  [32m✓[0m ...and when none are
  [32m✓[0m ...with the zero case saying so in its first sentence

37 passed, 0 failed
in-page SHA-256 fallback vs hashlib over 21 cases
21 matched, 0 mismatched

check_page_budget.py — it must fail the build on an over-budget page
  [32m✓[0m BASELINE: every published page is within budget
  [32m✓[0m an OVER-BUDGET page fails the build
  [32m✓[0m ...and the tree is within budget again

capture_gates.py — it must refuse a response that is the prompt read back
  [32m✓[0m BASELINE: a substantive answer returns clean
  [32m✓[0m a response that ECHOES THE PROMPT does not return clean
  [32m✓[0m ...and the gate that fired names prompt equality
  [32m✓[0m an EMPTY response is refused outright
  [32m✓[0m ...and the gate that fired is the non-empty one

8 passed, 0 failed

the expected answers pass
  [32m✓[0m 30 expected answers qualify the instrument
  [32m✓[0m ...and every pair is scored correct

an all-neutral answer FAILS — it used to pass
  [32m✓[0m six EQUALs do not qualify
  [32m✓[0m EQUAL is recorded as its own outcome, not as wrong
  [32m✓[0m six UNDETERMINEDs do not qualify

a wrong polarity call halts
  [32m✓[0m one wrong pair fails the whole run
  [32m✓[0m ...and the failure names the pair and both values

a fabricated or empty quotation halts — it used to pass beside a correct call
  [32m✓[0m a quote absent from the named text fails
  [32m✓[0m ...and is counted as bad_quote, not as a wrong polarity call
  [32m✓[0m an empty quote fails
  [32m✓[0m a quote taken from the OTHER member fails

missing and unusable samples halt
  [32m✓[0m zero samples never qualifies
  [32m✓[0m one unusable sample among five fails the run
  [32m✓[0m an absent pair fails
  [32m✓[0m ...and is counted as missing

the two arms' raw shapes score identically
  [32m✓[0m a local `content` record scores the same as a routed `parsed` one
  [32m✓[0m unparseable local content is unusable, not silently empty

the schema is flat scalars, so the summary writer cannot crash on it
  [32m✓[0m every variance field is a scalar enum
  [32m✓[0m variance fields are hashable values, not lists of objects
  [32m✓[0m `phase` is present; both summary writers require it
  [32m✓[0m `more_constraining` was removed rather than kept unscored
  [32m✓[0m only ONE scored field per pair; authority is not retained alongside obligation
  [32m✓[0m the spec carries k_by_party so scoring reads it from the commit
  [32m✓[0m the spec carries displayed_texts for quote checking

the prompt neither leaks provenance nor shows an example answer
  [32m✓[0m no example JSON values
  [32m✓[0m no claim that one member is the real rule
  [32m✓[0m no site URL to look the real language up
  [32m✓[0m it says provenance is irrelevant
  [32m✓[0m it does not claim responses are unscored

the registered direction is one question, and the same for all six
  [32m✓[0m every pair registers the ACTUAL text as the stricter constraint
  [32m✓[0m all six clauses are registered
  [32m✓[0m the prompt asks about strictness of constraint, not latitude to act
  [32m✓[0m the prompt names disabilities so C02 is orderable
  [32m✓[0m the prompt defines the comparison as a strict subset of permitted conduct

k is registered per party, exactly, not as a floor
  [32m✓[0m the local arm's 6 is registered, not inherited from round_cycle at score time
  [32m✓[0m every party has a registered k

the repaired pairs are strict polarity reversals
  [32m✓[0m C05's twin negates the actual duty rather than replacing it
  [32m✓[0m C01's twin drops the delegation requirement and nothing else
  [32m✓[0m C06's twin keeps manifest anchoring, isolating the edit permission

ordering is fixed, salted, and balanced
  [32m✓[0m the arrangement is stable across calls
  [32m✓[0m the actual text is not always shown in the same position
  [32m✓[0m the salt is recorded in the spec
  [32m✓[0m the spec says it is a fixed schedule, not randomisation

the normalised clauses are bound to the later ballot
  [32m✓[0m every non-verbatim clause is declared
  [32m✓[0m each carries the exact normalised text that must be balloted later

no retry: identity is the prompt hash, not the cohort name
  [32m✓[0m an unsent instrument has no prior sightings

46 passed, 0 failed

the three states stay distinct
  [32m✓[0m an authorized party maps to its proposal
  [32m✓[0m a balloted-but-indeterminate party is PRESENT in the mapping
  [32m✓[0m ...and holds nothing
  [32m✓[0m a party never balloted is ABSENT, not None

indeterminate does not revoke; explicit_none does
  [32m✓[0m a later indeterminate leaves a standing authorization alone
  [32m✓[0m a later unanimous NO_ACTIVE_PROPOSAL clears it
  [32m✓[0m a later authorization replaces an earlier one
  [32m✓[0m indeterminate then authorized ends authorized

the cap is opt-in and never silent
  [32m✓[0m enforce_cap defaults to OFF
  [32m✓[0m the default call is unaffected by the cap

on the live record
  [32m✓[0m every surviving proposal is its party's live authorized one
  [32m✓[0m no party holding nothing keeps a proposal through the cap
  [32m✓[0m the capped set is exactly the live authorizations that are still unasked
  [32m✓[0m nothing survives the cap that is not the party's live authorization
  [32m✓[0m an ASKED proposal is not removed by the cap — history is not rewritten

an authorization the queue cannot honour fails closed
  [32m✓[0m an authorized id absent from the queue raises rather than being ignored
  [32m✓[0m an authorized id the queue DOES contain does not raise

17 passed, 0 failed

[32m13 passed[0m
  ✓ the corpus is non-empty, so a miss means something — 38165902 chars
  ✓ D-53's fabricated sentence is still absent from the corpus
  ✓ a party name plus a speech verb is an attribution
  ✓ a party name with NO speech verb is not an attribution
  ✓ a JSON key containing a verb is not a speech verb — claim_as_stated must not match 'stated'
  ✓ the nearest preceding party is the one credited
  ✓ a planted fabrication FAILS the checker — exit 1
  ✓ the failure names the party it was attributed to
  ✓ the failure quotes the offending text
  ✓ the control sentence really is in the corpus
  ✓ a genuine quotation PASSES the checker — exit 0
  ✓ the planted quotation was examined, not skipped — baseline 0, with the planted quotation 1
  ✓ a marked correction quoting false text is exempt — exit 0

synthetic entries are TYPED, not left to a free-text note
  [32m✓[0m a note saying 'synthetic' types the entry as synthetic_test
  [32m✓[0m ...and synthetic_test is a distinct state, not a flavour of verified
  [32m✓[0m a real profiled pass is verified_postcondition
  [32m✓[0m an action with NO profile is observed_unprofiled, not verified
  [32m✓[0m every state carries a stated meaning

coverage is written on every new entry
  [32m✓[0m log_action stamps coverage
  [32m✓[0m an explicit coverage argument overrides the inference

reconciliation classifies effects the log has no word for
  [32m✓[0m a gate source file classifies as gate_modification
  [32m✓[0m raw material classifies as a paid solicitation
  [32m✓[0m a finding classifies as a finding
  [32m✓[0m a committed spec classifies as a prompt_or_spec_edit
  [32m✓[0m an unrelated path classifies as nothing

the live repository is reconciled, and the omissions are real
  [32m✓[0m it reports commits
  [32m✓[0m it names effect classes that occurred
  [32m✓[0m it reports omissions rather than a clean balance
  [32m✓[0m gate modifications are counted separately
  [32m✓[0m every omission says WHY it is unverified
  [32m✓[0m it states what it cannot establish

it does not claim to verify what it merely observed
  [32m✓[0m no omission claims a verifier

19 passed, 0 failed

[32m94 passed[0m
  ✓ all content parts survive the join — 'PART ONE\n\nPART TWO\n\nPART THREE'
  ✓ the multi-part join is recorded as model-visible
  ✓ a typed user message stays a user message
  ✓ developer maps to system
  ✓ the developer role map is recorded as model-visible
  ✓ function_call becomes an assistant tool_call
  ✓ the tool call keeps its name
  ✓ the tool call keeps its arguments
  ✓ the tool call keeps its id
  ✓ the result correlates to the call by id
  ✓ the call precedes its result
  ✓ consecutive calls merge into one assistant turn
  ✓ an unknown top-level field refuses — refused with R-FIELD-UNKNOWN-001, expected R-FIELD-UNKNOWN*
  ✓ previous_response_id refuses — refused with R-FIELD-REFUSED-001, expected R-FIELD-REFUSED*
  ✓ background refuses — refused with R-FIELD-REFUSED-001, expected R-FIELD-REFUSED*
  ✓ an unknown role refuses — refused with R-ROLE-001, expected R-ROLE*
  ✓ an unknown item type refuses — refused with R-ITEM-TYPE-001, expected R-ITEM-TYPE*
  ✓ a reasoning item refuses rather than being dropped — refused with R-REASONING-001, expected R-REASONING*
  ✓ a non-text content part refuses — refused with R-CONTENT-PART-002, expected R-CONTENT-PART*
  ✓ a function_call missing its call_id refuses — refused with R-FUNCALL-001, expected R-FUNCALL*
  ✓ an EMPTY namespace refuses — it would advertise a capability that is not there — refused with R-TOOL-NAMESPACE-003, expected R-TOOL-NAMESPACE-003*
  ✓ a namespace member that is not a flat function refuses — refused with R-TOOL-NAMESPACE-004, expected R-TOOL-NAMESPACE-004*
  ✓ the hosted web_search tool refuses — refused with R-TOOL-WEBSEARCH-001, expected R-TOOL-WEBSEARCH*
  ✓ an unknown tool type refuses — refused with R-TOOL-TYPE-001, expected R-TOOL-TYPE*
  ✓ leading system turns merge into one
  ✓ the merge preserves both texts in order
  ✓ the system merge is recorded as model-visible
  ✓ a single system turn is left alone
  ✓ a late system message refuses rather than being hoisted — refused with R-SYSTEM-POSITION-001, expected R-SYSTEM-POSITION*
  ✓ an unknown output format refuses — refused with R-TEXT-FORMAT-002, expected R-TEXT-FORMAT*
  ✓ web_search refuses even though upstream's schema accepts it — verified 2026-08-07: POST /v1/responses with a web_search tool returns 200
  ✓ a namespace member is flattened to its own name — fetch_url
  ✓ the member's schema survives flattening unchanged
  ✓ the member's description survives flattening
  ✓ the flattening is recorded as model-visible, not as free
  ✓ the record names both endpoints so the mapping can be inverted
  ✓ a name collision refuses rather than letting one tool shadow another — refused with R-TOOL-COLLIDE-001, expected R-TOOL-COLLIDE*
  ✓ the extra_forbidden fields are dropped, not forwarded
  ✓ each drop is recorded — 4 recorded
  ✓ cache and telemetry drops are classified as invisible to the model
  ✓ reasoning and truncation drops are classified as declared treatment, not as free
  ✓ every dropped field carries a stated reason
  ✓ the injected temperature reaches upstream
  ✓ the injected seed reaches upstream
  ✓ both injections are declared treatment, not silent defaults
  ✓ the injected values appear in the ledger detail, not just the fact of injection
  ✓ instructions become the leading system message
  ✓ a parsed tool call returns as a function_call item
  ✓ the call id survives the return trip
  ✓ the tool name survives the return trip
  ✓ assistant text returns as a message item
  ✓ the message text is intact
  ✓ the event stream opens with response.created
  ✓ the event stream closes with response.completed
  ✓ every output item is announced and completed
  ✓ a function call's arguments are streamed
  ✓ text output is streamed as a delta and a done
  ✓ sequence numbers are contiguous from zero
  ✓ the completed event carries the full output
  ✓ the created event carries no output yet
  ✓ a truncated reply is reported as incomplete, not completed
  ✓ the answer is separated from the chain of thought — 'Paris'
  ✓ the reasoning is captured, not discarded — 'Step 1. Consider Paris.'
  ✓ the split is recorded as model-visible
  ✓ the recorded detail carries both byte counts — chain of thought separated from the answer on '</think>': 23 bytes of reasoning removed, 5 bytes of answer kept. The full unsplit reply is p
  ✓ an opening think tag is stripped from the reasoning
  ✓ a thinking-off reply passes through untouched
  ✓ a server-supplied split is used instead of splitting again
  ✓ the server-supplied split is recorded as such
  ✓ an unclosed reasoning block is flagged, not passed off as an answer
  ✓ the flag says plainly that the text is not an answer
  ✓ an unclosed reasoning block yields no reasoning field to quote
  ✓ the client receives only the answer
  ✓ the reasoning is handed to the caller for the ledger
  ✓ the response reports the temperature actually used, not the client's silence
  ✓ the response reports the seed actually used
  ✓ every entry is on disk immediately
  ✓ the chain starts from a known root
  ✓ each entry links to the previous one
  ✓ sequence numbers are strictly increasing
  ✓ bodies are stored by content address and are recoverable
  ✓ the blob digest is stable under key order
  ✓ entry kinds distinguish the two sides of the boundary
  ✓ the stock Codex tool profile still refuses, on the unbacked web_search tool — refused with R-TOOL-WEBSEARCH-001, expected R-TOOL-WEBSEARCH*
  ✓ the narrowed Codex request translates
  ✓ the two leading system turns merge into the one upstream permits — ['system', 'user', 'user']
  ✓ the developer item's SECOND content part survives — the upstream defect this shim exists for
  ✓ the top-level instructions survive the system merge
  ✓ the merge is recorded as model-visible
  ✓ the client's item ids are dropped and each drop is recorded
  ✓ the surviving function tool reaches upstream in Chat Completions shape
  ✓ no refused field leaked into the upstream request
  ✓ every transformation carries a rule id and a classification
  ✓ the model-visible transformations are a strict subset of all of them — 9 of 17

nothing executable sits after the summary line
  [32m✓[0m test_agenda_activation.py: no test defined or run after the summary
  [32m✓[0m test_agenda_ratification.py: no test defined or run after the summary
  [32m✓[0m test_amendment_disposition.py: no test defined or run after the summary
  [32m✓[0m test_attempt_ledger.py: no test defined or run after the summary
  [32m✓[0m test_capability_fits.py: no test defined or run after the summary
  [32m✓[0m test_capture_gates.py: no test defined or run after the summary
  [32m✓[0m test_capture_lifecycle.py: no test defined or run after the summary
  [32m✓[0m test_chain_guards.py: no test defined or run after the summary
  [32m✓[0m test_closed_world.py: no test defined or run after the summary
  [32m✓[0m test_cohort_pages.py: no test defined or run after the summary
  [32m✓[0m test_deploy_check.py: no test defined or run after the summary
  [32m✓[0m test_deploy_obligations.py: no test defined or run after the summary
  [32m✓[0m test_derive_counts.py: no test defined or run after the summary
  [32m✓[0m test_disposition.py: no test defined or run after the summary
  [32m✓[0m test_executive_log.py: no test defined or run after the summary
  [32m✓[0m test_fault_injection.py: no test defined or run after the summary
  [32m✓[0m test_fetch_executor.py: no test defined or run after the summary
  [32m✓[0m test_gate_negative_controls.py: no test defined or run after the summary
  [32m✓[0m test_gate_parity.py: no test defined or run after the summary
  [32m✓[0m test_gate_refusals.py: no test defined or run after the summary
  [32m✓[0m test_k_solicited.py: no test defined or run after the summary
  [32m✓[0m test_no_blank_cells.py: no test defined or run after the summary
  [32m✓[0m test_page_hash_fallback.py: no test defined or run after the summary
  [32m✓[0m test_publication_gates.py: no test defined or run after the summary
  [32m✓[0m test_qualification_round.py: no test defined or run after the summary
  [32m✓[0m test_queue_cap.py: no test defined or run after the summary
  [32m✓[0m test_quotation_checker.py: no test defined or run after the summary
  [32m✓[0m test_reconcile.py: no test defined or run after the summary
  [32m✓[0m test_responses_shim.py: no test defined or run after the summary

every suite ends by exiting on its own failure count
  [32m✓[0m test_agenda_activation.py: exits non-zero when FAILED
  [32m✓[0m test_agenda_ratification.py: exits non-zero when FAILED
  [32m✓[0m test_amendment_disposition.py: exits non-zero when FAILED
  [32m✓[0m test_attempt_ledger.py: exits non-zero when FAILED
  [32m✓[0m test_capability_fits.py: exits non-zero when FAILED
  [32m✓[0m test_capture_gates.py: exits non-zero when FAILED
  [32m✓[0m test_capture_lifecycle.py: exits non-zero when FAILED
  [32m✓[0m test_chain_guards.py: exits non-zero when FAILED
  [32m✓[0m test_closed_world.py: exits non-zero when FAILED
  [32m✓[0m test_cohort_pages.py: exits non-zero when FAILED
  [32m✓[0m test_deploy_check.py: exits non-zero when FAILED
  [32m✓[0m test_deploy_obligations.py: exits non-zero when FAILED
  [32m✓[0m test_derive_counts.py: exits non-zero when FAILED
  [32m✓[0m test_disposition.py: exits non-zero when FAILED
  [32m✓[0m test_executive_log.py: exits non-zero when FAILED
  [32m✓[0m test_fault_injection.py: exits non-zero when FAILED
  [32m✓[0m test_fetch_executor.py: exits non-zero when FAILED
  [32m✓[0m test_gate_negative_controls.py: exits non-zero when FAILED
  [32m✓[0m test_gate_parity.py: exits non-zero when FAILED
  [32m✓[0m test_gate_refusals.py: exits non-zero when FAILED
  [32m✓[0m test_k_solicited.py: exits non-zero when FAILED
  [32m✓[0m test_no_blank_cells.py: exits non-zero when FAILED
  [32m✓[0m test_page_hash_fallback.py: exits non-zero when FAILED
  [32m✓[0m test_publication_gates.py: exits non-zero when FAILED
  [32m✓[0m test_qualification_round.py: exits non-zero when FAILED
  [32m✓[0m test_queue_cap.py: exits non-zero when FAILED
  [32m✓[0m test_quotation_checker.py: exits non-zero when FAILED
  [32m✓[0m test_reconcile.py: exits non-zero when FAILED
  [32m✓[0m test_responses_shim.py: exits non-zero when FAILED

every suite is reachable from the runner
  [32m✓[0m test_agenda_activation.py: run by run_all.py
  [32m✓[0m test_agenda_ratification.py: run by run_all.py
  [32m✓[0m test_amendment_disposition.py: run by run_all.py
  [32m✓[0m test_attempt_ledger.py: run by run_all.py
  [32m✓[0m test_capability_fits.py: run by run_all.py
  [32m✓[0m test_capture_gates.py: run by run_all.py
  [32m✓[0m test_capture_lifecycle.py: run by run_all.py
  [32m✓[0m test_chain_guards.py: run by run_all.py
  [32m✓[0m test_closed_world.py: run by run_all.py
  [32m✓[0m test_cohort_pages.py: run by run_all.py
  [32m✓[0m test_deploy_check.py: run by run_all.py
  [32m✓[0m test_deploy_obligations.py: run by run_all.py
  [32m✓[0m test_derive_counts.py: run by run_all.py
  [32m✓[0m test_disposition.py: run by run_all.py
  [32m✓[0m test_executive_log.py: run by run_all.py
  [32m✓[0m test_fault_injection.py: run by run_all.py
  [32m✓[0m test_fetch_executor.py: run by run_all.py
  [32m✓[0m test_gate_negative_controls.py: run by run_all.py
  [32m✓[0m test_gate_parity.py: run by run_all.py
  [32m✓[0m test_gate_refusals.py: run by run_all.py
  [32m✓[0m test_k_solicited.py: run by run_all.py
  [32m✓[0m test_no_blank_cells.py: run by run_all.py
  [32m✓[0m test_page_hash_fallback.py: run by run_all.py
  [32m✓[0m test_publication_gates.py: run by run_all.py
  [32m✓[0m test_qualification_round.py: run by run_all.py
  [32m✓[0m test_queue_cap.py: run by run_all.py
  [32m✓[0m test_quotation_checker.py: run by run_all.py
  [32m✓[0m test_reconcile.py: run by run_all.py
  [32m✓[0m test_responses_shim.py: run by run_all.py

87 passed, 0 failed

[1m▸ test_agenda_activation.py[0m

[1m▸ test_agenda_ratification.py[0m

[1m▸ test_amendment_disposition.py[0m

[1m▸ test_attempt_ledger.py[0m

[1m▸ test_capability_fits.py[0m

[1m▸ test_capture_gates.py[0m

[1m▸ test_capture_lifecycle.py[0m

[1m▸ test_chain_guards.py[0m

[1m▸ test_closed_world.py[0m

[1m▸ test_cohort_pages.py[0m

[1m▸ test_deploy_check.py[0m

[1m▸ test_deploy_obligations.py[0m

[1m▸ test_derive_counts.py[0m

[1m▸ test_disposition.py[0m

[1m▸ test_executive_log.py[0m

[1m▸ test_fault_injection.py[0m

[1m▸ test_fetch_executor.py[0m

[1m▸ test_gate_negative_controls.py[0m

[1m▸ test_gate_parity.py[0m

[1m▸ test_gate_refusals.py[0m

[1m▸ test_k_solicited.py[0m

[1m▸ test_no_blank_cells.py[0m

[1m▸ test_page_hash_fallback.py[0m

[1m▸ test_publication_gates.py[0m

[1m▸ test_qualification_round.py[0m

[1m▸ test_queue_cap.py[0m

[1m▸ test_quotation_checker.py[0m

[1m▸ test_reconcile.py[0m

[1m▸ test_responses_shim.py[0m

[1m▸ test_suite_hygiene.py[0m

[32mAll 30 suites passed.[0m

**Planning opportunity pass and C45 refactor**
exec
/bin/bash -lc "pgrep -af 'python3 tools/tests/run_all.py' || true
git status --short" in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
1024967 /bin/bash -c pgrep -af 'python3 tools/tests/run_all.py' || true git status --short
 M docs/artifacts/control-application.md
 M docs/control-application.html
 M record/claims/code-scan-dispositions.json
 M record/claims/dispositions.jsonl
 M record/controls/control-application.md
 M record/cycles/spend-ledger.json
 M record/executive/action-log.jsonl
 M tools/control_application.py
 M tools/control_coverage.py
 M tools/scan_own_code.py
?? tools/closed_world.py
?? tools/tests/test_closed_world.py

**Planning command rerun with output capture**
**Testing exit code capture via /dev/null redirection**
exec
/bin/bash -lc 'python3 tools/tests/run_all.py >/dev/null' in /home/reed/git/open-asi-governance-forum
**Executing unified command with stdin**
 succeeded in 51431ms:
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.
  [33m![0m DEPRECATED VOCABULARY: this is an NCP v0.1 document. It is still valid and always will be, but the profile was renamed on 2026-08-11 because 'negative control' reversed established laboratory terminology. New attestations should use FICP v0.2. See spec/ficp/MIGRATION.md.

**Inspecting generated HTML parity**
codex
The repository suite is green with its own direct exit status (`run_all.py` exit 0), but that does not clear the review: the new 25-case suite is green because its negative assertions target strings that are no longer the actual outputs being protected. I’ve finished the contract and call-site audit and am doing the final consistency/opportunity pass now.
exec
/bin/bash -lc 'python3 tools/control_application.py --check
git diff --check
python3 -m py_compile tools/closed_world.py tools/control_coverage.py tools/scan_own_code.py tools/tests/test_closed_world.py' in /home/reed/git/open-asi-governance-forum
 succeeded in 0ms:
  every control has a row (63); 0 of 36 code-governing controls are finished and tested.
  diagnostic — control_number_literal_present: 7 of 36 code rows name a test that contains the control's number as text. This measures typing, not testing.

**Identifying redundant parsing in scan_own_code**
**Highlighting need for single parse and unique status tracking**
**Designing callback orchestration with role tagging**
**Planning exact population tracking**
**Refining coverage report validations**
**Designing measurement inventory enforcement**
**Summarizing prioritized scan findings**
**Recommending deficiency filing before landing**
**Planning flexible nested event counts**
codex
Not ready to land. The policy choices are mostly right, but the implementation currently permits—and the routed tools actually publish—the partial counts it claims to withhold.

## Landing blockers

1. `Survey` does not enforce accounting.

[`closed_world.py`](/home/reed/git/open-asi-governance-forum/tools/closed_world.py:75) ignores the key passed to `seen()`, and [`result()`](/home/reed/git/open-asi-governance-forum/tools/closed_world.py:114) checks only unreadable/unregistered lists. I directly reproduced all of these as successful results:

- one artifact seen, none counted;
- 99 counted with zero artifacts seen;
- 99 counted from one artifact;
- an exclusion whose “ground” is ten spaces.

The type needs per-key terminal states: every declared population member must become exactly one of counted/accepted, excluded, unreadable, or unregistered; duplicates and outcomes for unseen keys must refuse. Aggregate bucket counts should remain separate because one artifact may legitimately contribute multiple events.

Ideally supply the expected population up front. Otherwise `result()` can also be called halfway through enumeration and cannot know the walk is unfinished.

2. Both integrations expose partial results before the guard.

[`scan_own_code.py`](/home/reed/git/open-asi-governance-forum/tools/scan_own_code.py:308) prints every hit count before calling `result()`. The comment saying those hits “are not a result” does not make them non-results. It also parses every file once per detector but records failures only during the first pass; a later-pass failure is silently omitted. Parse once, retain the trees, validate, then render only after `result()` succeeds.

[`control_coverage.py`](/home/reed/git/open-asi-governance-forum/tools/control_coverage.py:285) has two holes:

- `--check` returns success without consulting the survey.
- The normal path prints HAS/NONE/NOT_APPLICABLE/total before consulting it.

These are precisely the partial population counts being protected, even if the percentage itself is withheld.

The C5 row’s “there is no path to a number past an incomplete walk” claim therefore needs removal until these are repaired.

3. The integration assertions are vacuous relative to the new outputs.

[`test_closed_world.py`](/home/reed/git/open-asi-governance-forum/tools/tests/test_closed_world.py:126) checks only for the removed phrase `file(s) scanned` and the percentage phrase `% of tools`. It does not reject the actual leaked per-class counts or HAS/NONE/total table.

Also test:

- `seen()` without a terminal disposition;
- count without `seen()`;
- duplicate keys and duplicate dispositions;
- overcount/arbitrary `n`, including negative values;
- whitespace-only and placeholder exclusions;
- `control_coverage.py --check` with a broken test;
- a failure after `scan_own_code`’s first detector pass;
- exact absence of bucket labels/count records, rather than `"5" not in output`.

The suite and full `run_all.py` both exit 0, but these omissions explain the green result.

## Answers

1. Raising from `result()` is right, as is withholding partials from `report()`. Separating unregistered from unreadable is also right. The current object does not yet implement the claimed accounting contract, and its public mutable `counts` provide another direct path to partial numbers.

2. Including tests as dependencies is correct. Any test may establish coverage for any tool, so an unreadable test makes the NONE set and percentage unknown. Model them separately from the measured tool population—such as “validated dependency”—because they belong to the evidence closure, not the denominator. Blocking is preferable to routing around; the report should make that distinction explicit.

3. All three dispositions are semantically correct:

- `_api()`’s `None` becomes pending/unobserved at both call sites.
- `reviewed_by_custodian` is a known zero from a complete `Counter`.
- `detector` is likewise a known-zero bucket over every finding.

However, normalize the new disposition entries to the established `file`, `class`, `why`, and `read` shape. They currently use `path`, `note`, `by`, and `utc`, and nothing validates this artifact’s schema.

4. Leaving `derive_counts.py` unchanged was the right call for this batch. The new abstraction is not yet strong enough to replace the reference implementation. After fixing it, convergence is desirable, but only under retained C45 fixtures covering the historical 9-receipt result, unknown schemas, embedded specs, every disposition container, and non-solicitation exclusions.

5. Yes: the principal vacuities are the two output-string assertions and the `"5"` assertion. The suite proves unreadable/unregistered refusal, but not closed-world accounting.

6. C45 remains next after C5 is repaired and reviewed again. In fact, use C45 immediately for any later `derive_counts` migration.

The strongest follow-on opportunity is a registered measurement-producer inventory plus proof-carrying results: population specification/hash, schema-registry version, exclusions and grounds, dependency coverage, and counts bound together. That directly addresses manual routing; an independently registered population specification plus “new location/schema” metamorphic fixtures would also reduce—though not eliminate—the too-narrow-population residue.

These failures warrant a deficiency entry before landing under the repository’s own recording rule.
tokens used
105,537
